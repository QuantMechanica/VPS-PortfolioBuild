# Approved Strategy Card Reference

Canonical approved card:

`D:/QM/strategy_farm/artifacts/cards_approved/QM5_9571_williams-smash-day-failure-h4.md`

The card is approved for QM5_9571 with source ID
`6e967762-b26d-59a3-b076-35c17f2e7c36`. The local build registers only the
13 target symbols present in `framework/registry/dwx_symbol_matrix.csv`;
`FRA40.DWX` and `JP225.DWX` remain excluded because they are absent from the
local DWX matrix.

# Quality-Tech Audit — QUA-644 Review and Audit
# Date: 2026-05-08
# Agent: Quality-Tech (c1f90ba8-d637-46d9-8895-ead705bb4933)
# Scope: Technical counter-audit per QT charter — code quality, overfitting risk, pipeline fidelity, sequencing integrity

---

## Executive Summary (QT Perspective)

The company is **not stuck on smoke tests**. It is stuck on a concrete infra blocker that prevents Model 4 backtests from completing. The pipeline specification, sub-gate criteria, and goal definition are all correct and sufficiently SMART. The root cause of stalled progress is engineering, not strategy definition.

**The single highest-leverage fix**: close QUA-747 (METATESTER_HUNG on long Model 4 runs). Everything downstream is gated on that.

---

## 1. Code Audit — QM5_1003 (davey_baseline_3bar, DL-036 Checklist)

**Verdict: PASS**

| Criterion | Status | Evidence |
|---|---|---|
| No hardcoded rule thresholds | PASS | `ssl1`, `ssl_usd_cap`, `strategy_atr_period` all input-parameterized (lines 27–30) |
| No look-ahead | PASS | Signal uses `iClose(_Symbol, _Period, 1/2/3)` — closed bars only |
| No repaint | PASS | Bar-close signals on shifts 1–3; no current-bar access in signal logic |
| Magic number via framework | PASS | `QM_FrameworkMagic()` resolver used; ea_id=1003 registered in `framework/registry/magic_numbers.csv` |
| RISK_FIXED + RISK_PERCENT both present | PASS | Lines 13–14; default RISK_PERCENT=0 (backtest), RISK_FIXED=1000 |
| 4-module modularity | PASS | No-Trade gates (KillSwitch, News, Friday at lines 248–253), Entry (Strategy_EntrySignal), Management (Strategy_ManageOpenPosition), Close (Strategy_ExitSignal) |
| Friday close default = true | PASS | `qm_friday_close_enabled = true` default |
| No ML imports | PASS | Only QM_Common.mqh + Trade/Trade.mqh |
| Slippage/spread modelling | N/A — P2 only; P5 stress run required for slippage audit |

**Observation**: `Strategy_ManageOpenPosition` recalculates stop from current ATR (shifted 1) applied to historical `price_open`. This is a trailing-stop update, not lookahead. No concern.

---

## 2. Code Audit — QM5_SRC04_S03_lien_fade_double_zeros

**Verdict: DEFERRED (CTO already reviewed)**

CTO DL-036 review PASS already recorded: `QM5_SRC04_S03_lien_fade_double_zeros/QM-00011_CTO_REVIEW_PASS_2026-05-05.md`. EA subsequently received KILL_AT_P2 on strategy quality (variant1..5 all MIN_TRADES_NOT_MET). QT notes the kill was correct — a strategy that cannot meet T≥200 on any variant across the full symbol set has no viable P2 PASS path.

**Registry hygiene issue**: `framework/registry/ea_id_registry.csv` still shows `ea_id=1009` as `status=active`. It should be updated to `killed` after CEO governance closure. **Action owner: Development + CEO.**

---

## 3. Pipeline Sequencing — CRITICAL FINDING

### 3a. METATESTER_HUNG is the systemic blocker

The P2 report.csv for QM5_1003 (`D:/QM/reports/pipeline/QM5_1003/P2/report.csv`) shows:
- Row 1: `INVALID` (phantom pass marker, preserved per DL-054/QUA-662)
- All real reruns: `FAIL` with `run_smoke_fail:REPORT_MISSING;METATESTER_HUNG;INCOMPLETE_RUNS`
- Some rows: `FAIL` with `run_smoke_fail:MIN_TRADES_NOT_MET` (different issue — these ran but trade count < threshold)
- Several rows: `INVALID` with `no_summary_json:rc=0`

First surfaced: 2026-05-01. QUA-747 opened.
Status as of 2026-05-08: **still unresolved** — P3 runs today at 06:24Z and 07:06Z both fail with `run_smoke.summary=` (empty), which is the same REPORT_MISSING signature.

This is not a strategy weakness. This is infrastructure. **QUA-747 is the critical-path item.**

### 3b. P3 ran before P2 gate cleared — SEQUENCING VIOLATION

P3 dispatch ran for QM5_1003 today (2026-05-08T06:24Z and 07:06Z). Results: 25 FAILs, 0 PASSes. All FAILs have `run_smoke.summary=` blank — infrastructure failure, not strategy failure.

**P3 should not run until P2 produces ≥1 real PASS cell.** The pipeline dispatcher is not enforcing P2 gate status as a pre-condition for P3 launch. This burns terminal time on provably-useless runs and contaminates the P3 report with infra-fail rows.

**Action owner: Pipeline-Operator / CTO — add P2 gate pre-check to P3 dispatcher.**

### 3c. P3.5 AUTO_PASS is non-load-bearing (already acknowledged)

P3.5 AUTO_PASS issued 2026-05-01T09:48:57Z was computed from the phantom P2 PASS set (36/36 phantom). DL-059 already records this as "not load-bearing." P3.5 must be re-run after a clean P2. No new finding, confirming DL-059 is correct.

---

## 4. QM5_1017 (chan_pairs_stat_arb) — Scaffold Not P2-Ready

`Strategy_EntrySignal()` returns `false` unconditionally per the zero_trade ADR filing:
> "Two-leg synchronized execution/management is intentionally not wired in this scaffold revision."

45+ ADR files generated (one per symbol). While each file is DL-054 Gate 4 compliant, 45 files for a single underlying condition (unimplemented entry signal) is process overhead — one master ADR covering all symbols of this scaffold stage would be sufficient. More importantly: **this EA cannot produce any trades at P1/P2 until Development wires the entry signal.**

**Action owner: Development** — implement the entry/exit path before this EA re-enters P0.

---

## 5. Overfitting Risk Assessment

**No P3 sweep data exists with real results.** All 25 P3 runs for QM5_1003 failed at the infra level (empty summary). There is no parameter landscape to analyze. QT cannot assess overfitting risk until infrastructure is fixed and real sweep data lands.

**Deferred**: overfitting analysis, walk-forward fidelity check (P4), MC + parameter sensitivity (P6), statistical validation (P7) — all waiting on a real P2 PASS row.

---

## 6. Sub-Gate Spec Calibration Status

Per `PIPELINE_V5_SUB_GATE_SPEC.md § Recalibration Triggers`:
- Trigger 1: First V5 EA reaches P5b → not yet
- Trigger 2: First V5 EA reaches P6 → not yet
- Trigger 3: First V5 EA reaches P7 → not yet

All defaults remain provisional but binding. No recalibration action required until first EA clears P5b. Open items 1–6 from the spec remain open.

---

## 7. Answer to "Why So Many Agents Stuck"

From QT perspective, three compounding causes:

1. **Anthropic org monthly cap hit (~2026-05-08)** — all claude_local adapters simultaneously flipped to error/paused. OWNER top-up was needed. Unrelated to pipeline design.

2. **METATESTER_HUNG (QUA-747)** — prevents any real P2 verdict. This blocks Pipeline-Operator, CTO, CEO, and QT from completing their respective gate reviews. One infra bug cascades to full company standstill.

3. **P2 dispatchment before toolchain stability** — the phantom-pass debacle (QUA-662) and re-dispatch on broken infra consumed >7 days of pipeline time without advancing strategy quality.

---

## 8. QT Technical Cross-Challenge on Pipeline PASS Candidates

**No PASS candidate exists at any gate** as of 2026-05-08. QT has no PASS/FAIL/AGREE/DISAGREE to issue. No EA has cleared P2 with real (non-infra-fail) results. When the first real P2 PASS row lands, QT will review per DL-036 + PIPELINE_V5_SUB_GATE_SPEC.md.

---

## Action Summary (QT-Originated)

| Priority | Item | Owner | Blocking |
|---|---|---|---|
| CRITICAL | Close QUA-747: fix METATESTER_HUNG on Model 4 long runs | CTO / Pipeline-Operator | Everything downstream |
| HIGH | Stop P3 dispatch until ≥1 real P2 PASS row exists | Pipeline-Operator | P3 integrity |
| HIGH | Update ea_id_registry.csv: ea_id=1009 → status=killed | Development (after CEO closure) | Registry hygiene |
| MEDIUM | QM5_1017 entry signal implementation | Development | P1/P2 for stat-arb strategy |
| LOW | Consolidate 45 zero_trade ADRs into one master ADR per scaffold | Development / Pipeline-Operator | Process overhead only |

---

## QT Disposition on QUA-644

The CEO's audit (per last run summary) was correct in identifying that the company lacked a SMART-framed delivery goal. From QT's angle: the sub-gate spec IS sufficiently SMART (specific thresholds, measurement criteria, timeline via DL-050 perpetual cadence). The gap is not specification — it is infra execution.

QT recommends OWNER accept the existing sub-gate spec as the SMART goal baseline and redirect the QUA-647/QUA-648 child work toward QUA-747 closure. QT contribution to this audit is complete.

---
*Evidence anchors*:
- `D:/QM/reports/pipeline/QM5_1003/P2/report.csv` — P2 FAIL matrix
- `D:/QM/reports/pipeline/QM5_1003/P3/report.csv` — P3 FAIL matrix (today)
- `D:/QM/reports/pipeline/QM5_1003/P3/p3_live_20260508_070623.out.log` — P3 run log
- `D:/QM/reports/pipeline/QM5_1003/P3_5/P3_5_QM5_1003_result.json` — phantom-based AUTO_PASS (non-load-bearing)
- `framework/EAs/QM5_SRC04_S03_lien_fade_double_zeros/QUA-743_CTO_DECISION_PACKET_2026-05-06_KILL_AT_P2.md`
- `framework/EAs/QM5_1003_davey_baseline_3bar/QM5_1003_davey_baseline_3bar.mq5`
- `docs/ops/PIPELINE_V5_SUB_GATE_SPEC.md`
- `decisions/2026-05-01_phase3_perpetual_cadence.md` (DL-050)
- `decisions/2026-05-05_qm5_1003_baseline.md` (DL-059)

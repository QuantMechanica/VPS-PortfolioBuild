# QM5_41364 XTI/XNG Weekly Close-Location Divergence Reversion - G0

Date: 2026-09-06

Decision: `APPROVED` for the exact execution contract in
`strategy-seeds/cards/approved/QM5_41364_xtixng-wclv-div-rv_card.md`.

Authority: current explicit OWNER commodity/energy sleeve mission plus the
durable pre-extraction source approval at commit `9170cf4d10`.

## Gate Result

- R1 `PASS_WITH_RULE_TRANSLATION_RISK`: complete government and peer-reviewed
  oil/gas evidence supports the carrier, while weak and time-varying linkage
  and the untested CLV rule are explicit.
- R2 `PASS`: exact symbols, clock, synchronized completed-week OHLC, independent
  CLVs, strict terciles, sides, attempt, risk, stops, atomicity, and exit are
  deterministic.
- R3 `PASS_WITH_SYNCHRONIZATION_AND_CONTINUOUS_CFD_BASIS_RISK`: registered
  XTIUSD.DWX and XNGUSD.DWX D1 data plus native MT5 state supply all inputs.
- R4 `PASS`: timestamps, OHLC arithmetic, ATR risk, quotes, positions, deals,
  and persistent state only; no banned signal, trained logic, or external feed.

## Identity And Allocation

The canonical pre-allocation receipt is
`artifacts/xtixng_wclv_div_preallocation_dedup_20260906.json`. It found no
exact duplicate and one expected same-mechanic precious-metals sibling. Manual
review resolves the fuzzy match because this candidate owns the XTI/XNG
physical carrier, contract metadata, cost surface, and realized energy return
stream. Existing XTI/XNG rules do not use independent completed-week auction
locations. This is not a realized-correlation ruling.

The atomic allocator reserved `QM5_41364` for slug
`xtixng-wclv-div-rv` and strategy ID
`AI-CODEX-XTIXNG-WCLVDIV-RV-20260906_S01`.

## Authorized Next Action

Create the EA directory before magic allocation, allocate active slots 0/1,
regenerate and verify the resolver, build the exact approved card, create only
fixed-risk backtest presets and the logical basket manifest, run reference and
strict Q01 validation, then enqueue one logical Q02 only if the fresh CPU
admission check is below the ceiling.

This approval does not authorize manual backtests, terminal control, live or
demo artifacts, portfolio-gate changes, portfolio admission, correlation
waivers, deploy/live manifests, `T_Live`, AutoTrading, or live use.

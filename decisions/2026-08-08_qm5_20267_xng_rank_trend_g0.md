# QM5_20267 XNG Rank-Trend G0 Authorization

Date: 2026-08-08

Authority: OWNER commodity/energy portfolio mission delivered to Codex on the
`agents/board-advisor` branch.

## Decision

Authorize one bounded V5 Strategy Card and non-live build for
`QM5_20267_xng-rank-trend`. On the first D1 bar of each genuine broker-month
transition, the candidate reconstructs thirteen consecutive completed natural-
gas month-end closes, calculates the no-tie Mann-Kendall pairwise-order score,
and trades the score direction only when `abs(S) >= 28`. A tied, weak,
malformed, or unavailable path consumes the month without entry.

The candidate may proceed through source/card lint, deterministic registry and
magic allocation, resolver regeneration, strict compile, one `RISK_FIXED`
backtest setfile, Q01 validation, and one paced Q02 enqueue. This authorization
does not pre-approve efficacy, diversification, decorrelation, certification,
execution-contract promotion, or portfolio admission.

## Source Boundary

The approved source of record is
`strategy-seeds/sources/MOP-XNG-RANKTREND-2026/source.md`, bounded by the
completely reviewed repository packet
`strategy-seeds/sources/MOP-TSMOM-2012/source.md`:

- Moskowitz, Tobias J.; Ooi, Yao Hua; and Pedersen, Lasse Heje (2012),
  "Time Series Momentum," *Journal of Financial Economics* 104(2), 228-250,
  DOI `10.1016/j.jfineco.2011.11.003`.

The governed parent records a complete read of the 23-page published paper,
including own-return continuation over monthly lags one through twelve, a
monthly strategy clock, and natural gas in the commodity-futures universe. The
paper does not use a Mann-Kendall score or the fixed critical boundary below.
Those are transparent QM mechanizations of trend direction and path
monotonicity, not source claims. No source return, alpha, Sharpe ratio,
drawdown, trade count, cost, CFD equivalence, or portfolio correlation
transfers.

## Locked Rule

At the start of broker month `t`, take the thirteen consecutive completed
month-end closes `P_0..P_12` from months `t-13..t-1`, ordered oldest to newest.
Require every close to be positive and finite and require no exact tie between
any pair. For all `0 <= i < j <= 12`, define:

```text
pair(i,j) = +1 when P_j > P_i
            -1 when P_j < P_i
S         = sum(pair(i,j)) across all 78 pairs
tau       = S / 78
```

Require `abs(S) >= 28`. Buy XNG when `S > 0`; sell XNG when `S < 0`;
otherwise remain flat. With thirteen no-tie observations,
`Var(S) = 13*12*31/18 = 268.666...`; the continuity-corrected statistic at
`abs(S)=28` is `(28-1)/sqrt(Var(S)) = 1.647...`, fixing the conventional
two-sided ten-percent boundary before any pipeline result. Close before the
next monthly decision and do not retry within a consumed month.

## Non-Duplicate Decision

The pre-allocation review found no `xng-rank-trend` slug,
`MOP-TSMOM-2012_XNG_MK12_S17` strategy identity, or XNG Mann-Kendall/all-pairs
rank rule in the EA registry, intake cards, approved cards, decisions, or
source packets. The expected same-source hit is
`QM5_20264_wti-rank-trend`, whose locked statistic runs on WTI rather than XNG.
This mission preserves its parameters exactly; it does not fit a carrier-
specific threshold.

Manual review distinguishes the closest XNG systems:

- `QM5_20262_xng-lr-trend` fits log-price OLS slope and gates on `R^2`; its
  return magnitudes and squared residual dispersion are load-bearing.
- `QM5_20259_xng-mom-vote` combines one-, three-, and twelve-month cumulative
  return signs.
- `QM5_13116_xng-signmom` counts the signs of adjacent monthly returns.
- `QM5_12804_xng-tsmom12m-atr` uses one endpoint return and an ATR/price
  participation corridor.
- `QM5_12567_cum-rsi2-commodity` is a long-only daily RSI(2) pullback aligned
  to a slow trend with a five-bar lifecycle.

None compares every older/newer month-end pair while discarding return
magnitude. The XNG carrier, thirteen consecutive endpoints, all 78
comparisons, no-tie fail-closed rule, signed score, fixed `abs(S) >= 28`
boundary, consumed monthly attempt, and one-month renewal are jointly
load-bearing. Verdict:
`CLEAN_XNG_IDENTITY_WITH_LOCKED_CROSS_CARRIER_TEMPLATE`.

## Allocation And Kill Boundary

- intended EA ID: `QM5_20267`, subject to deterministic registry allocation;
- slug: `xng-rank-trend`;
- strategy ID: `MOP-TSMOM-2012_XNG_MK12_S17`;
- intended slot 0: `XNGUSD.DWX` / magic `202670000`;
- cadence: at most one XNG position per broker month after thirteen completed
  month ends; Q02 owns realized density and economics;
- retire below five completed trades per full post-warm-up year;
- fail on wrong endpoint order, nonconsecutive months, an accepted tie, wrong
  pair score, entry below the fixed boundary, wrong direction, repeated
  monthly attempt, invalid risk mode, missing hard stop, or nondeterminism;
- no post-result endpoint count, score threshold, direction, stop, spread,
  retry, or carrier rescue is authorized.

## Safety Boundary

This authorization excludes manual backtests; live, demo, shadow, stress, and
optimization setfiles; `T_Live`; AutoTrading; deploy or T_Live manifests;
portfolio admission; portfolio-gate edits; and correlation waivers. Q02 uses
exactly one `XNGUSD.DWX` D1 setfile with `RISK_FIXED=1000`,
`RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1`.

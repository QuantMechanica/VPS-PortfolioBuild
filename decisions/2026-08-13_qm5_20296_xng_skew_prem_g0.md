# QM5_20296 XNG Pearson-Skewness Premium G0 Authorization

Date: 2026-08-13

Authority: OWNER commodity/energy portfolio mission delivered to Codex on the
`agents/board-advisor` branch.

## Decision

Authorize one bounded V5 Strategy Card and non-live build for
`QM5_20296_xng-skew-prem`. At each genuine broker-month transition, the
candidate reconstructs the twelve complete preceding broker months from
completed `XNGUSD.DWX` D1 closes, computes Pearson's population moment
coefficient of daily log-return skewness, buys natural gas when skewness is
negative, sells it when skewness is positive, and consumes the month flat
inside a `1e-12` zero tolerance or on invalid state.

The candidate may proceed through bounded source/card extraction, schema and
G0 lint, deterministic registry and magic allocation, resolver regeneration,
strict compile, one `RISK_FIXED` backtest setfile, Q01 validation, and one
paced Q02 enqueue. Authorization does not pre-approve efficacy,
diversification, decorrelation, certification, execution-contract promotion,
or portfolio admission.

## Source Boundary

The approved source is Fernandez-Perez, Frijns, Fuertes, and Miffre (2018),
"The Skewness of Commodity Futures Returns," *Journal of Banking & Finance*
86, 143-158, DOI `10.1016/j.jbankfin.2017.06.015`. The governed complete-read
packet is `strategy-seeds/sources/FERNANDEZ-SKEW-2018/source.md`, bound by
SHA-256
`D9C9BDD383956A0190490E4977CAC5D247E9B250342B88577BF7439019C893F7`.
It records review of the complete 44-page accepted manuscript and identifies
natural gas as an explicit member of the source's commodity universe.

The source defines prior-twelve-month Pearson return skewness, monthly sorts,
and a low-skew-minus-high-skew direction. It tests a diversified futures
cross-section, not an absolute natural-gas timing threshold. The zero pivot,
single continuous-CFD carrier, broker-calendar reconstruction, fixed-dollar
sizing, ATR hard stop, spread ceiling, restart ledger, and lifecycle controls
are transparent QM mechanizations. No source return, alpha, natural-gas-only
result, cost, trade density, CFD equivalence, correlation, or portfolio
conclusion transfers.

## Locked Rule

On the first processed `XNGUSD.DWX` D1 bar after a genuine broker-month
transition, define a half-open formation interval covering the twelve complete
broker months immediately before the decision month. Include a log return only
when both adjacent completed-bar timestamps lie inside that interval:

```text
r[d] = ln(close[d] / close[d-1])
mu   = sum(r[d]) / n
m2   = sum((r[d] - mu)^2) / n
m3   = sum((r[d] - mu)^3) / n
skew = m3 / (m2^(3/2))

BUY  when skew < -1e-12
SELL when skew > +1e-12
FLAT otherwise or when state is invalid
```

Require every expected month key to contribute at least one return, `180 <= n
<= 280`, strictly increasing timestamps, positive finite closes, finite
returns and moments, and `m2 > 1e-12`. Do not substitute simple returns,
sample-moment bias correction, annualization, winsorization, a fitted pivot,
rank, RSI, trend, magnitude-scaled risk, or an external benchmark.

Consume and persist the broker month before history, signal, spread, quote,
news, ATR, sizing, or order gates. Use one `RISK_FIXED=1000` XNG position with
a frozen `3.5 * ATR(20,D1)` broker hard stop, a `2500`-point entry-spread cap,
and no take-profit. Close before monthly replacement or after forty calendar
days. Friday close and both news axes are OFF. At most one registered position
exists.

## Reputable-Source Criteria

- R1: PASS. Peer-reviewed *Journal of Banking & Finance* paper, DOI,
  institutional accepted manuscript, complete-read governed packet, and
  explicit natural-gas membership.
- R2: PASS. Exact twelve-month interval, contained-return inclusion,
  population moments, fixed zero pivot, direction, monthly attempt, stop,
  rollover, and stale-exit contracts.
- R3: PASS. Registered `XNGUSD.DWX` D1 history spans 2017-2026 across T1-T10;
  native MT5 time, close, ATR, quote, spread, and position state supply every
  runtime input.
- R4: PASS. Deterministic native arithmetic only, without trained output,
  prohibited signal indicator, external runtime feed, grid, martingale,
  scale-in, or pyramiding.

## Non-Duplicate Decision

The canonical pre-allocation checker scanned 4,361 EA-registry rows and 472
root cards. It found no exact identity and surfaced three expected same-paper
fuzzy neighbors. Manual mechanic review resolves them:

- `QM5_13118_energy-skew-rank` computes separate XTI and XNG skewness values,
  ranks the two legs, and maintains an opposite-side two-leg package. This
  candidate computes one absolute XNG state around zero and has no rank,
  second leg, shared-risk split, or orphan repair.
- `QM5_20233_xauxag-skew-rank` is the analogous two-leg precious-metal rank,
  with neither an outright XNG carrier nor an absolute zero pivot.
- `QM5_20290_wti-skew-prem` preserves the estimator and direction on WTI.
  This card is an explicitly authorized carrier extension to natural gas,
  with a distinct registered symbol, magic, contract economics, and spread
  guard. It is not a parameter rescue or evidence inheritance; its Q02 result
  stands alone.
- `QM5_12567_cum-rsi2-commodity` is a short-horizon, long-only oscillator
  pullback above a slow filter. It has no return-moment estimator, symmetric
  direction, monthly clock, or twelve-month state.

The XNG carrier, twelve complete months, boundary-contained log returns,
population Pearson skewness, fixed zero pivot, low-skew long/high-skew short
map, and consumed monthly lifecycle are jointly load-bearing. Verdict:
`CLEAN_AUTHORIZED_XNG_CARRIER_AFTER_MANUAL_REVIEW`.

## Risk And Kill Boundary

This is a low-prior carrier falsification. Natural gas has jump, weather,
seasonal, roll, financing, and continuous-CFD basis risks; a single-symbol
zero-pivot rule can remain directionally exposed for long regimes. Q02 must
retire it on zero trades, fewer than five completed positions per full
post-warm-up year, or nonpositive governed economics. Downstream gates alone
own robustness and realized correlation. No failure may be rescued by changing
the formation, estimator, pivot, direction, carrier, stop, hold, spread, or
retry contract.

## Safety Boundary

Research, deterministic allocation, build, strict compile/Q01, one fixed-risk
backtest set, and one paced non-live Q02 enqueue only. No manual backtest,
live/demo/shadow/stress/optimization set, dispatch tick, `T_Live` access,
AutoTrading change, deploy manifest, T_Live manifest, portfolio-gate edit,
portfolio admission, or correlation waiver is authorized.

# QM5_21526 XAU/XAG Annual CADF Residual Reversion — G0 Decision

Date: 2026-08-15

Decision: `APPROVED` for one bounded V5 Strategy Card, one branch-only
non-live build, strict Q01 validation, and one paced non-live Q02 enqueue.
This decision does not authorize a manual tester dispatch.

Authority: OWNER commodity/energy portfolio mission delivered to Codex on
the `agents/board-advisor` branch and durably recorded before extraction in
`decisions/2026-08-15_xau_xag_cadf_source_approval.md` at commit
`622acfccc`.

## Candidate

- EA: `QM5_21526_xau-xag-cadf`
- Strategy ID: `CHAN-SCHWEIKERT-XAUXAG-CADF-2026_S01`
- Source ID: `CHAN-SCHWEIKERT-XAUXAG-CADF-2026`
- host/traded symbol/slot/magic: `XAUUSD.DWX` / 0 / `215260000`
- companion/traded symbol/slot/magic: `XAGUSD.DWX` / 1 / `215260001`
- logical basket: `QM5_21526_XAU_XAG_CADF_D1`
- driver: fresh standardized crossing of one annually frozen log-gold-on-log-
  silver OLS residual admitted by the governed one-lag 5% CADF and fitted
  half-life gates
- lifecycle: opposite paired fade, beta-weighted fixed stop-risk shares,
  frozen per-leg `3.5 * ATR(20,D1)` stops, convergence/fitted-time/model exit,
  atomic partial-open repair, and fixed spread caps

## Source Decision

The approved composite packet is
`strategy-seeds/sources/CHAN-SCHWEIKERT-XAUXAG-CADF-2026/source.md`. It is
bound to the complete CEO-ratified Chan cointegration-family extraction, the
governed peer-reviewed gold/silver packet, and CME carrier context.

Chan supplies the mechanical OLS/CADF/frozen-training spread method and
distinguishes cointegration from correlation. His worked pair is GLD/GDX, not
spot gold/silver. The gold/silver papers support only a state-dependent
long-run relation, and CME supports only the carrier. The two-CFD annual model
is a QM falsification. No source efficacy, coefficient, density, cost,
neutrality, decorrelation, or portfolio result transfers.

## Locked Rule

For each broker calendar year:

1. Reconstruct the first host D1 bar of that year and load exactly 252
   synchronized completed XAU and XAG observations strictly before it.
2. Fit `log(XAU)=alpha+beta*log(XAG)+residual` by closed-form OLS with an
   intercept. Require beta in `[0.10,3.00]` and finite nonsingular arithmetic.
3. Run the locked residual-change regression with an intercept and one lagged
   residual difference. Require the residual-level t-statistic
   `<= -3.343`.
4. Fit the demeaned OU adjustment. Require negative adjustment and half-life
   in `[2,30]` completed D1 observations.
5. Freeze alpha, beta, residual mean and sample standard deviation, CADF
   statistic, and half-life for the entire year. Midyear restart must rebuild
   the same anchor; intrayear sliding is forbidden.
6. A fresh z-score cross above `+1.0` opens SELL XAU / BUY XAG; below `-1.0`
   opens BUY XAU / SELL XAG. Persist the completed signal timestamp before
   fallible gates and never retry that signal.
7. Split one aggregate `RISK_FIXED=1000` budget by normalized relative weights
   `1.0` and `abs(beta)`. Attach frozen `3.5 * ATR(20,D1)` hard stops and
   enforce 1,500-point spread caps on both legs.
8. Open XAU then XAG and immediately flatten all owned exposure after an order
   failure or invalid final package.
9. Close on `abs(z)<=0.5`, invalid synchronized state/package, broker-year
   rollover, or `ceil(fitted_half_life)` calendar days. Friday close and all
   news modes are OFF.

The carrier, annual anchor, log orientation, observation count, OLS intercept,
one-lag CADF form and boundary, fitted half-life, fresh crossing, direction,
aggregate risk, and atomic lifecycle are locked.

## Reputable-Source Criteria

- R1 `PASS`: complete CEO-ratified Wiley extraction plus governed
  peer-reviewed gold/silver and CME carrier evidence.
- R2 `PASS`: anchor, estimators, statistical gates, direction, risk, stops,
  spreads, attempt state, and lifecycle are fixed and reproducible.
- R3 `PASS`: registered XAU/XAG D1 routes supply every runtime input; Q02 must
  prove synchronized history, stationarity, fills, and density.
- R4 `PASS`: deterministic native arithmetic only, without trained output,
  prohibited signal indicator, external feed, grid, martingale, scale-in, or
  pyramid.

## Non-Duplicate Decision

The canonical checker scanned 4,398 registry rows and 534 root cards, found no
exact identity, and returned one expected CADF-family fuzzy match. Manual
review separates same-pair daily rolling OLS without CADF/half-life
(`QM5_20161`), fixed-ratio and conditional-quantile gold/silver baskets
(`QM5_12577`, `QM5_13205`), the AUDUSD/NZDUSD Chan implementation
(`QM5_1017`), and the rolling WTI/copper CADF carrier (`QM5_21525`). None
combines this precious-metals carrier with one pre-year frozen model, the
governed one-lag 5% boundary, fitted half-life, fresh crossing, and consumed
signal lifecycle. Verdict:
`CLEAN_XAU_XAG_ANNUAL_CADF_HALFLIFE_RESIDUAL_REVERSION_AFTER_FAMILY_REVIEW`.

## Allocation And Kill Boundary

The deterministic allocator reserved `QM5_21526`. Expected cadence is eight
to twenty completed packages per full post-warm-up year; Q02 must retire below
five/year or on nonpositive governed economics. Q09 alone may establish
realized book correlation. Opposite sides and beta-weighted stop risk do not
prove neutrality.

## Safety Boundary

Create exactly one logical-basket D1 backtest setfile with
`RISK_FIXED=1000`, `RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1`. This decision
excludes manual backtests; live, demo, shadow, stress, and optimization
setfiles; `T_Live`; AutoTrading; deploy or T_Live manifests; portfolio-gate
edits; portfolio admission; and correlation waivers. Enqueue once, but do not
dispatch or control a tester when the factory resource ceiling is binding.

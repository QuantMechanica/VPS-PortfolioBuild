# QM5_20256 WTI R6-q7 Memory-Enhanced Momentum G0 Authorization

Date: 2026-08-07

Authority: OWNER commodity/energy portfolio mission delivered to Codex on the
`agents/board-advisor` branch.

## Decision

Authorize one bounded V5 Strategy Card and non-live build for
`QM5_20256_wti-vr6-mom`. The candidate is the source-declared WTI `R6-q7`
memory-enhanced momentum configuration: at each broker-month transition it
combines the sign of the latest completed six-month WTI return with a
heteroskedasticity-robust `q=7` variance-ratio state estimated from the latest
32 completed monthly WTI returns. Significant persistence follows the
six-month return, significant anti-persistence reverses it, and an
insignificant state remains flat.

The candidate may proceed through source/card lint, deterministic registry and
magic allocation, resolver regeneration, strict compile, one `RISK_FIXED`
backtest setfile, Q01 validation, and one paced Q02 enqueue. This authorization
does not pre-approve efficacy, diversification, decorrelation, certification,
execution-contract promotion, or portfolio admission.

## Source Boundary

The approved source of record will be
`strategy-seeds/sources/MEHLITZ-AUER-WTI-R6Q7-2026/source.md`, bounded by the
completely reviewed repository packet
`strategy-seeds/sources/MEHLITZ-AUER-MEM-2024/source.md`:

- Mehlitz, Julia S., and Benjamin R. Auer (2024), "Memory-enhanced momentum in
  commodity futures markets," *The European Journal of Finance* 30(8),
  773-802, DOI `10.1080/1351847X.2023.2220118`; and
- the complete open precursor, Chapter 3 and Appendix C of Mehlitz (2021),
  *Risk and return of passive and active commodity futures strategies*.

The governed packet records a complete read of the data, methodology, results,
robustness, conclusion, and appendix. The source explicitly includes WTI,
links ranking periods `R={1,3,6,12}` to variance-ratio orders
`q={2,4,7,13}`, uses 32 monthly observations and a two-sided 10% significance
boundary, and defines the persistence-follow / anti-persistence-reverse
matrix. The selected `R6-q7` configuration is source-declared, not an
optimized parameter search.

The source does not test a standalone Darwinex continuous CFD, D1-derived
broker month ends, fixed cash risk, ATR stops, spread caps, restart state, or
the QM portfolio. No source PF, return, Sharpe ratio, drawdown, WTI constituent
result, trade count, or correlation statistic transfers.

## Non-Duplicate Decision

The pre-allocation checker scanned 4,313 registry rows and 430 canonical cards.
It found no exact slug or strategy-ID collision and returned the two expected
source-family fuzzy neighbors. Manual review resolves them and the relevant
plain-trend systems:

- `QM5_13134_energy-vr-mom` is the source's `R1-q2` state: one-month return,
  one autocorrelation lag, and one robust variance term.
- `QM5_20253_wti-vr3-mom` is `R3-q4`: three-month return, three
  autocorrelation lags, and weights `1.5/1.0/0.5`.
- This candidate is `R6-q7`: six-month return, six autocorrelation lags,
  variance-ratio weights `12/7,10/7,8/7,6/7,4/7,2/7`, and their squared
  robust-variance weights.
- `QM5_20059_wti-tsmom6m` follows a plain six-month return sign every month;
  it has no serial-dependence estimator, significance gate, or
  anti-persistence reversal.
- `QM5_20056_wti-dual-mom` requires two cumulative trend horizons to agree;
  it does not estimate return memory.
- Calendar, event, breakout, ratio, oscillator, carry, and inventory WTI EAs
  use different state variables and lifecycles.

The six-month ranking interval, `q=7` lag set, fixed Lo-MacKinlay weights,
robust significance test, four-state direction matrix, and monthly attempt
clock are jointly load-bearing. Changing only a threshold or relabeling an
existing parameter set is not authorized. Verdict:
`CLEAN_AFTER_EXPECTED_FUZZY_AND_MANUAL_REVIEW`.

## Allocation And Kill Boundary

- intended EA ID: `QM5_20256`, subject to deterministic registry allocation;
- slug: `wti-vr6-mom`;
- strategy ID: `MEHLITZ-AUER-MEM-2024_XTI_R6Q7_S03`;
- intended slot 0: `XTIUSD.DWX` / magic `202560000`;
- cadence: at most one completed package per broker month after a 33-month
  warm-up; Q02 owns the realized density verdict;
- retire below five completed packages per full post-warm-up year;
- retire on nonconsecutive month ends, wrong return orientation, wrong `q=7`
  lag or weight set, entry without a significant memory state, wrong
  continuation/reversal direction, repeated monthly attempt, invalid risk
  mode, or nonpositive governed economics;
- no post-result horizon, lag, significance, stop, spread, retry, direction,
  or carrier rescue is authorized.

## Safety Boundary

This authorization excludes manual backtests; live, demo, shadow, stress, and
optimization setfiles; `T_Live`; AutoTrading; deploy or T_Live manifests;
portfolio admission; portfolio-gate edits; and correlation waivers. Q02 uses
exactly `RISK_FIXED=1000`, `RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1`.

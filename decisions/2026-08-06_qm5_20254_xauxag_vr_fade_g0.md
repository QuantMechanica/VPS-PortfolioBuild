# QM5_20254 XAU/XAG Anti-Persistent Ratio Fade G0 Authorization

Date: 2026-08-06

Authority: OWNER commodity/energy portfolio mission delivered to Codex on the
`agents/board-advisor` branch.

## Decision

Authorize one bounded V5 Strategy Card and non-live build for
`QM5_20254_xauxag-vr-fade`. The candidate is an opposite-leg gold/silver
relative-value package. It may fade a completed-D1 log-price-ratio displacement
only while the latest 32 synchronized completed monthly gold-minus-silver log
returns exhibit significant `q=2` heteroskedasticity-robust anti-persistence.
The anti-persistence state is an entry gate, the signed ratio displacement
determines direction, and completed-D1 ratio convergence determines exit.

The candidate may proceed through card lint, directory-first registry and magic
allocation, resolver regeneration, strict compile, one logical-basket
`RISK_FIXED` backtest setfile, Q01 validation, and one paced Q02 enqueue. This
authorization does not pre-approve efficacy, neutrality, diversification,
decorrelation, certification, execution-contract promotion, or portfolio
admission.

## Source Boundary

The approved source of record will be
`strategy-seeds/sources/CME-MEHLITZ-XAUXAG-VRFADE-2026/source.md`, bounded by
three completely reviewed repository packets:

- `strategy-seeds/sources/MEHLITZ-AUER-MEM-2024/source.md`, covering Mehlitz
  and Auer (2024), *The European Journal of Finance* 30(8), 773-802, DOI
  `10.1080/1351847X.2023.2220118`, and its complete open precursor chapter;
- `strategy-seeds/sources/SCHWEIKERT-XAUXAG-RATIO-2026/source.md`, covering
  Schweikert (2018), *Journal of Banking & Finance* 88, 44-51, DOI
  `10.1016/j.jbankfin.2017.11.010`, plus the corroborating fractional-
  cointegration record; and
- `strategy-seeds/sources/CME-GSR-SPREAD-2025/source.md`, the exchange record
  for the gold/silver ratio and opposing-leg spread carrier.

The sources jointly supply a state-dependent gold/silver relation, the fixed
32-return `q=2` robust variance-ratio statistic, the two-sided 10% significance
boundary, and the relative-value carrier. They do not publish this exact
intersection, a 60-D1 ratio normalization, entry or exit bands, Darwinex CFD
results, ATR stops, fixed-risk sizing, trade density, neutrality, or portfolio
correlation. Those items remain disclosed QM hypotheses and controls.

## Non-Duplicate Decision

The pre-allocation checker scanned 4,311 registry rows and 428 canonical cards.
It returned `CLEAN`, with no exact collision or fuzzy hit, for slug
`xauxag-vr-fade`, strategy ID
`CME-MEHLITZ-XAUXAG-VRFADE-2026_S01`, and the full proposed mechanic. Manual
review resolves the material neighbors:

- `QM5_20157_xau-xag-ratio` fades a rolling log-ratio z-score without any
  memory estimator or significance gate.
- `QM5_20161_xauxag-ols-rv`, `QM5_13205_xau-xag-qc`, and
  `QM5_20012_xauxag-cmtar` trade OLS, conditional-quantile, or fixed asymmetric
  cointegration residuals rather than a raw ratio displacement gated by
  relative-return anti-persistence.
- `QM5_20249_xauxag-vr-spread` uses the latest relative monthly return as its
  direction and follows persistence or reverses anti-persistence at the month
  boundary. It never measures a D1 ratio-level displacement and never exits on
  ratio convergence.
- Breakout, momentum-rank, calendar, skew, jump, expected-shortfall,
  idiosyncratic-volatility, and volatility-of-volatility XAU/XAG builds use
  different state variables and lifecycle rules.

The conjunction of a significant negative robust `q=2` memory state, a
completed-D1 log-ratio displacement, fade direction, daily convergence exit,
and one-attempt-per-month basket lifecycle is jointly load-bearing. Verdict:
`CLEAN_AFTER_DETERMINISTIC_AND_MANUAL_REVIEW`.

## Allocation And Kill Boundary

- intended EA ID: `QM5_20254`, subject to deterministic registry allocation;
- slug: `xauxag-vr-fade`;
- strategy ID: `CME-MEHLITZ-XAUXAG-VRFADE-2026_S01`;
- intended slot 0: `XAUUSD.DWX` / magic `202540000`;
- intended slot 1: `XAGUSD.DWX` / magic `202540001`;
- logical basket: `QM5_20254_XAU_XAG_VRFADE_D1`;
- cadence: estimated 5-9 completed packages/year after warm-up; Q02 owns the
  realized density verdict;
- retire below five completed packages per full post-warm-up year;
- retire on wrong or unsynchronized month ends, wrong relative-return
  orientation, wrong robust statistic, a non-negative or insignificant memory
  state entering, wrong ratio orientation, same-direction or orphan legs,
  repeated monthly attempt, aggregate-risk breach, nonpositive governed
  economics, or later portfolio-correlation rejection;
- no post-result window, threshold, direction, retry, stop, spread, or carrier
  rescue is authorized.

## Safety Boundary

This authorization excludes manual backtests; live, demo, shadow, stress, and
optimization setfiles; `T_Live`; AutoTrading; deploy or T_Live manifests;
portfolio admission; portfolio-gate edits; and correlation waivers. Q02 uses
exactly `RISK_FIXED=1000`, `RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1` for one
aggregate two-leg package.

# QM5_21520 XNG weekly quiet-flow momentum — G0 decision

Date: 2026-08-14

Decision: `APPROVED` for one bounded V5 Strategy Card, one branch-only
non-live build, strict Q01 validation, and one paced non-live Q02 handoff if
the factory CPU ceiling permits.

Authority: OWNER commodity/energy portfolio mission delivered to Codex on
2026-08-14. The mission explicitly permits a second XNG sleeve when its logic
differs from `QM5_12567`, requires a structural low-frequency rule,
reputable-source criteria, and `RISK_FIXED` backtests, and forbids live and
portfolio-gate mutations.

## Candidate

- EA: `QM5_21520_xng-flow-mom`
- Strategy ID: `ZHAO-ST-MOMREV-2026_XNG_S04`
- Source ID: `ZHAO-XNG-QUIETFLOW-2026`
- Symbol/slot/magic: `XNGUSD.DWX` / 0 / `215200000`
- Driver: continue the latest completed five-D1 XNG return only when the
  same window's native tick-volume sum ranks in the bottom quartile of 40
  earlier non-overlapping five-bar volume windows
- Lifecycle: one consumed attempt per broker week, frozen
  `2.5 * ATR(14,D1)` hard stop, five-completed-D1-bar maximum hold, standard
  news gate, and framework Friday close

## Approved source boundary

The complete governed research note
`D:/QM/strategy_farm/artifacts/source_notes/28681f5d-aa78-584e-9698-750d1402e485.md`
and the existing canonical source record
`strategy-seeds/sources/ZHAO-ST-MOMREV-2026/source.md` were read before this
decision. The single bounded lineage for this card is
`strategy-seeds/sources/ZHAO-XNG-QUIETFLOW-2026/source.md`.

The source is Zhao, Ding, Yu, and Kang (2026), "Momentum and Reversal on the
Short-Term Horizon: Evidence from Commodity Markets," SSRN 6425598, DOI
`10.2139/ssrn.6425598`. Accessible metadata and abstract/methodology summaries
support only a narrow claim: the residual component of weekly commodity
returns positively predicts the following week's return, while the authors'
speculative-flow component reverses. The paper's decomposition uses investor
positions, which QM does not have as an approved runtime feed.

Native MT5 tick volume is therefore a disclosed conditioning proxy. A
bottom-quartile volume window is treated as a falsifiable candidate for a
quiet, residual-dominated week. The source does not specify that proxy, the
XNG carrier, the percentile, stop, spread, hold, expected performance, or
portfolio correlation. No source result transfers to this card.

On 2026-08-14 the deterministic QM URL router classified direct retrieval of
the canonical SSRN URL as `PERMISSION_REQUIRED`, adapter `generic`, state
`ROUTER_ONLY`, and lead status `DEFERRED:SOURCE_POLICY`. No proxy,
authentication, or access-control workaround was used.

## Locked rule

At the first processed D1 bar after a genuine framework broker-week
transition:

1. Persist the new week as attempted before history, signal, news, spread,
   quote, sizing, or order checks. There is no same-week retry.
2. Load exactly 205 completed XNG D1 bars and require strictly descending
   timestamps, positive finite closes, and positive tick volume.
3. Compute `weekly_return = Close[0] / Close[5] - 1` and sum tick volume over
   bars 0 through 4.
4. Form 40 earlier, non-overlapping five-bar tick-volume sums from bars 5
   through 204. Compute
   `rank = 100 * count(baseline_sum <= current_sum) / 40`, with ties included.
5. If `rank <= 25` and the return is positive, buy XNG. If `rank <= 25` and
   the return is negative, sell XNG. A zero return or higher rank consumes the
   week flat. Return or volume magnitude never scales risk.
6. Open at most one slot-0 XNG position with `RISK_FIXED=1000`, a frozen
   `2.5 * ATR(14,D1)` broker hard stop, no take-profit, and a 600-point spread
   cap.
7. Close after five completed D1 bars, at the hard stop, or through framework
   Friday close. There is no opposite-signal, target, trailing, break-even,
   partial, scale-in, grid, martingale, or pyramid rule.

## Reputable-source criteria

- R1 PASS: one attributable working paper with authors, date, URL, DOI,
  retrieval boundary, complete governed note, and a bounded approved packet.
- R2 PASS: completed-bar support, disjoint volume windows, empirical rank,
  direction, attempt clock, stop, hold, spread, and sizing are deterministic.
- R3 PASS for the disclosed proxy: XNG D1 close and native tick volume are
  available in MT5; no COT, position, file, API, or external signal is used.
- R4 PASS: fixed arithmetic and thresholds only, with no trained output,
  PnL-dependent fitting, grid, martingale, scale-in, or pyramid.

## Non-duplicate decision

The canonical pre-allocation checker found no exact slug or strategy-ID
collision and returned expected fuzzy Zhao-family neighbors. Manual mechanic
review resolves them:

- `QM5_12567_cum-rsi2-commodity` is a long-only short-horizon cumulative-RSI
  pullback with a slow trend filter. This sleeve is weekly, symmetric,
  raw-return continuation admitted by native tick-volume rank.
- `QM5_13101_xng-1w-mom-vol` requires a minimum five-day return magnitude and
  a low *realized-volatility* percentile, then permits an opposite-return
  exit. This sleeve has no move threshold, realized-volatility statistic, or
  signal exit; its conditioner is bottom-quartile tick volume.
- `QM5_21504_xng-flowrev` uses the same auditable disjoint volume-window
  construction but admits only the upper tail (`rank >= 75`) and fades the
  return. This card admits only the disjoint lower tail (`rank <= 25`) and
  follows the return. Their eligible weeks and directions are different.
- `QM5_21505_xag-weekly-lowvol-momentum` is an unbuilt, preallocated silver
  carrier from the same research batch. It is not an XNG or energy build and
  transfers no evidence to this carrier.

Verdict:
`CLEAN_XNG_WEEKLY_QUIET_FLOW_MOMENTUM_AFTER_SOURCE_FAMILY_REVIEW`.

## Allocation and kill boundary

The deterministic allocator reserved `QM5_21520` on 2026-08-14. Expected
cadence is roughly 10-14 entries per full post-warm-up year as a prior, not
test evidence. Q02 must retire the card below five completed trades per full
post-warm-up year or on nonpositive governed economics. Do not loosen the
volume cap, change the five-bar window, introduce a move threshold, or change
direction to rescue failure.

The new conditioning object is intended to diversify logic from the
certified XNG pullback, but G0 does not assert realized independence. Q09
alone may accept or reject portfolio correlation.

## Safety boundary

Create only one `XNGUSD.DWX` D1 backtest setfile with `RISK_FIXED=1000`,
`RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1`. This decision excludes manual
backtests; live, demo, shadow, stress, or optimization setfiles; `T_Live`;
AutoTrading; deploy or T_Live manifests; portfolio admission; portfolio-gate
edits; and correlation waivers. If the paced factory CPU ceiling is binding
before enqueue, stop without starting, stopping, reserving, reaping, or
reprioritizing any terminal.

# QM5_21525 WTI/Copper CADF Residual Reversion — G0 Decision

Date: 2026-08-15

Decision: `APPROVED` for one bounded V5 Strategy Card, one branch-only
non-live build, strict Q01 validation, and one paced non-live Q02 enqueue.
This decision does not authorize a manual tester dispatch.

Authority: OWNER commodity/energy portfolio mission delivered to Codex on
the `agents/board-advisor` branch and durably recorded before extraction in
`decisions/2026-08-15_wti_xcu_cadf_source_approval.md` at commit
`a16a01823`.

## Candidate

- EA: `QM5_21525_wti-xcu-cadf`
- Strategy ID: `CHAN-EIA-USGS-WTI-XCU-CADF-2026_S01`
- Source ID: `CHAN-EIA-USGS-WTI-XCU-CADF-2026`
- host/traded symbol/slot/magic: `XTIUSD.DWX` / 0 / `215250000`
- companion/traded symbol/slot/magic: `XCUUSD.DWX` / 1 / `215250001`
- logical basket: `QM5_21525_WTI_XCU_CADF_D1`
- driver: fresh standardized crossing of a log-WTI-on-log-copper OLS
  residual admitted by a simple one-lag CADF proxy and half-life gate
- lifecycle: opposite paired fade, beta-weighted fixed stop-risk shares,
  frozen per-leg `3.5 * ATR(20,D1)` stops, convergence/model/sixty-day exit,
  atomic partial-open repair, and fixed spread caps

## Source Decision

The approved composite packet is
`strategy-seeds/sources/CHAN-EIA-USGS-WTI-XCU-CADF-2026/source.md`. It is
bound to the complete CEO-ratified Chan cointegration-family extraction and
the governed official EIA/CME/USGS WTI/copper packets.

Chan supplies a mechanical OLS/CADF spread-reversion method and explicitly
distinguishes cointegration from correlation. His worked pair is GLD/GDX,
not WTI/copper. The official carrier sources establish distinct physical
energy and industrial-metal contexts only. The rolling two-CFD fit is a QM
falsification. No source efficacy, coefficient, density, cost, neutrality,
decorrelation, or portfolio result transfers.

## Locked Rule

On each new WTI D1 bar:

1. Load exactly 252 synchronized completed WTI and copper closes. Require
   exact timestamps, positive finite values, chronology, and a newest
   endpoint no more than ten calendar days stale.
2. Fit `log(WTI)=alpha+beta*log(copper)+residual` by closed-form OLS.
   Require finite non-singular arithmetic and beta in `[0.10,3.00]`.
3. Run the locked one-lag residual CADF proxy with intercept. Require
   `rho<0`, `t_rho<=-3.043`, `0<1+rho<1`, and implied half-life in `[2,60]`.
4. Standardize the newest two residuals using OLS residual sigma with 250
   degrees of freedom. A fresh cross above `+1.0` opens SELL WTI / BUY
   copper; a fresh cross below `-1.0` opens BUY WTI / SELL copper. No
   already-extreme or invalid state may enter.
5. Split one aggregate `RISK_FIXED=1000` budget by normalized relative
   weights `1.0` and `abs(beta)`. Attach frozen `3.5 * ATR(20,D1)` hard
   stops, no targets, and enforce 1,500/1,200-point WTI/copper spread caps.
6. Open WTI then copper. Immediately flatten all owned exposure after an
   order failure or invalid final package.
7. Close on `abs(z)<=0.5`, invalid synchronized model/data/package, or sixty
   calendar days. Friday close and all news modes are OFF.

The carrier, orientation, lookback, intercept, CADF proxy, critical value,
beta and half-life bounds, fresh-cross rule, direction, aggregate risk, and
atomic lifecycle are locked.

## Reputable-Source Criteria

- R1 `PASS`: complete CEO-ratified Wiley extraction plus governed official
  EIA, CME, and USGS carrier references.
- R2 `PASS`: estimator, stationarity gate, direction, risk, stops, spreads,
  and lifecycle are fixed and reproducible.
- R3 `PASS`: registered WTI/copper D1 routes supply every runtime input; Q02
  must prove synchronized history, stationarity, fills, and density.
- R4 `PASS`: deterministic native arithmetic only, without trained output,
  prohibited signal indicator, external feed, grid, martingale, scale-in, or
  pyramid.

## Non-Duplicate Decision

The canonical checker scanned 4,397 registry rows and 493 root cards and
returned `CLEAN`. Manual review separates WTI/copper return-spread reversion
(`QM5_13090`), price-channel continuation (`QM5_13094`), and twelve-month
relative momentum (`QM5_21524`); oil/gas trend-augmented ECM (`QM5_20237`);
precious-metal OLS residual reversion (`QM5_20161`); and the incumbent XNG
oscillator (`QM5_12567`). None combines this carrier, log orientation,
simple CADF proxy, fresh crossing, and lifecycle. Verdict:
`CLEAN_WTI_COPPER_CADF_RESIDUAL_REVERSION_PACKAGE`.

## Allocation And Kill Boundary

The next deterministic registry allocation reserves `QM5_21525`. Expected
cadence is five to twelve completed packages per full post-warm-up year; Q02
must retire below five/year or on nonpositive governed economics. Q09 alone
may establish realized correlation with the certified book. Opposite sides
and beta-weighted stop risk do not prove neutrality.

## Safety Boundary

Create exactly one logical-basket D1 backtest setfile with
`RISK_FIXED=1000`, `RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1`. This decision
excludes manual backtests; live, demo, shadow, stress, and optimization
setfiles; `T_Live`; AutoTrading; deploy or T_Live manifests; portfolio-gate
edits; portfolio admission; and correlation waivers. Enqueue once, but do not
dispatch or control a tester when the factory resource ceiling is binding.


# QM5_41244 XNG Tail-MTSM S2 — G0 Decision

Date: 2026-08-31

Decision: `APPROVED`

Authority: current explicit OWNER commodity/energy portfolio mission on
branch `agents/board-advisor`, bounded by
`decisions/2026-08-31_xng_tail_mtsm_s2_source_approval.md`.

## Identity

- EA ID: `QM5_41244`
- slug: `xng-tail-mtsm-s2`
- strategy ID: `LIU-MTSM-2021_XNG_S02`
- source ID: `LIU-MTSM-2021`
- host: exact `XNGUSD.DWX`, D1, slot 0
- intended magic after governed allocation: `412440000`

The identity is already present in
`framework/registry/ea_id_registry.csv`. Magic allocation remains a separate
deterministic step after the EA directory and approved card-of-record exist.

## Gate Findings

### R1 — reputable source: PASS with translation boundary

The governed packet `strategy-seeds/sources/LIU-MTSM-2021/source.md` records a
complete accepted-manuscript review of Liu, Lu, and Wang (2021),
*International Review of Financial Analysis* 78, article 101938, DOI
`10.1016/j.irfa.2021.101938`. It supplies the 30-day momentum, five-day
upper/lower partial moments, separate 80th-percentile tail states, and MTSM-S2
action map. The paper does not establish natural-gas or Darwinex-CFD results;
the XNG carrier and V5 fixed-risk contract are explicit translations.

### R2 — mechanical: PASS

The card locks completed D1 inputs; 30 simple returns; five-return upper and
lower squared partial moments; 252 older observations; separate nearest-rank
80th percentiles; the four-region S2 action map; one owned position; one
consumed entry attempt per D1 label; no same-label reversal; fixed risk; frozen
ATR stop; spread cap; eight-day repair; and framework Friday close.

### R3 — data: PASS with carrier risk

Registered `XNGUSD.DWX` D1 history and native MT5 OHLC, ATR, quotes,
positions, deals, and terminal state supply every runtime input. The
Chinese-futures-to-XNG and futures-to-CFD transport, spread, gap, session, and
Friday-package risks are binding falsification risks.

### R4 — deterministic / ML ban: PASS

The rule uses timestamps, completed closes, simple arithmetic, sorting,
nearest-rank order statistics, ATR risk control, quotes, positions, deals, and
persistent state. It uses no trained signal, external runtime feed, grid,
martingale, scale-in, or pyramid.

## Non-Duplicate Resolution

`artifacts/qm5_xng_tail_mtsm_s2_preallocation_dedup_20260831.json` scanned
4,743 registry rows, 1,381 cards, and 45 Strategy Wiki nodes. No exact identity
was found. The only fuzzy rows are the two copies of the intended WTI parent,
`QM5_13108_xti-mtsm-s2`.

This card is approved as a parameter-locked carrier port, not a duplicate
identity: it cannot trade the parent's WTI carrier, and no XNG card implements
upper/lower partial moments plus the S2 map. It is mechanically distinct from
certified `QM5_12567`, whose long-only cumulative-RSI2 pullback and 200-D1
trend filter are absent here.

Verdict:
`FUZZY_MATCH_RESOLVED_LOCKED_XNG_CARRIER_PORT_DISTINCT_FROM_QM5_12567`.

## Build And Kill Boundary

Build is authorized only from the approved card
`strategy-seeds/cards/approved/QM5_41244_xng-tail-mtsm-s2_card.md`, after the
one-slot magic row exists. Q01 must compile strictly and prove registry,
setfile, risk, and input-group cleanliness.

Q02 has one locked `RISK_FIXED=1000` baseline. Retire on zero positions, fewer
than five positions in any full scored year, nonpositive governed economics,
future leakage, wrong partial-moment or percentile arithmetic, wrong region
action, duplicate same-label attempt, same-label reversal, missing stop,
invalid risk mode, malformed lifecycle, or nondeterminism. There is no
after-result parameter rescue.

Approval covers build, deterministic reference tests, strict compile, and one
paced Q02 enqueue only while the governed whole-host CPU ceiling is clear. It
does not authorize a manual tester run, portfolio-gate edit, correlation
waiver, portfolio admission, live preset, deploy manifest, `T_Live`, terminal
control, or AutoTrading action.


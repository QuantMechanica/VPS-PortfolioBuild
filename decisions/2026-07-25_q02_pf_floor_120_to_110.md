# Decision: Q02 PF hard bottom 1.20 -> 1.10

- Date: 2026-07-25
- Status: accepted (executed)
- Owner: OWNER (ratified in chat, 2026-07-25: "Q02_PF_MIN senken wir auf 1.1")
- Affected: `framework/scripts/p2_baseline.py` (`Q02_PF_MIN`)
- Relation to DL-082 §4: complementary. DL-082 introduced the evidence-conditional floor
  and already set its `hard_bottom_pf` to 1.10; this aligns the standalone constant with it.

The constant's old comment read "profit factor HARD BOTTOM (per symbol); never softened
below this". OWNER overrode that deliberately. Recorded here rather than argued.

## What the change actually does

**It changes no verdict.** This is the honest result and it is the reason this record exists.

Since DL-082 §4 (2026-07-19) the operative Q02 PF floor is

```
effective_floor(N) = max(Q02_EVIDENCE_FLOOR["hard_bottom_pf"], curve(N))
```

with `hard_bottom_pf` **already 1.10**. `Q02_PF_MIN` survives in exactly two places:

1. the fallback in `evidence_conditional_pf_floor` when the cfg omits `hard_bottom_pf`
   (it does not) or when the curve is disabled (it is not), and
2. the `hard_bottom=` field of the `pf_below_q02_floor` reason string — which was
   therefore **reporting 1.20 as the hard bottom while the code enforced 1.10**.

So the change removes a false statement from the evidence trail and corrects a dormant
fallback. Verdict impact: zero.

A stale comment on the gate itself was corrected in the same commit: it claimed the floor is
"never lowered below 1.20", contradicting `max(1.10, curve)` five lines above it.

## Retest scope

OWNER asked for everything that failed on this floor to be retested. Measured against
`ea_metrics` (1 288 unique (EA, symbol) pairs with a Q02 FAIL and both pf and trades recorded):

| reading | revived |
|---|---:|
| `Q02_PF_MIN` 1.20 -> 1.10 as executed | **0** |
| hypothetical flat 1.10, evidence curve disabled, N >= 45 | 2 |

The two under the hypothetical: `QM5_11564/USDJPY` (pf 1.470, n=61, floor 1.635) and
`QM5_1241/USDCAD` (pf 1.110, n=66, floor 1.605).

## Why the number is so small — the PF floor is not the binding gate at Q02

Gate order in `_verdict_for_symbol` is trades-floor **before** PF-floor. The trades floor is
`max(min_trades, Q02_TRADES_MIN)` where `min_trades = 5 * window_years` — about 45 for a
full-history window. Of the 1 288 unique Q02 FAILs with recorded metrics, **2** have n >= 45.
Everything else died at the sample-size floor, before PF was ever consulted.

The apparent large population of "PF failures" at low N is an artifact of the evidence curve,
which *raises* the floor steeply below the N=450 anchor (n=2 -> 9.42, n=61 -> 1.64, n=100 ->
1.47). Those items are sample-size casualties wearing a PF label.

Sensitivity, if shorter card windows put the effective trades floor lower than 45:

| trades floor assumed | items with 1.10 <= pf < floor(N) |
|---:|---:|
| 10 | 43 |
| 20 | 13 |
| 30 | 3 |
| 45 | 2 |

## Amendment, same session — evidence curve DISABLED, floor is flat 1.10

Presented with the measurement above, OWNER chose to pull the actual lever:
`Q02_EVIDENCE_FLOOR["enabled"] = False`. The Q02 PF floor is now **flat 1.10 at every
sample size**.

| N | 5 | 20 | 61 | 100 | 450 | 1000 | 2000 |
|---|---:|---:|---:|---:|---:|---:|---:|
| floor before | 4.80 | 2.32 | 1.64 | 1.47 | 1.20 | 1.13 | 1.10 |
| floor now | 1.10 | 1.10 | 1.10 | 1.10 | 1.10 | 1.10 | 1.10 |

### Concern, recorded once

DL-082 §4 introduced the curve specifically to stop thin samples passing on statistically weak
evidence ("PF 1.05 @ 600 trades is stronger evidence than PF 1.30 @ 40"). Disabling it removes
that protection at Q02.

What limits the exposure: the trades floor `max(5 * window_years, Q02_TRADES_MIN)` is evaluated
**before** the PF gate, so on a full-history window everything under ~45 trades is already gone —
which is precisely the region where the curve was binding (N=5 -> 4.80, N=20 -> 2.32). For
N >= 45 the curve sat between 1.20 and 1.64, so the practical change is that band, not the
noise floor. Selection-bias protection now rests on the 5/yr frequency floor and on Q07
PBO/DSR, per the DL-082 §1 guard.

The curve function and its constants are left intact and still exercised by tests; re-enabling
is a one-flag change.

### Retest scope: zero, established from recorded reasons

OWNER asked for everything that failed on the PF floor to be retested. Counted directly from
`work_items.payload_json.verdict_reason` over **all 72 684 Q02 rows**:

```
pf_below_q02_floor   0 occurrences
```

**No Q02 item has ever been rejected by the PF floor.** There is nothing to requeue. Two
earlier estimates in this session (2 items, then 124) were reconstructions from pf/trades in
`ea_metrics` and both misattributed sample-size casualties to the PF gate; the recorded reason
is authoritative and says zero.

What actually rejects at Q02, by recorded reason:

| reason | rows |
|---|---:|
| `run_smoke_fail:MIN_TRADES_NOT_MET` | 3 432 |
| `run_smoke_fail:NO_HISTORY` | 2 326 |
| `P2_PRESCREEN_run_smoke_fail` | 2 310 |
| `run_smoke_fail:ONINIT_FAILED` | 1 144 |
| `setfile_missing` | 640 |
| `run_smoke_fail:BARS_ZERO` | 520 |
| `MIN_TRADES_NOT_MET` | 474 |
| `Q02_ZERO_TRADES` | 429 |
| `ACTIVE_TIMEOUT` | 347 |
| `run_smoke_fail:REPORT_MISSING` | 302 |
| `ex5_missing` | 216 |
| `run_smoke_fail:REPORT_FORMAT_DRIFT` | 202 |
| `G1_NO_REAL_TICKS` | 151 |
| `run_smoke_fail:INVALID_REPORT` | 85 |
| `pf_below_q02_floor` | **0** |

`run_smoke` enforces its own `-MinTrades` before the Python gate runs, so anything below the
floor short-circuits and the PF check is never consulted. Roughly 3 900 rejections are
sample-size; a further ~4 500 are infrastructure (`NO_HISTORY`, `ONINIT_FAILED`, `BARS_ZERO`,
`REPORT_MISSING`, `REPORT_FORMAT_DRIFT`, `INVALID_REPORT`) rather than strategy verdicts at all.

Two consequences:

1. Disabling the evidence curve carries **no thin-sample risk in practice** — `MinTrades` kills
   that population before the PF floor is reached. The concern recorded above is real in
   principle and empty in this pipeline's actual behaviour.
2. If the goal is to admit more candidates at Q02, the lever is unambiguously the **5/yr trades
   floor**, and the second-largest lever is fixing the infrastructure failure classes. Neither
   is decided here.

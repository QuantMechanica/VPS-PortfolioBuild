# G0 Decision - QM5_41056 XTI/XNG Pure 18-Month Reversal

Date: 2026-08-18

Decision: `APPROVED`

Authority: OWNER commodity/energy portfolio mission delivered to Codex on the
`agents/board-advisor` branch, bounded by
`decisions/2026-08-18_xtixng_18m_reversal_source_approval.md`.

Approved card:
`strategy-seeds/cards/approved/QM5_41056_energy-rev18_card.md`.

## Identity

- EA ID: `QM5_41056`, allocated by the deterministic registry at commit
  `0ceacf790`
- slug: `energy-rev18`
- strategy ID: `BIANCHI-MOMREV-2015_XTI_XNG_S04`
- source approval commit: `72bf6148c`
- magic allocation commit: `11cabe252`
- carrier: exact `XTIUSD.DWX` D1 host on slot 0 and exact `XNGUSD.DWX` D1
  companion on slot 1, with registered magics `410560000` and `410560001`
- mechanic: at each genuine broker-month boundary, buy the lower synchronized
  completed 18-month return and short the higher return, then renew both legs
  at the next month boundary

## Gate Findings

- R1 `PASS_WITH_TRANSLATION_RISK`: named authors, peer-reviewed *Journal of
  Banking & Finance* publication, DOI, complete accepted-manuscript review,
  and explicit WTI/natural-gas membership. The pure two-energy carrier is a
  disclosed narrow translation of the paper's 18-month reversal state.
- R2 `PASS`: synchronized endpoints, 18-month horizon, reversal map, tie
  band, durable attempt, aggregate fixed risk, hard stops, compensation, and
  monthly lifecycle are fully mechanical.
- R3 `PASS_WITH_HISTORY_AND_BASKET_RISK`: registered native XTI/XNG D1 data
  supplies every runtime field. Uniform energy-session label normalization
  and logical-basket evaluation are binding Q01/Q02 conditions.
- R4 `PASS`: deterministic timestamps, completed OHLC, logarithms,
  comparisons, ATR risk plumbing, quotes, positions, deals, and terminal
  state only; no trained output, banned signal indicator, external runtime
  feed, grid, martingale, scale-in, or pyramid.

## Duplicate Review

The canonical pre-allocation checker scanned 4,543 EA-registry rows and 625
root-card files and found no exact identity. Manual family review confirms:

- `QM5_13120_energy-momrev` opens only when 12-month momentum and 18-month
  reversal ranks disagree; this card never reads a 12-month state and trades
  every valid non-tied 18-month rank;
- `QM5_20202_xauxag-rev18` carries the isolated reversal state on metals,
  while this card carries XTI/XNG physical-energy and basis risk;
- `QM5_12733_xti-xng-xmom` follows a shorter relative winner, whereas this
  card fades the completed 18-month winner;
- `QM5_12840_xti-xng-rspread` uses a short-window standardized return spread,
  whereas this card has no mean, deviation, ratio, residual, or entry level;
- weekday, event, carry, same-calendar, and inventory energy cards use a
  different clock or state; and
- `QM5_12567_cum-rsi2-commodity` is a standalone daily long-only XNG
  oscillator pullback.

Verdict:
`CLEAN_XTI_XNG_PURE_SYNCHRONIZED_18_MONTH_REVERSAL_MONTHLY_BASKET_AFTER_CANONICAL_AND_FAMILY_REVIEW`.

## Approved Build Contract

Development may build exactly the approved card with:

- exact `XTIUSD.DWX` D1 host, `XNGUSD.DWX` D1 companion, slots 0/1, and
  registered magics `410560000`/`410560001`;
- native same-day or one uniform `+1` calendar-day energy-label
  normalization, applied consistently to both legs and every endpoint;
- first genuine broker-month D1 boundary and one durable `yyyymm` attempt
  persisted before every fallible entry gate;
- synchronized, completed month-end closes at the decision boundary and
  exactly 18 completed months earlier, each no more than ten calendar days
  before its target boundary;
- `ln(end/start)` for each leg, BUY of the lower return and SELL of the higher
  return outside an inclusive `1e-12` tie band;
- no 12-month rank, momentum agreement/disagreement gate, ratio, residual,
  z-score, weekday, event, inventory, carry, or optimizer-selected filter;
- one logical-basket `RISK_FIXED=1000`, `RISK_PERCENT=0`,
  `PORTFOLIO_WEIGHT=1` D1 backtest setfile;
- equal package-risk halves, each with a frozen `3.5 * ATR(20,D1)` hard stop,
  no target, XTI spread cap 1,500 points, and XNG spread cap 3,000 points;
- first-leg confirmation, immediate compensation on second-leg failure,
  every-tick malformed/orphan repair, next-month renewal, and a 35-day stale
  guard; and
- both news axes OFF, framework Friday close OFF, deterministic reference
  tests, strict compile, set/registry checks, and static Q01 validation before
  Q02 handoff.

No standalone-leg test, retry, scale-in, grid, martingale, pyramid, parameter
sweep, external runtime input, or after-result rescue is approved.

## Pipeline And Safety Boundary

Approval authorizes the branch-only non-live build, one `RISK_FIXED` logical-
basket backtest setfile, strict Q01, and one paced target-only Q02 enqueue only
if the exact-path tester count and host CPU are below their governed ceilings.
It does not authorize a manual tester dispatch or tester control.

Expected cadence is approximately eleven to twelve completed packages per
full post-warm-up year. Q02 must retire on zero trades, fewer than five/year,
nonpositive governed economics, wrong or stale endpoints, current-month
leakage, a hidden 12-month or spread-level gate, wrong reversal direction,
late/repeated entry, orphan persistence, nondeterminism, invalid risk mode, or
insufficient synchronized history. Q09 alone may establish realized book
correlation.

This decision excludes live/demo/shadow/stress/optimization setfiles,
AutoTrading, `T_Live`, deploy or T_Live manifests, portfolio-gate edits,
portfolio admission, decorrelation or neutrality claims, and correlation
waivers.

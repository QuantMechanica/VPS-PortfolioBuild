# QM5_41354 WTI ADF–Mann-Kendall Agreement Trend — G0 Decision

- Date: 2026-09-05
- Decision owner: OWNER
- Recorded by: Codex
- Decision: `APPROVED`
- Gate: G0 approval for falsification
- EA ID: `QM5_41354` (atomically reserved)
- Slug: `wti-adf-mkendall-agree-tr`
- Strategy ID: `AI-CODEX-WTI-ADF-MKENDALL-AGREE-TREND-20260905_S01`

## R1–R4 decision

Source approval is recorded at
`decisions/2026-09-05_wti_monthly_adf_mann_kendall_agreement_trend_source_approval.md`.

- R1 `PASS_WITH_GOVERNED_COMPLETE_REPUTABLE_EVIDENCE`: complete hash-bound
  Wiley and peer-reviewed WTI records define the ADF and ordinal-trend parents.
- R2 `PASS`: samples, ADF regression, all 78 newest-thirteen comparisons,
  inclusive boundaries, direction, attempt, fixed risk, stop, spread, and
  lifecycle are locked.
- R3 `PASS_WITH_CONTINUOUS_CFD_BASIS_RISK`: registered native
  `XTIUSD.DWX` D1.
- R4 `PASS`: deterministic native arithmetic only; no prohibited learned or
  banned signal machinery, external runtime feed, grid, martingale, scale-in,
  or pyramid.

## Identity and authorization

The corrected-root scan found no exact identity across 4,834 registry rows,
1,447 cards, and 45 Wiki nodes. Existing ADF-agreement siblings use different
second-state functions and endpoint return for direction. The standalone ADF
parent omits ordinal confirmation; the standalone Mann-Kendall parent omits the
ADF state. This identity requires `adf_t>=-2.594` on sixty completed log-price
endpoints and `abs(S)>=28` across all newest-thirteen pairs; `sign(S)` chooses
side.

The implementation uses slot zero, magic `413540000`, sixty completed month
ends, one consumed monthly attempt, `RISK_FIXED=1000`, a frozen
`3.5*ATR(20)` stop, 1,500-point spread ceiling, next-month exit, and 40-day
stale repair. News, Friday close, and stress are off.

This decision authorizes deterministic magic allocation, branch-only non-live
V5 build, reference tests, strict Q01, and one paced Q02 enqueue if CPU
admission is clear. It grants no performance/decorrelation claim,
portfolio-gate change, deployment/live manifest, `T_Live`, AutoTrading,
terminal control, or live use.

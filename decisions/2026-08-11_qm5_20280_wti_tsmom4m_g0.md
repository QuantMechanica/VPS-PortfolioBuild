# QM5_20280 WTI Four-Month Time-Series Momentum G0 Authorization

Date: 2026-08-11

Authority: OWNER commodity/energy portfolio mission delivered to Codex on the
`agents/board-advisor` branch.

## Decision

Authorize one bounded V5 Strategy Card and non-live build for
`QM5_20280_wti-tsmom4m`. The candidate observes the exact log return across
four consecutive completed broker months of WTI, follows its sign, and renews
one outright `XTIUSD.DWX` package at each genuine broker-month transition.

The candidate may proceed through bounded source/card extraction, schema and
G0 lint, deterministic registry and magic allocation, resolver regeneration,
strict compile, one `RISK_FIXED` backtest setfile, Q01 validation, and one
paced Q02 enqueue. This authorization does not pre-approve efficacy,
diversification, decorrelation, certification, execution-contract promotion,
or portfolio admission.

## Source Boundary

The approved source of record is the already complete governed packet
`strategy-seeds/sources/MOP-TSMOM-2012/source.md`, covering Moskowitz, Ooi,
and Pedersen (2012), "Time Series Momentum," *Journal of Financial Economics*
104(2), 228-250, DOI `10.1016/j.jfineco.2011.11.003`. Its durable retrieval
receipt records an end-to-end read of the 23-page published paper, the
author-hosted route, page count, byte count, and PDF SHA-256. The paper reports
positive own-return continuation over the first twelve monthly lags, defines
monthly own-past-return sign positions, and includes NYMEX WTI crude in its
commodity-futures universe.

The paper does not report a standalone WTI four-month result or prescribe a
Darwinex continuous-CFD port, broker-month endpoint reconstruction,
fixed-dollar sizing, ATR stop, spread ceiling, restart ledger, or lifecycle
controls. The fixed four-completed-month aggregation and every execution
choice below are transparent QM hypotheses. No source return, WTI-specific
alpha, trade density, CFD equivalence, correlation result, or portfolio
conclusion transfers.

No newly retrieved public source is used. The bounded child extraction may be
written at `strategy-seeds/sources/MOP-WTI-TSMOM4-2026/source.md` only after
this durable approval exists. The complete governed parent packet and its
retrieval receipt have been read in full for this mission.

## Locked Rule

On the first processed `XTIUSD.DWX` D1 bar of a genuine broker-month
transition, reconstruct exactly five consecutive completed broker-month-end
closes `C[0]..C[4]`, oldest to newest. The newest endpoint must belong to the
month immediately before the decision month. Define the fixed four-month log
return:

```text
four_month_return = ln(C[4] / C[0])

signal = BUY  when four_month_return > 0
         SELL when four_month_return < 0
         FLAT when four_month_return == 0 or state is invalid
```

Require positive finite closes, consecutive completed months, strictly
increasing endpoint timestamps, a finite logarithm, and no endpoint from the
current broker month. The three interior endpoints are mandatory continuity
evidence; there is no fallback to four D1 bars, a quarter, an average of
monthly signs, a moving average, regression, robust estimator, calendar
direction, or prior pipeline result. Signal magnitude never scales risk.

Consume and persist the decision month before history, signal, spread, quote,
ATR, sizing, news, or order gates. Close the prior package at the next genuine
month transition before considering replacement, including when the sign is
unchanged. Each entry uses one `RISK_FIXED=1000` budget and a frozen
`3.5 * ATR(20,D1)` hard stop. The entry spread ceiling is 1,500 points. No
take-profit is authorized. Close after forty calendar days if the normal
rollover close is missed. Friday close and both news axes are disabled for the
full-month native-price package.

## Non-Duplicate Decision

The canonical pre-allocation checker scanned 4,345 EA-registry rows and 456
cards for slug `wti-tsmom4m`, strategy ID
`MOP-TSMOM-2012_XTI_4M_S28`, and the declared mechanic. It found no exact
registry or card identity. It returned expected fuzzy same-source/token
matches for manual review, including `wti-tsmom12m`, `wti-tsmom3m`,
`wti-lr-trend`, `wti-mom-vote`, and the unrelated copper carrier
`xcu-tsmom3m`.

Manual mechanic and registry review resolves those matches:

- `QM5_20187_wti-tsmom1m`, `QM5_20064_wti-tsmom2m`,
  `QM5_20055_wti-tsmom3m`, `QM5_20059_wti-tsmom6m`,
  `QM5_12616` nine-month, and `QM5_12603_wti-tsmom12m` use different exact
  completed-month endpoints;
- `QM5_20056_wti-dual-mom` requires agreement between six- and twelve-month
  states rather than one four-month endpoint return;
- `QM5_20258_wti-mom-vote` votes one-, three-, and twelve-month signs;
- `QM5_20261_wti-lr-trend` fits thirteen log-price levels and gates on
  `R^2`; and
- robust-return, rank, path-efficiency, sign-run, linear-weight, exponential-
  weight, calendar, event, XNG, and XAU/XAG systems do not use this exact
  five-endpoint/four-completed-month state.

The registry contains no WTI four-completed-month time-series-momentum row.
The five endpoints, mandatory monthly continuity, exact `(C[0], C[4])`
orientation, symmetric sign direction, consumed monthly attempt, and monthly
renewal are jointly load-bearing. Verdict:
`CLEAN_AFTER_EXPECTED_SHARED_SOURCE_FUZZY_REVIEW`.

## Allocation And Kill Boundary

- intended EA ID: `QM5_20280`, subject to deterministic registry allocation;
- slug: `wti-tsmom4m`;
- strategy ID: `MOP-TSMOM-2012_XTI_4M_S28`;
- intended symbol/slot/magic: `XTIUSD.DWX` / 0 / `202800000`;
- expected cadence: approximately eleven to twelve completed monthly packages
  per full post-warm-up year because only exact-zero or invalid states stay
  flat; Q02 owns observed density and economics;
- retire below five completed packages per full post-warm-up year, on
  nonpositive governed economics, or later portfolio-correlation rejection;
- fail on a missing/interpolated/nonconsecutive endpoint, current-month
  leakage, reversed return orientation, a horizon other than four completed
  months, wrong-side entry, repeated monthly attempt, missing hard stop, risk-
  mode mismatch, hold beyond forty days, or nondeterminism; and
- no post-result horizon, direction, stop, hold, spread, retry, or carrier
  rescue is authorized.

WTI is a crude-oil carrier absent from the current XAU/SP500/NDX/XNG book.
That carrier difference and this exact horizon are diversification hypotheses,
not correlation evidence; unchanged Q09 alone may measure realized overlap.

## Safety Boundary

This authorization excludes manual backtests; live, demo, shadow, stress, and
optimization setfiles; `T_Live`; AutoTrading; deploy or T_Live manifests;
portfolio admission; portfolio-gate edits; and correlation waivers. Q02 uses
exactly one `XTIUSD.DWX` D1 setfile with `RISK_FIXED=1000`, `RISK_PERCENT=0`,
and `PORTFOLIO_WEIGHT=1`. If the paced farm reaches its binding backtest CPU
ceiling before enqueue, record the stop and do not enqueue or run a manual
test.

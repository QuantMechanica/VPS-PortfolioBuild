# QM5_20259 XNG Multi-Horizon Momentum Vote G0 Authorization

Date: 2026-08-07

Authority: OWNER commodity/energy portfolio mission delivered to Codex on the
`agents/board-advisor` branch.

## Decision

Authorize one bounded V5 Strategy Card and non-live build for
`QM5_20259_xng-mom-vote`. At the first D1 bar of each genuine broker-month
transition, the candidate reconstructs thirteen consecutive completed
natural-gas month ends and takes the signs of XNG's own completed one-, three-, and
twelve-month log returns. It buys when at least two signs are positive and
sells when at least two signs are negative. Exact-zero or invalid component
returns consume the month flat.

The candidate may proceed through source/card lint, deterministic registry and
magic allocation, resolver regeneration, strict compile, one `RISK_FIXED`
backtest setfile, Q01 validation, and one paced Q02 enqueue. This authorization
does not pre-approve efficacy, diversification, decorrelation, certification,
execution-contract promotion, or portfolio admission.

## Source Boundary

The approved source of record will be
`strategy-seeds/sources/MOP-XNG-MOMVOTE-2026/source.md`, bounded by the
completely reviewed repository packet
`strategy-seeds/sources/MOP-TSMOM-2012/source.md`:

- Moskowitz, Tobias J., Yao Hua Ooi, and Lasse Heje Pedersen (2012), "Time
  Series Momentum," *Journal of Financial Economics* 104(2), 228-250, DOI
  `10.1016/j.jfineco.2011.11.003`.

The governed parent records a complete read of the published paper, a durable
PDF hash, monthly own-return-sign rules across formation lags, and natural gas
as an explicit commodity-futures member. The one-, three-, and twelve-month
components are source-family rules already represented separately in the
repository; no horizon was selected from Darwinex results.

The two-of-three vote is a transparent QM multi-scale aggregation hypothesis,
not a performance claim made by the paper. The source does not test the vote,
a standalone Darwinex continuous CFD, D1-derived broker month ends, monthly
package renewal, fixed cash risk, ATR stops, spread caps, restart state, or the
QM portfolio. No source PF, return, Sharpe ratio, drawdown, XNG constituent
result, trade count, cost, or correlation statistic transfers.

## Non-Duplicate Decision

The deterministic pre-allocation checker scanned 4,316 registry rows and 433
canonical cards. It found no exact slug or strategy-ID collision and returned
three expected source/mechanic-family fuzzy neighbors. Manual review resolves
those and the economically closest XNG systems:

- `QM5_20258_wti-mom-vote` uses the same fixed vote on WTI crude. The traded
  carrier is load-bearing: this candidate trades only natural gas and does not
  reuse WTI evidence or claim cross-carrier equivalence.
- `QM5_20204_xng-tsmom1m`, `QM5_20063_xng-tsmom3m`, and
  `QM5_12804_xng-tsmom12m-atr` follow one XNG horizon alone; the twelve-month
  sibling also requires a volatility corridor.
- `QM5_13116_xng-signmom` compares the breadth of twelve separate monthly
  return signs with a fixed 0.40 threshold. It does not compare nested
  one-, three-, and twelve-month cumulative returns.
- `QM5_12358_tmom-fut-mom` votes on rolling 20/60/120 D1-bar returns, evaluates
  daily, exits on daily vote reversal, and does not register XNG. This
  candidate uses completed broker-calendar months, persists one monthly
  attempt, and holds a monthly package.
- `QM5_12567_cum-rsi2-commodity` is a long-only two-day cumulative-RSI(2)
  pullback aligned with SMA(200) and held at most five D1 bars. This candidate
  has no RSI or price-level trend filter, is symmetric, and holds to monthly
  renewal.
- XNG calendar, storage, weather, volatility-memory, reversal, ratio, carry,
  breakout, and event EAs use different state variables or lifecycles.

The exact XNG carrier, consecutive calendar-month endpoints, nested horizons,
strict component signs, two-of-three aggregation, monthly attempt clock, and
monthly package renewal are jointly load-bearing. Verdict:
`CLEAN_AFTER_EXPECTED_FUZZY_AND_MANUAL_REVIEW`.

## Allocation And Kill Boundary

- intended EA ID: `QM5_20259`, subject to deterministic registry allocation;
- slug: `xng-mom-vote`;
- strategy ID: `MOP-TSMOM-2012_XNG_MAJ1312_S13`;
- intended slot 0: `XNGUSD.DWX` / magic `202590000`;
- cadence: at most one completed package per broker month after a thirteen-
  month-end warm-up; Q02 owns the realized density verdict;
- retire below five completed packages per full post-warm-up year;
- retire on nonconsecutive month ends, wrong horizon orientation, wrong vote,
  entry with a zero component, repeated monthly attempt, invalid risk mode,
  missing hard stop, or nonpositive governed economics;
- no post-result horizon, vote, stop, spread, retry, direction, or carrier
  rescue is authorized.

## Safety Boundary

This authorization excludes manual backtests; live, demo, shadow, stress, and
optimization setfiles; `T_Live`; AutoTrading; deploy or T_Live manifests;
portfolio admission; portfolio-gate edits; and correlation waivers. Q02 uses
exactly `RISK_FIXED=1000`, `RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1`.

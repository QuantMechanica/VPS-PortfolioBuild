# QM5_21518 WTI-Brent Confirmed Trend — G0 Decision

Date: 2026-08-14

Decision: `APPROVED` for research allocation, one V5 non-live build, strict
Q01 validation, and one paced non-live Q02 handoff only.

Authority: OWNER commodity/energy portfolio mission delivered to Codex on
2026-08-14. The mission requests a new structural commodity/energy sleeve,
explicitly permits a low-frequency WTI trend or seasonality edge, requires
reputable-source criteria and `RISK_FIXED` backtests, and forbids live and
portfolio-gate mutations.

## Candidate

- EA: `QM5_21518_wti-brent-cfm`
- Strategy ID: `MOP-CME-WTI-BRENT-CFM-2026_S01`
- Traded host/slot 0: `XTIUSD.DWX`, D1, magic `215180000`
- Read-only benchmark: `XBRUSD.DWX`, D1, no magic and no order authority
- Driver: exact twelve-completed-month WTI return sign, admitted only when the
  independently reconstructed Brent twelve-completed-month return has the
  same strict sign
- Lifecycle: one consumed decision per broker month, monthly replacement,
  frozen `3.5*ATR(20,D1)` hard stop, forty-day stale guard

## Approved source boundary

The complete approved parent packets were read before this decision:

- Moskowitz, Ooi, and Pedersen (2012), "Time Series Momentum," *Journal of
  Financial Economics* 104(2), 228-250, DOI
  `10.1016/j.jfineco.2011.11.003`, with a durable complete-paper retrieval
  receipt and hash in `strategy-seeds/sources/MOP-TSMOM-2012/source.md`;
- CME Group's WTI-Brent Financial futures contract, ICE's Brent/WTI Futures
  Spread contract, and EIA benchmark-structure analysis, preserved in
  `strategy-seeds/sources/CME-WTI-BRENT-SPREAD-2026/source.md`.

The single bounded lineage for this card is
`strategy-seeds/sources/MOP-CME-WTI-BRENT-CFM-2026/source.md`. The paper
supports the twelve-month own-return sign family and WTI membership. The
exchange and agency records support the economic relationship between WTI and
Brent. They do not test the same-sign confirmation rule or transfer any
efficacy, CFD, neutrality, or portfolio-correlation claim.

## Locked rule

At the first processed `XTIUSD.DWX` D1 bar after a genuine broker-month
transition:

1. Persist the broker month as attempted before history, signal, spread,
   quote, news, ATR, sizing, or order checks. There is no same-month retry.
2. Load bounded completed D1 history for WTI and Brent; intersect exact
   timestamps and require strict chronology, positive finite closes, a latest
   common endpoint before the decision bar, and at most ten calendar days of
   endpoint staleness.
3. Derive exactly thirteen consecutive completed broker-month endpoints for
   both symbols, ending in the immediately completed broker month, using the
   same timestamps for each benchmark.
4. Compute each benchmark's exact twelve-completed-month log return. For each
   benchmark, cross-check the endpoint return against the sum of twelve
   component monthly log returns within `1e-10`.
5. Buy WTI only if both returns are strictly positive. Sell WTI only if both
   returns are strictly negative. A zero or disagreement consumes the month
   flat. Return magnitude never scales risk.
6. Open at most one slot-0 WTI position with one `RISK_FIXED=1000` budget, a
   frozen `3.5*ATR(20,D1)` broker hard stop, no take-profit, and a 1,500-point
   spread cap. Brent is read-only and must never be ordered.
7. Close before monthly replacement, after forty calendar days, or on
   malformed owned state. Friday close and both news axes are OFF.

No different horizon, endpoint convention, sign threshold, carrier,
confirmation symbol, direction, risk scale, stop, hold, retry, or calendar
rule is authorized.

## Reputable-source criteria

- R1 PASS: one bounded source ID with a complete peer-reviewed JFE parent and
  governed CME/ICE/EIA benchmark records.
- R2 PASS: synchronized month endpoints, exact log returns, strict same-sign
  entry, read-only/traded roles, consumed decision, stop, spread, rollover,
  and stale guard are fixed.
- R3 PASS: registered native `XTIUSD.DWX` and `XBRUSD.DWX` D1 histories supply
  all runtime inputs; only WTI is ordered.
- R4 PASS: deterministic native price and calendar arithmetic only, without a
  trained signal, external runtime feed, PnL adaptation, grid, martingale,
  scale-in, or pyramid.

## Non-duplicate decision

The canonical pre-allocation checker scanned 4,390 EA-registry rows and 486
cards. It found no exact slug or strategy-ID collision and four lexical fuzzy
matches. Manual mechanic review resolves them:

- `QM5_12848_wti-brent-brk` trades both legs on a daily spread channel
  breakout; this candidate never forms a spread and never orders Brent.
- `QM5_12843_wti-brent-spread` and `QM5_12860_wti-brent-rshock` trade paired
  mean reversion in spread levels or relative-return shocks.
- `QM5_12603_wti-tsmom12m` trades the same WTI horizon unconditionally and
  never reads Brent.
- Brent trend sleeves trade Brent outright. WTI dual-horizon variants compare
  WTI with its own history. `QM5_12844_commodity-trend-crude` is a daily
  Donchian/ADX breakout.

The WTI carrier, synchronized thirteen-month endpoint set, independent WTI
and Brent twelve-month signs, strict agreement, Brent read-only boundary,
monthly attempt, and single-leg fixed-risk execution are jointly load-bearing.
Verdict: `CLEAN_AUTHORIZED_WTI_BRENT_BENCHMARK_CONFIRMED_TREND`.

## Allocation and kill boundary

- deterministic allocator result: `QM5_21518`;
- slug: `wti-brent-cfm`;
- strategy ID: `MOP-CME-WTI-BRENT-CFM-2026_S01`;
- intended magic: slot 0 / `XTIUSD.DWX` / `215180000`;
- read-only `XBRUSD.DWX` receives no registry magic row;
- expected cadence: approximately eight to eleven completed WTI positions per
  full post-warm-up year; and
- retire below five completed positions/year, on nonpositive governed
  economics, or at later portfolio-correlation rejection.

The crude benchmark agreement is intended to isolate a common crude trend,
but it does not prove low correlation to XAU, SP500, NDX, or the certified XNG
sleeve. Q09 alone may accept or reject realized overlap.

## Safety boundary

Q02 uses exactly one `XTIUSD.DWX` D1 backtest setfile with
`RISK_FIXED=1000`, `RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1`. This decision
excludes manual backtests; live, demo, shadow, stress, and optimization
setfiles; `T_Live`; AutoTrading; deploy or T_Live manifests; portfolio
admission; portfolio-gate edits; and correlation waivers. If the paced
factory CPU ceiling is binding before enqueue, stop and record capacity
without starting, stopping, reserving, reaping, or reprioritizing a terminal.

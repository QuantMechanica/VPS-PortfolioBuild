# QM5_41058 XNG Weekly Flow-Agreement Continuation G0 Authorization

Date: 2026-08-18

Decision: `APPROVED` for one branch-only V5 build, strict Q01 validation, one
locked `RISK_FIXED` backtest setfile, and one paced non-live Q02 enqueue when
tester and host-CPU ceilings permit. This is not live, portfolio, or manual-
tester authority.

## Identity

- EA: `QM5_41058_xng-wflow-agree`
- strategy ID: `WILLIAMS-MOP-XNG-WFLOW-2026_S01`
- approved source: `WILLIAMS-MOP-XNG-WFLOW-2026`
- canonical card:
  `strategy-seeds/cards/approved/QM5_41058_xng-wflow-agree_card.md`
- host/slot 0: `XNGUSD.DWX`, D1, magic `410580000`
- risk: `RISK_FIXED=1000`, `RISK_PERCENT=0`, `PORTFOLIO_WEIGHT=1`

The deterministic EA registry reserves ID 41058 at commit `c32cc25b8`. The
magic registry and generated resolver reserve slot 0 / `410580000` at commit
`b2ae5243b`; no collision or dropped resolver row was observed.

## Source And Hypothesis Review

The OWNER-authorized source decision is
`decisions/2026-08-18_xng_weekly_flow_agreement_source_approval.md`, committed
as `929a14cae`. The bounded packet joins Williams' reputable practitioner
close/open flow decomposition with the complete-read peer-reviewed
Moskowitz-Ooi-Pedersen own-return continuation lineage and explicit natural-
gas membership. It transfers no source return, significance, XNG-only
efficacy, CFD equivalence, correlation, or portfolio result.

The approved falsifiable hypothesis is that continuation after an exact
completed Monday-through-Friday XNG week is admitted only when the five
close-to-open log returns and five open-to-close log returns have the same
strict sign. Their sum must reconcile to the weekly endpoint within `1e-10`.
The first eligible next-Monday observation owns one durable attempt and the
ordinary lifecycle ends through framework Friday close at broker hour 21.

## Reputable-Source Gates

- R1 `PASS_WITH_COMPOSITE_TRANSLATION_RISK`: named authors, a complete Tier-A
  practitioner extraction, a complete-read peer-reviewed paper with DOI and
  natural-gas membership, and explicit carrier/conjunction limitations.
- R2 `PASS`: exact symbol, period, calendar sequence, uniform energy-label
  normalization, completed endpoints, component arithmetic, reconciliation,
  direction, attempt, entry grace, risk, spread, stop, and exit are locked.
- R3 `PASS`: registered native `XNGUSD.DWX` D1 OHLC and MT5 execution state
  provide every runtime input; Q02 owns current route and fill validation.
- R4 `PASS`: calendar, OHLC, logarithms, comparisons, ATR risk plumbing,
  quotes, positions, deals, and terminal state only; no trained output,
  banned signal indicator, external feed, grid, martingale, scale-in, or
  pyramid.

## Non-Duplicate And Portfolio Boundary

The canonical pre-card check scanned 4,545 registry rows and 625 root cards,
found no exact identity, and raised only expected flow-family neighbors.
Manual review returned
`CLEAN_XNG_WEEKLY_OVERNIGHT_SESSION_FLOW_AGREEMENT_CARRIER_AFTER_FAMILY_REVIEW`.

The frozen mechanic is a deliberate XNG carrier port of
`QM5_41029_wti-flow-agree`; no WTI result transfers. Same-symbol XNG systems
use different admitted states and clocks: monthly opposed-flow, a single
Thursday proxy, or close-return magnitude plus volatility. The certified
`QM5_12567` commodity edge is a long-only two-day cumulative-RSI pullback;
this identity is symmetric weekly continuation with no oscillator. The build
therefore tests a materially distinct logic, but Q09 alone may establish
realized portfolio correlation.

## Locked Build Contract

- exact `XNGUSD.DWX`, D1, slot 0, magic `410580000`;
- exact prior Monday-Friday sessions plus preceding-Friday anchor, accepting
  only native labels or one uniform `+1`-day governed energy convention;
- one durable broker-Monday attempt before all fallible entry gates and a
  180-minute opening grace;
- strict positive/positive BUY and negative/negative SELL component signs,
  with opposition, equality, invalid values, or failed reconciliation flat;
- frozen `3.0 * ATR(20,D1)` hard stop, no take-profit, and a 3,000-point XNG
  spread ceiling;
- `RISK_FIXED=1000`, `RISK_PERCENT=0`, `PORTFOLIO_WEIGHT=1`, both news axes
  OFF, framework Friday close enabled at broker hour 21; and
- later-week and eight-calendar-day stale repair, with no retry, scale-in,
  grid, martingale, pyramid, or external runtime data.

No parameter sweep, after-result threshold change, carrier substitution,
weekday repair, component resize, added filter, or lifecycle rescue is
authorized.

## Validation And Kill Boundary

Q01 must prove exact calendar and endpoint selection, component signs,
endpoint reconciliation, no current-bar leakage, durable one-attempt state,
fixed-dollar sizing, frozen stop, reachable management/close paths, strict
compile, card/schema/build checks, resolver identity, reference fixtures, and
static P1 validation.

Q02 retires the identity on zero trades, fewer than five completed positions
per full post-warm-up year, nonpositive governed economics, wrong endpoint or
direction, failed reconciliation, late/repeated entry, wrong lifecycle,
nondeterminism, invalid risk mode, or unusable XNG history. A weak result may
not be rescued by changing the mechanic.

This authorization excludes manual tester dispatch; live, demo, shadow,
stress, and optimization setfiles; terminal control; AutoTrading; `T_Live`;
deploy or T_Live manifests; portfolio-gate changes; portfolio admission;
decorrelation claims; and correlation waivers. If either governed capacity
ceiling is met, stop before queue mutation and record the non-live handoff.

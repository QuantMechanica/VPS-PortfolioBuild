# QM5_41397 WTI Monthly Seasonal-Surprise Reversion — G0 Decision

- Date: 2026-09-09
- Decision owner: OWNER
- Recorded by: Codex
- Decision: `APPROVED`
- Card: `strategy-seeds/cards/approved/QM5_41397_wti-seas-surprise-rv_card.md`
- EA ID: `QM5_41397`
- Slug: `wti-seas-surprise-rv`
- Strategy ID: `KELOHARJU-YANG-WTI-SEASSURPRISE-2026_S01`
- Source ID: `KELOHARJU-YANG-WTI-SEASSURPRISE-2026`
- Host / traded slot 0: `XTIUSD.DWX`, D1
- Intended magic: `413970000`

## Source Decision

The source approval was committed before extraction at `28e093d625`. The
bounded packet preserves a complete-read peer-reviewed Journal of Finance
same-calendar commodity lineage with explicit crude-oil membership and a
governed named-author academic commodity-reversal supplement. The exact
standardized WTI residual, threshold, next-month fade, and continuous-CFD
carrier remain untested QM translations.

R1 is
`PASS_WITH_ACADEMIC_SUPPLEMENT_CONJUNCTION_AND_CFD_RISK`. No source or sibling
return, significance, density, cost, drawdown, CFD-equivalence, or correlation
claim transfers.

## Mechanical Decision

R2 is `PASS`. On the first genuine normalized broker-month D1 boundary the
card consumes the month before fallible gates, reconstructs the just-completed
WTI monthly log return from completed endpoints, subtracts the arithmetic
mean for that same calendar month across up to ten earlier years while
excluding the realized year, and divides by the `n-1` sample deviation.

It sells only above `+0.50+1e-10`, buys only below `-0.50-1e-10`, and consumes
all other states flat. One fixed-dollar position carries a frozen
`3.5*ATR(20,D1)` stop and no target, then closes at the next broker-month
boundary; 40 days is stale repair. The 1,500-point spread ceiling admits exact
zero modeled spread and rejects crossed quotes. No parameter sweep or retry
is allowed.

## Data And Determinism

R3 is `PASS_WITH_ENERGY_LABEL_AND_CONTINUOUS_CFD_RISK`. Registered WTI D1
history, broker time, quotes, symbol metadata, positions, deals, and terminal-
global attempt state provide every runtime field.

R4 is `PASS`. The signal uses dates, completed prices, logarithms, arithmetic,
sample variance, square root, and comparisons; ATR is risk plumbing only. No
trained output, banned signal component, external runtime feed, grid,
martingale, scale-in, or pyramid is permitted.

## Non-Duplicate Decision

The pre-allocation checker found no exact identity across 4,877 registry rows
and 1,489 cards; its missing external Wiki root is explicit. Manual review
separates three expected fuzzy siblings:

- `QM5_41208`: same contrarian estimator, XNG carrier;
- `QM5_41209`: WTI carrier, continuation direction; and
- `QM5_41393`: XNG carrier, continuation direction.

The direct WTI carrier and contrarian side are jointly load-bearing. The
result is also distinct from raw one-month WTI reversal, upcoming-month
seasonal forecasts, seasonal pullbacks, and `QM5_12567` cumulative RSI.
Verdict:
`DISTINCT_WTI_STANDARDIZED_SEASONAL_SURPRISE_REVERSION_AFTER_FAMILY_REVIEW`.

## Portfolio Intent And Falsification

This is a month-scale direct-WTI stream intended to add a commodity/energy
driver outside the XAU/SP500/NDX/XNG book. Structural distinction is not a
correlation result; unchanged Q09 alone owns portfolio overlap.

Q02 retires on zero trades, fewer than five completed positions in any full
post-warm-up year, nonpositive governed economics, or any clock, label,
endpoint, exclusion, sample, scale, side, attempt, fixed-risk, stop, spread,
lifecycle, or determinism defect. No history, threshold, statistic, direction,
carrier, stop, hold, spread, or gate may change after results to rescue it.

## Authorized Scope

This approval permits deterministic slot-zero magic allocation, one branch-
only V5 build, one exact D1 `RISK_FIXED` backtest setfile, strict compile/Q01,
and one paced Q02 enqueue only below the hard CPU ceiling and only after the
binding PACER input-pin audit passes.

It does not permit a manual backtest, terminal control, live/demo/shadow/
stress/optimization setfile, AutoTrading, `T_Live`, deploy or live manifest,
portfolio-gate mutation, portfolio admission, or a correlation waiver.

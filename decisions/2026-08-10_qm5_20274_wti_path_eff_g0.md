# QM5_20274 WTI Monthly Path-Efficiency Trend G0 Authorization

Date: 2026-08-10

Authority: OWNER commodity/energy portfolio mission delivered to Codex on the
`agents/board-advisor` branch.

## Decision

Authorize one bounded V5 Strategy Card and non-live build for
`QM5_20274_wti-path-eff`. The candidate measures the absolute net WTI move
across twelve completed broker months relative to the sum of the absolute
month-to-month moves. It follows the net direction only when this path-
efficiency ratio is at least `0.25`.

The candidate may proceed through source/card lint, deterministic registry and
magic allocation, resolver regeneration, strict compile, one `RISK_FIXED`
backtest setfile, Q01 validation, and one paced Q02 enqueue. This authorization
does not pre-approve efficacy, diversification, decorrelation, certification,
execution-contract promotion, or portfolio admission.

## Source Boundary

The approved source of record is the already complete governed packet
`strategy-seeds/sources/MOP-TSMOM-2012/source.md`, covering Moskowitz, Ooi, and
Pedersen (2012), "Time Series Momentum," *Journal of Financial Economics*
104(2), 228-250, DOI `10.1016/j.jfineco.2011.11.003`. Its durable retrieval
receipt records an end-to-end read of the 23-page published paper, the author-
hosted route, page count, byte count, and PDF SHA-256.

The source supports testing monthly own-price direction in WTI over the first
twelve monthly lags. It does not prescribe a path-efficiency ratio, a `0.25`
threshold, a standalone continuous CFD port, fixed-dollar sizing, ATR stop,
spread ceiling, restart ledger, or lifecycle controls. Those are transparent
QM hypotheses. No source return, WTI-specific alpha, trade density, CFD
equivalence, correlation result, or portfolio conclusion transfers.

No newly retrieved public source is used. The bounded child extraction will be
recorded at `strategy-seeds/sources/MOP-WTI-PATHEFF-2026/source.md` only after
this durable approval exists.

## Locked Rule

On the first processed `XTIUSD.DWX` D1 bar of a genuine broker-month
transition, reconstruct exactly thirteen consecutive completed broker-month-
end closes `C[0]..C[12]`, oldest to newest. The newest endpoint must belong to
the month immediately before the decision month. Define twelve chronological
log returns and the path-efficiency statistic:

```text
r[i] = ln(C[i+1] / C[i]), i = 0..11
N    = sum(r[i])
P    = sum(abs(r[i]))
E    = abs(N) / P
```

Require `P > 0`, finite arithmetic, and `E` in `[0,1]` up to a fixed numerical
tolerance. Buy when `N > 0` and `E >= 0.25`; sell when `N < 0` and
`E >= 0.25`; otherwise consume the month flat. An exact-zero net return is
flat. Neither `E` nor return magnitude scales risk.

Require positive finite closes, consecutive completed months, strictly
increasing endpoint timestamps, finite logarithms, and no endpoint from the
current broker month. Consume and persist the decision month before history,
signal, spread, quote, ATR, sizing, news, or order gates. Close the prior
package at the next month boundary before considering replacement risk. Use
exactly one `RISK_FIXED=1000` stop-risk budget, one frozen
`3.5 * ATR(20,D1)` broker hard stop, a 1,500-point entry spread ceiling, no
take-profit, and a forty-calendar-day stale exit. Friday close and both news
axes are disabled for the full-month native-price package. The framework kill
switch remains binding.

## Non-Duplicate Decision

The canonical pre-allocation checker scanned 4,337 EA-registry rows and 447
cards for slug `wti-path-eff`, strategy ID
`MOP-TSMOM-2012_XTI_PATHEFF12_S23`, and the declared mechanic. It found no
exact identity and no fuzzy match above threshold.

Manual review distinguishes the candidate from the closest systems:

- pure one-, two-, three-, six-, nine-, and twelve-month WTI TSMOM uses the
  sign of one cumulative endpoint return without a path-quality threshold;
- `QM5_13150_wti-signmom` and `QM5_20244_wti-trend-sign` count positive and
  negative months but discard return magnitude and net/path geometry;
- `QM5_20258_wti-mom-vote` votes three nested cumulative horizons;
- `QM5_20269` and `QM5_20270` aggregate sorted monthly returns by a median or
  trimmed mean rather than comparing net displacement with chronological path
  length;
- `QM5_20271_wti-theilsen-tr`, `QM5_20261_wti-lr-trend`, and
  `QM5_20264_wti-rank-trend` estimate slope or order from monthly price levels;
- `QM5_20272_wti-qtrvote-tr` votes four fixed quarterly blocks; and
- `QM5_20273_wti-signrun-tr` measures the longest adjacent sign run and
  discards all return magnitudes.

Repository search found generic efficiency-ratio EAs, but no approved WTI or
commodity card applying this exact twelve-completed-month net-to-path rule.
The thirteen endpoints, adjacent-log orientation, absolute-path denominator,
fixed `0.25` threshold, symmetric direction mapping, consumed monthly attempt,
and renewal clock are jointly load-bearing. Verdict: `CLEAN`.

## Allocation And Kill Boundary

- intended EA ID: `QM5_20274`, subject to deterministic registry allocation;
- slug: `wti-path-eff`;
- strategy ID: `MOP-TSMOM-2012_XTI_PATHEFF12_S23`;
- intended slot: `XTIUSD.DWX` / 0 / magic `202740000`;
- expected cadence: approximately six monthly packages per full post-warm-up
year under a seeded zero-drift Gaussian-return design reference; Q02 owns observed density
  and economics;
- retire below five completed packages per full post-warm-up year, on
  nonpositive governed economics, or later portfolio-correlation rejection;
- fail on endpoint leakage or nonconsecutiveness, reversed return orientation,
  a missing absolute-path term, a denominator that is not the sum of all
  twelve absolute log returns, wrong threshold/direction, repeated monthly
  attempt, missing hard stop, risk-mode mismatch, hold beyond forty days, or
  nondeterminism;
- no post-result lookback, threshold, direction, stop, hold, spread, retry, or
  carrier rescue is authorized.

## Safety Boundary

This authorization excludes manual backtests; live, demo, shadow, stress, and
optimization setfiles; `T_Live`; AutoTrading; deploy or T_Live manifests;
portfolio admission; portfolio-gate edits; and correlation waivers. Q02 uses
exactly one `XTIUSD.DWX` D1 backtest setfile with `RISK_FIXED=1000`,
`RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1`. If the paced farm reaches its
binding backtest CPU ceiling before enqueue, record the stop and do not enqueue
or run a manual test.

# QM5_21524 WTI/Copper Twelve-Month Relative Momentum — G0 Decision

Date: 2026-08-14

Decision: `APPROVED` for one bounded V5 Strategy Card, one branch-only
non-live build, strict Q01 validation, and one paced non-live Q02 handoff if
the factory CPU ceiling permits.

Authority: OWNER commodity/energy portfolio mission delivered to Codex on
the `agents/board-advisor` branch and durably recorded before allocation in
`decisions/2026-08-14_wti_xcu_relmom_source_approval.md` at commit
`65db4e46a`.

## Candidate

- EA: `QM5_21524_wti-xcu-relmom`
- Strategy ID: `FMR-EIA-USGS-WTI-XCU-RELMOM-2026_S01`
- Source ID: `FMR-EIA-USGS-WTI-XCU-RELMOM-2026`
- host/traded symbol/slot/magic: `XTIUSD.DWX` / 0 / `215240000`
- companion/traded symbol/slot/magic: `XCUUSD.DWX` / 1 / `215240001`
- logical basket: `QM5_21524_WTI_XCU_RELMOM_D1`
- driver: arithmetic-average twelve-completed-month WTI-versus-copper return
  rank, long the higher and short the lower
- lifecycle: one consumed monthly attempt, equal fixed-risk halves, frozen
  per-leg `3.5 * ATR(20,D1)` stops, monthly renewal, forty-day stale guard,
  atomic repair, and fixed spread caps

## Source Decision

The approved composite packet is
`strategy-seeds/sources/FMR-EIA-USGS-WTI-XCU-RELMOM-2026/source.md`. It is
bound to:

- the complete governed read of Fuertes, Miffre, and Rallis (2010),
  *Journal of Banking & Finance* 34(10), 2530-2548, for twelve-month
  cross-sectional commodity momentum and one-month holding; and
- the governed official EIA/CME/USGS WTI/copper packet for the structural
  distinction between energy and industrial-base-metal exposure.

Neither source tests this two-CFD subset, synchronized broker-month mapping,
equal stop-risk halves, ATR stops, spread ceilings, attempt ledger, or QM
book. No source return, significance, drawdown, density, cost, hedge ratio,
CFD equivalence, neutrality, decorrelation, or portfolio result transfers.

## Locked Rule

At the first processed WTI D1 bar after each genuine broker-month transition:

1. Run lifecycle repair and close prior-month owned exposure before entry-only
   gates. Persist the new broker month as attempted before all fallible gates.
2. Reconstruct exactly thirteen consecutive common completed broker-month
   endpoints for WTI and copper. Require positive finite closes, exact
   timestamp agreement, chronology, consecutive months, and a newest endpoint
   no more than ten calendar days stale.
3. Calculate exactly twelve simple monthly returns for each leg and the
   arithmetic mean of each twelve-return vector.
4. Buy WTI and sell copper only when `avg12_wti-avg12_xcu > 1e-10`. Sell WTI
   and buy copper only when it is below `-1e-10`. Consume every tied,
   deadband, missing, stale, misaligned, or invalid state flat.
5. Open at most one opposite-leg package with aggregate `RISK_FIXED=1000`,
   exactly one $500 stop-risk half per leg, frozen `3.5 * ATR(20,D1)` hard
   stops, no targets, and WTI/copper spread caps of 1,500/1,200 points.
6. Open WTI then copper and immediately flatten all owned exposure after a
   partial-open or final-package validation failure.
7. Close both legs before monthly replacement or after forty calendar days.
   Immediately flatten malformed package state. Friday close and all news
   modes are OFF.

The endpoint count, exact common timestamps, simple-return arithmetic means,
twelve-month horizon, strict rank, traded-leg topology, equal risk halves,
monthly cadence, and no-retry policy are locked.

## Reputable-Source Criteria

- R1 `PASS`: complete peer-reviewed institutional manuscript evidence plus
  governed official EIA, CME, and USGS carrier references.
- R2 `PASS`: exact history, estimator, direction, lifecycle, risk, stops,
  spreads, attempt state, and atomic repair are fixed.
- R3 `PASS`: registered WTI and copper D1 data and existing basket routes
  supply every runtime input; Q02 owns synchronization and fill proof.
- R4 `PASS`: deterministic native arithmetic and state only, without trained
  output, prohibited signal indicator, external feed, grid, martingale,
  scale-in, or pyramid.

## Non-Duplicate Decision

The canonical pre-allocation checker scanned 4,396 EA-registry rows and 492
root cards and returned `CLEAN`. Manual review confirms:

- `QM5_13094` follows a daily WTI/copper price-level channel.
- `QM5_13090` fades a short-window WTI/copper standardized return spread.
- `QM5_12733` ranks WTI against natural gas using cumulative D1 returns and a
  percentage band.
- `QM5_20050` applies the twelve-month source horizon to gold/silver, not
  energy versus industrial base metal.
- `QM5_12567` is a long-only XNG cumulative-RSI pullback.

The exact WTI/copper month-end rank and package contract are not already
built. Verdict:
`CLEAN_WTI_COPPER_TWELVE_MONTH_CROSS_SECTIONAL_MOMENTUM_PACKAGE`.

## Allocation And Kill Boundary

The deterministic registry allocation reserves `QM5_21524`. Expected cadence
is approximately twelve completed packages per full post-warm-up year; Q02
must retire below five/year or on nonpositive governed economics. Q09 alone
may establish realized correlation with the certified XAU, SP500, NDX, and
XNG book. Equal opposite-leg stop risk is not proof of neutrality.

## Safety Boundary

Create exactly one logical-basket D1 backtest setfile with
`RISK_FIXED=1000`, `RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1`. This decision
excludes manual backtests; live, demo, shadow, stress, and optimization
setfiles; `T_Live`; AutoTrading; deploy or T_Live manifests; portfolio-gate
edits; portfolio admission; and correlation waivers. If the paced factory CPU
ceiling is binding before enqueue, stop without starting, stopping, reserving,
reaping, or reprioritizing a terminal.

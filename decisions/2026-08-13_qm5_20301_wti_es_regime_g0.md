# QM5_20301 WTI Self-Relative Expected-Shortfall Regime G0 Authorization

Date: 2026-08-13

Authority: OWNER commodity/energy portfolio mission delivered to Codex on the
`agents/board-advisor` branch.

## Decision

Authorize one bounded V5 Strategy Card and non-live build for
`QM5_20301_wti-es-regime`. At each genuine broker-month transition, the
candidate computes the source-defined historical expected-shortfall
characteristic over two consecutive, non-overlapping 252-return blocks of
`XTIUSD.DWX` D1 history. It buys WTI when the recent block's mean of its worst
five percent of returns is higher (less negative) than the preceding block's
value and sells WTI when it is lower (more negative).

The candidate may proceed through bounded source/card extraction, schema and
G0 lint, deterministic registry and magic allocation, resolver regeneration,
strict compile, one `RISK_FIXED` backtest setfile, Q01 validation, and one
paced Q02 enqueue. Authorization does not pre-approve efficacy,
diversification, decorrelation, certification, execution-contract promotion,
or portfolio admission.

## Source Boundary

The approved source is Qin, Cai, Zhu, and Webb (2025), "Commodity Futures
Characteristics and Asset Pricing Models," *Journal of Futures Markets*
45(3), 176-207, DOI `10.1002/fut.22559`. The governed packet at
`strategy-seeds/sources/YIYI-ES-2025/source.md` records a complete read of the
open prepublication paper, including appendices. It defines expected
shortfall as the arithmetic mean of the worst five percent of daily returns
over months t-12 through t-1, ranks commodities monthly, and holds the higher-
ES group against the lower-ES group during month t.

The source's broad-universe one-way high-minus-low result has a full-sample
t-statistic of only 1.36, and its stronger evidence is concentrated in the
early subsample. It studies 34 exchange-traded commodity futures, not an
outright WTI continuous CFD or an own-history comparison. The existing
two-energy-CFD sibling `QM5_13143_energy-es-rank` passed Q02 but failed Q04 in
all three OOS folds (`pf_net` 0.782, 0.314, and 0.000). That result is adverse
family evidence and is not transferred or waived.

The two-block self-relative comparison, outright WTI direction, simple CFD
returns, fixed-dollar sizing, ATR hard stop, spread ceiling, restart ledger,
and lifecycle controls are transparent QM mechanizations. The source does not
test any of them. No source return, alpha, significance, WTI-only effect,
trade density, CFD equivalence, sibling result, correlation result, or
portfolio conclusion transfers.

## Locked Rule

On the first processed WTI D1 bar after a genuine broker-month transition,
load exactly 505 completed closes, newest first. For block offset `b`, where
`b=0` is recent and `b=252` is preceding, calculate 252 simple returns, sort
the complete vector ascending, set the tail count to the mathematical ceiling
of five percent, and average exactly the lowest observations:

```text
r[b,k] = close[b+k] / close[b+k+1] - 1, k = 0..251
K      = ceil(252 * 0.05) = 13
ES[b]  = arithmetic_mean(13 smallest r[b,0..251])

BUY  when ES[0] > ES[252] + 1e-12
SELL when ES[0] < ES[252] - 1e-12
FLAT otherwise or when state is invalid
```

The blocks use disjoint return support: recent close-index pairs `0/1` through
`251/252`, and preceding pairs `252/253` through `503/504`. They share only
the boundary close at index 252. Require strictly decreasing series indices
to map to strictly older timestamps, positive finite closes, finite returns
and ES values, exactly thirteen selected order statistics per block, and a
newest completed endpoint before the decision bar and no more than ten
calendar days stale.

Do not substitute log returns, a quantile cutoff without tail averaging,
expected shortfall at another probability, semivariance, skewness, kurtosis,
MAX, winsorization, overlapping blocks, a fitted threshold, a trend or
calendar filter, score-sized risk, or an alternate direction. Consume and
persist the broker month before history, signal, spread, quote, news, ATR,
sizing, or order gates. Use one `RISK_FIXED=1000` WTI position with a frozen
`3.5 * ATR(20,D1)` hard stop, a 1,500-point entry-spread cap, and no take-
profit. Close before monthly replacement or after forty calendar days. Friday
close and both news axes are OFF. At most one registered WTI position exists.

## Reputable-Source Criteria

- R1: PASS with material caveats retained: named peer-reviewed source, DOI,
  publisher record, complete open-paper read, exact expected-shortfall
  transform, and disclosed weak full-sample and failed sibling evidence.
- R2: PASS: two fixed 252-return blocks, ceiling-based five-percent tail
  count, lower-tail arithmetic mean, offsets, direction, attempt ledger,
  risk, stop, rollover, and stale guard.
- R3: PASS with translation risk: registered `XTIUSD.DWX` D1 OHLC plus native
  framework state suffice; collateralized futures equivalence is not assumed.
- R4: PASS: deterministic native arithmetic only, without trained output,
  prohibited signal indicator, grid, martingale, scale-in, or pyramid.

## Non-Duplicate Decision

The canonical pre-allocation checker scanned 4,366 EA-registry rows and 477
root cards. It found no exact identity and surfaced five fuzzy source/name
neighbors. Manual mechanic review resolves them:

- `QM5_13143_energy-es-rank` applies the same source estimator concurrently
  to XTI and XNG, ranks two instruments, opens two opposite legs, and manages
  package risk and orphan state. It does not compare two disjoint WTI history
  blocks or trade one outright WTI leg. Its Q04 failure is adverse evidence,
  not an identity or a reason to alter this baseline.
- `QM5_20235_xauxag-es-rank` is a paired precious-metals rank with two magics;
  it has no economic Q02 verdict and currently retains infrastructure-only
  failures plus one pending row.
- `QM5_20300_wti-max-regime` uses the five largest positive returns in each
  block and the source's low-MAX direction. This candidate uses the thirteen
  smallest returns and the source's high-ES direction.
- `QM5_20289_wti-rsj-rev` uses one complete month and normalized upside-minus-
  downside squared returns, not a two-year lower-tail mean comparison.
- WTI skewness, kurtosis, VoV, cumulative-return trend, robust-location,
  calendar, event, breakout, variance-ratio, and ordinary reversal EAs use
  different information objects or clocks.
- `QM5_12567_cum-rsi2-commodity` is a short-horizon, long-only oscillator
  pullback rather than a monthly symmetric downside-tail regime on WTI.

The 252 simple returns per block, mathematical-ceiling thirteen-return lower
tail, disjoint offsets `0/252`, self-relative higher-ES-long direction,
outright WTI carrier, and consumed monthly lifecycle are jointly load-bearing.
Verdict: `CLEAN_AUTHORIZED_WTI_TIME_SERIES_ES_AFTER_MANUAL_REVIEW`.

## Allocation And Kill Boundary

- intended EA ID: `QM5_20301`, subject to deterministic registry allocation;
- slug: `wti-es-regime`;
- strategy ID: `YIYI-ES-2025_XTI_TS_S04`;
- intended symbol/slot/magic: `XTIUSD.DWX` / 0 / `203010000`;
- expected cadence: approximately eleven to twelve completed monthly WTI
  positions per full post-warm-up year;
- retire below five completed positions per year, on nonpositive governed
  economics, or at later portfolio-correlation rejection;
- fail on wrong block support, wrong return type or count, a tail count other
  than thirteen, wrong sort tail, overlapping blocks, reversed direction,
  retry, missing stop, risk mismatch, hold beyond forty days, or
  nondeterminism; and
- no post-result formation, offset, statistic, probability, direction, stop,
  hold, spread, retry, or carrier rescue is authorized.

The WTI carrier and downside-tail state are diversification hypotheses, not
correlation evidence. Q09 alone may establish realized overlap with the
current XAU/SP500/NDX/XNG book.

## Safety Boundary

This authorization excludes manual backtests; live, demo, shadow, stress,
and optimization setfiles; `T_Live`; AutoTrading; deploy or T_Live manifests;
portfolio admission; portfolio-gate edits; and correlation waivers. Q02 uses
exactly one `XTIUSD.DWX` D1 setfile with `RISK_FIXED=1000`,
`RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1`. If the paced factory reaches its
binding seven-terminal backtest CPU ceiling before enqueue, record the stop
and do not enqueue or run a manual test.

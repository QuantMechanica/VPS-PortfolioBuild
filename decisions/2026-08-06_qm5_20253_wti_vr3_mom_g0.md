# QM5_20253 WTI R3-q4 Memory Momentum G0 Authorization

Date: 2026-08-06

Authority: OWNER commodity/energy portfolio mission delivered to Codex on the
`agents/board-advisor` branch.

## Decision

Authorize one bounded V5 Strategy Card and non-live build for
`QM5_20253_wti-vr3-mom`. At the first tradable WTI D1 bar of each genuine
broker month, reconstruct 33 completed month-end closes, form 32 monthly log
returns, and apply Mehlitz and Auer's source-declared `R3-q4` memory-enhanced
momentum state. Significant persistence follows the latest completed
three-month return; significant anti-persistence reverses it; insignificant
memory consumes the month flat. An actionable state opens one WTI position
and closes it at the next broker-month transition.

The candidate may proceed through card lint, directory-first registry and
magic allocation, resolver regeneration, strict compile, one `RISK_FIXED`
backtest setfile, Q01 validation, and one paced Q02 enqueue. This authorization
does not pre-approve efficacy, diversification, decorrelation, certification,
execution-contract promotion, or portfolio admission.

## Source Boundary

The approved source of record is
`strategy-seeds/sources/MEHLITZ-AUER-WTI-R3Q4-2026/source.md`. Its completely
reviewed parent is `strategy-seeds/sources/MEHLITZ-AUER-MEM-2024/source.md`,
covering Mehlitz and Auer (2024), *The European Journal of Finance* 30(8),
773-802, DOI `10.1080/1351847X.2023.2220118`, and the complete open precursor
chapter and appendix.

The source supplies the 32-return sample, `R3-q4` pairing, general
Lo-MacKinlay heteroskedasticity-robust variance-ratio statistic, fixed
two-sided 10% critical value, and persistence/anti-persistence direction
matrix. WTI is explicitly in the tested commodity universe. The continuous
CFD carrier, D1 month-end reconstruction, ATR stop, spread cap, fixed-risk
sizing, restart ledger, and stale exit are disclosed QM controls. No source
performance or portfolio statistic transfers.

## Non-Duplicate Decision

The pre-allocation checker scanned 4,310 registry rows and 427 canonical cards.
It found no exact collision and one expected fuzzy source sibling,
`QM5_13134_energy-vr-mom`. Manual review resolves the sibling:

- `QM5_13134` is the paper's `R1-q2` rule: latest one-month return sign,
  first-order autocorrelation only, `VR(2)=1+rho(1)`, and the q=2 robust
  variance term.
- `QM5_20253` is the separately source-declared `R3-q4` rule: the sign of the
  sum of the latest three completed monthly log returns, autocorrelation lags
  one through three, `VR(4)=1+1.5*rho(1)+rho(2)+0.5*rho(3)`, and robust
  weights `2.25`, `1.0`, and `0.25`.
- Plain WTI three-month momentum builds never estimate a variance ratio,
  require significance, or reverse momentum in an anti-persistent state.
- Recent `q=2` WTI composite cards keep the one-month q=2 memory object and
  add calendar or return-sign filters; none computes `R3-q4`.

The ranking horizon, q order, autocorrelation set, variance-ratio weights,
robust-variance weights, and resulting four-state path are jointly
load-bearing. Verdict: `CLEAN_AFTER_EXPECTED_SOURCE_SIBLING_REVIEW`.

## Allocation And Kill Boundary

- EA ID: `QM5_20253`
- slug: `wti-vr3-mom`
- strategy ID: `MEHLITZ-AUER-MEM-2024_XTI_R3Q4_S02`
- slot 0: `XTIUSD.DWX` / magic `202530000`
- cadence: estimated 5-9 completed positions/year after warm-up; Q02 owns the
  realized density verdict
- retire below five completed trades per full post-warm-up year
- retire on wrong/nonconsecutive month ends, wrong R3 return, wrong q4 or
  robust weights, missing significance gate, wrong direction matrix,
  repeated monthly attempt, risk-mode breach, nonpositive governed economics,
  or later portfolio-correlation rejection
- no post-result horizon, q, window, critical value, direction, retry, stop,
  spread, or carrier rescue is authorized

## Safety Boundary

This authorization excludes manual backtests; live, demo, shadow, stress, and
optimization setfiles; `T_Live`; AutoTrading; deploy or T_Live manifests;
portfolio admission; portfolio-gate edits; and correlation waivers. Q02 uses
exactly `RISK_FIXED=1000`, `RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1`.

# QM5_20261 WTI Linear-Trend Quality G0 Authorization

Date: 2026-08-07

Authority: OWNER commodity/energy portfolio mission delivered to Codex on the
`agents/board-advisor` branch.

## Decision

Authorize one bounded V5 Strategy Card and non-live build for
`QM5_20261_wti-lr-trend`. On the first D1 bar of each genuine broker-month
transition, the candidate reconstructs thirteen consecutive completed WTI
month-end closes, fits an ordinary least-squares line to their log prices, and
trades the slope direction only when the fixed regression fit is at least
`R^2 = 0.50`. A weak, flat, or invalid fit consumes the month without entry.

The candidate may proceed through source/card lint, deterministic registry and
magic allocation, resolver regeneration, strict compile, one `RISK_FIXED`
backtest setfile, Q01 validation, and one paced Q02 enqueue. This authorization
does not pre-approve efficacy, diversification, decorrelation, certification,
execution-contract promotion, or portfolio admission.

## Source Boundary

The approved source of record will be
`strategy-seeds/sources/MOP-WTI-LRTREND-2026/source.md`, bounded by the
completely reviewed repository packet
`strategy-seeds/sources/MOP-TSMOM-2012/source.md`:

- Moskowitz, Tobias J.; Ooi, Yao Hua; and Pedersen, Lasse Heje (2012),
  "Time Series Momentum," *Journal of Financial Economics* 104(2), 228-250,
  DOI `10.1016/j.jfineco.2011.11.003`.

The governed parent records a complete read of the 23-page published paper,
including its positive own-return continuation at monthly lags through twelve,
monthly trend strategy, and explicit WTI membership. The paper does not use an
OLS log-price path or an `R^2` gate. Those are transparent QM mechanizations of
trend direction and path consistency, not source claims. No source return,
alpha, Sharpe ratio, drawdown, trade count, cost, CFD equivalence, or portfolio
correlation transfers.

## Locked Rule

At the start of broker month `t`, take the thirteen consecutive completed
month-end closes from months `t-13` through `t-1`, ordered oldest to newest.
For `i = 0..12`, define:

```text
x_i   = i
y_i   = ln(close_i)
x_bar = 6
y_bar = average(y_i)
Sxx   = sum((x_i - x_bar)^2)
Sxy   = sum((x_i - x_bar) * (y_i - y_bar))
Syy   = sum((y_i - y_bar)^2)
beta  = Sxy / Sxx
R2    = (Sxy * Sxy) / (Sxx * Syy)
```

Require positive finite closes, consecutive month keys, finite arithmetic,
`Sxx > 0`, `Syy > 0`, `abs(beta) > 1e-10`, and `R2 >= 0.50`. Buy WTI when
`beta > 0`; sell WTI when `beta < 0`; otherwise remain flat. Close before the
next monthly decision and do not retry within a consumed month.

## Non-Duplicate Decision

The deterministic pre-allocation checker scanned 4,318 registry rows and 435
cards. It found no exact slug or strategy-ID collision and returned five
expected fuzzy neighbors caused by shared WTI/trend words or the common
Moskowitz-Ooi-Pedersen source family. Manual review resolves them:

- `QM5_12603_wti-tsmom12m` uses only the sign of one endpoint-to-endpoint
  twelve-month return; it does not measure the intervening price path.
- `QM5_20056_wti-dual-mom` and `QM5_20258_wti-mom-vote` combine fixed
  cumulative-return horizons; neither fits a price-time regression or requires
  path fit.
- `QM5_13150_wti-signmom` counts binary monthly return signs, while
  `QM5_20244_wti-trend-sign` requires that count to agree with cumulative
  return. Neither uses slope or regression residual dispersion.
- WTI calendar, variance-ratio, channel, moving-average, pullback, reversal,
  inventory/event, and relative-value builds use different state variables and
  clocks.

A content-level scan of every intake card found no existing WTI log-price OLS
slope plus fixed `R^2` quality gate. The thirteen synchronized endpoints,
oldest-to-newest regression orientation, slope direction, fixed fit threshold,
flat weak-trend state, consumed monthly attempt, and one-month renewal are
jointly load-bearing. Verdict:
`CLEAN_AFTER_EXPECTED_FUZZY_AND_MANUAL_REVIEW`.

## Allocation And Kill Boundary

- intended EA ID: `QM5_20261`, subject to deterministic registry allocation;
- slug: `wti-lr-trend`;
- strategy ID: `MOP-TSMOM-2012_XTI_LR12R2_S14`;
- intended slot 0: `XTIUSD.DWX` / magic `202610000`;
- cadence: at most one WTI position per broker month after thirteen completed
  month ends; Q02 owns realized density and economics;
- retire below five completed trades per full post-warm-up year;
- retire on wrong endpoint order, nonconsecutive months, wrong OLS arithmetic,
  entry below the fixed fit threshold, repeated monthly attempt, invalid risk
  mode, missing hard stop, nondeterminism, or nonpositive governed economics;
- no post-result lookback, fit threshold, direction, stop, spread, retry, or
  carrier rescue is authorized.

## Safety Boundary

This authorization excludes manual backtests; live, demo, shadow, stress, and
optimization setfiles; `T_Live`; AutoTrading; deploy or T_Live manifests;
portfolio admission; portfolio-gate edits; and correlation waivers. Q02 uses
exactly one `XTIUSD.DWX` D1 setfile with `RISK_FIXED=1000`,
`RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1`.

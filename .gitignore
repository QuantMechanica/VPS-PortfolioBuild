.private/
.claude/
.claude/settings.local.json
*.log
Thumbs.db
Desktop.ini
desktop.ini
*.env
*.key
*.pem
infra/reports/*.json
__pycache__/
**/__pycache__/
*.pyc
**/*.pyc
.claude/scheduled_tasks.lock
.tmp/
.gemini/
framework/registry/*.tmp

# Local one-off audit and queue helper files. Keep already tracked scratch
# fixtures, but do not surface new ad hoc scripts in normal worktree status.
.scratch/*
!.scratch/qua1540/
!.scratch/qua1582_empty_pipeline_root/
!.scratch/qua224/
!.scratch/all-issues.json
!.scratch/co-issues.json
!.scratch/compile_bars_startup.ini
!.scratch/mt5_queue_proto.db
!.scratch/open-issues.json
!.scratch/parent_id.txt
!.scratch/qm_orchestrator_producer_smoketest.py
!.scratch/qua-696-child-comment.json
!.scratch/qua-696-child-description.md
!.scratch/qua-696-disposition-comment.md
!.scratch/qua-696-disposition.json
!.scratch/qua-696-rc.json
!.scratch/qua-696-status-flip.json
!.scratch/qua-696-status-flip.md

# Artifact retention policy (historical QUA-186 record, current authority OWNER)
framework/build/
framework/build/step23_test/sample_report.htm
framework/build/step23_test/sample_report.htm.bak
framework/templates/EA_Skeleton.ex5
framework/tests/smoke/QM5_1001_framework_smoke.ex5
framework/EAs/*/QUA-*
framework/EAs/*/*.signal

# Generated optimizer/backtest setfile variants. Baseline *_backtest.set files
# remain trackable; grid/synth/ablation variants belong in report storage.
framework/EAs/*/sets/*_grid_*.set
framework/EAs/*/sets/*_synth_*_backtest.set
framework/EAs/*/sets/*_ablation_*.set

# Node tooling — never commit npm install footprint
node_modules/
package.json
package-lock.json

# Per-cycle scratch dirs from agents and audits
.codex_tmp/
tmp_audit/

# pip install --target artifacts (pypdf + other research deps installed into repo root)
Lib/
scripts/pip*.exe
.tmp_*.pdf
.tmp_*.html


# PDF-mining binary tool (kept on disk, not tracked)
scripts/pymupdf.exe
# stray Antigravity CLI installer
install.cmd

# Antigravity channel-harvest army: transient working files (final reconstruction is committed)
docs/research/unconventional_forex/logs/
docs/research/unconventional_forex/_*
docs/research/unconventional_forex/HARVEST_PROGRESS.log
docs/research/unconventional_forex/batch_*.md
# whole harvest working dir (final reconstruction force-added when done); tracked VIDEO_MANIFEST stays
docs/research/unconventional_forex/
# pip console-script binaries that land in scripts/
scripts/*.exe
# agy scratch helper scripts dropped at repo root during channel harvest
/get_batch*.py
/fetch_batch*.py
/*_batch_*.py

# T-WIN harvest scratch working dir (transient extraction scripts + dumps)
/scratch/
# transient build-retry attempt artifacts (build_result renamed across retries)
artifacts/*build_result.attempt*.json

# stray repo-root runtime DB (real state DB lives on D:\QM\strategy_farm\state) + build retry attempts
state/
artifacts/*attempt*.json
artifacts/**/*attempt*.json

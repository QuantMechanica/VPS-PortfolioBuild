# QUA-14 Day-1 T1 Dry-Run Evidence (2026-04-26)

## Scope and guard

- Issue: QUA-14 (`dry-run-only`, native symbol only)
- Terminal used: `T1` only
- Symbol: `EURUSD` (native, not `.DWX`)
- Window: `2017-01-01` to `2022-12-31`
- Model: `4` (fixed risk)

## Execution attempts

1. Primary config: `D:\QM\mt5\T1\tester_qua14_sm345_20260426_210618.ini`
2. Fallback config: `D:\QM\mt5\T1\tester_qua14_sm186_20260426_210650.ini`

Both runs were executed by controlled T1-only restart/restore:

- killed T1 PID: `11432` then `8272`
- run PIDs: `13480` (`SM_345`) and `15192` (`SM_186`)
- restored T1 PIDs: `8272` then `12588`

## Result classification

- `SM_345`: `SETUP_DATA_EX5_NOT_FOUND`
  - tester log: `Experts\Advisors\SM_345.ex5 not found` at `21:06:22.669`
  - terminal log: `tester didn't start`, `shutdown with -1000012355 (tester EX5 not found)`
- `SM_186`: `SETUP_DATA_EX5_NOT_FOUND`
  - tester log: `Experts\Advisors\SM_186.ex5 not found` at `21:06:55.674`
  - terminal log: `tester didn't start`, `shutdown with -1000012355 (tester EX5 not found)`

No `.htm` report was produced for either attempt:

- report roots: `D:\QM\reports\smoke_20260426\qua14_sm345_20260426_210618` and `...sm186_20260426_210650`
- `.htm` count at `D:\QM\reports\smoke_20260426`: `0`

## Filesystem-truth / tracker check

- `last_check_state.json`: not found on host paths previously checked in QUA-14.
- Therefore tracker parity cannot be computed; source of truth is filesystem counts/logs for this run.

## Aggregator state for this run

- `aggregator: not_operational`
- restore/rewire work tracked separately as child issue under QUA-14.

## NO_REPORT root-cause probe (same date, no full rerun)

To isolate report-chain behavior without repeating the full dry-run window, a short T1 probe matrix was executed on `EURUSD H1` with `ExpertMACD`:

1. `Report=D:\QM\reports\smoke_report_probe\probe_abs` (absolute path)
2. `Report=probe_rel` (relative name)
3. `Report=probe_relhtm.htm` (relative name with extension)

Observed outcome:

- Case 1: tester run passed, but no `.htm` artifact emitted.
- Case 2: emitted `D:\QM\mt5\T1\probe_rel.htm` (`28490` bytes).
- Case 3: emitted `D:\QM\mt5\T1\probe_relhtm.htm` (`28514` bytes).

Conclusion:

- MT5 report export on this host emits reliably for relative `Report=` values.
- Absolute `Report=` path is the direct cause of prior `NO_REPORT` artifacts on successful tester runs.

## Heartbeat follow-up (2026-04-26 21:29 Europe/Berlin)

- Factory terminal status snapshot:
  - `T1=14496`, `T2=4800`, `T3=7840`, `T4=13884`, `T5=13736`
- Disk free (`D:`): `444.19 GB`
- Aggregator recovery:
  - restarted `C:/QM/repo/scripts/aggregator/standalone_aggregator_loop.py`
  - active writer PID: `10228`
  - `D:/QM/reports/state/last_check_state.json` updated at `21:29:34`, iteration `2 -> 3`
  - `C:/QM/logs/aggregator/heartbeat.txt` updated at `21:29:34`
- Filesystem-truth parity at tick:
  - `D:/QM/reports` `.htm` count = `0`
  - tracker `report_htm_total` = `0`
  - parity result: `match`

# QM5_41395 XNG Winter Weekly Momentum — Build / Q02 CPU Stop

Date: 2026-09-09 (`2026-09-09T09:35:41.0901267Z`)

Branch: `agents/board-advisor`

## Outcome

`QM5_41395_xng-winter-wmom` is a new approved, allocated, committed
low-frequency natural-gas sleeve. At the first eligible D1 bar of a normalized
Monday-anchored week in November, December, January, February, or March, it
follows the strict sign of the immediately completed adjacent three-to-five-
session week's open-to-close log return. It holds through that normalized week,
uses a frozen `3.5*ATR(20,D1)` hard stop, and consumes each weekly attempt
before fallible gates.

Q02 was not enqueued because the binding whole-host CPU ceiling was reached.
The governed compile work item was active on T4 at stop and had not yet emitted
a verdict or EX5.

## Source And Non-Duplicate Boundary

The reputable-source packet combines locally preserved official U.S. Energy
Information Administration winter-demand context with the completely read
Moskowitz-Ooi-Pedersen (2012) peer-reviewed time-series-momentum lineage. The
exact weekly seasonal conjunction is disclosed as an untested QM translation.

Canonical dedup covered 4,875 registry rows and 1,487 repository cards. The
configured Strategy Wiki root was unavailable and is not claimed as checked.
The checker raised only `QM5_41392` as a fuzzy family neighbor. Manual review
separates that shoulder-season contrarian rule from this winter continuation.
`QM5_12567` remains materially different: a long-only two-day cumulative-RSI
pullback above a slow trend filter. Realized correlation belongs to Q09.

## Build And Guard Evidence

- Card schema lint: PASS; G0 lint: PASS; prohibited-ML hits: zero.
- EA/magic: `QM5_41395`, XNG slot zero, magic `413950000`.
- Source SHA-256:
  `d9185ec1665c9507383d4f357dfcadd5d76cc21310daf6c90fed33176120fa69`.
- Deterministic reference checks: 13 PASS, zero failures.
- SPEC validation: PASS.
- PACER audit ran after source generation and immediately before compile
  enqueue: exit zero, `ok=true`, predicate `EA_FRAMEWORK_INPUT_PINNED`,
  `hit_count=0`, empty findings.
- The locked guard compares only strategy inputs, `qm_ea_id`, slot, and
  fixed-risk mode. It does not compare RNG, news, or Friday-close inputs.
  Stress rejection is checked only for finiteness and inclusive `0..1`.
- Backtest preset binds `strategy_symbol=XNGUSD.DWX`, `RISK_FIXED=1000`, and
  `RISK_PERCENT=0`.

The live-factory guard correctly refused an ad-hoc build check while terminals
were alive. Governed compile work item
`6595b8b8-5e6e-43ea-bfb2-48402e755738` was enqueued, its rollout hold was
released through the canonical CAS path, and T4 claimed it. At stop it remained
`active`, with no compile verdict, evidence path, EX5, or build-hash rewrite.

## Binding CPU Stop

The final five one-second whole-host samples were `97.8539326920157%`,
`86.8313772795413%`, `78.5502120729899%`, `84.8875153129533%`, and
`91.4200825429243%`. Average CPU was `87.9086239800849%`; maximum CPU was
`97.8539326920157%`. Admission requires both average and maximum to be strictly
below `97%`, so the maximum refused the Q02 mutation. No Q02 enqueue, dispatch,
or manual tester command ran.

Machine-readable evidence is
`artifacts/qm5_41395_q02_cpu_ceiling_stop_20260909T093541Z.json`.

## Safety Boundary And Continuation

No portfolio gate, portfolio-admission state, deploy/live manifest, `T_Live`
surface, AutoTrading control, or manual MT5 tester surface was touched.

A later paced wake must first confirm that the governed compile row finished
source-matched `COMPILE_OK`, then run the canonical first-Q02 dry-run and a fresh
five-sample CPU gate. Apply exactly one Q02 canary only when both average and
maximum remain strictly below 97%.


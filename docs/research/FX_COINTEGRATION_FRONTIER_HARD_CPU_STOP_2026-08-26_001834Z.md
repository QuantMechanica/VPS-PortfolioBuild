# FX cointegration frontier: five-slot hard CPU stop

Date: 2026-08-26 UTC (`2026-08-26T00:18:34Z`), Europe/Berlin

Branch: `agents/board-advisor`

Observation base: `ce8eaa8a88bf41b3540efa7aa510fddfc6325e68`

Status: no non-duplicate unbuilt scan pair; stopped at the explicit backtest
CPU ceiling before any card, build, compile, queue, dispatch, or tester action

## Outcome

The bounded source result in
`docs/research/CROSS_ASSET_FX_DISCOVERY_2026-06-09.md` admitted only the two
original strict survivors. The durable sign-aware coverage audit in
`artifacts/fx_cointegration_frontier_cpu_stop_20260812T112137Z_board_advisor.json`
accounts for all 66 relationships: 66 covered and zero uncovered. A new
scan-derived identity would duplicate governed work or weaken the mission's
reputable-source criterion.

The latest durable supported work-item query, recorded 46 minutes earlier in
`artifacts/fx_cointegration_frontier_hard_cpu_stop_20260825T233158Z_board_advisor.json`,
shows that the preferred anchors do not have Q02 infrastructure blockers:

- `QM5_12532_AUDNZD_COINTEGRATION_D1` has Q02 PASS and Q04 PASS, followed by
  Q05 FAIL.
- `QM5_12533_EURJPY_GBPJPY_COINTEGRATION_D1` has Q02 PASS, followed by Q04
  FAIL.

The existing-pair fallback remains `QM5_20219_usdjpy-nzdusd`. Its latest
durable state is Q02 PASS with one unique Q03 successor already pending at
zero attempts and a preexisting Q04 row pending. Creating a competing row
would be duplicate work. The ceiling bound before another work-item query, so
no newer lineage claim is made here.

## Binding capacity stop

Five fresh one-second whole-host CPU readings were `100.00%`, `99.90%`,
`98.94%`, `99.81%`, and `97.96%`. Their average was `99.32%` and their maximum
was `100.00%`. The explicit ceiling binds when either measure is at least
`97%`; both measures required an immediate stop.

The supported `farmctl mt5-slots` snapshot at `2026-08-26T00:16:49Z` saw five
governed factory terminals actively testing: T4, T5, T6, T7, and T8. The
paced launch gate remained `1`, so active occupancy was five times that gate.
Five matching reservations and all ten terminal-worker daemons were present,
with no orphaned factory terminal. T5 was a governed pipeline run without a
work-item ID; the other four processes were bound to work items. `T_Live` and
the unrelated FTMO terminal were observed only to exclude them from the
factory count; neither was controlled.

Per the mission stop condition, no further candidate search, fresh work-item
query, card or EA creation, compile, build check, queue mutation, dispatch
tick, tester launch, terminal reservation, terminal control, or backtest
followed. Machine-readable evidence is in
`artifacts/fx_cointegration_frontier_hard_cpu_stop_20260826T001834Z_board_advisor.json`.

## Non-duplicate operational delta

The preceding receipt saw T4, T6, T7, and T8. T5 has now joined that roster as
a governed pipeline run, increasing active factory occupancy and reservations
from four to five. Average CPU eased from `99.80%` to `99.32%`, while the
maximum remained `100.00%`. This changed capacity state is the durable delta;
no duplicate pair or pipeline work item was created.

## Safety

- No portfolio-admission, portfolio-KPI, or Q08-contribution path changed.
- No T_Live manifest, terminal, AutoTrading state, or live artifact changed.
- No Strategy Card, EA, EX5, setfile, basket manifest, registry row, or magic row changed.
- Concurrent unrelated worktree changes were preserved and excluded from this receipt.

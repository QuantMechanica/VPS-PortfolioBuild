# QM5_41384 Q02 admission — paced CPU-ceiling stop

Date: 2026-09-08 (`2026-09-08T13:20:21.8473454Z`)

Branch: `agents/board-advisor`

Observation base: `b50aad766947d784d4a054ab9286c9077f2b82e1`

Status: stopped before Q02 enqueue or dispatch because the binding CPU ceiling
was reached in the final admission window.

## Candidate and deterministic admission result

`QM5_41384_wti-tsmom2-h2` is the newest compiled low-frequency WTI diversity
candidate. Its governed `COMPILE_EA` work item
`6a063c3e-c161-4069-90fe-2bbefc258f08` is `done/COMPILE_OK`, with source hash
`5b81bb16c6097eedc76858716bf9b441379033f900a122e3baab7dfab042d880`
and EX5 hash
`01c5f5e685589b7da9eed68a0426cd8c38718e11d05784823189ee1a7325feb0`.
The existing Q01 receipt records a zero-finding PACER framework-input-pin audit,
13/13 deterministic reference vectors, zero compiler errors/warnings, and the
backtest risk contract `RISK_FIXED=1000`, `RISK_PERCENT=0`.

The canonical dry run was:

```text
python tools/strategy_farm/farmctl.py intake-first-q02 --compile-work-item-id 6a063c3e-c161-4069-90fe-2bbefc258f08
```

It returned `eligible=true`, `would_enqueue=true`, reason `ELIGIBLE`, one
`XTIUSD.DWX` D1 canary, active magic `413840000`, and the canonical setfile hash
`8b67c1ec178363567f0f82cc8fea8f7e677b9f8dcf55381ce1810548b3cf3cca`.

## Binding capacity result

The initial five-sample whole-host CPU window was admitted: `89.715%`,
`91.182%`, `89.271%`, `86.195%`, and `88.968%` (average `89.066%`, maximum
`91.182%`). Immediately before the apply step, the required fresh window read
`92.775%`, `92.050%`, `97.266%`, `96.680%`, and `97.266%` (average `95.207%`,
maximum `97.266%`). Admission requires both average and maximum to be strictly
below `97%`; the final maximum therefore refused the mutation.

At the stop, the farm DB held eight active work items across T1, T2, T3, T4,
T5, T6, T9, and T10. A post-refusal read-only query confirmed that
`QM5_41384` still had zero Q02 rows.

## Mutation and safety boundary

No `--apply` command ran. No build task, compile row, Q02 row, priority mark,
dispatch tick, terminal process, or worker control was created or changed. No
EA source, EX5, setfile, card, registry, magic resolver, portfolio gate,
portfolio-admission state, deploy/live manifest, `T_Live` surface, or
AutoTrading control was touched.

Machine-readable evidence:
`artifacts/qm5_41384_q02_cpu_ceiling_stop_20260908T132021Z.json`.

## Continuation condition

On a later paced wake, repeat the exact zero-Q02 collision check, rerun the
canonical dry run, and take a fresh five-sample CPU window. Apply exactly one
first-Q02 canary only when both average and maximum remain strictly below 97%.


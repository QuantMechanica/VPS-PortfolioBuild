# Review — QM5_9933 bandy-choppiness-index-sideways-rsi2-mr-index

- Router task: 0ad560e3-9dbb-41b7-908a-9e3073fed0c0 (review_ea, assigned codex; source agent gemini/agy)
- Source build task: c10c1e2c-6932-4db8-afdd-40907ea34246
- Source verdict: SOURCE_READY_FOR_REVIEW (static build guardrails + symbol scope PASS)
- Reviewer: Claude review lane · Date: 2026-08-24
- Verdict: **APPROVED (clean for Q02)**

## Scope
mq5, SPEC.md, embedded strategy card (docs/strategy_card.md), setfiles, build/compile
evidence, registry rows. Read-only.

## Card-faithful mechanism — PASS
Long-only equity-index MR: CI(14) >= 61.8 AND RSI(2) <= 10 AND close > SMA(200), one
position per magic; exit on RSI(2) >= 70 OR 6-bar time stop OR 2.5*ATR(14) catastrophic
stop. All match the card Mechanik section.
- CI formula `100*log10(sum(TR,14)/(HHV-LLV))/log10(14)` implemented from primitives
  (Strategy_CalculateChoppinessIndex, .mq5:58-135); TR computed bar-by-bar, no iCustom.
- Degenerate-bar guard present: `range <= 0.0 || sum_tr <= 0.0` -> return false (.mq5:113-114),
  as the card requires.
- Bounded buffer: CopyRates depth = ci_period+1 (15 bars), perf-allowed comment present
  (.mq5:96). Cached once per D1 calendar key. No unbounded raw series.

## Dead-input scan — PASS (every strategy input has a use-site)
- strategy_ci_period .mq5:81,88,92 (calc) + :161 (filter)
- strategy_ci_threshold .mq5:197 (entry) + :162 (filter)
- strategy_rsi_period .mq5:201 (entry) + :245 (exit) + :164
- strategy_rsi_entry_threshold .mq5:202 (entry) + :165
- strategy_rsi_exit_threshold .mq5:247 (exit) + :166
- strategy_regime_sma_period .mq5:206 (entry) + :167
- strategy_atr_period .mq5:213 (entry) + :168
- strategy_atr_stop_mult .mq5:216 (entry) + :169
- strategy_time_stop_bars .mq5:253 (exit) + :170
- strategy_warmup_bars .mq5:156 (filter) + :186 (entry)
No dead inputs.

## Framework conformity — PASS
- QM_Common chain: `#include <QM/QM_Common.mqh>` (.mq5:5).
- MAE hook: QM_FrameworkTrackOpenPositionMae() first in OnTick (.mq5:~300).
- Risk: RISK_FIXED=1000, RISK_PERCENT=0 (backtest correct); setfiles risk_mode FIXED.
- Magic via framework: QM_FrameworkMagic(); registry rows 99330000+slot = ea_id*10000+slot,
  13 active rows (slots 0-12) all present in magic_numbers.csv.
- News wired: QM_NewsAllowsTrade2 / legacy fallback in OnTick; kill-switch + Friday-close wired.
- No ML, no invented commission/swap/DST values.

## Build evidence — PASS
- compile.log: `Result: 0 errors, 0 warnings` (COMPILE_OK) for current source.
- Source hash unchanged since build: sha256 1136450b... matches build_identity.json.
- Setfiles are governed-generator output (proper QM5 Set File header, magic_slot matches
  registry, RISK_FIXED/RISK_PERCENT correct, all strategy params emitted).

## Non-blocking observations (do not gate Q02)
1. Symbol scope: registry/setfiles cover 13 symbols (indices + FX majors + gold) vs the
   card's canonical 3 index targets (SP500/NDX/WS30). Registry-governed by the Codex
   allocator (2026-08-17). Long-only RSI2 MR on FX majors is off-thesis vs the card's
   equity-index-drift rationale; Q02 per-pair economics will filter these. Note for
   downstream selection, not a code defect.
2. SPEC.md cosmetic: slug typo "andy-choppiness..." (missing b) and RISK_FIXED renders as
   ",000" (encoding artifact); references cards_approved path that resolves to none_found
   (card lives in EA docs/). Documentation-only; no behavioural impact.
3. Entry rejects RSI==0.0 as not-ready (readiness idiom); a theoretically maximal-oversold
   bar would be skipped — negligible with real data.

## Verdict
APPROVED — card-faithful, no dead inputs, framework-conformant, compiles clean at current
hash, setfiles governed. Ready for Q02.

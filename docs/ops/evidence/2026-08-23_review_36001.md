# Claude review_ea: QM5_36001 (nnfx-classic-mcginley-ssl-wae)

- Router task: `2a6ee952-e292-4ace-a5d8-01c6340da256` (state REVIEW, assigned claude)
- Source task: `3c1da904-b03a-40d5-a1b3-c23e9ffad4b8` (build_ea, source_agent `gemini`/agy, source_verdict `REMEDIATED_AWAITING_CODEX_REVIEW`)
- Source artifact: `D:/QM/strategy_farm/artifacts/builds/3c1da904-b03a-40d5-a1b3-c23e9ffad4b8.json`
- Reviewed source: `framework/EAs/QM5_36001_nnfx-classic-mcginley-ssl-wae/QM5_36001_nnfx-classic-mcginley-ssl-wae.mq5` @ HEAD (sha256 `03be79d0e3dfbf52a8d2169ab22c9107271ab3ad713faeed1ebe908e1bfb43ac`)
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_36001_nnfx-classic-mcginley-ssl-wae.md`
- Prior Codex review: `docs/ops/evidence/d47d0803_qm5_36001_gemini_build_codex_review_2026-08-18.md` (CHANGES_REQUIRED, 6 findings)
- Remediation commits: `1c8d911f9`, `73acf69db`

## VERDICT: RECYCLE

Source is card-faithful and formally clean; **the tracked `.ex5` is the pre-remediation binary and is NOT hash-bound to the remediated source.** No valid COMPILE_OK exists for the current source. Do not enqueue for Q02 with this binary — it would test the OLD code with the known critical defects the remediation fixed. Per the standing gemini-code rule, a gemini-originated build is never self-advanced to PIPELINE by Claude regardless of verdict.

## Findings

### BLOCKER — stale binary (RECYCLE trigger)
- `.ex5` at HEAD sha256 `5b463d9138ee8ad1d31566ac0093b127932965facfde75c09f184c683f789a61` is **byte-identical to the pre-remediation commit `2ad86abe7`** (`git cat-file -p 2ad86abe7:.../*.ex5 | sha256sum` == HEAD). Last git change to the `.ex5` is commit `2ad86abe7` (pre-remediation). The two remediation commits changed only `.mq5`/setfiles.
- The build-result self-report (`docs/ops/evidence/3c1da904_qm5_36001_build_ea_result_2026-08-23.md`) states the recompile was correctly refused: `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` (live factory running). The source artifact JSON's `compile_succeeded:true` / `build_check_passed:true` re-record the **stale** ex5 hash; they are not a fresh compile of `03be79d0`.
- **Action (actionable, in-lane):** route a governed COMPILE_EA build (COMPILE_EA queue / next OFF window — no ad-hoc compile while factory runs, no recompile in active inventory = ROT) to emit a fresh `.ex5` hash-bound to source `03be79d0`, plus a non-blocked smoke report, then re-review before any Q02 enqueue.

## Verified clean (independent read; would be APPROVE-grade absent the binary gap)

- **Card mechanism faithful.** All 6 prior Codex findings independently re-verified resolved against the card (cross-checked in `docs/ops/evidence/2026-08-23_qm5_36001_claude_remediation_reverify.md`): (1) TP1 50% partial + runner-to-BE via `Strategy_ManageOpenPosition` `QM_TM_PartialClose` at `+tp_atr_mult*ATR`, `req.tp=0.0`; (2) genuine SSL crossover `Strategy_SSLCross(1)` shift1-vs-shift2; (3) DeMarker true crossover exit (0.70/0.30 with prior-bar guard) + SSL flip; (4) symmetric WAE long/short gate; (5) rollover blackout via `QM_BrokerToUTC`, 2.0% daily entry halt, `QM_KillSwitchInit(id,magic,2.5,5.0,1.0)` matching card §4.2; (6) OnTick runs management/exit before entry filters.
- **Dead-input scan: none.** All 16 `strategy_*` inputs have ≥1 use site beyond declaration (grep count ≥2 each): mcginley_period(2), ssl_period(3), vortex_period(2), wae_fast/slow/signal(3), wae_bb_period/deviation(3), wae_sensitivity(3), wae_deadzone_pts(2), demarker_period(3), atr_period(4), sl_atr_mult(2), tp_atr_mult(2), spread_atr_mult(2), warmup_bars(2).
- **Framework conformity.** `#include <QM/QM_Common.mqh>` chain (mq5:6); magic via `QM_FrameworkMagic()` / `QM_FrameworkInit`; magic_numbers.csv 5 rows = `36001*10000+slot` (`360010000..360010004`), collision-free; news filter wired (`QM_NewsAllowsTrade2` temporal+compliance, mq5 OnTick); MAE hook `QM_FrameworkTrackOpenPositionMae`; no ML libs; no hardcoded commission/swap/DST.
- **Setfiles governed.** 5 D1 backtest sets, `RISK_FIXED=1000` / `RISK_PERCENT=0`, `environment: backtest`, `build_hash` stamped (governed `gen_setfile` path). No raw MQ5 promotion (build artifact present).

## Disposition
RECYCLE — code-complete, binary stale. Re-review after a governed COMPILE_EA build produces a fresh `.ex5` bound to source hash `03be79d0` with a clean smoke report. Task remains gemini-originated: Claude does not self-advance to PIPELINE.

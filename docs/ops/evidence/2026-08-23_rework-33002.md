# rework-33002 — blocked by unresolved approved-card entry contract

## Status

`BLOCKED_CARD_DEFECT` — no EA, framework, registry, setfile, or test code was changed.

Router task `c734242f-6787-4bda-a735-05758753a9f0` cannot be repaired card-faithfully until the OWNER-approved Strategy Card defines precedence between its daily-open entry instruction and its rollover blackout.

## Work order and preflight evidence

- The read-only `agent_tasks` query returned state `RECYCLE`, artifact path `docs/ops/evidence/2026-08-17_point_1_11_e3_enforced_19_rows_held_9_reviews_closed.md`, and verdict `FAIL upheld: news suppresses exits/OCO, non-atomic daily-open bracket; rework`.
- The authoritative review is `D:/QM/strategy_farm/artifacts/reviews/c734242f-6787-4bda-a735-05758753a9f0.json`, SHA-256 `c66227cc65167bb76733f08a720de149011664a9eeaa06f3b1480940beca73f0`.
- The approved card is `D:/QM/strategy_farm/artifacts/cards_approved/QM5_33002_larry-williams-volatility-expansion-breakout.md`, SHA-256 `ddd14f2e50d435dd1e8364f2ff96fdf2dd7accd1b05caee8f091232ad22d1e66`. Its `last_updated` field is still `2026-08-15` (`:13`), and its bytes match the card hash reviewed on 2026-08-17.
- G0/build identity preflight otherwise passes: `status: APPROVED` and `g0_status: APPROVED` (`approved card:9-10`); D1 and three target symbols are declared (`approved card:33-39`); `framework/registry/ea_id_registry.csv:4433` has the active slug-matching EA row; `framework/registry/magic_numbers.csv:17349-17351` has all three active allocations. There is no registry gap, so no resolver or registry change is authorized.

## Deterministic blocker

The same approved card requires both:

- an inactive/no-trade state during the `23:55`–`00:05` GMT rollover blackout (`approved card:83-88`), and
- both pending stops to be placed "on Daily Open" (`approved card:90-94`).

The review names this conflict directly and requires an approved precedence/skip rule before implementation (`D:/QM/strategy_farm/artifacts/reviews/c734242f-6787-4bda-a735-05758753a9f0.json:29-32,134`). No later OWNER decision or amended approved card was found; the card predates the review and retains the exact reviewed SHA-256.

Each possible code change alters the experiment differently:

1. allow placement at the daily open despite blackout;
2. delay placement until after `00:05` using the daily-open anchor; or
3. skip that trading day.

Choosing among those is a strategy-contract decision, not an implementation repair. It would violate the ticket's card-faithfulness and no-criteria-change constraints. The card also retains the reviewed drawdown inconsistency: `expected_dd_pct: 18` (`approved card:42`) versus a `5.0%` maximum-total-drawdown stop (`approved card:111-113`).

## What changed

- Added this blocker evidence only.
- Deliberately left `framework/EAs/QM5_33002_larry-williams-volatility-expansion-breakout/` unchanged.
- Deliberately left `framework/registry/ea_id_registry.csv`, `framework/registry/magic_numbers.csv`, and `framework/include/QM/QM_MagicResolver.mqh` unchanged.
- Did not enqueue/delete backtests, overwrite verdicts, toggle the factory, change thresholds, touch `C:/QM/mt5/T_Live`, or run a pipeline phase.

## Tests and validation

Not run: the ticket's explicit card-defect branch requires a `BLOCKED` router disposition instead of an EA build. There are no touched code modules or unit tests to execute, and compiling the unchanged rejected implementation would not resolve or validate the missing strategy decision.

Read-only checks performed:

```text
approved card SHA-256: ddd14f2e50d435dd1e8364f2ff96fdf2dd7accd1b05caee8f091232ad22d1e66
review SHA-256:        c66227cc65167bb76733f08a720de149011664a9eeaa06f3b1480940beca73f0
EA registry:           active row 4433, slug match
magic registry:        active rows 17349-17351, slots 0-2
scoped git status:     clean before this evidence file
```

The supported router claim command initially returned `sqlite3.OperationalError: database is locked` while the existing factory sweep/maintenance writer was active; no direct database write or process intervention was used. After the lock released, the supported router command recorded:

```text
state:         BLOCKED
artifact_path: docs/ops/evidence/2026-08-23_rework-33002.md
verdict:       BLOCKED_CARD_DEFECT: approved card conflicts on daily-open placement versus 23:55-00:05 GMT blackout; OWNER precedence and drawdown reconciliation required
updated:       true
```

## Required OWNER action

Amend or supersede the approved card with an explicit daily-open/blackout rule (including whether the day is skipped or placement is delayed), reconcile the drawdown contract, and issue a new approved card hash. Development can then implement the remaining review findings without inventing strategy mechanics.

## Rollback

This commit contains evidence only. Revert it with `git revert <commit>` if the evidence is superseded. Do not edit the state database directly; after an amended OWNER-approved card exists, resume the task through `tools/strategy_farm/agent_router.py`.

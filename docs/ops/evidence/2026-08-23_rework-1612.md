# rework-1612 review repair evidence

## Scope and authority

- Router task: `ee8153cf-df11-47af-8157-aaf473ce8dbb` (`RECYCLE` -> `IN_PROGRESS`).
- Original build task: `690cd9ab-da44-4dd5-8cb2-0212384dc3db`.
- EA: `QM5_1612_aa-dsp-hplwma10` only.
- Work order: `docs/ops/evidence/ee8153cf_qm5_1612_codex_review_2026-08-22.md` and `D:/QM/strategy_farm/artifacts/reviews/ee8153cf-df11-47af-8157-aaf473ce8dbb.json`.
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1612_aa-dsp-hplwma10.md`, `g0_status: APPROVED`.

No gate threshold or criterion changed. No backtest was enqueued or deleted, the
factory was not toggled, and `C:/QM/mt5/T_Live` was not touched.

## What changed

1. Restored the card's dynamic entry spread rule. The five strategy inputs are
   declared at `framework/EAs/QM5_1612_aa-dsp-hplwma10/QM5_1612_aa-dsp-hplwma10.mq5:56`
   and each has executable use sites. The fail-closed, bounded 20-D1-day median
   implementation starts at the same file's line 106; its `CopySpread` count,
   exact-copy check, `ArraySize` proofs, positive-value checks, median, and 2.5x
   comparison are at lines 112-140. Entry calls the guard at line 203.
2. Restored completed-D1-bar cadence while retaining tick-responsive protection.
   MAE remains first at MQ5 line 322, management runs at line 333, the single
   `_Symbol/PERIOD_D1` new-bar gate is line 335, and the strategy exit is after
   that gate at line 340. News and entry follow the exit. The entry request is
   zeroed at line 371.
3. Repaired the governed oil identity. The existing alias maps `USOIL.cash` to
   `XTIUSD.DWX` at `framework/registry/execution_symbol_aliases_v1.json:78-79`.
   Only the missing slot was appended at
   `framework/registry/magic_numbers.csv:16655` (`16120013`). The generated
   resolver records registry SHA-256 `F53DE...F4A2` and 17,993 retained rows at
   `framework/include/QM/QM_MagicResolver.mqh:16-18`; dry-run regeneration
   reported `17993 rows kept, 0 dropped`. The new XTI backtest set binds source
   SHA-256, slot 13, fixed risk 1000, percentage risk 0, and spread inputs at
   `framework/EAs/QM5_1612_aa-dsp-hplwma10/sets/QM5_1612_aa-dsp-hplwma10_XTIUSD.DWX_D1_backtest.set:13-28`.
4. Regenerated all 14 strategy-bearing D1 backtest setfiles with source SHA-256
   `02ac5bc08d9caaa6b22079c3da10e88872758502b534836369101caa1409b477`.
   Every set uses `RISK_FIXED=1000` and `RISK_PERCENT=0`.
5. Replaced the corrupted spec. Slug/source identity is clean at
   `framework/EAs/QM5_1612_aa-dsp-hplwma10/SPEC.md:4-5`, the governed oil alias
   is documented at lines 36-41, and `$1,000 per trade` is restored at line 76.
6. Converted `build_identity.json` to the canonical build-result field set and
   made its unresolved compile state explicit at
   `framework/EAs/QM5_1612_aa-dsp-hplwma10/build_identity.json:43-47`. It does
   not misrepresent the old EX5 as a rework compile.
7. Added six ticket-specific regression tests at
   `tools/strategy_farm/tests/test_qm5_1612_review_rework_static.py:61-147` for
   spread fidelity, input wiring, D1 cadence, registry/setfiles, spec integrity,
   and the canonical build-result schema.

## Validation evidence

- `python -m pytest tools/strategy_farm/tests/test_qm5_1612_review_rework_static.py tools/strategy_farm/tests/test_gen_setfile.py -q`
  -> `7 passed in 2.09s`.
- `python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py -q -k "d10_indicator_buffer_bound_pass_and_fail_fixtures or d11_canonical_setfile_and_magic_symbol_pass or build_check_applies_hardening_pass_and_fail_fixtures or build_check_rejects_d11_phantom_symbol"`
  -> `4 passed, 25 deselected in 4.75s`.
- Full hardening-module run plus the ticket/gen-set tests -> `34 passed, 1 failed
  in 118.89s`; the sole failure is pre-existing and out of scope:
  `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599` in
  `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`. The scoped 1612
  hardening invocation has zero failures and zero warnings.
- `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_1612_aa-dsp-hplwma10`
  -> one file scanned, 14 symbols observed, `failures: []`, `warnings: []`.
- `python framework/scripts/validate_spec_doc.py framework/EAs/QM5_1612_aa-dsp-hplwma10`
  -> `1 PASS, 0 FAIL`.
- `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_1612_aa-dsp-hplwma10`
  -> `PASS`, 15 files checked, zero findings.
- `python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_1612_aa-dsp-hplwma10 --fail-on-leak --verbose`
  -> `SINGLE_SYMBOL_OK`, zero violations.
- `python framework/scripts/skill_build_ea_guard.py --ea-id 1612 --ea-label QM5_1612_aa-dsp-hplwma10`
  -> `status: ok`; registry, magic rows, and EA directory all true.

## Compile and smoke admission evidence

The required scoped command was attempted:

```text
pwsh -NoProfile -File framework/scripts/build_check.ps1 -EALabel QM5_1612_aa-dsp-hplwma10
```

It was refused before compilation with
`BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED` /
`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because managed T1-T10 `terminal64`
processes were alive. A repeat process snapshot still showed eight active
managed terminals. The sanctioned compile classifier was checked read-only and
reported this exact source ineligible for `EX5_ALREADY_PRESENT`,
`WORK_ITEMS_EXIST`, and `BOUND_SETFILE_HASH_EXISTS`; QM5_1612 is not covered by
either exact source-repair authority. Read-only SQLite query:

```sql
select id,status,verdict,evidence_path
from work_items
where ea_id='QM5_1612' and phase='COMPILE_EA'
order by created_at;
```

returned only two closed failures:

```text
04d0ec0c-dc35-4f7f-b1f8-7c73a3cefbd8 | failed | INVALID
7c03fd7f-229a-4c7e-b402-bee588ea387a | failed | COMPILE_FAIL
```

No compile work item or backtest was enqueued, no existing EX5 was overwritten,
and no guard/process was bypassed. Consequently a fresh EX5 and smoke report do
not exist. `build_identity.json` truthfully records
`build_check_passed=false`, `compile_succeeded=false`,
`smoke_result=build_check_failed`, and the stale pre-rework EX5 SHA-256
`abeb9a5a07acb2d5250dcd65864bddfbc8f3c6754013fc7901334aab157e0d45`.

## Rollback

Revert the single ticket commit that contains this evidence. That restores the
EA/spec/setfiles/build identity, removes the appended slot-13 magic row and XTI
setfile, restores the prior generated resolver, and removes the regression test.
After reverting, run
`python framework/scripts/update_magic_resolver.py --dry-run --keep-obsolete`
to confirm the restored registry/resolver pair is deterministic. No runtime or
queue rollback is needed because no compile/backtest work was enqueued and no
terminal was modified.

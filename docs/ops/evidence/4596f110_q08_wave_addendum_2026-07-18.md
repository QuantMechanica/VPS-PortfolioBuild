# Q08 wave addendum: verdict, timeout, and book-lineage hardening

Date: 2026-07-18

Router task: `4596f110-f3a3-4772-abfd-c3d2e9d5714c` (`triage_failure`, Codex)

Requested next state: `REVIEW`

Implementation commit on local `main`: `a17376efa`

## Review verdict

READY FOR REVIEW at code/evidence level. The change closes four Q08 grading/operations defects and
adds a read-only manifest-bound audit for the book requalification lane. It does not claim a new
pipeline verdict and does not change any existing work-item verdict.

## 1. Edge-decay severity needs an absolute recent-PF floor

The raw Q08.8 test still reports a failure at a relative PF decline of 40% or more. Aggregate
severity now becomes hard only when that decline is accompanied by `pf_last < 1.2`; otherwise the
signal is `EDGE_SOFT`. Missing/unparseable recent PF remains hard, fail-closed.

Static replay against the cited immutable aggregate evidence gives:

| EA/symbol | First PF | Last PF | Decline | New severity |
|---|---:|---:|---:|---|
| `QM5_10919/XTIUSD.DWX` | 41.2091 | 1.9998 | 95.15% | `EDGE_SOFT` |
| `QM5_10939/GBPUSD.DWX` | 2.0905 | 1.2420 | 40.59% | `EDGE_SOFT` |
| `QM5_12567/XNGUSD.DWX` | 1.7642 | 1.0318 | 41.52% | `EDGE_HARD` |

The source aggregates are respectively work items `8e45588c...`, `55813535...`, and
`2fcecbbf...`. This is a classification replay only, not a Q08 rerun.

## 2. Zero-trade Q08 cannot synthesize a merit failure

`D:/QM/strategy_farm/logs/work_item_16b48cd4-9493-4eee-90f4-5114fb4082f6.log` records the defect
twice: `trades=0`, PBO `82.86%` on 35 splits, and overall `FAIL_HARD`.

The aggregate now returns `INVALID` before hard-merit precedence whenever the baseline trade list is
empty, recording `baseline_trade_count=INVALID`. As defense in depth, farm verdict synthesis maps
any Q08/P5c summary with explicit `n_trades=0` to
`INFRA_FAIL:q08_zero_trade_baseline`, even if an older runner emitted `FAIL_SOFT` or `FAIL_HARD`.

Static replay now returns:

```text
INVALID {'8.7_pbo': 'EDGE_HARD', 'cost_cushion': 'INVALID',
         'baseline_trade_count': 'INVALID'}
```

## 3. Three PBO splits are low-sample evidence, not a hard edge verdict

The `QM5_11132/SP500.DWX` artifact under work item `c894d56e...` contains four common slices,
three complementary splits, two overfit splits, and raw PBO `66.667%`. Q08.7 now requires at least
10 evaluated splits. A smaller nonzero sample emits `INVALID` with
`insufficient_pbo_splits:got=<n>:need>=10`; raw PBO and split counts remain in evidence. Aggregate
calibration recognizes this as `LOW_SAMPLE`, so it cannot become `EDGE_HARD`.

The existing source-aware plateau containment remains unchanged: Q08.5 neighborhood fallback PBO is
capped at `EDGE_SOFT`; genuine Q03 cohorts retain the normal policy once their sample has at least 10
splits; unknown sources remain fail-closed.

Static replay of the exact 11132 score file now yields:

```text
INVALID insufficient_pbo_splits:got=3:need>=10 raw_pbo_pct=66.667
```

## 4. Q08 outer timeout now covers every bounded child

The old 120-minute work-item deadline undercut both the canonical baseline and Q08.5's atomic
five-run publication. The same derived timeout is now consumed by the farm reaper, future Q08
payload creation, and the terminal-worker monitor:

```text
baseline_outer = (2400s single or 5400s basket) + 120s
expected_runs = 1 + 2 * neighborhood_max_params = 5
neighborhood_outer = expected_runs * (TF_child_timeout + 120s) + 60s
phase_outer = baseline_outer + neighborhood_outer + 600s PBO + 300s publish headroom
```

The child floor remains aligned with the deployed runner: M1/M5 4200s, M15/M30 3600s,
H1/H2/H4 1800s, other single-symbol periods 900s, and baskets at least 3600s. Resulting phase
budgets are 143 minutes for a known D1 single, 218 for H1, 368 for M15/M30, 418 for M1/M5 or a
D1 basket, and 468 for an M5 basket. Missing timeframe metadata conservatively receives 368
minutes. Inner subprocess timeouts still bound genuine hangs.

No active worker or terminal was restarted. In particular, the active `QM5_12778` Q08 process was
not interrupted.

## 5. Book-24 setfile lineage audit

`tools/strategy_farm/audit_q08_book_setfiles.py` is read-only and binds its comparison to each
sleeve's declared `backtest_set`, rather than treating ordinary research variants as globally
invalid. The audited source was:

```text
D:/QM/reports/portfolio/portfolio_manifest_weekend_24sleeve_DRAFT_20260717.json
SHA-256 b5bcd1927dc0d4dc192a3b9efa93d19f8c1bdab585d1f70bb8422e46d29e4e61
```

Snapshot at `2026-07-18T06:36:09Z`:

- 24 manifest sleeves; 22 have at least one Q08 row;
- 106 matching book-sleeve Q08 rows;
- 65 exact paths;
- 1 byte-identical path alias (stale worktree root; safe canonicalization class);
- 39 variant mismatches and 1 other path mismatch;
- one open semantic mismatch: pending work item `5a8cb3a2-7698-4cf3-9981-1af4b147ec96`,
  `QM5_10403/XAUUSD.DWX`, points at `..._backtest_ablation_01.set` while the manifest declares
  `..._backtest.set` (different SHA-256).

The audit deliberately does not relabel all historical grid/ablation rows as defects: exploration
cascades legitimately preserve variant lineage. It also does not rewrite the current pending row;
that queue mutation remains an explicit review/operator decision. The tool supports a fail-closed
`--fail-on-open-mismatch` check for a future manifest-driven book enqueue/dispatch path.

Reproduction:

```powershell
python tools/strategy_farm/audit_q08_book_setfiles.py `
  --manifest D:/QM/reports/portfolio/portfolio_manifest_weekend_24sleeve_DRAFT_20260717.json `
  --summary-only
```

## Focused verification

Run from clean target files in `C:/QM/worktrees/cto_main`:

```text
python -m pytest -p no:cacheprovider \
  framework/scripts/tests/test_q08_davey_subgates.py \
  framework/scripts/tests/test_pbo_calculator.py -q
69 passed

python -m pytest -p no:cacheprovider \
  tools/strategy_farm/tests/test_farmctl_cascade.py \
  tools/strategy_farm/tests/test_terminal_worker_atomic_claim.py -q
52 passed, 4 subtests passed

python -m pytest -p no:cacheprovider \
  tools/strategy_farm/tests/test_basket_work_items.py \
  tools/strategy_farm/tests/test_terminal_worker_adoption.py \
  tools/strategy_farm/tests/test_verdict_taxonomy_ws2.py \
  tools/strategy_farm/tests/test_audit_q08_book_setfiles.py -q
40 passed

python -m py_compile framework/scripts/q08_davey/aggregate.py \
  framework/scripts/q08_davey/sub_8_7_pbo.py \
  tools/strategy_farm/farmctl.py tools/strategy_farm/terminal_worker.py
PASS

git diff --check
PASS (line-ending notices only)
```

## Guardrail audit

- No MT5 backtest was started; no active T1-T10 backtest was interrupted.
- `T_Live`, AutoTrading, and `terminal64.exe` were untouched.
- No pipeline PASS/FAIL is inferred from tests or static replays.
- No setfile, risk input, news seed, stale-news threshold, or gate threshold was weakened.
- Operator-facing phase names remain Q-only.
- The live farm database was read-only apart from the required router task transition after review
  packaging.

# rework-12612 — QM5_12612 review fix evidence

**Router task:** `dae6e821-63d1-4809-ae21-d8c082164db8`
**EA:** `QM5_12612_tsmom-12m-vol-scaled-ndx`
**Prepared:** 2026-08-24 (Europe/Berlin)
**Scope:** source/setfile review repair only; no backtest enqueue, verdict overwrite, factory toggle, registry mutation, or T_Live action.

## Work order and preflight

- Review work order: `docs/ops/evidence/2026-08-21_qm5_12612_gemini_build_review.md` and `docs/ops/evidence/e2d205ca_qm5_12612_gemini_codex_review_2026-08-23.json`.
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12612_tsmom-12m-vol-scaled-ndx.md`; `g0_status: APPROVED`, `ea_id: QM5_12612`, and slug match.
- EA registry: `framework/registry/ea_id_registry.csv:3579` is active with the exact slug.
- Required magic: `framework/registry/magic_numbers.csv:16175` binds slot 1 to `NDX.DWX` and magic `126120001`; no registry row was changed.
- Resolver chain: the EA includes `QM_Common.mqh`; `framework/include/QM/QM_Common.mqh:9` includes `QM_MagicResolver.mqh`, and framework magic is consumed by the EA rather than calculated locally.

## What changed

1. Replaced the EA-local `iTime`/`MqlDateTime` month-key implementation with `QM_CalendarPeriodKey(PERIOD_MN1, ...)` for current and prior D1 bars (`framework/EAs/QM5_12612_tsmom-12m-vol-scaled-ndx/QM5_12612_tsmom-12m-vol-scaled-ndx.mq5:65`).
2. Replaced repeated raw `Bars`, `iClose`, and `iSpread` calls with one closed-D1 `CopyRates` batch. The batch is capped at 512 bars, resized explicitly, and guarded by both copy-count and `ArraySize` checks before indexing (`...mq5:96`). The sole raw series call carries the required `perf-allowed` reviewer tag (`...mq5:109`).
3. Preserved the card mechanism on the guarded batch: 252-D1 sign, 63-return annualized sample volatility, 10% target, clamp 0.10–2.00, >20% resize, 20-day median spread guard, and ATR(14) × 3.0 stop (`...mq5:115`, `...mq5:165`, `...mq5:186`, `...mq5:219`, `...mq5:255`).
4. The explicit risk overload selects `QM_RISK_MODE_FIXED` when `RISK_FIXED > 0` and percent mode otherwise, scales the risk value by the card vol scalar, and resolves magic through `QM_FrameworkMagic()` (`...mq5:330`). Rebalance state is committed after a successful open, not before (`...mq5:334`).
5. Kept `QM_FrameworkTrackOpenPositionMae()` as the first OnTick instrumentation call. Management and exit now run before news/no-trade entry filters, and the equity stream is updated on every new bar even during an entry blackout (`...mq5:390`). The fallback request is zero-initialized (`...mq5:440`).
6. Made the backtest set identity explicit: `qm_ea_id=12612`, slot 1, `RISK_FIXED=1000`, `RISK_PERCENT=0`, both news axes, legacy compatibility mode, Friday-close inputs, RNG/stress inputs, and every declared strategy input are bound; no undeclared `qm_filter_*` key remains (`framework/EAs/QM5_12612_tsmom-12m-vol-scaled-ndx/sets/QM5_12612_tsmom-12m-vol-scaled-ndx_NDX.DWX_D1_backtest.set:17`).
7. Added a focused static regression suite covering the card mechanics, framework calendar/MAE/risk/magic wiring, buffer bounds, input use-sites, and setfile identity (`tools/strategy_farm/tests/test_qm5_12612_review_rework.py:1`).

## Verification output

### Pytest (scoped touched/relevant modules)

Command:

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_12612_review_rework.py tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_validate_symbol_scope.py -k "not qm5_411xx_sources_have_no_unbounded_numeric_buffers"
```

Output:

```text
...................................................... [100%]
54 passed, 1 deselected in 118.30s
```

The deselected test is a fleet-wide QM5_411xx census. When run, it failed on seven pre-existing buffer findings in unrelated QM5_411xx EAs; no 12612 finding was present. Those EAs are outside this ticket's explicit scope.

### EA hardening gate

Command:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_12612_tsmom-12m-vol-scaled-ndx
```

Output summary:

```text
files_scanned=1
failures=[]
warnings=[]
D7_mae_hook.failures=0
D9_trade_request_initialization.failures=0
D10_indicator_buffer_bounds.failures=0
magic_numbers.valid=true
dwx_symbol_matrix.valid=true
```

### Other scoped validators

```text
python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_12612_tsmom-12m-vol-scaled-ndx
PASS — findings=[]

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_12612_tsmom-12m-vol-scaled-ndx --json --fail-on-leak
SINGLE_SYMBOL_OK — n_violations=0

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_12612_tsmom-12m-vol-scaled-ndx
Summary: 1 PASS, 0 FAIL (of 1)
```

Input use-site audit found 23 declared inputs; every input occurs at least twice in the source (declaration plus one or more use-sites). The focused pytest also requires every declared input to exist in the backtest setfile.

### Governed compile/build-check interlock

Attempted scoped command:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_12612_tsmom-12m-vol-scaled-ndx -SkipCompile
```

Fail-closed output before static/report execution:

```text
LIVE_FACTORY_AD_HOC_COMPILE_REFUSED: terminal64 processes are alive; ad-hoc compile/build_check is refused.
```

Read-only process evidence included active T1–T10 tester terminals and `C:/QM/mt5/T_Live/MT5_Base/terminal64.exe`. The task forbids touching T_Live or toggling the factory, so the interlock was not bypassed. `farmctl compile-status QM5_12612_tsmom-12m-vol-scaled-ndx` returned `NOT_ENQUEUED`; a COMPILE_EA work item was not created because state mutation from this noncanonical worktree is also fail-closed and the task does not authorize overriding that guard.

Consequently, the checked-in EX5 remains the pre-rework binary (`sha256=d1395e74301d98cd86ad1afa4b05ee64e3bae954806530e41619944e70749960`) and must not be treated as compiled evidence for the repaired source. No smoke/backtest was run or claimed. Current repaired-source SHA-256 before commit: `4c89b9ca381be5d1777ddd58d78c211e0fc445fff7d61e19cf25158cc37ccb03`.

## Risk and review handoff

- Source/static review findings are repaired and machine-guarded.
- Binary freshness remains open: after this commit reaches the canonical checkout, the governed COMPILE_EA path must compile the source and refresh the EX5/setfile build hash before any pipeline phase. The old EX5 is not valid evidence for this source.
- The prior artifact's `deferred_p2_smoke` claim is not promoted here. Smoke evidence remains absent by design because this is build-only work and no backtest enqueue was authorized.
- No GENUINE_NO_SIGNAL or irreparable Strategy Card defect was found.

## Rollback

Revert the single ticket commit that contains this evidence and the three explicitly scoped implementation/test paths. This restores the prior MQ5 and setfile and removes the focused test/evidence note. Registry, router history, DB verdicts, queues, factory state, and T_Live require no rollback because none were modified by the code change.

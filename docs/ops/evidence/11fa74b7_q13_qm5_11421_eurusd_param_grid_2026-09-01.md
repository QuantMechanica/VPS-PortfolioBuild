# Q13 numeric parameter grid — QM5_11421 / EURUSD.DWX

- Router task: `11fa74b7-50ab-4c3b-887f-37983bc86850`
- Date: 2026-09-01
- Author: Codex
- Program: `DL089_QM5_11421_EURUSD_DWX_2019_2025`
- Runtime EA: `QM5_41162_ohlc-daily-squeeze-reversal-d1-opt`
- Subject/parent: `QM5_11421_ohlc-daily-squeeze-reversal-d1`
- Phase: Q13
- Verdict: `GRID_READY_FOR_ORCHESTRATOR_REVIEW; NO_ADVANCE; NO_SEALING`

## Outcome

The first genuine numeric grid for the EURUSD pilot is authored from three
already-wired strategy inputs and the exact candidate ladders pre-registered in
the QM5_11421 Strategy Card. The grid is within the sealed Option M budget and
contains the GELB fields for each lever: mechanical hypothesis, expected effect,
measurable refutation criterion, frequency check, technical bounds, and
interaction constraints.

Artifact:

`framework/EAs/QM5_41162_ohlc-daily-squeeze-reversal-d1-opt/opt_param_grid.json`

SHA-256:

`f5b16d253b733a6acb829f21d22c3af439d32571f8ffeafcec8c40c5aae2d21a`

The exact grid path is pinned to `text eol=lf` in `.gitattributes` so the
reviewed raw-byte SHA remains stable when checked out with `core.autocrlf=true`.

This ticket stops at REVIEW. It did not run `opt_census advance`, enqueue a
numeric cell, seal the grid into the pilot ledger, edit the ledger, alter a farm
work item, or enable `QM_ENABLE_Q13_OPTION_M_DECLARATIONS`.

## Governing contract

- DL-089 plan v3: numeric optimization follows the frozen pattern selection and
  precedes the final full-window confirmation; each trial varies one parameter;
  the parent value is mandatory; selection is the plateau median, never the best
  observed value.
- Option M: at most 3 parameters, at most 5 values per parameter including the
  parent, cumulative Q13-lineage increment at most +12, at most 114 physical
  cells, and at most 13.68 terminal-hours per pair.
- Fixed evaluation: `return_to_maxdd`; 2019-2025 yearly measurements; at least
  10 distinct entry-trading days in every scored year (partial years pro rata),
  checked before returns; at least +5% relative improvement in at least two-thirds
  of selection years; holdout-blind plateau-median selection.
- The Q12 filter is frozen at the valid six-zero/no-filter selection. All four
  walk-forward pattern steps selected no filters, and the ledger records stability
  4/4 with the final selection identical to the earlier steps.

The current ledger header still has `param_grid_path=null` and
`param_grid_sha256=null`. That is intentionally unchanged: the Orchestrator must
review this grid before any separately authorized sealing or advance action.

## Candidate discipline

The Strategy Card's P3 section names these exact three sweeps. No extra parameter
or candidate value was invented after seeing optimization results.

| Wired input | Parent | Candidates | Mechanical thesis | Refutation and frequency discipline |
|---|---:|---|---|---|
| `strategy_entry_range_mult` | 1.0 | 0.5, 1.0, 1.5 | A nearer stop entry should fill more often but admit weaker reversals; a deeper offset should fill less often but may improve trade quality. A genuine effect should form a stable local plateau around the parent. | Retain 1.0 if neither non-parent value is admissible and clears the sealed +5% / two-thirds consistency rule, or no defensible plateau exists. Entry days should be non-increasing as the offset rises; any scored year below the activity floor excludes the value. |
| `strategy_sl_range_mult` | 1.5 | 1.0, 1.5, 2.0 | A tighter stop cuts adverse moves earlier; a wider stop tolerates more noise. Fixed-risk sizing and the 80-pip hard cap should prevent an unconstrained exposure increase. | Retain 1.5 if no non-parent value qualifies, cap-binding makes treatments effectively identical, or no plateau exists. Signal generation is direct-invariant, but longer occupancy can suppress later entries, so yearly entry days are measured and the activity floor remains fail-closed. |
| `strategy_min_range_pips` | 30.0 | 20.0, 30.0, 50.0 | A 20-pip floor admits more but potentially noisier squeezes; 50 pips selects fewer, larger structures that may improve payoff quality. | Retain 30 if neither adjacent value qualifies or the apparent winner is an isolated peak. Entry days must be non-increasing from 20 to 30 to 50, and every scored year must retain the activity floor. |

All other numeric inputs remain at parent values in every cell. There is no full
cross. The frozen pattern inputs remain:

```text
opt_pp_buy1=0
opt_pp_buy2=0
opt_pp_buy3=0
opt_pp_sell1=0
opt_pp_sell2=0
opt_pp_sell3=0
```

## Source-use verification

The selected inputs are not declaration-only placeholders. Direct EA use-sites:

- `strategy_entry_range_mult`: lines 261 and 292 calculate short/long pending
  entry prices.
- `strategy_sl_range_mult`: lines 262 and 293 calculate short/long stop prices;
  lines 264-265 and 294-295 preserve the 80-pip cap.
- `strategy_min_range_pips`: line 252 rejects a squeeze before request creation
  when the closed day-2 range is below the threshold.

Source inspected:

`framework/EAs/QM5_41162_ohlc-daily-squeeze-reversal-d1-opt/QM5_41162_ohlc-daily-squeeze-reversal-d1-opt.mq5`

- Source SHA-256: `57298f812d62c24b41fea5333b7de0785004339610a48fd4934454de821c283b`
- Compiled EX5 SHA-256: `32ac75db71c957ea78fd65f34a3468f9241f91bc4a8ca05c1526b3b1fdcc1ccc`
- Frozen base-set SHA-256: `d85269e1f87b2046a0865f0cf06302521017579fe703960a8d35dcaab7ea82de`

The frozen backtest base set keeps `RISK_FIXED=1000`, `RISK_PERCENT=0`, and
`qm_news_stale_max_hours` remains 336 in the EA. No live or AutoTrading control
was touched.

## Budget arithmetic

| Field | Declared | Option M cap | Result |
|---|---:|---:|---|
| Parameter count | 3 | 3 | PASS |
| Values per parameter (parent included) | 3 / 3 / 3 | 5 each | PASS |
| New Q13 trials: `sum(V_i - 1)` | 6 | 12 | PASS |
| Effective trial count | 154 + 6 = 160 | n/a | disclosed |
| Physical cells: `7 * (1 + sum(V_i)) + 2` | 72 | 114 | PASS |
| Terminal-hours: `72 * 7.2 / 60` | 8.64 | 13.68 | PASS |

The all-parent yearly baseline is shared. Calendar years are repeated measurements
of the same hypotheses and do not multiply the DSR trial count.

## Focused verification

Executed in the canonical checkout `C:/QM/repo` on branch
`agents/board-advisor`:

```text
python -m json.tool <grid>
JSON_OK

load_param_grid(<grid>, expected_sha256=None)
sha256=f5b16d253b733a6acb829f21d22c3af439d32571f8ffeafcec8c40c5aae2d21a
parameters=3 increment=6

Option M/GELB structural assertions
parameter_count <= 3
candidate_count <= 5 and parent in every ladder
trial_increment = 6 <= 12
physical_cells = 72 <= 114
terminal_hours = 8.64 <= 13.68
all required hypothesis/refutation/frequency/bounds/use-site fields present
```

The ledger SHA-256 observed before artifact creation was
`90de2c9497f876ad73368638978243e83a2b43846b8ae0f43eb8c3cc7aa657cb`.
A post-write equality check is recorded before task handoff. The active farm DB
is intentionally not byte-hashed because scheduled workers legitimately update
its WAL concurrently; no operational DB-writing command was run in this ticket.

## Handoff

Orchestrator review is required before the grid can be sealed into the ledger.
Any later declaration/sealing/advance action is a separate control-plane action
and must preserve the Option M cap, the frozen Q12 filter, the disclosed +6 trial
increment, and the artifact SHA-256 above.

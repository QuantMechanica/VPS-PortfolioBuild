# rework-12944 — QM5_12944 review repair evidence

Date: 2026-08-24

Router task: `c74163e1-fd20-413d-a42f-e2f3eb432f4f`

EA: `QM5_12944_sperandeo-trend-fault-line-h4`

Scope: source/spec/unit-test repair only; no backtest enqueue, verdict overwrite,
factory toggle, registry mutation, `T_Live` access, or AutoTrading action.

## Work order read

- Router source build artifact:
  `D:/QM/strategy_farm/artifacts/builds/01bd8a9d-bd3f-4da5-a275-be9192a763ed.json`.
- Original review note:
  `docs/ops/evidence/2026-08-21_codex_gemini_review_qm5_12944_c74163e1.md`.
- Follow-up review findings:
  `docs/ops/evidence/4550f45e_qm5_12944_gemini_codex_review_2026-08-23.json`.
- Approved card:
  `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12944_sperandeo-trend-fault-line-h4.md`
  (`g0_status: APPROVED`).
- Registry preflight: `framework/registry/ea_id_registry.csv` has one active
  matching `12944`/slug row; `framework/registry/magic_numbers.csv` has 13
  active rows for slots 0–12. No registry file was changed.

## What changed

- `framework/EAs/QM5_12944_sperandeo-trend-fault-line-h4/QM5_12944_sperandeo-trend-fault-line-h4.mq5:17-28`
  now has the mutually exclusive backtest risk default (`RISK_PERCENT=0`,
  `RISK_FIXED=1000`) and disables the unsupported 30/30 temporal default.
- `...mq5:82-255` replaces the hard-coded three-point fit with bounded N-point
  regression controlled by `strategy_min_pivots`, applies the full configured
  pivot deviation between every adjacent pivot, and guards all arrays with
  `ArraySize`/fixed capacity.
- `...mq5:262-366` implements a complete 100-closed-bar rolling mean spread and
  one bounded, cached 160-bar fault-line scan. ATR(1) is compared with ATR(20),
  the prior close must be on the opposite side of the projected line, and the
  D1 close is read through `QM_ReadBar` rather than a raw series call.
- `...mq5:376-438` makes spread and the literal high-impact-news
  `QM_NewsInWindow(..., 15, 15, "high")` checks entry-only and leaves order
  sizing/stops/targets on framework request helpers.
- `...mq5:443-475` handles both failed breaks and fully qualifying opposite
  breakouts in `Strategy_ExitSignal`; `OnTick` evaluates that close before any
  opposite entry request.
- `...mq5:484-513` validates every strategy input and passes 15/15 news hints
  into `QM_FrameworkInit`. `...mq5:526-529` retains the MAE hook before the
  first per-tick guard. Magic continues through `QM_FrameworkMagic`, which is
  initialized through the `QM_Common`/MagicResolver chain.
- `framework/EAs/QM5_12944_sperandeo-trend-fault-line-h4/SPEC.md:7-27,119-130`
  records the corrected mechanics and risk-mode contract.
- `tools/strategy_farm/tests/test_qm5_12944_rework.py:1` adds focused regression
  coverage for risk/magic/MAE wiring, crossings, ATR expansion, spread/news,
  N-pivot use, reverse exits, input use-sites, buffer bounds, raw-series
  discipline, and forbidden ML tokens.

## Validation output

Commands were run from `C:/QM/worktrees/rework-slot-1`.

```text
python -m pytest tools/strategy_farm/tests/test_qm5_12944_rework.py -q
......                                                                   [100%]
6 passed in 0.61s
```

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_12944_sperandeo-trend-fault-line-h4
files_scanned=1; failures=[]; warnings=[]
```

```text
python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_12944_sperandeo-trend-fault-line-h4
files_checked=14; findings=[]; verdict=PASS

python framework/scripts/skill_build_ea_guard.py --ea-id 12944 --ea-label QM5_12944_sperandeo-trend-fault-line-h4
status=ok; ea_registry_row=true; magic_registry_rows=true; ea_dir_exists=true

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_12944_sperandeo-trend-fault-line-h4
Summary: 1 PASS, 0 FAIL (of 1)
```

The broader touched-gate test invocation completed as follows:

```text
python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_qm5_12944_rework.py -q
1 failed, 34 passed in 111.97s
```

The one failure is pre-existing and outside EA 12944:
`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven
findings in the `QM5_411xx` cohort. The scoped EA 12944 hardening command above
has zero failures.

## Compile interlock and binary status

All scoped attempts were refused before validation/compile:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_12944_sperandeo-trend-fault-line-h4 -Strict
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_12944_sperandeo-trend-fault-line-h4 -Strict -SkipCompile
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED: terminal64 processes are alive;
ad-hoc compile/build_check is refused. No retry was attempted.
```

The guard remained closed on the final pre-commit probe. The governed
`enqueue-compile` classifier is not an available substitute for this existing
EA: its default contract refuses existing EX5/work-item/build-task/bound-setfile
identities, and no QM5_12944 source-repair allowlist exists. Creating or
weakening such authority is outside this one-EA ticket. The factory was not
toggled and no active terminal was stopped.

Current identity evidence:

```text
MQ5 sha256 = edbfafabd4a40881100ea68f1aaef0ccb9952cfe9f28a168f107ff3cf7edd125
EX5 sha256 = e19a9d287c247aaec8f074ff0469aeb977f0fc03b349a95617aa02a347c2d711
MQ5 mtime UTC = 2026-08-24 07:45:43
EX5 mtime UTC = 2026-08-24 06:58:20
```

Therefore the source/static repair is evidenced, but the EX5 is explicitly
stale and neither compile PASS nor smoke PASS is claimed. A protected compile
must be performed in a genuine no-terminal burn window before pipeline use.
The build-only skill and ticket prohibition also mean no smoke/backtest was
run or enqueued here.

## Rollback

Revert the ticket commit with `git revert <ticket-commit>` to restore the prior
MQ5/SPEC and remove the focused test/evidence files. The existing EX5 and all
registries/runtime state are unchanged by the code commit. Router state can be
returned to `RECYCLE` through `agent_router.py update-task` if review rejects
the source-only resubmission; do not overwrite any verdict row.

# rework-39004 — review-fix evidence

- Router task: `6b47ceec-cb94-46e4-80a4-c3ab93de07a2`
- EA: `QM5_39004_forexfactory-thv-cobra-trix-scalper`
- Branch: `rework-slot-1`
- Scope: source, three existing backtest presets, EA-specific pytest regression, and this evidence note only
- Disposition: source remediation complete; resubmitted for code review with compile limitation disclosed below

## Authority and work order

The approved card is build-authorized at
`strategy-seeds/cards/approved/QM5_39004_forexfactory-thv-cobra-trix-scalper.md:10`
(`g0_status: APPROVED`). Its mandatory no-trade filters begin at card line 82,
and its 2.5% daily hard stop, 5.0% total-drawdown stop, and three-tick
slippage cap are at lines 111-113.

The review work order is
`docs/ops/evidence/6b47ceec_qm5_39004_gemini_build_codex_review_2026-08-18.md:18-50`:
missing no-trade filters, pip double conversion, non-fill-safe Fast-TRIX state,
missing loss limits, and missing committed source/binary identity. The router
row was read from `D:/QM/strategy_farm/state/farm_state.sqlite` via a
`mode=ro` URI, then claimed through `agent_router.py update-task` as
`IN_PROGRESS` with verdict `rework burn-window slot 1`.

No registry gap exists. The deterministic rows remain unchanged:

- `framework/registry/ea_id_registry.csv:4483` — active EA 39004 identity.
- `framework/registry/magic_numbers.csv:17517-17519` — active slots 0-2,
  magics 390040000-390040002 for EURUSD.DWX, USDJPY.DWX, and GBPUSD.DWX.
- The generated `QM_MagicResolver.mqh` contains the same three rows; no
  registry or resolver file was modified.

## Changes

The prior card remediation already supplied the principal review repair. This
ticket locks it closed and tightens the remaining conformance edges:

- `framework/EAs/QM5_39004_forexfactory-thv-cobra-trix-scalper/QM5_39004_forexfactory-thv-cobra-trix-scalper.mq5:131-132`
  now guards the bounded TRIX `CopyRates` buffer with `ArraySize` before any
  indexing.
- The same source at lines 198-201 places the card's one-position rule inside
  `Strategy_NoTradeFilter`; the entry hook retains its line 223 race guard.
- Existing repaired mechanisms remain wired: UTC rollover and realized-loss
  entry halt at lines 203-206, exact pip-native Coral buffer at line 232,
  broker-position reconstruction at lines 288-305, hard loss rails through
  `QM_KillSwitchInit` at lines 356-360, and the MAE hook at line 377.
- All three presets are rebound at line 13 to source SHA-256
  `99e8304b2a6221e15923ff175bfe71a4564a66ab76fed207f33ea6092311504a`.
  Their lines 19-20 retain `RISK_FIXED=1000` and `RISK_PERCENT=0`.
- `tools/strategy_farm/tests/test_qm5_39004_review_rework.py:25-127` adds four
  regression tests for the review findings, every declared input's executable
  use site, exact scoped hardening/magic rows, and fixed-risk preset coverage.

There is no ML use, raw trade request, direct `OrderSend`, or unbounded
indicator buffer. The two raw series reads are explicitly performance-allowed:
the bounded TRIX `CopyRates` window at source line 130 and the single closed-bar
`iClose` at line 229.

## Validation evidence

Commands were run from `C:/QM/worktrees/rework-slot-1` and scoped to this EA
where the command supports EA scoping.

```text
python -m pytest tools/strategy_farm/tests/test_qm5_39004_review_rework.py -q
.... [100%]
4 passed in 0.72s

python tools/strategy_farm/build_gate_hardening.py \
  --repo-root C:/QM/worktrees/rework-slot-1 \
  --ea-label QM5_39004_forexfactory-thv-cobra-trix-scalper
files_scanned=1; failures=[]; warnings=[]

python tools/strategy_farm/validate_build_guardrails.py \
  framework/EAs/QM5_39004_forexfactory-thv-cobra-trix-scalper/QM5_39004_forexfactory-thv-cobra-trix-scalper.mq5 \
  framework/EAs/QM5_39004_forexfactory-thv-cobra-trix-scalper/sets
PASS for one MQ5 and all three sets; max_news_stale_hours=336

python framework/scripts/validate_spec_doc.py \
  framework/EAs/QM5_39004_forexfactory-thv-cobra-trix-scalper
Summary: 1 PASS, 0 FAIL (of 1)

git diff --check
PASS (only core.autocrlf informational warnings)
```

The complete relevant hardening pytest module was also run:

```text
python -m pytest \
  tools/strategy_farm/tests/test_qm5_39004_review_rework.py \
  tools/strategy_farm/tests/test_build_gate_hardening.py -q
1 failed, 32 passed in 273.26s
```

The sole failure is an existing fleet-wide assertion over untouched
`QM5_41134` through `QM5_41140` sources
(`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`). It does not scan
or implicate QM5_39004. The exact-label hardening command above is the scoped
target result and passed with zero findings.

## Compile limitation

The required scoped command was attempted twice (normal and `-SkipCompile`):

```text
powershell -NoProfile -ExecutionPolicy Bypass \
  -File framework/scripts/build_check.ps1 \
  -EALabel QM5_39004_forexfactory-thv-cobra-trix-scalper \
  -RepoRoot C:/QM/worktrees/rework-slot-1
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED
```

Eight shared T1-T10 `terminal64` processes were alive. Per the compile guard,
no retry, terminal stop, factory toggle, or direct MetaEditor bypass was made.
`farmctl.py compile-status QM5_39004_forexfactory-thv-cobra-trix-scalper`
reported `NOT_ENQUEUED`. The governed enqueue classifier does not authorize
39004's bound source-repair rebuild, so no compile work item was created. The
EA currently has no `.ex5`; this review submission therefore makes no compile
or runtime claim.

## Safety and rollback

- No backtest was enqueued or deleted; no gate verdict or threshold changed.
- Factory state, AutoTrading, `C:/QM/mt5/T_Live`, DB verdict rows, registries,
  and other EAs were untouched.
- Roll back atomically with `git revert <the rework(39004) ticket commit>`.
  This restores the prior source/preset bytes and removes the regression test
  and this evidence note; no runtime or database rollback is required.

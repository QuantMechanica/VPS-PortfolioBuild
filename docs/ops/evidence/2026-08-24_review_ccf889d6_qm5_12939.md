# Review — QM5_12939 carney-alternate-bat-h4 (router ccf889d6)

Date: 2026-08-24
Reviewer: Claude (review lane)
Task: `ccf889d6-6684-49e1-b831-38fbe6e8e5b9` (review_ea, assigned codex)
Source deliverable: agy/gemini build_ea `2a3580e3` (same build as prior review task `98e0cec3`)
EA: `framework/EAs/QM5_12939_carney-alternate-bat-h4/`
Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12939_carney-alternate-bat-h4.md`

## Verdict: RECYCLE

This task is a second router review of the SAME source build already reviewed under task
`98e0cec3` (evidence `docs/ops/evidence/2026-08-24_review_qm5_12939.md`, commit d83aa1a8f),
which correctly ruled RECYCLE. This note confirms and concurs. Source is card-faithful and
framework-clean; the delivered `.ex5` is a STALE pre-fix binary. Recompile of the current
source with fresh COMPILE_OK evidence is required before Q02. No `.mq5` change needed.

## Source review — CLEAN (concurs with 98e0cec3)
- Card-faithful XABCD Alt-Bat: AB/XA=0.382 (mq5:233,273), BC/AB[1.13,2.618] (234,274),
  CD/BC[2.0,3.618] (235,275), D/XA=1.13 with d_below_x/d_above_x extension guard (236/239,
  276/279), confirmation close vs D-pivot bar (242-244,282-284), D1 RSI[25,75] (193-195),
  SL=D±1.27·ATR (249,289), T1 50% partial at 38.2% AD (251,392-400), T2 61.8% AD as broker TP
  (252,325), post-T1 ATR×1.0 trail (402-417,433-448), time-stop 30 (379), cooldown 18
  (246,286), spread ≤0.3·ATR (306). Fractal pivots = card-permitted ZigZag alternative.
- All 19 declared strategy_* inputs have live use-sites (grepped, ≥2 occurrences each). No
  dead inputs.
- Framework: QM_Common include (5), MAE hook (501), magic via QM_FrameworkMagic = 12939*10000
  +slot with 13 active registry rows, news QM_NewsAllowsTrade2 (542), RISK_FIXED=1000/
  RISK_PERCENT=0 backtest (20-21), bounded buffers (pivots[128] guard 134, CopyRates by
  scan_bars 199), all raw-series `// perf-allowed`, no ML, no invented commission/swap.
- 13 governed setfiles (generator header, build_hash, risk_mode=FIXED).

## Blocking defect — stale binary (the reason for RECYCLE)
- Current source `.mq5` sha256 = `d9a62e7b…` (fixed source, 2026-08-23, mtime 18:34).
- On-disk `.ex5` sha256 = `6fb96e5…`, mtime **2026-08-21 18:24** — PREDATES the `.mq5`.
  This is the PRE-FIX binary compiled before the double-new-bar fix landed on 08-23.
- `artifacts/qm5_12939_build_result.json` records the NEW `mq5_sha256=d9a62e7b…` alongside the
  OLD `ex5_sha256=6fb96e5…` with `compile_succeeded:true` — an inconsistent carryover from the
  08-21 build; the rebuild ran static guardrails but did NOT recompile the fixed source.
- Consequence: Q02 would execute the defective pre-fix `.ex5`. "Stale .ex5 voids healthy
  backtests / rebuilt EX5 = new identity" class.

## Actionable recycle instruction (Codex, OFF-window / COMPILE_EA queue)
Recompile current source (`d9a62e7b…`) so `.ex5` sha matches; regenerate the compile log and
refresh `build_result.json` with a consistent freshly-compiled `ex5_sha256` for source
`d9a62e7b…`. No `.mq5` change required. Then re-submit for review.

## Reviewer correction note
An initial APPROVED close on this task was reversed: the first pass verified the `.mq5` hash
matched `build_result.json` and that `compile_succeeded=true`, but did NOT verify the `.ex5`
was compiled from the fixed source. The `.ex5` mtime (08-21) predating the `.mq5` mtime
(08-23) proves the binary is stale. Corrected to RECYCLE.

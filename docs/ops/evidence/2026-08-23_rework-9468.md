# rework-9468 — review repair evidence

Date: 2026-08-24 UTC

Router task: `cd6442dd-4ad9-4845-862a-2ef6e3ec0172`

EA: `QM5_9468_connors-rsi4-3day-d1`

## Status

The review findings are repaired in source, SPEC, tests, and the packaged
setfile universe. Focused static hardening is PASS with zero findings. A fresh
binary is not claimed: governed compile work item
`adb5d0cf-b485-4db2-8127-2c0fbc7d814a` is released and pending, but canonical
terminal workers currently fail before claim because their Q09 schema support
is version 6 while the runtime DB is schema 7. The pre-rework EX5 is therefore
marked `STALE_PRE_REWORK_DO_NOT_USE` in `build_identity.json`.

No backtest was enqueued or deleted. Factory state, gate criteria, verdict
rows, `C:/QM/mt5/T_Live`, AutoTrading, `magic_numbers.csv`, and
`QM_MagicResolver.mqh` were not changed.

## Findings repaired

1. D1 is now fail-closed at initialization through
   `QM_FrameworkDeclareExecutionContract(PERIOD_D1, ...)`, and entry cadence is
   explicitly `QM_IsNewBar(_Symbol, PERIOD_D1)`
   (`framework/EAs/QM5_9468_connors-rsi4-3day-d1/QM5_9468_connors-rsi4-3day-d1.mq5:273`,
   `:319`).
2. MAE remains the first OnTick hook; Friday close and the three-D1-bar time
   exit execute before news, warmup, quote, and spread entry filters
   (`...mq5:287`, `:293-335`).
3. Cooldown state is reconstructed from owned deal history on init and updated
   for every `DEAL_ENTRY_OUT`, `DEAL_ENTRY_OUT_BY`, or `DEAL_ENTRY_INOUT`
   transaction. History/read failures block new entries, while management
   remains reachable (`...mq5:193-255`, `:278`, `:351`).
4. Raw `iBars`, `iBarShift`, and `iClose` use-sites were replaced with
   `QM_ReadBar` and restart-safe `QM_TM_HeldPeriods`; RSI/SMA/ATR remain pooled
   framework readers. All nine declared strategy inputs have validation and
   mechanism use-sites (`...mq5:59-187`).
5. The durable SPEC now documents D1, all nine strategy inputs, three-day hold,
   fixed-risk backtest/live-percent risk split, and only SP500/NDX/WS30
   (`framework/EAs/QM5_9468_connors-rsi4-3day-d1/SPEC.md:23-36`,
   `:45-64`, `:101-105`).
6. The package now contains only the three card-authorized setfiles, preserving
   their existing governed slots: NDX=1, SP500=2, WS30=4. Ten unauthorized
   FX/GDAXI/UK100/XAU setfiles were removed. Existing magic allocations were
   deliberately left unchanged per the ticket boundary.
7. `tools/strategy_farm/compile_work_items.py:86-90,223-225` adds an exact
   compile-only source-repair authority bound to this router task and this EA.
   It enables only an append-only, source-hash-bound `COMPILE_EA` successor;
   `tools/strategy_farm/tests/test_compile_work_items.py:213-228` proves the
   task and label boundary fails closed.
8. `tools/strategy_farm/tests/test_qm5_9468_rework_static.py:37-117` covers D1
   execution, management reachability, durable cooldown, input wiring, raw
   series prohibition, approved setfile scope, risk/slot bindings, SPEC, and
   build identity.

## Validation evidence

### Focused unit and touched-module tests

Command:

```text
python -m pytest tools/strategy_farm/tests/test_qm5_9468_rework_static.py tools/strategy_farm/tests/test_compile_work_items.py tools/strategy_farm/tests/test_build_guardrails.py -q
```

Result:

```text
.............................................                            [100%]
45 passed in 5.94s
```

### Focused hardening and validators

Commands and results:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_9468_connors-rsi4-3day-d1
schema=qm.build-gate-hardening/v1; files_scanned=1; failures=[]; warnings=[]

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_9468_connors-rsi4-3day-d1
verdict=PASS; files_checked=4; findings=[]

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_9468_connors-rsi4-3day-d1
PASS QM5_9468_connors-rsi4-3day-d1; Summary: 1 PASS, 0 FAIL

git diff --check
PASS (exit 0)
```

The broader `test_build_gate_hardening.py` repository scan completed with
28 PASS and one unrelated pre-existing failure in QM5_41134/QM5_411xx dynamic
buffers (`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`); the
focused 9468 hardening row itself has zero failures and zero warnings.

### Compile/build-check boundary

Direct single-EA commands were attempted and correctly refused before compile:

```text
powershell -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_9468_connors-rsi4-3day-d1/QM5_9468_connors-rsi4-3day-d1.mq5 -Strict
compile_one.reason_class=INCLUDE_MIRROR_REFUSED
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
log=C:/QM/worktrees/rework-slot-5/framework/build/compile/20260824_133721/QM5_9468_connors-rsi4-3day-d1.compile.log

powershell -File framework/scripts/build_check.ps1 -EALabel QM5_9468_connors-rsi4-3day-d1
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED
```

The prescribed compile-only path then produced append-only work item
`adb5d0cf-b485-4db2-8127-2c0fbc7d814a`, bound to MQ5 SHA-256
`2e741e74a14b3c20be6c807908e08c476641b9b1b9cb31e91b8c9cb9b69e18b9`.
Its exact activation hold was released at `2026-08-24T13:44:19+00:00` after a
matching-hash dry run. Release backup:
`D:/QM/strategy_farm/state/backups/farm_state_before_compile_wave_20260824T134414Z_dec77bfb.sqlite`
(SHA-256 `baba58da006ef99a62410653343279558f2dfcd917d475504391f82ee2edbcf0`).

Read-only status query:

```sql
SELECT id,ea_id,status,claimed_by,created_at,updated_at,verdict
FROM work_items
WHERE id='adb5d0cf-b485-4db2-8127-2c0fbc7d814a';
-- pending | claimed_by NULL | verdict NULL
```

Canonical worker evidence is in
`D:/QM/strategy_farm/logs/terminal_worker_T2.log.err`: terminal-worker claim
initialization raises `q09_news_schema.SchemaError: database Q09 schema 7 is
newer than supported 6`. No worker, factory, or schema mutation was attempted
to bypass that fail-closed boundary.

## Input and registry checks

- Each `strategy_*` declaration has at least one validation use and one
  mechanism use; focused test `test_every_strategy_input_is_declared_validated_and_used`
  enforces this.
- `framework/registry/ea_id_registry.csv:690` remains active for 9468.
- Existing `framework/registry/magic_numbers.csv:16863-16875` rows were not
  edited. The retained setfiles bind the already-allocated approved slots 1,
  2, and 4.
- Backtest files bind `RISK_FIXED=1000`, `RISK_PERCENT=0`; source keeps both
  framework risk inputs for the governed live inverse if later authorized.

## Rollback

Revert the resulting git commit on the current branch to restore the prior EA
source, SPEC, setfile package, build identity, compile authority, and tests.
Do not overwrite or delete the append-only compile work item. If the source is
rolled back, allow the normal worker/reviewer to disposition that hash-bound
item as stale. The compile-wave DB backup is evidence only and must not be
restored over a live DB because later concurrent state would be lost.

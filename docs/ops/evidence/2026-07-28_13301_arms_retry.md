# QM5_13301 deviation-arm retry and T3 diagnosis

Date: 2026-07-28  
Router task: `c5f2dd92-d143-4185-ac15-05618df30b72`

## Result

Both governed Q02 deviation work items were returned to `pending` at
`2026-07-28T17:54:19.591208+00:00`. Their task-scoped staged binaries and
required SHA-256 values are unchanged:

| Work item | Arm | Avoid terminals | Staged EX5 SHA-256 |
|---|---|---|---|
| `efc84bc7-8e44-4cb0-8e05-a03ed24d8f7d` | tick | `T3,T5,T9` | `3f3deac97d4819bf030bcf3e5153bc21f439a6aedb0ca430b3967fcbb236c625` |
| `bbe93c4f-4b5c-4e24-878c-ef21ca9beec6` | timer 1s | `T3` | `08e55289829a1aef2670322c0da3755b31f268ef6f69f4785bb88a4b59fdfe82` |

The queue still owns scheduling. No terminal was reserved, started, stopped, or
re-imported.

Pending QM5_20181 work item
`f0a3c02e-c1b1-42ec-9675-b1e600d15f78` was also protected with
`avoid_terminals=["T3"]`. It remains pending and retains staged EX5 SHA-256
`806e53c1fe94bc2cbae3ddc8de66a3add985c7ef443e0a6ab9f226083778a7cb`.

## Evidence correction

The task brief says both arms failed with `BARS_ZERO` on T3. Durable
row-bound evidence does not support that exact statement:

- Tick-arm summary
  `D:\QM\reports\work_items\efc84bc7-8e44-4cb0-8e05-a03ed24d8f7d\QM5_13301\20260728_153554\summary.json`
  records `terminal=T3` and `reason_classes=["BARS_ZERO","INCOMPLETE_RUNS"]`
  for 2025-07-01 through 2025-12-31.
- The same tick arm later passed that prescreen on T6
  (`20260728_154406\summary.json`) and was eventually reaped for
  `ACTIVE_TIMEOUT`.
- Timer-arm summary
  `D:\QM\reports\work_items\bbe93c4f-4b5c-4e24-878c-ef21ca9beec6\QM5_13301\20260728_150753\summary.json`
  records `terminal=T9` and `reason_classes=["OK"]` for the same window. Its
  subsequent failure was `ACTIVE_TIMEOUT` on T9; no row-bound T3
  `BARS_ZERO` summary exists for this arm.

## T3 versus T10 cache diagnosis

The T3 GDAXI history is not missing or cold relative to T10:

| Artifact | T3/T10 size | T3 SHA-256 | T10 SHA-256 |
|---|---:|---|---|
| `bases\Custom\history\GDAXI.DWX\2025.hcc` | 17,302,263 bytes | `c7fdc059a4cde2b4b1db999902d360e8c46c1c22f50fe8555fe787e017a6dcc4` | same |
| `bases\Custom\history\GDAXI.DWX\cache\M1.hc` | 6,036,230 bytes | `62148be7f3b02aa2272c17448416cf41fd96c30c45f4f27a88ead25ceea3d723` | same |

All twelve T3 and T10 GDAXI 2025 monthly `.tkc` files also have matching
sizes and timestamps. The 2025 H2 bar source is therefore present and
byte-identical at the durable-file level.

Classification: transient T3 tester/custom-symbol cache state, not the known
missing/cold-`.hcc` class. One row-bound `BARS_ZERO` event is insufficient to
class T3 as a dead indicator-engine terminal, especially because the same arm
passed on T6 and the durable T3 cache matches T10. T3 is **not** added to
`disabled_terminals.txt` on this evidence. The three affected measurements are
nevertheless steered away from T3 so the governed queue can obtain clean
evidence without repeating the suspect local state.

## Verification

Post-transaction database checks established:

- both 13301 rows are `pending`, unclaimed, verdict-null, and attempt count 0;
- both payloads contain T3 avoidance;
- both staged paths and required hashes are unchanged;
- the 20181 row remains `pending`, unclaimed, and now contains T3 avoidance.

No strategy mechanics, set files, risk settings, news guardrails, terminal
configuration, `T_Live`, or AutoTrading state were changed.

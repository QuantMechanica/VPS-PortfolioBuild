# review_ea — QM5_11518 carter-t-ema5-100-mtf-m15-h1 (Claude, 2026-08-23)

**Router task:** `127a492b-b30f-4308-9bd2-81312f914e9e` (REVIEW, assigned claude)
**Source build:** `d0f1e256-7e79-48ed-ac9b-ecdde5128a35` (gemini/agy), reason
`codex_review_required_for_gemini_code`.
**Gemini build-verdict claim:** "Carter-T EMA5/100 MTF strategy implemented and verified".

Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11518_carter-t-ema5-100-mtf-m15-h1.md`
EA dir: `framework/EAs/QM5_11518_carter-t-ema5-100-mtf-m15-h1/`

## Card faithfulness (mechanism)
Card mechanism = MTF EMA(5/100): H1 regime filter (EMA5>EMA100 = bullish) + M15 fresh
EMA5/100 cross, SL 15p / TP 30p (1:2), spread cap 12p, EURUSD.DWX+GBPUSD.DWX, M15.
Source .mq5 `Strategy_EntrySignal` (mq5:66-135) implements exactly this:
- H1 filter read at shift 1 (last closed H1 bar): `QM_EMA(_Symbol,PERIOD_H1,fast,1)` vs slow
  (mq5:79-80) — matches card "Implementation Notes: use shift 1 (last completed H1 bar)".
- M15 cross detected shift1 vs shift2 (mq5:85-95) — closed bars only, no lookahead.
- LONG requires `h1_fast>h1_slow && m15_cross_up`; SHORT the mirror (mq5:98,116).
- SL fixed 15p via `QM_StopFixedPips`; TP via `QM_TakeRR` rr=tp/sl=2.0 (mq5:105-108).

## Dead-input check (every strategy input grepped for use sites)
All 5 declared strategy inputs are wired beyond their declaration:
- `strategy_fast_ema_period` (mq5:36) -> H1+M15 EMA reads (mq5:79,86,88).
- `strategy_slow_ema_period` (mq5:37) -> H1+M15 EMA reads (mq5:80,87,89).
- `strategy_sl_pips` (mq5:38) -> `QM_StopFixedPips` + rr denom (mq5:105-107,124-126).
- `strategy_tp_pips` (mq5:39) -> rr numerator (mq5:106,125).
- `strategy_max_spread_pips` (mq5:40) -> `Strategy_NoTradeFilter` spread block (mq5:49,54).
No dead inputs, no dead code.

## Framework conformity
- Include chain: `#include <QM/QM_Common.mqh>` (mq5:6). QM_EMA helper resolved
  (`framework/include/QM/QM_Indicators.mqh:549`).
- Risk mode: `RISK_FIXED=1000`, `RISK_PERCENT=0` (mq5:22-23) — correct for backtest.
- Magic via MagicResolver: `QM_FrameworkMagic()` (mq5:71,151...); registry
  `magic_numbers.csv` = 115180000 (slot0/EURUSD.DWX), 115180001 (slot1/GBPUSD.DWX) =
  ea_id*10000+slot. Consistent with build_result `symbols_registered`.
- News filter wired: `QM_NewsAllowsTrade2/QM_NewsAllowsTrade` in OnTick (mq5:210-214);
  stale ceiling 336h (at ceiling, not above).
- MAE hook: `QM_FrameworkTrackOpenPositionMae()` (mq5:196).
- No ML libs, no hardcoded commission/swap/DST values.
- Friday handling via governed `QM_FrameworkHandleFridayClose()` (mq5:203).

## Build evidence
- mq5 SHA256 = `c940168b...f52583` matches `artifacts/qm5_11518_build_result.json`.
- Compile log `framework/build/compile/20260822_143854/...compile.log`:
  "0 errors, 0 warnings" — COMPILE_OK for current source hash.
- build_result: `build_check_passed:true`, `compile_succeeded:true`, `smoke_result:passed`.
- Set files generated via governed generator (governed header, `build_hash`,
  `risk_mode:FIXED`, RISK_FIXED=1000 / RISK_PERCENT=0) in `sets/` for both symbols.
- No raw MQ5 promotion: .ex5 built.

## Verdict
**APPROVED** — formally clean enough for Q02. Card mechanism faithfully implemented on
closed bars only, all strategy inputs wired, framework hooks present, magic registry
consistent, compile clean, set files governed. No defects.

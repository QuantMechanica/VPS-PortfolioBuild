# greview-11516 — Codex cross-review of QM5_11516

- Router task: `53266c28-bd4a-4400-80da-dd621c2558ff`
- Builder lane: gemini/agy
- Reviewer: Codex
- Review date: 2026-08-24
- EA: `framework/EAs/QM5_11516_carter-t-sma7-21-cci5-m15`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11516_carter-t-sma7-21-cci5-m15.md`
- Verdict: **RECYCLE**

## Result

The current MQ5 is card-faithful and passes the current static build guardrails, but the delivered EX5 is stale. The only governed compile receipt that produced the delivered EX5 hash is bound to an older MQ5 hash. Q02 admission therefore fails closed until the current source is recompiled through governed `COMPILE_EA` and a new hash-bound compile/build receipt is delivered.

## Reviewed task identity

Read-only command:

```text
python tools/strategy_farm/agent_router.py list-tasks --agent gemini
```

Filtering the returned JSON for task `53266c28-bd4a-4400-80da-dd621c2558ff` returned `state=REVIEW`, `task_type=build_ea`, `ea_id=11516`, and `slug=carter-t-sma7-21-cci5-m15`.

The approved card declares `g0_status: APPROVED`, M15, and the EURUSD/GBPUSD/USDJPY DWX universe (`D:/QM/strategy_farm/artifacts/cards_approved/QM5_11516_carter-t-sma7-21-cci5-m15.md:19-22`).

## Card fidelity and input wiring — PASS

- Entry matches the card's SMA(7)/SMA(21) cross plus CCI(5) zero-cross synchronized within one bar (`card:60-70`; MQ5 `:78-109`, `:195-239`).
- Initial stop is fixed at 15 pips (`card:72-80`; MQ5 `:205`, `:228`). TP1 closes 50% at 25 pips and moves the stop to breakeven; the remainder exits on a closed-bar SMA(7) recross (`card:72-75`; MQ5 `:244-307`).
- The card explicitly classifies SMA(84)/SMA(336) as structural context not used in the entry and defers them to a later filter sweep (`card:112-113`); their absence from the executable entry predicate is not an invented omission.
- Spread cap and no-Friday-entry match the card (`card:86-90`; MQ5 `:64-76`, `:181-184`).
- All 11 declared `strategy_*` inputs have executable use-sites beyond their declarations: fast SMA (`:82`, `:84`, `:196`, `:300`), slow SMA (`:83`, `:85`, `:219`), CCI (`:101-102`, `:199`, `:222`), cross lookback (`:196`, `:219`), synchronization (`:203`, `:226`), SL (`:205`, `:228`), TP1 (`:265`), partial ratio (`:273`), BE buffer (`:152`, `:280-281`), spread cap (`:66`), and Friday filter (`:181`). No dead strategy input was found.

## Framework conformity — PASS

- Canonical include chain: `#include <QM/QM_Common.mqh>` (MQ5 `:5`).
- MAE hook and lifecycle: `QM_FrameworkTrackOpenPositionMae()` is first in `OnTick` (MQ5 `:349-353`); Friday close, equity stream, `OnTimer`, `OnTradeTransaction`, and `OnTester` are wired (`:359-360`, `:394`, `:404-419`).
- Risk: backtest setfiles use `RISK_FIXED=1000` and `RISK_PERCENT=0` (`sets/*.set:17-21`); live default is `RISK_PERCENT=0.5` (MQ5 `:16-19`), passed through `QM_FrameworkInit` (`:321-336`).
- Magic: the entry request carries `req.symbol_slot = qm_magic_slot_offset` (MQ5 `:171-179`). Active registry rows are `115160000`, `115160001`, and `115160002` for slots 0-2 (`framework/registry/magic_numbers.csv:16099-16101`), i.e. `ea_id*10000+slot`; framework initialization resolves through `QM_MagicChecked` (`framework/include/QM/QM_Common.mqh:223-227`; `framework/include/QM/QM_MagicResolver.mqh:178-205`).
- News: temporal/compliance inputs default to PRE30/POST30 and DXZ (MQ5 `:21-26`), and entry is blocked through `QM_NewsAllowsTrade2`/legacy fallback before new-bar entry evaluation (`:383-392`).
- Indicator access uses pooled `QM_SMA`/`QM_CCI` readers (MQ5 `:82-85`, `:101-102`). Each framework read copies exactly one item into a fixed one-element buffer and checks the returned count (`framework/include/QM/QM_Indicators.mqh:281-289`); the hardening D10 buffer-bounds check returned zero failures. The sole raw series call is a single closed-bar `iClose(...,1)` with the required perf-allowed annotation (MQ5 `:300-302`).
- Grep found no ML, DLL import, grid/martingale/averaging logic, or invented commission/swap handling.
- Three backtest setfiles exist for the exact approved universe. Their governed header records environment, fixed-risk mode, slot, and generator build hash (`sets/*.set:1-16`); the governed compile evidence records successful `setfile_generation` for all three exact paths at `D:/QM/reports/work_items/4487d576-266c-43fd-afd3-7020aabc5bcc/QM5_11516/COMPILE_EA/compile_evidence.json` query `$.setfile_generation[*]`. Current strategy parameters are present at `sets/*.set:25-35`.

## Read-only checks and tests

Commands and results:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_11516_carter-t-sma7-21-cci5-m15
PASS: failures=[]; warnings=[]; D7=0, D9=0, D10=0, D17=0

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_11516_carter-t-sma7-21-cci5-m15
PASS: files_checked=4; findings=[]; max_news_stale_hours=336

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_11516_carter-t-sma7-21-cci5-m15
PASS: 1 PASS, 0 FAIL

python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py::test_d7_framework_mae_hook_pass_and_fail_fixtures tools/strategy_farm/tests/test_build_gate_hardening.py::test_d9_trade_request_zero_init_pass_and_bare_request_fails tools/strategy_farm/tests/test_build_gate_hardening.py::test_d10_indicator_buffer_bound_pass_and_fail_fixtures tools/strategy_farm/tests/test_build_gate_hardening.py::test_d11_canonical_setfile_and_magic_symbol_pass tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_gen_setfile.py tools/strategy_farm/tests/test_magic_resolver_reconcile_newlines.py tools/strategy_farm/tests/test_agent_router.py::AgentRouterTests::test_close_review_requires_existing_artifact_for_approval
27 passed in 11.17s
```

No compilation, backtest enqueue, factory toggle, verdict overwrite, or T_Live access was performed. No code or test file was changed because the ticket explicitly permits committing only this review artifact.

## Blocking finding — stale EX5

Current checkout hashes (PowerShell `Get-FileHash -Algorithm SHA256`):

```text
MQ5  931a1646e0608b615cdfe189e5a43ab2e46d7fd279e083c2df3db62b3f739d7b
EX5  520c169ea8a9dee22914347fe79ed78d54c0bddc14de9c3f361a098c1d52dfb9
```

The handoff identity merely records this pair (`docs/ops/evidence/53266c28_QM5_11516_carter-t-sma7-21-cci5-m15_build_identity.json:5-9`); it does not contain a compile receipt binding the two.

The governed receipt at `D:/QM/reports/work_items/4487d576-266c-43fd-afd3-7020aabc5bcc/QM5_11516/COMPILE_EA/compile_evidence.json` proves:

```text
$.candidate_recheck.mq5_sha256 = 2db0313d7d3f14498616bcd2c3fa00c53628fad2ac77bd343367e877b0962c52
$.ex5_sha256                   = 520c169ea8a9dee22914347fe79ed78d54c0bddc14de9c3f361a098c1d52dfb9
$.completed_at                 = 2026-08-22T14:33:40+00:00
$.compile_result               = PASS
$.build_check_result           = FAIL
```

The corresponding compile log is `C:/QM/repo/framework/build/compile/20260822_143314/QM5_11516_carter-t-sma7-21-cci5-m15.compile.log` and ends `Result: 0 errors, 0 warnings`; its work-item receipt binds the older `2db0313d...` MQ5. Git then changed the MQ5 substantially in commit `3c6f6da7d281ef19f207bfc3f7779e0d62e0759a` (390-line source diff) while committing the already-produced EX5. No later governed receipt was found that binds EX5 `520c169e...` to current MQ5 `931a1646...`.

This is a stale binary under the review contract. Static cleanliness of the current source cannot establish what the delivered EX5 executes.

## Verdict and named remediation

**RECYCLE** — current source is formally clean, but delivered EX5 `520c169e...` was compiled from MQ5 `2db0313d...`, not current MQ5 `931a1646...`.

Required fix: **recompile via governed `COMPILE_EA`** using the current MQ5, obtain strict compile `0 errors / 0 warnings` plus build-check PASS, and deliver a new compile/build identity receipt that binds the current MQ5 SHA-256 to the newly produced EX5 SHA-256. Then return the task for cross-review. Do not enqueue Q02 as part of that repair.

## Rollback

This ticket changes only this evidence note and the router review disposition. Revert the evidence commit with `git revert <review-commit-sha>` if the note itself must be withdrawn. Do not overwrite router verdict rows; if later hash-bound compile evidence disproves the finding, create the governed successor/re-review record through `agent_router.py` and append a correcting evidence note.

# QM5_12712 Q05 5400s Requeue - 2026-07-02

Branch: `agents/board-advisor`

## Scope

Mission: grow the certified V5 portfolio book with forex sleeves, preferring
new market-neutral FX cointegration baskets from the 66-pair scan, or fixing
`QM5_12532` / `QM5_12533` if they were still Q02-blocked.

No `T_Live` manifest, AutoTrading, portfolio admission, portfolio KPI, Q08
contribution, portfolio gate, or deploy manifest files were touched.

## Research State

Read `docs/research/CROSS_ASSET_FX_DISCOVERY_2026-06-09.md` and reran:

```text
python framework/scripts/mt5_diagnostics/analyze_cross_asset_v3.py
```

The strict 66-pair screen still has only two survivors:

| Pair | State |
|---|---|
| `EURJPY~GBPJPY` | already built as `QM5_12533`; Q02 PASS, later Q04 FAIL |
| `AUDUSD~NZDUSD` | already built as `QM5_12532`; Q02 PASS, Q04 PASS, Q05 already pending |

No unbuilt strict survivor was available. The prior local evidence also shows
the registered EdgeLab positive-hedge candidate set is already built, so I used
the fallback path and advanced an existing FX basket.

## Selected Work

Selected `QM5_12712` (`EURGBP~EURAUD`) because it had:

| Phase | State |
|---|---|
| Q02 | PASS |
| Q03 | PASS |
| Q04 | PASS |
| Q05 | completed `INFRA_FAIL`, no pending duplicate |

The latest actual Q05 run failed inside `run_smoke` after the old 3300-second
inner timeout:

`D:/QM/reports/work_items/f064eb24-9f7e-4d80-8180-88b1b0165b52/QM5_12712/20260701_030029/summary.json`

Current runner code now has `DEFAULT_TIMEOUT_SEC = 5400` and `farmctl` writes
`timeout_min=120` into the requeue payload, so this is not an identical replay
of the previous 3300-second execution envelope.

## Queue Action

Backed up the farm DB:

`D:/QM/strategy_farm/state/backups/farm_state_before_qm5_12712_q05_5400s_requeue_20260702_223422.sqlite`

Then requeued the existing Q05 row in place:

```text
python tools/strategy_farm/farmctl.py --root D:/QM/strategy_farm enqueue-backtest --ea QM5_12712 --phase Q05
```

Result:

| Field | Value |
|---|---|
| Work item | `f064eb24-9f7e-4d80-8180-88b1b0165b52` |
| Created rows | `0` |
| Requeued rows | `1` |
| Status after enqueue | `pending` |
| Updated at | `2026-07-02T20:34:28+00:00` |
| Archived report root | `D:/QM/reports/work_items/f064eb24-9f7e-4d80-8180-88b1b0165b52.requeued_20260702T2034280000` |

Post-requeue payload keeps the basket contract:

- host symbol: `EURGBP.DWX`
- host timeframe: `D1`
- tester currency/deposit: `USD` / `100000`
- manifest symbols: `EURGBP.DWX`, `EURAUD.DWX`, `EURUSD.DWX`, `GBPUSD.DWX`, `AUDUSD.DWX`
- `q04_latest_full_year=2024`
- `timeout_min=120`

## Verification

- `python tools/strategy_farm/farmctl.py --root D:/QM/strategy_farm work-items --ea QM5_12712`
  - Result: Q05 is now `pending`; Q02/Q03/Q04 remain `PASS`.
- `python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_12712_edgelab-eurgbp-euraud-cointegration --verbose`
  - Result: `BASKET_OK`, `n_violations=0`, manifest declares 5 symbols.
- `powershell -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_12712_edgelab-eurgbp-euraud-cointegration -RepoRoot C:/QM/repo -SkipCompile`
  - Result: `PASS`, 0 failures, 16 existing shared-framework DWX advisories.
  - Report: `D:/QM/reports/framework/21/build_check_20260702_203445.json`.

No manual MT5 backtest was launched from this session; the paced farm will
claim the pending Q05 row.

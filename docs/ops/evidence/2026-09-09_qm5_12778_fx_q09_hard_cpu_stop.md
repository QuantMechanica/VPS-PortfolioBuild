# QM5_12778 FX cointegration Q09 hard-CPU stop

**Observed:** 2026-09-09 01:52:14Z

**Branch:** `agents/board-advisor`

**Outcome:** selected one existing D1 market-neutral FX continuation; stopped before mutation because the immediate CPU sample exceeded the hard ceiling

## Selection

The governed 66-pair FX cointegration scan is fully mechanized, so another
Card, EA, registry allocation, basket manifest, or Q02 row would duplicate
committed work. The preferred anchors are not blocked at Q02:

- `QM5_12532_AUDNZD_COINTEGRATION_D1` has Q02 PASS and later Q05 FAIL.
- `QM5_12533_EURJPY_GBPJPY_COINTEGRATION_D1` has Q02 PASS and later Q04 FAIL.

The existing-card fallback is rank-25
`QM5_12778_AUDUSD_EURJPY_COINTEGRATION_D1`. It is a structural, fixed-beta,
closed-D1-bar spread-reversion basket from the OWNER-approved Chan extraction
and frozen scan. The approved Card remains G0/R1-R4 PASS and expects only 4-8
logical packages per year. Its two traded legs are `AUDUSD.DWX` and
`EURJPY.DWX`; `EURUSD.DWX` and `EURAUD.DWX` are conversion-history-only
dependencies in `basket_manifest.json`.

The basket has Q02 through Q07 PASS, Q08 FAIL_SOFT, and historical
Q09_PORTFOLIO PASS_PORTFOLIO evidence. The current autonomous continuation is
the unique Q09 multi-seed row
`1ba8f6c7-7725-47c5-b0e5-6a4736c4ad44`. At the final read it was pending,
unclaimed, unheld, attempt zero, not priority-tracked, and canonical rank 3,240
of 4,462 claimable rows. No duplicate Q09 identity exists.

The logical backtest setfile remains `RISK_FIXED=1000`, `RISK_PERCENT=0`, and
`PORTFOLIO_WEIGHT=1`. Strategy-card lint passed with no ML hits. The binding
framework-input audit also passed:

```text
python tools/strategy_farm/audit_framework_input_pins.py --check-source C:/QM/repo/framework/EAs/QM5_12778_edgelab-audusd-eurjpy-cointegration/QM5_12778_edgelab-audusd-eurjpy-cointegration.mq5
exit_code=0; EA_FRAMEWORK_INPUT_PINNED findings=0
```

## Capacity refusal

A dry run of the exact guarded priority mark returned `applied=false`,
`already_priority_track=false`, and `reason=dry_run`. The mandatory immediate
five-sample CPU reading was `100.00%`, `99.13%`, `98.64%`, `97.56%`, and
`98.77%` (average `98.82%`, maximum `100.00%`). Every sample met or exceeded
the 97% hard ceiling.

The mission therefore stopped before applying the mark. No Card, EA, source,
binary, registry row, setfile, manifest, work item, queue payload, verdict,
claim, dispatch, compile, tester, or terminal state changed. `T_Live` was
observed only to exclude it and was not controlled.

Machine-readable evidence:
`artifacts/fx_cointegration_qm5_12778_q09_hard_cpu_stop_20260909T015214Z_board_advisor.json`.

## Resume contract

Re-read the same Q09 work item and repeat the priority dry run. Apply exactly
one priority mark only if the row remains unique, pending, unheld and unclaimed
and every immediate CPU sample stays below 97%. Never append a duplicate row,
force a claim, dispatch a tester, or touch portfolio-admission, portfolio-KPI,
Q08-contribution, portfolio-gate, T_Live, or AutoTrading surfaces.

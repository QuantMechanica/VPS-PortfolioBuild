# QM5_20181 repair

Date: 2026-07-27  
Task: `eabfd168-4334-4163-95ce-3b8bf370dcdf`  
Scope: source/tooling repair only; no backtest was run.

## Verdict

The known F3/F4 defects are repaired in source and the replay comparator now
reports the F2 mismatch decomposition. QM5_20181 compiles with 0 errors and 0
warnings. Fidelity remains a workflow measurement: this repair does not claim a
pipeline verdict.

## F3 — runner invariance

Slot 0 remains on the original host `OnTick` path
(`QM5_20181_ftmo-joint-multisym-timer.mq5:422`). Enabling sleeve 1 does not call
`QM_SymbolGuardInit` or `QM_BasketWarmupHistory`; `basket_mode` stays false
(`:219`) and the framework's host guard/news/sizing/default-entry globals are
therefore not replaced. The isolation decision and foreign-symbol warmup are
explicit at `:280-303`.

The satellite uses `QM_BasketOrder` with its explicit symbol, slot and resolved
magic (`:394-406`). Thus basket machinery cannot change slot 0's warmup, bar
latch, news call, sizing, or default order path. The remaining global behaviours
are the intended account-wide kill switch, trade-transaction feed and equity
sampler; they do not select basket ownership.

## F4 — complete 10145 binding

The binding was transcribed from
`framework/EAs/QM5_10145_tsm-meanret/QM5_10145_tsm-meanret.mq5`: D1 rolling mean
log return over 15 bars; long by default; optional symmetric short; ATR(14)
3.0 stop; no trailing/break-even/partial management; exit on sign reversal.
The timer implementation is `QM20181_Run10145` at `:359-406`.

It owns slot 1 and a distinct magic (`:294-304`). Processing is once per closed
D1 bar and restart-safe: the last processed closed-bar time is loaded from and
written to a terminal global variable (`:300-303`, `:359-366`). A repeated timer
fire and a restart on the same bar therefore cannot submit a second entry.
`req.symbol_slot = sat.slot` is explicit at `:403`.

## F2 — replay mismatch decomposition

`tools/strategy_farm/compare_joint_replay.py:51-84` now classifies exact,
same-entry/same-volume/shifted-exit, different-entry, extra and missing rows.
The JSON output includes the counts at `:142`. Focused tests cover all
categories in
`tools/strategy_farm/tests/test_compare_joint_replay.py`.

## 13301 alternative

The 35 passing compositions were checked in
`docs/ops/evidence/2026-07-27_runner_satellite_composition.md:19-52`.
No comparable TIMER-SAFE replacement is established.

The strongest set without 13301 that retains both 10145 and a source-verified
bar-gated candidate is rank 17 (9936+10145+13108), OOS FUND_SCORE 0.527 versus
0.641 for rank 1. 13108 gates management/exit/entry behind its new-bar latch
(`QM5_13108_xti-mtsm-s2.mq5:379-397`), but the 0.114 OOS score loss is not
comparable. Higher-scoring substitutions use 9403 or 12969; both evaluate
time/sign exits on the per-tick path (`QM5_9403...mq5:248-270,316-361`;
`QM5_12969...mq5:256-263,303-342`) and are not established TIMER-SAFE.
Accordingly 13301 remains the documented satellite-2 measurement candidate;
failure of its fidelity gate is an accepted finding, not a surprise.

## Verification

- `python -m pytest tools/strategy_farm/tests/test_compare_joint_replay.py -q`
  — 2 passed.
- `framework/scripts/compile_one.ps1 -EAPath
  framework/EAs/QM5_20181_ftmo-joint-multisym-timer/QM5_20181_ftmo-joint-multisym-timer.mq5
  -EALabel QM5_20181_ftmo-joint-multisym-timer -Strict`
  — PASS, 0 errors, 0 warnings.
- Compile log:
  `framework/build/compile/20260727_201330/QM5_20181_ftmo-joint-multisym-timer.compile.log`.
- Binary:
  `framework/EAs/QM5_20181_ftmo-joint-multisym-timer/QM5_20181_ftmo-joint-multisym-timer.ex5`.
- No terminal/backtest was started.

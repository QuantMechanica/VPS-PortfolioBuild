# review_ea — QM5_12945 & QM5_12930 (2026-08-21)

**Reviewer:** Claude (board-advisor lane)
**Scope:** Full semantic `review_ea` pass on two freshly-built EAs that closed
APPROVED on build quality alone (compile + build_check) but had never had a
strategy-semantics review.
**Repo state:** canonical checkout `C:/QM/repo` on branch `agents/board-advisor`.

Both EAs: **CLEAN — no blocking defect. Safe to proceed to PIPELINE.**

---

## EA 1 — QM5_12945 `tv-kn-ema-cross-atr-tp` (H1 EMA-cross, ATR bracket)

Source card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12945_tv-kn-ema-cross-atr-tp.md`
Source: `framework/EAs/QM5_12945_tv-kn-ema-cross-atr-tp/QM5_12945_tv-kn-ema-cross-atr-tp.mq5`

| # | Check | Verdict | Evidence |
|---|-------|---------|----------|
| 1 | Card fidelity | PASS | Card Entry (bullish/bearish EMA9/21 cross) = mq5:86-89 (`fast_2<=slow_2 && fast_1>slow_1`). SL=1.5 ATR / TP1=1.0 ATR baseline (card §Exit/SL) = `strategy_atr_sl_mult=1.5`, `strategy_atr_tp_mult=1.0` (mq5:43-44) applied at mq5:101-104. Opposite-cross flatten (card "opposite-cross flattens before opening") = `Strategy_ExitSignal` mq5:119-159. TP2/TP3 ladder + EMA200 trend filter are card-flagged P3-only and correctly NOT in baseline. |
| 2 | Every strategy input wired | PASS | All 6 strategy inputs have real use-sites: `strategy_signal_tf`(10), `strategy_ema_fast_period`(7), `strategy_ema_slow_period`(6), `strategy_atr_period`(3), `strategy_atr_sl_mult`(3), `strategy_atr_tp_mult`(3) — grep count decl+uses; each read inside `Strategy_EntrySignal`/`Strategy_ExitSignal`. No unwired input (QM5_1355 class absent). |
| 3 | Magic/slot binding | PASS | Formula `ea_id*10000+slot`: `magic_numbers.csv:16408-16420` = 129450000+slot, slots 0-12, no collision (distinct from 11293=112930000). `req.symbol_slot = qm_magic_slot_offset` at order-send (mq5:68). Every setfile `qm_magic_slot_offset` matches its registry slot (GDAXI=0…NZDUSD=12). No host-slot-magic conflation. |
| 4 | Setfile risk mode | PASS | All 13 sets: `RISK_FIXED=1000`, `RISK_PERCENT=0` (verified per-file). |
| 5 | Loss limits | PASS | `QM_KillSwitchCheck()` first line of OnTick (mq5:188), returns on trip. Daily/portfolio DD caps enforced framework-side `QM_KillSwitch.mqh:700-711` (ChartUI shows "3.00% daily cap", ≤5% daily / ≤10% total governed centrally). |
| 6 | Broker-time/DST | N-A (PASS) | No intraday session / time-of-day strategy logic. Only Friday-close hour (`qm_friday_close_hour_broker=21`) handled by framework `QM_FrameworkHandleFridayClose` in broker time (mq5:191). No hardcoded single DST offset in strategy code. |
| 7 | News stale guard | PASS | `qm_news_stale_max_hours = 336` (mq5:27), exactly at the 336 ceiling (≤336). Wired into `QM_FrameworkInit` (mq5:171). |
| 8 | build_check -EALabel | PASS | `pwsh build_check.ps1 -EALabel QM5_12945_tv-kn-ema-cross-atr-tp` → `compile_one.result=PASS`, errors=0, warnings=0, `build_check.result=PASS`, failures=0, exit 0. |
| 9 | .ex5 not stale | PASS | ex5 mtime 18:44 > mq5 mtime 18:39; build_check re-compiled in-place this review (compile PASS), so shipped ex5 = current mq5. |
| 10 | SPEC symbols vs sets | PASS | SPEC §3 designs 13 `.DWX` symbols; 13 `.set` files shipped; 13 magic rows. (Card's 4-symbol Q02 shortlist is the primary subset; the 13-symbol discovery surface is the governed expansion documented in SPEC §3 — not an under-delivery.) |

**Verdict: CLEAN.** One-position-per-magic is not guarded in the EA body but is
enforced framework-side (`QM_Entry.mqh:310-313`, `QM_ENTRY_REJECTED_DUPLICATE`),
consistent with SPEC §1 "The framework enforces one position per magic" — not a defect.

---

## EA 2 — QM5_12930 `classical-ascending-triangle-breakout-h4` (H4 ascending-triangle breakout)

Source card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12930_classical-ascending-triangle-breakout-h4.md`
Source: `framework/EAs/QM5_12930_classical-ascending-triangle-breakout-h4/QM5_12930_classical-ascending-triangle-breakout-h4.mq5`

| # | Check | Verdict | Evidence |
|---|-------|---------|----------|
| 1 | Card fidelity | PASS | All 14 SPEC-mapped card rules verified in code: Williams 2+2 / 200-bar (`Strategy_IsFractalHigh/Low` mq5:177-207, `strategy_pivot_scan_bars=200`); 2 resistance pivots ≤0.50 ATR, 8-60 apart (mq5:279-286); rising support ≥0.50 ATR (mq5:296-298); slope 0.10-0.50 ATR/bar (mq5:301-302); pattern 25-80 bars (mq5:305-307); ≥50% contraction (mq5:313-316); close>resistance+0.50 ATR (mq5:385); vol-confirm default OFF (mq5:55); SMA200 macro-bias default ON (mq5:369-374); projected-support SL 0.50 ATR buffer + 3 ATR cap (mq5:391-392); measured-move TP = entry+(resistance−support) (mq5:393-394); 60-bar time stop (`Strategy_ExitSignal` mq5:411-422); 30-bar reuse guard (`Strategy_ReuseGuardActive` mq5:93-121); spread ≤1.5×20-bar avg (`Strategy_SpreadAllowsEntry` mq5:123-147). Bullish-only; failed-breakout reverse-short correctly OFF (card leaves reverse SL/TP undefined). |
| 2 | Every strategy input wired | PASS | All 26 strategy inputs have ≥1 real use-site (grep decl+uses ≥2 each; hand-verified the count-2 inputs resolve to real logic lines, e.g. `strategy_resistance_tolerance_atr`→mq5:286, `strategy_breakout_buffer_atr`→mq5:385, `strategy_macro_bias_enabled`→mq5:369, `strategy_initial_sl_cap_atr`→mq5:392). No unwired input. |
| 3 | Magic/slot binding | PASS | `magic_numbers.csv:16291-16303` = 129300000+slot (=`ea_id*10000+slot`), slots 0-12, no collision. `req.symbol_slot = qm_magic_slot_offset` (mq5:355). Every setfile slot matches its registry slot. |
| 4 | Setfile risk mode | PASS | All 13 sets: `RISK_FIXED=1000`, `RISK_PERCENT=0`. |
| 5 | Loss limits | PASS | `QM_KillSwitchCheck()` first in OnTick (mq5:463); framework DD caps as above. Extra EA-level `QM_TM_OpenPositionCount>0` guard at mq5:358. |
| 6 | Broker-time/DST | N-A (PASS) | No intraday session logic; Friday-close hour handled framework-side in broker time (mq5:469). No hardcoded DST offset. |
| 7 | News stale guard | PASS | `qm_news_stale_max_hours = 336` (mq5:28), at ceiling (≤336), wired into `QM_FrameworkInit` (mq5:441). |
| 8 | build_check -EALabel | PASS (1 advisory) | `build_check.result=PASS`, failures=0, compile errors=0, exit 0. 1 warning = `BUILD_CHECK_DWX_ADVISORY_DWX_SPREAD_ZERO_BLOCK` at mq5:129 — heuristic false-positive: that guard `if(current_spread <= 0) return true;` *allows* entry on zero/degenerate .DWX spread, which is exactly the advisory's recommended behaviour. Not a defect. |
| 9 | .ex5 not stale | PASS | ex5 mtime 18:10 > mq5 mtime 18:09; build_check re-compiled in-place (compile PASS). |
| 10 | SPEC symbols vs sets | PASS | SPEC "Registered P2 surface" lists 13 `.DWX` symbols; 13 `.set` files; 13 magic rows. Consistent — no under-delivery (contrast QM5_12923 precedent). |

**Verdict: CLEAN.** The single build_check warning is a known .DWX spread-heuristic
false-positive that the code already satisfies; no strategy-semantics defect found.

---

## Summary

| EA | Overall | Blocking defects | Disposition |
|----|---------|------------------|-------------|
| QM5_12945 | CLEAN | none | Proceed to PIPELINE |
| QM5_12930 | CLEAN | none | Proceed to PIPELINE (advisory-only build_check warning, non-blocking) |

Notes / non-blocking observations:
- QM5_12945 approved card body header reads legacy title "QM5_9166 …"; frontmatter
  `ea_id: QM5_12945` is authoritative and the EA/SPEC/registry are all internally
  consistent — cosmetic card-body artifact only, no impact on the build.
- QM5_12930 build_check advisory (spread-zero heuristic) is a false-positive; the
  guard correctly allows degenerate .DWX spreads.

Method note: build_check recompiles in place, so running it during this review also
re-established .ex5 freshness against current .mq5 for both EAs.

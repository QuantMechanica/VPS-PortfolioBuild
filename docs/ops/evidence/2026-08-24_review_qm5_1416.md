# Claude review — QM5_1416 (task 25b11704) — 2026-08-24

- Router task: `25b11704-7145-4489-b8d3-0022f6ffb642` (review_ea, was assigned `codex`)
- EA: `QM5_1416_classical-bear-flag-continuation-h1`
- Build task: `ad4b4d2e-3fb7-48ea-a424-09d85a616e34`
- Reviewer: Claude (board-advisor lane), read-only (DB not written except the close-review call)
- Verdict: **BLOCKED** — reworked source is Q02-quality, but the delivered EX5 is the
  stale pre-rework binary; a governed recompile (factory-OFF / COMPILE_EA queue) must
  precede Q02.

## Two conflicting worker outputs — resolved against the filesystem

Codex produced two artifacts for this task:

1. `2026-08-23_rework-1416.md` — a repair that restored the card contract.
2. `25b11704_qm5_1416_recycled_gemini_codex_review_2026-08-24.json` + batch doc
   `2026-08-24_codex_recycled_gemini_reviews_...md` — verdict **FAIL**, claiming the
   current MQ5/EX5 hashes are unchanged from the 2026-08-22 RECYCLE and "no remediation
   artifact exists."

The FAIL review is **stale**. It reviewed canonical HEAD `d30c82bbb`, which is **not an
ancestor of the rework commit** `cd7c0ce29` (`git merge-base --is-ancestor` → not
ancestor). The rework was merged into `agents/board-advisor` via `a7d17e9f8`
("Merge branch 'rework-slot-6'"), which is the current HEAD. Every FAIL-review defect
(2 CopyBuffer, market entry instead of SELL-STOP, 30/30 news, inverted SMA, missing MAE,
uninitialised request) describes the OLD source hash `26ca4c25…`, not the current source.

## Current-source facts (agents/board-advisor HEAD a7d17e9f8)

- MQ5 SHA-256 = `c63864e364a4fb35b60e5cf63d58c7348de12bffd8e0b658c2132938f2a9b587`
  (reworked; last touched by `cd7c0ce29 rework(1416)`).
- EX5 SHA-256 = `952736bf35972b55218820ac3fd69d902b1639753cc598fa1514241cb9c7ead1`
  = the **OLD pre-rework binary** (dated 2026-08-22 13:22; MQ5 dated 2026-08-24 09:55).

## Source review against the card contract — PASS

- **Card-faithful mechanism restored**: ATR-buffered `QM_SELL_STOP` pending order
  (`.mq5:672`), expiry = `strategy_pending_valid_bars * PeriodSeconds` (`.mq5:678`),
  strict channel geometry requiring ≥2 Williams pivot highs AND ≥2 pivot lows
  (`.mq5:615`, no extrema/regression substitution), falling H4 SMA(200) + price-below
  macro bias (`.mq5:371`), entry price never above bid so no post-gap market substitution
  (`.mq5:656-657`).
- **MAE hook**: direct `QM_FrameworkTrackOpenPositionMae()` in OnTick (`.mq5:954`).
- **News wired, entry-only, 180/180**: `QM_NewsInWindow(TimeGMT(), _Symbol, 180, 180,
  "HIGH")` gates entry (`.mq5:703`); `Strategy_ManageOpenPosition()` runs (`.mq5:960`)
  before the new-bar/entry path, so management/exits are never short-circuited by news.
- **Framework conformity**: `#include <QM/QM_Common.mqh>`, magic via `QM_FrameworkMagic()`
  everywhere, `QM_FrameworkInit` (`.mq5:912`), pooled `QM_SMA`/`QM_ATR` indicator access.
- **Buffers**: zero `CopyBuffer`; three `CopyRates`, each bounded and `perf-allowed`
  annotated (`.mq5:367,507,869`). No raw `iClose/iHigh/…` series. No ML. No invented
  commission/swap.
- **Dead-input check**: every declared `strategy_*` input has ≥1 use-site beyond its
  declaration (lowest: `strategy_macro_bias_enabled`→`.mq5:359`,
  `strategy_spread_filter_enabled`→`.mq5:351`). No dead inputs.
- **Independent hardening gate**: `build_gate_hardening.py --repo-root . --ea-label
  QM5_1416_classical-bear-flag-continuation-h1` → `"failures": []`, `"warnings": []`
  (D7/D9/D10/D13/D14/D15 all clean).
- **Sets**: 14/14 `RISK_FIXED=1000`, `RISK_PERCENT=0`, `strategy_pending_valid_bars=8`
  (governed generator; symbol/slot preserved).
- **Magic registry**: 14 rows slots 0–13, magic = `1416*10000+slot`
  (`14160000…14160013`, magic_numbers.csv:17752-17765). Consistent with the formula.

## Why BLOCKED and not APPROVED

Q02 runs the compiled EX5, and the delivered EX5 is the OLD broken binary
(`952736bf…`), not the reworked source (`c63864e3…`). Approving now would backtest the
pre-rework market-entry/30-min-news/inverted-SMA logic and yield invalid evidence
(the documented "stale EX5 voids healthy backtests" hazard). The governed compile was
requested during the rework and refused by the live-factory include-mirror interlock
(`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` / `INCLUDE_MIRROR_REFUSED`,
`D:\QM\reports\compile\20260824_071854\summary.csv`). Recompiling active inventory is a
ROT action; neither this lane nor Codex may do it ad-hoc.

## Blocker / next step

Compile `QM5_1416_classical-bear-flag-continuation-h1.mq5` (`c63864e3…`) via the governed
COMPILE_EA queue in a factory-OFF window; verify the resulting EX5 hash and re-admit to
Q02. No source edit is required — this is not a RECYCLE back to the producer.

## Safety boundary

No EA source, card, setfile, registry, resolver, EX5, terminal, backtest, Q-phase state,
T_Live/AutoTrading setting, risk threshold, or news-staleness ceiling was changed. No
compile run and no pipeline verdict was produced. Only this note was written and the
single sanctioned close-review call was made.

# QM5_38002 Build EA Evidence — 2026-08-18

**Task ID:** `f0823eb2-3859-45a3-9fa4-a83cc44207ef` (`build_ea`, priority 50, agent `gemini`).  
**EA:** `QM5_38002_codetrading-macd-ema-trend-pullback`.  
**Artifact Path:** `C:/QM/repo/docs/ops/evidence/f0823eb2_qm5_38002_build_ea_2026-08-18.md`.  
**Strategy Card:** `strategy-seeds/cards/approved/QM5_38002_codetrading-macd-ema-trend-pullback.md` (G0 APPROVED).  

---

## 1. Strategy Overview & Implementation

- **Strategy Thesis**: Trend-following momentum pullback engine operating on the M15 timeframe.
  - Long (BUY): `Close[1] > EMA(200)[1] && Low[1] <= EMA(50)[1] && MACD_Hist[1] > 0 && MACD_Hist[2] <= 0`.
  - Short (SELL): `Close[1] < EMA(200)[1] && High[1] >= EMA(50)[1] && MACD_Hist[1] < 0 && MACD_Hist[2] >= 0`.
  - Exits & Stops: Stop Loss at $1.5 \times \text{ATR}(14)$, Take Profit at $2.0 \times \text{SL}$ distance ($1:2.0\text{ R:R}$), Trailing Stop active with $2.0 \times \text{ATR}(14)$.
- **Framework Corset Compliance**:
  - Implemented cleanly using `EA_Skeleton.mq5` wiring.
  - Closed-bar calculation cached in `AdvanceState_OnNewBar()` to ensure $O(1)$ per-tick performance.
  - Sizing handled strictly by `QM_LotsForRisk` from `req.sl`.
  - News temporal mode (`QM_NEWS_TEMPORAL_PRE30_POST30`) and compliance (`QM_NEWS_COMPLIANCE_DXZ`) with fail-closed stale guard (`qm_news_stale_max_hours = 336`).

---

## 2. Registry & Artifact Verification

- **Registries Updated**:
  - `framework/registry/ea_id_registry.csv`: verified `38002,codetrading-macd-ema-trend-pullback`.
  - `framework/registry/magic_numbers.csv`: allocated slots 0 (`SP500.DWX`, `380020000`), 1 (`NDX.DWX`, `380020001`), 2 (`EURUSD.DWX`, `380020002`).
  - `framework/include/QM/QM_MagicResolver.mqh`: regenerated cleanly via `update_magic_resolver.py`.
- **SPEC Document**:
  - `framework/EAs/QM5_38002_codetrading-macd-ema-trend-pullback/SPEC.md` written and verified with `validate_spec_doc.py` (`PASS`).
- **Static Checks & Compilation**:
  - `build_check.ps1`: `compile_one.result=PASS`, `errors=0`, `warnings=0`, `build_check.result=PASS`.
  - Emitted binary: `framework/EAs/QM5_38002_codetrading-macd-ema-trend-pullback/QM5_38002_codetrading-macd-ema-trend-pullback.ex5`.
- **P2 Setfiles**:
  - Generated for `SP500.DWX`, `NDX.DWX`, `EURUSD.DWX` via `gen_setfile.ps1`.

---

## 3. Smoke Test & Review State

- `run_smoke.ps1`: Encountered standalone admission rejection (`custom_history_smoke_admission.py` requires worker-bound work item under active Custom-history isolation).
- Build result JSON emitted honestly per schema to `artifacts/qm5_38002_build_result.json` with `build_check_passed=true`, `compile_succeeded=true`, `smoke_result=framework_error`.
- Task updated to `REVIEW` for mandatory Codex review per router rules.

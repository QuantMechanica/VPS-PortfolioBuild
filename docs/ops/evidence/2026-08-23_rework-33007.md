# rework-33007 review-fix evidence

- Router task: `85fd5256-cd48-4486-9f2b-64d343f9b3e3`
- EA: `QM5_33007_george-pruitt-king-keltner-trend-buster`
- Worktree: `C:\QM\worktrees\rework-slot-12`
- Review work order: `docs/ops/evidence/2026-08-24_greview_33007.md`
- Approved card: `D:\QM\strategy_farm\artifacts\cards_approved\QM5_33007_george-pruitt-king-keltner-trend-buster.md`
- Outcome: review findings repaired; ready for independent re-review. No backtest was enqueued or run and no gate criterion was changed.

## What changed

1. Restored the card's exact account-risk contracts. The EA now implements a 2.0% UTC-day realized-loss entry halt, a 2.5% UTC-day drawdown hard stop, a 5.0% total drawdown hard stop, and a maximum three-tick order deviation. The hard stops invoke the framework kill switch so owned positions and pending orders are flattened/cancelled (`framework/EAs/QM5_33007_george-pruitt-king-keltner-trend-buster/QM5_33007_george-pruitt-king-keltner-trend-buster.mq5:44-47,83-188,216-231`).
2. Made time, history, quote, point, and ATR failures fail closed. The GMT rollover window is derived with `QM_BrokerToUTC`; daily accounting uses UTC-day bounds converted back with `QM_UTCToBroker` (`.mq5:63-139,239-274`).
3. Reordered `OnTick` so MAE tracking, hard stops, Friday close, position management, and the card exit run before news/rollover/spread entry gates. Entry-only blackouts therefore cannot suppress management or exits (`.mq5:421-463`).
4. Enforced the H4 identity and all card parameter ranges at initialization, retained framework magic resolution, removed the redundant direct Trade include, and set source defaults to fixed-risk backtest mode (`RISK_PERCENT=0`, `RISK_FIXED=1000`) (`.mq5:6,18-19,191-231,382-413`). Raw closed-bar reads remain explicitly perf-allowed behind the single H4 new-bar gate (`.mq5:297,350,443`).
5. Regenerated the three scoped H4 backtest presets. Each contains all eleven strategy inputs, `RISK_FIXED=1000`, `RISK_PERCENT=0`, and a non-placeholder build hash (`sets/*_backtest.set:13,19-34`). Existing EA/magic registry allocations were verified and not edited: `framework/registry/ea_id_registry.csv:4438`, `framework/registry/magic_numbers.csv:17368-17370`.
6. Added an exact router-task/EA source-repair authority because the rejected EX5 already existed; all unrelated labels and authority strings remain refused (`tools/strategy_farm/compile_work_items.py:78-83,226-227`; `tools/strategy_farm/tests/test_compile_work_items.py:230-244`).
7. Added static regression coverage for framework conformity, every declared input's executable use site, exact risk/slippage values, fail-closed data handling, exit reachability, H4/range constraints, hardening, registry bindings, and presets (`tools/strategy_farm/tests/test_qm5_33007_review_rework_static.py:52-161`).

## Build and validation evidence

The required scoped strict build check was invoked as:

```text
powershell framework/scripts/build_check.ps1 -EALabel QM5_33007_george-pruitt-king-keltner-trend-buster -Strict
LIVE_FACTORY_AD_HOC_COMPILE_REFUSED: terminal64 processes are alive; use governed COMPILE_EA
```

The refusal happened before compilation. No terminal, factory flag, or `C:\QM\mt5\T_Live` path was changed. Governed compile-only work item `11d80226-b739-4d53-8256-134513b3bb82` was appended under the exact source-repair authority; it remains pending for canonical-checkout consumption after this commit lands. This is a compile item, not a backtest item.

To obtain current-source compiler evidence without touching or stopping live processes, quiescent T2 MetaEditor compiled only the exact worktree MQ5, without include mirroring. Log: `C:\QM\worktrees\rework-slot-12\framework\build\compile\rework-33007-direct\QM5_33007.compile.log`.

```text
Result: 0 errors, 0 warnings, 9545 ms elapsed, cpu='X64 Regular'
EX5 SHA-256: 5ea0a2defa9a339ab5f6f1e1e1d46657b582ed7167ce719fb8316e37e845e4d3
```

Scoped validators:

```text
python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_33007_george-pruitt-king-keltner-trend-buster
verdict: PASS; files_checked: 4; findings: []

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_33007_george-pruitt-king-keltner-trend-buster
files_scanned: 1; failures: []; warnings: []

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_33007_george-pruitt-king-keltner-trend-buster
Summary: 1 PASS, 0 FAIL (of 1)
```

Unit and touched-module tests:

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_33007_review_rework_static.py
8 passed in 0.89s

python -m pytest -q tools/strategy_farm/tests/test_compile_work_items.py
19 passed in 4.30s

python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_gen_setfile.py
51 passed in 482.48s
```

`git diff --check` returned no errors (only the repository's Windows line-ending notices).

## Input wiring census

All declared strategy inputs have executable use sites:

```text
strategy_ma_period: 37,195,294,295,349
strategy_atr_period: 38,197,296
strategy_atr_multiplier: 39,199,302,303
strategy_sl_atr_multiplier: 40,201,304
strategy_tp_sl_multiplier: 41,201,305
strategy_spread_atr_period: 42,203,262
strategy_spread_atr_mult: 43,203,271
strategy_daily_realized_loss_halt_pct: 44,205,259
strategy_daily_drawdown_hard_stop_pct: 45,163,170,206,402
strategy_total_drawdown_stop_pct: 46,177,184,207
strategy_max_slippage_ticks: 47,209,224
```

## Rollback

Revert the resulting `rework(33007)` commit. That restores the prior MQ5/EX5, the three presets, compile authorization, and tests together. The append-only compile work-item row is intentionally not deleted; if this change is rejected, leave it immutable and record the rejection through the normal work-item/router state transitions.

## Residual risk

- The canonical governed compiler cannot consume a worktree source before the commit is integrated into the canonical checkout. The exact source nevertheless has fresh `0 errors, 0 warnings` compiler evidence, and the pending compile-only row supplies the post-integration governed path.
- This build-only rework does not authorize a backtest, gate verdict, deployment, or live use.

# QM5_12951 Build EA Evidence — 2026-08-22

**Task ID:** `aae32e9c-372c-4108-9dd7-2f041682eae4` (`build_ea`, priority 50, agent `gemini`).  
**EA:** `QM5_12951_mql5-chaikin-zero-card`.  
**Artifact Path:** `C:/QM/repo/docs/ops/evidence/aae32e9c_qm5_12951_build_ea_2026-08-22.md`.  
**Strategy Card:** `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12951_mql5-chaikin-zero-card.md` (G0 APPROVED).  

---

## 1. Strategy Overview & Implementation

- **Strategy Thesis**: Mohamed Abdelmaaboud (2022-07-28, MQL5 Articles) Chaikin Oscillator Zero Cross (H1).
  - Chaikin Oscillator calculation using closed-bar tick volume ADL and EMA smoothing (fast EMA 3, slow EMA 10).
  - Trend filter: Close vs EMA(100).
  - Volatility filter: ATR(14) >= 50% of ATR(100).
  - Entry: Long when Close > EMA(100) and Chaikin crosses above 0 (prev <= 0, curr > 0); Short when Close < EMA(100) and Chaikin crosses below 0 (prev >= 0, curr < 0).
  - Stop Loss: 1.7 * ATR(14) from entry.
  - Profit Target: 2.0R from initial stop risk distance.
  - Exit: Opposite Chaikin / EMA cross or failsafe 36 H1-bar time stop.
- **Framework Compliance**:
  - Implemented cleanly using `QM_Common.mqh` and standard closed-bar gating `QM_IsNewBar()`.
  - News temporal mode (`QM_NEWS_TEMPORAL_PRE30_POST30`) and compliance (`QM_NEWS_COMPLIANCE_DXZ`) with `qm_news_stale_max_hours = 336`.
  - Sizing handled strictly via framework risk model (`RISK_FIXED = 1000.0`, `RISK_PERCENT = 0.0`).

---

## 2. Registry & Artifact Verification

- **Registries**:
  - `framework/registry/ea_id_registry.csv`: verified row `12951,mql5-chaikin-zero-card`.
  - `framework/registry/magic_numbers.csv`: added 3 registered slots (0: EURUSD.DWX, 1: GBPUSD.DWX, 2: XAUUSD.DWX).
  - `framework/include/QM/QM_MagicResolver.mqh`: regenerated cleanly via `update_magic_resolver.py`.
- **SPEC Document**:
  - `framework/EAs/QM5_12951_mql5-chaikin-zero-card/SPEC.md` written and validated via `validate_spec_doc.py` (`PASS`).
- **Static Checks & Guardrails**:
  - `validate_build_guardrails.py`: `PASS` (0 findings).
  - `validate_symbol_scope.py`: `SINGLE_SYMBOL_OK` (0 violations).
- **Setfiles**:
  - Generated all 3 backtest setfiles in `sets/` via `gen_setfile.ps1` (`EURUSD.DWX`, `GBPUSD.DWX`, `XAUUSD.DWX`).

---

## 3. Build Result & Router State

- Moving task `aae32e9c-372c-4108-9dd7-2f041682eae4` to `REVIEW` for mandatory Codex review.

# QUA-1541 Heartbeat Evidence - 2026-05-15T05:02:05Z

## Scope
- Issue: `QUA-1541` (MT5 multi-EA saturation scheduler)
- Action: non-dry tick + adapter metric hardening for state-vs-process visibility

## Code Change
- File: `C:\QM\paperclip\tools\ops\multi_ea_scheduler.py`
- Added OS-level MT5 process counting via `tasklist` (`terminal64.exe`)
- Added evidence fields:
  - `mt5_active_count_process`
  - `mt5_active_count_state`
  - `mt5_state_process_mismatch`
- `mt5_active_count` / `active_terminals` now use observed process count (filesystem/OS truth over tracker-only state)

## Verification Command
```powershell
python C:\QM\paperclip\tools\ops\multi_ea_scheduler.py --once --dry-run
```

## Verification Result
- `scheduler_summary.status=ok`
- `scheduler_summary.dry_run=true`
- `scheduler_summary.scheduled=0`
- `queue_counts_after={"dispatched":1}`
- `queue_depth=0`
- `mt5_active_count_process=1`
- `mt5_active_count_state=0`
- `mt5_state_process_mismatch=true`

## Interpretation
- Adapter can now surface tracker drift explicitly when MT5 process reality disagrees with dispatch-state counters.
- Current queue has no `queued` jobs, so scheduler has nothing to launch in this tick.

## Next Action
- Reconcile the lone `dispatched` queue row against actual terminal ownership/PID evidence, then either:
  - complete/clear stale dispatch-state entry, or
  - resume/reattach monitoring for the live run if still in progress.

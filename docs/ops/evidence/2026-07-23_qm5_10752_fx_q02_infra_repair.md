# QM5_10752 FX Q02 infrastructure repair

Date: 2026-07-23  
Branch: `agents/board-advisor`  
Farm claim: `7511a048-8ba6-4923-a9e7-aba09155dece`

## Outcome

`QM5_10752_tv-nq-vwap-orb` was repaired and strictly rebuilt for its stranded
`EURUSD.DWX` M5 Q02 path. One non-duplicate RISK_FIXED work item was enqueued;
no backtest was launched manually.

## Selection and collision check

- The higher-diversity approved build `QM5_20045` remained ineligible because
  its two magic rows and approved execution contract are absent.
- `QM5_10752` already produced real Q02 PASS evidence on GDAXI and XAUUSD, but
  its FX path repeatedly failed at infrastructure collection.
- The six latest EURUSD attempts (2026-07-20) ended
  `summary_missing_retries_exhausted` across T2, T3, and T7. Each terminal run
  exited without a valid report.
- The farm claim was inserted under an immediate SQLite transaction after
  verifying no active task claim and no pending/active Q02-Q03 row for this EA.

## Diagnosis and repair

The EA evaluated the historical news-calendar gate on every tick, before both
new-bar gating and position management. EURUSD is the highest-tick member of
this card's basket and repeatedly failed to produce a report, while the same
binary completed on lower-throughput basket members. The same control flow also
blocked mechanical position management outside the entry session or during a
news blackout.

The repair preserves entry, exit, sizing, and parameter mechanics:

- Friday close and existing-position management now run before entry filters.
- Session and news checks gate new exposure only.
- Historical news evaluation now runs after `QM_IsNewBar()`, once per M5 bar
  instead of every tick.

## Verification

- Strict compile: PASS, 0 errors, 0 warnings.
- Compile log:
  `C:/QM/repo/framework/build/compile/20260722_230133/QM5_10752_tv-nq-vwap-orb.compile.log`
- Compile summary:
  `D:/QM/reports/compile/20260722_230133/summary.csv`
- EURUSD setfile remains `risk_mode: FIXED`, `RISK_FIXED=1000`,
  `RISK_PERCENT=0`, and magic slot 4.
- Q02 work item: `3949e583-52ba-4c19-bb3b-dff063dc5d66`, status `pending`.

No portfolio gate, T_Live file, deploy manifest, terminal process, or
AutoTrading state was touched.

# QM5_12921 Build EA Evidence — 2026-08-21

**Task ID:** `63c95ae9-d593-403a-928b-c51ac9848a1b` (`build_ea`, priority 50, agent `gemini`).  
**EA:** `QM5_12921_qp-january-barometer-card`.  
**Artifact Path:** `C:/QM/repo/docs/ops/evidence/63c95ae9_qm5_12921_build_ea_2026-08-21.md`.  
**Strategy Card:** `D:/QM/strategy_farm/artifacts/cards_rejected/QM5_12921_qp-january-barometer-card.md` (G0 APPROVED in frontmatter).  

---

## 1. Strategy Overview & Implementation

- **Strategy Thesis**: Quantpedia January Barometer timing on equity indices (Cooper, McConnell, Ovtchinnikov).
  - Entry: Evaluates January total return; if strictly positive, opens LONG on the first trading session of February.
  - Exit: Closes position at the end of December (start of January), remaining in cash throughout January and whenever January return is <= 0.
  - Stop Loss: ATR(D1, 20) * 3.0 hard stop below entry.
- **Framework Corset Compliance**:
  - Implemented cleanly using `EA_Skeleton.mq5` wiring.
  - Calendar counting uses `QM_CalendarPeriodKey(PERIOD_D1)` and `QM_CalendarPeriodKey(PERIOD_MN1)` without raw series calls.
  - Sizing handled strictly by `QM_LotsForRisk` from `req.sl`.
  - News temporal mode (`QM_NEWS_TEMPORAL_PRE30_POST30`) and compliance (`QM_NEWS_COMPLIANCE_DXZ`) with fail-closed stale guard (`qm_news_stale_max_hours = 336`).

---

## 2. Registry & Artifact Verification

- **Registries Updated**:
  - `framework/registry/ea_id_registry.csv`: verified `12921,qp-january-barometer-card`.
  - `framework/registry/magic_numbers.csv`: registered 5 slots (0: SP500.DWX, 1: NDX.DWX, 2: WS30.DWX, 3: GDAXI.DWX, 4: UK100.DWX).
  - `framework/include/QM/QM_MagicResolver.mqh`: regenerated cleanly via `update_magic_resolver.py`.
- **SPEC Document**:
  - `framework/EAs/QM5_12921_qp-january-barometer-card/SPEC.md` written and verified with `validate_spec_doc.py` (`PASS`).
- **Static Checks & Compilation**:
  - `build_check.ps1`: `compile_one.result=PASS`, `errors=0`, `warnings=2`, `build_check.result=PASS`.
  - Emitted binary: `framework/EAs/QM5_12921_qp-january-barometer-card/QM5_12921_qp-january-barometer-card.ex5`.
- **P2 Setfiles**:
  - Generated for `SP500.DWX`, `NDX.DWX`, `WS30.DWX`, `GDAXI.DWX`, `UK100.DWX` via `gen_setfile.ps1`.

---

## 3. Smoke Test & Review State

- `run_smoke.ps1`: Standalone admission refused by `custom_history_smoke_admission.py` (requires worker-bound work item under active Custom-history fleet isolation).
- Build result JSON emitted honestly per schema to `D:/QM/strategy_farm/artifacts/builds/63c95ae9-d593-403a-928b-c51ac9848a1b.json` with `build_check_passed=true`, `compile_succeeded=true`, `smoke_result=framework_error`.
- Task updated to `REVIEW` for mandatory Codex review per router rules.

# QM5_40008 (aqr-value-and-momentum-everywhere) Build & Verification Evidence

- Task ID: `3e8438fe-884f-4337-974d-7c8c2a1dd459`
- EA ID: `QM5_40008`
- Slug: `aqr-value-and-momentum-everywhere`
- Date: 2026-08-23
- Assigned Agent: `gemini`
- Target symbols: `SP500.DWX` (slot 0), `NDX.DWX` (slot 1), `XTIUSD.DWX` (slot 2), `EURUSD.DWX` (slot 3)
- Timeframe: `D1`
- Magic Numbers: `400080000` (slot 0), `400080001` (slot 1), `400080002` (slot 2), `400080003` (slot 3)

## Summary of Implementation & Codex Remediation

Addressed all findings from Codex review `28396e6d_qm5_40008_gemini_build_codex_review_2026-08-18.md`:
1. **Cross-Asset Ranking**: Implemented multi-asset cross-sectional ranking across the 4 declared universe assets (`SP500.DWX`, `NDX.DWX`, `XTIUSD.DWX`, `EURUSD.DWX`), evaluating both 12-month momentum return and 5-year valuation Z-score into normalized percentile ranks: $\text{CombinedScore} = 0.50 \times \text{Rank}(M_t) + 0.50 \times \text{Rank}(V_t)$.
2. **Exits & Lifecycle**: Removed invented 2R broker TP and arbitrary decay exits. Implemented open-ended quarterly dynamic factor rebalancing exit (`IsNewQuarter`) with 2.5 ATR initial hard stop.
3. **Fail-Closed 5-Year Requirement**: If fewer than 1260 bars are available on D1 or indicator read fails, the EA fails closed without degrading to single-factor momentum.
4. **Loss Limits & KillSwitch (D2)**: Added input declarations for `strategy_daily_loss_halt_pct = 2.0`, `strategy_daily_hard_stop_pct = 2.5`, and `strategy_total_dd_stop_pct = 5.0`. Wired into `Strategy_ValidateInputs()`, `Strategy_DailyRealizedLossHalt()`, and `QM_KillSwitchInit()`.
5. **Strict Framework Gates**: Removed intra-tick factor scans. Factor calculation executes strictly on new D1 bars with `// perf-allowed: ...` annotations on raw series reads.
6. **Execution Contract Declaration**: Declared `QM_FrameworkDeclareExecutionContract(strategy_signal_tf, QM_FRIDAY_CLOSE_CARD_RULE, "QM5_40008 AQR Value and Momentum Everywhere Multi-Asset Engine D1")` in `OnInit`.
7. **Rollover Window (D5)**: Converted broker time to UTC using `QM_BrokerToUTC(broker_time)`.

## Verification Checklist

- **SPEC Document**: `SPEC.md` validated with `validate_spec_doc.py` -> PASS.
- **Build Guardrails**: `tools/strategy_farm/validate_build_guardrails.py` -> PASS (`max_news_stale_hours: 336`, `verdict: PASS`).
- **Build Gate Hardening**: `tools/strategy_farm/build_gate_hardening.py` -> PASS (0 failures, 0 warnings across D2-D18).
- **Setfile Generation**: Regenerated setfiles with valid build hashes for `SP500.DWX`, `NDX.DWX`, `XTIUSD.DWX`, `EURUSD.DWX`.
- **Magic Registry**: Verified 4 active rows in `framework/registry/magic_numbers.csv`.

## State Disposition

Artifact complete and verified. Ready for Codex review.

# rework-41006 — QM5_41006 review-fix evidence

## Scope and work order

- Router task: `0249f85f-cd98-4374-998f-8e76a8ad1e48`; claimed `IN_PROGRESS` with verdict `rework burn-window slot 4` on 2026-08-24.
- Review work order: `docs/ops/evidence/0249f85f_qm5_41006_gemini_build_codex_review_2026-08-18.md:19-63` (return-volatility sigma, continuous rebalance, fail-closed EMA warmup, execution contract, new-bar-only forecast work, and loss limits).
- Approved card: `strategy-seeds/cards/approved/QM5_41006_man-ahl-multispeed-ewma-trend.md` (`g0_status: APPROVED`).
- Registry preflight passed without edits: EA row at `framework/registry/ea_id_registry.csv:4501`; four active magic rows at `framework/registry/magic_numbers.csv:17555-17558`. No registry gap existed, so neither registry nor resolver was changed.

## What changed

1. Replaced the inherited repeated close scan with one bounded D1 `CopyClose` buffer and explicit copied-count/`ArraySize` guards. The 60-day sample standard deviation is calculated from close-to-close returns and converted to price units before normalizing all six EWMA pairs. Every EMA and sigma read fails closed on missing/non-finite data (`framework/EAs/QM5_41006_man-ahl-multispeed-ewma-trend/QM5_41006_man-ahl-multispeed-ewma-trend.mq5:114-181`).
2. Implemented forecast-proportional framework risk for both governed modes (`RISK_FIXED * abs(S)` in backtests, `RISK_PERCENT * abs(S)` live) and D1 close/reopen rebalancing. Reopen intent persists across blocked/failed attempts, while a blocked same-direction resize retains current exposure instead of accidentally liquidating it (`...mq5:251-378`, `:428-474`).
3. Declared and wired the card-fixed 2.0% daily realised-loss entry halt, 2.5% daily hard stop, and 5.0% total drawdown stop (`...mq5:42-44`, `:110`, `:388-409`). The GMT rollover window now converts broker time through `QM_BrokerToUTC` (`...mq5:242`).
4. Preserved V5 conformity: umbrella `QM_Common` include, `QM_FrameworkDeclareExecutionContract(PERIOD_D1, ...)` (`...mq5:400-403`), MAE hook before tick guards (`...mq5:426`), framework kill switch, framework trade helpers, and resolver-backed framework magic. No raw indicator handles, raw `OrderSend`, `CopyBuffer`, blocking `Sleep`, or ML mechanisms were added.
5. Sealed all eight Strategy inputs into each of the four backtest setfiles with `RISK_FIXED=1000`, `RISK_PERCENT=0`, and registered slots 0-3 (`framework/EAs/QM5_41006_man-ahl-multispeed-ewma-trend/sets/*.set:18-31`). Setfile build-hash headers were refreshed using `build_check.ps1`'s `Update-SetFileBuildHash` algorithm.
6. Updated the spec for return-volatility normalization, resilient continuous rebalancing, and the three loss-limit inputs (`framework/EAs/QM5_41006_man-ahl-multispeed-ewma-trend/SPEC.md:13-30`).
7. Added seven EA-specific contract tests covering the six review findings, every declared input use-site/setfile binding, framework risk/magic/MAE conformity, forbidden mechanisms, and symbol slots (`tools/strategy_farm/tests/test_qm5_41006_rework_contract.py:41-158`).

## Validation evidence

### Compile and binary

- Required scoped command attempted:
  - `powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_41006_man-ahl-multispeed-ewma-trend -Strict`
  - Result: fail-closed refusal `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because T1-T10 `terminal64` processes were active. No factory process was stopped, no compile work item was enqueued, and no retry bypass was used.
- Hermetic fallback followed the repository's T_Export isolated-compile pattern (`framework/scripts/tests/Invoke-FrameworkP1CompileTest.ps1`): staged source/includes, parked T_Export, no terminal-include mutation, no T_Live access.
  - Result: **PASS, 0 errors, 0 warnings**.
  - Compile log: `D:/QM/reports/compile/20260824_082551/QM5_41006_man-ahl-multispeed-ewma-trend.compile.log`.
  - Manifest: `D:/QM/reports/compile/20260824_082551/QM5_41006_man-ahl-multispeed-ewma-trend.isolated_compile.json`.
  - MQ5 SHA-256: `ef726c52d3c5b0bed3e8e798bebc125337a022aa5cde779120f6187fb98c2e01`.
  - Fresh EX5: 400,988 bytes; SHA-256 `2cd97d4e24cc2ddf71ea46ad5db5bec079bd633a1c86ee4e4d2dc84716abdb0c`; EX5 mtime is later than MQ5 mtime.

### Static gates and tests

- `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_41006_man-ahl-multispeed-ewma-trend`
  - **PASS**: 1 file, 0 failures, 0 warnings; 4 symbols observed; symbol matrix and magic registry valid.
- `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_41006_man-ahl-multispeed-ewma-trend`
  - **PASS**: 5 files, 0 findings, 336-hour news ceiling.
- `python framework/scripts/validate_spec_doc.py framework/EAs/QM5_41006_man-ahl-multispeed-ewma-trend`
  - **PASS**: 1 PASS, 0 FAIL.
- `python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_41006_man-ahl-multispeed-ewma-trend --json`
  - **PASS**: `SINGLE_SYMBOL_OK`, 0 violations.
- Focused pytest command (EA tests plus exact D2/D4/D5/D7/D8/D10/D11/D15/D17 and fixed-risk guardrail tests):
  - **18 passed in 1.38s**.
- Broad relevant suite:
  - `python -m pytest -q tools/strategy_farm/tests/test_qm5_41006_rework_contract.py tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_build_guardrails.py`
  - **55 passed, 1 unrelated failure in 382.23s**. The only failure is the pre-existing farm-wide `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`, first reporting `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`; QM5_41006 is not in that cohort and its D10 scan passes.

## Boundaries and residual risk

- No backtest was enqueued or deleted; no verdict row, gate threshold/criterion, factory state, registry allocation, resolver, T_Live file, or AutoTrading state was changed.
- Residual operational risk: the canonical scoped `build_check.ps1 -Strict` could not run while the live factory owned the shared include mirror. Compilation and all component gates were proven independently and hermetically, but a future quiescent canonical build-check run remains useful confirmation.
- No smoke/backtest was run because this is a build-only rework and the ticket explicitly forbids enqueueing backtests.

## Rollback

Revert the single ticket commit produced with this evidence (`git revert <rework-41006-commit>`). That restores the prior MQ5, EX5, SPEC, and four setfiles and removes the focused test/evidence files. No database, registry, resolver, factory, queue, or live rollback is required.

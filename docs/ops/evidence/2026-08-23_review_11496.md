# Claude review_ea — QM5_11496 carter-t-ema100-psar-macd64128-m5

**Router task:** `0de901bc-58f8-4ccd-bc84-227ab710f2b7` (REVIEW → RECYCLE)
**Source:** gemini/agy build `f1331db1-...` (`reason: codex_review_required_for_gemini_code`)
**Reviewer:** Claude (review lane) · **Date:** 2026-08-23
**EA dir:** `framework/EAs/QM5_11496_carter-t-ema100-psar-macd64128-m5/`
**Card:** `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11496_carter-t-ema100-psar-macd64128-m5.md`

This closes the task left open by the earlier batch note
`docs/ops/evidence/2026-08-23_review_ea_11496_11516_11517.md` (which recorded the same
findings but made no close-review call). Re-verified against the current source.

## Build integrity — OK
- Current `.mq5` sha256 `34a32e0b82e299b390391de88e54c7c34c6d80ac6be92e85f5daf66619c23949`
  **matches** `f1331db1_..._build_identity.json` (`build_check_passed: true`) → COMPILE_OK
  for the source under review.
- Set files generated via governed path (build_hash `34a32e0b...` in headers matches source);
  no raw MQ5 promotion. `RISK_FIXED=1000`, `RISK_PERCENT=0` in all 3 backtest sets — correct.

## Framework conformity — OK
- `#include <QM/QM_Common.mqh>`; MAE hook first in `OnTick`
  (`QM_FrameworkTrackOpenPositionMae()`, line 210); kill-switch, Friday-close, new-bar
  gating all wired.
- Magic base `114960000` = `11496*10000+0` via `QM_FrameworkMagic()` (line 63);
  `magic_numbers.csv:16096-16098` covers EURUSD/GBPUSD/AUDUSD.DWX offsets 0-2, matches the
  3 setfiles 1:1. `req.symbol_slot = qm_magic_slot_offset` wired (lines 104, 178).
- No ML libs, no grid/martingale, one-position-per-magic guard (`Strategy_HasOpenPosition`).
- All 15 `strategy_*` inputs have use sites (EMA/PSAR/MACD periods → `QM_EMA/QM_SAR/QM_MACD_Main`;
  sl_buffer/sl_cap/tp_rr in the SL/TP block; max_spread in `Strategy_SpreadAllowsEntry`;
  timeframe throughout). **No dead inputs.**
- Mechanism is card-faithful: `close1>ema100 && psar1<low1 && macd>0` (long, mirrored short),
  PSAR-dot SL + 3-pip buffer / 20-pip cap, 2R TP.

## Blocking defects (RECYCLE)

1. **News filter fully disabled, not card-authorized** — `qm_news_temporal =
   QM_NEWS_TEMPORAL_OFF` (line 22), `qm_news_compliance = QM_NEWS_COMPLIANCE_NONE`
   (line 23), `qm_news_mode_legacy = QM_NEWS_OFF` (line 26). With all three off,
   `news_allows` is unconditionally `true` (lines 216-219) — no news blackout at all.
   The card is silent on news, but the two sibling EAs from the identical source/session
   (QM5_11516, QM5_11517) default to framework-standard `PRE30_POST30`/`DXZ`. A silent card
   is not authorization to disable news, and there is no compensating control (unlike the
   OWNER-ratified event-anchored exemption precedent). Wire the standard news default.

2. **SPEC.md missing** — no `SPEC.md` in the EA dir (siblings
   `QM5_11516/SPEC.md`, `QM5_11517/SPEC.md` both ship one). `validate_spec_doc.py` fails on
   this EA. Regenerate the SPEC via the governed path.

## Verdict

**RECYCLE.** Mechanically card-faithful and framework-conformant with a matching build
hash, but two actionable build-completeness gaps (undocumented news disablement; missing
SPEC.md) must be fixed before Q02. Both are build-lane regenerations, not OWNER/infra
blocks.

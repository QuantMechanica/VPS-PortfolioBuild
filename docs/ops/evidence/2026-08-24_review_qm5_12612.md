# QM5_12612 review (review_ea e2d205ca) — Claude lane close

- Review task: `e2d205ca-3b70-4b7d-8d31-ff030c987b1e` (state REVIEW, assigned codex, source_agent gemini/agy)
- Source build task: `27fb255a-232e-4a06-9e12-f80e263f98e3`
- Build artifact: `artifacts/builds/27fb255a-232e-4a06-9e12-f80e263f98e3.json`
- EA dir: `framework/EAs/QM5_12612_tsmom-12m-vol-scaled-ndx/`
- Reviewed card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12612_tsmom-12m-vol-scaled-ndx.md`
- Source SHA-256 (current == artifact): `eceb50fa82fb5a32496fed383bcf9aae35b65a9b1ec154193165edf8d4a48291`
- **Verdict: RECYCLE**

## Blocking finding (actionable)

1. **Framework corset — monthly cadence hand-rolled with raw calendar series.**
   `QM5_12612_tsmom-12m-vol-scaled-ndx.mq5:55-56` (file-scope `g_last_entry_rebalance_key`
   / `g_last_pos_vol_scalar`), `:63-80` (`Strategy_MonthKey` via `TimeToStruct` +
   `Strategy_IsMonthlyRebalanceBar` on raw `iTime(D1,0/1)`), and `:258-260` (raw `iTime`
   reused for the rebalance key) implement the monthly rebalance trigger by hand and
   self-grant a `// perf-allowed` comment. A canonical, tester-robust helper for exactly
   this cadence exists — `QM_CalendarPeriodKey(PERIOD_MN1)` / `QM_IsNewCalendarPeriod`
   (`framework/include/QM/QM_Indicators.mqh:205-254`), demonstrated in
   `framework/include/QM/modules/QM_Mod_AaZakMom12.mqh:93`. As reviewer I decline the
   `perf-allowed` sign-off for the cadence path (a mandated helper exists); the EA must
   route the monthly trigger through `QM_CalendarPeriodKey`/`QM_IsNewCalendarPeriod`.
   The `iClose`/`Bars` perf-allowed uses for the 252-bar TSMOM lookback (`:161-165`) and
   63-bar realized-vol (`:182-192`) are genuinely bespoke (no canonical helper) and are
   acceptable — this finding is scoped to the calendar cadence only.

## Checks that PASS

- Card-faithful mechanism: 12m (252 D1) return-sign signal, 63-bar realized-vol scalar
  `target_vol/realized_vol` clamped [0.10, 2.00], monthly rebalance with sign-reversal +
  >20% vol_scalar resize, ATR(14)*3 protective stop, NDX.DWX/D1/slot-1 only. Matches card.
- No dead inputs: every `strategy_*` input is grepped to a use-site
  (`lookback_d1_bars` :157/160/165/232; `vol_window` :178/234; `target_vol` :180/183/218/236;
  `vol_resize_threshold` :287; `min_d1_bars` :160; `atr_period` :238/308; `atr_sl_mult` :238/308;
  `spread_days` :110; `spread_mult` :146/150/240).
- Framework conformity: `#include <QM/QM_Common.mqh>`; `QM_FrameworkInit/Shutdown/OnTimer/
  OnTradeTransaction` wired; MAE hook `QM_FrameworkTrackOpenPositionMae()` present (`:381`);
  kill-switch (`:383`); news filter wired via `QM_NewsAllowsTrade2` (`:390-396`); magic via
  `QM_FrameworkMagic()`; no ML, no raw unbounded series, no invented commission/swap.
- Magic registry: NDX.DWX slot 1 → `126120001` = 12612*10000+1 (`framework/registry/magic_numbers.csv:16175`); ea_id active (`ea_id_registry.csv:3579`).
- Risk mode: backtest set `RISK_FIXED=1000`, `RISK_PERCENT=0`; vol scalar multiplies base
  risk via `QM_TM_OpenPosition(...,rmode,scaled_val)` (`:323-328`). Correct.
- Setfile: `sets/..._NDX.DWX_D1_backtest.set` now carries `qm_ea_id=12612`, correct
  risk keys, and no legacy `qm_filter_news_*` keys (the 2026-08-21 setfile defects are resolved).
- Compile: `framework/build/compile/20260821_145150/...compile.log` = 0 errors, 0 warnings;
  build artifact `compile_succeeded: true`, `build_check_passed: true` for the current hash.
- Smoke deferred to Q02 (`smoke_result: deferred_p2_smoke`, custom-history isolation needs a
  worker-bound work item) — expected, not itself a build defect.

## Notes

- Codex's own review sidecar (`docs/ops/evidence/e2d205ca_qm5_12612_gemini_codex_review_2026-08-23.json`,
  verdict FAIL) independently identified the same cadence defect; the actionable pipeline
  verdict is RECYCLE (single named fix; EA otherwise well-formed and compiles clean).
- Read-only review: no EA, setfile, registry, queue, terminal, or pipeline state modified.

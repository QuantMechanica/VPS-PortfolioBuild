# QM5_12921 review (Claude lane) — 57239345

- Review task: `57239345-d21c-4c93-bff4-3000a50a773b` (state REVIEW, assigned codex)
- Source build task: `63c95ae9-d593-403a-928b-c51ac9848a1b` (gemini/agy, `build_ea`)
- Artifact: `artifacts/qm5_12921_build_result.json`
- Card of record: **none approved** — sole card is
  `D:/QM/strategy_farm/artifacts/cards_rejected/QM5_12921_qp-january-barometer-card.md`
- EA dir: `framework/EAs/QM5_12921_qp-january-barometer-card/`
- MQ5 SHA-256 `ecf9d150...113f14` (matches artifact); EX5 `72a3ea9e...923112` (matches)
- **Verdict: RECYCLE** (named actionable defects; concurs with prior gemini RECYCLE and codex FAIL)

## Blocking findings (file:line)

1. **No approved card of record.** The only card is in `cards_rejected`. The `g0_status: APPROVED`
   field inside the rejected artifact does not restore build authority; building a rejected card is
   unauthorized. Card must be re-approved before any build.
2. **January baseline computed from the wrong bar/price.**
   `QM5_12921...mq5:67` sets `g_strategy_jan_open = QM_SMA(_Symbol,PERIOD_D1,1,1,PRICE_OPEN)`.
   `QM_SMA(sym,tf,period,shift=1,price)` (QM_Indicators.mqh:16/555) → period-1 at shift 1 = the
   previously completed D1 bar. On the first observed January transition that bar is the last
   December session, so the baseline is December's **open**. The card
   (cards_rejected card, "Mechanics/Entry") requires the return from the **final December close** to
   the final January close.
3. **Final January close is missed.** `g_strategy_jan_close` is refreshed only inside the January
   branch at line 69 using shift-1 close; at the Feb transition (line 71) `jan_return` therefore uses
   the penultimate January close, not the final January close.
4. **Annual entry opportunity consumed on any failure.** `Strategy_EntrySignal` clears
   `g_strategy_entry_due = false` at line 110 *before* input/position/price/stop/order-success checks
   (lines 112-131). A validation or order failure burns the once-a-year signal; February restart
   cannot reconstruct January state.
5. **Friday-close destroys the multi-month hold.** `qm_friday_close_enabled = true` (lines 30-31)
   and `QM_FrameworkHandleFridayClose()` runs at lines 204-205. The card holds the long from the
   post-January entry through the year-end exit; Friday close would liquidate on the first Friday.
   Friday close must be disabled for this seasonal hold. (Exit at line 149 also closes in *January*,
   not the card's "final tradable close of December" — additional fidelity drift.)
6. **Unauthorized multi-index expansion.** Build generates active D1 backtest setfiles for
   `NDX.DWX, WS30.DWX, GDAXI.DWX, UK100.DWX` in addition to `SP500.DWX`
   (`artifacts/qm5_12921_build_result.json` symbols_registered / setfiles_generated). The card
   authorizes only the SP500 baseline and treats NDX/WS30 solely as a later live-promotion caveat;
   GDAXI/UK100 are not mentioned at all.
7. **Build-result sanity fails.** `smoke_result=framework_error`, no smoke report; no QM5_12921
   summary under `D:/QM/reports/smoke`. Not a Q02 pass.

## Checks that passed
- Framework wiring present: QM_Common include, `QM_FrameworkTrackOpenPositionMae` (MAE hook, line 196),
  MagicResolver via `QM_FrameworkMagic()`, news filter wired (lines 232-238), RISK_FIXED=1000 /
  RISK_PERCENT=0 in backtest setfiles, no ML, no raw unbounded series, no invented commission/swap.
- `build_check`/compile passed for the current source hash (`build_check_passed: true`,
  `compile_succeeded: true`), SHA-256 of MQ5/EX5 match the artifact.

## Method
Read-only. Read payload, artifact, both prior reviews, mq5 (all 265 lines), rejected card, and
`QM_SMA` signature. No EA, card, setfile, registry, queue, terminal, or pipeline state modified.

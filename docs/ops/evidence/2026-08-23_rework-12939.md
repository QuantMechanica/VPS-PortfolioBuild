# rework-12939 — QM5_12939 review repair

Date: 2026-08-24  
Router task: `98e0cec3-d7a2-46f7-b8ad-f09146151f78`  
EA: `QM5_12939_carney-alternate-bat-h4`  
Scope: source/spec/setfile repair and deterministic validation only; no backtest, queue, factory, threshold, verdict-row, registry-allocation, or live mutation.

## Work order read

- Router payload (`agent_router.py list-tasks --agent codex`) identified EA `12939`, source artifact `C:/QM/repo/artifacts/qm5_12939_build_result.json`, and RECYCLE finding: double new-bar gate, uninitialized request, and missing T1/T2 plus opposite-pattern exit.
- Original review note: `docs/ops/evidence/2026-08-21_codex_gemini_review_qm5_12939_98e0cec3.md`.
- Latest review work order: `docs/ops/evidence/ccf889d6_qm5_12939_gemini_codex_review_2026-08-23.json`; findings were non-card ZigZag mechanics, pre-accept cooldown mutation, false T1 completion, restart-unsafe trade geometry/state, and absent smoke evidence.
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12939_carney-alternate-bat-h4.md`, `g0_status: APPROVED`.
- Claim command moved only the named router task to `IN_PROGRESS` with verdict `rework burn-window slot 2`.

## What changed

1. Card-faithful pivot mechanism:
   - Strategy inputs now expose the card's ZigZag depth/deviation/backstep `(12,5,3)` and retain a bounded scan depth (`framework/EAs/QM5_12939_carney-alternate-bat-h4/QM5_12939_carney-alternate-bat-h4.mq5:39`).
   - The EA creates the standard MT5 `Examples\\ZigZag` through the framework indicator pool (`...mq5:98`) and reads buffer 0 once per new bar with a bounded `CopyBuffer`/`CopyRates` pair, captured result counts, `ArraySize` checks, and newest-first D-C-B-A-X ordering (`...mq5:119`).
   - The previous two-wing local-fractal approximation is removed.
2. Accepted-action state transitions:
   - `Strategy_EntrySignal` only builds a zero-initialized request; it no longer consumes cooldown (`...mq5:403`).
   - Cooldown resets only inside the successful `QM_TM_OpenPosition` branch (`...mq5:651`).
3. T1/T2, restart state, and exit fidelity:
   - Accepted entry geometry (`t1`, `t2`, `t1hit`) is persisted by framework magic plus position ticket and restored after reattach/restart; missing state blocks partial/trailing guesses but leaves broker SL/T2 and the 30-bar time stop active (`...mq5:190`, `...mq5:442`).
   - T1 becomes complete only when the normalized half-volume is valid and `QM_TM_PartialClose` returns true; only then is post-T1 ATR trailing armed (`...mq5:477`, `...mq5:509`).
   - T2 remains the broker TP for the remaining half. Opposite-direction Alternate-Bat exit is evaluated before reversal entry (`...mq5:547`, `...mq5:621`).
4. Framework/set conformity:
   - `QM_Common`, `QM_FrameworkMagic`, explicit MAE hook, fixed-risk backtest inputs, two-axis news inputs, Friday close, and no-ML contract remain intact (`...mq5:5`, `...mq5:574`, `...mq5:601`).
   - All 20 declared `strategy_*` inputs have executable use-sites; the focused pytest enforces this mechanically.
   - All 13 existing backtest setfiles were regenerated for their already-active magic slots. They now include every declared strategy input, `RISK_FIXED=1000`, `RISK_PERCENT=0`, and source build hash `02d1e70da0ae22e915323585b86c459ffb35e8e23e9a12f67cac083c0f1a03a7` (representative: `framework/EAs/QM5_12939_carney-alternate-bat-h4/sets/QM5_12939_carney-alternate-bat-h4_EURUSD.DWX_H4_backtest.set:13`).
   - Registry inspection found 13 active rows/slots/magics and a matching EA registry row. No registry file was changed.
5. Documentation/tests:
   - `SPEC.md` now describes actual ZigZag, partial-target, trail, opposite-exit, restart, and live-risk contracts (`framework/EAs/QM5_12939_carney-alternate-bat-h4/SPEC.md:12`).
   - Added `tools/strategy_farm/tests/test_qm5_12939_review_rework_static.py`, covering the five review defect classes, framework corset, every input use-site, active magic rows, and all setfile bindings.

## Validation evidence

| Command | Result |
|---|---|
| `python framework/scripts/skill_build_ea_guard.py --ea-id 12939 --ea-label QM5_12939_carney-alternate-bat-h4` | PASS: EA row, magic rows, and directory all present |
| `python framework/scripts/validate_spec_doc.py framework/EAs/QM5_12939_carney-alternate-bat-h4` | PASS: 1/1 |
| `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_12939_carney-alternate-bat-h4` | PASS: 14 files, 0 findings, news stale ceiling 336h |
| `python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_12939_carney-alternate-bat-h4 --fail-on-leak --json` | `SINGLE_SYMBOL_OK`, 0 violations |
| `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_12939_carney-alternate-bat-h4` | PASS: 1 source scanned, 0 failures, 0 warnings |
| `python -m pytest tools/strategy_farm/tests/test_qm5_12939_review_rework_static.py tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_validate_symbol_scope.py -q` | PASS: 28 passed in 1.53s |
| `python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py -q -k "not qm5_411xx_sources_have_no_unbounded_numeric_buffers"` | PASS: 29 passed, 1 deselected in 122.39s |
| Full `test_build_gate_hardening.py` | 29 passed, 1 unrelated failure in the pre-existing `QM5_411xx` cohort fixture (7 findings outside EA 12939); no ticket file implicated |
| `git diff --check` | PASS; only configured LF-to-CRLF checkout warnings |

## Compile/build-check interlock evidence

The required commands were attempted with exact EA scope:

```text
powershell ... framework/scripts/compile_one.ps1 \
  -EAPath framework/EAs/QM5_12939_carney-alternate-bat-h4/QM5_12939_carney-alternate-bat-h4.mq5 \
  -EALabel QM5_12939_carney-alternate-bat-h4 -Strict

powershell ... framework/scripts/build_check.ps1 \
  -EALabel QM5_12939_carney-alternate-bat-h4 -SkipCompile
```

Both stopped before compilation/static build-check execution with the governed failure `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`: `terminal64` processes are alive. The compile attempt receipt is:

- Log: `C:/QM/worktrees/rework-slot-2/framework/build/compile/20260824_075505/QM5_12939_carney-alternate-bat-h4.compile.log`
- Summary: `D:/QM/reports/compile/20260824_075505/summary.csv`

No retry/bypass, terminal stop/start, factory toggle, compile/backtest enqueue, or T_Live access was performed. Consequently the tracked `.ex5` remains the pre-rework binary and is deliberately not claimed or committed as fresh compile evidence. This is the only residual review risk; source-level and scoped mechanical gates are clean.

## Rollback

Revert the ticket commit with `git revert <commit>` to restore the prior MQ5, SPEC, setfile parameters/hashes, and remove the focused static test/evidence note. No database verdict row, backtest queue, magic allocation, factory state, or live file needs rollback. The router task can be returned from `REVIEW` to `RECYCLE` independently if review requires a governed fresh compile receipt.

# Review — router task 6a131ec6 (lane-independent build REVIEW hardening)

Reviewer: Claude (review lane) · Date: 2026-08-24
Task: 6a131ec6-e9f1-44f2-b161-d83edff08e0f (ops_issue, assigned codex, state REVIEW)
Verdict: **APPROVED**

## Scope
Codex was asked to move the strict-hardening review-dispatch gate from a
Gemini-lane condition (`agent_router.py:1871`) to the defect class — every
`build_ea` row must be re-hardened against the hash-bound canonical MQ5 before
transitioning to REVIEW, lane-independent — without weakening any gate criterion.

## Verification (read-only)
- Delivery commit: `07951e300` (fix: enforce build review hardening across lanes).
- Core change confirmed at HEAD, `agent_router.py:1896`:
  `if row["task_type"] == "build_ea" and state == "REVIEW":` — the
  `assigned_agent == "gemini"` clause is removed. The gate now fires for every
  producer lane.
- Legitimately lane-specific behavior preserved: the Gemini→Codex independent
  `review_ea` minting still guards on `assigned_agent == "gemini"`
  (`agent_router.py:2003`); the close-review build-guardrails backstop is already
  lane-independent (`:2167`). Correct separation of the two concerns.
- Ordering fix (acceptance #2): canonical hardening now runs on the hash-bound
  MQ5 *before* EX5/packet identity, so a Codex hold packet with a real source
  defect but incomplete final binary is refused for the substance
  (`D3_D10_BUILD_GATE_HARDENING_FAIL`) instead of hiding behind a formal
  `D6_BUILD_IDENTITY_MISSING`. Verified in the diff (`_hash_bound_build_path`
  helper + reordered checks).
- Tests: one negative per lane plus a Codex positive
  (`test_codex_claimed_pass_reaches_and_fails_canonical_hardening`,
  `test_codex_clean_build_identity_can_transition_to_review`,
  Gemini negative retained). Ran focused suite read-only:
  `pytest -k "hardening or review_dispatch or codex_claimed or codex_clean or gemini"`
  → 5 passed, 28 deselected.
- Real clean-source control cited (QM5_1612) exists on disk; its documented hash
  was bound to commit 9fb5ad806 at control-run time (working tree has since been
  rebuilt — expected, not a defect).

## Acceptance checklist
1. Gate re-runs canonical strict hardening for every build_ea→REVIEW,
   lane-independent — YES (`:1896`).
2. Per-lane test: producer-claimed PASS at canonical FAIL is refused — YES
   (Gemini + Codex negative fixtures).
3. Original gemini restriction reason answered honestly in the evidence doc
   (discovery point, not a substantive exemption; review-minting stays gemini) — YES.
4. Positive test proves a correctly built EA is not falsely blocked — YES
   (Codex positive fixture + QM5_1612 real-source control).
5. Evidence doc under docs/ops/evidence/ — YES
   (2026-08-22_lane_independent_build_review_hardening.md).

## Hard constraints
- No gate criterion/threshold changed — only scope + check ordering. Confirmed.
- No recompile, no T_Live, no verdict/trade-stream touched. Confirmed.
- Hardening not weakened to make the change green. Confirmed.

## Verdict
APPROVED — clean, disciplined delivery. The latent lane-scope hole in the
emergency prevention is closed; the change strengthens the Codex lane
(reaches the substantive source check first) without altering any gate criterion.

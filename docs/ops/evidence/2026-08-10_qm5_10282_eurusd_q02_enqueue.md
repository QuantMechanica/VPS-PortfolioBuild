# QM5_10282 EURUSD Low-Frequency FX Q02 Enqueue

Date: 2026-08-10 (Europe/Berlin)

Branch: `agents/board-advisor`

Status: one existing approved FX sleeve advanced to Q02; exactly one EURUSD
work item pending

## Outcome

The frozen 66-pair FX cointegration frontier is already fully mechanized, so
creating another Card or EA from that scan would be duplicate work. The
requested anchor repair is also inapplicable:

- `QM5_12532` has a logical-basket Q02 PASS, a Q04 PASS, and a terminal Q05
  FAIL.
- `QM5_12533` has a logical-basket Q02 PASS and a terminal Q04 FAIL.
- Neither anchor has a pending or active Q02 `ONINIT` / `NO_HISTORY` blocker.

The mission fallback therefore advanced the existing low-frequency FX card
`QM5_10282_jstm-bbw` on `EURUSD.DWX` H1. Work item
`ede93177-885c-4a04-a7c7-6d6f96ff51a1` was inserted through the canonical
selector at `2026-08-10T14:38:15Z`. Immediate canonical readback found exactly
one row: Q02 `pending`, attempt 0, unclaimed, and without a verdict.

This is a screening enqueue only. It is not a Q02 result, an economic edge
claim, a certified sleeve, or a portfolio-admission decision.

## Non-Duplicate And Source Boundary

The exhaustive frontier decision is recorded in
`docs/research/FX_COINTEGRATION_FRONTIER_Q02_CPU_CEILING_STOP_2026-08-07.md`
at commit `eda8802a13b98b83b60bdb4125af39a8758da250`: all 66 ranked
relationships already have dedicated or explicit umbrella coverage. No new
cointegration Card, EA, registry row, magic row, setfile, or basket manifest
was created in this fallback.

`QM5_10282` is an existing OWNER-approved card, not a new source intake. Its
public source is the `je-suis-tm/quant-trading` Bollinger Bands Pattern
Recognition implementation. The approved card estimates only 3-8 trades per
year per symbol and specifies a deterministic 75-bar Bollinger bottom-W
pattern, upper-band breakout, fixed `2 * ATR(14)` catastrophic stop, and
volatility-contraction exit. It contains no ML, learned output, grid,
martingale, adaptive fit, or external runtime feed.

The active deterministic bindings are:

- EA / slug: `QM5_10282` / `jstm-bbw`.
- Build commit: `908003072710d728bd80f2fc32a8f7f2849b425e`.
- Symbol / slot / magic: `EURUSD.DWX` / 1 / `102820001`.
- Timeframe: H1.
- Backtest risk: `RISK_FIXED=1000`, `RISK_PERCENT=0`,
  `PORTFOLIO_WEIGHT=1`.

The canonical selector reported zero prior work items before the targeted dry
run. That dry run selected exactly the EURUSD preset. `GBPUSD.DWX` and
`WS30.DWX` remained staged-deferred, while `NDX.DWX` and `XAUUSD.DWX` were
excluded by the explicit target-symbol filter.

## Paced-Fleet Enqueue

The mandatory immediate pre-apply `farmctl mt5-slots` sample at
`2026-08-10T14:37:56Z` found six executing T1-T10 factory terminals:

```text
T1, T5, T6, T7, T8, T9
```

Six is below the binding ceiling of seven. `T_Live` and the unrelated FTMO
terminal were observed separately and excluded; neither was controlled.

The exact guarded apply was:

```powershell
python tools/strategy_farm/sweep_enqueue_built_eas.py --ea QM5_10282 --symbols EURUSD.DWX --apply
```

The selector recorded 1,101 pending rows before insertion against the 7,000
queue ceiling, selected one never-tested priority row, selected no stranded
retry, and promoted no deferred row. Normal workers own any later claim and
execution; no dispatch tick or tester was launched here.

## Artifact Identity

| Artifact | SHA-256 |
|---|---|
| Approved card | `B2FDCBA6C3389DA3F3E2F1182742CE02D54DE0F6F1E360BCF639AB33529A7D3E` |
| MQ5 | `CE9578118CE06542C38174CCDF5124CEBC66DED726F4E5BBACD625111264A417` |
| EX5 | `E91019F539EB319828B18499BBB1DD8AF71D9E3CCDFDC37709DA76DAF653D4CD` |
| EURUSD fixed-risk setfile | `9188C4B4C42554570F057EE9F3BB2A6B5D785DF0C9D96D19B977C92A612E9E15` |
| Apply evidence at readback | `95120B1C5DAD8B275A009EE87D4F2E7352A0637FFE62DA4426F0D60787930446` |

The durable machine-readable snapshot is
`artifacts/qm5_10282_eurusd_q02_enqueue_20260810T143815Z.json`.

## Safety

- No manual backtest, smoke test, dispatch tick, or downstream phase was run.
- No terminal was started, stopped, reserved, reaped, or changed.
- No `T_Live` file, manifest, process, or AutoTrading state was changed.
- No live/demo/shadow setfile or deploy artifact was created.
- No portfolio-admission, portfolio KPI, or Q08-contribution path was touched.
- Pre-existing unrelated dirty-worktree files were preserved and excluded.

# QM5_9465 connors-rsi25-d1 — Claude review-lane close

- Router task: `088f128e-39c7-4193-a2a6-6b088d0eab0b` (`review_ea`, assigned codex, state REVIEW)
- Source task: `1b490cf7-9172-410b-8e5b-07b24c0cb517` (`build_ea`, gemini/agy) — verdict `BUILD_READY_REVIEW`
- Reviewed artifact: `framework/EAs/QM5_9465_connors-rsi25-d1/build_identity.json`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9465_connors-rsi25-d1.md`
- Reviewer: Claude (review lane); read-only DB, no factory/T_Live/recompile actions.

## Verdict: RECYCLE

Card-faithful in its core RSI(4)/SMA(200) mean-reversion mechanism and framework-clean,
but two named, actionable defects block promotion to Q02. A prior Codex note
(`docs/ops/evidence/2026-08-23_qm5_9465_gemini_code_review.md`, REQUEST_CHANGES) reached
the same conclusion; this review independently confirms it and calibrates severity.

## What is clean (verified)

- Compile: `framework/build/compile/20260822_111803/QM5_9465_connors-rsi25-d1.compile.log`
  = `0 errors, 0 warnings`. Current `.mq5` sha256 `286d70a1…005713` matches
  `build_identity.json` — compile evidence is for the current source hash.
- No dead inputs: all 9 `strategy_*` inputs grepped to real use-sites in the .mq5
  (rsi_period/atr_period/warmup used 3–4×; the rest 2× = decl + one use). RSI25 entry,
  RSI55 exit, 12-bar time stop, 3.0×ATR(14) SL, 0.25×ATR spread filter all wired.
- Framework conformity: `#include <QM/QM_Common.mqh>`; MAE hook
  `QM_FrameworkTrackOpenPositionMae()` in OnTick; news gate `QM_NewsAllowsTrade2` wired
  (min_impact "high"); Friday close via `QM_FrameworkHandleFridayClose`; magic via
  `QM_FrameworkInit`/`QM_FrameworkMagic` (`ea_id*10000+slot` = `94650000+slot`, all 13
  registry rows active and matching setfiles); `QM_StopATR(sym,side,entry,period,mult)`
  used correctly (reads ATR internally); bounded QM_RSI/QM_SMA/QM_ATR wrappers, no raw
  series, no ML, no invented commission/swap.
- Setfiles via governed generator (`gen_setfile.ps1` header present); `risk_mode FIXED`,
  `RISK_FIXED=1000`, `RISK_PERCENT=0`, params = card defaults.

## Defects (RECYCLE drivers)

1. **Card's bounded second-unit is absent and the deviation is unrecorded.** The approved
   card (Entry §: RSI(4)<20 add-on as a bounded second slot with a distinct sub-magic,
   max two slots, risk split evenly; Stop §: SL recomputed from average entry only while
   adding the second slot) is not implemented. The .mq5 blocks any second entry
   (`QM_TM_OpenPositionCount(magic) > 0` → return false) and constructs only the RSI25
   request; there is no RSI20 threshold, second sub-magic, split-risk, or avg-entry stop
   path, and the 13 registry rows are one slot per symbol (no same-symbol second
   identity). The card permits single-slot fallback ONLY "if slot allocation is rejected
   by G0/P1 … and record the deviation" — no such rejection or deviation is recorded in
   the card or Gemini SPEC. Fix: implement the two-slot contract, or record the G0/P1
   rejection + an OWNER-approved single-slot deviation in card and SPEC before rebuild.

2. **Symbol universe expanded beyond the approved card (governance / ROT).** The card
   scopes SP500.DWX (backtest SPY-equivalent) plus live analogues NDX.DWX / WS30.DWX,
   noting the concept ports to "major index CFDs." The Gemini SPEC §3 and the 13 delivered
   setfiles expand this to include 7 FX majors and XAUUSD — a materially different claim
   for an equity-index mean-reversion edge, with no card revision. Candidate-pool/universe
   definition is an OWNER (ROT) zone. Fix: trim setfiles to the card-approved universe, or
   obtain an OWNER card revision authorizing the wider port.

## Advisory (not independently Q02-blocking)

3. `QM_FrameworkDeclareExecutionContract(PERIOD_D1, …)` is not called in OnInit, so
   `QM_IsNewBar()` (no-arg) follows the attached chart TF while all indicators are D1-pinned;
   a wrong-TF attachment would not fail closed. This is real hardening value, but adoption
   is only 157/3800 fleet EAs, and the factory pins the chart to D1 via the setfile — so it
   is an improvement to fold into the rebuild, not an independent blocker.

## Recommended next step

Recycle to the build lane (Codex or the gemini build lane) to resolve defects 1 and 2
(and fold in advisory 3), then re-review before Q02 dispatch.

# QM5_20175 build preflight refusal

- Router task: `2d61b824-c3c2-4d46-a087-f89ab39ce6f7`
- Checked at: `2026-08-24T04:34:36Z`
- Canonical checkout: `C:/QM/repo`
- Branch: `agents/board-advisor`
- Starting HEAD: `59149dfad4ce230a9528ae91f7475530b0b0e966`
- Verdict: `BUILD_NOT_STARTED — PREBUILD_BLOCK_MISSING_MAGIC_ROWS`

## Deterministic preflight

| Gate | Evidence | Result |
|---|---|---|
| Approved card | `D:/QM/strategy_farm/artifacts/cards_approved/QM5_20175_alpha-inst-magnet-r1-recovery.md` has `g0_status: APPROVED` | PASS |
| Card identity | `ea_id: QM5_20175`; `slug: alpha-inst-magnet-r1-recovery` | PASS |
| EA registry | Exactly one row for `20175`; slug matches; status is `active` | PASS |
| EA directory | `framework/EAs/QM5_20175_alpha-inst-magnet-r1-recovery/` exists | PASS |
| Magic registry | Zero rows and zero active rows for `ea_id=20175` | **FAIL** |

The approved card declares six symbol slots: `EURUSD.DWX`, `GBPUSD.DWX`,
`USDJPY.DWX`, `XAUUSD.DWX`, `NDX.DWX`, and `WS30.DWX`. Every declared
`(ea_id, symbol_slot)` pair must exist in `framework/registry/magic_numbers.csv`
before the V5 build may start.

## Focused verification

The exact-card governed allocator was run in read-only mode:

```powershell
python C:/QM/repo/tools/strategy_farm/governed_magic_allocator.py `
  --repo C:/QM/repo `
  --cards-dir D:/QM/strategy_farm/artifacts/cards_approved `
  --dry-run `
  --card D:/QM/strategy_farm/artifacts/cards_approved/QM5_20175_alpha-inst-magnet-r1-recovery.md
```

It returned `action=allocate`, `reason=eligible`, `identity_registered=true`,
`create_directory=false`, `planned_eas=1`, and `planned_rows=6`. This proves
that the missing registry rows are bounded and allocator-ready; dry-run made no
mutation.

The directory currently contains only a tracked 4,085-byte `.mq5` scaffold
(introduced by pump auto-commit `5c5bb6023`). It has no `.ex5`, setfile, or
card-of-record. The scaffold also still declares `RISK_PERCENT = 0.5`; it was
not treated as a valid backtest deliverable and was not modified.

## Boundary and next gate

The `qm-build-ea-from-card` contract requires active magic rows before build
implementation and explicitly excludes allocating registry rows. Linked repair
task `8d1d903f-39cc-461f-ab90-7b932ce62fee` is still in `REVIEW` and requires
all registry mutations to use the governed allocator in bounded serial batches.
No hand edit, real allocator run, source build, strict compile, pipeline phase,
terminal action, or live action was performed.

Reroute this build only after all six active rows survive resolver regeneration
and the registry duplicate scan passes.

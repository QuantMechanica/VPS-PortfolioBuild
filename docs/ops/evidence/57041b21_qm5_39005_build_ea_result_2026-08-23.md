# QM5_39005 (forexfactory-genesis-matrix-scalper) Build & Verification Evidence

- Task ID: `57041b21-12da-464d-b275-c91cb3fa8673`
- EA ID: `QM5_39005`
- Date: 2026-08-23
- Branch: `agents/board-advisor`
- Target symbols: EURUSD.DWX (slot 0), GBPUSD.DWX (slot 1)
- Timeframe: M5
- Magic Numbers: 390050000 (slot 0), 390050001 (slot 1)

## Summary of Implementation & Codex Review Fixes

Addressed all findings from Codex review (`a2055dc7-5e44-4ba0-a130-235c9e6a2c75`):
1. **Timeframe Contract**: Corrected `QM_FrameworkDeclareExecutionContract` to `PERIOD_M5` in `OnInit()`.
2. **Pip Scaling**: Fixed `QM_StopRulesPipsToPriceDistance` call to pass unscaled `strategy_sl_buffer_pips` (2.0 pips) rather than multiplying by 10.
3. **Break-Even Mechanics**: Corrected `QM_TM_MoveToBreakEven` call to pass unscaled `strategy_be_trigger_pips` (15 pips) and buffer of 1 pip (Entry + 1 pip).
4. **Indicator Readiness**: Enforced fail-closed validation on all 4 indicators (`CalculateTVI`, `QM_CCI`, `CalculateT3CCI`, `CalculateGHL`, `QM_EMA`, `QM_ATR`, `iClose`) in `AdvanceState_OnNewBar()` so unready data is never converted into arbitrary bullish/bearish scores.
5. **Stateful Gann High-Low Activator**: Implemented stateful direction persistence across the neutral band `[SMA(Low), SMA(High)]` with proper warmup lookback.
6. **Structural Swing Stop**: Removed arbitrary ATR clamping and fallback, strictly placing the stop loss beyond the 10-bar swing structure +/- 2.0 pips buffer per strategy card.
7. **Loss Limit Enforcement**: Implemented `Strategy_DailyRealizedLossHalt()` (2.0% daily realized loss halt) and initialized `QM_KillSwitchInit` with daily hard stop 2.5%, total drawdown stop 5.0%, and per-trade risk cap 1.0%.

## Verification Checklist

- **Magic Numbers Registry**: Verified 2 target DWX symbols in `framework/registry/magic_numbers.csv` (390050000, 390050001).
- **SPEC Document**: Validated `framework/EAs/QM5_39005_forexfactory-genesis-matrix-scalper/SPEC.md` with `validate_spec_doc.py` -> PASS.
- **Compilation**: Compiled via `python tools/strategy_farm/compile_ea.py --ea-id 39005 --force --json` -> COMPILED (0 errors, 0 warnings; .ex5 size 408,894 bytes).
- **Setfile Generation**: Generated `EURUSD.DWX` and `GBPUSD.DWX` M5 backtest setfiles in `sets/` with `RISK_FIXED=1000` and `RISK_PERCENT=0`.
- **Build Guardrails**: Verified via `tools/strategy_farm/validate_build_guardrails.py` -> PASS (max_news_stale_hours: 336, verdict: PASS).

## State Disposition

Artifact ready for Codex review.

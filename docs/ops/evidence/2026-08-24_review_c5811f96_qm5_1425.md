# Claude review-lane close: QM5_1425 (task c5811f96)

- Router task: `c5811f96-bf23-490f-a8e5-2887606d16a7` (review_ea, assigned codex, state REVIEW)
- EA: `QM5_1425_classical-triple-bottom-reversal-h4`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1425_classical-triple-bottom-reversal-h4.md`
- Reviewed tree: `agents/board-advisor` HEAD (includes rework commit `2441e3a82`)
- Current MQ5 blob hash (git, LF): `08c2b65d1212ff9699a7a3df0347f1a8049ab7dc090d05abc1c26bcb97b9a6fd`
- Setfile-bound source hash (rework, LF-normalized): `45c2da17808f38def24b158d4fe7c517a77cafab02491ca759cc05b8bfb6d408`
- On-disk EX5 hash: `7cae1f96a30910f832df2d726e89205d714f26edd1b1eaaf0cf9b8a3e60fc51d` (original `154312a89` binary)
- **Verdict: RECYCLE** — source review PASSES; deliverable is not Q02-ready because no compiled binary exists for the repaired source.

## Timeline reconciliation (why the codex verdict is stale)

The codex recycled-review artifact (`c5811f96_qm5_1425_recycled_gemini_codex_review_2026-08-24.json`,
verdict FAIL, `reviewed_at 2026-08-24T07:26:14Z`) reviewed the pre-rework tree
and correctly reported the old `263e03e4` MQ5 with 8 defects. The rework commit
`2441e3a82` ("rework(1425): restore card-faithful triple-bottom execution")
landed at `2026-08-24 09:32:23 +0200` = `07:32:23Z` — ~6 minutes AFTER the codex
timestamp. The current committed source is the reworked version; the codex FAIL
no longer describes the tree. This review re-evaluates the current source directly.

## Source review — PASS

All 8 findings from `c5811f96_qm5_1425_gemini_build_codex_review_2026-08-22.md`
are resolved in the current source (`QM5_1425_...h4.mq5`):

1. Pivot ordering: `Strategy_FindFractals` appends ascending-shift (newest-to-oldest);
   entry loop treats consecutive elements as T3/T2/T1 with an explicit
   `s_t3 < s_t2 < s_t1` guard (`:436`, `:529-541`). Zero-trade path removed.
2. Trough significance: `Strategy_IsSignificantTrough` enforces the 20-bar context
   / `0.5*ATR` rule and is wired into the low-pivot filter (`:394-416`, `:467`);
   `strategy_trough_depth_atr` now read (`:415`).
3. Gate 3 exactly-one intervening peak: `p12_count/p23_count != 1` rejects (`:559-582`).
4. BUY-STOP restored: `req.type=QM_BUY_STOP`, `price=neckline+0.4*ATR`,
   `expiration_seconds=12*tf` (`:646-652`).
5. D1 SMA bias direction: `QM_SMA` shift 1 vs shift 2, accepts `sma_newer >= sma_older`
   (flat/rising) (`:361-368`).
6. News window: `Strategy_EntryNewsAllows` fail-closes unless the computed window is
   exactly 480 minutes and passes `480,480` to the calendar (`:775-808`); OnInit
   declares 480/480 to the framework (`:816-821`).
7. Restart/order state: setup persisted by magic to terminal globals and restored
   fail-closed (`:135-312`); pattern committed only after `QM_TM_OpenPosition`
   success (`:888-894`).
8. Execution contract declared: `QM_FrameworkDeclareExecutionContract(PERIOD_H4,...)`
   (`:825-828`).

Dead-input check: all 30 `strategy_*` inputs are read at a use-site beyond
declaration (verified by grep) — no dead inputs.

Framework conformity:
- QM_Common chain (`:5`), MAE hook is first `OnTick` action (`:851`),
  news gate gates ENTRIES only; management + exits run before it (`:857-884`).
- Magic via `QM_FrameworkMagic()` (MagicResolver); one active EA registry row
  (`ea_id_registry.csv:301`), 14 active magic rows.
- No `CopyBuffer`, no raw `OrderSend`, no `iCustom`, no ML, no blocking `Sleep`,
  no invented commission/swap. Single bounded `CopyRates` carries a `perf-allowed`
  marker with an `ArraySize` guard (`:512-514`).
- 14 backtest setfiles: `RISK_FIXED=1000`, `RISK_PERCENT=0`, `build_hash 45c2da17...`.

Verification commands (read-only):
- `python -m pytest -q tools/strategy_farm/tests/test_qm5_1425_review_rework_static.py` -> 6 passed.
- `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_1425_...h4`
  -> `failures=0`, 1 benign warning `EA_CARD_SMA_DIRECTION_UNDECIDABLE` (checker only
  recognizes raw two-element `CopyBuffer`; this EA uses the `QM_SMA` helper, direction
  verified manually and by the focused rework test).

## Blocker — stale binary (the RECYCLE reason)

No EX5 exists for the repaired source. The on-disk EX5 (`7cae1f96`) is the
original `154312a89` binary carrying the reviewed zero-trade defect; the setfiles
bind `build_hash 45c2da17` but no matching compiled binary was produced. The
governed compile was refused because terminal workers were active
(`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` / `INCLUDE_MIRROR_REFUSED`, receipt
`D:/QM/reports/compile/20260824_072735/summary.csv`). Dispatching to Q02 now
would test the defective original binary — the documented "stale EX5 voids healthy
backtests" failure class.

Actionable item: enqueue a governed COMPILE_EA for QM5_1425 during an OFF/compile
window to produce an EX5 bound to source hash `45c2da17...`, verify EX5 identity,
then re-review for Q02 handoff. Do NOT promote the stale `7cae1f96` EX5. No code
change is required.

## Safety boundary

DB read-only (mode=ro). No backtest enqueued, no factory toggle, no recompile,
no T_Live/AutoTrading action, no verdict/registry/setfile/source mutation. Only
the sanctioned router close-review call was made.

# review_ea — QM5_11300_macd-psar-atr-trender-h4 — 2026-08-23

Router task `72cb14ec-664d-41c9-a9c0-a0047d593eb3` (`review_ea`, assigned claude,
`reason=codex_review_required_for_gemini_code`, source_agent gemini/agy, source build task
`03dbc26e-174f-4879-bbbc-ac69b07ec692`). Static/evidence-based review only — factory has live
terminal64 workers (T1–T10), so no ad-hoc `build_check.ps1` recompile; relied on the build task's
captured build identity, filesystem/registry state, and manual card-fidelity reading.

This supersedes the un-closed multi-EA note `2026-08-23_review_ea_11300_9972_9979.md`, whose
11300 findings 2 and 3 are re-evaluated below against current filesystem state (source-of-truth #2).

## Reviewed identity

- MQ5: `framework/EAs/QM5_11300_macd-psar-atr-trender-h4/QM5_11300_macd-psar-atr-trender-h4.mq5`
- MQ5 SHA-256 (current): `83ef727b80d9010706c7faddc8d1eea90a933a3bf37226a2bccd140af7504b53`
  — **matches** the recorded build-identity `mq5_sha256`
  (`docs/ops/evidence/03dbc26e_QM5_11300_macd-psar-atr-trender-h4_build_identity.json`), so the
  compiled `.ex5` (`272e3f06…`) corresponds to the current source. `build_check_passed: true`.
- Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11300_macd-psar-atr-trender-h4.md`
  (g0_status APPROVED, R2 PASS mechanical, R3 PASS, R4 PASS ML-free; source = MACD Trender PDF,
  Dropbox forex archive).

## Verdict: RECYCLE

Code mechanics are card-faithful and framework-clean; the blockers are build-completeness /
exit-fidelity items that Codex can fix without OWNER or infra. No thresholds/verdicts touched.

## Clean checks (evidence)

- **No dead inputs.** All ten `strategy_*` inputs (mq5:36–45) have live use-sites beyond
  declaration: `strategy_timeframe` (mq5:111–120,183,205), MACD periods (mq5:111–114),
  `strategy_psar_step`/`_maximum` (mq5:119,183), `strategy_atr_period` (mq5:120),
  `strategy_tp_atr_mult` (mq5:152,160), `strategy_sl_max_atr_mult` (mq5:149,157),
  `strategy_max_spread_pips` (mq5:79,87).
- **Framework wiring.** `#include <QM/QM_Common.mqh>` (mq5:5); `QM_FrameworkInit`/`Shutdown`
  (mq5:231,239); magic via `QM_FrameworkMagic()` (mq5:62,182); MAE hook
  `QM_FrameworkTrackOpenPositionMae()` (mq5:243); kill-switch (mq5:244); news gate wired
  (mq5:248–253); Friday close (mq5:255); all helper symbols resolve in `framework/include/QM/`
  (`QM_MACD_Main/Signal`, `QM_SAR`, `QM_ATR`, `QM_StopRulesNormalizePrice`, `QM_TM_MoveSL`).
  No ML libs, no hardcoded commission/swap.
- **Magic / risk / registry.** `magic_numbers.csv` ea_id 11300 → slots 0–3 =
  EURUSD/GBPUSD/USDJPY/AUDUSD.DWX, magic `113000000+slot` — matches card `target_symbols` and the
  4 shipped set files. `ea_id_registry.csv` row present/active. All 4 backtest sets:
  environment=backtest, risk_mode=FIXED, `RISK_FIXED=1000`, `RISK_PERCENT=0`, `build_hash` =
  current source hash. Sets carry the governed QM5 header (gen_setfile path), not raw promotion.
- **Card fidelity of the entry.** MACD histogram (main−signal) zero-cross gated by PSAR-vs-bar
  position: long = `hist_2<=0 && hist_1>0 && psar_1<low_1` (mq5:132), short mirrored (mq5:133) —
  matches card Implementation Notes. Bar-close evaluation on closed bars (shift 1/2) + new-bar
  gate (mq5:272). Spread cap (mq5:77–88) and ATR-clamped catastrophic SL (mq5:149–159) present.
  PSAR trailing stop in `Strategy_ManageOpenPosition` (mq5:203–213) — matches card exit intent.

## Defects (actionable)

1. **SPEC.md missing (RECYCLE).** Directory contains only `.mq5`, `.ex5`, `sets/` — no design
   doc. Every sibling EA in the cohort ships a SPEC.md. Now that an approved card exists, Codex
   should generate a SPEC.md that references it and records the deviations below.
2. **Exit deviates from card partial-close schedule (RECYCLE).** Card §Exit specifies three
   partial closes (TP1=1×ATR / TP2=2×ATR / TP3=3×ATR-or-PSAR-flip, 1/3 each). The implementation
   uses a single fixed TP at `strategy_tp_atr_mult=2.0 × ATR` (mq5:43,152,160) with no scaling,
   and `Strategy_ExitSignal()` returns false (mq5:218–221) — PSAR-reversal close is delivered
   only implicitly via the trailing SL, not an explicit flip-close. Either implement the partial
   schedule, or document the single-TP / trailing-stop reduction in SPEC as an intentional
   one-position-per-magic simplification.

## Notes (non-blocking)

- News filter compiled OFF (mq5:22–26, no set override). Card §Additional filters explicitly
  states "News filter: off in P2" — so this is **card-sanctioned for backtest**, not an
  undocumented exemption (revises finding 3 of the prior note). Codex should confirm the eventual
  `live` set carries the standard DXZ/PRE30-POST30 gate before any live promotion.
- Set-file header shows `card_defaults_status=none_found`: the card exists but has no
  machine-readable strategy-param defaults block (`card_body_incomplete: true` in card
  front-matter). Non-blocking for Q02 (inputs default in the `.mq5`), but the SPEC/card should
  gain a defaults block so future regens bind param values.
- `Strategy_NoTradeFilter()` returns false unconditionally; harmless because all indicator reads
  pass `strategy_timeframe` explicitly rather than `_Period`.

## Authority note

Gemini/agy-authored build; per hard rule this Claude static review does not itself move the task
past REVIEW into PIPELINE. RECYCLE returns it for the named fixes above.

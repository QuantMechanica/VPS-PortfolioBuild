# rework-1640 — QM5_1640 card-faithful review repair

## Status

`SOURCE_READY_REVIEW; GOVERNED_COMPILE_HELD`

- Router task: `65e3d317-40d4-4bba-a801-48bcc2ba89d9`
- EA: `QM5_1640_aa-indmom-12-0`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1640_aa-indmom-12-0.md`
- Review work order: `docs/ops/evidence/2026-08-23_qm5_1640_gemini_code_review.md`
- Current MQ5 SHA-256: `7c44a11dc1e6d8a7f9197b0d336aa3abe62eebd2d6bb3b87c3ac7a9135074c22`

The six blocking review findings are repaired in source, presets, and durable
universe documentation. A fresh binary is intentionally absent because the
governed include-mirror interlock refused ad-hoc compilation while factory and
live terminal processes were active. No compile was enqueued, no terminal was
stopped, and no guard was bypassed.

## What changed

1. Card-faithful cross-sectional snapshot
   - `framework/EAs/QM5_1640_aa-indmom-12-0/QM5_1640_aa-indmom-12-0.mq5:88`
     loads a bounded completed-MN1 buffer for every governed proxy and computes
     exactly `Close(1) / Close(13) - 1`.
   - `...mq5:119` reconstructs one deterministic snapshot per calendar month,
     fails closed if any constituent lacks 14 completed monthly bars, sorts by
     score with a deterministic symbol tie-break, and selects only the positive
     top five.
   - `...mq5:374` exits an open host position when it is absent from the current
     valid monthly selection.

2. Portfolio and restart controls
   - `...mq5:210` counts open positions across all five MagicResolver slots and
     `...mq5:346` enforces the portfolio-wide slot cap before admission.
   - `...mq5:233` limits admission to the first D1 bar of the month;
     `...mq5:242` reconstructs same-month admission from deal history, preventing
     stop-out/restart re-entry.
   - `...mq5:416` validates the host/slot binding with `QM_MagicChecked`; all
     position ownership/cap checks resolve magic through `QM_Magic`.

3. Exact spread rule and safe framework ordering
   - `...mq5:274` reads exactly the prior 20 completed D1 spread observations,
     guards every dynamic buffer, rejects missing/zero/invalid samples, and
     applies the card's `2.5 x median` threshold.
   - `...mq5:436` retains the explicit MAE hook. Management and monthly exits at
     `...mq5:448` precede the two-axis entry-only news gate at `...mq5:468`.

4. Governed universe and presets
   - `framework/EAs/QM5_1640_aa-indmom-12-0/basket_manifest.json:7` seals the
     available basket as GDAXI, NDX, SP500, UK100, and WS30 with existing slots
     0-4. Lines 21-26 record FCHI, SPA35, NETH25, and STOXX50E as unavailable in
     the active DWX matrix.
   - The five retained D1 backtest presets bind the current MQ5 hash, use
     `RISK_FIXED=1000` / `RISK_PERCENT=0`, and seal all six strategy defaults.
   - Eight off-thesis FX/metal presets were removed. Existing magic registry
     rows were not edited, deleted, or repurposed; `git diff -- framework/registry`
     is empty.
   - `framework/EAs/QM5_1640_aa-indmom-12-0/SPEC.md:49` documents the final
     rankable universe, and line 109 maps the implementation to V5 framework
     contracts.

5. Regression coverage
   - `tools/strategy_farm/tests/test_qm5_1640_review_rework.py:57` checks every
     declared input for an executable use-site. The remaining tests cover the
     completed-MN1 formula/ranking, restart and portfolio controls, fail-closed
     spread median, entry-only news ordering, exact set cohort, risk modes, and
     scoped hardening.

The rejected-source EX5 (`5939b001...e752cd5`) and its stale
`build_identity.json` were removed so the repository cannot misrepresent that
binary as belonging to the repaired source. Both remain recoverable from Git.

## Verification evidence

Commands were run from `C:/QM/worktrees/rework-slot-4`.

```text
python -m pytest tools/strategy_farm/tests/test_qm5_1640_review_rework.py -q
.......                                                                  [100%]
7 passed in 1.50s
```

```text
python framework/scripts/validate_spec_doc.py framework/EAs/QM5_1640_aa-indmom-12-0
PASS  QM5_1640_aa-indmom-12-0
Summary: 1 PASS, 0 FAIL (of 1)
```

```text
python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_1640_aa-indmom-12-0
verdict: PASS; files_checked: 6; findings: []; max_news_stale_hours: 336
```

```text
python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_1640_aa-indmom-12-0 --fail-on-leak --json
verdict: BASKET_OK; n_violations: 0
```

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_1640_aa-indmom-12-0
failures: []; files_scanned: 1; D7 MAE: 0; D10 buffers: 0; D15 news: 0
```

```text
git diff --check
PASS (line-ending conversion notices only; no whitespace errors)
```

Every strategy input was also enumerated with `rg --word-regexp`; each has at
least one executable use-site (MQ5 lines 41-46 declarations; use-sites at
91-422).

## Governed compile hold

The required exact-scope command was attempted:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 \
  -EAPath framework/EAs/QM5_1640_aa-indmom-12-0/QM5_1640_aa-indmom-12-0.mq5 \
  -EALabel QM5_1640_aa-indmom-12-0 -Strict
```

Result: `INCLUDE_MIRROR_REFUSED` /
`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`; errors and warnings are `-1` because
MetaEditor was never invoked. Durable attempt summary:
`D:/QM/reports/compile/20260824_125624/summary.csv`.

A second exact-scope `build_check.ps1 -EALabel ... -SkipCompile` attempt was
also stopped at the same compile guard. The sanctioned alternative suggested
by the guard is a compile enqueue, but this ticket explicitly forbids enqueueing
backtests or compile work and forbids factory toggles. Therefore no current EX5,
strict compile PASS, smoke result, or pipeline verdict is claimed. Review must
keep this EA out of PIPELINE until a separately governed compile produces a
hash-bound EX5 and strict build-check PASS.

## Rollback

Revert the commit containing this evidence file with `git revert <commit>`.
That restores the rejected source package, including the removed EX5 and eight
off-thesis presets. This rollback is technically complete but must not be used
to promote the restored EA: the prior review verdict remains `RECYCLE`.

No DB verdict row, gate criterion, gate threshold, factory flag, backtest queue,
magic allocation, `T_Live` file, or AutoTrading setting was changed.

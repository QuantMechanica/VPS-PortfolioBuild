# rework-36005 — QM5_36005 review repair evidence

Date: 2026-08-24  
Router task: `ddb87b6b-a6db-4f8d-be8f-337341238a8c`  
EA: `QM5_36005_nnfx-coral-trendlord-woodies-harvester`  
Approved card: `strategy-seeds/cards/approved/QM5_36005_nnfx-coral-trendlord-woodies-harvester.md` (`g0_status: APPROVED`)

## Work order and scope

The durable review work order is
`docs/ops/evidence/ddb87b6b_qm5_36005_gemini_build_codex_review_2026-08-18.md`.
It rejected the producer for a full-volume +1 ATR TP, T3 in place of Coral
SMMA, an unauthorized Woodies-CCI exit, missing GMT/loss controls, entry
filters suppressing the open-position lifecycle, no smoke, and no committed
build identity. The router task was claimed as `IN_PROGRESS` with verdict
`rework burn-window slot 2`.

Scope remained build-only. No backtest was enqueued or deleted, no verdict row
or gate criterion was changed, no registry allocation was changed, no factory
state was toggled, and `C:/QM/mt5/T_Live` was not touched.

## What changed

1. Card-faithful signal and runner lifecycle

   - Entry uses the pooled 20-period `QM_SMMA` Coral baseline at
     `framework/EAs/QM5_36005_nnfx-coral-trendlord-woodies-harvester/QM5_36005_nnfx-coral-trendlord-woodies-harvester.mq5:271`; the rejected six-stage T3 arrays are absent.
   - TP remains zero on the full broker order. At +1 ATR, the manager issues
     exactly one `QM_TM_PartialClose` from the captured initial volume and
     preserves the runner (`...mq5:329-415`). The successful scale-out marker
     is persisted before BE-stop management (`...mq5:419`), so a failed stop
     modification or live re-attach cannot trigger repeated half-closes.
   - The discretionary exit starts at `...mq5:439` and uses Trend Lord color
     reversal only. Woodies CCI remains an entry confirmation.

2. Loss, time, and execution controls

   - The 2.0% account realized-loss entry halt is reconstructed from closed
     history (`...mq5:187-199`). The 23:55–00:05 window converts broker time
     through `QM_BrokerToUTC` (`...mq5:220`).
   - The 2.5% daily hard stop is wired through `QM_KillSwitchInit`, while the
     5.0% total stop now also has a local initial-equity latch and owned-position
     retry sweep that runs in tester and live (`...mq5:99-176`, `:507-514`).
   - The card's maximum three-tick deviation is converted to symbol points and
     applied through `QM_EntryConfigure` (`...mq5:55`, `:178-185`, `:516-527`).

3. Framework conformity and management reachability

   - The request uses relative host slot zero after `QM_FrameworkInit` resolves
     and validates each setfile's `qm_magic_slot_offset` through MagicResolver
     (`...mq5:252`). Existing active registry rows remain untouched:
     `framework/registry/ea_id_registry.csv:4460` and
     `framework/registry/magic_numbers.csv:17440-17442`.
   - The explicit MAE hook remains first in `OnTick` (`...mq5:540`). TP1
     management and Trend Lord exit run before news, new-bar, rollover, spread,
     daily realized-loss, and max-position entry gates (`...mq5:554-584`).
   - All declared inputs have executable non-declaration use-sites. Indicator
     access is through bounded pooled `QM_*` readers; there are no raw series,
     raw `CopyBuffer`, unbounded numeric buffers, or ML dependencies.

4. Presets, specification, and tests

   - All three D1 backtest presets retain `RISK_FIXED=1000`,
     `RISK_PERCENT=0`, registered slots 0/1/2, every declared strategy input,
     and source SHA-256
     `f1869369d5b3584f5ae99694c5e9946b2ac29d6c443900d87b6d36116b87c358`
     (each setfile lines 13, 19-20, and 42).
   - `SPEC.md` documents the one-shot durable runner and execution/loss
     contracts (`framework/EAs/QM5_36005_nnfx-coral-trendlord-woodies-harvester/SPEC.md:13-39`) and corrects the risk-tool failure name at line 101.
   - Added eight focused regressions in
     `tools/strategy_farm/tests/test_qm5_36005_review_rework_static.py:49-210`.
     They lock card/registry identity, SMMA versus T3, CCI entry-only use,
     one-shot TP1, restart state, loss/GMT/slippage controls, management order,
     framework/MAE/magic/no-ML conformity, every input use-site, and all three
     preset bindings.

## Validation evidence

| Command | Result |
|---|---|
| `python framework/scripts/skill_build_ea_guard.py --ea-id 36005 --ea-label QM5_36005_nnfx-coral-trendlord-woodies-harvester` | PASS: registry row, magic rows, and EA directory present |
| `python framework/scripts/validate_spec_doc.py framework/EAs/QM5_36005_nnfx-coral-trendlord-woodies-harvester` | PASS: 1/1 |
| `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_36005_nnfx-coral-trendlord-woodies-harvester` | PASS: four files, zero findings, 336-hour news ceiling |
| `python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_36005_nnfx-coral-trendlord-woodies-harvester --json --fail-on-leak` | `SINGLE_SYMBOL_OK`, zero violations |
| `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_36005_nnfx-coral-trendlord-woodies-harvester` | PASS: one EA, zero failures/warnings; D2/D4/D5/D7/D9/D10/D11/D17 clean |
| `python -m pytest tools/strategy_farm/tests/test_qm5_36005_review_rework_static.py -q` | PASS: `8 passed in 0.98s` |
| `python -m pytest tools/strategy_farm/tests/test_qm5_36005_review_rework_static.py tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_build_guardrails.py -q -k "not qm5_411xx_sources_have_no_unbounded_numeric_buffers"` | PASS: `57 passed, 1 deselected in 242.11s` |
| `git diff --check -- framework/EAs/QM5_36005_nnfx-coral-trendlord-woodies-harvester tools/strategy_farm/tests/test_qm5_36005_review_rework_static.py` | PASS; only configured LF-to-CRLF notices |

## Compile/build-check interlock evidence

The required exact-scope attempts were made:

```text
powershell ... framework/scripts/build_check.ps1 \
  -EALabel QM5_36005_nnfx-coral-trendlord-woodies-harvester -Strict -SkipCompile

powershell ... framework/scripts/compile_one.ps1 \
  -EAPath framework/EAs/QM5_36005_nnfx-coral-trendlord-woodies-harvester/QM5_36005_nnfx-coral-trendlord-woodies-harvester.mq5 \
  -EALabel QM5_36005_nnfx-coral-trendlord-woodies-harvester -Strict
```

Both stopped before MetaEditor/static build-check execution with the governed
`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` / `INCLUDE_MIRROR_REFUSED` interlock
because protected `terminal64` processes are alive. Durable receipt:
`D:/QM/reports/compile/20260824_111057/summary.csv` (`result=FAIL`,
`reason_class=INCLUDE_MIRROR_REFUSED`, `errors=-1`, `warnings=-1`). No compile
log exists because the guard refused before MetaEditor launch.

The sanctioned compile-utility lane currently contains older pending rows with
`COMPILE_EA_WORKER_ROLLOUT_PENDING`; creating another non-executable queue row
would not validate this worktree. No guard was bypassed, no terminal was
stopped, and no compile/backtest work item was created. Therefore no fresh
`.ex5` or smoke result is claimed. This is the sole residual review risk; the
source-level and scoped deterministic gates are clean.

## Rollback

Revert the ticket commit with `git revert <rework-36005-commit>`. This restores
the EA source, SPEC, and three presets and removes the focused test and this
evidence note. No database verdict, work-item queue, magic allocation, factory,
or live rollback is required; router coordination state can be returned from
`REVIEW` to `RECYCLE` independently if a reviewer requires a governed fresh
compile receipt before acceptance.

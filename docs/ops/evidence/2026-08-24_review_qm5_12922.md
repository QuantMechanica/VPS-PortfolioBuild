# Review — QM5_12922 ariel-first-half-month-idx (router task 089cc8c8)

Date: 2026-08-24
Reviewer: Claude (review lane)
Router task: `089cc8c8-db14-4c63-90fc-7fa2aa5f711f` (`review_ea`, assigned `codex`)
Source build task: `11468a5a-89fc-4872-b6ec-2a78250ae792` (`build_ea`, `gemini`/agy)
Source verdict: "Fixed restart-safe calendar reconstruction from D1 history and implemented
news-blackout deferral from T+1 to T+2"
Artifact: `C:/QM/repo/artifacts/qm5_12922_build_result.json`

## Verdict: RECYCLE — stale .ex5 (does not contain the delivered fix)

## Source review (mq5 = card-faithful and framework-conformant)

`framework/EAs/QM5_12922_ariel-first-half-month-idx/QM5_12922_ariel-first-half-month-idx.mq5`
(sha256 `02bdd8bd...`). Reviewed against SPEC.md (card: Ariel 1987 first-half-of-month
equity-index effect) — no code-level defects:

- Mechanism faithful: long on trading day T+1, hold through T+9, exit at open of T+10
  (`g_strategy_exit_due = trading_day > hold_days`, mq5:126); ATR(14)*3 hard stop
  (`QM_StopATR`, mq5:169). Matches SPEC §1/§2.
- Restart-safe reconstruction correct: `Strategy_GetTradingDayOfMonth` uses a bounded
  `CopyTime(_Symbol, PERIOD_D1, month_start, current, ...)` window (mq5:69), fails closed
  to 0 on no history (mq5:70-71) rather than manufacturing a T+1 on restart.
- T+2 news deferral sound: day-1 block sets `g_strategy_entry_deferred` (mq5:280-283),
  re-armed on day 2 (mq5:114-117), abandoned once trading_day>2 (mq5:121-122). Double-entry
  prevented by open-position guards (mq5:161, 190) — safe across mid-month restart.
- Every declared strategy input has a use-site beyond declaration: `strategy_atr_period`
  (mq5:137,159,169), `strategy_atr_stop_mult` (137,159,169), `strategy_hold_trading_days`
  (125), `strategy_require_d1` (135,157,186). No dead inputs.
- Framework conformity: `#include <QM/QM_Common.mqh>` (mq5:5); MAE hook
  `QM_FrameworkTrackOpenPositionMae()` (mq5:234); magic via `QM_FrameworkMagic()`
  (129220000 = 12922*10000, matches build_result); news wired
  `QM_NewsAllowsTrade2(...)` (mq5:274); bounded buffers only; no ML; no invented
  commission/swap. RISK_FIXED=1000 / RISK_PERCENT=0 for backtest (mq5:18-19).
- Setfiles governed (5/5), `; risk_mode: FIXED`, `RISK_FIXED=1000`, `RISK_PERCENT=0`,
  strategy inputs present; card_defaults_source points at the D: runtime approved card.
- Source compile clean at build time: `framework/build/compile/20260821_150617/
  QM5_12922_ariel-first-half-month-idx.compile.log` = 0 errors, 0 warnings.

## Blocking defect: the committed .ex5 predates the source fix

The build task's whole deliverable (restart-safe reconstruction + T+2 news deferral) was
added to the source on 2026-08-23 in commit `c5cc91ff0` (+98/-26 lines, incl. `CopyTime`
reconstruction, `g_strategy_entry_deferred`, `ARIEL_FIRST_HALF_MONTH_T2_DEFERRED`). The
.ex5 was NEVER recompiled after that edit:

- `.mq5` mtime 2026-08-23 18:36; `.ex5` (`49ab4db2...`) mtime 2026-08-21 17:06.
- `git log` on the .ex5 shows ONE commit only — `06e013ec4` (Aug-21 initial build) —
  while the .mq5 has the later `c5cc91ff0` fix commit on top.
- No `QM5_12922*.compile.log` exists after 2026-08-21 (checked all
  `framework/build/compile/*` dirs; the 20260823_182039 dir has no 12922 log).
- `artifacts/qm5_12922_build_result.json` (written Aug-23 18:37) reports
  `compile_succeeded: true` and `ex5_sha256: 49ab4d...` — but that hash is the pre-fix
  Aug-21 binary. The rebuild-review evidence (2026-08-23) lists only source-level
  validators (`validate_build_guardrails`, `validate_symbol_scope`), never a fresh compile.

Consequence: the on-disk/committed .ex5 does not contain the fix. Sending this to Q02 would
either test the OLD behavior (silently voiding the deferral/restart fix — the documented
stale-ex5 → Q04 INVALID class) or leave the artifact internally inconsistent. Not clean for
Q02.

## Actionable fix (source needs NO change)

1. Recompile the .ex5 from current source (mq5 sha `02bdd8bd...`) via the COMPILE_EA
   queue / OFF-window (recompile in active inventory is ROT — outside the review lane).
2. Regenerate `artifacts/qm5_12922_build_result.json` with the TRUE new ex5 sha256 and a
   post-2026-08-23 compile log.
3. Re-submit for review; source review already passes, so this should re-clear quickly.

## Evidence
- mq5: `framework/EAs/QM5_12922_ariel-first-half-month-idx/QM5_12922_ariel-first-half-month-idx.mq5`
- ex5 (stale): same dir, `.ex5` (`49ab4db2...`, committed `06e013ec4`)
- build_result: `artifacts/qm5_12922_build_result.json`
- source fix commit: `c5cc91ff0`; initial build: `06e013ec4`
- compile log (Aug-21 only): `framework/build/compile/20260821_150617/QM5_12922_ariel-first-half-month-idx.compile.log`

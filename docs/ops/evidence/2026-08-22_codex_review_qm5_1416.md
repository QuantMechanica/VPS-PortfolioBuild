# Codex review — QM5_1416 — 2026-08-22

Router task: `25b11704-7145-4489-b8d3-0022f6ffb642`
Gemini task: `ad4b4d2e-3fb7-48ea-a424-09d85a616e34`

Verdict: **RECYCLE**. The artifact hashes match the current MQ5 and EX5, but the
source fails strict hardening and materially changes the approved entry geometry.

## Findings

1. Canonical strict hardening reports four failures: missing direct D7 MAE hook,
   a D9 `QM_EntryRequest` whose `expiration_seconds` is uninitialized, and two
   D10 unchecked `CopyBuffer` array accesses (lines 192 and 312).
2. The card requires an ATR-buffered SELL-STOP that remains valid for eight H1
   bars. The EA waits for a completed-bar breakdown and submits a market sell at
   current bid. It does not create, age, cancel, or re-evaluate the specified
   pending order.
3. The card requires at least two pivot highs and two pivot lows for the parallel
   channel. When either side has fewer than two, the EA substitutes flag-wide
   extrema and a regression slope, accepting geometry that the card rejects.
4. The card requires a ±3 H1-bar high-impact-news blackout. The EA wires only
   `QM_NEWS_TEMPORAL_PRE30_POST30`, not the specified three-hour window.
5. TP1 and projected-channel exit state is held only in globals and is not
   reconstructed for an open position after restart. That can disable the
   pattern-failure exit or repeat partial-close behavior.

## Verified identity and safe contracts

- MQ5 SHA-256 matches artifact:
  `26ca4c25b50083f61e5abaac1c1bee8ad46abc35a90f2f3bd0cff03355da7dc6`.
- EX5 SHA-256 matches artifact:
  `952736bf35972b55218820ac3fd69d902b1639753cc598fa1514241cb9c7ead1`.
- Representative backtest set uses `RISK_FIXED=1000`, `RISK_PERCENT=0`.
- `qm_news_stale_max_hours` remains 336.

No pipeline verdict is inferred, and this Gemini output is not approved or
advanced. Repair must restore exact card semantics, pass strict hardening, and
use the governed compile path.

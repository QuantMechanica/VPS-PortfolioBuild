# rework-9466 — QM5_9466 review-fix evidence

Date executed: 2026-08-24 (Europe/Berlin)

Router task: `bf0ead8d-bde8-4b4b-838f-cace0a26e5c3`

EA: `QM5_9466_connors-r2-d1`

Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9466_connors-r2-d1.md` (`g0_status: APPROVED`)

## Status

The four review findings are repaired in source, package scope, durable documentation, and focused regression coverage. Static validators pass. A fresh EX5 is **not claimed**: the exact EA-scoped `compile_one.ps1 -Strict` command stopped before MetaEditor because active factory terminals triggered `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`. No factory toggle, process stop, compile enqueue, backtest enqueue, verdict-row overwrite, or live mutation was performed.

This is a repairable implementation/package defect, not `GENUINE_NO_SIGNAL` or an irreparable card defect.

## What changed

1. The EA now declares `PERIOD_D1` immediately after framework initialization and binds the framework Friday-close override with a machine-searchable reason (`framework/EAs/QM5_9466_connors-r2-d1/QM5_9466_connors-r2-d1.mq5:168-172`). Its only strategy decision boundary is `QM_IsNewBar(_Symbol, PERIOD_D1)` (`:212`).
2. MAE remains the first `OnTick` hook. Friday close, the restart-safe ten-D1-bar time stop, and RSI exit execute before entry-only warmup/quote/ATR/spread/news gates (`...mq5:184-230`). The time stop uses `QM_TM_HeldPeriods` instead of a local raw-series count (`:133`).
3. The entry path remains card-faithful: SMA(200), three declining RSI(2) closes, RSI day-one ceiling, ATR stop, spread/ATR ceiling, and one-position-per-magic are all wired. Invalid ATR/stop geometry now fails closed (`...mq5:52-111`). Raw D1 reads are bounded, new-bar-only, and explicitly `perf-allowed`; no `CopyBuffer` or ML path exists.
4. The package now contains only the card-approved ports: `SP500.DWX`, `NDX.DWX`, and `WS30.DWX`. Ten unauthorized FX/metals/additional-index setfiles were removed. The three retained fixed-risk setfiles bind MQ5 SHA-256 `ced213c3889080ace807f4bb97c0984e2acc5b15e44464d625080e00f88670bc`.
5. `SPEC.md` was regenerated as clean UTF-8, identifies only the approved ports, declares the D1 execution boundary, restores the exact source/card identity, and states `$1,000` fixed backtest risk (`framework/EAs/QM5_9466_connors-r2-d1/SPEC.md:43-49`, `:55-62`, `:76-94`).
6. `validate_spec_doc.py` now fails closed on invalid UTF-8 or non-whitespace Unicode control characters (`framework/scripts/validate_spec_doc.py:57-76`). The focused regression proves the former `0x1B` defect is rejected and audits execution ordering, every declared input, raw-series annotations, framework/magic/risk/MAE wiring, no ML, and the exact setfile universe (`tools/strategy_farm/tests/test_qm5_9466_rework.py:36-123`).

## Registry and identity evidence

- `framework/registry/ea_id_registry.csv` has active row `9466 / connors-r2-d1`.
- `framework/registry/magic_numbers.csv` has the existing active slots 0–12. No row is missing, so neither registry nor `QM_MagicResolver.mqh` was changed.
- Existing allocations outside the approved package remain untouched as required; package authorization is enforced by the three emitted setfiles and regression test.
- MQ5 SHA-256: `ced213c3889080ace807f4bb97c0984e2acc5b15e44464d625080e00f88670bc`.
- Existing/stale EX5 SHA-256: `f0f7faafebc99819cac1eff9c492a4b7b087d9fcecbda27f24de2d907bd8d681`.
- `build_identity.json` records `build_check_passed=false`, `ex5_current_for_mq5=false`, and the compile refusal instead of presenting the old binary as current.

## Test and validation output

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_9466_rework.py
5 passed in 0.64s

python -m pytest -q tools/strategy_farm/tests/test_qm5_9466_rework.py tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_validate_symbol_scope.py
28 passed in 1.54s

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_9466_connors-r2-d1
Summary: 1 PASS, 0 FAIL

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_9466_connors-r2-d1
verdict=PASS; files_checked=4; findings=[]

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_9466_connors-r2-d1 --fail-on-leak --verbose
SINGLE_SYMBOL_OK; n_violations=0

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_9466_connors-r2-d1
files_scanned=1; failures=[]; warnings=[]; D2-D18 mechanized checks have zero failures

python framework/scripts/skill_build_ea_guard.py --ea-id 9466 --ea-label QM5_9466_connors-r2-d1
status=ok; ea_registry_row=true; magic_registry_rows=true; ea_dir_exists=true

git diff --check
PASS (no whitespace errors; Git emitted only normal LF-to-CRLF notices)
```

Full requested suite:

```text
python -m pytest -q tools/strategy_farm/tests
4 failed, 4697 passed, 4 skipped, 42 subtests passed in 2632.75s (0:43:52)
```

All four failures are outside the changed QM5_9466/validator/test paths and match known fleet-wide drift:

1. `test_build_gate_hardening.py::test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` — seven QM5_41134–41139 buffer findings.
2. `test_execution_contract_lint.py::test_20030_20032_bindings_follow_deployed_calendar_but_remain_blocked` — deployed calendar coverage/hash advanced from 2026-08-21 to 2026-08-29.
3. `test_execution_contract_lint.py::test_20009_ftmo_news_calendar_is_exact_and_evidence_bound` — the same refreshed calendar hashes differ from pinned expectations.
4. `test_opt_census_dispatch.py::test_opt_census_ranks_tier6_not_priority` — expected active-manifest rank 7, query returned 6.

## Compile boundary

Exact scoped command:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_9466_connors-r2-d1/QM5_9466_connors-r2-d1.mq5 -Strict
```

Result: `FAIL` before MetaEditor, `reason_class=INCLUDE_MIRROR_REFUSED`, `failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`. Durable receipts:

- `C:/QM/worktrees/rework-slot-4/framework/build/compile/20260824_134837/QM5_9466_connors-r2-d1.compile.log`
- `D:/QM/reports/compile/20260824_134837/summary.csv`

The existing EX5 remains stale and no smoke/backtest was run. A reviewer must require a governed fresh compile in a quiescent/authorized compile window before any Q02 or pipeline handoff.

## Rollback

Revert the ticket commit with `git revert <ticket-commit>`. This restores the prior MQ5, SPEC, build identity, and 13-setfile package; removes the focused test/evidence; and reverts the validator hardening. No runtime database, registry, factory, backtest queue, terminal, or live rollback is required.

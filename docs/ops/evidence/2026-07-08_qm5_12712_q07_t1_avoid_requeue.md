# QM5_12712 Q07 T1-Avoid Requeue - 2026-07-08

Scope: branch `agents/board-advisor`; no `T_Live`, no AutoTrading, no portfolio-gate edits, no portfolio admission/KPI/Q08 contribution edits, and no deploy-manifest edits.

## Decision

`docs/research/CROSS_ASSET_FX_DISCOVERY_2026-06-09.md` remains the controlling 66-pair FX cointegration scan artifact. It carded only `QM5_12532` (`AUDUSD~NZDUSD`) and `QM5_12533` (`EURJPY~GBPJPY`); both are already built and neither is Q02-blocked.

The extended screen artifacts under `D:/QM/strategy_farm/artifacts/research/coint_screen_ext_20260706/` did not leave a clean unbuilt pair:

- `AUDCAD~EURUSD`: formal survivor, but trade check OOS net Sharpe `-0.39`.
- `AUDCAD~GBPAUD`: already built as `QM5_13024`, latest state Q02 PASS / Q04 FAIL.
- `GBPCAD~GBPNZD`: already built as `QM5_13029`, latest state Q02 PASS / Q03 PASS / Q04 FAIL.
- `AUDCAD~GBPNZD`: watchlist only, OOS net Sharpe `0.76` below the 0.8 bar and overlaps `QM5_13024`.
- `AUDNZD~EURGBP`: collapses toward existing AUDNZD reversion exposure.

Per the mission fallback, this pass advanced an existing forex basket instead of minting a duplicate.

Selected basket:

- EA: `QM5_12712_edgelab-eurgbp-euraud-cointegration`
- Pair: `EURGBP.DWX` / `EURAUD.DWX`
- Logical basket: `QM5_12712_EURGBP_EURAUD_COINTEGRATION_D1`
- Prior state: Q02 PASS, Q03 PASS, Q04 PASS, Q05 PASS, Q06 PASS, Q07 `INFRA_FAIL`

## Prior Q07 Failure

Existing Q07 row:

| Field | Value |
|---|---|
| Work item | `1b9fa7a5-48a7-4e57-a699-9e55dfb0cb19` |
| Prior status | `failed` |
| Prior verdict | `INFRA_FAIL` |
| Prior terminal | `T1` |

The prior retry was mixed T1 infrastructure evidence:

- `20260707_161335` and `20260707_165337`: `TIMEOUT`, `METATESTER_HUNG`, `INCOMPLETE_RUNS`, `MODEL4_MARKER_REQUIRED` at 2400 seconds.
- `20260707_210423`: run 01 invalid with `EMPTY_EXPERT`, `EMPTY_SYMBOL`, `M0_1970_PERIOD`, `BARS_ZERO`; run 02 valid.
- Valid T1 seed reports showed 172 trades, PF 1.13, net profit 2482.82, no ONINIT failure, and no log bomb.

That supports an in-place infrastructure reroute away from T1, not a strategy failure and not a duplicate queue item.

## Queue Action

Requeued the existing row in place:

```text
python tools/strategy_farm/farmctl.py --root D:/QM/strategy_farm enqueue-backtest --ea QM5_12712 --phase Q07
```

Result:

- Created rows: `0`.
- Requeued work item: `1b9fa7a5-48a7-4e57-a699-9e55dfb0cb19`.
- Status after requeue: `pending`.
- Verdict after requeue: `NULL`.
- Attempt count after requeue: `0`.
- Archived previous report root: `D:/QM/reports/work_items/1b9fa7a5-48a7-4e57-a699-9e55dfb0cb19.requeued_20260707T2347020000`.

Backed up the farm DB before the payload update:

```text
D:/QM/strategy_farm/state/backups/farm_state_before_qm5_12712_q07_t1_avoid_20260707T234724Z.sqlite
```

Then conditionally updated only that pending/unclaimed row with:

- `priority_track=true`
- `avoid_terminals=["T1"]`
- `skip_terminals=["T1"]`
- `cpu_ceiling_dispatch_hold=true`
- `q07_seed_timeout_sec=5400`
- `reroute_reason_class=T1_Q07_TIMEOUT_AND_BLANK_REPORT_INFRA`

## Verification

Readback after mutation:

- `status=pending`
- `verdict=NULL`
- `attempt_count=0`
- `claimed_by=NULL`
- `priority_track=true`
- `avoid_terminals=["T1"]`
- `skip_terminals=["T1"]`

Duplicate guard:

- Exactly one pending row exists for the same EA/phase/symbol/setfile.
- No new work item was created.

`framework/scripts/mt5_queue_status.py --sqlite D:/QM/strategy_farm/state/farm_state.sqlite --limit 12` shows `QM5_12712` Q07 as `queued_top[0]`.

Stop condition: the farm is at the backtest CPU ceiling (`active=7`, `pending=5439`). No manual MT5/tester run was launched; paced workers own execution.

Machine-readable artifact: `artifacts/qm5_12712_q07_t1_avoid_requeue_20260708.json`.

# QM5_41006 rework build evidence — 2026-08-23

- Task: `b42bac52-ccda-4a73-b49b-faab46b48c88`
- EA: `QM5_41006_man-ahl-multispeed-ewma-trend`
- Approved card: `D:\QM\strategy_farm\artifacts\cards_approved\QM5_41006_man-ahl-multispeed-ewma-trend.md`
- Approved-card SHA-256: `eb537b7a95a3357332cad3c8ae2bd12d7bdaa58b9458cd74eaae160111160a80`
- Reworked source SHA-256: `b02152f51bd94a0a1714fb89d1acf87a5a40d8e167c48e259be8bf5525e1517d`
- Build artifact: `C:\QM\repo\artifacts\builds\b42bac52-ccda-4a73-b49b-faab46b48c88.json`

## Rework completed

The D1 forecast now averages all six approved EWMAC pairs `(2,8)` through `(64,256)`, divides each spread by a 60-bar realized-volatility estimate from close-to-close price changes, and fails closed if any EMA or volatility input is unavailable. The aggregate score is bounded to `[-1,1]`, entry uses the approved `+/-0.35` threshold crossings, and open exposure is closed/reopened on each new D1 forecast to implement score-proportional dynamic rebalancing without an invented zero-cross exit. ATR(14) stop/spread values, the execution contract, fixed-risk default, and approved daily/total loss controls are wired through the framework.

## Focused verification

- `validate_spec_doc.py`: PASS.
- `validate_build_guardrails.py --max-news-stale-hours 336`: PASS; five files checked, no findings.
- `validate_symbol_scope.py --fail-on-leak`: `SINGLE_SYMBOL_OK`; zero violations.
- `raw_mq5_quarantine.py check --purpose compile`: `RAW_MQ5_SOURCE_ALLOWED`.
- Registry/set audit: slots `0..3`, magic `410060000..410060003`, and registered symbols `NDX.DWX`, `SP500.DWX`, `XTIUSD.DWX`, `XAUUSD.DWX` agree; every backtest set retains `RISK_FIXED=1000` and `RISK_PERCENT=0`.
- Forbidden-literal scan and `git diff --check`: clean.

## Compile interlock

The strict build wrapper refused an ad-hoc compile with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because protected T1–T10 processes were active. No process was stopped. The required governed fallback, `farmctl enqueue-compile QM5_41006`, then refused the bound existing build with `EX5_ALREADY_PRESENT` and `BOUND_SETFILE_HASH_EXISTS`; `force_rebuild_authorized` was false. Consequently no fresh EX5 was emitted and no smoke or pipeline verdict is claimed. The existing EX5 is explicitly stale relative to this source (EX5 SHA-256 `a9accb29338de23d6e55bc9d9d66e496db0c660236bf520df699293a3eb41651`, modified `2026-08-18T17:29:22Z`).

Disposition: rework is complete and static verification passes; fresh strict compilation remains held by the governed interlock and requires authorized force rebuild.

## Scheduled single-pass recheck — 2026-08-23T18:16:53Z

Codex repeated `validate_spec_doc.py`, `validate_build_guardrails.py --max-news-stale-hours 336`, `validate_symbol_scope.py --fail-on-leak`, raw-source quarantine, and `git diff --check`; all static checks passed and the source hash remained `b02152f51bd94a0a1714fb89d1acf87a5a40d8e167c48e259be8bf5525e1517d`. The sanctioned `compile_ea.py --ea-id 41006 --json --fail-on-error` attempt stopped with `COMPILE_FAILED / INCLUDE_MIRROR_REFUSED`. Contemporaneous farm health reported 9 active rows, 20 PowerShell workers, and all 10 T1-T10 workers alive. No process was stopped or started; no fresh EX5, smoke, or pipeline verdict is claimed.

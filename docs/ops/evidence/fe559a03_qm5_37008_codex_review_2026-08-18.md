# QM5_37008 Codex review evidence — 2026-08-18

- Review task: `fe559a03-a33b-4003-a976-8bd9244cfe55`
- Source build task: `dbf26171-4fbc-4ac0-bdf8-c76c761e4974` (Gemini)
- EA: `QM5_37008_garch-volatility-forecast-breakout`
- Disposition: `FAIL_CODE_REVIEW`; remain in `REVIEW`

## Verification

- Repository branch: `agents/board-advisor`
- Mechanical pre-screen: PASS (artifacts, freshness, three active magic rows, setfiles, fixed-risk mode, input wiring)
- `validate_build_guardrails.py`: PASS, four files checked, no findings, maximum news staleness 336 hours
- Fresh compile through `compile_ea.py --force`: PASS, 0 errors, 0 warnings
- Backtest sets: `RISK_FIXED > 0`, `RISK_PERCENT=0`, and symbol slots match the registry
- Card-to-code review: FAIL

## Blocking findings

1. The approved card requires a trailing stop that ratchets with the one-sigma GARCH cone (card line 98). `Strategy_ManageOpenPosition()` is empty (MQ5 line 237), so the required trade-management mechanic is entirely absent.
2. On a new D1 bar, `OnTick()` applies `Strategy_NoTradeFilter()` at line 300 before refreshing GARCH/ATR state at line 330. Entry is evaluated after the refresh without re-running admission. The spread decision for an entry can therefore use the prior cached ATR rather than the card-required `ATR(14,D1)[1]` associated with the newly evaluated signal.
3. The code adds a separate `strategy_max_spread_points=100` hard cap (MQ5 lines 56 and 181) that is absent from the approved card. This instrument-dependent extra admission rule changes the governed strategy universe/frequency and is not justified by the source build evidence.

## Required repair

- Implement deterministic one-sigma GARCH-cone trailing for this EA's symbol and magic, with stop tightening only.
- Refresh closed-bar state before admission and recheck current spread against the refreshed ATR immediately before entry.
- Remove the undocumented absolute spread cap or obtain an OWNER-approved card change that defines it per symbol.
- Recompile and regenerate bound evidence; a later Codex review remains mandatory.

No pipeline verdict is inferred. No Q phase was started, no terminal was launched, and AutoTrading/T_Live were not enabled.

# review_ea — QM5_11518 carter-t-ema5-100-mtf-m15-h1 (Claude, 2026-08-23)

**Router task:** `127a492b-b30f-4308-9bd2-81312f914e9e` · source build `d0f1e256-7e79-48ed-ac9b-ecdde5128a35`
(gemini/agy) · reason `codex_review_required_for_gemini_code` — left in REVIEW per that rule.

Gemini's build-verdict claim: *"Carter-T EMA5/100 MTF strategy implemented and verified"*.

## What was checked

`.mq5` (226 lines) and `SPEC.md` read in full, `magic_numbers.csv` rows for ea_id 11518.
Unwired-input check per `feedback_ea_review_unwired_input_check`.

## Findings

Simple, mechanical H1-regime-filtered M15 EMA(5/100) crossover, single leg per symbol (no
basket). SPEC.md §1–§7 matches the shipped `.mq5` exactly — parameter table, symbol universe,
timeframe, and risk model all line up (unlike the QM5_34008 review this cycle, where the spec had
drifted from the code).

- **Inputs — all wired.** `strategy_fast_ema_period`, `strategy_slow_ema_period` (H1 regime +
  M15 cross, both legs), `strategy_sl_pips`/`strategy_tp_pips` (SL fixed-pips, TP via R:R = tp/sl
  = 2.0, matches spec's "1:2.0"), `strategy_max_spread_pips` (entry-blocking spread filter).
- **Magic/slot registry — consistent.** `magic_numbers.csv`: slot 0 = EURUSD.DWX (115180000),
  slot 1 = GBPUSD.DWX (115180001), registered by Codex 2026-08-16, matches `symbols_registered`
  in the build result.
- **Entry logic is a textbook regime + cross combo**, no lookahead: H1 filter reads shift 1
  (last closed H1 bar), M15 cross compares shift 1 vs shift 2 (last two closed M15 bars) — no
  use of the forming bar.
- **Risk/news — compliant.** `RISK_FIXED=1000`, `RISK_PERCENT=0` (correct for backtest).
  `qm_news_stale_max_hours=336` — at the ceiling, not above it.
- **Charter fit.** Mechanical, no ML, no martingale/grid, fixed SL/TP with bounded R:R, 2–8h
  hold per spec — fits Edge Lab Charter's swing/scalping horizon.
- No unwired inputs, no dead code, no magic/slot mismatch, no framework hook left as an
  unexplained stub beyond the standard `Strategy_ManageOpenPosition`/`Strategy_ExitSignal`
  no-ops that this single-leg fixed-SL/TP design correctly doesn't need (exit is TP/SL only,
  handled by the broker/framework order, not by an explicit exit hook).

No defects found.

## Verdict

**PASS-leaning, clean build.** SPEC.md and `.mq5` agree, inputs wired, magic registry consistent,
entry logic uses only closed bars. Left in REVIEW — Codex review still required before this can
move to APPROVED/PIPELINE per the gemini-code rule.

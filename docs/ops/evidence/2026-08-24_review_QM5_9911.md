# Review — QM5_9911 bandy-donchian-20-classic-breakout-trend (review_ea)

- Router task: `afef5155-0368-4013-89c2-918df766d6e6` (state REVIEW, assigned codex, source_agent gemini/agy)
- Source build task: `970379cc-27ef-4f71-a07e-5421e45171ef`
- Build artifact: `D:/QM/strategy_farm/artifacts/builds/970379cc-27ef-4f71-a07e-5421e45171ef.json`
- Reviewer: Claude review lane (read-only, mode=ro)
- **Verdict: APPROVED (clean for Q02)**

## Scope
EA `framework/EAs/QM5_9911_bandy-donchian-20-classic-breakout-trend/` (.mq5, SPEC.md, sets/)
reviewed against the approved Strategy Card
`D:/QM/strategy_farm/artifacts/cards_approved/QM5_9911_bandy-donchian-20-classic-breakout-trend.md`
(`g0_status: APPROVED`, R1-R4 all PASS).

## Card-faithful mechanism — CONFIRMED
- Entry: `close[1]` vs `HHV(high,20)/LLV(low,20)` over prior 20 completed bars (shifts 2..21, current+signal bar excluded → no look-ahead), gated by `SMA(close,200)` regime. mq5:100-138, 158-179. Matches card Entry.
- Exit: 10-bar Turtle trailing channel (`close[1] < LLV(low,10)` long / `> HHV(high,10)` short), shifts 2..11. mq5 Strategy_ExitSignal:220-255. Matches card Exit.
- Hard time stop 60 D1 bars via `QM_TM_HeldPeriodsForMagic`. mq5:213-216. Matches.
- Catastrophic stop `2.5*ATR(14)` via `QM_StopATRFromValue`, side-validated. mq5:181-196. Matches card Stop Loss.
- One position per magic, long/short mutually exclusive (`QM_TM_OpenPositionCount>0` guard). mq5:152. Matches.
- Setfile defaults (20/10/200/14/2.5/60) match card defaults and lie within card P3 sweep ranges.

## Dead-input check — CLEAN
Every declared strategy input has ≥1 use-site beyond declaration:
`strategy_entry_lookback`(6), `strategy_exit_lookback`(6), `strategy_regime_sma_period`(3),
`strategy_atr_period`(3), `strategy_atr_stop_mult`(3), `strategy_time_stop_bars`(3).
Framework inputs (news, seed, stress, weight) all wired through `QM_FrameworkInit`.

## Framework conformity — CLEAN
- `#include <QM/QM_Common.mqh>`; QM_FrameworkInit/Shutdown/OnTimer/OnTradeTransaction wired.
- MAE hook `QM_FrameworkTrackOpenPositionMae()` present in OnTick.
- Risk: `RISK_FIXED=1000`, `RISK_PERCENT=0` (backtest); live scaffold correct per card §Position Sizing.
- Magic via `QM_FrameworkMagic()`; registry rows active `ea_id*10000+slot` (slot0 GDAXI=99110000 … slot6 EURUSD=99110006). `framework/registry/magic_numbers.csv`, `ea_id_registry.csv` source_id matches card.
- News filter wired (`QM_NewsAllowsTrade2` temporal/compliance path).
- Bounded buffers only (lookback-bounded loops, per-bar `iHigh/iLow/iClose`, framework `QM_SMA/QM_ATR`); no raw series copies, no ML, no invented commission/swap.

## Build evidence — COMPILE_OK for current source hash
- `build_check_passed: true` in build artifact.
- mq5 sha256 matches current file: `13b1bfefd404cddf97e0086b4212c0f1bc1f053c6bd51aa48e67a0307499cdf0`.
- ex5 sha256 matches: `607c9675c25083743b71c478a620881d0f1551de767b4dd04e7b3c426bcd319d`.
- Setfiles carry governed-generator header (`set_version`, `build_hash`, `card_defaults_source` → the D: canonical card that exists).

## Observation (non-blocking, repo-mirror hygiene)
The approved card exists at the canonical runtime path (`D:/QM/strategy_farm/artifacts/cards_approved/`)
but is not mirrored into the repo `artifacts/cards_approved/`, unlike siblings QM5_9923/9924.
SPEC's `per artifacts/cards_approved/...` resolves canonically on D:. Not a Q02 gate defect;
recommend a later mirror commit for repo-side provenance completeness.

## Verdict
APPROVED — card-faithful, no dead inputs, framework-conformant, build_check COMPILE_OK at current hash. Clean for Q02.

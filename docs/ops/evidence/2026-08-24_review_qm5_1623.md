# QM5_1623 hopwood-bermaui-dss-h4 — Claude review (gemini build)

Date: 2026-08-24 UTC
Router task: `9c4f7a27-e62e-41e4-8d21-1e74c7a05c33` (review_ea, source_agent gemini/agy)
Source build task: `8d8774bf-011a-4c1e-ab3b-544e290d6435`
Reviewed: `framework/EAs/QM5_1623_hopwood-bermaui-dss-h4/` (.mq5, SPEC.md, docs/strategy_card.md, sets/)
Card: `framework/EAs/QM5_1623_hopwood-bermaui-dss-h4/docs/strategy_card.md` (`g0_status: APPROVED`)

## Verdict: RECYCLE — delivered EX5 implements a different strategy than the approved card

This is an independent re-review; it confirms the prior Codex review note
`docs/ops/evidence/2026-08-23_qm5_1623_gemini_code_review.md` (REQUEST_CHANGES). The
strategy-identity divergences below are decisive on their own.

## Card vs implementation (file:line, `QM5_1623_hopwood-bermaui-dss-h4.mq5`)

Critical — strategy identity does not match the APPROVED card:

1. DSS parameters. Card = DSS(10,5,5). EA = (8,5,3): `strategy_dss_stoch_period=8`
   (mq5:47), `strategy_dss_inner_ema=5` (mq5:48), `strategy_dss_outer_ema=3` (mq5:49).
2. Band methodology. Card = Bermaui 80th/20th **percentile** of DSS over **100** H4 bars.
   EA = `mean ± 1.8·population_std` over a **20**-bar lookback: `strategy_bermaui_lookback=20`
   (mq5:50), `strategy_bermaui_k=1.8` (mq5:51), thresholds computed at mq5:159-171. A
   mean/σ band is not a percentile band, and the window is 20 not 100.
3. Invented entry gate. EA adds `strategy_min_overshoot_mult=2.0` (mq5:52), an extra
   "overshoot ≥ 2σ" condition ANDed into both entries (mq5:181,190). Not in the card;
   materially changes entry frequency.
4. Stop loss. Card = 2.0·ATR(14). EA = `strategy_atr_sl_mult=1.5` (mq5:56).
5. Take-profit. Card specifies **no** baseline TP. EA adds `strategy_atr_tp_mult=1.5`
   (mq5:57), wired into both entries (mq5:212,229).
6. Break-even. Card has no break-even rule. EA adds `strategy_be_atr_mult=0.75` (mq5:59)
   with BE-move logic in `Strategy_ManageOpenPosition()` (mq5:~245-275).
7. Time stop. Card = 20 H4 bars. EA = `strategy_max_hold_bars=10` (mq5:58).

High — required exits missing:

8. DSS band re-cross exit (card "long exits when DSS crosses above overbought-band")
   is not implemented. `Strategy_ExitSignal()` (mq5:~289-305) fires only on the opposite
   **entry** signal.
9. D1 EMA(200) regime-flip exit (card) is not implemented anywhere.

Medium:

10. Exit/state ordering. `OnTick` runs `Strategy_ManageOpenPosition` + `Strategy_ExitSignal`
    before `AdvanceState_OnNewBar()`, so exits act on the prior closed bar's state and the
    card's "fresh opposite signal closes current position before opening the new one" cannot
    occur on the same bar.
11. Spread guard bypass on first eligible bar: `Strategy_NoTradeFilter` (mq5:~232) skips the
    spread check while `g_atr_1==0` (pre-first-AdvanceState), and later compares against the
    prior cached ATR.

Low:

12. `SPEC.md:86` renders the fixed risk as `,000 per trade` (missing `$1`), leaving the
    durable spec ambiguous.

## Checks that PASS (artifact/framework conformity only — not a strategy verdict)

- Framework chain: `#include <QM/QM_Common.mqh>`; `QM_FrameworkInit`/`Shutdown`/`OnTimer`/
  `OnTradeTransaction` wired; MAE hook `QM_FrameworkTrackOpenPositionMae()` is first in
  `OnTick`; magic via `QM_FrameworkMagic()` (MagicResolver, ea_id·10000+slot); news gate
  `QM_NewsAllowsTrade2` wired; Friday close before news gate.
- Risk mode: `RISK_FIXED=1000`, `RISK_PERCENT=0` in EA and in all 13 backtest setfiles.
- All 14 declared strategy inputs have ≥1 use-site beyond declaration (no dead inputs) —
  but with the wrong values/mechanism per above.
- Indicator buffers bounded; raw `iHigh/iLow/iClose` carry `perf-allowed` comments; no ML,
  martingale, grid, or invented commission/swap.

## Disposition

No source, binary, registry, setfile, verdict, or trade stream was modified. T_Live and
AutoTrading untouched. Current EX5 embodies a different strategy from the approved
`QM5_1623` identity and must not be enqueued to Q02. Corrected gemini code (exact
DSS(10,5,5); rolling 80/20 percentile over 100 H4 bars; SL 2.0·ATR; remove TP + BE +
overshoot gate; 20-bar time stop; add band-recross and D1-regime-flip exits; snapshot
closed-bar state before exits/entries; fail-closed spread guard) requires a fresh
mandatory Codex review.

Verdict: RECYCLE.

# rework-36007 — QM5_36007 review-fix evidence

- Router task: `e349534a-89d0-4640-b7d5-48761fd3b5f3`
- EA: `QM5_36007_nnfx-vidya-trix-fisher-momentum`
- Worktree: `C:/QM/worktrees/rework-slot-1`
- Card: `strategy-seeds/cards/approved/QM5_36007_nnfx-vidya-trix-fisher-momentum.md` (`g0_status: APPROVED`)
- Review work order: `docs/ops/evidence/e349534a_qm5_36007_gemini_build_codex_review_2026-08-18.md`
- Follow-up review read: `C:/QM/repo/docs/ops/evidence/e349534a_qm5_36007_recycle1_codex_review_2026-08-24.json`
- Scope: build/review repair only; no backtest enqueue, verdict overwrite, factory toggle, registry allocation, or T_Live access.

## What changed

1. Closed the remaining build-gate failure by guarding both rolling CMO buffers
   with explicit `ArraySize` checks before indexed removal
   (`framework/EAs/QM5_36007_nnfx-vidya-trix-fisher-momentum/QM5_36007_nnfx-vidya-trix-fisher-momentum.mq5:176-179`).
2. Added a defensive Fisher/rates bound before the lookback loop
   (`...mq5:223-224`). The single bespoke raw-series fill remains bounded to a
   D1 cache refresh and carries the reviewed `perf-allowed` annotation
   (`...mq5:125`).
3. Rebound the card's 2.5% daily hard stop and 5.0% total-DD value into the
   framework kill-switch after `QM_FrameworkInit` (`...mq5:764-770`). The EA's
   local risk state distinguishes the 2.0% daily realized-loss entry halt,
   2.5% daily equity hard stop, and 5.0% local total-equity halt
   (`...mq5:379-434`). The three card values are accepted only at their exact
   approved values (`...mq5:98-100`).
4. Declared the approved D1 execution contract and made the entry new-bar gate
   explicitly D1 (`...mq5:756-759,826`). This prevents a non-D1 host chart from
   creating faster re-entry cadence.
5. Made risk anchors and all halt latches restart-safe in live/demo operation
   using magic-scoped terminal global variables, while testers initialize
   isolated per-run state (`...mq5:280-376`). OnInit refuses an unavailable or
   invalid durable anchor (`...mq5:771-772`).
6. Added focused regression tests for all review findings, all declared input
   use-sites, each backtest setfile's fixed-risk contract, hardening output, MAE
   ordering, and the existing magic allocation
   (`tools/strategy_farm/tests/test_qm5_36007_review_rework.py:39-167`).
7. Updated the EA SPEC revision and corrected its obsolete risk-gate code name
   (`framework/EAs/QM5_36007_nnfx-vidya-trix-fisher-momentum/SPEC.md:7,103-111`).

## Review-finding closure map

- **50% TP1 + runner:** partial close is `QM_TM_PartialClose(... volume * 0.5 ...)`
  and the remaining position is protected at BE+1 pip (`...mq5:616-680`); TRIX
  recross remains the runner exit (`...mq5:692-719`). No full-position broker
  TP is attached (`req.tp` remains zero).
- **Raw calls / buffer safety:** the only raw `CopyRates` call is bounded and
  cadence-annotated (`...mq5:125`); all dynamic indexes now have mechanical
  bounds. Focused hardening reports zero failures and zero warnings.
- **Per-tick TRIX recomputation:** `Strategy_ComputeClosedBarSignals` is reached
  only from the D1 `QM_CalendarPeriodKey` cache advance (`...mq5:265-277`), not
  from `Strategy_ExitSignal`.
- **D1 host contract:** OnInit declares `PERIOD_D1` and the entry cadence uses
  `QM_IsNewBar(_Symbol, PERIOD_D1)` (`...mq5:756-759,826`), independent of the
  chart timeframe.
- **GMT and drawdown:** rollover converts broker time through
  `QM_BrokerToUTC(TimeCurrent())` (`...mq5:475`); all 2.0% / 2.5% / 5.0% limits
  have exact init validation, executable use-sites, magic-scoped durable
  anchors/latches, and framework kill-switch wiring
  (`...mq5:98-100,280-434,764-772`).
- **Management reachability:** an open strategy position makes the no-trade
  hook return false while retaining `g_entry_blocked`, so management and exits
  run while rollover/spread/risk filters still block new entries
  (`...mq5:500-510,786-830`).
- **Tracked identity:** source, SPEC, regression test, and this evidence note
  are committed together with explicit pathspecs. The pre-existing EX5 was not
  falsely relabeled as freshly compiled.

## Registry and risk evidence

- EA registry: `framework/registry/ea_id_registry.csv:4462` is active.
- Existing magic rows are active and unchanged:
  `framework/registry/magic_numbers.csv:17448-17450` = slots 0/1/2,
  magics 360070000/360070001/360070002.
- All three D1 backtest setfiles retain `RISK_FIXED=1000` and
  `RISK_PERCENT=0`; the focused test checks every declared `strategy_*` input
  is bound in every setfile.
- Source SHA-256 after repair:
  `874cccd1b60d70aeebb0f9d1407d9b1e63aee262de4b4fb7ce4ba81c67999448`.

## Validation

PASS:

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_36007_review_rework.py
....                                                                     [100%]
4 passed in 1.33s

python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_qm5_36007_review_rework.py -k "not qm5_411xx_sources_have_no_unbounded_numeric_buffers"
................................                                         [100%]
32 passed, 1 deselected in 115.64s

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_36007_nnfx-vidya-trix-fisher-momentum
files_scanned=1 failures=[] warnings=[] D10_indicator_buffer_bounds.failures=0

python framework/scripts/skill_build_ea_guard.py --ea-id 36007 --ea-label QM5_36007_nnfx-vidya-trix-fisher-momentum
status=ok ea_registry_row=true magic_registry_rows=true ea_dir_exists=true

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_36007_nnfx-vidya-trix-fisher-momentum
Summary: 1 PASS, 0 FAIL (of 1)

python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 framework/EAs/QM5_36007_nnfx-vidya-trix-fisher-momentum
files_checked=4 findings=[] verdict=PASS
```

Full hardening-module run (recorded, out-of-scope pre-existing failure):

```text
python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_qm5_36007_review_rework.py
1 failed, 32 passed in 124.93s
Failure: test_qm5_411xx_sources_have_no_unbounded_numeric_buffers
First unrelated source: QM5_41134_wti-mdaily-iqrmean-mom.mq5:599
```

Compile/build-check interlock (not bypassed):

```text
powershell.exe ... framework/scripts/compile_one.ps1 -EALabel QM5_36007_nnfx-vidya-trix-fisher-momentum -Strict
LIVE_FACTORY_AD_HOC_COMPILE_REFUSED: terminal64 processes are alive
compile log: C:/QM/worktrees/rework-slot-1/framework/build/compile/20260824_103623/QM5_36007_nnfx-vidya-trix-fisher-momentum.compile.log
final-source compile log: C:/QM/worktrees/rework-slot-1/framework/build/compile/20260824_105452/QM5_36007_nnfx-vidya-trix-fisher-momentum.compile.log
final-source compile summary: D:/QM/reports/compile/20260824_105452/summary.csv

powershell.exe ... framework/scripts/build_check.ps1 -RepoRoot C:/QM/worktrees/rework-slot-1 -EALabel QM5_36007_nnfx-vidya-trix-fisher-momentum -SkipCompile
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED: terminal64 processes are alive
```

A redundant post-follow-up run of the same 32-test selected suite was stopped
after several minutes of machine saturation without additional output. The
final-source focused suite and direct analyzer were rerun separately and both
completed PASS; the earlier completed 32-pass selected-suite result remains
shown above.

The repository guard requires a governed compile work item while factory
terminals are live. This ticket explicitly forbids enqueueing backtests/work,
stopping the factory, or bypassing that guard. Therefore no fresh EX5 is
claimed. The unchanged pre-existing EX5 SHA-256 is
`1697c53bb1be220da295e39a8854095e0cd3128b9dcd3cf6402327d2e0e9464a`.

## Rollback

Revert the single `rework(36007)` ticket commit. This restores the prior MQ5
and SPEC and removes the focused test/evidence file. No registry, DB verdict,
queue, factory, terminal, or live-state rollback is required.

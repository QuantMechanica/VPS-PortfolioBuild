# QM5_1252 strict rebuild and append-only Q02 recovery handoff

Date: 2026-08-29

Observed at: `2026-08-29T08:47:35Z`

Branch: `agents/board-advisor`

Farm task: `25d7265a-332b-4d4c-8c5e-6518c7caa52a`

EA: `QM5_1252_carver-handcraft-ens`

Outcome: **SOURCE REPAIR VERIFIED; GOVERNED COMPILE STILL PENDING; NO Q02
SUCCESSOR SEEDED WITHOUT A CURRENT COMPILE RECEIPT**

## Current rebuild boundary

The task-scoped source repair remains committed in `37693691f`. Its current
MQ5 SHA-256 is
`cf41554ab02caa59610e3379bd6e9ca49774ee5727ea68697a9cbd331a641afd`.
The exact governed compile work item is
`62044bf3-dcf2-4337-a5f9-7196eb4a2efa`.

`farmctl compile-status QM5_1252_carver-handcraft-ens` reports one pending
compile and zero active, compiled, or failed compile rows. The row has no
verdict, build-check result, evidence path, or EX5 receipt. The EX5 currently
on disk is therefore still the historical binary with SHA-256
`89e4ba8ccb83755624f0d7a5dc998207ffaaea5f0769f0f619ab191cb23b69c9`.
It was not treated or relabeled as a build of the repaired source.

## Append-only Q02 state

The two task-selected predecessors remain immutable terminal infrastructure
failures:

| Symbol | Source work item | State |
| --- | --- | --- |
| EURUSD.DWX | `edfcba29-ad06-4b74-907f-38eba94d2610` | `failed / INFRA_FAIL` |
| GBPUSD.DWX | `8cc9c3c2-1ecd-42f6-8b40-b292b782bf88` | `failed / INFRA_FAIL` |

No post-repair Q02 successor exists. `seed-fresh-q02` requires the expected
current EX5 SHA-256, so invoking it with the historical binary would violate
the strict rebuild binding. The predecessors were not edited and no pipeline
or gate verdict was inferred from them.

## Focused verification

- `validate_build_guardrails.py` passed the EA package: 25 files and zero
  findings; the maximum configured news staleness remains 336 hours.
- All 12 backtest setfiles retain `RISK_FIXED=1000` and `RISK_PERCENT=0`.
- `test_compile_work_items.py`: 30 passed.
- The EA directory is clean relative to commit `37693691f`.
- Current MQ5 and historical EX5 hashes were re-read from the canonical
  checkout and remain distinct as recorded above.

No terminal, tester, MetaEditor, T_Live, or AutoTrading process was started or
changed. No active T1-T10 run was interrupted, and the compile queue was not
reordered or bypassed.

## Deterministic continuation

Allow the canonical compile worker to claim
`62044bf3-dcf2-4337-a5f9-7196eb4a2efa`. Only after it records `COMPILE_OK`
with a receipt whose source and EX5 hashes match the repaired source may an
append-only EURUSD.DWX Q02 canary be seeded from the preserved predecessor.
GBPUSD.DWX remains the second bounded lineage after the canary establishes
that the infrastructure failure is cleared.

# QM5_11173 EURUSD Q02 stale-EX5 repair and requeue

## Selection

- Live farm DB: `D:/QM/strategy_farm/state/farm_state.sqlite`.
- EA: `QM5_11173_weiss-dmi-adx`.
- Diverse carrier: `EURUSD.DWX`, D1.
- Source work item: `898de640-ae88-432f-8c99-2fb08c199ffa`.
- The EA had no pending or active Q02 sibling when this repair was claimed.
- The source row and several earlier retries ended `INFRA_FAIL` with
  `summary_missing_retries_exhausted`; they were not strategy verdicts.

The final source log shows that the worker resolved T7, deployed the EA,
resolved the RISK_FIXED setfile, wrote the tester INI, and started the terminal.
The terminal then exited without a latched report or structured logger file.
That makes a refreshed binary/package the bounded repository-side repair; no
strategy rule or parameter was changed.

## Repair

- Recompiled the unchanged MQ5 source with strict build validation.
- Compile: PASS, zero errors, zero warnings.
- Build check: PASS, zero failures, zero warnings.
- Compile log:
  `framework/build/compile/20260723_230216/QM5_11173_weiss-dmi-adx.compile.log`.
- Refreshed package commit: `9c7b32b1c178a0495da05fecede070daad18925a`.
- Refreshed the canonical `.ex5` and build hashes in the EA's existing
  setfiles.
- Confirmed the requeued EURUSD ablation setfile retains
  `RISK_FIXED=1000` and `RISK_PERCENT=0`.
- No manual MT5 run was launched; backtest CPU remains owned by the paced
  worker fleet.

## Coordination and Q02 handoff

- Pre-write DB backup:
  `D:/QM/strategy_farm/state/backups/farm_state_before_qm5_11173_stale_ex5_requeue_20260724_010320.sqlite`.
- New Q02 work item: `db5fae4e-615b-4c78-96fc-231f66e368f2`.
- Initial state: `pending`, unclaimed, attempt 0.
- Symbol/timeframe: `EURUSD.DWX` / D1.
- Window: 2020-2022, Model 4.
- The enqueue and coordination event were committed in one immediate SQLite
  transaction after rechecking that no open Q02 row existed for this EA.

No T_Live terminal, AutoTrading setting, deploy manifest, portfolio gate, or
T_Live manifest was touched.

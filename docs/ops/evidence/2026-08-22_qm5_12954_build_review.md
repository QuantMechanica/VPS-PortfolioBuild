# QM5_12954 Pring Coppock H4 build review handoff

Date: 2026-08-22

Task: `14e4021c-7420-4072-ba54-028dd4d0883a`

Branch: `agents/board-advisor`

## Scope

Replace the inert `QM5_12954_pring-coppock-h4-variant` skeleton with the
OWNER-authorized G0 strategy-card implementation and submit it for independent
review. This is build-only evidence; it asserts no pipeline or live verdict.

## Deterministic preflight

- Approved card:
  `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12954_pring-coppock-h4-variant.md`
- Card identity: `ea_id=QM5_12954`, `slug=pring-coppock-h4-variant`,
  `g0_status=APPROVED`, R1-R4 all `PASS`.
- EA registry: active row for `12954,pring-coppock-h4-variant`.
- Magic registry: active slots 0-12 for the exact 13-symbol build universe.
- Folder, source filename, card slug, and registry slug agree.

## Delivered build surface

- Card-faithful closed-H4 Coppock calculation: ROC(11) + ROC(14), WMA(10).
- Bidirectional closed-bar zero-cross entries.
- Initial 2.5 ATR(20) stop with the card's 4.0 ATR cap.
- Break-even move after 2.0 ATR favorable excursion.
- Opposite-cross and 200-H4-bar exits.
- 35-bar initialization warm-up and 0.25 ATR spread entry filter.
- Central V5 high-impact news control using the fail-closed 30/30 mode because
  the framework has no 15/15 enum.
- One-position-per-resolved-magic enforcement.
- Thirteen H4 backtest setfiles, one per governed magic slot.
- Strategy specification and focused static contract tests.

All backtest sets bind the current source SHA-256
`b85857056120d4dc504298e7bdb344e7ffb4ee32b3c9d8e6be353c449f4fec10`, use
`RISK_FIXED=1000`, `RISK_PERCENT=0`, and retain
`qm_news_stale_max_hours=336`.

## Focused verification

1. `python -m pytest tools/strategy_farm/tests/test_qm5_12954_pring_coppock_static.py -q`
   returned `4 passed`.
2. `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_12954_pring-coppock-h4-variant`
   returned `PASS`, 14 files checked, zero findings.
3. Governed strict compile was attempted with
   `framework/scripts/compile_one.ps1 -EAPath ... -Strict`. It failed closed
   before compilation with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because
   `terminal64` processes are alive. No terminal was interrupted and no manual
   terminal was started.
4. `farmctl.py enqueue-compile QM5_12954_pring-coppock-h4-variant` was
   idempotent: existing governed compile work item
   `a44c3c83-7840-4b28-98eb-6d5988a3a82d` remains pending. No duplicate work
   item was created.

## Review boundary

Source, specification, sets, and focused tests are ready for independent code
review. The build does **not** yet have a compile-PASS or `.ex5`; the governed
compile item must finish after the active-terminal safety condition clears.
Nothing in this handoff authorizes a pipeline phase, T_Live mutation, or live
use.

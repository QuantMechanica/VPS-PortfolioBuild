# rework-39003 review-fix evidence

- Router task: `ec8363cd-b870-450d-9ce9-4ec345ff87db`
- EA: `QM5_39003_forexfactory-james16-price-action-ppz`
- Review work order: `docs/ops/evidence/ec8363cd_qm5_39003_gemini_build_codex_review_2026-08-18.md`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_39003_forexfactory-james16-price-action-ppz.md`
- Worktree/branch: `C:/QM/worktrees/rework-slot-4`, `rework-slot-4`
- Generated: 2026-08-24
- Scope: one EA source, SPEC, three backtest setfiles, one focused pytest module, and this evidence file. No registry allocation changed.

## What changed

1. PPZ entry and target selection now use one bounded D1 `CopyRates` buffer
   with an `ArraySize` guard and confirmed strength-two swing pivots. Longs
   select the nearest confirmed resistance above entry and shorts select the
   nearest confirmed support below entry; a setup is rejected when that PPZ
   does not offer the card's minimum 2.5R (`framework/EAs/QM5_39003_forexfactory-james16-price-action-ppz/QM5_39003_forexfactory-james16-price-action-ppz.mq5:138`, `:188`, `:374`, `:408`, `:428`). Fixed `QM_TakeRR` targets are absent.
2. Open positions now trail the most recent confirmed D1 swing low/high. The
   stop uses the same two-pip buffer and can only improve; management runs
   before all entry-only strategy/news filters (`.mq5:247`, `:448`, `:467`,
   `:485`, `:596`).
3. The stop buffer is passed directly to
   `QM_StopRulesPipsToPriceDistance`; the rejected `* 10` conversion is absent
   (`.mq5:399`, `:467`).
4. The mandatory no-trade contract is executable: max one owned position,
   GMT rollover blackout 23:55-00:05 via `QM_BrokerToUTC`, 1.8-ATR spread
   ceiling, and cached account-level daily realized-loss halt at 2.0%
   (`.mq5:105-136`, `:280-321`). The history scan refreshes only on broker-day
   or balance change.
5. `OnInit` overrides the framework's generic kill-switch values with the
   card's 2.5% daily hard stop and 5.0% portfolio/total-drawdown channel. A
   session-equity fallback makes the 5% total stop deterministic in the tester
   (`.mq5:130-136`, `:509-515`, `:529-587`).
6. Framework conformance remains explicit: `QM_Common` umbrella include,
   `QM_FrameworkDeclareExecutionContract(PERIOD_D1, ...)`, magic through
   `QM_FrameworkMagic()`, `RISK_FIXED=1000` / `RISK_PERCENT=0` backtest
   defaults, and the direct MAE hook before the kill switch (`.mq5:5`,
   `:38-49`, `:565-579`, `:601`). No ML or raw indicator-handle calls exist.
7. All three backtest setfiles removed obsolete parameter names and now seal
   every declared strategy input. The complete strategy block is lines 24-37
   in each setfile; slot/risk bindings remain EURUSD=0, GBPUSD=1, XAUUSD=2 and
   `RISK_FIXED=1000` / `RISK_PERCENT=0`.
8. `SPEC.md` now documents the exact confirmed-swing PPZ target, minimum-RR
   admission rule, dynamic trail, and hard-loss wiring
   (`framework/EAs/QM5_39003_forexfactory-james16-price-action-ppz/SPEC.md:13-15`, `:31-36`, `:108`).
9. The focused static regression module covers the card mechanism, filters,
   loss limits, management reachability, every input use-site/set seal,
   framework/magic/MAE rules, forbidden calls, approved-card status, and exact
   registry slots (`tools/strategy_farm/tests/test_qm5_39003_rework_contract.py:51-180`).

## Preflight and registry evidence

- Approved card has `g0_status: APPROVED`, `ea_id: QM5_39003`, slug
  `forexfactory-james16-price-action-ppz`, and the exact three-symbol D1 scope.
- `framework/registry/ea_id_registry.csv:4482` is the active EA identity.
- `framework/registry/magic_numbers.csv:17513-17515` contains the unchanged
  active rows `390030000-390030002` for EURUSD/GBPUSD/XAUUSD slots 0-2.
- All three magic values are present in the generated resolver at
  `framework/include/QM/QM_MagicResolver.mqh:22`; no registry gap or resolver
  regeneration was required.
- Strategy-input source grep counts (declaration included):
  `InpPPZLookback=8`, `InpTrendEMA=4`, `strategy_atr_period=4`,
  `strategy_ppz_zone_atr_fraction=3`, `strategy_pinbar_wick_fraction=5`,
  `strategy_pinbar_body_fraction=4`, `strategy_spread_atr_multiplier=3`,
  `strategy_sl_buffer_pips=4`, `strategy_reward_risk=4`,
  `strategy_slippage_tolerance_ticks=3`, `strategy_max_open_positions=4`,
  `strategy_daily_entry_loss_limit_pct=4`,
  `strategy_daily_drawdown_stop_pct=4`, and
  `strategy_total_drawdown_stop_pct=5`. Every input has a non-declaration
  use-site and is sealed in all three sets.
- The card's legacy `InpRiskPercent` maps to the V5 framework input
  `RISK_PERCENT`; no duplicate strategy risk input was added. Backtest sets
  keep fixed risk, while any future reviewed live set must activate
  `RISK_PERCENT` and set `RISK_FIXED=0`.

## Tests and validation

Focused EA test:

```text
> python -m pytest tools/strategy_farm/tests/test_qm5_39003_rework_contract.py -q
......                                                                   [100%]
6 passed in 0.81s
```

Focused EA plus relevant D2/D3/D4/D5/D7/D10/D11/D17 hardening nodes and the
build-guardrail unit module:

```text
> python -m pytest <focused/relevant node selection> -q
...................................                                      [100%]
35 passed in 2.03s
```

Scoped mechanical hardening:

```text
> python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_39003_forexfactory-james16-price-action-ppz
files_scanned=1; failures=[]; warnings=[]
D2 loss limits: 2.0 / 2.5 / 5.0, failures=0
D3 pip conversion: failures=0
D4 management reachability: failures=0
D5 GMT conversion: converter_present=true, failures=0
D7 MAE hook: failures=0
D10 bounded buffers: failures=0
D11/D17 symbol registry/card scope: failures=0
```

Other scoped checks:

```text
validate_spec_doc.py: PASS (1 PASS, 0 FAIL)
validate_build_guardrails.py --max-news-stale-hours 336: PASS (4 files, no findings)
validate_symbol_scope.py --fail-on-leak: SINGLE_SYMBOL_OK, 0 violations
raw_mq5_quarantine.py check --purpose compile: RAW_MQ5_SOURCE_ALLOWED
git diff --check: PASS (line-ending notices only; no whitespace errors)
```

## Compile/build-check interlock and residual risk

The exact scoped strict build gate and its scoped `-SkipCompile` form were
attempted. Both stopped before validation/compilation because the live-factory
include-mirror guard observed active `terminal64` processes:

```text
> powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -Strict -EALabel QM5_39003_forexfactory-james16-price-action-ppz
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED

> powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -Strict -SkipCompile -EALabel QM5_39003_forexfactory-james16-price-action-ppz
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
```

The sanctioned compile orchestrator was also attempted without `--force`:

```text
> python tools/strategy_farm/compile_ea.py --ea-label QM5_39003_forexfactory-james16-price-action-ppz --json --fail-on-error
verdict=COMPILE_FAILED
reason=compile_one.ps1 reason_class=INCLUDE_MIRROR_REFUSED errors=-1 warnings=-1
```

A bounded 50-second read-only observation found 10,10,10,10,11,11 active
terminals and no safe zero-terminal window. No terminal/factory process was
stopped, no guard was bypassed, and no compile or backtest work item was
enqueued. The tracked EX5 therefore remains the pre-rework binary (SHA-256
`8880fb107e078c01067d22d948d6214ced662494d5911257ef8fbceda5e3e8c3`,
390624 bytes, mtime 2026-08-24T07:00:36Z), while the repaired MQ5 SHA-256 is
`41f2a880d4246f65b0cce92efbd7ad8e71f397e3a6cb0b77597bdfcf205bae47`.
The stale binary must not be treated as compile evidence. This is the sole
open validation risk for deterministic review.

## Boundaries and rollback

- No gate threshold/criterion, verdict row, backtest queue, factory state,
  AutoTrading state, registry, resolver, other EA, or `C:/QM/mt5/T_Live`
  content was changed. The only state mutation before resubmission was the
  explicitly requested router task transition to `IN_PROGRESS`.
- Rollback: revert the ticket commit. This restores the EA source, SPEC, and
  three setfiles and removes the focused test/evidence file. The EX5 was not
  regenerated, so binary rollback is not required.

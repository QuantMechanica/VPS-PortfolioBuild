# rework-9716 — QM5_9716 review repair evidence

Date: 2026-08-24 (ticket-required evidence filename date: 2026-08-23)

- Router task: `a06e3ace-6c6a-437e-9a6e-da3e37700a59`
- Source build task: `d573fb90-49fa-4f73-87a5-4f01d0254002`
- EA: `QM5_9716_bandy-trend-stretch-ratio-mr-index`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9716_bandy-trend-stretch-ratio-mr-index.md`
- Review artifacts: `docs/ops/evidence/2026-08-24_review_9716.md` and
  `docs/ops/evidence/2026-08-23_qm5_9716_gemini_code_review.md`

## What changed

1. Card universe is now enforced by the delivered artifacts: only NDX.DWX,
   SP500.DWX, and WS30.DWX D1 backtest setfiles remain. Their existing magic
   offsets are 1, 2, and 4, and each has `RISK_FIXED=1000`, `RISK_PERCENT=0`,
   and all nine strategy inputs at setfile lines 17-32. The other ten setfiles
   were removed; no registry allocation was modified.
2. Existing-position management and the TSR exit now execute before every
   entry-admission guard. MAE tracking is at MQ5 line 207, the seven-day time
   stop is reached at line 215, the TSR exit at line 217, and kill-switch,
   news, warm-up/spread, and entry gates follow at lines 233-258:
   `framework/EAs/QM5_9716_bandy-trend-stretch-ratio-mr-index/QM5_9716_bandy-trend-stretch-ratio-mr-index.mq5`.
3. `OnInit` declares the D1 execution contract and framework Friday-close
   policy at MQ5 line 195. Closed-price access uses the pooled framework
   indicator path at line 56; the remaining bounded bar-count/time lookups are
   explicitly performance-allowed at lines 75, 100, 151, and 161.
4. The SPEC now states the card-faithful formula, nine parameters, exact
   three-symbol universe, D1-only timeframe, execution contract, and risk
   model at `framework/EAs/QM5_9716_bandy-trend-stretch-ratio-mr-index/SPEC.md:9`,
   `:21`, `:38`, `:50`, and `:87`.
5. The producer artifact is schema-complete and truthful about the compile
   interlock, deferred smoke, three delivered symbols, exact hashes, and open
   review points at
   `docs/ops/evidence/2026-08-23_QM5_9716_bandy-trend-stretch-ratio-mr-index_build_result.json:1`.
   Matching durable copies are at EA `build_result.json:1` and
   `build_identity.json:1`.
6. Six focused regressions cover the approved-card/SPEC contract, all nine
   input use-sites, exit reachability, framework/MAE/risk/magic/performance
   conformity, exact setfile universe, and producer hash binding:
   `tools/strategy_farm/tests/test_qm5_9716_review_rework.py:56`, `:78`, `:95`,
   `:113`, `:131`, and `:163`.

The approved card is OWNER-authorized (`g0_status: APPROVED` at card line 15),
specifies the mechanical TSR rules at lines 34-53, D1-only operation at line
63, and the exact three-symbol universe at line 82. The existing registry
mappings used by this delivery are `framework/registry/magic_numbers.csv:16904`,
`:16905`, and `:16907`.

No gate threshold/criterion, verdict row, backtest queue, factory state,
AutoTrading setting, T_Live path, EA-ID registry row, magic allocation, or
resolver file was changed. No backtest or smoke test was run or enqueued.

## Compile evidence

The required scoped command was run:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 \
  -EALabel QM5_9716_bandy-trend-stretch-ratio-mr-index
exit=1
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
retry_attempted=false
detail=terminal64 processes are alive; ad-hoc compile/build_check is refused
```

The factory was not toggled and the suggested governed compile was not
enqueued because this ticket forbids queue mutation. The exact repaired source
was instead copied into an isolated portable MetaEditor tree containing the
current repository framework includes. Its source SHA-256 was verified equal
before the compiled EX5 was copied back.

```text
compile log: D:/QM/strategy_farm/temp/rework-9716-slot12-20260824T135416Z/compile.log
Result: 0 errors, 0 warnings, 12317 ms elapsed, cpu='X64 Regular'
source SHA-256: 25d2c8a8494d33e36651f971f7c9f94fb9573742ede174b6093194034d236399
EX5 SHA-256:   de9928aee42d3bd26a77037c9d50cc23a5b62102797a6fe67d1ea1ff79e0c528
EX5 bytes:     397110
log SHA-256:   367e0d4e5d1659ed860a04cc2f09eba8430192ff9dd81ff01c25b4c082776083
```

`build_result.json:38` and `build_identity.json:6` therefore retain
`build_check_passed=false`; neither artifact misrepresents the interlocked
full-script result. Compile success is separately bound to the exact hashes.

## Test and validation output

Focused EA regressions after the final SPEC/hash update:

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_9716_review_rework.py
......                                                                   [100%]
6 passed in 0.80s
```

Touched hardening and symbol-scope module tests:

```text
python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py \
  tools/strategy_farm/tests/test_validate_symbol_scope.py
.................................                                        [100%]
33 passed in 526.99s (0:08:46)
```

Scoped validators:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . \
  --ea-label QM5_9716_bandy-trend-stretch-ratio-mr-index
PASS — 1 file, 0 failures, 0 warnings

python tools/strategy_farm/validate_build_guardrails.py \
  framework/EAs/QM5_9716_bandy-trend-stretch-ratio-mr-index
PASS — 4 files, 0 findings

python tools/strategy_farm/validate_symbol_scope.py \
  --ea-label QM5_9716_bandy-trend-stretch-ratio-mr-index --json --fail-on-leak
SINGLE_SYMBOL_OK — 0 violations

python framework/scripts/validate_spec_doc.py \
  framework/EAs/QM5_9716_bandy-trend-stretch-ratio-mr-index
PASS — 1 PASS, 0 FAIL

python framework/scripts/skill_build_ea_guard.py --ea-id 9716 \
  --ea-label QM5_9716_bandy-trend-stretch-ratio-mr-index
status=ok; ea_registry_row=true; magic_registry_rows=true; ea_dir_exists=true

git diff --check
PASS — no whitespace errors
```

## Risks and open review points

- Ten pre-existing active magic allocations remain outside the approved card
  universe. The ticket explicitly forbids changing existing allocations; no
  delivered setfile or EA code consumes them. The focused regression enforces
  the three-symbol artifact universe independently because the current
  hardening card parser reports an empty `card_target_symbols` list for this
  prose-form card.
- Full `build_check` cannot be claimed PASS while the live-factory interlock is
  active. Review can accept the isolated exact-source 0/0 compile plus scoped
  gates, or request a later governed COMPILE_EA/OFF-window run.
- Smoke remains `deferred_p2_smoke`; running or enqueueing it was explicitly
  outside this build-only ticket.

## Rollback

Revert the ticket commit with `git revert <rework-9716-commit>`. This restores
the prior MQ5, EX5, SPEC, producer JSON, and thirteen-setfile delivery and
removes the focused test plus EA-local build identity/result files. No database,
registry, queue, factory, terminal, or live rollback is required because none
of those surfaces was changed beyond the authorized router task transitions.

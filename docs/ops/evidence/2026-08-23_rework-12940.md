# rework-12940 — QM5_12940 review-fix evidence

Date executed: 2026-08-24 (Europe/Berlin)

Router task: `395e4a56-b7a5-4c07-9187-253b4d2d6dd0`

EA: `QM5_12940_bressert-cycle-trigger-line-h4-card`

Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12940_bressert-cycle-trigger-line-h4-card.md` (`g0_status: APPROVED`)

## Status

Source rework and focused regression tests are complete. Static build gates pass. A fresh EX5 compile and smoke are **not claimed**: both scoped `compile_one.ps1` and `build_check.ps1` attempts were fail-closed by `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` while terminal processes were active. No factory toggle, terminal termination, compile enqueue, backtest enqueue, or T_Live mutation was performed.

## Review findings addressed

1. Cooldown is no longer reset inside `Strategy_EntrySignal`. Directional cooldown is reset only inside the success branch of `QM_TM_OpenPosition`, so rejected orders do not consume the signal's cooldown (`framework/EAs/QM5_12940_bressert-cycle-trigger-line-h4-card/QM5_12940_bressert-cycle-trigger-line-h4-card.mq5:370`, `:735`). On initialization, accepted entry deals rebuild both directional cooldown counters (`:117`, `:125`, `:669`).
2. T1 is activated only when `QM_TM_PartialClose` returns true. Invalid normalized half-volume or a rejected partial leaves `t1_hit=false`, so the EA retries and does not enable trailing prematurely (`:573`, `:578`, `:604`, `:609`).
3. The signal-time T1 is persisted in the accepted order comment (`:94`, `:381`, `:391`). Restart reconstruction uses `POSITION_IDENTIFIER`, position/deal/order comments, opening volume, and outbound deal history; missing durable evidence suppresses partial/trailing fail-closed (`:401-523`).
4. DSS arrays now reject configured lengths beyond their fixed capacity and guard every configured index with `ArraySize` before access (`:192-260`).
5. Position management now executes before entry-only no-trade/news guards; the MAE hook remains first in `OnTick` (`:680-726`).

The earlier review repairs also remain present: one cached new-bar gate (`:689`), explicit request initialization (`:370-374`, `:730-732`), configured trigger-period calculation (`:261-270`, `:292-293`), and card-faithful 50% T1 plus post-T1 ATR trail (`:573-629`).

## Input wiring audit

Every declared `strategy_*` input was grepped individually and has at least one executable use-site. The executable-use regression is `tools/strategy_farm/tests/test_qm5_12940_rework.py:47`.

The same test file covers accepted-entry cooldown (`:57`), partial-close success gating (`:75`), restart-safe T1 reconstruction (`:87`), indicator bounds (`:107`), framework/risk/magic/MAE ordering (`:122`), and registry/setfile invariants (`:143`).

Registry preflight remained read-only and unchanged:

- `framework/registry/ea_id_registry.csv:3828` — active `12940` slug row.
- `framework/registry/magic_numbers.csv:16369-16381` — 13 active slots, magics `129400000..129400012`.
- Existing resolver contains the same 13 magics; no registry gap was found, so no registry or resolver file was modified.
- All 13 backtest setfiles retain `RISK_FIXED=1000` and `RISK_PERCENT=0`.

## Validation evidence

### PASS

- `python -m pytest tools/strategy_farm/tests/test_qm5_12940_rework.py -q`
  - `7 passed in 0.75s`
- Ticket-scoped suite (`test_qm5_12940_rework.py`, `test_build_guardrails.py`, `test_validate_symbol_scope.py`)
  - `30 passed in 1.58s`
- Build-hardening module excluding its unrelated fleet-wide QM5_411xx census assertion:
  - `28 passed, 1 deselected in 341.31s`
- Full relevant combined suite:
  - `58 passed, 1 failed in 166.21s`
  - The sole failure is pre-existing/out of scope: `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven findings in `QM5_41134` through `QM5_41139`; it does not reference QM5_12940 or a touched file.
- `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_12940_bressert-cycle-trigger-line-h4-card`
  - `files_scanned=1`, `failures=[]`, `warnings=[]`; D2-D18 automated checks report zero failures.
- `python framework/scripts/skill_build_ea_guard.py --ea-id 12940 --ea-label QM5_12940_bressert-cycle-trigger-line-h4-card`
  - `status=ok`; EA registry, magic rows, and EA directory all true.
- `python framework/scripts/validate_spec_doc.py framework/EAs/QM5_12940_bressert-cycle-trigger-line-h4-card`
  - `1 PASS, 0 FAIL`.
- `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_12940_bressert-cycle-trigger-line-h4-card`
  - `PASS`; 14 files checked, zero findings, news ceiling 336 hours.
- `python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_12940_bressert-cycle-trigger-line-h4-card --fail-on-leak --json`
  - `SINGLE_SYMBOL_OK`; zero violations.
- `git diff --check`
  - No whitespace errors (Git emitted only its normal LF→CRLF working-copy notice).

### Compile/build-check interlock (not a source PASS)

- Scoped strict command: `framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_12940_bressert-cycle-trigger-line-h4-card/QM5_12940_bressert-cycle-trigger-line-h4-card.mq5 -Strict`
  - Result: `FAIL`, `reason_class=INCLUDE_MIRROR_REFUSED`, `failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`.
  - Durable summary: `D:/QM/reports/compile/20260824_075231/summary.csv`.
- Scoped command: `framework/scripts/build_check.ps1 -EALabel QM5_12940_bressert-cycle-trigger-line-h4-card -SkipCompile`
  - Result: fail-closed at the same live-factory compile guard before gates executed.

The tracked EX5 therefore remains the prior binary and is deliberately not represented as fresh:

- MQ5 SHA-256: `fd7170ad4622bcc9e82210c82f3b966dcdadf4edf9bbf92d256e67b1ab2df224`
- Existing/stale EX5 SHA-256: `a0e09832809f652fd62fb674208bf4b49f6775a173fa9a655e17b1f1ee292c28`
- Regression test SHA-256: `765d898b67657a2f0b844dc97e89d6e8d981f5e599570557de9cedb0c3f26662`

Smoke status: `NOT_RUN`; no deferred-smoke waiver is asserted. This complies with the ticket's prohibition on backtest enqueue and avoids repeating the rejected artifact's unsupported `deferred_p2_smoke` claim.

## Files changed

- `framework/EAs/QM5_12940_bressert-cycle-trigger-line-h4-card/QM5_12940_bressert-cycle-trigger-line-h4-card.mq5`
- `tools/strategy_farm/tests/test_qm5_12940_rework.py`
- `docs/ops/evidence/2026-08-23_rework-12940.md`

No registry, setfile, framework include, verdict row, queue, factory, or live file was changed.

## Risks and open review items

- A reviewer must treat the existing EX5 as stale and require the governed compiler to rebuild this MQ5 when a quiescent compile slot is available.
- No tester smoke evidence exists for the repaired source. Static review and unit tests do not substitute for a compile or tester run.
- The durable T1 reconstruction depends on the broker/tester preserving the compact entry comment in at least one of the live position, opening deal, or accepted order. If all three omit it, partial/trailing intentionally remain fail-closed while the server-side initial SL remains active.

## Immediate second-review result and compile authority block

The first resubmission at 2026-08-24 10:08 CEST was automatically returned to `RECYCLE` with this narrower verdict: the source is card-faithful and all prior defects are fixed, but the EX5 is stale and must be rebuilt in an OFF window / `COMPILE_EA` queue.

The requested governed path was then checked without bypasses:

- `FACTORY_OFF_FLAG=False`; T1 through T10 all had active `terminal64.exe` processes, so no OFF-window compile was available.
- The read-only `compile_work_items.classify_candidate` result for the exact worktree source SHA reports `eligible=false`, reasons `EX5_ALREADY_PRESENT` and `BOUND_SETFILE_HASH_EXISTS`, and both `force_rebuild_authorized=false` and `source_repair_authorized=false`.
- `python tools/strategy_farm/farmctl.py enqueue-compile QM5_12940_bressert-cycle-trigger-line-h4-card` refused before mutation because state-mutating `farmctl` commands must run from `C:/QM/repo`, while this ticket explicitly scopes repository work to this worktree.

No `QM_ALLOW_NONCANONICAL` override was set, no compile allowlist was edited, and no stale canonical source was enqueued. Completion of the binary step now requires an authorized canonical integrator to make commit `9dc2c7a60ed7f007f945355c749ea2984411093f` visible in `C:/QM/repo` and either provide an OFF window or grant the exact governed source-repair compile authority for QM5_12940.

## Rollback

Revert the single commit containing this evidence with `git revert <that-commit>`. This restores the prior MQ5 and removes the focused regression/evidence files. The existing EX5 was not changed, and no runtime/database rollback is required.

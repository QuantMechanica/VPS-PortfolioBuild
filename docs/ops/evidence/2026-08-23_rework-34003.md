# rework-34003 review-fix evidence

**Router task:** `91dfb3c7-e13c-4226-bdb5-c70209c27405`  
**EA:** `QM5_34003_triple-timeframe-williams-r-champion`  
**Worktree:** `C:/QM/worktrees/rework-slot-4`  
**Disposition:** source/spec/setfile repair ready for deterministic code review;
compiled binary refresh remains fail-closed behind the active-factory compile
interlock.

## Work order read

- Router payload identified EA 34003 and source evidence
  `docs/ops/evidence/d5a96505_qm5_34003_build_ea_result_2026-08-17.md`.
- Review findings were read from
  `docs/ops/evidence/1ff032e2_qm5_34003_gemini_codex_review_2026-08-23.json:16`.
- The task was claimed through `agent_router.py update-task` as `IN_PROGRESS`
  with verdict `rework burn-window slot 4`. Initial attempts were refused by a
  live SQLite writer lock; the successful documented router update returned
  `"updated": true` and `"state": "IN_PROGRESS"`.
- Approved-card preflight passed: `g0_status: APPROVED`; the EA registry row is
  `framework/registry/ea_id_registry.csv:4442`; existing active magic rows are
  `framework/registry/magic_numbers.csv:17376`, `:17377`, and `:17378`.
  No registry or resolver file was modified.

## What changed

1. Restored the V5 corset and risk-mode defaults:
   `framework/EAs/QM5_34003_triple-timeframe-williams-r-champion/QM5_34003_triple-timeframe-williams-r-champion.mq5:6`
   now has only the `QM_Common.mqh` umbrella include, and `:18` defaults
   `RISK_PERCENT=0` while `RISK_FIXED=1000` remains the backtest mode.
2. Added and wired the card's one-position, three-tick slippage, 2.0% realized
   daily-loss halt, 2.5% daily hard stop, and 5.0% total drawdown inputs at
   `...champion.mq5:48`. Input validation is fail-closed at `:67`; tick-to-point
   deviation conversion is at `:90`; realized-loss reconstruction is at `:100`;
   and the total-equity check is at `:112`.
3. Corrected the 23:55-00:05 window from raw broker time to framework UTC at
   `...champion.mq5:126`. The no-trade hook applies realized and total-loss
   entry halts at `:131` and the max-position input at `:162`.
4. Bound slippage through `QM_EntryConfigure` and card hard stops through
   `QM_KillSwitchInit` at `...champion.mq5:241` and `:249`. Magic remains
   framework/MagicResolver-owned through `QM_FrameworkMagic()`.
5. Reordered `OnTick` so MAE, Friday close, management, and hard exits execute
   before entry-only filters (`...champion.mq5:268`, `:277`, `:279`). The entry
   request is zero-initialized at `:304`; the correctly spelled MQL5 trade
   callback and framework forwarding are at `:313` and `:315`.
6. Sealed every strategy input into all three fixed-risk backtest presets.
   Representative lines are
   `framework/EAs/QM5_34003_triple-timeframe-williams-r-champion/sets/QM5_34003_triple-timeframe-williams-r-champion_EURUSD.DWX_M15_backtest.set:33`;
   the GBPUSD and USDCHF files have the same block. All 13 mandatory set headers
   are asserted by the regression test. `build_hash` is intentionally `pending`
   until the governed compiler can mint the replacement EX5.
7. Repaired the corrupted SPEC and documented the exact strategy, parameters,
   symbols, timeframe, expected behavior, source, and risk contract at
   `framework/EAs/QM5_34003_triple-timeframe-williams-r-champion/SPEC.md:8`.
8. Added six EA-specific static regression tests beginning at
   `tools/strategy_farm/tests/test_qm5_34003_rework_contract.py:60`, including
   per-input executable-use/setfile coverage at `:128`, framework/callback
   checks at `:140`, mandatory set headers at `:172`, registry identity, and an
   in-process semantic-hardening assertion.

No raw series/indicator-buffer calls or ML dependencies were introduced.

## Validation evidence

### EA-specific tests

Command:

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_34003_rework_contract.py
```

Output:

```text
...... [100%]
6 passed in 0.82s
PYTEST_EXIT=0
```

### Touched validation suites

Command:

```text
python -m pytest -q tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_gen_setfile.py tools/strategy_farm/tests/test_setfile_canonicalization.py
```

Output:

```text
........................ [100%]
24 passed in 12.28s
PYTEST_EXIT=0
```

### EA semantic hardening

Command:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_34003_triple-timeframe-williams-r-champion
```

Result: `files_scanned=1`, `failures=[]`, D2 loss-limit failures `0`, D5 UTC
window failures `0`, D7 MAE failures `0`, D9 request-init failures `0`, and all
three registry/setfile symbols exact.

### SPEC and directory guardrails

```text
python framework/scripts/validate_spec_doc.py framework/EAs/QM5_34003_triple-timeframe-williams-r-champion
PASS  QM5_34003_triple-timeframe-williams-r-champion
Summary: 1 PASS, 0 FAIL

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_34003_triple-timeframe-williams-r-champion
verdict: PASS; files_checked: 4; findings: []
```

### Scoped build check / compile interlock

Command (scoped exactly to this EA):

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_34003_triple-timeframe-williams-r-champion
```

Result:

```text
LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
detail: terminal64 processes are alive; ad-hoc compile/build_check is refused
BUILD_CHECK_EXIT=1
```

No retry, compile enqueue, factory toggle, or safety bypass was attempted. The
ticket explicitly forbids enqueueing backtests and toggling the factory; the
compile guard itself directs a governed enqueue, which was outside this ticket's
authority. Consequently the existing EX5 was not overwritten and the three
presets remain content-unbound (`build_hash: pending`) for the governed compile
worker.

### Repository-wide hardening regression note

The full `test_build_gate_hardening.py` suite completed with `28 passed, 1
failed in 465.54s`. The sole failure is an out-of-scope repository census finding
against `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599` (seven existing unbounded
buffer findings); it does not name EA 34003 or any file changed by this ticket.
EA 34003's direct hardening command and in-process regression assertion both
pass with zero failures.

## Rollback

After merge, revert the ticket commit with `git revert <rework-34003-commit>`.
Before merge, restore only the explicit paths listed in this evidence from the
parent commit. No DB, magic allocation, resolver, factory, queue, verdict-row,
or live-terminal rollback is required because none of those surfaces changed.

## Residual risk / handoff

- Source semantics and static gates are green, but a replacement EX5 and final
  build-hash binding could not be minted while the live-factory compile
  interlock was active. Deterministic review should verify the source change;
  the governed compile worker must compile and bind the presets before any
  pipeline execution.
- The 5% local total-drawdown baseline is initialized on EA attach. The approved
  card does not declare a durable restart-state contract (hardening D16 is
  explicitly non-mechanizable); the framework's external total-DD channel
  remains the persistent live authority.

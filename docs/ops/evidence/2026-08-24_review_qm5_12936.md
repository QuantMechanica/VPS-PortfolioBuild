# Review — QM5_12936 DeMark TD-Reverse-Differential H4 (review_ea 9de2c519)

- **Router task:** 9de2c519-2ca6-480b-a568-ac5f90611962 (review_ea, assigned codex, state REVIEW)
- **Build task:** c4f759a4-6be6-4465-aa92-a691fbbf5c68 (build_ea, source_agent gemini/agy)
- **Worker verdict:** `PASS: Rebuilt end-to-end (SPEC.md, .mq5, 13 setfiles, guardrails PASS). Awaiting Codex review.`
- **Reviewer:** Claude (review lane), independent verification against source + card
- **Card:** `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12936_demark-td-reverse-differential-h4.md`
- **Verdict: RECYCLE** — named actionable defects; back to build lane (recompile is ROT for the review lane).

## What passes
- Card mechanism is faithfully implemented: 5-rule buy/sell TD-Reverse-Differential checks (`.mq5:91-121`) match the card's inequality set exactly; D1 SMA(200) regime gate, ATR range gate, pullback-depth cap, ATR SL/TP, trailing/BE all present.
- Framework corset: `#include <QM/QM_Common.mqh>`, `QM_FrameworkInit` (`.mq5:334`), MagicResolver via `QM_FrameworkMagic()` (`.mq5:70`), magic rows correct `12936*10000+slot = 129360000+slot` (`framework/registry/magic_numbers.csv`), backtest risk mode correct (`RISK_FIXED=1000`, `RISK_PERCENT=0`, `.mq5:17-18`).
- Bounded buffers only (`QM_ReadBar`/`QM_ATR`/`QM_SMA`); no raw series, no ML, no invented commission/swap.
- No dead inputs: all 12 `strategy_*` inputs grepped to live use-sites.

## Defects (actionable)
1. **BLOCKER — stale EX5, no binary bound to reviewed source.** Commit `9ad5fd821` (2026-08-23 20:11) rewrote the `.mq5` (+292/-11) plus SPEC + 13 setfiles but did **not** update the `.ex5`. The `.ex5` (`b84346fc…`, dated 2026-08-21 18:12) was compiled from the earlier TODO-skeleton source; the only compile log is `framework/build/compile/20260821_161242/…` (Aug 21), which predates the Aug 23 source. `build_result.json` asserts `compile_succeeded=true`/`build_check_passed=true`, but that does not bind to the current `mq5_sha256=d3b92939…`. A Q02 run against this `.ex5` would execute obsolete code → void/invalid Q04 (known "stale .ex5 voids backtests" class). **Fix:** recompile current source in an OFF window / COMPILE_EA queue and commit the matching `.ex5`.
2. **DEFECT — news filter inert by default; card mandates the pause.** `.mq5:22-23` default `qm_news_temporal=QM_NEWS_TEMPORAL_OFF`, `qm_news_compliance=QM_NEWS_COMPLIANCE_NONE`, and `qm_news_mode_legacy=QM_NEWS_OFF` (`.mq5:26`). In `OnTick` (`.mq5:351-356`) both-OFF falls to the legacy path with mode OFF → `news_allows` always true. No setfile overrides (0/13 enable news). Same-family siblings QM5_1585 / QM5_1591 default `QM_NEWS_TEMPORAL_PRE30_POST30 + QM_NEWS_COMPLIANCE_DXZ`. Card §Additional filters: "News filter: standard QM news-calendar pause." **Fix:** default news to the fleet standard (or enable in the governed setfiles).
3. **LOGIC — `QM_IsNewBar` new-bar edge double-consumed.** `QM_IsNewBar` is stateful (`framework/include/QM/QM_Indicators.mqh:148-165`: first call per (symbol,tf,bar) returns true and advances `last_bar_time`; later same-bar calls return false). It is called inside `Strategy_ExitSignal` (`.mq5:310`, reached at `.mq5:363` when a position is open) and again for the entry gate (`.mq5:375`). On a bar where an opposite signal closes the position, the entry path is then deterministically suppressed (no same-bar re-entry). Limited blast radius (only affects the close-then-reenter bar), but a real deviation. **Fix:** compute the new-bar edge once per tick and share it.
4. **MINOR** — time-stop uses wall-clock seconds (`.mq5:303-307`: `TimeCurrent()-open_time >= bars*PeriodSeconds`), so weekends/closures age the timer; card specifies "8 H4 bars". Cooldown state is stamped on signal (`.mq5:185,211`) before `QM_TM_OpenPosition` (`.mq5:382`), so a rejected order still burns the 4-bar cooldown. `smoke_result=deferred_p2_smoke` with `blocked_reason=null` and no smoke summary under `D:/QM/reports/smoke` — deferral lacks the saturation-waiver evidence.

## Cross-check
Independent Codex review artifact `docs/ops/evidence/9de2c519_qm5_12936_gemini_codex_review_2026-08-23.json` reached FAIL on the same evidence; findings 1, 4, 6 above are the load-bearing ones and are reproduced here from source. Concur: not clean for Q02.

## Disposition
RECYCLE to build lane (Codex): recompile the current source and commit the bound `.ex5` (defect 1), wire news to the fleet standard (defect 2), de-duplicate the new-bar edge (defect 3), and address the minor items. Recompile-in-inventory is ROT for the review lane, so no source/binary change is made here.

# QM5_41335 FX fallback — Q05 enqueue

Recorded: 2026-09-10T12:47:02Z (14:47 Europe/Berlin)

Branch: `agents/board-advisor`

Observation head: `d8881996f1720415e2d1fce0511bd6482438ea46`

## Outcome

No new FX cointegration basket was created. The frozen sign-aware 66-pair
frontier remains fully mechanized: the latest durable reconciliation at
`artifacts/fx_cointegration_frontier_cpu_stop_20260910T0616Z_board_advisor.json`
finds all 16 approved next-best Edge Lab cointegration Cards already represented
by EA directories and terminal Q02 history. Creating another scan-derived Card,
EA, or Q02 row would therefore duplicate governed work.

The two requested anchors remain beyond Q02:

- `QM5_12532_AUDNZD_COINTEGRATION_D1`: Q02 `PASS`, Q04 `PASS`, then Q05
  `FAIL`.
- `QM5_12533_EURJPY_GBPJPY_COINTEGRATION_D1`: Q02 `PASS`, then Q04 `FAIL`.

The mission-authorized existing-forex fallback was advanced instead. Approved,
structural, low-frequency `QM5_41335_fx-usd-exhaustion-reversal-opt` on
`AUDUSD.DWX` D1 completed Q04 work item
`41b82b89-c50e-4eb0-8563-928c4d5d5d9a` with `PASS_LOWFREQ`. The authenticated
aggregate contains three complete OOS folds, 32 pooled trades, and pooled net
PF `1.033` over 2023–2025. Before mutation the EA had no Q05 row.

One exact Q05 successor was appended through the canonical cascade path:

```powershell
python tools/strategy_farm/farmctl.py --root D:/QM/strategy_farm enqueue-backtest --ea QM5_41335 --phase Q05 --from-work-item-id 41b82b89-c50e-4eb0-8563-928c4d5d5d9a
```

The command created work item `5b046970-f847-42c6-a3df-77b2d5ec11bc` for
`AUDUSD.DWX`, status `pending`, with no requeues or skips. A post-insert census
found exactly one QM5_41335 Q05 row. No dispatch tick was run; resident paced
workers own eventual claim and execution.

## Identity, risk, and PACER checks

The approved runtime Card is
`D:/QM/strategy_farm/artifacts/cards_approved/QM5_41335_fx-usd-exhaustion-reversal-opt.md`.
It records `g0_status: APPROVED`, R1–R4 `PASS`, D1 cadence, deterministic
closed-bar rules, and no ML. Current identities remain the same as the Q02/Q04
chain:

| Artifact | SHA-256 |
| --- | --- |
| MQ5 | `d7eec6373be1b7b865902cd8420b2a3fe305c7a7e026603e9baef9e32ee1e7bc` |
| EX5 | `f17045107715b1ee127011f14ee60ad36e6ad23e8dbfc372819a27d4bb596280` |
| AUDUSD D1 setfile | `6ed5b4fe7549362276fa8812bcbf9450f6aca2b3b03fd169336a89799874bd47` |
| Q04 aggregate | `d3b4674fce8420806fa9e45040c305a58757e3e280bc63a58c856ef034dc1c01` |

The backtest set contains `RISK_FIXED=1000`, `RISK_PERCENT=0`, and
`PORTFOLIO_WEIGHT=1`.

No MQ5 was generated or edited and no compile command was enqueued. The binding
framework-input audit was nevertheless run read-only against the absolute
source path before the queue mutation:

```powershell
python C:/QM/repo/tools/strategy_farm/audit_framework_input_pins.py --check-source "C:/QM/repo/framework/EAs/QM5_41335_fx-usd-exhaustion-reversal-opt/QM5_41335_fx-usd-exhaustion-reversal-opt.mq5"
```

It exited zero with `ok=true`, predicate `EA_FRAMEWORK_INPUT_PINNED`, and zero
findings.

Immediately before enqueue, five one-second whole-host CPU samples were
`91.517552%`, `77.024739%`, `74.820050%`, `71.501711%`, and `72.265322%`
(average `77.425875%`, maximum `91.517552%`). Both measures were below the 97%
hard ceiling. The canonical database had five active work items, below
`BUILD_BACKPRESSURE_ACTIVE_WORK_ITEM_LIMIT=7`.

## Safety

- No Card, EA source or binary, setfile, registry, magic row, compile work,
  verdict, hold, claim, terminal, or tester was changed.
- No portfolio-admission, portfolio KPI, Q08-contribution, portfolio-gate,
  deploy-manifest, `T_Live`, or AutoTrading surface was touched.
- Existing unrelated shared-worktree changes were left untouched and excluded
  from this commit.

Machine-readable companion:
`artifacts/qm5_41335_fx_q05_enqueue_20260910T124702Z_board_advisor.json`.

# QM5_1618 Build Evidence — Source Ready, Governed Compile Held

- Task: `55c469d0-5432-4efb-8d91-19d9a8fd010f` (`build_ea`, priority 50, assigned to Codex)
- EA: `QM5_1618_mql5-ma-support`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1618_mql5-ma-support.md`
- Current MQ5 SHA-256: `efb7c4e598671c7c47d90c3ffdc1469fb3b44f2ab8fe262831db684be56cbac1`
- Outcome: `SOURCE_READY_COMPILE_HELD`

## Governed identity

The card declares `g0_status: APPROVED`, `ea_id: QM5_1618`, and slug `mql5-ma-support`. The active EA registry row matches that identity. Thirteen active magic rows exist for slots 0-12: GDAXI, NDX, SP500, UK100, WS30, XAUUSD, EURUSD, GBPUSD, USDJPY, USDCHF, AUDUSD, USDCAD, and NZDUSD, all with the `.DWX` suffix.

## Card implementation

The source implements the approved H1 moving-average touch continuation:

- completed-bar SMA(10) support touch for buys and resistance touch for sells, with both signal-bar open and close remaining on the continuation side;
- next-bar entry, one position per symbol/magic, and an optional MA-slope filter disabled for baseline;
- structural stop beyond the signal-bar extreme by 1.0 x ATR(14), rejecting entries whose stop exceeds 2.0 x ATR;
- deterministic 2R take profit plus opposite-touch or close-through strategy exit;
- closed-bar gating before all OHLC and indicator reads used for exits and entries;
- standard fail-closed news gate and Friday liquidation;
- `RISK_FIXED=1000`, `RISK_PERCENT=0`, and `qm_news_stale_max_hours=336`;
- no ML, HFT, grid, martingale, averaging, or discretionary logic.

The card supplies authorized sweep ranges but does not select a single baseline ATR buffer, stop cap, or take-profit multiple. The implementation resolves these as 1.0 x ATR, 2.0 x ATR, and 2R respectively and exposes all three as inputs for reviewer confirmation.

Framework alignment is explicit: the source calls the current MAE hook on every tick and zero-initializes every `QM_EntryRequest` before use.

## Focused verification

- `validate_spec_doc.py`: PASS (1/1).
- `validate_build_guardrails.py`: PASS; 14 files checked, zero findings, stale-news ceiling 336 hours.
- `validate_symbol_scope.py --fail-on-leak`: `SINGLE_SYMBOL_OK`, zero violations.
- Thirteen H1 backtest presets regenerated by canonical `gen_setfile.ps1`.
- Every preset contains `RISK_FIXED=1000`, `RISK_PERCENT=0`, the six strategy defaults, its governed magic slot, and pending build hash.
- `git diff --check`: clean for the EA package.
- Codex source review confirmed closed-bar touch/exit logic, structural-stop direction and cap, magic-scoped position control, MAE tracking, and request initialization.

## Compile boundary

Direct `build_check.ps1` stopped before compilation with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because terminal workers are alive. No bypass, retry, process stop, terminal launch, or backtest interruption was attempted.

The sanctioned command `farmctl.py enqueue-compile QM5_1618_mql5-ma-support` was then refused without enqueueing a row: reason `WORK_ITEMS_EXIST`. The two existing governed compile records are terminal failures (`fdfc721c-0670-4f7f-8902-568eb2b0a37f` INVALID and `e4fc6093-5748-40b2-b6b0-21570ee1b7df` COMPILE_FAIL). The latter was bound to obsolete source SHA-256 `8719a60286a06604ca67e5764773d673acddd3958a87b8008269a7257af2ba48`, not the current source. No `.ex5` exists for the current source.

Smoke was not run, and no pipeline verdict is claimed.

## Required next governed action

A reviewer should confirm the baseline ATR/2R resolutions. Separately, a sanctioned compile-record supersede/rebuild authorization must admit a fresh record bound to the current source. Its worker must produce strict compile and build-check PASS evidence, bind final set-file hashes, and only then permit smoke/review closure.

Short verdict: `SOURCE_READY_COMPILE_HELD: static gates PASS; parameter resolution requires review; fresh governed compile refused by prior work-item guard.`

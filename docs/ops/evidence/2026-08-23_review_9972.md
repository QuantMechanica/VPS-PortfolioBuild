# review_ea — QM5_9972 bandy-heikin-ashi-color-flip-trend — 2026-08-23

Router task `7fadd6f7-da24-4bc4-85eb-522db8a97c19` (review_ea/claude,
`reason=codex_review_required_for_gemini_code`, source_agent=gemini/agy build_ea
`f3b6bf03-fa59-4d17-8336-bb440cf90a0d`). Static/evidence-based review — factory has live
terminal64 workers (T1–T10 active), so no ad-hoc `build_check.ps1` compile; relied on the build
task's captured compile result plus registry/setfile/guardrail evidence and manual card-fidelity
reading. Confirms and re-affirms the prior cohort note
`docs/ops/evidence/2026-08-23_review_ea_11300_9972_9979.md` (9972 section).

## Reviewed identity

- MQ5 SHA-256 (current on disk): `ee65efe4cb93520863ebe83811de25b3e29fa01713537e5aba0ee38c16a0f47a`
  — matches `artifacts/qm5_9972_build_result.json` `mq5_sha256`, i.e. the ex5 compile is for the
  current source hash. `build_check_passed:true`, `compile_succeeded:true`.
- EX5 SHA-256: `8b2f568d0d2d1a9ac7341f9a4310c5c96d2310d3b69fe934cab5ddf989a99cea`.
- Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9972_bandy-heikin-ashi-color-flip-trend.md`,
  Howard B. Bandy, "Quantitative Technical Analysis" (2015), ISBN 978-0-9791037-7-1; G0 APPROVED,
  R1–R4 all PASS. SPEC present (`framework/EAs/QM5_9972_.../SPEC.md`, ex-post generated).

## Card fidelity (mechanism)

- HA recurrence: `Strategy_HeikinAshi2Bars` (mq5:64–99) recomputes the recursive HA open/close
  each call over a bounded `strategy_ha_seed_bars=50` lookback, seeding at `start_shift` with
  `(open+close)/2` (mq5:80–82) then `ha_open[t]=(ha_open[t-1]+ha_close[t-1])/2` — matches the
  card seed/recurrence. Bounded recomputation is a robust alternative to the card's persisted-state
  approach; the seed bias decays ~2^-N so 50 bars is numerically ample. HA-open converges; regime
  SMA(200) is read independently via `QM_SMA`, so the card's "201 bars" concern does not apply.
- Long: `ha_green1 && ha_red2 && bar1.close > SMA(200)` (mq5:158, 151); Short mirrors (mq5:161).
  Regime on **raw** close, ATR on **raw** OHLC (`QM_ATR`/`QM_StopATR`, mq5:143,177,202) — the
  card's explicit "do not conflate" instruction, verified.
- Razor-body skip `|ha_close1-ha_open1| < 0.1*ATR(14)` (mq5:148); anti-cluster 3-bar window per
  direction via `iBarShift` against `g_last_{long,short}_entry_bar_time` (mq5:167–171,192–196);
  exit = opposite HA color flip OR 30-bar time stop (mq5:241–261); cat-SL `2.5*ATR(14)`
  (mq5:177,202). One position per magic (`QM_TM_OpenPositionCount`, mq5:130), no pyramiding,
  empty `Strategy_ManageOpenPosition` — no trailing/scale/martingale/ML added. All match card.

## Input use-site audit (dead-input check)

All 7 declared `strategy_*` inputs have live use sites — none dead:
`strategy_regime_sma_period` (151), `strategy_atr_period` (143,177,202),
`strategy_atr_stop_mult` (177,202), `strategy_time_stop_bars` (241,244),
`strategy_anti_cluster_bars` (170,195), `strategy_razor_body_atr_mult` (148),
`strategy_ha_seed_bars` (71,74,127).

## Framework conformity

- `#include <QM/QM_Common.mqh>`; `QM_FrameworkInit`/`QM_FrameworkShutdown` wired; MAE hook
  `QM_FrameworkTrackOpenPositionMae()` present (OnTick); kill-switch, Friday-close, equity stream,
  timer, trade-transaction all wired.
- Magic: `ea_id*10000+slot` — `magic_numbers.csv` has 13 active rows `99720000+slot` for the 13
  designed symbols, matching the SPEC "Designed for" universe and the 13 shipped setfiles 1:1;
  `ea_id_registry.csv` single active row.
- Risk: `RISK_FIXED=1000` / `RISK_PERCENT=0.5` both declared; setfiles carry RISK_FIXED=1000,
  RISK_PERCENT=0 (backtest). News: `QM_NEWS_TEMPORAL_PRE30_POST30`/`QM_NEWS_COMPLIANCE_DXZ`
  (standard blackout active, not disabled), stale-max 336h. No ML libs, no hardcoded
  commission/swap/DST. Setfiles generated via governed path; guardrails pass.
- `validate_build_guardrails.py` → PASS (14 files, 0 findings). `validate_spec_doc.py` → PASS.

## Non-blocking nit

- SPEC §2 Parameters says "(no strategy-specific inputs) — uses framework defaults only", which is
  a generated-doc inaccuracy: the mq5 declares 7 `strategy_*` inputs whose defaults match the card
  values exactly (200/14/2.5/30/3/0.1/50). Documentation drift only; no code/behaviour impact.

## Verdict

**APPROVED** — card-faithful mechanism, no dead inputs, controls-clean on magic/risk/news/framework
wiring, compile evidence for the current source hash. Formally clean enough for Q02. One
non-blocking SPEC-doc nit noted above.

# rework-37001 — card-defect preflight and blocked disposition

**Ticket:** `rework-37001`  
**Router task:** `22e95cd4-390e-48a4-aa40-1b144a2817e3`  
**EA:** `QM5_37001_ernest-chan-ornstein-uhlenbeck-statarb`  
**Disposition:** `BLOCKED` — an OWNER-authorized card correction is required before a card-faithful two-leg build is deterministic.

## What changed

- Claimed the router task as `IN_PROGRESS` with verdict `rework burn-window slot 6`.
- Performed the required card/build/review/registry preflight.
- Made **no EA, setfile, framework, resolver, or registry change**. In particular,
  `framework/registry/magic_numbers.csv` was not touched; its existing active rows
  for slots 0–2 remain the source of truth.
- Recorded this fail-closed evidence instead of inventing a pair contract.

## Deterministic blocker

The approved card requires a multi-symbol package but does not provide enough
information to implement one without changing strategy mechanics:

- The card names three eligible symbols and one primary symbol, and explicitly says
  the strategy is not single-symbol-only
  (`D:/QM/strategy_farm/artifacts/cards_approved/QM5_37001_ernest-chan-ornstein-uhlenbeck-statarb.md:33-36`).
- It requires `BUY Spread Package` / `SELL Spread Package`
  (`...QM5_37001_ernest-chan-ornstein-uhlenbeck-statarb.md:91-94`).
- Its complete implementation-parameter table contains only lookback, entry Z,
  maximum half-life, and combined risk percent
  (`...QM5_37001_ernest-chan-ornstein-uhlenbeck-statarb.md:163-166`). It does not
  identify either leg pairing, partner-slot mapping, hedge-ratio estimation/freeze
  rule, or how the combined risk budget is divided between legs.
- The card's declared source id is
  `ernest-chan-ornstein-uhlenbeck-statarb-official-source` (`...md:8`), but neither
  `strategy-seeds/sources/ernest-chan-ornstein-uhlenbeck-statarb-official-source`
  nor `strategy-seeds/sources/ernest-chan-ornstein-uhlenbeck-statarb` exists in the
  worktree (`Test-Path` returned `False` for both).

The review confirms both sides of the gate:

- The existing implementation is rejected because it has no second leg, hedge ratio,
  or spread construction
  (`docs/ops/evidence/2026-08-23_review_QM5_37001.md:38-42` and
  `docs/ops/evidence/22e95cd4_qm5_37001_gemini_build_codex_review_2026-08-18.md:17-23`).
- The latest review also classifies raw-price versus cointegrated-spread scope as an
  OWNER design decision and says not to silently rescope it
  (`docs/ops/evidence/2026-08-23_review_QM5_37001.md:61-66`).

Choosing, for example, AUDUSD/NZDUSD, inventing a rolling hedge-ratio regression, or
mapping the three current setfiles to different partner legs would therefore be a new
strategy contract rather than a card-faithful repair. That is outside this build-only
ticket and conflicts with the rework instruction's explicit card-defect fallback.

## Registry evidence

Read-only registry census:

```text
framework/registry/ea_id_registry.csv:4464
37001,ernest-chan-ornstein-uhlenbeck-statarb,...,active,...

framework/registry/magic_numbers.csv:17450-17452
slot 0 AUDUSD.DWX magic 370010000 active
slot 1 USDCAD.DWX magic 370010001 active
slot 2 NZDUSD.DWX magic 370010002 active
```

There is no registry gap. Resolver regeneration is neither required nor permitted by
the review scope.

## Validation and tests

EA-scoped compile/build checks and build-gate hardening were intentionally not run:
there is no authorized implementation to compile, and running them against the known
rejected source would not resolve or test the blocking contract gap.

Router state-transition regression:

```text
> python -m pytest tools/strategy_farm/tests/test_agent_router_state_exits.py -q
........................                                                 [100%]
24 passed in 9.55s
```

Final runtime-state verification used SQLite URI
`file:D:/QM/strategy_farm/state/farm_state.sqlite?mode=ro`:

```sql
SELECT id, state, artifact_path, verdict
FROM agent_tasks
WHERE id = '22e95cd4-390e-48a4-aa40-1b144a2817e3';
```

```text
state         BLOCKED
artifact_path docs/ops/evidence/2026-08-23_rework-37001.md
verdict       BLOCKED: approved card omits exact two-leg pair, partner slots,
              hedge-ratio freeze rule, and package risk allocation;
              OWNER card revision required
```

Preflight commands also verified:

```text
> git status --short --branch
## rework-slot-6

> rg -n '^37001,' framework/registry/ea_id_registry.csv framework/registry/magic_numbers.csv
framework/registry/ea_id_registry.csv:4464:...
framework/registry/magic_numbers.csv:17450:...
framework/registry/magic_numbers.csv:17451:...
framework/registry/magic_numbers.csv:17452:...
```

## Required OWNER/Card action

Provide an APPROVED card revision that freezes, at minimum:

1. exact leg pair(s) and which chart/setfile is the package host;
2. symbol-slot mapping for both legs;
3. hedge-ratio estimation, lookback, refit/freeze cadence, and validity bounds;
4. coordinated partial-fill/rollback behavior;
5. combined package-risk allocation and per-leg safety-stop construction; and
6. whether all three currently registered symbols remain in scope.

After that authorization, the rework can implement the already-actionable secondary
findings in the same identity: closed-bar OU caching, entry-half-life persistence for
the `2.5 × tau` time stop, protective exits outside entry filters, and the 2.0% / 2.5%
/ 5.0% capital-preservation controls.

## Rollback

This ticket changes only this evidence document plus the sanctioned router task state.
To roll back the repository artifact, revert the commit containing this file. Router
state is append-audited operational state and should be advanced by a new sanctioned
`agent_router.py update-task` call after an APPROVED card revision; do not rewrite the
historical task record.

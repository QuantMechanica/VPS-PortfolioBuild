# QM5_12949 Build EA Evidence — 2026-08-22

**Task ID:** `b40150ec-8dc7-46fa-b15d-9e82762240ea` (`build_ea`, priority 50, agent `gemini`).  
**EA:** `QM5_12949_mql5-rvi-ma-card`.  
**Artifact Path:** `C:/QM/repo/docs/ops/evidence/b40150ec_qm5_12949_build_ea_2026-08-22.md`.  
**Strategy Card:** `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12949_mql5-rvi-ma-card.md` (G0 APPROVED).  

---

## 1. Strategy Overview & Implementation

- **Strategy Thesis**: Mohamed Abdelmaaboud (2022) Relative Vigor Index & MA Crossover (H1).
  - Calculates RVI(10) with signal line, SMA(100), and ATR(14) on closed H1 bars.
  - Volatility filter: Skips entries if ATR(14) < 0.5 * ATR(100) to avoid dead ranges.
  - Long Entry: `close > SMA(100)` and `RVI_main > RVI_signal`.
  - Short Entry: `close < SMA(100)` and `RVI_main < RVI_signal`.
  - Protective Stop: `entry - ATR(14) * 1.6` for Long; `entry + ATR(14) * 1.6` for Short.
  - Take Profit: 2.0R distance from entry (`entry +/- (entry - sl) * 2.0`).
  - Indicator Exits:
    - Long exit: `RVI_main < RVI_signal` or `close < SMA(100)`.
    - Short exit: `RVI_main > RVI_signal` or `close > SMA(100)`.
  - Failsafe time exit: after 48 H1 bars.
  - Single position per magic number; opposite signal closes position.
- **Framework Corset Compliance**:
  - Implemented using V5 framework standards and `QM_Common.mqh`.
  - News temporal mode (`QM_NEWS_TEMPORAL_PRE30_POST30`) and compliance profile (`QM_NEWS_COMPLIANCE_DXZ`) with fail-closed stale guard (`qm_news_stale_max_hours = 336`).
  - Sizing handled strictly via framework risk model (`RISK_PERCENT = 0.0`, `RISK_FIXED = 1000.0` for backtest).

---

## 2. Registry & Artifact Verification

- **Registries**:
  - `framework/registry/ea_id_registry.csv`: verified `12949,mql5-rvi-ma-card`.
  - `framework/registry/magic_numbers.csv`: registered 3 slots (0: `EURUSD.DWX`/`129490000`, 1: `GBPUSD.DWX`/`129490001`, 2: `XAUUSD.DWX`/`129490002`).
  - `framework/include/QM/QM_MagicResolver.mqh`: regenerated cleanly via `update_magic_resolver.py` (17,624 rows preserved, 0 dropped).
- **SPEC Document**:
  - `framework/EAs/QM5_12949_mql5-rvi-ma-card/SPEC.md` generated via `gen_spec_md.py` and validated with `validate_spec_doc.py` (`PASS`).
- **Static Checks & Guardrails**:
  - `validate_build_guardrails.py`: `PASS` (0 findings, stale news max hours <= 336).
- **P2 Setfiles**:
  - Generated all 3 backtest setfiles in `sets/` via `gen_setfile.ps1` (`EURUSD.DWX`, `GBPUSD.DWX`, `XAUUSD.DWX`) with `RISK_FIXED=1000` and `RISK_PERCENT=0`.

---

## 3. Build Result & Review State

- Emitted build result JSON to `D:/QM/strategy_farm/artifacts/builds/b40150ec-8dc7-46fa-b15d-9e82762240ea.json` and `C:/QM/repo/artifacts/qm5_12949_build_result.json`.
- Moving task `b40150ec-8dc7-46fa-b15d-9e82762240ea` to `REVIEW` for mandatory Codex review per router rules.

# rework-9908 — review-fix evidence

- Router task: `9bd88385-fdac-4b6d-9437-0cdc14fb3f25`
- Source build task: `ce7ef250-d7c0-418a-aa51-fff4f7a8136e`
- EA: `QM5_9908_bandy-psar-flip-trend`
- Worktree: `C:/QM/worktrees/rework-slot-6`
- Reworked: 2026-08-24 (Europe/Berlin)
- Intended disposition: `REVIEW`

## Work order and preflight

The mandatory Codex review at `docs/ops/evidence/2026-08-23_qm5_9908_gemini_code_review.md:13-94` rejected the Gemini build for six concrete issues: chart-timeframe entry cadence, fixed-risk sizing from the wrong stop, absent PSAR protective trailing, an invented spread filter that could block exits, unauthorized GDAXI/UK100 setfiles with omitted oil, and an incomplete producer receipt.

Preflight evidence:

- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9908_bandy-psar-flip-trend.md:15` has `g0_status: APPROVED`; its D1 entry/exit contract is at lines 43-56, PSAR/catastrophe/risk rules at lines 58-63, and authorized universe at lines 79-83.
- EA registry: `framework/registry/ea_id_registry.csv:784` is the active `9908 / bandy-psar-flip-trend` row.
- Existing magic rows were preserved byte-for-byte. The review exposed that approved `XTIUSD.DWX` had no row, so only new slot 13 was allocated at `framework/registry/magic_numbers.csv:17006`.
- The task was claimed with router verdict `rework burn-window slot 6`; no factory, queue, backtest verdict, or live-terminal state was changed.

## Card-faithful repair

### D1 execution and bounded data access

- `framework/EAs/QM5_9908_bandy-psar-flip-trend/QM5_9908_bandy-psar-flip-trend.mq5:290-299` declares the fail-closed `PERIOD_D1` framework execution contract.
- Entry uses exactly one explicit daily edge at `.mq5:353`; it no longer inherits the chart timeframe.
- The two closed daily bars are loaded by a constant-size `CopyRates` call with `ArrayResize` and `ArraySize` guards at `.mq5:68-80`. The raw `Bars`/`CopyRates` calls carry same-line `perf-allowed` justification. No raw `CopyBuffer`, `OrderSend`, `PositionModify`, or ML entry point exists.

### PSAR risk, catastrophe boundary, trailing, and exits

- Initial fixed-risk sizing now uses the normalized PSAR level directly via `req.sl` at `.mq5:155,176`; the framework therefore calculates `RISK_FIXED=1000` volume from the initial PSAR distance rather than 4 ATR.
- The separate 4-ATR catastrophe boundary is still calculated from the entry price. `.mq5:82-99,157,178` serializes that boundary into `POSITION_COMMENT`, making the entry-time value reconstructable after restart.
- `.mq5:239-258` clamps the moving PSAR stop against the persisted catastrophe boundary and ratchets only through `QM_TM_MoveSL`.
- `.mq5:213-219` uses restart-safe framework D1 held-period counting for the 60-bar time stop. Closed-bar PSAR reversal exits remain symmetric at `.mq5:229-234`.
- Position management executes at `.mq5:315`, before kill-switch, news, Friday-close, daily-edge, warmup, and every other entry-only return. The invented spread/ATR filter and its input were removed.
- Framework conformity is retained: `QM_Common` umbrella include, `QM_FrameworkTrackOpenPositionMae` at `.mq5:311`, framework risk modes, host magic resolved through `QM_FrameworkMagic`/MagicResolver, timer/transaction/tester hooks, and no direct trade-send bypass.

Declared strategy-input use-site census (source occurrences, declaration included):

| Input | Occurrences |
|---|---:|
| `strategy_psar_step` | 6 |
| `strategy_psar_max` | 5 |
| `strategy_sma_period` | 4 |
| `strategy_atr_period` | 4 |
| `strategy_sl_atr_mult` | 4 |
| `strategy_time_stop_bars` | 3 |
| `strategy_warmup_bars` | 3 |

`tools/strategy_farm/tests/test_qm5_9908_review_rework_static.py:39-188` locks all of these mechanism, lifecycle, bounded-read, input-sealing, symbol-scope, registry/resolver, and producer-identity properties.

## Authorized package and registry

- The package now contains exactly 12 card-authorized backtest sets: seven FX majors, XAUUSD, XTIUSD, NDX, WS30, and backtest-only SP500. The GDAXI and UK100 setfiles were removed; their pre-existing registry allocations were not modified, as required by the ticket.
- New oil set evidence is at `framework/EAs/QM5_9908_bandy-psar-flip-trend/sets/QM5_9908_bandy-psar-flip-trend_XTIUSD.DWX_D1_backtest.set:7,13,18-20`: symbol `XTIUSD.DWX`, source hash, slot 13, `RISK_FIXED=1000`, and `RISK_PERCENT=0`.
- All 12 retained/new setfiles seal every declared strategy input and MQ5 SHA-256 `587525bbf2413cde5bbdb44fbdefc86f95fe6710c964b1268254d3c3092c121f`.
- `python framework/scripts/update_magic_resolver.py --keep-obsolete` wrote `framework/include/QM/QM_MagicResolver.mqh:16-18` with registry SHA `620732E72C04997C45F8603C06C0E2A9EAACA5A03FCEF8C3628868FA204F6918`, 17,993 kept rows, and zero drops. A subsequent `--keep-obsolete --dry-run` was idempotent with the same count/hash and zero drops.
- `SPEC.md:18,44,57,93,101` now documents the actual PSAR/catastrophe lifecycle, oil universe, D1 contract, tester/live risk inversion, and review revision.

## Compile and producer identity

- The required scoped strict attempt was run as `compile_one.ps1 -EAPath framework/EAs/QM5_9908_bandy-psar-flip-trend/QM5_9908_bandy-psar-flip-trend.mq5 -Strict`. Its include mirror refused before compilation with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because terminal64 processes were active. The separately required scoped `build_check.ps1 -EALabel QM5_9908_bandy-psar-flip-trend -SkipCompile` hit the same fail-closed interlock before report generation. No terminal was stopped and no compile item was enqueued.
- The EA was compiled in a hidden, isolated portable MetaEditor profile under `D:/QM/strategy_farm/tmp/rework-9908-slot6-compile`: standard library seeded from idle T7, then the exact worktree framework include tree overlaid. The compile log proves all QM includes resolved from that isolated profile and ends `Result: 0 errors, 0 warnings, 10231 ms elapsed`: `D:/QM/reports/compile/20260824_rework9908_slot6/QM5_9908_bandy-psar-flip-trend.compile.log`.
- MQ5 SHA-256: `587525bbf2413cde5bbdb44fbdefc86f95fe6710c964b1268254d3c3092c121f`.
- EX5 SHA-256: `fdb24a8f857df584e007f6bed94b775656930fd33cff179306e1a3c120a3abba`; size 406,736 bytes.
- The prior EX5 is recoverable at `D:/QM/reports/compile/20260824_rework9908_slot6/pre_rework_QM5_9908_bandy-psar-flip-trend.ex5`.
- `framework/EAs/QM5_9908_bandy-psar-flip-trend/build_identity.json:2-55` is now schema-complete and binds task/rework IDs, paths, hashes, magic base, approved symbols, all setfiles, compile status, component checks, and the exact interlock outcomes.

## Validation evidence

Commands ran from the worktree root.

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_9908_bandy-psar-flip-trend
files_scanned=1; failures=[]; warnings=[]

python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 framework/EAs/QM5_9908_bandy-psar-flip-trend
verdict=PASS; files_checked=13; findings=[]

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_9908_bandy-psar-flip-trend
Summary: 1 PASS, 0 FAIL (of 1)

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_9908_bandy-psar-flip-trend --fail-on-leak --json
verdict=SINGLE_SYMBOL_OK; n_violations=0

python tools/strategy_farm/raw_mq5_quarantine.py check --source-path framework/EAs/QM5_9908_bandy-psar-flip-trend/QM5_9908_bandy-psar-flip-trend.mq5 --purpose compile --repo-root .
allowed=true; code=RAW_MQ5_SOURCE_ALLOWED

python -m pytest -q tools/strategy_farm/tests/test_qm5_9908_review_rework_static.py
6 passed in 1.09s

python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py -k "d2_loss or d4_management or d7_framework or d8_cached or d10_indicator or d11_canonical or d17_explicit"
7 passed, 22 deselected in 1.20s

python -m pytest -q tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_validate_symbol_scope.py tools/strategy_farm/tests/test_validate_spec_doc.py
25 passed in 1.90s

python -m pytest -q tools/strategy_farm/tests/test_magic_resolver_reconcile_newlines.py framework/scripts/tests/test_magic_resolver_symbol_fail_closed.py framework/scripts/tests/test_magic_resolver_strict_default.py framework/scripts/tests/test_magic_resolver_hash_newlines.py framework/scripts/tests/test_magic_resolver_binary_search.py
13 passed, 1 failed in 1.51s
FAIL is the unrelated repository-baseline assertion that default regeneration drops no rows; current obsolete-directory rows 1001/1015/1016 are dropped without --keep-obsolete. This ticket's governed --keep-obsolete regeneration kept 17,993 and dropped zero.

python -m pytest -q framework/scripts/tests/test_magic_resolver_binary_search.py -k "not equivalent_over_every_generated_row_and_misses"
3 passed, 1 deselected in 0.16s
```

## Smoke interlock evidence

One non-enqueued build smoke was attempted with the exact EX5, EURUSD.DWX D1 fixed-risk set, 2024 window, model 4, one run, minimum one trade, and idle T7. Terminal resolution succeeded, but `run_smoke.ps1` refused before terminal launch:

```text
run_smoke.stage=resolved_terminal terminal=T7
Custom-history gate/reservation refused terminal 'T7':
active Custom-history isolation requires a worker-bound work item whose archives were privatized before run_smoke
```

No smoke/backtest ran. Enqueueing a worker-bound backtest solely to obtain smoke evidence is explicitly forbidden by this ticket, so `build_identity.json:52-56` records `framework_error`, null report path, and the exact refusal instead of claiming a false PASS or saturation waiver.

## Risks and open questions

- The monolithic build-check wrapper did not PASS; it was blocked before execution by its live-factory interlock. All scoped component checks and the actual isolated compile passed, and the receipt exposes this limitation.
- Canonical smoke evidence remains absent because active custom-history isolation requires a governed worker-bound work item, while this ticket forbids enqueueing backtests. Reviewer must decide whether the compiled/component evidence is sufficient for this resubmission.
- GDAXI/UK100 registry rows remain active because the ticket forbids altering existing magic allocations. They have no delivered setfiles and are not listed in the producer's approved package.
- The full resolver test group has one unrelated default-mode corpus fixture failure as documented above; every other resolver test passed, and the required `--keep-obsolete` zero-drop regeneration is deterministic.
- No gate threshold/criterion, factory state, backtest queue, verdict row, T_Live file, or AutoTrading state was changed.

## Rollback

Revert the ticket commit with `git revert <commit>`. This restores the prior MQ5/EX5/SPEC/receipt/setfiles, removes the XTI set and focused test/evidence, removes only the new slot-13 CSV row, and restores the prior generated resolver. If binary-only recovery is needed before a revert, copy the exact pre-rework EX5 backup from the compile report path above. No factory, queue, verdict, backtest, or live-terminal rollback is required.

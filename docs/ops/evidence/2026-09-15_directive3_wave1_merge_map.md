All data gathered. Here is the merge map.

---

# Directive-3 Wave-1 Merge Map (for Fable)

## 0. Ground truth about main's dirty state (IMPORTANT — supersedes git-context snapshot)

Main HEAD is `db4bd1fa83`, **3 commits ahead of the slices' base** `1617cb22be` (edge_lab measurements + scheduled-task repair). None of those 3 commits touch any slice path. Blob-level comparison of all 5 "colliding" paths:

| Path | base | HEAD | main WT | Verdict |
|---|---|---|---|---|
| `tools/strategy_farm/book_evolution_runner.py` | 4e32c482 | 4e32c482 | 4e32c482 | **PHANTOM-dirty** (stat-dirty only; content == HEAD) |
| `decisions/REGISTRY.md` | 67beb924 | 67beb924 | 67beb924 | **PHANTOM-dirty** (diff is empty) |
| `tools/strategy_farm/strategy_wiki_sync.py` | b2ff3292 | b2ff3292 | b2ff3292 | PHANTOM-dirty |
| `tools/strategy_farm/tests/test_strategy_wiki_sync.py` | e837eab2 | e837eab2 | e837eab2 | PHANTOM-dirty |
| `docs/ops/FTMO_CHALLENGE_READINESS.md` | 8c8bcbb8 | 8c8bcbb8 | **868729f1** | **REAL uncommitted regen drift** (Generated 20:37Z vs committed 15:32Z; validation-days 0.023→0.267; freshness 0.065→0.276) |

Other real main drift (none on slice paths): `docs/research/STRATEGY_LINEAGE_MAP_2026-09.md`, `STRATEGY_UNIVERSE_MAP_2026-09.md`, `RESEARCH_PROGRAMME_ROI_2026-09.md` (all pure regen drift from scheduled renderer runs, timestamps ~20:31Z), `framework/EAs/...12109....set` (4 lines), `ea_origin.v1.csv` (line-ending drift), deleted `artifacts/qm5_41262_build_result_20260901.json`. The 7 slice review files (`review/a1…j1_*_review.md`) are **already in main as untracked files** (reviewer wrote them there) — nothing to apply; commit them from main.

Ownership: FTMO readiness + 3 research maps drift is machine-generated by `challenge_readiness.py` / `lineage_map.py` / `universe_map.py` / `external_roi.py` renderers — almost certainly the scheduled book-evolution/wiki-sync task family repaired in `db4bd1fa83`. No human signature → **UNKNOWN-OWNER (machine cadence)**, discard-and-regenerate or commit, both safe. `.set` / `ea_origin.csv` / artifacts deletion: **UNKNOWN-OWNER**, unrelated to wave-1.

## 1. Per-slice status

Note: review files are in MAIN (untracked), not in the worktrees; each maps to a design report staged in its worktree. No slice has a review file inside its own worktree.

**ba6-1 (a1_ai_capacity_remeasure)** — Verdict **ACCEPT**. No blocking fixes. 4 minor advisories (schedule a `render` for doc/vault blocks; Kimi auth fragility → carry into c1 routing; snapshot staleness; large sealed decision surface is as-designed). Files: 5 NEW (decision record `decisions/2026-09-15_owner_max_factory_utilization_eligibility_v2.md` ← **this mints the record d1/e1/b1 cite as dangling**, `docs/ops/AI_FACTORY_CAPACITY_AND_ROUTING.md`, `ai_capacity_readmodel.py` 783 LOC, design report a1, test) + 3 MODIFIED (`CLAUDE.md` appended annex, `REGISTRY.md` +1 row, `book_evolution_runner.py` +5). All main-clean.

**ba6-2 (b1_ai_capability_benchmark)** — **UNREVIEWED (impl agent died)**. See §5.

**ba6-3 (d1_strategy_eligibility_v2)** — Verdict **ACCEPT_WITH_FIXES**, no blocking. Major (tracked, cross-slice): (1) mint the OWNER-DEC-D3 decision record → satisfied by ba6-1; (2) grid/martingale passes new intake but `governed_magic_allocator.py:374` still rejects `prohibited_technique:grid` at build — safe fail-closed, track as explicit follow-up. Minor: stale comment in `mechanization_check.py:62`; residual schema gap (deferred, correct). Files: 7 NEW (risk contract module+config, 2 research docs, design report d1, test) + 5 MODIFIED (`card_intake_prescreen.py`, `strategy_card_v3.py`, 2 tests, 2 prose docs). All main-clean.

**ba6-4 (e1_second_chance_scan)** — Verdict **ACCEPT_WITH_FIXES**, advisory only. Major (advisory): 2025 holdout is not the canonical sealed 2026 OOS window — mitigated by 0 challengers + spec-level independent-holdout requirement; caveat must travel with any enqueue decision. Minor: dangling decision-record citation (fixed by ba6-1); HISTORICAL_POLICY (412) must stay §30-bound, never batch-enqueued; population = wiki projection only. Files: 5 NEW (`second_chance_register.py`, config, programme doc, design report e1, test) + 2 MODIFIED (`strategy_wiki_sync.py` +96, `test_strategy_wiki_sync.py` +44). Main-clean; collides only with ba6-6.

**ba6-5 (g1_vps_resource_scheduler)** — Verdict **ACCEPT_WITH_FIXES**. MAJOR applies only to opt-in STEP 2 (`QM_RAM_TABLE=calibrated`, default-off): calibrated table would double `opt_census_cell` 4→8 GB against its own comment/small-lane policy — fix before STEP 2 activation (pin 4 GB or fix comment + add watch); STEP 1 is clean, ship it. 3 minor (two-layer heavy-row wait >45 min wall; snapshot drift between comment and doc; census measured max 13.6 GB context). Files: 9 NEW (2 modules 804 LOC, 2 configs, 3 tests, ops doc, design report g1) + 1 MODIFIED (`terminal_worker.py` +293, additive guarded seam, kill-switch `QM_RESOURCE_SCHEDULER=0` default). Main-clean.

**ba6-6 (h1_live_sleeve_pnl_attribution)** — Verdict **ACCEPT_WITH_FIXES**, no blocking. 3 minor (return-pct denominator = first deposit not book-inception equity, ~1.8% relative off, disclosed; sparse-overlap correlation emitted below n_days≥20 gate — no consumer weights it today; headline live values drifted between report and commit — load-bearing figures stable). Files: 4 NEW (`live_sleeve_attribution.py` 554 LOC, ops doc, design report h1, test) + 6 MODIFIED (`book_evolution_runner.py` +4, `frozen_snapshot.py`, `external_roi.py`, `strategy_wiki_sync.py` +70, 2 tests). Main-clean; collides only with ba6-1 and ba6-4.

**ba6-7 (i1_ftmo_first_passage)** — Verdict **ACCEPT_WITH_FIXES**, **1 apply-time blocking condition**: whole-patch apply fails on `docs/ops/FTMO_CHALLENGE_READINESS.md` because main's working tree carries an uncommitted regeneration of that machine-generated living doc (review saw 17:52Z; now 20:37Z). Fix per review: `git checkout -- docs/ops/FTMO_CHALLENGE_READINESS.md` then apply, OR apply code-only and regenerate via `python tools/strategy_farm/ftmo/challenge_readiness.py build`. Rule-contract widening (STANDARD+SWING) verified benign. 3 minor (pin identical-projected-fields invariant; wall-clock timestamp; calendar-vs-trading-day note). Files: 3 NEW (`first_passage.py` 974 LOC, design report i1, test 259 LOC) + 4 MODIFIED (`FTMO_CHALLENGE_READINESS.md` +17/-, `challenge_readiness.py`, `ftmo_fitness.py`, `ftmo_rule_contract.py`).

**ba6-8 (j1_pattern_filter_catalog)** — Verdict **ACCEPT_WITH_FIXES**, no blocking. **One MAJOR descriptive-data defect (fix before figures are cited, non-blocking):** `load_census_join` baseline bucket is contaminated by non-null arms (wf combos, range-mult params, final:*) because of `predicate_id in (None,0)` OR-clause + last-wins dict + no ORDER BY; 123/210 groups have >1 baseline-classified row; published negative trade-reductions are impossible-signed vs a true null. Fix is confined to `load_census_join` (strict `arm=='baseline'` + ORDER BY + dedupe/skip-as-EVIDENCE_MISSING), then re-run + re-commit JSON/doc/Vault. Authoritative finding (0 filters ever selected, cap never bound) is independent and verified correct. 3 minor. Files: **4 NEW, 0 MODIFIED** — cleanest slice.

## 2. Collision analysis (the real ones)

**(a) `book_evolution_runner.py` — ba6-1 vs ba6-6.** Main: no content change. ba6-1 inserts an `ai_capacity` state-build entry (with comment) immediately after the `research_roi` entry; ba6-6 inserts a `live_sleeve_attribution` entry (with comment) at the exact same anchor, both before `]`. **Textually colliding** (same anchor, both pure insertions); **semantically fully compatible** (independent entries in the same list). 3-way merge auto-resolves to both entries; `git apply` of the second patch after the first fails without `--3way`.

**(b) `strategy_wiki_sync.py` — ba6-4 vs ba6-6.** Both add an independent optional read-only join: ba6-4 = Second-Chance Register fields (optional keys, folded into node digest only when present), ba6-6 = live realized-PnL fields (always emitted; absent feed → EVIDENCE_MISSING/NOT_APPLICABLE). Hunk map:

| Region | ba6-4 | ba6-6 | Merge result |
|---|---|---|---|
| `Sources` class | `second_chance_register` property after `sidecar_path` | `live_attribution` field after `health_out`; `default_sources` param | different regions → auto |
| new loader fn | `load_second_chance` | `load_live_pnl` | auto |
| `resolve_fields` signature | +`second_chance=None` (4th param) | +`live_pnl=None` (4th param) | **same lines → conflict; resolve `(..., handwritten, second_chance=None, live_pnl=None)`** |
| fields dict | `fields.update(sc)` after literal | live fields inline in literal | auto (keep both; SC update stays before digest) |
| `_node_inputs_sha256` | +`payload["second_chance"]` after `source_hash` | +2 live keys after `source_hash` | overlapping context → conflict; keep both |
| `_FRONTMATTER_ORDER` | +6 SC keys after `handwritten_node` | +4 live keys after `live_demo_status` | adjacent hunks → likely conflict; keep both groups |
| `render_node` | SC section appended at end | 3 bullets mid-body | auto |
| `build()`/`lint()` | +`load_second_chance` line; call passes `second_chance` | +`load_live_pnl` line; call passes `live_pnl` (also in `lint`) | **same anchor + same call line → conflict; resolve `resolve_fields(record, sources, handwritten, second_chance, live_pnl)` and keep both loader lines** |

Semantics compatible: SC keys optional/absent-keyed, live keys unconditional — no interplay. Resolution = keep-both everywhere.

**(c) `test_strategy_wiki_sync.py` — ba6-4 vs ba6-6.** ba6-4: completeness test skips `_OPTIONAL_FRONTMATTER_KEYS` + `_write_register` helper + idempotency test. ba6-6: fixture gains `live_attribution=` + live-PnL render test. Disjoint regions → auto-merge; combined suite is coherent (fixture satisfies both).

**(d) `decisions/REGISTRY.md` — main vs ba6-1.** Main's " M" is phantom (empty diff). ba6-1 adds one row after the `|---|` header, before the OWNER-DEC-CBE row. **CONFLICT-FREE.**

**(e) `docs/ops/FTMO_CHALLENGE_READINESS.md` — main vs ba6-7.** Main has a REAL newer regen (20:37Z) vs the patch's 18:00:30Z render. Patch hunks overlap the header timestamp line, demo-cycle line, and rules-freshness line → `git apply` fails (confirmed by i1 review). Per review: discard WT regen, apply, regenerate; or apply code-only + regenerate. Doc content is 100% reproducible from `challenge_readiness.py`.

## 3. Recommended apply order

1. **Settle main's own drift first**: commit (or `git checkout --`) the 4 machine-regenerated living docs (`FTMO_CHALLENGE_READINESS.md`, 3 `docs/research/*_2026-09.md`) — committing preserves evidence and makes ba6-7's patch apply; the phantom-dirty files need no action (content == HEAD; `git status` clears on refresh). `.set`/`ea_origin.csv`/artifacts-deletion are unrelated — separate discretionary commit. Rationale: only real main-vs-slice collision is the FTMO living doc.
2. **Commit the 7 review files already untracked in main** — they are the wave-1 review record; zero risk.
3. **Apply ba6-1** — mints `OWNER-DEC-D3` decision record, resolving the dangling-authority Major/Minor findings of ba6-3 (d1-Major-1) and ba6-4 (e1-Minor-2); its runner + REGISTRY hunks then anchor.
4. **Apply ba6-6 with `git apply --3way`** — book_evolution_runner + wiki-sync hunks auto-merge keep-both on top of ba6-1.
5. **Apply ba6-4 with `git apply --3way`** — remaining wiki-sync/test conflicts auto-merge keep-both (4↔6 order interchangeable; both need `--3way`).
6. **Apply ba6-7** — only after step 1; then regenerate the readiness doc per the review's blocking fix.
7. **Apply ba6-3, ba6-5, ba6-8 in any slot** — zero collisions with anything.
8. **ba6-2: decision gate** (§5) — apply only after Fable decides complete-vs-abandon; no file overlap either way.
9. Post-merge: run each slice's test files (per-slice counts verified in reviews: a1 14, d1 59+48, e1 26, g1 33(+88 regression), h1 51, i1 15+26, j1 10); queue the tracked advisory fixes (j1 census-join MAJOR re-run; g1 STEP-2 census pin; d1 build-path wiring follow-up; e1 OOS caveat + §30 gating note).

New-file note: **every** untracked/added slice file (27 across 8 slices) is absent from main → all apply as clean adds. Exclude `tools/strategy_farm/ai_benchmark/__pycache__/` from any ba6-2 commit.

## 4. ba6-2 inventory (b1_ai_capability_benchmark — unreviewed)

Runtime evidence is REAL and matches its doc (verified on disk): `D:/QM/reports/state/ai_capability_scorecard.json` (schema `qm.ai-capability-scorecard/v1`, 6 runs, 4 cells, generated 18:21Z) and `..._runs.jsonl` (6 kimi runs) reproduce the doc's table exactly: coding 2 runs correctness 1.00, research 2×1.00, review 1×0.80, operations 1×0.00. Patch contents:

- **M `tools/strategy_farm/config/kimi_adapter.v1.json`** — adds `benchmark_code` role (the only writer role with Bash), fenced 3 ways (`requires_allow_shell`, sandbox under `D:/QM/research/ai_benchmark/<run_id>/`, agent-file allowlist Read/Grep/Glob/List/Write/Edit/Bash) + embedded agent file + doc comment.
- **M `tools/strategy_farm/kimi_adapter.py`** — `VALID_ROLES` += `benchmark_code`; `_materialize_agent_file` handles the new `benchmark_code` kind. Small, self-contained, mirrors existing `research_ml` fencing pattern.
- **NEW `tools/strategy_farm/ai_benchmark/`** — 4 modules: `run_benchmark.py` (runner CLI: sandbox materialize → governor-gated provider invoke → deterministic score → scorecard; two-scope mutation guard; never live-risk), `providers.py` (`invoke_provider` + `governor_gate` for kimi/claude/codex/agy, SKIPPED_* never forced), `scorers.py` (pure deterministic scorers: pytest/planted_defects/json_schema/classification/keyword_answer), `scorecard.py` (persistent `qm.ai-capability-scorecard/v1`, deterministic rebuild from ledger); plus **17 task specs** (`tasks/*.json`: 6 coding, 4 review, 4 research, 3 operations) each with seeded fixtures under `tasks/fixtures/<id>/`.
- **NEW `tools/strategy_farm/tests/test_ai_benchmark_scorers.py` (8.9 KB) + `test_ai_benchmark_scorecard.py` (4.2 KB)** — hermetic, no provider calls.
- **NEW `docs/ops/AI_CAPABILITY_BENCHMARK_2026-09.md`** (19 KB) — full contract: suite description, governor discipline, first-round results (above), grant rule (n≥8, correctness ≥0.80, zero constraint violations — **all cells NOT_EARNED_YET, no grant applied**), wave-2 router consumption plan, Antigravity relogin instructions.

Gap vs siblings: **no design report** (`design/b1_*_report.md` was never written) and no adversarial review. Recommendation for Fable: the work is substantively complete and its evidence checks out against live state, so "complete" is cheap — write the b1 design report, run one adversarial review pass (highest-risk areas: `providers.py` spawn paths/guard, the new shell-capable role's fencing, scorer vacuous-pass guards), fix findings, then apply. Abandoning is also defensible (directive §4 measurement contract exists in runnable form either way), but note the doc + scorecard are already cited by the wave-2 routing plan, and the kimi `benchmark_code` role config is inert until granted. There is no test-run residue in the worktree beyond `__pycache__` (exclude on commit).

## 5. Caveats

- The `i1` review states worktree HEAD == main HEAD; that was true at review time (`1617cb22be`), main has since advanced 3 commits (no slice-path overlap — verified via blob hashes).
- I did not re-run any test suites (read-only mandate); all test counts cited come from the adversarial reviews, which independently re-ran them.
- `git apply --3way` for ba6-4/ba6-6 will succeed textually, but the merged `strategy_wiki_sync.py` should get one full-suite re-run (26 tests per e1 review) before commit.
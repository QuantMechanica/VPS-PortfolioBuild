# Codex recycled Gemini EA review pass — 2026-08-24

- Lane: `codex`, `agents/board-advisor`
- Reviewed canonical HEAD: `d30c82bbb0539f36a8cfe16404c9d7eba696a6cc`
- Scope: the six equal-priority `review_ea` tasks returned by the deterministic router, in returned tie order
- Verdict: **all six FAIL and remain REVIEW; no pipeline handoff**
- Review timestamp: `2026-08-24T07:26:14Z`

The task payload names `code-review` and `gemini-output-review`; those skills are
not installed in this session. The canonical Codex review prompt, canonical JSON
schema, approved cards, current MQ5/EX5 files, registries, setfiles, prior review
evidence, and current static gates were reviewed directly.

## Identity result

No recycled candidate contains a new source or binary. Every current MQ5 and EX5
hash is identical to the hash reviewed on 2026-08-22 and to the supplied producer
artifact. Therefore the already closed RECYCLE defects are still present; the
current gates below independently reproduce the objective blockers.

| Router task | EA | MQ5 SHA-256 | EX5 SHA-256 |
|---|---|---|---|
| `9b0b37f8-087b-436e-a044-8b004c20905f` | `QM5_1409` | `deffd643e4c78e12f58960cca2568e737bd90542a235eca83777c531e8dfaeb3` | `71640fddfa134ae235543e034e67a72737a4fa83830b088219b28a23f4648979` |
| `25b11704-7145-4489-b8d3-0022f6ffb642` | `QM5_1416` | `26ca4c25b50083f61e5abaac1c1bee8ad46abc35a90f2f3bd0cff03355da7dc6` | `952736bf35972b55218820ac3fd69d902b1639753cc598fa1514241cb9c7ead1` |
| `c5811f96-bf23-490f-a8e5-2887606d16a7` | `QM5_1425` | `263e03e45c3b6fe16398919bfb51421f23462ef2e1737c1eca8c6571a434af14` | `7cae1f96a30910f832df2d726e89205d714f26edd1b1eaaf0cf9b8a3e60fc51d` |
| `ee8153cf-df11-47af-8157-aaf473ce8dbb` | `QM5_1612` | `b33a94addfe2c85ed3a8607aaeedd6a2c2305f1f045059857f5095eabefc824e` | `abeb9a5a07acb2d5250dcd65864bddfbc8f3c6754013fc7901334aab157e0d45` |
| `116de4eb-f236-4302-9622-dbfe69c49c1d` | `QM5_1408` | `38b9ddf1d6345c5a87fc2e3c359f94297b2de0d01230621d7e36cd967d336c74` | `d4a41c274c53c48ffae719c4103d8fa3c614798bfab76e381250b06b0fa292bb` |
| `98a8ca09-c712-4064-866e-cf7112c465d5` | `QM5_1417` | `8f01e6c78b44922626a70c79ba96d8479427c0267f9f1cd6e8a92d24fad1fc5c` | `a22a278f4526793299851a1f4557bcaa9ee3fceb411c14d26726e68051830ee9` |

## Current verification summary

| EA | Hardening | Guardrails | SPEC | Raw `CopyBuffer` | Set risk | Build/smoke contract | Verdict |
|---|---:|---|---|---:|---|---|---|
| `QM5_1409` | 17 failures | PASS, ceiling 336 | FAIL, sections 6/7 missing | 4 | 14/14 safe | incomplete identity; no smoke | FAIL |
| `QM5_1416` | 8 failures | PASS, ceiling 336 | PASS | 2 | 14/14 safe | invalid unsupported `deferred_p2_smoke`; `spec_md_path` missing | FAIL |
| `QM5_1425` | 5 failures | PASS, ceiling 336 | PASS | 3 | 14/14 safe | invalid deferral; current MQ5 newer than EX5; `spec_md_path` missing | FAIL |
| `QM5_1612` | 0 failures | PASS, ceiling 336 | superficial PASS; byte audit FAIL | 0 | 13/13 safe | incomplete identity; no smoke | FAIL |
| `QM5_1408` | 8 failures | PASS, ceiling 336 | FAIL, sections 6/7 missing | 2 | 14/14 safe | incomplete identity; no smoke | FAIL |
| `QM5_1417` | 6 failures | PASS, ceiling 336 | PASS | 2 | 14/14 safe | invalid deferral; current MQ5 newer than EX5; `spec_md_path` missing | FAIL |

The smoke section is `UNKNOWN` for each candidate because no governed smoke
summary was supplied. That is not being converted into a pipeline verdict.
The build-result section nevertheless fails: the three identity-only records
omit required canonical fields, while the other three use literal
`smoke_result="deferred_p2_smoke"` with an empty `blocked_reason`, which lacks
the schema-required tester-capacity evidence.

For `QM5_1425` and `QM5_1417`, the current source timestamps post-date the
existing binaries by hours. The prior governed forced-compile attempts were
refused by the include-mirror safety check while workers were active, and no
newer compile evidence exists. The producer boolean therefore does not bind
the current source hashes to those EX5 files.

## Candidate findings

### QM5_1409 — FAIL

The label-scoped gate reproduces missing MAE tracking, an incompletely
initialized entry request, six unsafe indicator-buffer/index accesses, a
30/30-minute news implementation versus the approved 480/480 minutes, news
blocking before management, and seven out-of-card symbols (including an
unapproved implicit `GER40.DWX` to `GDAXI.DWX` substitution). Direct
`CopyBuffer` calls also violate the Codex framework corset. The unchanged
manual blockers remain: the declared trimmed range bands are not implemented,
management/reuse state is not restart-safe, and per-symbol volume reliability
is unresolved.

### QM5_1416 — FAIL

The gate reproduces missing MAE tracking, incomplete request initialization,
two unsafe buffer reads, SELL-STOP replaced by market entry, inverted falling
SMA direction, a 30/30-minute rather than 180/180-minute news window, and news
blocking before management. Direct `CopyBuffer` calls violate the framework
corset. Mandatory pivot geometry and restart-safe TP/exit state remain absent.

### QM5_1425 — FAIL

The gate proves the descending-pivot ordering makes the nested entry path
unreachable, and reproduces BUY-STOP replaced by market entry, inverted rising
SMA direction, a 30/30-minute rather than 480/480-minute news window, and news
blocking before management. Direct `CopyBuffer` calls violate the framework
corset. The mandatory trough-significance and exactly-one-intervening-peak
contracts remain absent, and order/restart state remains unsafe.

### QM5_1612 — FAIL

Static hardening and forbidden-code checks pass, but the card contract does
not. The approved baseline names `USOIL.DWX`; neither that token nor the
governed `XTIUSD.DWX` symbol has a magic row or setfile. The required dynamic
2.5-times-20-day-median spread guard is replaced by a fixed points cap whose
default is zero (disabled), and the D1 zero-cross exit is evaluated on every
tick before `QM_IsNewBar` rather than once per completed D1 bar. `SPEC.md`
contains control bytes `0x07` at offset 77 and `0x1B` at offsets 106 and 2063;
the lightweight structural validator does not detect them. The identity JSON
also omits nine required build-result fields and supplies no smoke report.

### QM5_1408 — FAIL

The gate reproduces missing MAE tracking, incomplete request initialization,
two unsafe buffer reads, BUY-STOP replaced by market entry, inverted rising
SMA direction, a 30/30-minute rather than 180/180-minute news window, and news
blocking before management. Direct `CopyBuffer` calls violate the framework
corset. Mandatory pivot geometry, pending-order lifecycle, and restart-safe
management remain absent.

### QM5_1417 — FAIL

The gate reproduces both missing pending-stop entry types, both inverted SMA
directions, a 30/30-minute rather than 180/180-minute news window, and news
blocking before management. Direct `CopyBuffer` calls violate the framework
corset. Fabricated fallback trendlines, pre-acceptance state mutation, frozen
failure projections, restart-unsafe state, and the undeclared execution
contract remain unresolved.

## Commands and safety boundary

Focused checks used `build_gate_hardening.py --ea-label`,
`validate_build_guardrails.py --max-news-stale-hours 336`,
`validate_spec_doc.py`, SHA-256 identity checks, exact build-result schema
inspection, setfile risk inspection, registry/card comparison, and a byte-level
SPEC audit. No EA source, card, setfile, registry, resolver, EX5, terminal,
backtest, Q-phase state, T_Live setting, AutoTrading setting, risk threshold,
or news staleness ceiling was changed. No compile or pipeline verdict was
claimed.

# review_ea — QM5_33006 andrea-unger-turtle-soup-false-breakout

- Router task: `a87743e8-9ede-4b97-b4ce-e6206614d836` (review_ea, REVIEW, assigned codex)
- Source: build_ea by gemini/agy (`1da33eb5-...`), artifact `artifacts/qm5_33006_build_result.json`
- Worker verdict: `PASS: QM5_33006 build verified (SPEC PASS, guardrails PASS, sets generated for SP500/XTIUSD/EURUSD D1)`
- Reviewer: Claude review lane · 2026-08-24 · read-only DB

## Verdict: APPROVED (clean for Q02)

## Card-faithful mechanism
Card `strategy-seeds/cards/approved/QM5_33006_...md` = 20-day Donchian false-breakout fade (turtle soup), D1, TP at 10-day opposite extreme, SL at trigger-bar extreme ±2 ticks, stop-entry at breached extreme ±1 tick.
- Long: `low_1 < low_20 && close_1 > low_20` → BUY_STOP at `low_20 + buffer` (.mq5:157-176). Faithful.
- Short: `high_1 > high_20 && close_1 < high_20` → SELL_STOP at `high_20 - buffer` (.mq5:179-197). Faithful.
- 20-bar extreme computed with `iHighest/iLowest(..., strategy_lookback_period, start=2)` (.mq5:143-144) — correctly EXCLUDES trigger bar [1], the only interpretation under which the breach condition is satisfiable (card §2 formula i=1..20 with breach at [1] is internally contradictory; code resolves it correctly).
- TP 10-day extreme via `iHighest/iLowest(..., strategy_tp_lookback, start=1)` (.mq5:163,183); SL `low_1 - 2*buffer` / `high_1 + 2*buffer` (.mq5:166,186). Matches card §3.4.

## Declared strategy inputs — all wired (grep for use-sites)
- `strategy_lookback_period` .mq5:39 → used :143,144
- `strategy_tp_lookback` .mq5:40 → used :163,183
- `strategy_buffer_ticks` .mq5:41 → used :154 (buffer)
- `strategy_atr_period` .mq5:42 → used :112 (NoTradeFilter ATR)
- `strategy_spread_atr_mult` .mq5:43 → used :118
No dead inputs. All framework inputs (:11-36) consumed in OnInit/OnTick.

## Framework conformity
- QM_Common chain included (.mq5:6); QM_FrameworkInit/Shutdown wired (:245-259).
- MAE hook `QM_FrameworkTrackOpenPositionMae()` in OnTick (:264).
- Kill switch, news filter (QM_NewsAllowsTrade2 + legacy), Friday close all wired (:265-277).
- Magic via `QM_FrameworkMagic()` / MagicResolver; registry `magic_numbers.csv` rows 33006 slots 0/1/2 → 330060000/1/2 (SP500/XTIUSD/EURUSD) = ea_id*10000+slot. Resolver `QM_MagicResolver.mqh` contains all three magic values + ea_id 33006 x3 (consistent).
- Bounded indicator access: iHigh/iLow/iHighest/iLowest + QM_ATR, all perf-allowed-annotated behind QM_IsNewBar D1 gating; no raw series arrays, no leaked handles, no ML, no invented commission/swap.
- RISK_FIXED=1000 backtest / RISK_PERCENT=0.5 live inputs present; setfiles RISK_FIXED=1000 RISK_PERCENT=0 env=backtest.

## Build evidence (current source hash)
- `.mq5` sha256 = `6f5169975874...800acc0c` — MATCHES artifact `mq5_sha256`.
- Compile log `framework/build/compile/20260817_190816/`: `Result: 0 errors, 0 warnings` → COMPILE_OK for current hash.
- 3 setfiles via governed generator (build_hash + card_defaults_source headers), magic_slot 0/1/2 correct per symbol.

## Non-blocking notes (not RECYCLE)
1. Card §3.1(3) daily-loss 2% circuit breaker not implemented in Strategy_NoTradeFilter — FTMO overlay / kill-switch territory, not a Q02 mechanism blocker.
2. Card cadence "80-160/yr" vs conservative prior 25/yr/symbol — Q02 measures reality (activity criterion applies downstream).

Clean for Q02.

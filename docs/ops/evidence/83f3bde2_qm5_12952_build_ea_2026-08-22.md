# QM5_12952 Build EA Evidence — 2026-08-22

**Task ID:** `83f3bde2-3bfe-44cf-998d-ca623626a6d5` (`build_ea`, priority 50, agent `gemini`).  
**EA:** `QM5_12952_mql5-force-ema-card`.  
**Artifact Path:** `C:/QM/repo/docs/ops/evidence/83f3bde2_qm5_12952_build_ea_2026-08-22.md`.  
**Strategy Card:** `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12952_mql5-force-ema-card.md` (G0 APPROVED).  

---

## 1. Strategy Overview & Implementation

- **Strategy Thesis**: Mohamed Abdelmaaboud (2022-08-03, MQL5 Articles) Force Index EMA Signal (H1).
  - Force Index calculation using closed-bar tick volume and 13-period EMA smoothing.
  - Trend filter: Close vs EMA(100).
  - Volatility filter: ATR(14) >= 50% of ATR(100).
  - Entry: Long when Close > EMA(100) and Force Index crosses above 0 (prev < 0, curr > 0); Short when Close < EMA(100) and Force Index crosses below 0 (prev > 0, curr < 0).
  - Stop Loss: 1.6 * ATR(14) from entry.
  - Profit Target: 2.0R from initial stop risk distance.
  - Exit: Opposite Force Index / EMA cross or failsafe 36 H1-bar time stop.
- **Framework Compliance**:
  - Implemented cleanly using `QM_Common.mqh` and standard closed-bar gating `QM_IsNewBar()`.
  - News temporal mode (`QM_NEWS_TEMPORAL_PRE30_POST30`) and compliance (`QM_NEWS_COMPLIANCE_DXZ`) with `qm_news_stale_max_hours = 336`.
  - Sizing handled strictly via framework risk model (`RISK_FIXED = 1000.0`, `RISK_PERCENT = 0.0`).

---

## 2. Registry & Artifact Verification

- **Registries**:
  - `framework/registry/ea_id_registry.csv`: verified row `12952,mql5-force-ema-card`.
  - `framework/registry/magic_numbers.csv`: added 3 registered slots (0: EURUSD.DWX, 1: USDJPY.DWX, 2: GER40.DWX).
  - `framework/include/QM/QM_MagicResolver.mqh`: regenerated cleanly via `update_magic_resolver.py`.
- **SPEC Document**:
  - `framework/EAs/QM5_12952_mql5-force-ema-card/SPEC.md` written and validated via `validate_spec_doc.py` (`PASS`).
- **Static Checks & Guardrails**:
  - `validate_build_guardrails.py`: `PASS` (0 findings).
  - `validate_symbol_scope.py`: `SINGLE_SYMBOL_OK` (0 violations).
- **Setfiles**:
  - Generated all 3 backtest setfiles in `sets/` via `gen_setfile.ps1` (`EURUSD.DWX`, `USDJPY.DWX`, `GER40.DWX`).

---

## 3. Build Result & Router State

- Moving task `83f3bde2-3bfe-44cf-998d-ca623626a6d5` to `REVIEW` for mandatory Codex review.

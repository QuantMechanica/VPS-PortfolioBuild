# Governor v2 (runbook gap G6): design, activation gate, DISABLED action adapter

Date: 2026-09-03 20:15Z (CEO, Opus workflow wf_36c785e8-af5; adversarially verified ok).

Delivered code: `tools/strategy_farm/account_governor_action_adapter.py` (DISABLED decision-to-instruction boundary + enforce-mode receipt; never connects to MT5; CLI hardcodes executor=None so enforce always refuses) and its 12 tests. No live-action path exists (verifier constructed the strongest case: valid OWNER_SIGNED stage-2 policy + valid activation artifact + level-3 decision -> ENFORCE_REFUSED / no_execution_adapter_present, nothing written).

## Contract summary

SP-C1 = docs/ops/ACCOUNT_PORTFOLIO_GOVERNOR_CONTRACT_2026-08-22.md (router task 5c02a347-…, line 5). Status: "design implemented behind a dry-run-only boundary; not authorized for live execution" (line 7).

Governs the WHOLE broker account, not an EA registry: every open position and pending order is in scope; magic numbers are attribution metadata, never an inclusion filter (contract:10-14) — necessary because T1-T10 and T_Live share account 4000090541 and account-level rows can be mirrors (CLAUDE.md OQ-17).

MONITOR CADENCE + INPUTS. One read-only producer, QM_AccountMonitor.mq5 v1.10, writes one atomic snapshot on its existing timer (contract:35). In-tree: framework/monitor/QM_AccountMonitor.mq5 — #property version "1.10" (mq5:33), input InpTimerSeconds=60 (mq5:42), OnInit arms nothing without OWNER wiring (mq5:28), WriteAllAtomic tmp→rename to <terminal>\MQL5\Files\QM\journal\account_snapshot.json (mq5:800-801). Schema qm.account-monitor.snapshot/v2 (mq5:759; contract:32). The primary live-account read path is that account_snapshot.json — the SAME file the equity backstop live_book_dd_guard.py reads (live_book_dd_guard.py:41-42, C:\QM\mt5\T_Live\MT5_Base\MQL5\Files\QM\journal\account_snapshot.json). morning_brief.py consumes the derived surfaces (LIVE_BOOK_PULSE_STATE morning_brief.py:113, DDGUARD_STATE :112, DEPLOY_POINTER :121); live_book_pulse.py PRODUCES D:\QM\reports\state\live_book_pulse.json from T_Live journals (live_book_pulse.py:31,35). Contract §"Equity freshness backstop" (contract:104-113) explicitly moved dd-guard OFF the event-driven live_book_pulse.json equity onto the timer snapshot, 180 s max age, login-checked, wrong/stale/future = BLIND — so the governor's authoritative input is the atomic v2 snapshot, not the pulse.

SLEEVE-LEVEL EXPOSURE. The contract's ENFORCED aggregates are ACCOUNT-WIDE only: gross leverage = Σ|notional|/equity (contract:63), net directional (:64), currency-net buckets on broker base/profit currency (:67-69), planned loss at stop via OrderCalcProfit (:71-72). Sleeve-level is attribution ONLY: each recognized position carries magic passthrough (governor `recognized_positions`, magic field ~line 343) resolvable to a sleeve via framework/registry/magic_numbers.csv (columns ea_id,ea_slug,symbol_slot,symbol,magic,…status). There is NO per-sleeve ceiling in SP-C1 — magic never gates inclusion (contract:12-13).

ACCOUNT-WIDE LIMITS ENFORCED. Exactly four, all threshold-free until an OWNER policy supplies them (governor _policy_thresholds account_portfolio_governor.py:371-386): min_free_margin_account, max_gross_leverage, max_abs_currency_net_leverage, max_planned_stop_loss_account. Thresholds are never embedded; accepted only from a raw-file SHA-256-bound policy status:OWNER_SIGNED with matching account/validity (contract:98-101).

ATOMIC PRE-TRADE ACTION SEMANTICS (staged, contract:87-90): L0 CLEAR (complete snapshot + valid bound policy + no breach); L1 ENTRY_FREEZE_* (any uncertainty or no valid policy → freeze new entries, fail-closed default, no invented thresholds); L2 PENDING_CANCEL_AND_ENTRY_FREEZE (a policy threshold breached → freeze + list every pending ticket to cancel; needs policy authorizing stage 2); L3 CONTROLLED_FLATTEN_AUTHORIZED_DRY_RUN (stage 2 PLUS a separate independently-hash-bound, time-limited OWNER emergency policy tied to the trigger policy + incident ID → also list every open ticket to flatten). Stage 3 is unreachable with the ordinary policy alone (contract:92-96); emergency schema qm.account-governor.emergency-policy/v1 must bind the SHA-256 of the triggering stage-2 policy.

EVIDENCE/RECEIPT OUTPUTS + MODES. The consumer independently recomputes producer aggregates and treats drift as uncertainty (contract:75, governor analyze_snapshot at :178). Output schema qm.account-governor.dry-run/v1 always carries dry_run:True, execution_adapter_present:False, action_plan.actions_executed:[] (governor :479,:484-485). There is NO apply mode (contract:23; governor CLI refuses without --dry-run at :524). "Enforce mode" is not implemented anywhere today — the contract's stage-2 execution adapter is "a separate ROT implementation and review; none exists here" (contract:125).

## Existing components

All under tools/strategy_farm/ (the governor lives at the package top level, NOT under portfolio/):

1. account_portfolio_governor.py (589 lines, commit 593c9ddcad "Harden account portfolio governor dry run"). The pure stateless evaluator. analyze_snapshot() (:178) validates schema/login/freshness/finite metrics/write_ok/unique tickets/reconciliation and recomputes gross+net notional, currency-net buckets, planned stop loss, unpriced/uncovered counts; drift vs producer = uncertainty. evaluate() (:389) does the staged escalation. _policy_thresholds() (:371) requires the 4 named limits and stage2_cancel_pending_authorized==True (:384). Emergency gate: flatten_authorized (:458), trigger_policy_sha256==policy.sha256 (:460), incident_id present. action_plan.actions_executed always [] (:479); dry_run True / execution_adapter_present False (:484-485). CLI hard-requires --dry-run (:524). DEFAULT_SNAPSHOT = C:/QM/mt5/T_Live/…/account_snapshot.json (:32-34). Test: tests/test_account_portfolio_governor.py.

2. framework/monitor/QM_AccountMonitor.mq5 v1.10 — SOURCE ONLY, not compiled/attached/deployed (contract:26-28). Emits the v2 snapshot (mq5:759), direct PositionsTotal/PositionGetTicket + OrdersTotal/OrderGetTicket enumeration, no MagicAllowed filter, OrderCalcProfit conversion (contract:51-58). The .ex5 in-tree is the artifact of a build but the LIVE terminal still runs the unversioned v1 shape (evidence 2026-08-22 §"Staged reconciliation proof": live snapshot is LEGACY_UNVERSIONED → governor correctly returns ENTRY_FREEZE_UNCERTAINTY).

3. live_book_dd_guard.py — the equity freshness backstop already hardened by 593c9ddcad: reads the timer snapshot (:41-42), QM_BOOK_DD_MAX_ACCOUNT_SNAPSHOT_AGE_SEC default 180 (:57-58), QM_BOOK_DD_EXPECTED_ACCOUNT_LOGIN default 4000090541 (:60-61), 10% DD unchanged, writes only the single portfolio_dd.signal on breach (:54). Test: tests/test_live_book_dd_guard.py.

4. TWO duplicate SP-C6 recurring dry-run wrappers (a cleanup item, not two functions):
   (a) governor_dry_run_watch.py (303 lines, commit 925e9f4b73, documented in evidence 2026-08-23). Persisted-state watcher: STATE_JSON/GUARD_LOG/HISTORY_JSONL under D:\QM\reports\state (:51-54), shared health_alarms.log at D:\QM\strategy_farm\state (:53). run_once() never raises → ENTRY_FREEZE_WATCHER_ERROR fail-closed (:166-199); level transitions → ALARM (increase)/RECOVERY (decrease) (:204-224); --status prints state; --dry-run required. Test: tests/test_governor_dry_run_watch.py.
   (b) account_portfolio_governor_recurring_dry_run.py (219 lines, commit 63bf0ba2dd). Pure-function build_record() (:52), append-only JSONL journal D:\QM\reports\state\account_portfolio_governor_recurring_dry_run.jsonl (:39-40), _LEVEL_SEVERITY map (:44-49), no persisted latch (each run re-derived). Test: tests/test_account_portfolio_governor_recurring_dry_run.py.

5. Policy schema constants live in the evaluator (POLICY_SCHEMA/EMERGENCY_SCHEMA governor:30-31); the FTMO governor policy shape (portfolio/ftmo_governor_policy.py, _v2.py; artifacts/ftmo_governor_policy_golden_2026-07-*.json) is a sibling reference, not the account-governor policy.

6. Evidence + gate wiring: docs/ops/evidence/2026-08-22_account_portfolio_governor_dry_run.md (PASS_DRY_RUN), 2026-08-23_sp_c6_governor_dry_run_watch.md (IMPLEMENTED, not scheduled). risk_freeze.py LIFT_CONDITIONS carries GOVERNOR-HARDENING id (:61), requirement "account/portfolio governor hardened AND actually enforcing" (:62), status PARTIAL blocked_by "v2 monitor deploy and action adapter remain OWNER/ROT-gated and are not live" (:63-64).

Confirmed absent: any Python order-send / cancel / flatten / apply path. The Python governor has no MT5 connection; the only matches for OrderSend/PositionClose in tools/strategy_farm are the self-describing "never sends an order" comments in the governor files themselves.

## Gaps (what G6 still needs)

G6 (docs/ops/BOOK_CEREMONY_RUNBOOK_2026-09.md:352-355): "Governor v2 not enforcing (freeze lift GOVERNOR-HARDENING). SP-C1 is approved + dry-run-proven (commit 593c9ddca) but the v2 monitor deploy + action adapter are OWNER/ROT and not live. → Router task (Codex, ops_issue, prio 65): finish the v2 monitor + atomic account-wide pre-trade action adapter; OWNER-gated activation." It is one of THREE freeze-lift conditions (runbook:118-121) whose verbatim lift rule is "All three conditions met AND an explicit written OWNER lift. No AI seat lifts this freeze, and no seat lifts it by inference from a condition merely being satisfied."

Two missing pieces, per the task framing:

MISSING 1 — the v2 monitor as a scheduled/looped process. Today the recurring wrappers are CAPABILITY only: evidence 2026-08-23 §"Deployment boundary — not scheduled yet" states no Windows Scheduled Task is registered. And the LIVE terminal still runs the unversioned v1 monitor (the v1.10 source is not compiled/deployed — contract:26-28), so even a scheduled watcher reads a LEGACY_UNVERSIONED snapshot and correctly stays at L1. So "finish the v2 monitor" = two sub-gaps: (a) OWNER/ROT governed compile + deploy of QM_AccountMonitor.mq5 v1.10 into T_Live so a real v2 snapshot exists (contract "Required gates" item 1, :119-121; ROT: never start/change an active terminal by an AI seat), and (b) register ONE canonical recurring dry-run process as a scheduled task (mirroring QM_StrategyFarm_LiveBookDDGuard, 5 min) — after collapsing the two duplicate wrappers into one.

MISSING 2 — the atomic account-wide pre-trade action adapter. Nothing that can ACT exists (contract:125 "a separate ROT implementation and review; none exists here"). This is the enforce-mode component: (a) enforce the L1 entry-freeze as an account-wide pre-trade gate every EA/slot consults before opening (the account is shared, so one atomic decision file must reach all of T1-T10 + T_Live, analogous to the existing QM\halt\portfolio_dd.signal convention live_book_dd_guard.py:54); (b) execute the L2 pending-cancel and L3 flatten ticket lists. (b) requires actual MT5 trade operations, which the Python governor cannot and must not perform — the order-management executor is a ROT MQL-side (or MT5-bridge) component, out of this design's write scope. The adapter I design is the DISABLED decision→instruction boundary plus the enforce-mode receipt, never the order sender.

Not a gap (explicitly): sleeve-level ceilings — SP-C1 enforces only the four account-wide limits; sleeve is attribution metadata (magic→magic_numbers.csv), so no new limit is owed.

## Activation gate (exact, fail-closed)

The contract names NO machine env var (grep of the contract for env/machine/environ/activation = none; and the governor has no getenv-based apply path). The exact gate is a signed OWNER policy FILE, in three tiers, all fail-closed:

1. ORDINARY POLICY (thresholds + entry-freeze/L2): a raw file, SHA-256-bound, schema qm.account-governor.policy/v1, status:"OWNER_SIGNED", authorized_by starts "OWNER", account_login==4000090541, inside [valid_from_utc, valid_until_utc], carrying the four thresholds, and stage2_cancel_pending_authorized:true for L2 (contract:98-101; enforced governor:371-386 and _load_bound_policy:120-142 requiring the caller-supplied --trusted-policy-sha256 to match the file's actual SHA-256). Absent this, evaluate() pins L1 ENTRY_FREEZE_POLICY_UNBOUND.

2. EMERGENCY POLICY (L3 flatten only): a SEPARATE file, schema qm.account-governor.emergency-policy/v1, independently hash-bound, time-limited, flatten_authorized:true, incident_id set, and trigger_policy_sha256 == the exact SHA-256 of the tier-1 policy (contract:92-96; governor:458-466). Stage 3 is unreachable with the ordinary policy alone.

3. FREEZE-LIFT / GO-LIVE: even with (1)+(2), enforce may not run until risk_freeze GOVERNOR-HARDENING (risk_freeze.py:61-64) plus the other two lift conditions are met AND there is "an explicit written OWNER lift" — "No AI seat lifts this freeze, and no seat lifts it by inference from a condition merely being satisfied" (runbook:120-121).

My design adds a fourth belt-and-suspenders local gate for the NEW action adapter: an OWNER activation artifact under decisions/ (e.g. decisions/YYYY-MM-DD_account_governor_enforce_<incident>.md) that the adapter must find and whose hash must bind the tier-1 policy before enforce is even considered. NONE of these four artifacts exists today (the live snapshot is still LEGACY_UNVERSIONED, no policy file is bound in production, the freeze is ACTIVE armed 2026-08-31 OWNER-DEC-RISK-FREEZE). So the governor stays at L1 fail-closed, exactly as designed. Contract "Required gates" items 1-6 (:116-131) additionally require the ROT monitor compile/deploy (item 1), multi-interval live v2 reconciliation (item 2), OWNER-recorded policy hash+window (item 3), a separate ROT stage-2 adapter review (item 4), a fresh emergency policy per incident (item 5), and T_Live/AutoTrading staying OWNER-only (item 6).

## Design

All new Python lives under tools/strategy_farm/ (where account_portfolio_governor.py and both wrappers already sit), NOT under portfolio/. Everything ships DISABLED: enforce mode is unreachable unless BOTH an OWNER_SIGNED SHA-256-bound policy is bound AND a NEW explicit OWNER activation artifact under decisions/ is present (see activation_gate). No such artifact exists today → the adapter's only reachable behavior is dry-run, identical to the current evaluator.

MODULE LAYOUT (proposed, to be built by Codex under G6 — not created here):

1. Consolidate the two duplicate SP-C6 wrappers into ONE canonical looped process. Recommend keeping governor_dry_run_watch.py (it has persisted transition state + --status + is the evidence-documented SP-C6) and retiring account_portfolio_governor_recurring_dry_run.py, OR vice-versa — but two processes writing the same health_alarms.log and reports/state is an operational hazard. Register QM_StrategyFarm_GovernorDryRunWatch (SYSTEM, 5 min, `--dry-run --expected-login 4000090541`), mirroring QM_StrategyFarm_LiveBookDDGuard. This scheduling is read-only (no live action) but is an OWNER-noted infra change, gated on the v1.10 monitor deploy (until then every run is L1 by design).

2. NEW tools/strategy_farm/governor_action_adapter.py — the atomic account-wide pre-trade action adapter, DISABLED. Interface: consume one governor.evaluate() result (unchanged evaluate(), no new authority) + a required --mode {dry-run|enforce} defaulting to dry-run + the same --policy/--trusted-policy-sha256/--emergency-policy hash bindings. In dry-run (default, always available): emit the decision and would_cancel/would_flatten ticket lists to a receipt; write NOTHING any EA or terminal reads; actions_executed stays []. In enforce (must stay OFF): refuse-closed unless (i) an OWNER_SIGNED policy is bound and validates via the existing evaluate() gates AND (ii) the decisions/ activation artifact is present and hash-binds this exact policy; even then the adapter's ONLY act is to atomically publish the account-wide decision (entry-freeze flag; cancel/flatten instruction manifest) tmp→rename so no consumer ever reads a half-written state — and it hands cancel/flatten to the ROT MQL executor rather than sending orders itself. The atomicity requirement mirrors the producer's WriteAllAtomic (mq5:800-801).

3. The actual order-send/cancel/close executor and the T_Live-side freeze-signal writer are ROT MQL / T_Live components — out of this design's file scope (writing under C:/QM/mt5/T_Live is a hard no-write; enabling live action is OWNER-only).

STATE / RECEIPT FILES (written ONLY in enforce mode, which stays off):
- D:/QM/reports/governor/enforce_receipts/<incident_id-or-UTCts>.json — one atomic receipt per enforce action (schema qm.account-governor.enforce-receipt/v1: bound policy SHA-256, emergency policy SHA-256, incident_id, decision level/name, cancelled/flattened ticket lists, activation-artifact path+hash, actions_executed actually populated). Deliberately NOT under D:/QM/reports/state (that subtree is the pipeline-state surface and is a hard no-write here) and NOT under decisions/. In dry-run these are never written.
- Dry-run journals stay where they already are (governor_dry_run_watch: D:/QM/reports/state/governor_dry_run_watch_{state.json,.log,_history.jsonl}; shared alarms D:/QM/strategy_farm/state/health_alarms.log). Design note/recommendation: the enforce receipts must NOT reuse reports/state, and ideally the dry-run journals migrate out of reports/state too, since it collides with the pipeline-state no-write zone.
- The account-wide entry-freeze signal that EAs poll = the T_Live sandbox halt dir (QM\halt\, live_book_dd_guard.py:54 convention). That write is ROT / OWNER — the Python adapter must never write there.

## Test plan

All fixture-based, no MT5, no live account, no scheduled-task registration. Runner: C:/Python311/python.exe -m pytest -q <files>. After any run: `git status --short framework/calibrations/` MUST be empty; if a QM5_9993 autostub appears, `git checkout -- framework/calibrations` and state so. (Verified today: baseline run left calibrations clean.)

BASELINE (must stay green): tools/strategy_farm/tests/test_account_portfolio_governor.py, test_account_portfolio_governor_recurring_dry_run.py, test_governor_dry_run_watch.py, test_live_book_dd_guard.py → today 21 passed in 2.74s.

NEW governor_action_adapter tests (tools/strategy_farm/tests/test_governor_action_adapter.py) — the whole suite is a proof that enforce stays inert:
1. Default mode = dry-run: actions_executed==[], no enforce receipt written, no signal file written, exit 0 (mirror governor CLI --dry-run guard governor:524).
2. --mode enforce WITHOUT --dry-run acknowledgement OR without a bound OWNER_SIGNED policy → refuse-closed, actions_executed==[], no receipt.
3. --mode enforce WITH a valid tier-1 policy but WITHOUT the decisions/ activation artifact → refuse-closed (the fourth-gate assertion).
4. Fixture L2 breach (trivially low ceiling, as in evidence 2026-08-23): plan lists would_cancel pending tickets, but with no activation writes nothing an EA reads.
5. Fixture L3 requires an emergency policy hash-bound to the tier-1 policy (reuse the evaluate() gates governor:458-466): a stage-2 breach with an ordinary policy only stays L2, would_flatten==[].
6. Atomicity: the (mocked, temp-dir) enforce receipt is written tmp→rename; simulate a fault before rename and assert no partial receipt and no signal remain.
7. Fail-closed on bad/stale/wrong-login/LEGACY_UNVERSIONED snapshot → L1 ENTRY_FREEZE_*, never CLEAR, never a cancel/flatten list (extends the analyze_snapshot uncertainty tests).
8. Magic-independence preserved: a 3-position fixture (registered magic, magic 0, unregistered 999) is fully enumerated (contract:12-13) and, if sleeve attribution is added, resolves against a magic_numbers.csv fixture WITHOUT ever gating inclusion.

Focused command: C:/Python311/python.exe -m pytest -q tools/strategy_farm/tests/test_account_portfolio_governor.py tools/strategy_farm/tests/test_governor_action_adapter.py tools/strategy_farm/tests/test_governor_dry_run_watch.py tools/strategy_farm/tests/test_live_book_dd_guard.py -p no:cacheprovider. Then re-check framework/calibrations/ clean.

Preserve CRLF byte-exactly and ASCII-only in every tools/strategy_farm/*.py touched (the whole tree is CRLF).

## Risks

LIVE-ACCOUNT PATHS (the ones that matter most):
- The ONLY path that can touch the live account is enforce mode + the ROT MQL executor. It must ship DISABLED and default to dry-run; no default, no env, no inference lifts it (contract:98-101; runbook:120-121). The Python adapter has no MT5 connection and must never gain one — cancel/flatten stays MQL-side ROT.
- SHARED ACCOUNT BLAST RADIUS: T1-T10 + T_Live are the same Darwinex-Live login 4000090541 (CLAUDE.md OQ-17). A stage-3 flatten enumerates ALL account positions with no magic filter (contract:12-13,51-54) — it would close the real live-book positions, and any account-level position the shared login shows, not a subset. That account-wideness is intended but is exactly why L3 is emergency-policy + incident-scoped + OWNER-lift gated. Any per-magic "only my sleeve" carve-out would violate the contract and must be refused.
- STALE / WRONG-LOGIN / LEGACY SNAPSHOT must fail closed to entry-freeze, never CLEAR (contract:80-82; already enforced by analyze_snapshot uncertainties and the dd-guard BLIND handling contract:110-113). A regression here that let a bad snapshot read CLEAR could green-light enforce — the fail-closed tests above guard it.
- FREEZE-SIGNAL WRITE LOCATION: the account-wide entry-freeze that EAs poll is the T_Live QM\halt\ dir — writing under C:/QM/mt5/T_Live is a hard no-write and OWNER/ROT. Design keeps that write out of Python scope.

OPERATIONAL / HYGIENE:
- DUPLICATE WRAPPERS: two SP-C6 processes (governor_dry_run_watch.py and account_portfolio_governor_recurring_dry_run.py) both append health_alarms.log and both write reports/state — scheduling both would double-log and race. Consolidate to one before registering a task.
- REPORTS/STATE COLLISION: existing dry-run journals default under D:/QM/reports/state (governor_dry_run_watch.py:51-54; recurring_dry_run.py:39-40), which is the pipeline-state surface and a hard no-write zone for this work. Enforce receipts must go to a NON-state subtree (proposed D:/QM/reports/governor/); recommend migrating the dry-run journals off reports/state too.
- v1.10 MONITOR DEPLOY is a ROT compile + deploy of a terminal binary into T_Live; terminal64.exe is transient and never started manually by an AI seat (CLAUDE.md). Until it is deployed, any scheduled watcher truthfully reads LEGACY_UNVERSIONED and stays L1 — do not "fix" that by relaxing the v2-schema check.
- GATE INTEGRITY: this design must not touch framework/registry/dxz23_execution_contracts.json, gate thresholds, verdict logic, live_risk_freeze.json, deployment pointers, or decisions/ — the only decisions/ interaction is READING an OWNER activation artifact that does not yet exist. No thresholds are invented anywhere; every limit comes from an OWNER_SIGNED policy at runtime (contract:98-101).

## Verifier verdict

ok=True

- NON-MATERIAL (advisory, not blocking): DEFAULT_ACCOUNT_LOGIN = 4000090541 (account_governor_action_adapter.py:89) is dead code -- defined but never referenced anywhere in the module. It is an account identity (CLAUDE.md OQ-17, matches live_book_dd_guard default), not a risk threshold, so no correctness/safety impact; suggest removing or wiring it as the expected_login default.
- NON-MATERIAL (advisory): the 5-second future-clock-skew tolerance `if age_seconds < -5` (line 182) is an invented literal not traceable to the contract. It only governs whether a `decision_from_future` freshness reason is appended (lenient by 5s); it never enables a live action and, in enforce mode, staleness only causes MORE refusals. No safety impact.
- NON-MATERIAL (future-ROT note): the enforce-activation artifact binds only the stage-2 `trigger_policy_sha256` (load_activation, line ~223); it does not independently bind the stage-3 emergency-policy hash. For level-3 flatten, upstream governor.evaluate is responsible for validating the emergency policy before it emits a level-3 action_plan, so the flatten ticket list cannot appear without that upstream check. Acceptable for a DISABLED skeleton per contract gate #4 (executor is a separate future ROT component), but the eventual ROT executor review should confirm emergency-policy binding at the action boundary too.
- NON-MATERIAL (transparency): with the opt-in `--apply-dry-run-receipts` flag (default OFF), write_receipt writes an evidence RECEIPT (not a live action) to DEFAULT_RECEIPT_DIR=D:/QM/reports/state (line 361). This is a JSON receipt sink, not an account action; the docstring discloses it. Default dry-run writes nowhere unless --out is given.

REFUTATION FAILED -- implementation is sound and ships DISABLED. Merge: ff-only clean; worktree at board-advisor tip cb8acbff (report's BEFORE a92cda60 / AFTER dee73200 are now ancestors, consistent). Only the two claimed files exist (adapter 557 lines, test 454 lines), both CRLF-consistent and ASCII-only; git status shows just those two untracked files; `git status --short framework/calibrations/` empty (no QM5_9993 autostub, no revert needed); no commit, no farm-DB write, no writes under T_Live / D:/QM/reports/state / decisions/. Tests: 12/12 pass (0.77s). NO LIVE-ACTION PATH PROVEN via two independent gates: (a) activation binding and (b) executor presence. main() hardcodes executor=None (line 534), so I constructed the strongest adversarial case -- a fully valid OWNER_SIGNED stage-2 policy + valid enforce-activation artifact bound to its exact SHA-256 + both env vars set (QM_ACCOUNT_GOVERNOR_ENFORCE_ACTIVATION[_SHA256]) + a fresh level-3 CONTROLLED_FLATTEN decision -- and the CLI still refused: outcome=ENFORCE_REFUSED, refusal=no_execution_adapter_present, executed=False, actions_executed=[], rc=3, and NO file was written to the T_Live halt path or D:/QM/reports/state. No enforce-activation artifact exists in decisions/ today and the env vars are unset in production, so enforce is structurally unreachable. No production module imports the adapter with a real executor (only the test + self). Idempotency/atomicity independently proven with my own executor: identical decision -> identical SHA-256 decision_id -> executor.apply called twice but exactly ONE real account action (ENFORCE_APPLIED then ENFORCE_APPLIED_IDEMPOTENT_NOOP); a new snapshot tick yields a distinct decision_id (one-action-per-tick). Numeric literals justified: 180.0 <- contract:104-113 equity-freshness backstop; 90.0 <- matches governor CLI default (account_portfolio_governor.py:510) and recurring dry-run default; the only unsourced literals (dead 4000090541, -5s skew) are non-thresholds with no safety impact. Contract SP-C1 mapping: level table (freeze L1 / cancel-list L2 / flatten-list L3) honored by faithful passthrough of the evaluator's action_plan; gate #4 (execution adapter = separate ROT, none exists) met; thresholds not embedded; AutoTrading never touched (gate #6). No import-time I/O; no MT5/sqlite/OrderSend/socket references. ok=true: no material defect and no path that can act on the live account without a nonexistent OWNER activation artifact AND an injected executor.

## OWNER / ROT items that remain (not for any AI seat)

1. Governed compile + deploy of QM_AccountMonitor.mq5 v1.10 into T_Live so a versioned v2 snapshot exists (contract required gate 1).
2. Register ONE canonical recurring dry-run monitor as a scheduled task (5 min, mirroring QM_StrategyFarm_LiveBookDDGuard) after collapsing the duplicate wrappers.
3. The order-management executor (L2 cancel / L3 flatten) is a separate ROT MQL-side or MT5-bridge component with its own review; the adapter refuses without it.
4. Signed OWNER policy file (tier 1), emergency policy (tier 2), enforce-activation artifact under decisions/ (tier 4), and the written GOVERNOR-HARDENING freeze lift.

# QM5_21001 Q15 challenger Q02 infrastructure recovery

Date: 2026-08-16
Scope: append-only Q02 recovery; no EA logic, live manifest, portfolio gate, T_Live, or AutoTrading change

## Selection

The unbuilt high-diversity cards inspected for this fleet unit were not suitable
non-duplicate builds. In particular, QM5_40002 repeats the D1 FX momentum basket
already implemented by QM5_1111 and QM5_10717, while the reviewed basket-dispersion
and OU/cointegration cards overlap existing market-neutral families. The unit was
therefore routed to mission priority 2: recover a distinct USDJPY challenger stuck
at Q02 for infrastructure-only reasons.

## Diagnosis

The frozen Q15 exit challenger is `QM5_21001_balke-gmt3-range-breakout-exit-c1`,
USDJPY.DWX H1, opt card
`OPT-13213-USDJPY-EXIT-SURGERY-1e2bb8e4c42f21f7`.

- Q15 work item `161e130a-8e17-5cde-8c3d-ad062bae5917` is terminal
  `CHALLENGER_SPAWNED` and binds
  `D:\QM\reports\opt_track\OPT-13213-USDJPY-EXIT-SURGERY-1e2bb8e4c42f21f7\freeze_addendum.json`.
- The freeze addendum SHA-256 is
  `cbe481f13c543b54a76db3c1ab6097e0fe0c44e6bb51c3e181b70373de41a405`,
  exactly matching the Q02 seed payload.
- Q02 work item `545caffe-8e28-5bed-b49c-1f75af70192c` is preserved as
  `failed / INFRA_FAIL`. It refused before tester spawn with
  `staged_ex5_sha256_mismatch_before_spawn`; its recorded source, required,
  pre-run, and expected EX5 identities all bind the same challenger hash.
- A later Q02 PASS, `2674c604-e66c-4c88-84d8-1c78e04ea893`, binds EX5
  `854e364dbbe8e860d7e4e12ebe8fb71b261d25f5aa08ed51fb9ef6367e5129ca`.
  It is a different binary and cannot disposition this frozen challenger.

Current authenticated execution identity:

| Artifact | SHA-256 |
|---|---|
| EX5 | `46c1075a921db25a6ba61712ba76b10a89ea7e2a9a4f52472ab49f57f65e4944` |
| MQ5 | `0ff3ea868a08d800b2184a5d828776e80527a4e9b51927f40ab5292656fd9820` |
| USDJPY H1 backtest setfile | `1d78ada3a3e74a5f811febbfb8120590659099c8f97884e4e79ee9f75dea3ecf` |

The setfile contract is `RISK_FIXED=1000` and `RISK_PERCENT=0`.

## Repair

`farmctl` now accepts the Q15 freeze addendum as rerun evidence only when all of
the following are authenticated: the exact pre-spawn refusal reason, Q15 row ID,
Q15 development identity, EA/symbol/setfile equality, terminal Q15 verdict,
card/Q02 back-reference, evidence path, and freeze SHA-256. Missing or tampered
freeze evidence fails closed. Ordinary null-evidence Q02 INFRA rows remain
ineligible.

The same-setfile duplicate guard now compares EX5 identity. An existing
non-infrastructure result still blocks an identical binary (and legacy unbound
rows remain conservative blockers), but a result from a different sealed EX5
does not mask the current challenger.

## Enqueue receipt

The governed append-only enqueue created Q02 work item
`c745b7e6-f619-457a-bd45-1da4aa34e6d7` at
`2026-08-16T00:37:54+00:00`.

- Source row preserved: `545caffe-8e28-5bed-b49c-1f75af70192c`
- Initial state at verification: `pending`, unclaimed
- Symbol/timeframe: `USDJPY.DWX` / `H1`
- EX5: `46c1075a921db25a6ba61712ba76b10a89ea7e2a9a4f52472ab49f57f65e4944`
- Evidence binding:
  `payload.source_q15_work_item_id->work_items.evidence_path`
- Q15 freeze authenticated: `true`
- Risk: fixed USD 1,000; percent risk zero

This unit only enqueued the row. It did not invoke `dispatch-tick`, launch MT5,
reserve a terminal, or run a backtest.

## Verification

- `python -m py_compile tools/strategy_farm/farmctl.py tools/strategy_farm/tests/test_candidate_repair_enqueue.py` — PASS
- `python -m pytest tools/strategy_farm/tests/test_candidate_repair_enqueue.py -q -k "q02 and not fresh"` — `14 passed, 20 deselected`
- Focused authenticated-freeze and binary-identity tests — `4 passed, 30 deselected`
- `git diff --check` — PASS

# Review — SP-C6 Recurring Governor Kill/Recovery Dry-Run Watch

Date: 2026-08-23
Reviewer: Claude (review lane)
Router task: `96eb3708-4c3a-407c-aaf5-3533ecabd036` (`SP-C6`, ops_issue, GELB)
Worker artifact: `docs/ops/evidence/2026-08-23_sp_c6_governor_dry_run_watch.md`
Commit under review: `925e9f4b7`

## Verdict

**APPROVED** — capability delivered dry-run only; scheduled-task registration is a
follow-up for OWNER/Codex (not a defect).

## Acceptance criteria vs. evidence

1. **"Jede aktive Position im Dry Run erkannt"** — PASS.
   `test_every_active_position_is_recognized_and_persisted`
   (`tools/strategy_farm/tests/test_governor_dry_run_watch.py:99`) builds a
   3-position/1-order fixture with magics `111320000`, `0`, `999` and asserts all
   four tickets (`101,102,103,201`) land in persisted state and in the appended
   history record. Magic-independence matches SP-C1's reconciliation proof.

2. **"Alarm/Freeze/Recovery belegt"** — PASS.
   `test_level_increase_writes_alarm_and_level_decrease_writes_recovery`
   (`...:133`) runs three sequential invocations: first observation → level 2, no
   transition; clear → level 0 RECOVERY (`severity=INFO`, `level_decrease` in
   `health_alarms.log`); breach returns → level 2 ALARM (`severity=WARN`,
   `level_increase`). `entry_freeze=True` asserted at every non-zero level.

3. **"signierte Notfallpolicy als Voraussetzung fuer Stufe-3-Flatten vorhanden"** —
   PASS. `test_stage3_flatten_requires_owner_signed_emergency_policy_bound_to_trigger`
   (`...:210`): breach with an ordinary bound policy but no emergency policy stays
   level 2, `would_flatten_position_tickets=[]`, `emergency_policy_binding.bound=false`.
   The gate lives in SP-C1's unmodified `evaluate()`; the watcher adds no bypass.

## Framework / safety conformity

- No new authority or execution path. `--dry-run` is a required acknowledgement
  (`governor_dry_run_watch.py:284-293`, `test_dry_run_flag_is_a_required_acknowledgement:262`);
  no apply mode exists; `action_plan.actions_executed` is always `[]`. No MT5
  connection, no AutoTrading toggle, no order/terminal/T_Live touch.
- Wraps `account_portfolio_governor.evaluate()` unchanged — no thresholds,
  verdicts, seeds, or windows altered (ROT respected). Referenced governor
  symbols exist (`account_portfolio_governor.py:30-33,62,74,94,108,389`).
- Fail-closed: unreadable/missing snapshot or policy-load failure downgrades to a
  level-1 `ENTRY_FREEZE_WATCHER_ERROR` decision, exit 0, no crash
  (`governor_dry_run_watch.py:167-208`; `test_unreadable_snapshot_fails_closed_without_crashing:242`).
- Sibling-import trap handled (package-qualified → bare fallback,
  `governor_dry_run_watch.py:52-55`).

## Independent verification

- `python -m pytest tools/strategy_farm/tests/test_account_portfolio_governor.py
  tools/strategy_farm/tests/test_live_book_dd_guard.py
  tools/strategy_farm/tests/test_governor_dry_run_watch.py -q` → **18 passed** (re-run
  by reviewer, matches claim). Tests monkeypatch STATE/GUARD/ALARM/HISTORY paths to
  `tmp_path` — no production writes during tests (`test:17-21`).
- Dependency `SP-C1` (`5c02a347-e91c-44e3-b592-6dad7c6f4d81`) is `APPROVED` in
  `farm_state.sqlite` — dependency satisfied.
- Production state confirmed genuine telemetry, not synthetic:
  `D:\QM\reports\state\governor_dry_run_watch_state.json` → level 1
  `ENTRY_FREEZE_UNCERTAINTY`, `run_count=2`, `alarm_count=0`, `recovery_count=0`;
  history JSONL shows real login `4000090541`, balance `99095.26`, zero recognized
  positions (v1 legacy-unversioned snapshot boundary, same as SP-C1). Synthetic
  smoke data confirmed removed.

## Follow-up (not blocking)

- Register `QM_StrategyFarm_GovernorDryRunWatch` (5-min cadence, mirroring
  `QM_StrategyFarm_LiveBookDDGuard`) — left to OWNER/Codex, consistent with SP-C1's
  own source-only deployment boundary. Until an OWNER policy is bound, every run
  reports fail-closed level 1 (no false CLEAR).

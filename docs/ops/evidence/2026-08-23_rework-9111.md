# rework-9111 — QM5_9111 review-fix evidence

Date: 2026-08-24 UTC

Router task: `f82c46f1-5294-45fa-b75d-6b1120b9a591`

EA: `framework/EAs/QM5_9111_aa-dlwma-trend10/`

Disposition: **REVIEW_REQUIRED — source/card defects fixed; governed compile remains fail-closed on the live-factory interlock.**

## Authority and preflight

- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9111_aa-dlwma-trend10.md`, SHA-256 `52397765103cfe01490da2089e685e49596cb8574af5014e2cbf6a7823da1a6b`, `g0_status: APPROVED`.
- `framework/registry/ea_id_registry.csv` has the active `9111 / aa-dlwma-trend10` identity.
- `framework/registry/magic_numbers.csv` has 13 active rows, slots 0..12, magics 91110000..91110012. No registry gap was found and no registry/resolver file was changed.
- Work order: `C:/QM/repo/docs/ops/evidence/2026-08-24_review_qm5_9111.md` plus `C:/QM/repo/docs/ops/evidence/2026-08-23_qm5_9111_gemini_code_review.md`.

## Changes and file:line evidence

1. The ATR-ratio spread proxy was replaced with the card's entry-only rule. `QM5_9111_aa-dlwma-trend10.mq5:51,62-85` samples exactly 20 completed D1 spread observations with bounded `CopySpread`, checks the copy count and `ArraySize`, rejects every missing/non-positive sample, computes the even-sample median, and admits only `current <= 2.5 * median`. The raw-series call is explicitly `perf-allowed` at line 71.
2. Card exits and framework risk handling are independent of entry filters. `QM5_9111_aa-dlwma-trend10.mq5:281-317` runs MAE, kill-switch, Friday close, management, the completed-D1 gate, and the opposite-cross exit before either news admission or `Strategy_NoTradeFilter()`.
3. The decision clock is explicitly D1. `QM5_9111_aa-dlwma-trend10.mq5:272-275` declares a `PERIOD_D1` execution contract and `:291` gates both signal exit and entry evaluation on `QM_IsNewBar(_Symbol, PERIOD_D1)`.
4. The signal now seals the Stern/Brown DLWMA slope equation. `QM5_9111_aa-dlwma-trend10.mq5:120-143` calculates `3/(N-1) * (LWMA1-LWMA2)`, fails closed on invalid component values, and preserves the fixed completed-bar indexing. Reference vectors for LWMA1, LWMA2, and final trend at shifts 1 and 2 are in `tools/strategy_farm/tests/test_qm5_9111_review_rework_static.py:80-117`.
5. `SPEC.md:32,71` now documents the fixed median-spread rule and D1 attachment contract. All 13 scoped backtest setfiles replace the dead ATR-spread key with `strategy_spread_median_mult=2.5`, retain `RISK_FIXED=1000` / `RISK_PERCENT=0`, and bind checkout-form source hash `49b21e7604f41908ff6120fc565d7b1f00865ae05ddbff63586f5a7e56ddcd46`.
6. Regression coverage is in `tools/strategy_farm/tests/test_qm5_9111_review_rework_static.py:57-181`: hardening, fail-closed spread sampling, exact DLWMA vectors, D1/order reachability, framework/magic/input rules, setfile risk mode, and source-hash binding.

## Declared-input use-site census

Command:

```text
python -c <regex census over input declarations and executable use-sites>
```

Result: every input has at least one non-declaration use-site. Strategy inputs resolve as `strategy_period:123`, `strategy_min_daily_bars:152,173`, `strategy_atr_period:184`, `strategy_sl_atr_mult:194,212`, `strategy_spread_median_mult:66,85`, and `strategy_allow_short:206`. Framework inputs resolve through `QM_FrameworkInit`, `QM_FrameworkMagic`, request slotting, or the news entry gate. No raw magic computation, `CopyBuffer`, ML import, grid, or martingale mechanism is present.

## Validation output

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_9111_review_rework_static.py
..... [100%]
5 passed in 1.97s

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_9111_aa-dlwma-trend10
failures=[]; warnings=[]; files_scanned=1; symbols_observed=13; registry checks valid

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_9111_aa-dlwma-trend10
PASS; files_checked=14; findings=[]; max_news_stale_hours=336

python -m pytest -q tools/strategy_farm/tests/test_qm5_9111_review_rework_static.py tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_build_guardrails.py
53 passed, 1 failed in 587.16s
unrelated failure: test_qm5_411xx_sources_have_no_unbounded_numeric_buffers detects seven pre-existing findings beginning at untouched QM5_41134_wti-mdaily-iqrmean-mom.mq5:599

git diff --check
PASS (line-ending conversion notices only)
```

### Governed compile check

Both scoped invocations were attempted, never unscoped:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_9111_aa-dlwma-trend10
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_9111_aa-dlwma-trend10 -SkipCompile
```

Both fail closed before any compile or setfile mutation with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`: factory terminal64 processes are alive and the tool requires the governed `COMPILE_EA` path. At the final check T1, T3, T5, T6, T8, and T10 were active. Read-only DB evidence:

```sql
SELECT id,phase,status,verdict,created_at,updated_at
FROM work_items
WHERE ea_id IN ('9111','QM5_9111') AND phase='COMPILE_EA'
ORDER BY created_at DESC;
```

The latest row is historical failed row `f0832902-5ce6-42b5-81f2-d3462b841e3e`; there is no pending/active authorized compile row. `compile_work_items.py` has no 9111 source-repair/force-rebuild authority, so no queue row was manufactured and no safety interlock was bypassed. The existing `.ex5` remains the prior binary (`dcb01b20753f61ecbb5c39c2649552e7ffeb243d42855f26b45d9ba50e1d9ad5`) and `build_identity.json` remains prior-build evidence; neither is claimed as current.

## Safety and risk

- No backtest or compile item was enqueued or deleted; no verdict row was overwritten.
- Factory state, gate criteria, magic allocations, `T_Live`, and AutoTrading were untouched.
- Open risk: a governed current-source compile is still required before Q02. This resubmission is for source/card review and compile-authority adjudication, not a claim that the stale EX5 is deployable or pipeline-ready.

## Rollback

Revert this ticket commit. That restores the prior MQ5, SPEC, and all 13 setfiles and removes the regression test/evidence note. The binary and registries require no rollback because this ticket did not change them.

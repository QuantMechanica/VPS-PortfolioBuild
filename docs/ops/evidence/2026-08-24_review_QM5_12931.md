# Claude review — QM5_12931_classical-triple-top-reversal-h4 (review_ea aa15d2e4)

- **Router task:** aa15d2e4-40a5-40b0-a8a1-0df2d4b3cc62 (`review_ea`, assigned codex, state REVIEW)
- **Source build task:** ab171f6d-bd52-4b39-9386-936314d5439e (gemini/agy), source_verdict PASS
- **EA:** QM5_12931, dir `framework/EAs/QM5_12931_classical-triple-top-reversal-h4/`
- **Card:** `D:\QM\strategy_farm\artifacts\cards_approved\QM5_12931_classical-triple-top-reversal-h4.md` (build-gate resolves it; not present in repo worktree)
- **Reviewed source hash:** `332a07aa4649abc7421544c9c79f4f15a587d07a30371e8b09fe5f0b6d9af994` (== build artifact `mq5_sha256`, current worktree byte-identical)
- **Verdict: RECYCLE** — named, actionable, source-level card-conformity and framework-corset defects.

## Independent verification (not just re-quoting codex)

`python tools/strategy_farm/build_gate_hardening.py --repo-root C:/QM/repo --ea-label QM5_12931_classical-triple-top-reversal-h4`
returns **4 top-level card failures** on the current source hash — directly contradicting the build
artifact's `build_check_passed=true`:

1. **EA_CARD_PENDING_ORDER_TYPE_MISMATCH** — card requires a SELL_STOP entry; source issues a market
   request. No executable SELL_STOP present.
2. **EA_CARD_SMA_DIRECTION_INVERTED** — `.mq5:187` compares `sma_vals[0]` vs `sma_vals[1]` for the
   card-declared *falling* D1 SMA(50), but `CopyBuffer(start=1,count=2)` at `.mq5:182` stores the
   older shift-2 value at index 0 and shift-1 at index 1 → the sign is inverted (admits a rising SMA).
3. **EA_CARD_NEWS_WINDOW_MISMATCH** — card requires ±2 H4 bars (480/480 min); source configures
   `QM_NEWS_TEMPORAL_PRE30_POST30` = 30/30 min (`.mq5:25`).
4. **EA_CARD_NEWS_BLOCKS_MANAGEMENT** — `.mq5:575` `if(!news_allows) return;` fires *before*
   `Strategy_ManageOpenPosition()` (`.mq5:581`) and the exit block; card news is entry-only, so
   protective management must stay reachable during a blackout.

## Additional defects confirmed by direct read

5. **DEAD INPUT** — `strategy_peak_height_atr` declared at `.mq5:46`, **zero** use-sites
   (`grep -n strategy_peak_height_atr` → only line 46). The card's Gate-1 peak-height gate
   (≥0.5 ATR above surrounding high) is therefore not implemented. Dead strategy input = RECYCLE.
6. **Framework corset / forbidden raw indicators** — raw `iATR` (`.mq5:93`), `iMA` (`.mq5:101`),
   `CopyBuffer` (`.mq5:182,280,520`), `CopyRates` (`.mq5:296,526`) used directly in the user EA
   instead of QM indicator wrappers.
7. **Partial-close return ignored** — `.mq5:490` calls `QM_TM_PartialClose(...)` but discards the
   bool result; `g_tp1_done=true` is set unconditionally at `.mq5:492`, so a rejected 50% close
   permanently skips the card's partial exit.
8. **Restart-unsafe state** — partial/break-even/failure-exit state is file-scope only and not
   reconstructed on OnInit; on restart with an open position management is disabled.

## Build evidence

- **Stale EX5:** `.ex5` mtime `Aug 23 11:04` **predates** `.mq5` mtime `Aug 23 19:21`
  (commit 02f10d024 rewrote the MQ5 by ~500 lines without recompiling). The compiled binary does
  **not** represent the reviewed source → no valid COMPILE_OK / D10-clean for the current hash.
- **Smoke:** `smoke_result=deferred_p2_smoke`, no summary under `D:/QM/reports/smoke` — not durable
  evidence (acceptable to defer, but not a pass).

## Disposition

Not clean for Q02. Rework required: implement SELL_STOP entry per card; fix D1-SMA ordering sign;
set news window to 480/480 and make it entry-only (management reachable during blackout); wire
`strategy_peak_height_atr` into Gate-1 or remove it from the card; route indicators through QM
wrappers; honour the `QM_TM_PartialClose` result; reconstruct management state on OnInit; recompile
so the EX5 matches the source. Route back to Codex (build lane) for remediation.

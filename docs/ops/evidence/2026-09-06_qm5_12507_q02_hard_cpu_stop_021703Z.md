# QM5_12507 logical FX basket Q02 hard-CPU stop

**Observed:** 2026-09-06 02:17:03Z  
**Branch:** `agents/board-advisor`  
**Outcome:** no queue mutation or tester launch; the existing unique Q02 row is preserved

## Selection

The governed 66-pair FX cointegration scan is fully mechanized, so creating a
new card or EA would duplicate committed work. The two preferred anchors are
already beyond Q02: `QM5_12532` has Q02 PASS followed by Q05 FAIL, and
`QM5_12533` has Q02 PASS followed by Q04 FAIL. Neither has a current Q02
ONINIT or NO_HISTORY blocker.

The valid fallback remains the existing EURUSD/GBPUSD H1 logical basket
`QM5_12507_EURUSD_GBPUSD_COINTEGRATION_H1`. Work item
`547c4fd3-f3fd-4c59-b9dc-654e96521251` is the only open row for that exact
identity. It is pending, unclaimed, unheld, attempt zero and already carries
`priority_track=true`. Its predecessor Q01 row
`7d1a179d-4d25-5d37-a69a-3a52fd78ae63` passed with 632 observed leg trades.
The logical setfile is correctly backtest-bound with `RISK_FIXED=1000`,
`RISK_PERCENT=0` and `PORTFOLIO_WEIGHT=1`; the basket manifest is present.

## Capacity stop

Five one-second total-CPU samples were `94, 98, 94, 93, 99` percent. The
maximum reached 99%, crossing the 97% hard ceiling while eight farm work items
were active. The mission therefore stopped before enqueue, claim, dispatch,
compile or terminal launch. The existing worker fleet remains responsible for
claiming the already-priority-tracked row when governed capacity clears.

Machine-readable evidence:
`artifacts/fx_cointegration_qm5_12507_q02_hard_cpu_stop_20260906T021703Z_board_advisor.json`.

## Safety

No portfolio admission, portfolio KPI, Q08 contribution, portfolio gate,
T_Live manifest, T_Live file or AutoTrading state was touched. No card, EA,
binary, setfile, basket manifest, registry row, verdict or work-item payload
was changed.

## Resume contract

Re-read the same work item and re-sample CPU on the next paced wake. Proceed
only if the row remains unique, pending, unheld and unclaimed and every sample
stays below 97%. Do not append a duplicate Q02 row.

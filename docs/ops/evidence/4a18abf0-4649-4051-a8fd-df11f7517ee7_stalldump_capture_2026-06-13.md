# Factory Stall Worker Stack Capture Evidence

Task: `4a18abf0-4649-4051-a8fd-df11f7517ee7`
Date: 2026-06-13
Agent: codex

## Scope

Changed only:

- `tools/strategy_farm/terminal_worker.py`
- `tools/strategy_farm/factory_watchdog.ps1`

## Implementation

- `terminal_worker.py` now enables `faulthandler` at startup and starts a daemon `stalldump_watcher` thread.
- Each worker polls `D:/QM/reports/state/STALLDUMP_REQUEST` every 5 seconds.
- When the request file appears, the worker appends all Python thread tracebacks to `D:/QM/reports/state/worker_stalldump/<terminal>_<pid>.txt` with a UTC header.
- The watcher tracks the request file mtime/size and dumps once per request instance.
- `factory_watchdog.ps1` now creates `STALLDUMP_REQUEST` only when `dispatchStalled` is true, waits 8 seconds, deletes the request file, rotates `worker_stalldump/*.txt` to the newest 50 files, and then proceeds into the existing clean-slate kill/respawn flow.
- No worker claiming/spawning logic was changed.
- No terminal kill scope was broadened; T1-T10 matching remains unchanged.

## Verification

PASS:

```text
python -m py_compile tools/strategy_farm/terminal_worker.py
```

PASS:

```text
$null = [scriptblock]::Create((Get-Content -Path tools/strategy_farm/factory_watchdog.ps1 -Raw)); 'parser_ok'
```

PASS:

```text
stalldump_watcher_smoke PASS
```

The smoke test monkeypatched the request/dump paths to a temporary directory in-process, started the watcher, created a temp request file, and verified a dump file containing the `STALLDUMP` header and Python traceback body. It did not create the production `D:/QM/reports/state/STALLDUMP_REQUEST` file.

PASS:

```text
git diff --check -- tools/strategy_farm/terminal_worker.py tools/strategy_farm/factory_watchdog.ps1
```

Git emitted only existing line-ending normalization warnings for the two edited files.

## Expected Next Stall Evidence

On the next dispatch stall, before the watchdog kills/respawns workers, the directory below should contain timestamped Python stack dumps from live worker processes:

```text
D:/QM/reports/state/worker_stalldump/
```

The watchdog JSONL detail will include `stalldump_request files=<n>` and a sample of dump filenames when files are captured.

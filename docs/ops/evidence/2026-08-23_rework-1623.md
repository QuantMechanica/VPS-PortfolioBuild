# rework-1623 review repair evidence

## Scope and authority

- Router task: `9c4f7a27-e62e-41e4-8d21-1e74c7a05c33` (`RECYCLE` ->
  `IN_PROGRESS`). Three initial governed claim attempts returned
  `sqlite3.OperationalError: database is locked`; the later identical router
  command succeeded. No direct SQLite write was used.
- EA: `QM5_1623_hopwood-bermaui-dss-h4` only.
- Work order: `docs/ops/evidence/2026-08-23_qm5_1623_gemini_code_review.md`
  and `docs/ops/evidence/2026-08-24_review_qm5_1623.md`.
- Approved card:
  `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1623_hopwood-bermaui-dss-h4.md`,
  `g0_status: APPROVED`.
- Existing active registry allocations were verified and left unchanged:
  `framework/registry/ea_id_registry.csv:4616` and
  `framework/registry/magic_numbers.csv:17806-17818`.

The three repository rebaseline documents and the OWNER vault directive were
read before implementation. This ticket changes no gate topology, threshold,
criterion, data window, verdict row, queue, factory state, or live surface. No
backtest or compile work item was enqueued or deleted, the factory was not
toggled, and `C:/QM/mt5/T_Live` was not touched.

## What changed

1. Restored the approved baseline inputs: DSS `(10,5,5)`, a 100-bar window,
   80th/20th percentile bands, `2.0 * ATR(14)` initial stop, 20-H4-bar time
   stop, six-bar cooldown, and `0.3 * ATR` spread ceiling at
   `framework/EAs/QM5_1623_hopwood-bermaui-dss-h4/QM5_1623_hopwood-bermaui-dss-h4.mq5:39-50`.
   The rejected standard-deviation multiplier, overshoot gate, take-profit,
   and break-even inputs/mechanics were removed. Entry now explicitly sends
   `tp=0` at MQ5 lines 282-319.
2. Replaced the substituted mean/standard-deviation bands with a bounded DSS
   series and nearest-rank percentile implementation. The completed-H4 raw
   stochastic/EMA construction, fixed warmup, allocation proofs, and explicit
   `ArraySize` guards are at MQ5 lines 75-159. The sorted nearest-rank
   percentile is at lines 162-186, and the current/previous 100-bar snapshots
   are bound at lines 189-249.
3. Implemented every approved exit family from the same immutable closed-bar
   snapshot. Entry signals and upper/lower band-recross exits are calculated at
   MQ5 lines 231-245. The 20-bar time stop is at lines 326-343. Opposite-signal
   closes use `QM_EXIT_OPPOSITE_SIGNAL`, while percentile capture and D1
   EMA(200) regime invalidation use the strategy exit path at lines 346-374.
4. Fixed ordering and entry admission. MAE remains first on tick at MQ5 line
   432; Friday close and time-stop management precede the single H4 new-bar
   gate; state advances at lines 443-446 before exits at lines 451-462. Spread
   and news checks occur only afterward at lines 464-475, so they cannot block
   risk-reducing exits. A successful opposite close can therefore be followed
   by the opposite entry from the same snapshot.
5. Made the spread guard fail closed when the snapshot ATR, bid, ask, or spread
   relationship is unavailable, and compare against the current completed-H4
   ATR at MQ5 lines 268-279. Initialization rejects non-H4 charts and invalid
   strategy inputs before framework startup at lines 383-401.
6. Moved direction-specific cooldown resets out of signal construction and
   behind confirmed `QM_TM_OpenPosition` success at MQ5 lines 478-488.
7. Replaced the drifted SPEC with the approved percentile, risk, and exit
   contract at `framework/EAs/QM5_1623_hopwood-bermaui-dss-h4/SPEC.md:13-48`;
   the malformed fixed-risk value is restored to `$1,000 per trade` at line 91.
8. Made the build identity truthful. It binds the repaired MQ5 SHA-256 and
   explicitly records the compile interlock and stale pre-rework EX5 at
   `framework/EAs/QM5_1623_hopwood-bermaui-dss-h4/build_identity.json:2-10`.
9. Added twelve ticket-specific regression tests at
   `tools/strategy_farm/tests/test_qm5_1623_review_rework_static.py:67-275`.
   They cover card defaults, numerical nearest-rank thresholds, bounded buffers,
   all entry/exit families, ordering, spread/H4 fail-closed behavior, post-fill
   cooldown, every declared strategy input's executable use site, preserved
   registries/setfiles, SPEC integrity, and truthful binary identity.

## Validation evidence

- `python -m pytest -q tools/strategy_farm/tests/test_qm5_1623_review_rework_static.py tools/strategy_farm/tests/test_build_guardrails.py`
  -> final rerun `32 passed in 3.45s`.
- `python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py -k "d7_framework_mae_hook_pass_and_fail_fixtures or d9_trade_request_zero_init_pass_and_bare_request_fails or d10_indicator_buffer_bound_pass_and_fail_fixtures or d11_canonical_setfile_and_magic_symbol_pass or build_check_applies_hardening_pass_and_fail_fixtures or build_check_rejects_d11_phantom_symbol"`
  -> final rerun `6 passed, 24 deselected in 10.99s`.
- A preceding aggregate of all three modules emitted 19 passing dots and then
  made no progress for more than three minutes with no child process. It was
  interrupted as a stalled run and replaced by the two deterministic runs
  above; no assertion failure was reported.
- `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_1623_hopwood-bermaui-dss-h4`
  -> one file scanned, `failures: []`, `warnings: []`; 13 registry/set symbols
  matched the DWX matrix.
- `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_1623_hopwood-bermaui-dss-h4`
  -> `PASS`, 14 files checked, zero findings, `max_news_stale_hours=336`.
- `python framework/scripts/skill_build_ea_guard.py --ea-id 1623 --ea-label QM5_1623_hopwood-bermaui-dss-h4`
  -> `status: ok`; EA row, magic rows, and EA directory all true.
- `python framework/scripts/validate_spec_doc.py framework/EAs/QM5_1623_hopwood-bermaui-dss-h4`
  -> `1 PASS, 0 FAIL`.
- `git diff --check` -> no whitespace error; only checkout line-ending warnings.

## Compile admission and artifact truth

Both required EA-scoped attempts were made:

```text
pwsh -NoProfile -File framework/scripts/build_check.ps1 -EALabel QM5_1623_hopwood-bermaui-dss-h4 -Strict
pwsh -NoProfile -File framework/scripts/build_check.ps1 -EALabel QM5_1623_hopwood-bermaui-dss-h4 -Strict -SkipCompile
```

Both were refused before compilation with
`BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED` /
`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because `terminal64` processes were
alive. The guard's only alternative was `farmctl.py enqueue-compile`; this
ticket explicitly forbids enqueueing, and the factory could not be toggled.
No bypass was attempted.

- Repaired MQ5 SHA-256:
  `74e40c70b94451e74f2d4f00dedf833e52d3399fee82b52405f181003cee62a5`.
- Unchanged pre-rework EX5 SHA-256:
  `913e0fe6818410fcaa9aed753380509a1b76883f15e4aa451793fbfdb48d6144`.

The checked-in EX5 is therefore not represented as a rebuilt artifact. A fresh
governed compile remains a review-visible risk; this source repair itself is
fully static-gate and unit-test clean.

## Rollback

Revert the single ticket commit. That restores the rejected EA source, SPEC,
and prior build identity, removes the ticket regression test and this evidence
file, and leaves every registry row and setfile exactly as before. The EX5 was
never modified, so no binary/runtime rollback is required. No queue, verdict,
factory, terminal, or live rollback is required because none was changed by
the implementation.

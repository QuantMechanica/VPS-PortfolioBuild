# rework-39005 — QM5_39005 review repair evidence

- Ticket: `rework-39005`
- Router task: `a2055dc7-5e44-4ba0-a130-235c9e6a2c75`
- EA: `QM5_39005_forexfactory-genesis-matrix-scalper`
- Date executed: 2026-08-24 (Europe/Berlin)
- Scope: build/review repair only; no backtest enqueue, verdict overwrite, factory toggle, registry allocation, T_Live write, or AutoTrading change

## Work order and preflight

The repair was derived from both review artifacts:

- `docs/ops/evidence/a2055dc7_qm5_39005_gemini_build_codex_review_2026-08-18.md`
- `docs/ops/evidence/149b2735_qm5_39005_gemini_codex_review_2026-08-23.json`

The approved card is `D:/QM/strategy_farm/artifacts/cards_approved/QM5_39005_forexfactory-genesis-matrix-scalper.md`; its front matter has `status: APPROVED`, `g0_status: APPROVED`, EA id 39005, slug `forexfactory-genesis-matrix-scalper`, targets EURUSD.DWX/GBPUSD.DWX, and timeframe M5.

Registry preflight found the existing active allocations at `framework/registry/magic_numbers.csv:17525-17526`:

- slot 0, EURUSD.DWX, magic 390050000
- slot 1, GBPUSD.DWX, magic 390050001

No registry or resolver file was changed.

## What changed

### EA mechanism and framework conformity

- `framework/EAs/QM5_39005_forexfactory-genesis-matrix-scalper/QM5_39005_forexfactory-genesis-matrix-scalper.mq5:34-35` makes the source contract backtest-safe by default: `RISK_PERCENT=0.0`, `RISK_FIXED=1000.0`.
- `...mq5:87-149` checks the `CopyRates`/`ArrayResize` results and binds every dynamic-buffer access to `ArraySize`; the card parameter ranges are also fail-closed at `...mq5:361-376`, bounding warmup allocations.
- `...mq5:157-180` reconstructs persistent GHL direction over prior bars and leaves the bar not-ready if no direction was established; readiness is propagated for TVI, T3-CCI, GHL, EMA, pooled close, and ATR before `g_state_valid` is set at `...mq5:183-216`.
- `...mq5:168` and `...mq5:204` replace direct `iClose` calls with pooled `QM_SMA(..., 1, ..., PRICE_CLOSE)` reads. The sole raw series call is the bounded TVI `CopyRates` window with an explicit `perf-allowed` annotation at `...mq5:89`.
- `...mq5:247-251` converts broker time through `QM_BrokerToUTC` before applying the card's 23:55-00:05 GMT blackout.
- `...mq5:379-401` converts the card's maximum three symbol ticks into deviation points and reconfigures the framework through `QM_EntryConfigure`, preserving the framework-resolved host magic.
- Prior review repairs remain locked: M5 execution contract at `...mq5:431-434`; pip-native SL/BE helper arguments at `...mq5:268` and `...mq5:331`; structural swing stops without an ATR inward cap at `...mq5:276-310`; card loss limits through the realized-loss halt and `QM_KillSwitchInit` at `...mq5:220-230` and `...mq5:436-442`; explicit MAE hook at `...mq5:457`.
- No ML, raw `OrderSend`, `CopyBuffer`, blocking `Sleep`, or direct OHLC/time series accessor remains.

### Specification and regression tests

- `framework/EAs/QM5_39005_forexfactory-genesis-matrix-scalper/SPEC.md:7-17` documents UTC rollover, three-tick order deviation, fail-closed readiness, and buffer bounds; the v3 revision is recorded at `SPEC.md:102`.
- `tools/strategy_farm/tests/test_qm5_39005_review_rework.py` adds four focused tests covering every review finding, executable use-sites for every declared input, scoped hardening plus exact magic rows, and fixed-risk/setfile strategy-input coverage.

Input grep found every declared input has at least one executable use-site beyond its declaration (occurrence counts ranged from 2 to 7). The regression test repeats this against comment-stripped source.

## Validation evidence

### PASS

Command:

```text
python -m pytest -q --disable-warnings tools/strategy_farm/tests/test_qm5_39005_review_rework.py tools/strategy_farm/tests/test_build_gate_hardening.py::test_d2_loss_limit_matching_and_mismatching_fixtures tools/strategy_farm/tests/test_build_gate_hardening.py::test_d3_pip_helper_correct_and_double_conversion_fixtures tools/strategy_farm/tests/test_build_gate_hardening.py::test_d5_raw_broker_hour_fails_and_utc_conversion_passes tools/strategy_farm/tests/test_build_gate_hardening.py::test_d7_framework_mae_hook_pass_and_fail_fixtures tools/strategy_farm/tests/test_build_gate_hardening.py::test_d10_indicator_buffer_bound_pass_and_fail_fixtures tools/strategy_farm/tests/test_build_gate_hardening.py::test_d11_canonical_setfile_and_magic_symbol_pass
```

Output:

```text
..........                                                               [100%]
10 passed in 4.00s
```

Command: `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_39005_forexfactory-genesis-matrix-scalper`

Result: schema `qm.build-gate-hardening/v1`, one file scanned, zero failures, zero warnings; both card-authorized symbols matched the DWX and magic registries.

Command: `python framework/scripts/validate_spec_doc.py framework/EAs/QM5_39005_forexfactory-genesis-matrix-scalper`

Output: `Summary: 1 PASS, 0 FAIL (of 1)`.

Command: `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_39005_forexfactory-genesis-matrix-scalper --max-news-stale-hours 336`

Result: `PASS`, three files checked, zero findings.

Command: `git diff --check`

Result: PASS (no whitespace errors).

### Compile/build-check interlock (not a claimed PASS)

The required strict compile was attempted with exact EA scope:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_39005_forexfactory-genesis-matrix-scalper/QM5_39005_forexfactory-genesis-matrix-scalper.mq5 -Strict
```

It was refused before compilation with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` / `INCLUDE_MIRROR_REFUSED` because live `terminal64` processes were present. The durable summary records `result=FAIL`, `reason_class=INCLUDE_MIRROR_REFUSED`, `errors=-1`, and `warnings=-1`:

- summary: `D:/QM/reports/compile/20260824_085518/summary.csv`

The script emitted `C:/QM/worktrees/rework-slot-1/framework/build/compile/20260824_085518/QM5_39005_forexfactory-genesis-matrix-scalper.compile.log` as the intended compile-log path, but correctly did not materialize a compiler log because MetaEditor was never invoked.

The exact scoped `build_check.ps1 -EALabel QM5_39005_forexfactory-genesis-matrix-scalper -Strict -SkipCompile` attempt was refused by the same preflight. No terminal was stopped, no include mirror was bypassed, and no compile/backtest work item was enqueued.

No smoke run is claimed. A fresh EX5 is a prerequisite, and this ticket explicitly forbids backtest enqueue. The prior review's missing-smoke finding therefore remains an external runtime-validation requirement after a governed compile slot is available.

## Risks and open validation

- Source/static/spec/guardrail validation is green, but a fresh EX5 was not produced under the active live-factory interlock.
- No smoke report was produced; runtime OnInit and order-deviation behavior remain to be proven by the governed compile/smoke path.
- The tracked pre-repair EX5 and setfile `build_hash` comments must not be treated as binding evidence for this source revision.

## Rollback

Revert the ticket commit with `git revert <rework-39005-commit>`. This restores the prior MQ5/SPEC and removes the focused test and this evidence file. No magic, EA-id, resolver, backtest queue, gate verdict, factory, deploy, live terminal, or AutoTrading rollback is required because none of those surfaces were changed.

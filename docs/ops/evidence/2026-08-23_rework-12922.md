# rework-12922 — QM5_12922 review repair evidence

- Ticket: `rework-12922`
- Router task: `800cfced-22aa-49e9-83d0-2ef5356ef2a4`
- EA: `QM5_12922_ariel-first-half-month-idx`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12922_ariel-first-half-month-idx.md`
- Worktree: `C:/QM/worktrees/rework-slot-6`
- Date: 2026-08-24
- Source status: ready for review
- Binary status: fresh compile blocked before compiler launch by the governed active-terminal interlock; the tracked EX5 remains the prior binary

## Work order

The router artifact `C:/QM/repo/docs/ops/evidence/2026-08-21_qm5_12922_gemini_build_review.md` and every matching `docs/ops/evidence/*review*12922*` artifact were read. The repair addresses the combined findings:

1. restart-safe trading-session ordinal reconstruction;
2. full-session named FOMC/NFP/ECB detection and one-session T+2 deferral;
3. reconstruction of deferred/already-traded state after restart;
4. no signal consumption on validation, transient news, or order failure;
5. no framework Friday close during the card-mandated T+1 through T+9 hold;
6. explicit compile/smoke evidence instead of an unsupported waiver claim.

## What changed

- Replaced raw `iTime`/`CopyTime` calendar logic with a bounded 32-D1-bar `QM_CalendarPeriodKey` reconstruction. This produces the actual current-month trading-session ordinal and fails closed if history is incomplete (`framework/EAs/QM5_12922_ariel-first-half-month-idx/QM5_12922_ariel-first-half-month-idx.mq5:42`, `:104-118`).
- Added explicit high-impact FOMC/NFP/ECB name predicates. Tester mode loads the framework calendar CSV once, bounds stored event days at 4096, checks array sizes, converts event UTC timestamps to broker days, and scans the whole T+1 day. Live mode queries the complete broker day with the native MT5 calendar (`...mq5:121-143`, `:180-276`, `:283-310`).
- Reconstructs whether the current magic already traded this month from the open position or deal history, so a restart after a stop-out cannot manufacture a duplicate entry (`...mq5:321-360`).
- Recomputes T+1/T+2 eligibility on init and every D1 rollover. T+2 is eligible only when the prior actual D1 session was a named macro-event day; no terminal-global state is used (`...mq5:362-408`, `:502-516`, `:545-555`).
- `Strategy_EntrySignal` no longer consumes `g_strategy_entry_due`. A transient news denial returns with the signal intact, and the signal is consumed only after `QM_TM_OpenPosition` succeeds (`...mq5:418-453`, `:566-592`).
- Locked framework Friday close off in source and fail-closed init validation (`...mq5:30`, `:479-483`). All five backtest setfiles explicitly bind `qm_friday_close_enabled=false`, canonical two-axis news settings, `qm_news_stale_max_hours=336`, `RISK_FIXED=1000`, and `RISK_PERCENT=0` (for example `framework/EAs/QM5_12922_ariel-first-half-month-idx/sets/QM5_12922_ariel-first-half-month-idx_GDAXI.DWX_D1_backtest.set:19-27`; the same keys are present in NDX, SP500, UK100, and WS30 setfiles).
- Updated the strategy spec to state named-event deferral and the Friday-close lock, and corrected the documented build-gate risk code (`framework/EAs/QM5_12922_ariel-first-half-month-idx/SPEC.md:13`, `:92`, `:101`).
- Added six focused regression tests covering bounded framework calendar access, restart reconstruction, named full-day events, retry-safe signal consumption, Friday-close/setfile locks, framework/risk/magic/MAE conformance, every declared input use-site, and no ML (`tools/strategy_farm/tests/test_qm5_12922_review_rework.py:21-148`).

## Input wiring census

Every declared input has at least one use beyond its declaration. Observed source-token counts were: `qm_ea_id=2`, `qm_magic_slot_offset=3`, `qm_rng_seed=2`, `RISK_PERCENT=2`, `RISK_FIXED=2`, `PORTFOLIO_WEIGHT=2`, `qm_news_temporal=4`, `qm_news_compliance=4`, `qm_news_stale_max_hours=2`, `qm_news_min_impact=2`, `qm_news_mode_legacy=3`, `qm_friday_close_enabled=3`, `qm_friday_close_hour_broker=2`, `qm_stress_reject_probability=2`, `strategy_atr_period=4`, `strategy_atr_stop_mult=4`, `strategy_hold_trading_days=2`, `strategy_require_d1=4`.

## Validation evidence

### PASS

- `python -m pytest tools/strategy_farm/tests/test_qm5_12922_review_rework.py -q` -> `6 passed in 0.62s`.
- `python -m pytest tools/strategy_farm/tests/test_qm5_12922_review_rework.py tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_validate_symbol_scope.py -q` -> `29 passed in 1.60s`.
- Broader relevant suite including `test_build_gate_hardening.py` -> `57 passed, 1 failed in 336.23s`; the one failure is an unrelated existing repository-baseline defect in `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`, not QM5_12922.
- Exact hardening check: `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_12922_ariel-first-half-month-idx` -> schema `qm.build-gate-hardening/v1`, `files_scanned=1`, `failures=0`, `warnings=0`, `row_failures=0`.
- `python framework/scripts/skill_build_ea_guard.py --ea-id 12922 --ea-label QM5_12922_ariel-first-half-month-idx` -> `status=ok`; EA registry, magic registry, and directory checks true.
- `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_12922_ariel-first-half-month-idx` -> `PASS`, six files, zero findings.
- `python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_12922_ariel-first-half-month-idx --fail-on-leak --json` -> `SINGLE_SYMBOL_OK`, zero violations.
- `python framework/scripts/validate_spec_doc.py framework/EAs/QM5_12922_ariel-first-half-month-idx` -> `1 PASS, 0 FAIL`.
- `python tools/strategy_farm/raw_mq5_quarantine.py check --source-path framework/EAs/QM5_12922_ariel-first-half-month-idx/QM5_12922_ariel-first-half-month-idx.mq5 --purpose compile --repo-root .` -> `RAW_MQ5_SOURCE_ALLOWED`.
- `git diff --check` -> zero errors (Git emitted only the repository's CRLF conversion notices).

### Governed compile/build-check refusal

Scoped command attempted:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 \
  -EAPath framework/EAs/QM5_12922_ariel-first-half-month-idx/QM5_12922_ariel-first-half-month-idx.mq5 \
  -EALabel QM5_12922_ariel-first-half-month-idx -Strict
```

Result: `FAIL`, `reason_class=INCLUDE_MIRROR_REFUSED`, before compiler launch. The guard reported `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because `terminal64` processes were alive and directed use of the governed compile queue. The durable row is `D:/QM/reports/compile/20260824_080809/summary.csv`. A scoped strict `build_check.ps1 -EALabel QM5_12922_ariel-first-half-month-idx -Strict -SkipCompile` was also refused by the same mandatory compile-pipeline guard before static checks.

At `2026-08-24T08:18:59Z`, the read-only process census observed `terminal64=7` and `metatester64=4`. No terminal was stopped, no factory state was toggled, and no compile/backtest work item was enqueued. No smoke/backtest was run because this ticket explicitly forbids backtest enqueue and the governed terminal pool was active. This supplies the missing saturation evidence but does not claim a smoke PASS.

Current hashes:

- repaired MQ5: `922fdfd52a8659c0614182b5b36970741692adf66a5c599a9818f1a8bb39fb80`
- prior/stale EX5: `49ab4db2d889eff07eb7441388b6222bf253f16cb52446704991c3581204e274`

## Scope and invariants

- No registry allocation changed; existing `ea_id_registry.csv` and `magic_numbers.csv` rows were read only.
- No state/verdict row was overwritten. The only authorized runtime mutations were router transitions for task `800cfced-22aa-49e9-83d0-2ef5356ef2a4`.
- No backtest was enqueued or deleted.
- Factory state and AutoTrading were not changed.
- `C:/QM/mt5/T_Live` was not touched.
- No gate threshold, criterion, or manifest was changed.

## Risks and required reviewer action

1. The source has not produced a fresh EX5 because the governed active-terminal interlock refused ad-hoc compilation. Review must arrange a governed compile for this commit before build acceptance; the checked-in EX5 is intentionally unchanged and stale.
2. No smoke report was produced under the explicit no-backtest boundary. The process census proves terminal/tester saturation at handoff, but it is not a substitute for later governed smoke evidence if review requires it.
3. The approved card front matter says `expected_trades_per_year_per_symbol: 40`, while its mechanics/spec say one monthly entry (approximately 12/year). This pre-existing card inconsistency was not changed because card governance is outside this repair.

## Rollback

Revert the ticket commit (`rework(12922): make calendar entries restart-safe`) with `git revert <commit>` to restore the prior MQ5, spec, and five setfiles and remove the focused test/evidence file. No database, registry, queue, factory, or live-terminal rollback is required; none of those surfaces changed beyond the authorized router task state.

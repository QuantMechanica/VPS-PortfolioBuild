# Claude review — QM5_35005 sma-crossover-pullback-system (Gemini build)

- Review task: `954f01d5-652a-4d01-bf2b-0e243bf8955f` (review_ea, reason `codex_review_required_for_gemini_code`)
- Source build task: `da39e160-b043-4528-8592-4a23f672fc55` (`artifacts/builds/da39e160-b043-4528-8592-4a23f672fc55.json`, built_by Gemini, backend agy)
- Card: `D:\QM\strategy_farm\artifacts\cards_approved\QM5_35005_sma-crossover-pullback-system.md` (status APPROVED, g0_status APPROVED)
- Reviewed source: `framework/EAs/QM5_35005_sma-crossover-pullback-system/QM5_35005_sma-crossover-pullback-system.mq5`, sha256 `758b0021667e9354415331c20dbee6389934813db590e3f837c242f4bac5be90` (matches build record + on-disk, `sha256sum`)
- **Verdict: RECYCLE** — source is clean, but the delivered `.ex5` is stale (never recompiled against the remediated source). Not Q02-ready. Remain in REVIEW/RECYCLE; no pipeline handoff.

## Source-level review — PASS (all clean)

- **Card parity**: long/short entries match card §3.2/§3.3 exactly — `SMA(100)[1] vs SMA(200)[1]` trend, `Stoch_K[2]` oversold≤20 / overbought≥80, `Stoch_K[1]` vs `Stoch_D[1]` cross, shift indices [1]/[2] on closed bars (`.mq5:146-198`). TP=300 / SL=150 pips, trailing 150-pip trigger+step (`.mq5:200-213`) match §3.4. No-trade filter (spread>1.8×ATR(14,H1)[1], UTC rollover 23:55–00:05, 2.0% daily loss halt, max-1-position) matches §3.1 (`.mq5:88-134`). Kill-switch 2.5%/5.0%/0.5% wired to `QM_KillSwitchInit` matches §4.2 (`.mq5:238-242`).
- **Unwired-input check** (per `feedback_ea_review_unwired_input_check`): every `strategy_*` input has ≥1 use-site beyond declaration (grep counts ≥2 for all 17 inputs). Zero dead inputs.
- **Framework conformity**: `#include <QM/QM_Common.mqh>`; pooled helpers only (`QM_SMA`/`QM_Stoch_K`/`QM_Stoch_D`/`QM_ATR`/`QM_StopFixedPips`/`QM_TakeFixedPips`/`QM_TM_TrailStep`/`QM_IsNewBar`) — all resolve in `framework/include/QM/`; no raw `iMA`/`iATR`/`iStoch`. MAE hook `QM_FrameworkTrackOpenPositionMae()` present (`.mq5:250`). News wired (`QM_NewsAllowsTrade2`, stale ceiling 336h = at-ceiling, compliant). RISK_FIXED=1000 / RISK_PERCENT=0 (backtest correct). No ML, no hardcoded commission/swap/DST.
- **Magic registry**: `framework/registry/magic_numbers.csv` rows for ea_id 35005 slots 0/1/2 → 350050000/1/2 = `ea_id*10000+slot`, one each, matching EURUSD/GBPUSD/EURJPY.DWX target universe.
- **Set files**: all 3 (EURUSD/GBPUSD/EURJPY H1 backtest) governed format — `risk_mode: FIXED`, `RISK_FIXED=1000`/`RISK_PERCENT=0`, `environment: backtest`, build_hash + all strategy params present.
- Prior 2026-08-21 RECYCLE (task `3281881e`) findings (untracked source, broker-time rollover, missing card loss limits, filter-suspended trailing) are all resolved at source level in this .mq5.

## Blocking defect — stale `.ex5` (recompile required before Q02)

The build artifact claims `"compile_succeeded": true` at `built_at 2026-08-23T11:24:55Z`. This is **not verified by any compile event** and is contradicted by the repo:

| Fact | Evidence |
|---|---|
| Remediation commit `951db3b68` changed the `.mq5` (+74 lines) but did **not** touch the `.ex5` | `git show --name-only 951db3b68` — no ex5 entry |
| `.ex5` last modified by commit `bfd467bc6` (earlier `pump auto-commit`), predating the remediation | `git log -- ...ex5` |
| Only compile record compiled the **pre-fix** source | `D:\QM\reports\compile\QM5_35005_.../result.json`: `ex5_mtime_utc 2026-08-17T20:36:40Z`, `mq5_mtime_utc 2026-08-17T20:35:59Z`; log `20260817_203616`; no newer compile exists |
| On-disk `.ex5` sha `28ef9a97341ab09666f4b8ac6a817bbdabe806c968fbc96279a0e1be0b2fbd59` unchanged since the 08-17/08-18 compile | matches ex5 sha in prior review evidence |

Consequence: dispatching this build to Q02 would exercise the **unpatched binary** — broker-time (not UTC) rollover blackout and the framework's generic kill-switch defaults instead of the card's 2.0/2.5/5.0% limits — while the repo would appear to certify the fixed logic. This is the documented stale-`.ex5` class (`project_qm_stale_ex5_voids_healthy_backtests`; predicate = build **date/hash**, not a claimed success flag). Per Evidence-over-claims (Hard Rules) the artifact's `compile_succeeded` claim is treated as false until a compile record post-dates the current `.mq5`.

Note: `farmctl health` also shows a pre-existing `pending_artifact_binding_drift` FAIL (3 `CONTENT_CHANGED:HELD` Q02 rows for this EA) — consistent with repeated rebuilds without a matching recompile.

## Rework directive (actionable)

Recompile `QM5_35005_sma-crossover-pullback-system.mq5` via the canonical `compile_ea.py` / COMPILE_EA queue in an OFF-window (recompile in active inventory is ROT for the review lane; factory is currently running). Produce a fresh `result.json` whose `ex5_mtime_utc` post-dates `2026-08-23T11:21:39Z` (or whose `ex5_sha256` differs from `28ef9a97...`), then resubmit the build artifact for review. Do **not** fan out to Q02 on the current `.ex5`.

## Verification commands run (read-only)

- `sha256sum *.mq5 *.ex5` — on-disk hashes match the build artifact's recorded hashes (internally consistent; the problem is they correspond to no compile of the remediated source).
- `git log`/`git show --name-only` on the `.mq5` and `.ex5` — remediation commit `951db3b68` excludes the ex5.
- `cat D:\QM\reports\compile\QM5_35005_.../result.json` — only compile dated 2026-08-17, pre-fix source.
- grep of all 17 `strategy_*` inputs for use-sites — none dead.
- `grep 35005 framework/registry/{magic_numbers,ea_id_registry}.csv` — slots 0/1/2, formula-correct.

No registry, work-item, or pipeline state was changed by this review.

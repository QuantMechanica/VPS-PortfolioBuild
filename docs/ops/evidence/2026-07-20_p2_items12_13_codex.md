# P2 items 12-13 — priority wiring and Mulham litmus verification

Date: 2026-07-20

Owner: Codex (`agents/codex`)

## Item 12 — cost class and live-book orthogonality

`tools/strategy_farm/strategy_priority.py` now keeps the existing top-level
ranking weights (`div=0.65`, `met=0.35`) while splitting the metrics component
into:

- expected quality: 50%;
- venue cost class: 20%;
- live-book symbol orthogonality: 30%.

The cost input is the checked-in
`framework/registry/venue_cost_model.json`. Exact aliases are resolved through
the registry; exact-but-unresolved cost values and unknown symbols score the
neutral value 0.5. The live-book input is an explicit reviewed manifest, either
the `QM_DXZ_BOOK_MANIFEST` path or the fixed 2026-07-19 fallback. It is never
selected by directory recency. Missing, malformed, or unresolved inputs are
neutral rather than rewarded. Each score carries component values plus input
path, SHA-256, load status, and resolution detail.

Runtime dry-run evidence:

- `D:/QM/reports/state/strategy_priority_p2_item12_20260720.txt`
- SHA-256:
  `0ba3a6975390cb824d402475edec1c52c0aa6696c98c0f5f959d938e50a3bbc8`
- result: 2,989 cards scored, 691 unbuilt candidates, 16 distinct buckets in
  the top 20;
- venue-cost input: `loaded`, SHA-256
  `cf4559d5fe2e90864a514eed6b7bd30f64117051654057d0b774c5126ad78406`;
- live-book input: `loaded`,
  `D:/QM/reports/portfolio/portfolio_manifest_sunday_final_24sleeve_DRAFT_20260719.json`,
  SHA-256
  `5ce872cc4ff92fe849edcfbcb9d1fd8e4e75d0b0d9892cdc025f6590f008fb8a`.

Deterministic regression coverage is in
`tools/strategy_farm/tests/test_strategy_priority_cost_orthogonality.py`. It
checks exact and aliased cost resolution, neutral missing/unresolved inputs,
orthogonality ordering, component weights, provenance hashes, environment-free
path injection, and score-cache invalidation when either evidence file changes.

## Item 13 — five-digit health regex and force-build litmus

No Codex source mutation was needed. Claude's existing `health.py` fix is
present at both card-ID parsers: `QM5_\d{4,5}` at lines 242 and 1080. The two
approved D-store cards exist with `force_build: true`:

| Card | SHA-256 | Dry-run result |
|---|---|---|
| `D:/QM/strategy_farm/artifacts/cards_approved/QM5_13209_mulham-pm-range-sweep.md` | `c2f60c10a652139127577ac2315a9a28786c5d919cb9e53f4005731b5c82e1bf` | rank 1 |
| `D:/QM/strategy_farm/artifacts/cards_approved/QM5_13212_mulham-us500-org.md` | `d4627ef412163145b7f1dde6937ff09c8551d95f7065d9acdcd6f919145a4f88` | rank 2 |

The same dry-run artifact above proves both five-digit IDs are visible to the
priority reader and force-forward above the organic queue. `force_build` remains
an ordering prior only; it does not bypass any build or pipeline gate.

## Tests

- `python -m pytest -q tools/strategy_farm/tests/test_strategy_priority_cost_orthogonality.py tools/strategy_farm/tests/test_health_registry_uniqueness.py`
  — PASS.
- `python tools/strategy_farm/strategy_priority.py --dry-run --top 20`
  — PASS; output captured at the evidence path above.

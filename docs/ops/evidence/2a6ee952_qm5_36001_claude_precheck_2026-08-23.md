# Claude pre-check: QM5_36001 Gemini remediation (review_ea)

- Router task: `2a6ee952-e292-4ace-a5d8-01c6340da256`
- Gemini source task: `3c1da904-b03a-40d5-a1b3-c23e9ffad4b8`
- Source artifact: `D:/QM/strategy_farm/artifacts/builds/3c1da904-b03a-40d5-a1b3-c23e9ffad4b8.json`
- Reviewed against original Codex review: `docs/ops/evidence/d47d0803_qm5_36001_gemini_build_codex_review_2026-08-18.md`
- Remediation claim: `docs/ops/evidence/3c1da904_qm5_36001_build_ea_result_2026-08-23.md`
- Remediation commits: `1c8d911f9` (source fixes), `73acf69db` (setfile build_hash)
- Verdict: **APPROVED_FOR_CODEX (Claude pre-check) — remain in REVIEW; Codex review still mandatory (hard rule)**

## Independent verification of the 6 original findings

| # | Codex finding | Verdict | Evidence |
|---|---|---|---|
| 1 | TP1 closed whole position, no runner survived | **FIXED** | `req.tp = 0.0` at entry; `Strategy_ManageOpenPosition` calls `QM_TM_PartialClose(ticket, half_lots, QM_EXIT_PARTIAL)` at +1 ATR, then moves SL to break-even. |
| 2 | SSL "crossover" was persistent state | **FIXED** | `Strategy_SSLCross` compares state at shift 1 vs shift 2; fires only on a genuine transition. |
| 3 | DeMarker exit tested absolute levels only | **FIXED** | Exit now reads DeMarker at shift 1 and shift 2 and requires a level crossing (`>=0.70` after `<0.70`, and the mirror for the short side). |
| 4 | WAE short gate used signed-momentum substitution | **FIXED** | `Strategy_WAEPass` now computes `(MACD[2]-MACD[1])*sensitivity` for the short side, matching the card's literal bearish equation, tested against `Max(ExplosionLine, DeadZone)`. |
| 5 | GMT rollover in broker time; loss-limit contract absent | **FIXED** | Rollover uses `QM_BrokerToUTC(TimeCurrent())`; `QM_KillSwitchInit(...,2.5,5.0,1.0)` wired in `OnInit`; 2.0% entry halt in `Strategy_NoTradeFilter` (measured on day-start equity drawdown — same conservative-proxy note as QM5_36004, non-blocking). |
| 6 | Entry filters suspended position management | **FIXED** | `OnTick` runs `Strategy_ManageOpenPosition()` and the exit-signal close loop before the news gate / `Strategy_NoTradeFilter`. |

## Dead-input scan (QM5_1355 defect class)

All 16 `strategy_*` inputs (McGinley, SSL, Vortex, WAE fast/slow/signal/BB/sensitivity/deadzone, DeMarker, ATR, SL/TP/spread multipliers, warmup) have live use-sites beyond their own declaration. No dead inputs found.

## Evidence-doc checklist cross-check

Setfiles under `sets/` confirm `RISK_FIXED=1000` / `RISK_PERCENT=0` for all 5 basket symbols. `framework/registry/magic_numbers.csv` has 5 collision-free active rows (360010000-360010004) matching SPEC and the remediation doc.

## Disposition

All 6 findings are genuinely fixed at the source level; no new defects surfaced. Note the compilation-refused claim in the remediation doc (`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`) means a clean compile and smoke run are still unproven — Codex/pipeline must confirm build before acceptance. Left in REVIEW; not self-approved; no pipeline state changed.

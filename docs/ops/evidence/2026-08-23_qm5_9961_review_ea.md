# QM5_9961 bandy-hma-supertrend-confluence-trend — Claude review_ea

Date: 2026-08-23

Router task: `1bec9666-7684-442f-b85a-982a3a981eb4` (`review_ea`, source: gemini
build `cf579137-6e4e-4044-a2c1-fb0a4dfa84bb`, `BUILD_COMPLETE_PASS`).

## Verdict

REVIEW (per Hard Rule: gemini-authored code requires Codex review before
acceptance — this task does not self-approve or move to PIPELINE/APPROVED).
Independent read of the .mq5 against its approved card and set files found
**no correctness defect**; one **process/evidence gap** worth Codex/OWNER
attention.

## Chain of custody — verified

- `.mq5` sha256 `b23164335d7991e77c91ba1b4fd16414ed3618f1ffe3bee47d9381a6fdc8eccc`
  and `.ex5` sha256 `a5a021aad808496cb55a38e75d8710ff67bc09e7c8c54635c35b6fb289de989e`
  both match `docs/ops/evidence/cf579137_qm5_9961_..._build_identity.json`
  exactly (independently recomputed with `hashlib.sha256`, not just trusted).
- Compile log: `0 errors, 0 warnings`.
- Magic registry (`framework/registry/magic_numbers.csv:17214-17226`):
  all 13 symbol/slot pairs match every generated `.set` file's
  `qm_magic_slot_offset` exactly (e.g. EURUSD.DWX slot 6 in both).
- All 13 `.set` files: `RISK_FIXED=1000`, `RISK_PERCENT=0` (correct for
  backtest per Hard Rule); `qm_news_stale_max_hours` absent from every set
  file, so it falls back to the compiled-in input default `336` — at, never
  above, the guardrail ceiling.

## Card fidelity — checked line-by-line, no deviation found

Approved card: `D:\QM\strategy_farm\artifacts\cards_approved\QM5_9961_bandy-hma-supertrend-confluence-trend.md`.

- Entry: `hma_long AND st_long AND close>regime AND first-bar-of-(hma,st)-confluence`
  matches the EA exactly. Confirmed the EA's `long_confluence_prev`/
  `short_confluence_prev` intentionally tracks only the HMA/Supertrend pair
  (not the regime filter) — this matches the card's own literal wording
  ("first D1 close where **both confluence conditions** agree", i.e. the two
  indicator states, not a three-way state including regime). Initially
  flagged this as a possible missed-entry edge case, then confirmed against
  the card text that the implementation is the more literal, correct
  reading.
- Supertrend recurrence (`Strategy_GetSupertrendStates`,
  QM5_9961...mq5:83-142): manually re-derived the standard final-band
  formula (`finalUpper[i] = basicUpper[i] if basicUpper[i] < finalUpper[i-1]
  or close[i-1] > finalUpper[i-1] else finalUpper[i-1]`, mirrored for lower,
  with the trend-flip check against the just-updated band) and confirmed the
  code matches term-for-term, including chronological iteration order
  (`idx = copied-2 downto 0` over a series-ordered buffer, oldest-to-newest).
- Exit: "either filter flips against the position" implemented as OR logic
  (`(st_ok && st_curr==-1) || hma_curr==-1` for a BUY) — matches the card's
  "belt-and-suspenders single-exit" description exactly; regime is
  correctly entry-only, not re-checked on exit, per the card.
- Stop loss: `3.0 * ATR(14)`, clamped to the broker's minimum stop distance
  in the direction that always widens (never tightens or inverts) the stop —
  verified both the long and short clamp branches move away from price.
- Time stop (`strategy_max_hold_bars=60` converted to `60 * PeriodSeconds()`
  elapsed wall-clock time rather than a 60-bar count): this undercounts
  actual D1 trading-day holding time by roughly the weekend fraction. Not
  flagged as an EA-specific defect — the identical `max_hold_bars *
  PeriodSeconds()` pattern is already in production across at least 7 other
  approved EAs in `framework/EAs/` (e.g. `QM5_10191`, `QM5_10179`,
  `QM5_10115`), so this is an established, already-accepted codebase
  convention, not something specific to this gemini submission.
- The card's "skip flip-signals where close-vs-Supertrend distance <
  0.2*ATR" filter is **not implemented**. The card itself labels this "same
  **optional** ablation as QM5_9946" and does not list it in the mandatory
  P3 sweep-parameter table — read as an optional variant for later
  optimization phases, not a baseline-build requirement. Noting for the
  record per "evidence over claims," not blocking.
- All 10 `strategy_*` inputs are read on at least one code path (checked via
  grep over the file); none are declared-but-unwired.

## Process/evidence gap — the one finding for Codex/OWNER

`docs/ops/evidence/cf579137_..._build_identity.json` was produced with
`card_defaults_status=none_found` inside every one of the 13 generated
`.set` files' header comments
(`card_defaults_source=D:\QM\strategy_farm\artifacts\cards_approved\QM5_9961_bandy-hma-supertrend-confluence-trend.md`,
`card_defaults_status=none_found`). But the card **does** exist at exactly
that path — I read it directly for this review. A repo-relative search
(`artifacts/cards_approved/QM5_9961...md`, the path cited in `SPEC.md` and
the `.mq5` header comment) finds nothing; only the `D:\QM\strategy_farm\...`
runtime copy exists. So the set-file generator either looked in the wrong
place or ran before the card was written to its current location — either
way, none of the 13 `.set` files carry an explicit, tool-verified
strategy-parameter block pulled from the card; every `strategy_*` value
comes from the `.mq5`'s compiled-in input default instead.

I independently compared all 10 compiled-in defaults (HMA 14/50, Supertrend
10/3.0, regime SMA 200, ATR period 14, stop multiplier 3.0, max hold 60,
warmup 250, max spread 0) against the card's `## Mechanik` section and they
match exactly — **this build is not miscalibrated**. But the match is
because the `.mq5` author (gemini) happened to hardcode the same numbers the
card specifies, not because any tool checked them against the card at
set-file generation time. A future EA where the compiled defaults drift from
its card would produce the same `none_found` status silently, with nothing
in the pipeline positioned to catch it. Recommend Codex investigate why
`gen_setfile.ps1`'s card lookup didn't resolve the `D:\QM\strategy_farm\`
path (or fix the path convention so `card_defaults_status` reflects reality)
as separate, non-blocking follow-up.

## Scope note

No code was changed by this review. No compile, backtest, or pipeline state
was touched. This artifact and the router task remain in `REVIEW` per the
Hard Rule that gemini-authored code requires Codex review before any
APPROVED/PIPELINE transition.

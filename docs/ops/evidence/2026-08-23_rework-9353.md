# rework-9353 — QM5_9353 mandatory review repair

Date executed: 2026-08-24 (Europe/Berlin)

Router task: `5fdc87be-5559-456c-9c2e-d6e4c9243070`

EA: `QM5_9353_chande-stochrsi-base-cross-h4`

Disposition: `READY_FOR_REVIEW_STATIC`; governed compile remains fail-closed as recorded below.

## Work order and preflight

- Review work order: `docs/ops/evidence/2026-08-23_qm5_9353_gemini_code_review.md`.
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9353_chande-stochrsi-base-cross-h4.md`, `g0_status: APPROVED`.
- EA identity is active at `framework/registry/ea_id_registry.csv:641`.
- All 13 existing magic allocations are active and unchanged at `framework/registry/magic_numbers.csv:16785-16797`; no registry file was edited.
- The router task was claimed with state `IN_PROGRESS` and verdict `rework burn-window slot 5` before source mutation.

## What changed

1. Removed the unsafe fixed `rsi_vals[14]` array. `ComputeStochRSIRaw` now keeps the current RSI in a scalar and scans the configured window without indexed storage (`framework/EAs/QM5_9353_chande-stochrsi-base-cross-h4/QM5_9353_chande-stochrsi-base-cross-h4.mq5:74`). `Strategy_InputsValid` fail-closes the documented parameter ranges, including `strategy_stoch_period` 7..30 (`mq5:56`).
2. Declared the H4 execution contract immediately after framework initialization (`mq5:291`) and changed the decision clock to `QM_IsNewBar(_Symbol, PERIOD_H4)` (`mq5:340`).
3. Sealed the card-approved HIGH-impact 60/60 news window in the source input (`mq5:24`), legacy framework window arguments (`mq5:287`), SPEC (`SPEC.md:97`), and all 13 backtest presets (representative `sets/QM5_9353_chande-stochrsi-base-cross-h4_EURUSD.DWX_H4_backtest.set:23`).
4. Kept open-position management and strategy exits ahead of entry-only filters: management begins at `mq5:322`, the H4 entry decision boundary at `mq5:340`, and `Strategy_NoTradeFilter` is not reached until `mq5:356`.
5. Added `perf-allowed` evidence to every raw series call (`mq5:116`, `:133`, `:145`, `:206`). The Common include chain, framework magic, MAE hook, fixed-risk backtest defaults, and no-ML contract remain intact.
6. Regenerated all 13 presets with `framework/scripts/gen_setfile.ps1` for their existing registered symbols/slots, then sealed news mode `4` and compliance mode `1`. Byte verification: `setfiles=13 crcrlf_sequences=0`; each preset has `RISK_FIXED=1000` and `RISK_PERCENT=0`.
7. Updated the SPEC H4 clock and real risk failure code (`SPEC.md:60`, `:95`) and added the execution-filter contract (`SPEC.md:97`).
8. Added nine focused pytest regressions covering the maximum StochRSI period, H4/60-minute contracts, management ordering, use-sites for every declared input, framework/magic/MAE/raw-series/no-ML rules, all setfiles, registry immutability, and fail-closed build identity (`tools/strategy_farm/tests/test_qm5_9353_rework_static.py:73-188`).
9. Refreshed `build_identity.json` to the repaired MQ5 SHA-256 `964e8d1e883fcb2225d68abcc8104e5297d4ff07ff49c48f0abf147a752958b0` and explicitly marked the pre-rework EX5 stale/do-not-use (`build_identity.json:2-9`). This avoids falsely binding the old binary to the repaired source.

## Validation evidence

### EA-focused pytest

Command:

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_9353_rework_static.py
```

Output:

```text
.........                                                                [100%]
9 passed in 0.84s
```

The input use-site audit reported every declared input at least twice; strategy inputs have 4-7 occurrences each.

### Related module tests

Command:

```text
python -m pytest -q tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_validate_symbol_scope.py tools/strategy_farm/tests/test_runtime_execution_contract_static.py tools/strategy_farm/tests/test_setfile_canonicalization.py
```

Output:

```text
................................                                         [100%]
32 passed in 32.40s
```

The full `test_build_gate_hardening.py` module completed with `28 passed, 1 failed in 453.54s`. The sole failure is an unrelated existing corpus finding in `QM5_41134_wti-mdaily-iqrmean-mom.mq5`; it does not name or inspect QM5_9353 as a failure. The required EA-scoped hardening check below is clean.

### Scoped static build gates

Commands and results:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_9353_chande-stochrsi-base-cross-h4
=> files_scanned=1, failures=[], warnings=[]; D10 buffers=0, D15 news=0, D7 MAE=0

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_9353_chande-stochrsi-base-cross-h4
=> PASS; Summary: 1 PASS, 0 FAIL

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_9353_chande-stochrsi-base-cross-h4
=> PASS; files_checked=14, findings=[]

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_9353_chande-stochrsi-base-cross-h4 --fail-on-leak --json
=> SINGLE_SYMBOL_OK; n_violations=0
```

### Authoritative build-check boundary

Command (scoped; never run unscoped):

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_9353_chande-stochrsi-base-cross-h4 -Strict -SkipCompile
```

Result:

```text
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED
terminal64 processes are alive; ad-hoc compile/build_check is refused.
pipeline_hint: python tools/strategy_farm/farmctl.py enqueue-compile <EA label>
retry_attempted: false
```

Per ticket constraints, no factory process was stopped, no compile work item was enqueued, and no existing work item was available for EA 9353 in the read-only state-DB query. Therefore no fresh EX5 or compile PASS is claimed. `build_identity.json:2-9` carries this boundary explicitly.

## Safety and unchanged surfaces

- No backtest was enqueued, run, deleted, or modified.
- No verdict row was overwritten.
- No gate threshold/criterion, factory state, AutoTrading state, registry allocation, or `T_Live` file was changed.
- State DB access was read-only via `file:D:/QM/strategy_farm/state/farm_state.sqlite?mode=ro`.

## Rollback

Revert the ticket commit with `git revert <ticket-commit>`. This restores the MQ5, SPEC, build identity, 13 presets, regression test, and this evidence note together. The checked-in EX5 was not changed, and no database, registry, factory, queue, verdict, or live-terminal rollback is required.

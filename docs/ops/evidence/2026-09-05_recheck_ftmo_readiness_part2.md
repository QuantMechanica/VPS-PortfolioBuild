# FTMO readiness part 2 narrow recheck — REJECT

Task `91ffb9e6-b484-4434-acb2-314898d504fa`. Exact revision `355d88586ec00b99c9739db349dd4c16b52ff667`. F5, the N1 replacement paragraph, the part-1 caption and both runbook residuals close. **N2 is still internally contradictory in part 2 §§B.2 and B.7**, and the B.6 action table still commissions an absent “new tool.” This is a documentary verdict, with no implementation acceptance or runtime authority.

Focused verification: 16 assertions and eight exact source/document bindings. All current files match the reviewed revision after LF normalization. `2026-09-05_recheck_ftmo_readiness_part2_verify.py` reproduces the JSON evidence without importing or executing operational code. No broad re-review of the previously checked returns, costs or provider rules was performed.

| Correction | Exact revised passage | Source check / closure |
|---|---|---|
| F5, §A.3 | “Runtime signed mint is currently blocked while freeze is ACTIVE: the generator checks the freeze before building or writing the pointer” | CLOSED. `generate_live_deployment_pointer.py:215` calls the guard before pointer construction; `risk_freeze.py:446` requires an INACTIVE/LIFTED state with explicit lift authority and timestamp. The atomic OWNER-act claim is removed. |
| N1, §B.2 | “defines eight lanes — XAUUSD, GER40.cash, GBPUSD, EURUSD, USDCAD, NZDUSD, USOIL.cash (evaluator symbol XTIUSD, source XTIUSD.DWX) and XAGUSD — and `NATIVE_SYMBOLS = tuple(SYMBOL_LANES)`” | Replacement is correct. AST extraction gives eight entries and the stated oil mapping. No surviving two-symbol-only claim occurs in the requested sections; the old symbol-gap tail remains below. |
| N2, §B.2 | “implementation delivered, not yet accepted” and “Review, approved installation and bound capture evidence are still required” | Correct replacement. Both named files exist in the reviewed commit. The MQL script appends schema `qm.ftmo-trial-telemetry.raw/v1`, balance/equity, position/order census and session/sequence values on a one-second timer. The Python consumer produces Prague-day/M5 checks. This proves implementation existence, not accepted capture quality. |
| N2, §B.4 | “The delivered pair `QM_FTMO_TrialTelemetry.mq5` + `ftmo_trial_telemetry.py` is designed to feed this stream but is not yet reviewed, installed or evidenced” | Correct qualification. The retained “new collector ... missing capture authority” tail should refer to acceptance and bound capture of the delivered pair, for consistency. |
| Part 1 caption | “Earlier captured swaps” | CLOSED. The following September snapshot is no longer contradicted by “Only captured swaps.” |
| Runbook §6 | “The current signed mint is blocked while the freeze is ACTIVE; this sequence starts only after the separately reviewed ceremony dependency is resolved” | CLOSED; consistent with the pre-write freeze guard. |
| Runbook provider table | “QM news blackout mandatory; preserve the approved calendar/filter contract in both research and trial” | CLOSED. The adjacent paragraph makes the QM requirement explicit independently of provider exemptions. |

The requested all-clear for §§A.3/B.2/B.4/B.7 cannot be given:

1. **§B.2, line 140:** “Until that collector exists” still treats implementation as absent, immediately after acknowledging the delivered pair. The same sentence retains “set-path (limit 1) and symbol (limit 2) gaps,” although the corrected N1 paragraph establishes all six candidate instruments' symbol support. Replace the tail with: “For-record capture remains blocked until the delivered collector is accepted, installed under approval and proven with bound evidence, and the live-risk set path and per-lane exact-profile verification are resolved.”
2. **§B.7, line 192:** “the only live instrument (`QM_AccountMonitor.mq5`) overwrites its snapshot every 60s” is followed by “until BOTH a trial runs AND the new Prague-day-keyed interval-minimum collector (B.2 hard limit 3) exists to persist it.” Existing retained data and the older deployed monitor can lack the required evidence; that does not mean the new implementation is absent. State that the delivered pair needs acceptance/installation and a bound trial capture. Preserve the requested restart, midnight and entry-window scenarios without claiming they were tested here.
3. **Related action table §B.6, line 206:** “commission a **Prague-day-keyed M5 interval-minimum + position/pending collector** (new tool)” still orders a duplicate build. Replace that action with review/acceptance of the delivered pair, approved installation and collection of the required evidence.

These are remaining passages in the current reviewed revision, not a request for a different implementation or a broader research program. The separate live-risk set-file gap, exact-profile verification and measured capture adequacy remain legitimate open dependencies. No pointer was signed, freeze lifted, collector installed, account opened, purchase made or AutoTrading changed. Leave the artifact in REVIEW.

# FTMO readiness re-review of 95343b30e9

Task `85e4d7d4-e63b-4cd4-b765-a1defb01e61e`. Reviewed exact commit `95343b30e9f4d5689cbcda33dad2c78bb440fe62`; all three current document files still match those committed bytes after LF normalization. Documentary acceptance only; no pipeline, deployment, purchase or runtime approval is granted.

| Document | Verdict | Basis |
|---|---|---|
| Readiness part 1 | **ACCEPT_WITH_RESIDUALS** | Corrected attribution, counts, costs and mandatory QM blackout are supported. One stale “Only captured swaps” caption remains, qualified immediately by the new snapshot paragraph. |
| Readiness part 2 | **REJECT** | F5 remains internally contradictory. Fresh checks also disprove the claims that only two symbols and no durable telemetry implementation exist. |
| Stage-transition runbook | **ACCEPT_WITH_RESIDUALS** | Numeric provider-rule anchors and OWNER action boundaries hold. Its signed-pointer/held-freeze prerequisite must explicitly inherit the unresolved ceremony dependency before operational use. |

The assertion that all nine findings are fully closed is not supported. Eight requested corrections are substantively present; F5 remains unclosed in part 2 §A.3. Two additional stale implementation claims were already false in the reviewed commit, rather than becoming false only after this review started.

Evidence: [machine-readable checks](2026-09-05_rereview_ftmo_readiness_packs.json), reproduced by [the read-only verification script](2026-09-05_rereview_ftmo_readiness_verification.py). The fresh sample has **20 anchors per document, 60 total**; 58 match and two implementation-existence claims fail. Separately, semantic inspection identifies the surviving F5 contradiction. All **15 hash-bound validation inputs still match**, and eight native reports were parsed again for counts and holding medians. No evaluation stream was opened.

## Closure of the original findings

1. **F1 — CLOSED.** Corrected part-1 rows say `13054 ... 82`, `1537 ... 96`, `20048 ... 60`, `21505 ... 116`. Re-parsing the hash-bound native Deals tables as strict IN/OUT lifecycles reproduces **82/96/60/116**, and the other four counts **360/91/195/63**. All eight medians also reproduce the validation. The position key remains explicitly derived, not mislabelled as a native `DEAL_POSITION_ID`.

2. **F2 — CLOSED.** Part 1 §A.5 now says **“−472.96 USD over the window `2026-08-01..2026-09-04` (65 closed positions, 24 active close-days)”**, and **“latest-30-active-close-day ... −1,436.59 USD.”** Independent Decimal addition of governed position lifecycles from the unchanged normalized deal export gives **−472.96 / 65 / 24**, and **−1,436.59 / 85 closes** for the latest 30 distinct close-dates. The source validation's calendar-day Sharpe fields support −1.325 and the stated bootstrap interval; the old −469 figure is now explicitly historical and incompletely windowed. No new bootstrap or unopened OOS data is needed to establish this correction.

3. **F3 — CLOSED WITH A MINOR CAPTION RESIDUAL.** Part 1 says **“Provisional swap now on disk for all six current-pool symbols”**; part 2 says **“provisional September-5 public-API snapshot; triple-rollover/platform and matched-session spread confirmation remain open.”** The six `symbols[]` rows reproduce the listed swaps, margin percentages and provisional rollover status. These values are not adopted into the governed cost model. Part 1 §B.3 still begins an older bullet with **“Only captured swaps”** for the July snapshot; change that to **“Earlier captured swaps”** to avoid contradicting the immediately following current snapshot. This does not hide the now explicit six-symbol evidence.

4. **F4 — CLOSED.** Both parts now state **“QM news blackout mandatory; preserve the approved calendar/filter contract in both research and trial.”** That agrees with the active charter at **`docs/ops/EDGE_LAB_CHARTER_2026-05-22.md:28`** and the cycle's OWNER instructions. The provider-only Swing exemption remains in its own explanation. The earlier review's abbreviated/mislocated charter reference is corrected here to `docs/ops/`; the verified requirement itself is unchanged.

5. **F5 — PARTIAL, STILL A BLOCKER IN PART 2.** The new §A.1 correctly says **“Runtime signed mint is currently blocked while freeze is ACTIVE”** and requires a separately reviewed resolution. Part 1 §C.1 now correctly limits signing to identity condition 1. But part 2 §A.3 still says **“the OWNER's written lift + the signed-mint command are one atomic OWNER act.”** `generate_live_deployment_pointer.py:215–218` checks the freeze before building or writing the pointer; `risk_freeze.py:446` requires an already durable INACTIVE/LIFTED state with lift authority and timestamp. Passing an approval path does not perform that state transition, and the written statement is not an atomic implementation of the two actions. Delete the retained atomic-act sentence and replace it with the same blocked-dependency wording already used in §A.1. No temporary lift, bypass, signing or state mutation was attempted in this review.

6. **F6 — CLOSED.** The draft command now carries **`--expected-server Darwinex-Live`**, **`--expected-phase DXZ_LIVE`**, a current-receipt placeholder and **`--dry-run`**. The pointer confirms those exact identity values. The generator defaults for the two identity arguments remain None and the dry-run branch returns before the write. The approval-bound wording accurately describes the boolean/evidence-path mechanism. The command is expressly a draft shape; its placeholders are not an approval record.

7. **F7 — CLOSED.** Part 2 and the runbook reference **R5 RATIFIABLE**, pending OWNER ratification, and retain the binding **`lower_95 ≥ 0.80`** plus adequacy guards. The current R5 LF-normalized SHA is exactly **`de549514fd75e68adb6f972a39451c89b43d7f7e35961e6e3da197903bfa923d`**. Part 2 adds **“Capture-only exploratory data may inform a new preregistration, but is ineligible as confirmation under that revised plan; for-record data remain sealed until the plan is ratified.”** This closes the result-dependent calibration ambiguity and does not lift NO-BUY.

8. **F8 — REQUESTED WORDING CLOSED; FRESH IMPLEMENTATION FINDINGS BELOW.** Trial risk is now bound to the **“future FTMO-specific reviewed manifest”**. REAL_TICKS is labelled a **“strategy-tester ... setting, not a live-account execution mode.”** The retained-artifact limitation now allows an instrumented replay while preserving the separate need for exact-profile execution evidence. The runbook labels a lower-risk verification profile a **“vorgeschlagene QM-Wahl, keine Provider-Vorgabe.”** Those distinctions hold. The separate assertions about which implementation files currently exist do not hold, as N1/N2 show.

9. **F9 — CLOSED.** Part 2 §A.4 says **“OWNER-authorized rollback after verification failure, preserving the failed pointer and receipt for audit; AI prepares the proposed bytes only.”** The draft retains server/phase identity fields. The generator's unsigned write capability does not imply authority to use it; the corrected paragraph no longer makes that inference.

## Fresh source findings and operational residuals

**N1 — HIGH, part 2 §B.2: the two-symbol limitation is stale.** `ftmo_lane_runner.py` at the reviewed commit already defines eight `SYMBOL_LANES`: XAUUSD, GER40.cash, GBPUSD, EURUSD, USDCAD, NZDUSD, USOIL.cash and XAGUSD. `NATIVE_SYMBOLS = tuple(SYMBOL_LANES)` includes all six distinct candidate instruments, with XTIUSD mapped to USOIL.cash. The code history identifies `222d1df879` as the symbol-pool expansion. Replace “only XAUUSD and GER40.cash; none of the candidates supported” with the delivered mapping and the remaining exact-profile verification/adoption status. The live-risk set-path guard still requires fixed risk and remains a separate unresolved issue; symbol support does not authorize a live trial.

**N2 — HIGH, part 2 §B.2/B.4/B.7: a durable telemetry implementation exists.** Both `framework/monitor/QM_FTMO_TrialTelemetry.mq5` and `tools/strategy_farm/portfolio/ftmo_trial_telemetry.py` are present in commit 95343b30e9. The monitor declares `qm.ftmo-trial-telemetry.raw/v1`, appends sampled balance/equity and position/pending-order census with session/sequence identity, and defaults to a one-second timer. The Python consumer validates/compacts that stream into Prague-day and M5 checks. This disproves “no on-disk tool” and “a new collector must be commissioned.” It does **not** establish approved deployment, complete capture, interval-minimum adequacy, restart coverage or a defect-free run. Replace the absence claim with **“implementation delivered; review, approved installation and bound capture evidence still required.”** This review does not independently accept that implementation or install it.

**Runbook residual — clarify the held-freeze prerequisite.** Section 6 still lists signed pointer first, then **“Freeze lift or deliberate hold ... while the pointer stands authenticated.”** Its reference to part 2 §A.1 points at the correct new blockage, but the local sequence can still be read as executable. Add **“the current signed mint is blocked while ACTIVE; this sequence starts only after the separately reviewed ceremony dependency is resolved.”** The runbook is explicitly dormant and its action table remains OWNER-only, so this is an operational-use residual rather than a new authorization.

The runbook's provider-news table is expressly sourced to the provider rulepack. For standalone operation, repeat the mandatory QM blackout beside that table; the provider exemption is not an internal exemption. Keep the provisional cost fields unadopted until their required confirmation is recorded.

## Authority check

The corrected action tables continue to reserve signing, freeze lift, account creation/login, terms acceptance, purchase, payout and AutoTrading for OWNER. AI work is limited to preparation, read-only verification, capture and separately authorized implementation. The rulepack still declares `runtime_integration=NOT_IMPLEMENTED`, `deploy_authorization=OWNER_ONLY`, `factory_action_authorized=false`, `mt5_action_authorized=false`. The explicit purchase exclusion and NO-BUY remain. No observed source change grants an AI seat additional authority.

The failed part-2 verdict is about the remaining procedure contradiction and stale implementation inventory, not an observed unauthorized action. Original drafting tasks remain in REVIEW. This re-review is also returned in REVIEW on canonical `agents/board-advisor`.

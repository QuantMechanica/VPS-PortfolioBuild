# Claude review — router task 50467e7e (QM5_411xx EA_INDICATOR_BUFFER_UNBOUNDED wave)

- Router task: `50467e7e-3148-4792-9223-083bd8ee2516` (ops_issue, assigned codex, state REVIEW)
- Worker artifact: `docs/ops/evidence/2026-08-23_codex_50467e7e_411xx_buffer_review.md`
- Worker verdict: `STATIC_REMEDIATION_PASS_COMPILE_PENDING`
- Fix commit reviewed: `565e5110f` ("fix(build): bound QM5_411xx generated buffers"), present in `agents/board-advisor` history
- Reviewer: Claude review lane · 2026-08-24 · read-only DB (mode=ro)
- Review verdict: **RECYCLE**

## What was verified good

1. **The 20 named EAs are correctly repaired.** The D10 checker
   (`build_gate_hardening.check_indicator_buffer_bounds`) reports **zero** findings
   across the 20 EAs named in the task (41104, 41109–41133). Spot-check
   `framework/EAs/QM5_41109_.../QM5_41109_...mq5:724` shows the fail-closed
   `completed_month_sessions >= ArraySize(ratios)` guard as claimed.
2. **At least one governed COMPILE_OK exists.** `work_items` row
   `cc09145c-a6d6-4cb0-88a4-e10ef58cc58d` (QM5_41109 COMPILE_EA) = `verdict=COMPILE_OK`,
   `status=done` — the repaired source compiles under the governed lane. Remaining
   named rows are pending/held, append-only, no overwrite observed (spot-check
   `62ce0b4a…` still `pending`). Consistent with the worker's append-only claim.
3. **No threshold/verdict weakening.** Diff of `compile_work_items.py` / `farmctl.py`
   in `565e5110f` adds append-only / `COMPILE_OK`-awareness only; no gate threshold,
   period, risk, or verdict-logic change. No EA input/threshold lines changed.

## Actionable defect (why RECYCLE)

The task's headline mandate was to **root-cause the shared template/generator defect
and fix the generator so the wave is fixed in one wave.** The "generator fix" is a
purely advisory prompt edit at `tools/strategy_farm/prompts/codex_build_ea.md:256-264`
— it relies on the LLM builder complying and on downstream review, not on a mechanical
guard at generation time. It did not hold: the identical
`EA_INDICATOR_BUFFER_UNBOUNDED` class recurs in **5 sibling EAs of the same
QM5_411xx commodity/XAUXAG wave, all built AFTER the fix commit (565e5110f, 2026-08-23
20:11)**:

| EA | build commit / time | D10 finding (file:line) |
|---|---|---|
| QM5_41134_wti-mdaily-iqrmean-mom | `9de298a2f` 08-23 22:29 | `:599` `daily_returns[first_retained]` |
| QM5_41135_xauxag-mdaily-iqrmean-rv | `51fbbfd19` 08-23 23:57 | `:824` `sorted_returns[i]` |
| QM5_41136_xng-mdaily-iqrmean-mom | `ac8257f05` 08-24 01:11 | `:599` `daily_returns[first_retained]` |
| QM5_41138_xauxag-mdaily-hl-rv | `d45395b64` 08-24 06:25 | `:832` `daily_returns[i]`; `:854` `pairwise_averages[i]` |
| QM5_41139_wti-mdaily-hl-mom | `c9de910e4` 08-24 07:21 | `:575` `daily_returns[left]`; `:591` `pairwise_values[pair_index]` |

7 findings total. Consequence: `test_build_gate_hardening.py::test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`
is **RED on the current source hash** (`1 failed, 46 passed` in the combined run),
i.e. D10 is not family-clean for QM5_411xx even though the 20 named EAs are.

## Required rework

- Add the same fail-closed `ArraySize`/loop-bound guards to the 7 sites above
  (41134, 41135, 41136, 41138, 41139), re-run `build_check -EALabel <label>` per EA
  (never unscoped), enqueue COMPILE_EA append-only.
- Make the generator defense enforcing rather than advisory: run the D10
  `check_indicator_buffer_bounds` gate at the generation/build step (or a pre-commit
  builder hook), so a probabilistic prompt is not the only barrier — the recurrence
  proves prompt text alone does not prevent the class.

The 20-EA static remediation itself is sound and its COMPILE_OK evidence is emerging;
this RECYCLE is scoped to the unmet systematic requirement plus the 5 named recurrences.
Do not mutate the existing append-only COMPILE_EA rows.

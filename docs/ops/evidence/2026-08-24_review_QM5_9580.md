# review_ea — QM5_9580_bandy-regslope-pullback-mr-index

- **Router task:** ac694029-ea0a-4d27-ae10-6a22963b8d0e (review_ea, assigned codex, state REVIEW)
- **Source build:** c3f03e05 (agy/gemini lane), source_verdict BUILD_READY_REVIEW
- **Reviewer:** Claude review lane, 2026-08-24
- **Verdict: APPROVED** (clean for Q02)

## Card-faithful mechanism
Card `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9580_bandy-regslope-pullback-mr-index.md`
(Bandy QTA 2015, source 9ef19e06) — long-only D1 index mean-reversion:
- Entry: OLS slope(50)>0 AND R²(50)>=0.30 AND RSI(2)<=10 AND close>SMA(200), one position/magic.
  Implemented in `Strategy_EntrySignal` (.mq5:129-171); `CalculateLinearRegression` (.mq5:49-108)
  is a correct closed-form OLS slope + R² over `CopyClose` of fixed `strategy_reg_period` bars.
- Exit: RSI(2)>=70 (`Strategy_ExitSignal` .mq5:196-204) + 5-day time stop
  (`Strategy_ManageOpenPosition` .mq5:173-194).
- SL: 2.5*ATR(14) via `QM_StopATR` (.mq5:165). All AND-combined, long-only, skip-incomplete-bar.
Matches the card mechanics exactly.

## Input use-site audit (no dead inputs)
All 11 strategy inputs grepped in the .mq5 have >=1 use-site beyond declaration:
reg_period(2), r2_min(2), rsi_period(3), rsi_entry_thresh(2), rsi_exit_thresh(2),
sma_period(2), time_stop_days(2), atr_period(3), sl_atr_mult(2), spread_max_atr(2),
warmup_bars(4). No dead inputs.

## Framework conformity
QM_Common chain, `QM_FrameworkInit`/`Shutdown`, MAE hook `QM_FrameworkTrackOpenPositionMae`,
`QM_KillSwitchCheck`, Friday close, news wired via `QM_NewsAllowsTrade2`/`QM_NewsAllowsTrade`,
magic via `QM_FrameworkMagic` (8 framework-call sites). RISK_FIXED=1000 / RISK_PERCENT=0 backtest.
Bounded CopyClose buffers, no raw-series abuse, no ML, no invented commission/swap.

## Magic / registry
`magic_numbers.csv`: ea_id 9580 slots 0-12 active (Codex governed allocator 2026-08-17).
Formula ea_id*10000+slot verified: slot0 GDAXI=95800000, slot2 SP500=95800002. Consistent.
`ea_id_registry.csv`: 9580 active, source 9ef19e06.

## Build / setfiles
`build_identity.json` build_check_passed=true. Live source hashes MATCH recorded:
mq5 5ec3aa4d…, ex5 b2d3f34e… → COMPILE_OK evidence valid for current source. 13 governed
D1 backtest setfiles (environment=backtest, RISK_FIXED=1000, RISK_PERCENT=0, slot matches registry).

## Minor defect (non-blocking, cosmetic doc)
`SPEC.md` §4/§5 auto-generated boilerplate lists "Base timeframe H1" and "12 trades/yr"; the EA
and card are D1 (all hooks use PERIOD_D1; setfiles are `_D1_backtest`). Generator (gen_spec_md.py)
boilerplate mismatch only — the executed .mq5 is D1-correct, no functional/Q02 impact.

## Verdict
APPROVED — card-faithful, framework-conformant, no dead inputs, magic/registry consistent,
build evidence valid for current hash. Clear for Q02. SPEC.md timeframe boilerplate is a
cosmetic doc nit for the generator, not a blocker.

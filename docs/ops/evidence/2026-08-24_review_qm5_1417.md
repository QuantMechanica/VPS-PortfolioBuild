# Review — QM5_1417 classical-pennant-continuation-h1 (router 98a8ca09)

- Router task: `98a8ca09-c712-4064-866e-cf7112c465d5` (state REVIEW, `review_ea`, source_agent gemini/agy, recycle_count 1)
- EA: `framework/EAs/QM5_1417_classical-pennant-continuation-h1/`
- Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1417_classical-pennant-continuation-h1.md` (`g0_status: APPROVED`)
- Current MQ5 (disk, CRLF) SHA-256: `81ef61c05c0fb6be24cdb1b58b537eeaf5f77fa30a9754afcbcaef264d22f3bc`
- Rework commit: `0c4d5e34d` ("rework(1417): restore card-faithful pennant execution")
- Verdict: **RECYCLE**

## Prior context
Recycled 2026-08-22 (Claude) for four semantic defects: invented pivot fallback, pending-stop replaced by market entry, inverted SMA slope, wrong news window. Codex re-review artifact `98a8ca09_qm5_1417_recycled_gemini_codex_review_2026-08-24.json` returned FAIL, but its findings 1-2 ("hashes unchanged / hardening reports six failures") were measured on a **stale checkout** — the canonical repo carries rework commit `0c4d5e34d` and the label-scoped hardening gate now reports 0 failures (re-run below). Codex findings 3-4 (raw CopyBuffer, compile gap) survive on the current source and are confirmed here.

## Original four defects — CONFIRMED FIXED
1. **Pivot fallback removed.** `Strategy_FitPivotsLine` returns false when `n < 2` (mq5:353); caller guards `ArraySize(highs) < 2 || ArraySize(lows) < 2 -> continue` (mq5:577). No fabricated +/-ATR trendline.
2. **Pending-stop entry restored.** `QM_BUY_STOP` (mq5:623) / `QM_SELL_STOP` (mq5:651) with `expiration_seconds = STRATEGY_PENDING_VALID_BARS * tf_seconds`; trigger/SL/TP derived from trigger price, SL capped at `strategy_sl_cap_atr` (card 2.0 ATR). Hardening D13 = 0 failures (evidence lines 623/651).
3. **SMA slope corrected.** CopyBuffer chronology gives `sma_vals[0]`=shift2 (older), `sma_vals[1]`=shift1 (newer); rising = `sma_vals[1] > sma_vals[0]` (mq5:314), falling = `<` (mq5:319). Card-faithful macro-bias. Hardening D14 = 0 failures, no inverted line.
4. **News window corrected.** Literal +/-180 min (= +/-3 H1 bars) entry-only blackout at mq5:794, placed after MAE/management/exits (management_line 835). Hardening D15 = 0 failures, entry_only true, placement checked.

Converging-trendline geometry is card-faithful: `s_up < 0`, `s_lo > 0`, slope-symmetry `<= strategy_penn_slope_symmetry_max`, apex-distance and retracement gates present (mq5:585-600).

## Input use-site check
All 46 declared inputs occur >= 2x (declaration + >=1 use-site). No dead inputs.

## Framework conformity
- QM_Common chain (`#include <QM/QM_Common.mqh>`), `QM_FrameworkInit(qm_ea_id, qm_magic_slot_offset, ...)` at OnInit (mq5:803); magic via framework resolver; registry = 14 active magic rows (`rg -c '^1417,.*,active$'`). MAE hook D7 = PASS. RISK_FIXED=1000 / RISK_PERCENT=0 in all 14 setfiles. No ML. Bounded indicator buffers (D10 PASS). Symbol scope SINGLE_SYMBOL_OK.

## Blocking defect (RECYCLE)
**EA_FRAMEWORK_RAW_COPYBUFFER** — raw `CopyBuffer` calls bypass the framework corset:
- `QM5_1417_classical-pennant-continuation-h1.mq5:303` — `CopyBuffer(g_h_sma_h4, 0, 1, 2, sma_vals)` (H4 SMA read)
- `QM5_1417_classical-pennant-continuation-h1.mq5:443` — `CopyBuffer(g_h_atr_h1, 0, 1, 1, atr_val)` (H1 ATR read)

Neither carries `// perf-allowed`, and `build_check.ps1:1082-1085` raises this as an **unconditional FAIL** with no override path ("a repeated codex_review system-class failure"). Fix: replace with framework reader helpers — `QM_SMA(_Symbol, PERIOD_H4, strategy_macro_sma_period, shift)` (QM_Indicators.mqh:555) for the shift-1/shift-2 SMA pair, and `QM_ATR(_Symbol, PERIOD_H1, strategy_atr_period, 1)` (QM_Indicators.mqh:514) for ATR; or `QM_IndicatorReadBuffer(handle, idx, shift)` (:281) if the raw handle is retained. Preserve the corrected chronology semantics from fix #3.

## Compile / binding gap (secondary)
The on-disk EX5 SHA-256 `a22a278f...30ee9` is the pre-rework rejected binary; no COMPILE_OK evidence binds the current MQ5 to any EX5 (rework doc `2026-08-23_rework-1417.md` states compile was refused INCLUDE_MIRROR_REFUSED under a live factory). After the CopyBuffer fix, a governed compile (OFF-window / COMPILE_EA queue) must rebuild the EX5 and produce COMPILE_OK before Q02. Not actioned here (recompile in active inventory is ROT; Claude does not compile).

## Gate evidence (re-run on current source, read-only)
- `build_gate_hardening.py --ea-label QM5_1417_classical-pennant-continuation-h1` -> failures=[] (D13/D14/D15/D18/D10/D7 all 0)
- `validate_build_guardrails.py` -> verdict=PASS
- `validate_symbol_scope.py` -> SINGLE_SYMBOL_OK
(These Python gates do not cover the raw-CopyBuffer corset rule, which lives in build_check.ps1 — hence the residual defect.)

## Recommendation
RECYCLE to build lane: swap the two raw CopyBuffer calls (mq5:303, 443) for QM_SMA/QM_ATR helpers, then governed recompile + COMPILE_OK, then re-review for Q02. The semantic rework itself is sound — this is the last corset item.

# rework-34001 — blocked by unresolved Strategy Card mechanics

## Status

`BLOCKED_CARD_CONTRACT`: no EA source change was made. The approved Strategy Card does not
define the Kalman filter's initial state, initial covariance, or warm-up/history contract, and it
shows break-even/trailing protection states without numerical triggers. Those choices materially
change entries and exits, so Development cannot supply them without inventing strategy mechanics.

Router task: `73eb18d9-a7db-4291-b90c-48ae1004dd92` (`RECYCLE` -> `IN_PROGRESS` while
investigated; final disposition recorded as `BLOCKED`).

## Evidence and findings

- Approved card identity is present and G0-approved:
  `strategy-seeds/cards/approved/QM5_34001_kalman-filter-state-estimation-scalper.md:1-20`.
- The card defines only the recursive Kalman equations, not the required initial state,
  covariance, or warm-up/history length:
  `strategy-seeds/cards/approved/QM5_34001_kalman-filter-state-estimation-scalper.md:76-79`.
- The current EA therefore invents a 100-bar window, oldest-close initial state, and `P=1.0`:
  `framework/EAs/QM5_34001_kalman-filter-state-estimation-scalper/QM5_34001_kalman-filter-state-estimation-scalper.mq5:61-75`.
- The card's lifecycle diagram includes protected break-even and trailing-stop states but supplies
  no trigger values: card lines `145-151`; the implementation hook is empty at EA line `189`.
- The exact take-profit contract is the posterior Kalman state (card line `98`), while the current
  source substitutes an unauthorized `1.5 * sl_dist` target at EA lines `162-180`.
- The original review artifact
  `D:/QM/strategy_farm/artifacts/reviews/73eb18d9-a7db-4291-b90c-48ae1004dd92.json`
  classifies the missing Kalman and protection-state definitions as upstream card defects and
  explicitly requires that they not be guessed by Development.
- Current deterministic registries are complete; no allocation repair is needed:
  `framework/registry/ea_id_registry.csv` has active EA `34001`, and
  `framework/registry/magic_numbers.csv:17379-17381` has active slots 0-2 for EURUSD.DWX,
  GBPUSD.DWX, and USDJPY.DWX. The resolver already contains magics `340010000-340010002`.

## Validation

Commands were run from `C:/QM/worktrees/rework-slot-4`.

1. `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_34001_kalman-filter-state-estimation-scalper`
   - Result: FAIL, 4 failures.
   - Failures: three `EA_CARD_LOSS_LIMIT_MISMATCH` findings (2.0%, 2.5%, 5.0%) and one
     `EA_BROKER_TIME_USED_FOR_GMT_WINDOW` finding.
   - Registry/universe checks: PASS for all three card symbols.
2. `powershell.exe -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_34001_kalman-filter-state-estimation-scalper -SkipCompile`
   - Result: refused before validation by `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because live
     `terminal64` processes exist.
   - No retry, compile enqueue, factory toggle, or terminal interference was attempted.
3. `python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py`
   - The whole-module attempt advanced through seven tests but did not yield a terminal result while
     several independent workers were running the same suite. It emitted no failure before the
     runner session stalled, so it is not claimed as a PASS.
4. Targeted contractual rerun:
   `python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py::test_d2_loss_limit_matching_and_mismatching_fixtures tools/strategy_farm/tests/test_build_gate_hardening.py::test_d5_raw_broker_hour_fails_and_utc_conversion_passes tools/strategy_farm/tests/test_build_gate_hardening.py::test_nonmechanizable_semantic_classes_name_required_card_declarations`
   - Result: PASS, `3 passed in 0.92s`.
5. `python -m pytest -q tools/strategy_farm/tests/test_agent_router_state_exits.py`
   - Result: PASS, `24 passed in 33.90s`.

## Files changed

- Added this evidence note only.
- No EA, setfile, framework include, registry, resolver, verdict row, queue, factory, or live file
  was changed.

## Required upstream decision

Research/OWNER must amend and re-approve the Strategy Card with all of the following before this
EA can be mechanically implemented:

1. Kalman initial state and initial covariance.
2. Warm-up/history length and restart reconstruction/persistence contract.
3. Break-even trigger, protected stop level, trailing trigger, and trailing-distance mechanics,
   or an explicit statement that the diagram is non-operative.

After that amendment, rework can implement the already-objective findings: exact loss/DD limits,
three-tick slippage, UTC rollover conversion, fail-closed spread data, exact-state TP rejection,
entry-only filter ordering, input validation, and canonical framework-only includes.

## Rollback

This commit is documentation-only. Roll back by reverting the commit that adds this file. Router
state is operational metadata and should be reopened only after an OWNER-approved card amendment;
do not overwrite historical review/verdict evidence.

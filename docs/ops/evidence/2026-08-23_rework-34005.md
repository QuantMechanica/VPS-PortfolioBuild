# rework-34005 — deterministic card-defect blocker

**Ticket:** `rework-34005`

**Router task:** `feb8cb93-40b3-4eef-b8ce-0edf12b4207f`

**EA:** `QM5_34005_sokolov-cstrategy-donchian-atr-breakout`

**Disposition:** `BLOCKED` — the approved card does not define its mandatory trailing-stop mechanism completely enough to implement without inventing strategy rules.

## What changed

- Added this evidence record only.
- No EA source, setfile, binary, framework include, registry, gate criterion, verdict row, queue, factory state, or live terminal was changed.
- The existing magic allocation is valid and was left untouched:
  - `framework/registry/ea_id_registry.csv:4444`
  - `framework/registry/magic_numbers.csv:17382-17384`

## Review work order reproduced

The review artifact records the blocking trail finding at
`docs/ops/evidence/30f8c4ef_qm5_34005_gemini_codex_review_2026-08-23.json:20`:
the EA substitutes `QM_TM_TrailATR` for a card-declared dynamic parabolic ATR
trail, while the card provides no parabolic acceleration formula.

The same artifact also records the independently repairable defects:

- direct non-corset include and dual-positive risk defaults (`:16-17`);
- three missing loss-limit contracts and raw broker time for the GMT window (`:18-19`);
- missing H4 attachment enforcement and three-tick slippage configuration (`:21-22`);
- unsupported deferred-smoke waiver (`:23`).

The current source confirms the rejected substitution at
`framework/EAs/QM5_34005_sokolov-cstrategy-donchian-atr-breakout/QM5_34005_sokolov-cstrategy-donchian-atr-breakout.mq5:175`.

## Why the card defect is terminal for this build ticket

The approved card is G0-approved at
`strategy-seeds/cards/approved/QM5_34005_sokolov-cstrategy-donchian-atr-breakout.md:10`
and makes the dynamic parabolic ATR trail a mandatory position-exit rule at
`:99`. Its complete MQL5 parameter table at `:163-165` defines only Donchian
period, ATR period, and risk percent. It defines no acceleration step, maximum
acceleration, activation condition, extreme-price state, ATR-to-acceleration
mapping, or closed-bar/tick update rule for the trail.

The cited Sokolov primary source is an architecture article, not this strategy:

- <https://www.mql5.com/en/articles/2169> describes the CStrategy event model and
  contains no Donchian or ATR rule.
- <https://www.mql5.com/en/articles/2411> states that each trailing algorithm has
  unique configuration parameters, then implements classical and moving-average
  examples; it contains no ATR or Parabolic algorithm.

Consequently, none of the following is card-faithful:

1. retaining the rejected plain ATR ratchet;
2. choosing standard Parabolic SAR constants such as `0.02/0.2`;
3. inventing a custom ATR-scaled acceleration formula; or
4. deleting the mandatory trailing exit.

Per the `qm-build-ea-from-card` preflight boundary and the ticket's explicit
"card-defect beyond repair" branch, implementation stops fail-closed. The
approved card must first be superseded or amended through OWNER-governed research
with a deterministic trail formula and parameter defaults/ranges.

## Deterministic validation evidence

Scoped hardening command:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_34005_sokolov-cstrategy-donchian-atr-breakout
```

Result: exit `0` for the diagnostic runner, with four reported EA failures:

```text
EA_CARD_LOSS_LIMIT_MISMATCH daily_entry_halt=2%
EA_CARD_LOSS_LIMIT_MISMATCH daily_hard_stop=2.5%
EA_CARD_LOSS_LIMIT_MISMATCH total_drawdown_stop=5%
EA_BROKER_TIME_USED_FOR_GMT_WINDOW
```

The same output reports `files_scanned: 1`, `magic_numbers.valid: true`, and exact
symbol-matrix matches for EURUSD.DWX, SP500.DWX, and XTIUSD.DWX.

Focused hardening tests:

```text
python -m pytest -q \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_d2_loss_limit_matching_and_mismatching_fixtures \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_d5_raw_broker_hour_fails_and_utc_conversion_passes \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_build_check_applies_hardening_pass_and_fail_fixtures

...                                                                      [100%]
3 passed in 46.99s
```

The full hardening test module was not used as completion evidence: its canonical
fleet scan is intentionally long-running and was stopped after the first seven
tests passed. The focused tests above cover the reproduced mechanical failures
and completed successfully.

No compile or smoke was run because producing a binary with an invented exit rule
would violate the approved-card contract; the ticket forbids backtest enqueue.

## Required upstream repair

Research/OWNER must publish a superseding approved card that defines, at minimum:

- the exact parabolic ATR stop recurrence/formula;
- acceleration/step and cap defaults plus allowed ranges;
- ATR period/multiplier roles without overloading the initial-SL multiplier;
- activation, update cadence, long/short extreme handling, and restart-state rule.

After that approval, this ticket can be rerouted for the remaining mechanical
repairs, scoped compile, smoke evidence, and review resubmission.

## Rollback

Remove this evidence file and revert its dedicated commit. There are no runtime,
database, registry, EA, setfile, binary, queue, factory, or live changes to undo.

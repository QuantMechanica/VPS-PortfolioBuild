# rework-40005 — blocked approved-card mechanics

- Router task: `96b77b6a-9abe-4acc-8c4d-9f59d2b9bf3a`
- EA: `QM5_40005_tradingview-multitimeframe-supertrend-atr`
- Disposition: `BLOCKED_CARD_MECHANICS`
- Authoritative approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_40005_tradingview-multitimeframe-supertrend-atr.md`
- Approved-card SHA-256: `094bee5401161ba2376bc6f0bf827dba4dd922df4df63a6810e16e4bf9d070a7`
- Existing MQ5 SHA-256: `9b18e8943caea8222cc4c06405bc2b926046e1ec93ba73c244fae01280ed7bbe`
- Existing EX5 SHA-256: `bdef574b18f3f875801f27a6419d8e708df762e3142a8b2eba1ee70eeb98bb5e`

## What changed

Only this evidence file was added. No EA source, binary, preset, strategy card,
registry, resolver, framework include, gate criterion, verdict row, backtest queue,
factory state, terminal, or `T_Live` file was changed.

The task was not resubmitted to REVIEW because the ticket explicitly requires a
`BLOCKED` disposition when the finding is a card defect beyond Development's
authority to repair. Writing a code or unit-test change for a guessed Supertrend
variant would falsely turn an upstream strategy decision into an implementation
decision.

The required router claim succeeded first, followed by the conditional close. A
read-only `mode=ro` query of `agent_tasks` verified at
`2026-08-24T09:14:44+00:00`:

```text
id=96b77b6a-9abe-4acc-8c4d-9f59d2b9bf3a
state=BLOCKED
artifact_path=docs/ops/evidence/2026-08-23_rework-40005.md
verdict=BLOCKED_CARD_MECHANICS: approved card omits recursive Supertrend initialization, warm-up, tie, and flip/trailing semantics; Development cannot invent strategy mechanics.
```

## Deterministic blocker

The runtime-approved card is still the same byte identity cited by the prior blocker
and remains `g0_status: APPROVED`, H1, non-ML, for the three governed symbols (card
lines 8-10, 33, 37-38, 44). It specifies only the one-bar basic-band expression at
line 76, BULL/BEAR observations at lines 90-93, and the stop/trailing statements at
lines 97-98. It does not specify the path-dependent information required to derive
those values mechanically:

1. final upper/lower-band recurrence and flip ordering;
2. initial trend and final-band state;
3. deterministic history boundary/warm-up;
4. equality/tie behavior at the median and final bands; or
5. whether the two-pip buffer applies to every trail and whether an opposite flip is
   a market exit or only changes the trailing boundary.

The cited source directory
`strategy-seeds/sources/tradingview-multitimeframe-supertrend-atr-official-source/`
does not exist (directory count `0`). Therefore there is no durable approved source
implementation that resolves the omissions. The earlier blocker records the same
gap at
`docs/ops/evidence/788cf6e9_qm5_40005_card_contract_blocker_2026-08-23.md:25-45`
and the required upstream amendment at line 83.

The current rejected source demonstrates why guessing is unsafe. It invents a
100-bar warm-up (`.mq5:52,68`), initialization from `close >= median`
(`.mq5:95-97`), recursive/tie choices (`.mq5:102-107`), 0.5-3.5 ATR stop clamps
(`.mq5:172-173,196-197`), and a separate flip exit
(`.mq5:252-273`). The review work order independently rejects the pip conversion,
ATR cap, per-tick reconstruction, market flip exit, and absent loss limits at
`docs/ops/evidence/96b77b6a_qm5_40005_gemini_build_codex_review_2026-08-18.md:19-56`.

## Governed identity and input audit

- EA registry: `framework/registry/ea_id_registry.csv:4492` binds active ID 40005
  to the exact slug.
- Magic registry: `framework/registry/magic_numbers.csv:17531-17533` contains the
  existing active EURUSD/GBPJPY/XAUUSD slots 0-2. No allocation gap exists, so the
  registry and resolver were not regenerated.
- All declared inputs have executable use-sites in the rejected source: framework
  inputs at `.mq5:287-302,353-356`; strategy inputs at
  `.mq5:68,121,126,145,150,155,159,163,177,201,219,222,256`; symbol-slot uses at
  `.mq5:184,208`.
- The source includes the `QM_Common.mqh` umbrella (`.mq5:6`), initializes the
  framework at `.mq5:285-303`, and calls the explicit MAE hook at `.mq5:321`.
  Those conforming surfaces do not authorize the missing strategy mechanics.

## Validation evidence

Commands were run from `C:/QM/worktrees/rework-slot-5` on 2026-08-24.

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_40005_tradingview-multitimeframe-supertrend-atr
FAIL (expected): 6 findings
  2 x EA_PIP_DOUBLE_CONVERSION (.mq5:163,219)
  3 x EA_CARD_LOSS_LIMIT_MISMATCH (2.0%, 2.5%, 5.0%)
  1 x EA_BROKER_TIME_USED_FOR_GMT_WINDOW
```

```text
python tools/strategy_farm/validate_build_guardrails.py <MQ5> <three scoped setfiles>
PASS: MQ5 + EURUSD.DWX + GBPJPY.DWX + XAUUSD.DWX; max stale news 336 hours

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_40005_tradingview-multitimeframe-supertrend-atr
PASS: 1/1
```

These two PASS results cover their narrow structural/preset contracts only and do
not resolve the strategy-card ambiguity or the six hardening failures.

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 \
  -EALabel QM5_40005_tradingview-multitimeframe-supertrend-atr -RepoRoot . -SkipCompile
REFUSED: LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
```

The check was correctly scoped to this EA. It refused before validation because
`terminal64` factory workers were alive. No retry, bypass, factory toggle, compile
enqueue, process stop, terminal launch, or backtest was attempted.

```text
python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py \
  tools/strategy_farm/tests/test_build_guardrails.py \
  tools/strategy_farm/tests/test_review_repair.py -q
55 passed, 1 failed in 375.31s
```

The single failure is an unrelated fleet-wide assertion for existing QM5_411xx
sources (`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`), reporting
seven buffer findings beginning with QM5_41134. It does not reference or exercise
QM5_40005.

```text
python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py -q \
  -k 'not test_qm5_411xx_sources_have_no_unbounded_numeric_buffers'
28 passed, 1 deselected in 327.41s
```

## Required unblock

Research/OWNER must amend and approve the card (or attach an immutable approved
source implementation) defining the recurrence, initialization, warm-up/history
boundary, equality/tie semantics, and exact buffer/trailing/flip-exit behavior.
Only then can Development remove the rejected mechanics, implement the declared
2.0%/2.5%/5.0% loss controls and three-tick slippage contract, cache closed-bar
indicator state, compile under the governed build lane, and resubmit a hash-bound
binary and presets.

## Rollback

This commit changes documentation only. Revert it with `git revert <commit>`.
That does not make the EA buildable; the router task should be re-opened only after
an amended OWNER-approved card or immutable approved source is available.

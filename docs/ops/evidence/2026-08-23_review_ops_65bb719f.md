# Review — SP-E3 Provider-versioned Rulepacks (Darwinex Zero / FTMO)

Router task: `65bb719f-75d5-4e73-a21b-69daeaaf8976` (`SP-E3`, task_type `ops_issue`, zone GRUEN, priority 46)
Reviewer: Claude (review lane), 2026-08-23
Worker deliverable: `docs/ops/RULEPACK_REVIEW_SLA_2026-08-23.md`, commit `3cc3f5e33` (+ tracker `33947ed77`)

## Acceptance criterion
"Rulepacks mit Quelle/Abrufdatum/Review-SLA vorhanden und regelmaessig geprueft."
Hard constraint: "Quelle+Abrufdatum dokumentiert (keine erfundenen Werte)."

## Verification performed (read-only)

1. **Rulepack snapshots present with source + retrieval date.**
   - `docs/ops/evidence/2026-08-23_ftmo_official_rules_snapshot.json` — schema `qm.ftmo-official-rules-snapshot/v2`, `retrieved_at_utc` 2026-08-23T07:46:57Z, 7 `ftmo.com` source URLs each with http_status/response_bytes/sha256/last_modified. Supersedes the v1 2026-07-29 snapshot (which was 25d past its own 7d ceiling — the concrete gap this task closes). FTMO required fields covered: 5% max daily loss (`two_step_maximum_daily_loss_percent_of_initial`), 10% max loss, CE(S)T reset (`maximum_daily_loss_reset_timezone`=Europe/Prague, `_local_time`=00:00 CE(S)T), fees (`fee_recurring`=false, refund model), equity/floating-P/L breach operator (`EQUITY_STRICTLY_BELOW_LIMIT`).
   - `docs/ops/evidence/2026-08-23_darwinex_zero_risk_engine_snapshot.json` — schema v1, VaR range 3.25–6.5%, D-Leverage caps 16.25/13/9.75 by hold-duration bucket, leverage formula, 45d VaR lookback. Primary source `help.darwinex.com/risk-manager` raw-captured; secondary `document360` recorded as SECONDARY_RENDERED_FETCH_ONLY with nulls + capture_note (CDN redirect loop), not fabricated.

2. **No invented values (hard constraint met).** FTMO exact fee-by-tier = `null` with `unretrievable_by_automated_fetch` note (JS-rendered widget). Darwinex short-hold bucket boundary disagreement (<15min vs <30min) recorded verbatim in `d_leverage_bucket_boundary_discrepancy` as NOT_RESOLVED rather than silently picked; QM's operating figure (9.75x, >60min) is agreed by both sources and unaffected.

3. **Review-SLA present and operational.** `RULEPACK_REVIEW_SLA_2026-08-23.md` defines a 7-day freshness ceiling (for purchase/live/leverage decisions) + append-only refresh process. A separate additive tracker (`tools/strategy_farm/rulepack_review_sla.py` + `config/target_rulepack_review_sla.json`) fail-closes any rulepack with no review entry as overdue; both `DXZ_BETTER_BOOK_V1` and `FTMO_2S_100K_SWING_V1` recorded CONFIRMED_UNCHANGED 2026-08-23, next due 2026-11-21.
   - Tests: `python -m pytest tools/strategy_farm/tests/test_rulepack_review_sla.py -q` → 5 passed.
   - `python tools/strategy_farm/rulepack_review_sla.py status` → both entries OK.

4. **Commit integrity.** `3cc3f5e33` and `33947ed77` are on `agents/board-advisor`; superseded v1 file retained; working tree clean for these paths.

## Minor note (non-blocking)
The SLA doc names a 7-day freshness ceiling for the snapshot files while the tracker config uses a 90-day `interval_days` cadence. These are two distinct mechanisms (7d = gate before any purchase/live/leverage decision; 90d = routine background re-check), both documented and internally coherent — no action required, flagged for awareness.

## Verdict
APPROVED — acceptance fully met: versioned rulepacks with documented source + retrieval date, no invented values (nulls/verbatim-disputes where evidence was unreachable), and an operational, fail-closed Review-SLA with passing tests.

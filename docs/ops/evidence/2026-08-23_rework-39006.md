# rework-39006 review-fix evidence

- Router task: `8893bf1e-0f1b-4270-a0f5-559def0c771b`
- EA: `QM5_39006_forexfactory-spudfyre-stochastic-ribbon`
- Worktree: `C:/QM/worktrees/rework-slot-6`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_39006_forexfactory-spudfyre-stochastic-ribbon.md`
- Disposition: source/static repair complete; compile and smoke remain fail-closed behind the active-terminal interlock.

## Work order and preflight

The first Codex review recorded the H1/D1 mismatch, pip double-conversion,
non-structural ATR stop cap, and absent card loss limits at
`docs/ops/evidence/8893bf1e_qm5_39006_gemini_build_codex_review_2026-08-18.md:19`,
`:28`, `:38`, and `:46`. Those four findings were already repaired by ancestor
commit `b96ef1004` and remain covered by the focused tests in this change.

The recycle work order is the later independent review:

- dual-positive risk defaults:
  `docs/ops/evidence/de4a48c9_qm5_39006_gemini_codex_review_2026-08-23.json:16`;
- broker-time rollover instead of GMT:
  `docs/ops/evidence/de4a48c9_qm5_39006_gemini_codex_review_2026-08-23.json:17`;
- missing three-symbol-tick entry deviation:
  `docs/ops/evidence/de4a48c9_qm5_39006_gemini_codex_review_2026-08-23.json:18`;
- missing smoke evidence:
  `docs/ops/evidence/de4a48c9_qm5_39006_gemini_codex_review_2026-08-23.json:19`.

The canonical card is `g0_status: APPROVED`, H1, and declares the 2.0% entry
halt, 2.5% daily hard stop, 5.0% total drawdown stop, and three-tick slippage
limit at lines 10, 37, 87, and 112-114 respectively. Preflight found the
existing EA registry row at `framework/registry/ea_id_registry.csv:4485` and
the three active magic rows at `framework/registry/magic_numbers.csv:17521-17523`.
No registry gap existed, so neither registry nor resolver was changed.

The router claim initially encountered the governed sweep's SQLite writer
lock. After the sweep exited, the requested command succeeded and confirmed:

```text
state: IN_PROGRESS
verdict: rework burn-window slot 6
updated: true
```

## What changed

1. Backtest-safe framework risk defaults: `RISK_PERCENT=0.0` with
   `RISK_FIXED=1000.0` at
   `framework/EAs/QM5_39006_forexfactory-spudfyre-stochastic-ribbon/QM5_39006_forexfactory-spudfyre-stochastic-ribbon.mq5:28-29`.
2. The card's 23:55-00:05 GMT blackout now converts broker time through
   `QM_BrokerToUTC` before evaluating the clock at the same source file line
   129.
3. Added a bounded `strategy_max_slippage_ticks=3.0` input (line 58), rejects
   non-positive or greater-than-three values (line 262), converts symbol ticks
   to MT5 deviation points using `SYMBOL_TRADE_TICK_SIZE / SYMBOL_POINT`
   (lines 282-289), and reconfigures the framework entry path through
   `QM_EntryConfigure` (lines 291-297).
4. Added the explicit three-tick value to all three H1 backtest setfiles at
   line 33 and documented it in `SPEC.md:34`; the v3 revision note is at
   `SPEC.md:99`.
5. Added six pytest regressions at
   `tools/strategy_farm/tests/test_qm5_39006_review_rework.py:30-139` covering
   hardening, prior-review mechanics, risk/UTC/slippage/framework wiring,
   every declared input's use-site, bounded framework indicator access, and
   exact setfile/magic scope.

The existing H1 execution contract, card-faithful structural stops, +1R
break-even, kill-switch values, MagicResolver-backed framework initialization,
and MAE hook remain at source lines 299, 149-198, 216-244, 304-309, 265-280,
and 325 respectively. No ML or raw series/indicator-handle calls were added.

## Validation evidence

### Focused EA regressions

Command:

```text
python -m pytest tools/strategy_farm/tests/test_qm5_39006_review_rework.py -q
```

Output:

```text
......                                                                   [100%]
6 passed in 0.68s
```

### Build-gate hardening

Command:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_39006_forexfactory-spudfyre-stochastic-ribbon
```

Result: `files_scanned=1`, `failures=[]`, `warnings=[]`; D2/D3/D5/D7/D10 and
the authorized-universe/magic checks all passed.

### Guardrails and SPEC

Commands:

```text
python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 framework/EAs/QM5_39006_forexfactory-spudfyre-stochastic-ribbon
python framework/scripts/validate_spec_doc.py framework/EAs/QM5_39006_forexfactory-spudfyre-stochastic-ribbon
```

Output:

```text
validate_build_guardrails: PASS; files_checked=4; findings=[]
PASS  QM5_39006_forexfactory-spudfyre-stochastic-ribbon
Summary: 1 PASS, 0 FAIL (of 1)
```

### Touched build-tool tests

The unfiltered command ran 56 tests and produced:

```text
1 failed, 55 passed in 555.16s (0:09:15)
```

The sole failure was the pre-existing repository-wide
`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` assertion, beginning
with `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`; this ticket does not touch any
411xx EA. The scoped rerun deselecting only that unrelated corpus assertion
completed cleanly:

```text
..........................................                  [100%]
55 passed, 1 deselected in 301.16s (0:05:01)
```

### Compile and smoke interlock

Both the compile-capable scoped build check and an attempted scoped
`-SkipCompile` invocation were refused at the same mandatory preflight:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_39006_forexfactory-spudfyre-stochastic-ribbon
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED: terminal64 processes are alive;
ad-hoc compile/build_check is refused. Use the governed pipeline path.
```

The refusal occurred before compile or report creation. Per ticket boundaries,
no factory toggle, terminal interruption, compile/backtest enqueue, or T_Live
write was performed. `rg --files D:/QM/reports/smoke | rg 39006` returned no
matches, so no smoke PASS is claimed. The tracked EX5 and existing setfile
`build_hash` comments remain the prior build identity and are intentionally not
represented as compiled evidence for this repaired source.

## Rollback

After commit, rollback is a normal `git revert <ticket-commit>` of the explicit
paths listed in this evidence. That restores the prior MQ5, SPEC, and three
setfiles and removes the focused test/evidence files. No registry, resolver,
database verdict, queue, factory, terminal, or live state requires rollback.

## Residual risk / reviewer action

- Source and static validation are complete, but a new EX5 and smoke report do
  not exist because the mandatory active-terminal interlock refused them.
- Before pipeline handoff, run the governed compile/build identity refresh and
  build smoke when an authorized idle terminal window is available. Do not
  infer runtime PASS from the prior EX5.

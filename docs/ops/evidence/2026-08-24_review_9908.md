# QM5_9908 bandy-psar-flip-trend — Claude review-lane closure

Date: 2026-08-24 UTC
Router task: `9bd88385-fdac-4b6d-9437-0cdc14fb3f25` (review_ea, assigned codex)
Source: build_ea `ce7ef250-...` (gemini/agy), reviewed by Codex
(`docs/ops/evidence/2026-08-23_qm5_9908_gemini_code_review.md`, REQUEST_CHANGES).
Claude verified Codex's findings independently against card + source, read-only.

Reviewed:
- `framework/EAs/QM5_9908_bandy-psar-flip-trend/QM5_9908_bandy-psar-flip-trend.mq5`
  (SHA256 `fb914b7c...da13`, matches build_identity)
- `.../SPEC.md`, `.../build_identity.json`, 13 D1 setfiles
- Card `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9908_bandy-psar-flip-trend.md`
  (`g0_status: APPROVED`)
- Compile log `framework/build/compile/20260822_120535/...compile.log`: 0 errors, 0 warnings.

## Verdict: RECYCLE

Card-faithful mechanism deviations block Q02 promotion. Three defects independently
confirmed at file:line; all actionable.

### Confirmed defects

1. **Exit reachability bug (HIGH).** `OnTick` runs `if(Strategy_NoTradeFilter()) return;`
   (mq5:216) *before* `Strategy_ManageOpenPosition()` (mq5:220). `Strategy_NoTradeFilter`
   returns true during warmup (`iBars < strategy_warmup_bars`, mq5:52) or wide spread
   (mq5:62). So an open position's 60-bar time-stop and PSAR-flip exit are skipped on any
   such bar — mandatory exits become suppressible by an entry-oriented filter. Management
   must be reachable regardless of entry eligibility.

2. **Risk sizing bound to catastrophic stop, not PSAR distance (HIGH).** Card §Position
   Sizing: "$1,000 risk based on the initial distance between entry price and PSAR level
   (typically ~1.5–2.5 * ATR)". Both entry branches set `req.sl = QM_StopATR(... 4.0*ATR)`
   (mq5:97, 115). Framework derives `sl_points` → `QM_LotsForRisk`
   (`QM_RiskSizer.mqh:414`) from `req.sl`, so the EA sizes against the wide 4-ATR
   catastrophic backstop, ~1.6–2.7× the card's intended risk distance. Different exposure
   model, not a parameterization.

3. **Invented spread filter not in card (HIGH).** `strategy_spread_max_atr = 0.30`
   (mq5:44) and the spread reject in `Strategy_NoTradeFilter` (mq5:59–63) have no card
   basis (card defines no spread/ATR gate). Compounds defect 1.

### Secondary (also actionable)

4. **Symbol scope expansion.** Setfiles add `GDAXI.DWX`, `UK100.DWX` and omit the card's
   named oil CFD (`XTIUSD.DWX`); card names FX majors, XAUUSD, oil CFD, NDX/WS30,
   SP500 backtest-only. Restrict to the approved universe or obtain a card amendment.

5. **D1 contract undeclared / chart-TF entry gate.** `OnInit` (mq5:186) does not declare a
   D1 execution contract; entry is gated by bare `QM_IsNewBar()` (mq5:245), whose default
   is `PERIOD_CURRENT` (`QM_Indicators.mqh:148`) while all signal reads are `PERIOD_D1`.
   Benign under a D1 backtest chart but not card-robust (intraday chart / Q14 sweep would
   reconsider the same D1 flip each intraday bar).

6. **Producer evidence incomplete.** `build_identity.json` lacks canonical result fields
   (task_id, ea_id, ea_dir, magic_base, smoke_result/report) per SCHEMAS.md; no smoke
   result supplied.

## Checks that passed

- Compiles clean (0/0). QM_Common chain, MAE hook (`QM_FrameworkTrackOpenPositionMae`),
  kill-switch, Friday-close, news wiring, timer/transaction/tester hooks all present.
- All 8 declared strategy inputs have use-sites beyond declaration (grep) — **no dead input**.
- Two-bar PSAR flip detection, 200-SMA regime gate, long/short symmetry, one-position
  enforcement, 60-bar time stop, close-before-reverse all materially correct.
- Bounded QM_SAR/QM_SMA/QM_ATR indicators; no raw CopyBuffer, no ML, no invented
  commission/swap, no raw OrderSend.
- 13 setfiles: `RISK_FIXED=1000 > 0`, `RISK_PERCENT=0`. MQ5 SHA matches build_identity.

## Disposition

No source/binary/registry/setfile/verdict/trade-stream changed. T_Live/AutoTrading
untouched. Task closed RECYCLE; a corrected gemini rebuild + fresh Codex review required
before enqueue. Aligns with the prior Codex REQUEST_CHANGES.

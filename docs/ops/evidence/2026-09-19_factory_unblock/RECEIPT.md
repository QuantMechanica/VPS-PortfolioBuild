# Factory unblock — 2026-09-19 08:45–09:20Z (Fable, OWNER-DEC-FABLE-FULL-EXECUTIVE-AUTHORITY-20260917)

Directive (OWNER, 2026-09-19 ~08:45Z): seven-point factory unblock, GRÜN-zone only. All actions below used
existing tools with unchanged criteria; no verdict or evidence was deleted or overwritten; T_Live, FTMO Demo and
AutoTrading were not touched (FTMO trial pulse RUNNING 6/6, T_Live RUNNING, both before and after).

## 1. Codex build lane (repo_dirty_build_guard)

- Cause confirmed as `_repo_dirty_status` = 49 blocking entries (classification v2), not auth: 9 tracked
  modifications (evidence-cohort baseline appends, P5 calibration autostubs, `ea_origin` rows, regenerated FTMO
  readiness + research maps, the 44 GB park plan regenerated by the hold tool) + 40 untracked evidence files
  (27 Q08-context hold receipts, stranded-infra triage sweeps 09-12..09-19, critic-wave and opt-sibling receipts).
  The deleted tracked file `artifacts/qm5_41262_build_result_20260901.json` was restored (evidence immutability).
- Commit `fbc9284107` (explicit pathspecs, secret scan clean, EX5 guard PASS). Guard after: `blocked:false`,
  `count:0`, `generated_count:484` (setfile/ex5 churn stays ignored by design).
- Verification: `farmctl health` → `codex_zero_activity OK | 1 codex builds in 3h`, `codex_auth_broken OK`;
  Codex weekly line reset 08:37Z (0 % used); build_ea tasks moved IN_PROGRESS→BLOCKED with real verdicts
  (`BLOCKED_COMPILE_AUTHORITY_AND_BUILD_IDENTITY_REQUIRED`, artifact 2026-09-19_qm5_21514_symbol_scope_rework.md).

## 2. Factory idle diagnosis and fix

Diagnosis (claim_result `skips`/`skip_samples` on all idle terminals, canonical `pending_claim_order_sql` executed
read-only): the claim order returned **31 rows**, not 75 — 19 unwinnable 44 GB rows (single_index_tick NDX/SP500,
heavy multisymbol XTI baskets, Q09_NEWS index rows), 1 Q08 row without DSR context (QM5_1567), 12 XAU/XAG basket
Q02 rows that are serialized by the "one multisymbol backtest farm-wide" rule (OWNER 2026-06-24). Of 3,708 pending
rows 3,312 carry active holds (PRESCREEN_SKIPPED 2,555; RAM_RESERVATION_44GB 487; …) and 299 are canonically
superseded. The health counter "75 unblocked" applies neither the RAM class nor the serialization filter — a known
false-positive class (ticket c9cff1f2). **Not** the 2026-09-15 head-of-line preflight bug (no rows behind the head).

Actions:
- Parked the six 44 GB Q10_NEWS rows at the claim head that kept arming drain windows against an unwinnable target
  (QM5_12831, 13059, 11147, 1355, 11294, 9973) with exact non-restart holds `RAM_RESERVATION_44GB_NOT_WINNABLE_20260914`
  (superseding inactive `NEWS_CALENDAR_TAINTED`); receipts `hold_ram44_*.json`. The two SP500 canaries of
  QM5_41475/41476 stay with ticket 7b2c1bc7 (supersede-and-replan). Idle-terminal skips fell from 17 to 13.
- Staggered worker restart onto HEAD `terminal_worker.py` (4d0786334e, drain fix c9cff1f2): T10 canary 09:01:51Z
  (transient duplicate from the 11:00 watchdog respawn removed by `--dedupe`), then T4+T6, T1–T3, T5/T7/T9,
  T8 last at 09:09:20Z after its basket finished (QM5_41166 XAU_XAG Q02 **PASS** 09:08:19Z). Claim cycles clean on
  every restarted worker, 0 tracebacks.
- Compile lane: `release_compile_wave.py --apply --max-items 6` released the six source-fresh
  `COMPILE_EA_WORKER_ROLLOUT_PENDING` rows (41135, 41138, 12952, 21505, 13054, 40008) → all six **COMPILE_OK**
  09:07–09:09Z on HEAD workers (`compile_wave_release.json`). The 13 stale predecessors need current-source
  successors (4 EAs with broken source: 41179, 41189, 41142) → ticket 23ccce7a. The 7 Q12 rollout holds
  (release_on_restart) need the governed release path → same ticket.
- Structural finding: the 44 GB class (all index long runs Q08/Q09_NEWS/Q10_NEWS on NDX/GDAXI/SP500, XTI baskets)
  is not winnable on the 63.1 GB host even after a full drain (plateau memory: need 51 GB vs 42.5 GB free). OWNER
  decision "no VPS upgrade" → these 487+ held rows are a dead frontier until reservations are measured per EA.
  Q12 declarations (37 unheld, `GOVERNED_ANALYTIC_DISPATCH`) produced 0 completions in 24 h → ticket 12582f77.

## 3. Pending artifact binding drift (79 bindings / 42 rows)

- Census `2026-09-19_binding_drift/census.json`: 42 rows, all `GOVERNED_BUILD_SUCCESSOR_REQUIRED`; 41 rows already
  under active `ARTIFACT_BINDING_*` holds; the one unheld row (64604d7a QM5_10706 Q14 GBPUSD) received an exact
  non-restart `ARTIFACT_BINDING_CONTENT_CHANGED` hold (`hold_10706_q14_binding.json`).
- LINE_ENDINGS_ONLY row 8be76d81 (QM5_20143 GBPUSD Q02) is under `ARTIFACT_BINDING_REBUILD_IN_PROGRESS`; rebind
  only after LF-normalized byte proof. Per-EA provenance review + governed successors commissioned: ticket 42877794
  (Claude lane). Nothing rebound, no verdict touched.

## 4. Q02 stranded-exhausted pairs (8)

- Packet `2026-09-19_q02_stranded_pairs/q02_stranded_20260919.json`: all 8 = `INVALID_EVIDENCE_DEFECT`
  (ONINIT_FAILED ×4, TIMEOUT_METATESTER_HUNG ×2, LOG_BOMB ×1, NO_HISTORY_TRANSIENT ×1); `retire_draft` empty —
  **no valid zero-trade exit**, so no RETIRE/frequency-floor disposition applies.
- Canary check: QM5_12582/XNGUSD (13× INFRA_FAIL) fails OnInit with the *identical* EX5 identity
  (c6d66c60… bound == on disk); a requeue would reproduce the failure. Per memory
  `project_qm_inputsvalid_framework_pin_defect_2026-08-20` ONINIT_FAILED is never requeued blind → root-cause ticket
  4216dd75 (Codex) before any canary. No bulk requeue.

## 5. Q09 sealed-plan holds (640 h / 335 h)

- `q09_pass_anchor_binder.py` census: both HOLD. QM5_11476/USDJPY: Q09 FAIL ×2 (fae2b8eb, 2a7e17b7), no Q09 PASS
  source can exist → hold superseded by terminal `Q09_FAIL_TERMINAL_NO_SEALED_PLAN_20260919`
  (`hold_11476_q10news_terminal.json`), stops futile autoseal retries. QM5_10148/EURNZD: Q09 PASS exists, but the
  Q02–Q07 evidence directories of June/July are **missing on disk** (`aggregate.json` absent for 804510b0,
  6ac6b3ca, d520d715, 7ee62674, f3586824, c96deb5b, bad1b2f7); `enqueue-backtest --append-only-rerun-of` refuses
  (no Q06 evidence to chain from). Ordinary hold kept; recovery depends on the evidence-loss forensics
  (EvidenceCohortWatch LOSS_OBSERVED, 177 missing) — ticket f8432ebf requeued to TODO.

## 6. Agent-task aging

- `agent_router.py reconcile-exits`: dry-run then apply → 121 APPROVED→PASSED, 9 PIPELINE→BLOCKED (no EA binding /
  no work items); 210 PIPELINE rows left in place (in flight, no closing verdict). RECYCLE: 20 of 24 requeued to
  TODO by capacity decision (Codex week fresh at 0 %, agy OK): 8 review_ea, 8 ops_issue (incl. evidence-loss
  forensics f8432ebf, DSR cohort 8def3323), 4 research_strategy. Left in RECYCLE: 4b52f1b2 (video → OWNER lane),
  787ab0aa (acceptance "x"), 29c7dd22 and 42a437a4 (Claude, superseded by D2g6 — to close).

## 7. Disk reserve

- D: 69 GB free (93 % used); purge task `QM_StrategyFarm_TesterCachePurge` LastTaskResult 0, 10-min trigger live
  (next 11:00 local). No capacity action taken that lowers the reserve; the cold-restart reserve gap (47 GB) stays a
  WARN.

## Tickets opened (agent_tasks)

| id | lane | subject |
|---|---|---|
| 4216dd75 | Codex | ONINIT_FAILED root cause for 12582/10369/10327/10505 (no blind requeue) |
| 42877794 | Claude | binding-drift per-EA provenance review + governed successors (42 rows) |
| 23ccce7a | Codex | stale COMPILE_EA rollout predecessors + 7 Q12 rollout holds, governed release |
| 12582f77 | Codex | Q12 DL-089 declarations not expanded (0 completions / 24 h) |

## Files

`compile_wave_release*.json`, `compile_rollout_reconcile_dryrun.json`, `hold_*.json`,
`../2026-09-19_binding_drift/{census,plan,rebind_plan,unheld_rows}.json`,
`../2026-09-19_q02_stranded_pairs/q02_stranded_20260919.{json,csv}`, commit `fbc9284107`.

## Addendum 09:20–09:55Z — OWNER "44-GB-Klasse: dann räum mehr frei" → index lane unlocked

- **RAM inventory (63.1 GB host):** metatester 10 GB (basket) + Chrome 7.6 GB (27 procs) + Firefox 2.4 GB +
  pythonw 2.8 + claude 1.5 + FTMO terminal 0.9 + OS. Closed Chrome/Firefox (both idle in the disconnected
  session-1 desktop, no factory role): free 30.0 → 37.2 GB.
- **Root cause of the dead index frontier:** `_resolve_ram_reservation` = max(flat, measured, floor). The tester
  memory ledger (20,418 runs) shows full-window index long runs at ~10 GB (NDX Q08 n=11 max 10.4; Q09 n=3 max 10.5;
  GDAXI Q08 n=4 max 9.3; index|H1|backtest n=127 p95 10.6/max 10.7), but the 44 GB flat (NDX/GDAXI reverted to 44 on
  2026-09-16 after two D1 monsters at 39.5 GB and one GDAXI D1 smoke at 35.4) could never be lowered by evidence.
- **Fix (commit `cadfb48731`, tests 84 pass):** single_index_tick rows take `ceil(max(p95·1.5+2, max+2))` clamped
  to [12, flat] from the per-EA key (n≥3, same TF) or the class key (n≥30); news rows fall back to the backtest key;
  SP500 lowers on per-EA evidence only; D1 monsters stay 42–44. Rollback `QM_INDEX_EA_MEASURED_LOWERS=0`.
  **Fix 2 (commit `40a6a738e1`):** evidence-reserved index rows clear at an 8 GB post-reservation floor (OPT_CENSUS
  rule of 2026-09-03) instead of 14 GB. Fleet reloaded staggered (T10/T4 → T1/T2/T5 → T7/T9/T8, then again after fix 2).
- **Result within 20 min:** QM5_10855 GDAXI Q04 → FAIL (genuine verdict, peak 1.3 GB), QM5_10599 NDX Q04 →
  PASS_SOFT (cascaded into Q05, running), NDX Q10_NEWS 1355 + 11294 running in parallel beside a basket
  (free 34.9 GB). 5 RAM44 holds released per row by evidence (`release_44gb_holds_0919.py`, journal
  `ram44_release_journal.jsonl`); 41 formerly unheld index rows (Q04/Q08/Q10_NEWS) are now claimable.
- **Side finding:** legacy-phase `Q09_NEWS` index rows (v3 storage phase, `avoid_terminals` T6–T10) became
  claimable and crashed at run_item without evidence (5× INFRA_FAIL); six siblings parked
  (`Q09_NEWS_RUNNER_CRASH_NO_EVIDENCE_20260919`), disposition = migrate/supersede to v4 `Q10_NEWS`
  (`q09news_legacy_phase_note.md`, ticket c73ed341).

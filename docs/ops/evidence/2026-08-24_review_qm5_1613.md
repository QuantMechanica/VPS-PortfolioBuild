# QM5_1613 aa-dsp-atsmom — Claude review-lane close of Codex review_ea

Date: 2026-08-24 UTC
Router task: `6cb2150d-fd37-4068-a0cd-5a11e9d90b32` (review_ea, assigned codex, state REVIEW)
Source build task: `de6c76f5-05f5-46cb-ac53-4fa4b81fc9b1` (`gemini`/agy, build delivery only)
Worker verdict under review: "Drafted complete QM5_1613 MQL5 EA and SPEC.md per card; compiled clean ex5; build guardrails PASS; ready for Codex review"
Prior Codex review artifact: `docs/ops/evidence/2026-08-23_qm5_1613_gemini_code_review.md` (verdict REQUEST_CHANGES)

Approved card (authority): `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1613_aa-dsp-atsmom.md` (`g0_status: APPROVED`).
Reviewed source: `framework/EAs/QM5_1613_aa-dsp-atsmom/QM5_1613_aa-dsp-atsmom.mq5`
MQ5 sha256 `7d9b88cdf5225fecbc69207ee600cb93ae43432e14e03e402f10934197fe9c20` — matches `build_identity.json`.

## Verdict: RECYCLE (concur with Codex REQUEST_CHANGES)

The build is framework-clean and mechanical, but the delivered code deviates from the
approved card on three mechanical points and ships a corrupt SPEC. These are named,
actionable defects — not a pipeline judgement. No source/binary/registry/setfile/verdict/
trade stream changed; T_Live and AutoTrading untouched.

## Confirmed defects (independently verified against the card)

1. **Entry rule deviates from card (High).** Card §Entry (lines 38-39) is state-based:
   "Open long when `ATSMOM > 0`", "Open short when `ATSMOM < 0`". The EA implements a
   zero-cross instead — `QM5_1613_aa-dsp-atsmom.mq5:180-188`:
   `atsmom1 > 0.0 && atsmom2 <= 0.0` (long) / `atsmom1 < 0.0 && atsmom2 >= 0.0` (short).
   Without ATR stops the two are trade-equivalent (one-position-per-magic makes flat→signal
   transitions coincide with sign flips), but they diverge after an ATR stop-out mid-regime:
   the card re-enters same-direction while `ATSMOM` keeps its sign; the zero-cross variant
   stays flat until a fresh cross. `SPEC.md` documents the unapproved zero-cross reading.
   Reconcile: implement the literal state-based entry, or obtain a card/identity that
   ratifies the zero-cross reading (note the card frontmatter line 21 and §Stop-Loss line 49
   loosely say "sign-flip", so this is reconcilable but must not be silently reinterpreted).

2. **Card-required median-spread filter absent (High).** Card line 58: "Skip new entries
   when D1 spread exceeds 2.5 x 20-day median spread." The EA has only an optional absolute
   ceiling — `mq5:60` `input int strategy_max_spread_points = 0` (default 0 = disabled),
   used at `mq5:141-146`. No 20-day median is computed anywhere. The mandated mechanical
   filter is not present in the delivered build.

3. **Delivered symbol universe does not match the card (High).** Card line 40 baseline =
   9 symbols (SP500, NDX, WS30, GDAXI, XAUUSD, USOIL, EURUSD, GBPUSD, USDJPY). Delivery =
   13 setfiles under `sets/`: omits approved `USOIL.DWX`, adds `AUDUSD, NZDUSD, USDCAD,
   USDCHF, UK100`. Reconcile card ⇄ allocator ⇄ broker naming before any enqueue; do not
   test an expanded universe without OWNER-approved card provenance.

4. **SPEC.md control-byte corruption (Medium).** Non-whitespace control bytes at offsets
   75 (`0x07`), 102 (`0x1b`), 1928 (`0x1b`) corrupt the slug ("a-dsp-atsmom") and source ID
   ("de348b4..." vs card "ede348b4..."). Risk table `SPEC.md:79` renders `,000` instead of
   `$1,000`. Regenerate SPEC from literal text.

## Assessed and cleared

- **Every strategy input is wired** (no dead inputs): `strategy_min_daily_bars` (mq5:172),
  `strategy_atr_period` (mq5:168,190), `strategy_atr_sl_mult` (mq5:168,201),
  `strategy_max_spread_points` (mq5:143). Framework inputs all consumed in OnInit/OnTick.
- **Framework conformity:** `#include <QM/QM_Common.mqh>`; MAE hook first on tick
  (`QM_FrameworkTrackOpenPositionMae()`, mq5:~279); RISK_FIXED=1000 / RISK_PERCENT=0.0 for
  backtest; magic via `QM_FrameworkMagic()`/MagicResolver; news filter wired
  (`QM_NewsAllowsTrade2`); bounded buffers (CopyClose 1-element, `QM_ATR`); no raw series;
  no ML/grid/martingale; no invented commission/swap.
- **News-before-exit ordering** (Codex Finding 3): `if(!news_allows) return;` precedes
  `Strategy_ExitSignal()`. This matches the sibling TSMOM template (QM5_1538, QM5_12618
  identical), so it is a codebase-wide pattern rather than a 1613-specific gemini defect;
  logged as a consistency observation, not weighted in this verdict.
- Setfiles governed-generator shape; all 13 use RISK_FIXED=1000 / RISK_PERCENT=0.
- ex5 present, sha256 matches build_identity; guardrails PASS per prior review.

## Evidence gap (non-blocking)

Only on-disk compile log is `framework/build/compile/20260822_104356/` — 78 errors against
an EARLIER source (undeclared QM_* identifiers = include chain unresolved that morning).
The final 20:54 source has no COMPILE_OK log on disk; the matching ex5 + guardrails PASS are
the only build evidence. Recommend the rebuild after corrections emit a fresh COMPILE_OK log
for the then-current hash.

## Disposition

RECYCLE to the gemini/build lane: fix defects 1-4, rebuild, re-run mandatory Codex review
before any acceptance or Q02 enqueue.

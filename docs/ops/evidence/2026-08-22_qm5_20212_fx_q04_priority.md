# QM5_20212 FX cointegration Q04 priority advance

Date: 2026-08-22 UTC

Branch: `agents/board-advisor`

Status: one existing, non-duplicate FX basket advanced in place

## Outcome

No new Strategy Card or EA was created. Commit `a80493291` accounts for all
66 relationships in the frozen sign-aware FX cointegration scan, so another
scan-derived Card, registry allocation, EA, or basket manifest would duplicate
governed work.

The preferred anchors do not need Q02 infrastructure repair:

- `QM5_12532_AUDNZD_COINTEGRATION_D1` has Q02 PASS and Q04 PASS, followed by
  Q05 FAIL.
- `QM5_12533_EURJPY_GBPJPY_COINTEGRATION_D1` has Q02 PASS, followed by Q04
  FAIL.

The strongest current continuation, rank 27 `QM5_20208` NZDUSD/EURAUD, already
owns the sole active Q05 successor. Ranks 28 through 31 are terminal at Q04 or
Q05. The next safe existing-pair fallback is therefore rank 32,
`GBPUSD.DWX` / `EURJPY.DWX`, implemented once as
`QM5_20212_gbpusd-eurjpy`.

## In-place advancement

The logical Q02 work item
`d87a25e0-c67b-48d4-a5ca-effe04cdd009` is a terminal PASS. Exactly one Q04
successor existed before this turn:
`660e940e-73af-47e5-aca0-201b081cfc84`.

Before mutation, that Q04 row was pending, unclaimed, at attempt zero, and had
no priority marker. A guarded `BEGIN IMMEDIATE` transaction under the shared
factory mutation lock added `priority_track=true` plus the current OWNER
mission authority and non-duplicate rank-32 reason. The exact-ID CAS preserved
the original `updated_at`, status, verdict, claim state, and attempt count.
Exactly one row changed; no work item was inserted or requeued.

The canonical selector moved the same row from rank 100 to rank 2 while the
claimable population remained 3,213 rows. Farm event `373662`
(`priority_boost_applied`) binds the payload transition:

- before: `3dcdd29acbefad24da5f000dd0f63015892978fe97c32dd0cb49092005bea919`
- after: `273c99c7a56e232180058a22649e489813cfa602da10adc53c0cee0b2761dc0d`

The pre-mutation backup is
`D:/QM/strategy_farm/state/backups/farm_state_pre_qm5_20212_q04_priority_20260822T132327Z.sqlite`
(420,503,552 bytes, SHA-256
`17218f7a8eb9dab9aca08f3f263857951395de534f07bc3876a8a1298dc8b3cb`).
The backup and live database both returned `PRAGMA quick_check = ok`, and the
factory mutation lock released normally.

## Card and package contract

The OWNER-approved Card retains G0 APPROVED and R1-R4 PASS. Its structural
method comes from the OWNER-ratified Tier-A extraction of Ernest Chan's
*Quantitative Trading*; the fixed pair comes from the OWNER-requested Darwinex
D1 scan. Chan supplies the method and makes no GBPUSD/EURJPY performance claim.

The frozen pair evidence is adverse rather than promotional: DEV net Sharpe
`-0.215933`, OOS net Sharpe `0.221108`, OOS return `2.097281%`, 17 state
changes, fixed beta `-0.080732288`, and an estimated 103.753-D1-bar half-life.
The Card permits one pipeline test and requires retirement, not refitting or
rescue filters, after a terminal economic failure.

The two active traded slots remain `GBPUSD.DWX` / magic `202120000` and
`EURJPY.DWX` / magic `202120001`. `EURUSD.DWX` and `USDJPY.DWX` are
conversion-history dependencies only. The logical backtest setfile remains
`RISK_FIXED=1000`, `RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1`.

Strategy-card schema/ML lint and G0 lint passed. The Card, EA source, EX5,
setfiles, basket manifest, EA registry, and magic registry were not changed.

## Capacity and safety

The five pre-apply CPU samples were `82.53%`, `68.38%`, `68.17%`, `63.50%`,
and `66.04%`; the 82.53% maximum stayed below the 97% hard ceiling. The five
post-action samples reached a 96.21% maximum, also below the ceiling.

The final supported slot view observed factory terminals `T2` and `T3`.
`T_Live` and the unrelated FTMO terminal were observed only to exclude them;
neither was controlled. No dispatch tick, tester launch, terminal reservation,
AutoTrading change, or live action occurred.

No portfolio-admission, portfolio-KPI, Q08-contribution, or T_Live-manifest
path changed. Concurrent unrelated worktree changes remained unstaged and
untouched.

Machine-readable evidence:
`artifacts/fx_cointegration_qm5_20212_q04_priority_20260822T132327Z_board_advisor.json`.

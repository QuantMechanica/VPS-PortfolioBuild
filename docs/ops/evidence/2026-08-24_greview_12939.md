# Gemini cross-review — QM5_12939

- Date: 2026-08-24
- Ticket: `greview-12939`
- Router task: `2a3580e3-ddbf-4853-b012-0cab4471109e` (`build_ea`, assigned `gemini`, reviewed by Codex)
- EA: `framework/EAs/QM5_12939_carney-alternate-bat-h4/`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12939_carney-alternate-bat-h4.md`

## Verdict

`RECYCLE` — the deliverable is not formally clean for Q02. The checked-in EX5 is the pre-fix binary, all 13 setfiles still bind the removed single-target parameter, and the current MQ5 has card/execution defects in pivot selection, management reachability, partial-close acknowledgement, and cooldown acknowledgement. These defects are repairable; this is not an unsalvageable strategy failure.

One-line router verdict:

> RECYCLE: repair the pivot/management/partial-close/cooldown defects, regenerate all 13 governed setfiles, then recompile the current MQ5 via governed COMPILE_EA with fresh hash-bound evidence.

## Inputs and review scope

- `python tools/strategy_farm/agent_router.py list-tasks --agent gemini`, filtered to the exact UUID, returned `state=REVIEW`, `ea_id=12939`, `slug=carney-alternate-bat-h4`, and `recycle_count=1`. The payload omits explicit `ea_dir`, `card_path`, and `artifact` fields, so the canonical paths were resolved by the EA ID and slug. `artifacts/qm5_12939_build_result.json:2-11` confirms the same task, EA directory, source, binary, and approved-card paths.
- The four required rebaseline documents were read first. This review made no gate/threshold change and performed no factory, queue, backtest, compile, or T_Live action.
- The state DB was opened through `file:D:/QM/strategy_farm/state/farm_state.sqlite?mode=ro`; it contains no `work_items` row for `QM5_12939`, consistent with the build remaining before Q02.

## Blocking findings

### 1. Delivered EX5 is stale and not a build of the current MQ5

- The canonical LF-normalized current MQ5 SHA-256 is `d9a62e7b379ffcb65f7968fcb38320b3aa37379ab93b718d09e3004fa3f40177`; this matches `artifacts/qm5_12939_build_result.json:6-7`. The worktree's CRLF byte hash is `fb80a1a069aa2e19907fe043f2c95978e0eeecea1d116faf96bba343651afd4f`, and normalizing CRLF to LF reproduces `d9a62e7b...`.
- The delivered EX5 SHA-256 is `6fb96e5765d999f831928c89955e89393371d17bcb5e8ac667d01b73e72735a2`, matching `artifacts/qm5_12939_build_result.json:8-9`.
- Git history is decisive: commit `c877d9f987336208576e213755f7247cbd8263a3` (2026-08-21) changed both MQ5 and EX5; later commit `c5cc91ff0505dfc06333888f04d4ebe609fb05b5` (2026-08-23) added the missing exits and changed only the MQ5. No QM5_12939 compile log exists under `framework/build/compile` after that source change. The artifact's `compile_succeeded=true` at `artifacts/qm5_12939_build_result.json:43-44` therefore does not bind the current source to the old binary.
- Action: after the source and setfile repairs below, run the governed `COMPILE_EA` path. Produce a fresh compile log/build identity that binds the current MQ5 hash to a newly emitted EX5 hash; refresh the build-result hashes. Do not reuse `6fb96e57...`.

### 2. All governed-looking setfiles are stale against the current input contract

- The current EA declares `strategy_tp1_ad_retracement`, `strategy_tp2_ad_retracement`, and `strategy_atr_trail_mult` at `QM5_12939_carney-alternate-bat-h4.mq5:52-54`.
- Every one of the 13 setfiles instead contains removed key `strategy_tp_ad_retracement` and omits all three current exit keys. Representative evidence is `sets/QM5_12939_carney-alternate-bat-h4_GDAXI.DWX_H4_backtest.set:35`; the same parameter-contract comparison was run across all 13 files with identical results.
- The files retain their 2026-08-21 generator headers (`...GDAXI...backtest.set:2-15`) even though the source input contract changed on 2026-08-23. The risk rail itself is correct (`RISK_FIXED=1000`, `RISK_PERCENT=0`; lines 19-20), but the strategy binding is not current.
- Action: regenerate all 13 backtest setfiles through the governed generator after the MQ5 repair. The regenerated files must contain the three current exit keys, contain no obsolete `strategy_tp_ad_retracement`, preserve the registered slots and fixed-risk rail, and carry refreshed provenance/build hashes.

### 3. Pivot detector is not the approved card's declared ZigZag contract

- The approved card requires ZigZag `(12,5,3)` or the stated `iHighest`/`iLowest` pivot route (`card:41`, reiterated at `card:77`).
- The EA invents `strategy_fractal_wing_bars=2` (`mq5:39`) and selects local two-wing extrema with `Strategy_FractalHigh/Low` (`mq5:92-159`, called at `mq5:197-204`). It implements neither ZigZag depth/deviation/backstep `(12,5,3)` nor an approved equivalence binding for those mechanics. This changes which XABCD sequences exist and is strategy-mechanical, not a presentation difference.
- Action: implement the card's ZigZag `(12,5,3)`/approved equivalent deterministically, or obtain a revised approved card before rebuilding. Do not treat the build-authored SPEC as authority over the approved card.

### 4. Entry-only filtering can suppress mandatory position exits

- The card specifies the spread threshold as an entry skip (`card:71`) and the 30-H4-bar time stop as an exit regardless of P&L (`card:57`).
- `Strategy_NoTradeFilter()` returns true for unready cached state or high spread (`mq5:302-308`). `OnTick()` applies that return at `mq5:515` before `Strategy_ManageOpenPosition()` at `mq5:517` and the opposite-signal exit block beginning at `mq5:519`. A wide spread or temporary state-readiness failure therefore suppresses the time stop, T1/trail management, and pattern exit.
- Action: run owned-position management and exit handling before entry-only filters; apply readiness/spread/news filters only to new entries.

### 5. T1 completion is recorded even when the required partial close did not occur

- The card requires closing 50% at T1 (`card:55`). The framework API returns a success boolean (`framework/include/QM/QM_TradeManagement.mqh:422-425`).
- For both long and short positions, the EA ignores that boolean and sets `g_trade_state.t1_hit=true` even when half-volume normalization is invalid or `QM_TM_PartialClose` fails (`mq5:392-400`, `mq5:423-431`). It then activates the post-T1 trail without proving the 50% exit.
- Action: advance `t1_hit` only after a successful partial close (or an explicit, deterministic minimum-volume disposition), otherwise retain/retry the required T1 action.

### 6. Cooldown is consumed before entry success is known

- The card defines an 18-bar no-re-entry cooldown (`card:76`). The EA resets the directional counter inside signal construction (`mq5:327`, `mq5:339`), before `QM_TM_OpenPosition` is called; `OnTick()` ignores the open result at `mq5:551-555`.
- A rejected order therefore consumes the cooldown despite no entry having occurred.
- Action: reset the relevant cooldown only after `QM_TM_OpenPosition` returns success.

## Clean findings

- The principal bullish/bearish ratio arithmetic and mirror logic are present: AB/XA, BC/AB, CD/BC, AD/XA 1.13 plus D-beyond-X, confirmation, RSI, SL, T1/T2, time stop, opposite signal, ATR trail, cooldown, and spread checks are located at `mq5:193-195`, `217-295`, `375-471`.
- All 19 declared `strategy_*` inputs have executable use-sites: `fractal_wing_bars` 147; `scan_bars` 179/199; tolerance 215; AB 233/273; BC bounds 234/274; CD bounds 235/275; D ratio 236/276; RSI bounds 194; ATR period 185/405/436; SL multiplier 249/289; T1/T2 251-252/291-292; trail 409/440; hold 379; cooldown 246/286; spread 306. No declared strategy input is dead.
- Framework chain is present: `QM_Common.mqh` include (`mq5:5`), `QM_FrameworkInit` (`mq5:483-487`), MAE hook (`mq5:501`), one cached new-bar call (`mq5:508-513`), news gate (`mq5:539-545`), and framework trade management (`mq5:381`, `397`, `414`, `428`, `445`, `532`, `554`).
- Magic resolution is governed: `QM_Common.mqh:223-227` calls `QM_MagicChecked`; `QM_MagicResolver.mqh:55` computes `ea_id*10000+slot`; registry rows `framework/registry/magic_numbers.csv:16356-16368` bind slots 0-12 to magics 129390000-129390012.
- Backtest risk is fixed (`mq5:20-22`; all setfiles `RISK_FIXED=1000`, `RISK_PERCENT=0`). The EA exposes the live percent-risk input, but no live setfile was reviewed or authorized in this Q02 review.
- Buffer/series use is bounded: `pivots[128]` has an explicit count cap (`mq5:134-141`); CopyRates is bounded by `strategy_scan_bars` and checked by `copied` (`mq5:197-205`). Raw series calls at `mq5:179`, `199`, `243`, `283`, `376`, `408`, and `439` carry `// perf-allowed`. No ML, grid/martingale, or EA-invented commission/swap logic was found.
- News is wired through `QM_NewsAllowsTrade2`/legacy fallback (`mq5:539-545`) with 336-hour stale input passed into framework init (`mq5:485-486`).

## Read-only validation and tests

Commands were run from `C:/QM/worktrees/rework-slot-10` without compiling or starting a terminal:

- `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_12939_carney-alternate-bat-h4` — no failures or warnings; 13 symbols matched the matrix/registry. Its declared D16 scope explicitly does not automate restart-state review.
- `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_12939_carney-alternate-bat-h4` — `PASS`, 14 files checked, 0 findings, max news stale 336 hours.
- `python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_12939_carney-alternate-bat-h4 --json --fail-on-leak` — `SINGLE_SYMBOL_OK`, 0 violations.
- Focused unit tests: `python -m pytest -q tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_setfile_canonicalization.py tools/strategy_farm/tests/test_gen_setfile.py tools/strategy_farm/tests/test_agent_router.py tools/strategy_farm/tests/test_agent_router_state_exits.py` — `81 passed in 41.25s`.
- Full hardening module run: `python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_setfile_canonicalization.py tools/strategy_farm/tests/test_gen_setfile.py` — `33 passed, 1 failed in 404.59s`. The failure is the pre-existing repository-wide `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`, reporting seven findings in unrelated `QM5_411*` EAs; the direct hardening invocation for QM5_12939 passed.
- Full strategy-farm suite: `python -m pytest -q tools/strategy_farm/tests` — `6 failed, 4683 passed, 4 skipped, 2 warnings, 42 subtests passed in 1791.25s (0:29:51)`. All six failures are outside QM5_12939: the same QM5_411xx buffer cohort; two rolling news-calendar hash/coverage fixtures in `test_execution_contract_lint.py`; one v4 phase-rank expectation in `test_opt_census_dispatch.py`; and two stale source/setfile/build-identity hashes for unrelated QM5_1612. The focused 81-test review set and the direct QM5_12939 hardening/guardrail/symbol checks remain green.

## Constraints observed

- No compile or backtest was run.
- No work item was enqueued, deleted, or overwritten.
- Factory/AutoTrading was not toggled.
- `C:/QM/mt5/T_Live` was not accessed or changed.
- No gate threshold/criterion or existing verdict row was changed.

## Rollback

This review changes only this evidence note in Git. Revert its dedicated commit with `git revert <review-commit>` if the note itself must be withdrawn. The router close is a governed review-state transition; do not delete or overwrite it—supersede it through a new governed review after the named repairs and new build identity exist.

# rework-9467 — QM5_9467 review-fix evidence

Date executed: 2026-08-24 (Europe/Berlin)
Router task: `f5be58aa-c65f-4652-8fdb-6816c30957b0`
EA: `QM5_9467_connors-crsi-pullback-d1`
Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9467_connors-crsi-pullback-d1.md`

## Scope and authority

The task was read in `RECYCLE`, its review work order was read from
`docs/ops/evidence/2026-08-23_qm5_9467_gemini_code_review.md`, and it was claimed
with:

```text
python tools/strategy_farm/agent_router.py update-task f5be58aa-c65f-4652-8fdb-6816c30957b0 --state IN_PROGRESS --verdict "rework burn-window slot 6"
=> updated=true, state=IN_PROGRESS
```

The required rebaseline proposal, automation inventory, gate-name census, and
OWNER vault directive were read before implementation. This repair changes no
gate threshold/criterion, verdict, backtest queue, factory state, live terminal,
AutoTrading setting, EA registry, magic registry, or generated magic resolver.

Registry preflight remains valid and unchanged:

- `framework/registry/ea_id_registry.csv:689` — active `9467 / connors-crsi-pullback-d1`.
- `framework/registry/magic_numbers.csv:16851-16854` — the three approved ports
  use existing active slots NDX=1, SP500=2, and WS30=4. The other pre-existing
  rows at `:16850-16862` were not edited or repurposed.

## Review findings closed in source

1. **Fill-relative protective stop and risk distance.**
   `framework/EAs/QM5_9467_connors-crsi-pullback-d1/QM5_9467_connors-crsi-pullback-d1.mq5:284`
   derives the final SL from `POSITION_PRICE_OPEN` and the same ATR distance used
   by request sizing, then uses `QM_TM_SendSLTPModify`. The idempotent
   transaction hook is at `:547-564`; failure to verify protection closes the
   new exposure with `QM_EXIT_KILLSWITCH`.

2. **One closed-D1 CRSI snapshot.** The bounded recursive reconstruction is
   isolated behind `Strategy_RefreshClosedD1Snapshot` at `:183-196` and invoked
   only after the explicit D1 boundary at `:482-490`. `Strategy_ExitSignal` at
   `:436-441` reads the cache and performs no history reconstruction.

3. **Trading-bar expiry and one exposure.** `Strategy_HasOurExposure` at
   `:222-244` covers both open positions and pending orders for the resolved
   magic. `Strategy_ReconcilePendingAtD1Boundary` at `:246-273` removes an
   unfilled order when its setup bar becomes a prior D1 bar. The request uses
   GTC at `:405-407`; the governed D1 reconciliation is authoritative, avoiding
   the rejected 86,400-second weekend/DST approximation.

4. **Declared D1 execution contract.** `OnInit` binds the framework contract at
   `:458-462`, and entry/exit cadence uses
   `QM_IsNewBar(_Symbol, STRATEGY_TIMEFRAME)` at `:482`.

5. **Approved universe only.** `Strategy_IsApprovedSymbol` and the immutable
   approved slot mapping are at `:199-218`; `OnInit` rejects every other
   symbol/slot pair at `:449-450`. Only the NDX, SP500, and WS30 backtest sets
   remain. Ten unauthorized FX/commodity/extra-index setfiles were removed;
   registry allocations were deliberately left untouched.

6. **Exits independent of entry filters.** The D1 expiry, snapshot, time-stop,
   and CRSI exit run at `:489-505`. News admission begins at `:512` and the
   spread/quote no-trade filter at `:520`, after every risk-reducing path.

7. **Clean SPEC and validator regression.** The literal clean, card-scoped SPEC
   is `framework/EAs/QM5_9467_connors-crsi-pullback-d1/SPEC.md:1`; the approved
   universe is at `:55-64`, D1 contract at `:66-77`, and `$1,000` fixed-risk
   declaration at `:99`. `framework/scripts/validate_spec_doc.py:46-72` now
   rejects text-corrupting control characters and truncated currency tokens.
   Focused regressions are in
   `tools/strategy_farm/tests/test_validate_spec_doc.py` and
   `tools/strategy_farm/tests/test_qm5_9467_rework.py`.

Every declared input was mechanically grepped after the repair. All 28 inputs
have at least one non-declaration use-site; observed token counts range from 2
to 4. The focused pytest also fails on any unwired input, any raw series read
without a `perf-allowed:` annotation, a missing `ArraySize` guard, ML tokens,
or a package symbol outside the three approved ports.

## Validation evidence

### Passing checks

```text
python -m pytest -q tools/strategy_farm/tests/test_validate_spec_doc.py tools/strategy_farm/tests/test_qm5_9467_rework.py
...... [100%]
6 passed in 0.94s

python -m pytest -q tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_validate_symbol_scope.py
....................... [100%]
23 passed in 1.79s

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_9467_connors-crsi-pullback-d1
PASS QM5_9467_connors-crsi-pullback-d1
Summary: 1 PASS, 0 FAIL (of 1)

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_9467_connors-crsi-pullback-d1
PASS; files_checked=4; findings=0; max_news_stale_hours=336

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_9467_connors-crsi-pullback-d1 --fail-on-leak --verbose
SINGLE_SYMBOL_OK; n_violations=0

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_9467_connors-crsi-pullback-d1
files_scanned=1; failures=0; warnings=0; D4=0; D7=0; D10=0
```

`git diff --check` reports no whitespace errors (only the repository's expected
LF-to-CRLF checkout notices).

The broader hardening unit module was also executed:

```text
python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py
1 failed, 28 passed in 267.88s
```

Its sole failure is outside this ticket:
`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven
pre-existing findings beginning at
`QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`. The scoped QM5_9467 hardening run
above is zero/zero.

### Strict compile/build-check hold

The required single-EA compile was attempted with:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 \
  -EAPath framework/EAs/QM5_9467_connors-crsi-pullback-d1/QM5_9467_connors-crsi-pullback-d1.mq5 -Strict
```

It stopped before MetaEditor with the governed failure class
`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` / `INCLUDE_MIRROR_REFUSED` because
`terminal64.exe` processes are active. The durable receipt is
`D:/QM/reports/compile/20260824_130950/summary.csv`.

A separately scoped
`build_check.ps1 -EALabel QM5_9467_connors-crsi-pullback-d1 -Strict -SkipCompile`
correctly stopped on the same interlock. The sanctioned compile enqueue was
checked, but the existing-binary/source-repair classifier refused EA 9467
(`EX5_ALREADY_PRESENT`, `BOUND_SETFILE_HASH_EXISTS`) because no exact governed
source-repair authority exists for this label. No allowlist, guard, terminal,
factory state, queue criterion, or live process was altered.

The artifact is truthful about this hold:
`framework/EAs/QM5_9467_connors-crsi-pullback-d1/build_identity.json` records
`build_check_passed=false`, source SHA-256
`f3ca946b0f425c32a558de37d39cf94b0b1c9ccad9b70141bcab7c662624a3eb`, and marks
the unchanged pre-rework EX5 (`a90ef161...a08af6`) as
`STALE_PRE_REWORK_BINARY_NOT_RESUBMITTED`.

## Rollback

Revert the ticket commit with `git revert <rework-9467-commit>`. That restores
the prior MQ5/SPEC/build identity and the ten removed setfiles, and removes the
two focused test files plus the validator extension. No registry, database
verdict, backtest, factory, or live-state rollback is required. The router task
state can be returned to `RECYCLE` by the reviewer if the source repair is not
accepted.

## Residual risk / review request

The seven code-review findings are repaired and statically covered. A current
EX5 and strict build-check PASS remain externally held by the live-terminal
interlock plus missing compile source-repair authority. Review must not treat
the stale EX5 as evidence for this source; a governed compile authority/window
is still required before any pipeline or backtest handoff.

# Fleet session clock audit — part 2

Date: 2026-09-12  
Task: `ea9e3c61-488a-4587-97ab-73d886e7a9bc`  
RESULT: REVIEW — all 93 prior `MATCH` rows have an exact source/hash/line placement trace; no measurement run or work item was created.

## Outcome

The reproducible CSV is `docs/ops/evidence/2026-09-12_fleet_clock_pending_trace.csv`, generated by `2026-09-12_fleet_clock_pending_trace.py` from the frozen 2026-09-09 inventory. Source hashes are checked before output, so source drift fails closed.

| Behavior when price is already beyond a computed pending level | Rows |
|---|---:|
| `MARKET` / no pending level | 85 |
| `OPPOSITE_ONLY` possible after independent stop submissions | 6 |
| `SKIP` explicit | 1 |
| `UNDEFINED` at strategy level | 1 |

The 85 `MARKET` rows do not have a stop-pending level to cross; their signal path submits a market request or delegates the market request. The eight stop-pending rows were traced at the placement call sites. `OPPOSITE_ONLY` means a crossed stop can be rejected while the other independently submitted stop remains; it is not a claim that every such event was historically accepted. `UNDEFINED` means the EA supplies no mechanical crossed-level branch and relies on lower-layer/platform rejection behavior.

## Ranked top 10

This is a static risk ranking, not a profitability ranking. Historical phase depth breaks ties because a defect in deeper evidence can have more downstream impact. No affected-trade count is invented.

| Rank | EA | Finding | One-line hypothesis for governed re-measurement |
|---:|---|---|---|
| 1 | `QM5_20188` | `OPPOSITE_ONLY`, historical Q09 | A delayed 06:00 placement can silently retain only the still-valid opposite stop and consume the day, changing the intended straddle population. |
| 2 | `QM5_11435` | `OPPOSITE_ONLY`, historical Q04 | A gap beyond the prior-day bracket can leave a one-sided order because the duplicate guard then blocks re-forming the pair. |
| 3 | `QM5_41097` | `OPPOSITE_ONLY`, historical Q03 | GMT3 range-breakout results may include one-leg survivors when placement follows a fast range break. |
| 4 | `QM5_1120` | `OPPOSITE_ONLY`, historical Q03 | A move through the Asian-range boundary before the 07:00 call can turn the intended two-leg bracket into a single opposite stop. |
| 5 | `QM5_11505` | `UNDEFINED`, historical Q02 | A directional stop already crossed during the entry window is submitted with no strategy rule, making retry/rejection behavior evidence-sensitive. |
| 6 | `QM5_41398` | `OPPOSITE_ONLY`, historical Q02 | The repaired Balke variant can mark permission intent for the day even when crossed-level submissions fail, suppressing later valid placement. |
| 7 | `QM5_41324` | `OPPOSITE_ONLY`, historical Q02 | Path-filtered Balke evidence may contain an unmeasured one-leg survivor population after delayed placement. |
| 8 | `QM5_11697` | `SKIP`, historical Q02 | Explicit guards should produce zero invalid submissions; a counterfactual receipt would verify that skips, not hidden market conversions, explain missed entries. |
| 9 | `QM5_13036` | `MARKET`, historical Q10 control | As a deep market-entry control, clock shifts can change eligibility timing but cannot create a crossed pending-level failure. |
| 10 | `QM5_10094` | `MARKET`, historical Q09 control | As a second deep control, any counterfactual delta should arise from session membership rather than pending-price validity. |

## Proposed governed measurements (not started)

1. Instrument the eight stop-pending EAs in a new, explicitly approved measurement cohort with counters for `attempted`, `accepted`, `invalid_price`, `other_reject`, `one_leg_survivor`, and `day_consumed_without_order`.
2. Replay only the already-approved Q02 symbols/date windows with unchanged risk and verdict thresholds; compare current timing against the approved clock alternative only after the source clock is confirmed.
3. Require report paths and exact set hashes for both arms. Do not promote results based on this static audit.

## Verification

- Generator assertion: exactly 93 `MATCH` rows.
- Output assertion: no blank `file_line_evidence`; every row includes the current source SHA-256.
- State assertion: every `work_items_created` value is `0`; farm state was opened by no command in this task.
- Scope: source and state were read-only. No EA, terminal, task, verdict, or threshold was changed.


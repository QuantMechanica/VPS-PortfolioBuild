# QM5_11912 magic-allocation preflight repair — 2026-07-10

Task: `64867eb5-6613-4ccd-af15-8c42fa6bd45d`

## Verdict

**PASS — MAGIC PREFLIGHT UNBLOCKED.** `QM5_11912` now has one active,
collision-free magic allocation for every symbol in its approved Strategy
Card, and `QM_MagicResolver.mqh` was regenerated from the resulting registry.

This was a registry-only ops repair. The EA was not built, compiled, smoked,
or enqueued by this task; those remain separate deterministic router actions.

## Preconditions verified

- Approved card:
  `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11912_cheng-triangle-2touch-second-break-h1.md`
  with `g0_status: APPROVED`.
- Active EA-ID row:
  `11912,cheng-triangle-2touch-second-break-h1,...,active,claude,2026-05-25`.
- Existing canonical EA directory:
  `C:/QM/repo/framework/EAs/QM5_11912_cheng-triangle-2touch-second-break-h1`.
- Before the repair, `magic_numbers.csv` contained zero `ea_id=11912` rows
  and none of the proposed magic values was already allocated.

The existing EA directory means the required order of operations was
respected: directory first, registry append second, resolver regeneration
third.

## Allocations

| Slot | Symbol | Magic |
|---:|---|---:|
| 0 | `EURUSD.DWX` | 119120000 |
| 1 | `GBPUSD.DWX` | 119120001 |
| 2 | `USDJPY.DWX` | 119120002 |
| 3 | `USDCAD.DWX` | 119120003 |
| 4 | `USDCHF.DWX` | 119120004 |
| 5 | `AUDUSD.DWX` | 119120005 |
| 6 | `NZDUSD.DWX` | 119120006 |
| 7 | `EURJPY.DWX` | 119120007 |
| 8 | `GBPJPY.DWX` | 119120008 |
| 9 | `AUDJPY.DWX` | 119120009 |

All rows use slug `cheng-triangle-2touch-second-break-h1`, reservation date
`2026-07-10`, reserver `Codex`, and status `active`. The symbol order exactly
matches the approved card and the established ten-FX allocation convention.

## Focused verification

- Target CSV verifier: `PASS`; 10 rows, slots `0..9`, exact card symbols,
  formula `magic = 11912 * 10000 + slot`, zero target-magic duplicates.
- `python framework/scripts/update_magic_resolver.py`: `OK`; 14,799 rows kept,
  registry SHA prefix `6A4CF7B849EC0F2A...`.
- Resolver parser: `PASS`; parallel EA-ID/slot/magic arrays are each 14,799
  elements and contain exactly the ten expected `11912` triples.
- Full registry SHA-256 equals the generated resolver’s embedded SHA-256:
  `6A4CF7B849EC0F2A3D78F081F54657985445B786F64C69FAD5D28F9B48C50C3D`.
- `python framework/scripts/skill_build_ea_guard.py --ea-id 11912
  --ea-label QM5_11912_cheng-triangle-2touch-second-break-h1`: `status: ok`,
  with EA registry, magic registry, and EA directory checks true.
- `git diff --check` on the registry and resolver: no whitespace errors.

The resolver generator retained its existing warning that EA IDs `1001`,
`1015`, and `1016` have registry rows but no canonical EA directories. Those
unrelated legacy warnings predate this task; `11912` was retained correctly.
The global `validate_registries.py` command also remains red from the existing
legacy registry backlog, so this artifact claims only the verified target-
scoped repair.

## Safety and handoff

No terminal, backtest, T_Live file, AutoTrading state, news-staleness guard,
risk setfile, or pipeline verdict was changed. The next build task may now run
the normal build guard and, if otherwise eligible, compile against the current
resolver.

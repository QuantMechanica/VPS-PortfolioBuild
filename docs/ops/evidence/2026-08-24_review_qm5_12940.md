# Review — QM5_12940 Bressert Cycle-Trigger-Line on DSS (H4)

Date: 2026-08-24
Reviewer: Claude (review lane)
Router task: `c600e224-e78c-46a2-853e-b1f4ffd26e42` (review_ea; source build_ea `f2e0fa39` by gemini/agy)
Prior review: `395e4a56` -> RECYCLE (commit 45f5552d8), same decisive defect — unchanged.
EA dir: `framework/EAs/QM5_12940_bressert-cycle-trigger-line-h4-card`
Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12940_bressert-cycle-trigger-line-h4-card.md`
Current MQ5 SHA-256: `1f7db8429d7b41c530f381f909a9d7ab1437ad6a8e7552cc2fc9926acff3e86c` (matches build_result.json)

## Verdict: RECYCLE

Source is card-faithful and clean, but the delivered `.ex5` is a **stale pre-fix binary** that
cannot go to Q02. This is the identical defect the prior review (`395e4a56`) already RECYCLEd, and
it is unremediated — the worker re-submitted without recompiling. Recompile is ROT for the review
lane; route back to the OFF-window / COMPILE_EA queue.

## Decisive defect — stale .ex5 (recompile required)
- `.ex5` git blob is `6a258475efce949f7f157aa6e9fbff169109049b` at HEAD **and** before the hardening
  commit `394521ed8~1` — identical. The only commit that ever touched the `.ex5` is `c877d9f98`
  (2026-08-21, the original pre-fix compile).
- The `.mq5` was changed after that compile by two Aug-23 commits: `3ee27e1eb` (fix for review) and
  `394521ed8` (D10 buffer-bounds hardening, 19 mq5 lines) — neither recompiled the binary.
- Consequence: the binary Q02 would run is the Aug-21 build, which still carries the double-new-bar
  starvation and the DSS out-of-bounds access that the prior RECYCLE required be fixed. The fixes
  exist only in source.
- `build_result.json` reports `compile_succeeded:true` with `ex5_sha256:a0e0983...` — that compile
  flag/hash reflect the Aug-21 build, not the current source hash `1f7db`, so the artifact is
  misleading. `build_gate_hardening` PASS (independently reproduced: `failures: []`, exit 0) is a
  **static source** pass, not a fresh compile of current source. No COMPILE_OK evidence exists for
  `1f7db`.
- Follow-on: setfiles carry `build_hash 541cf5ba...` / `set_version s20260821-001` (Aug-21) —
  regenerate via the governed generator against the new build hash after recompile.

## Source review (all clean — for the record, so recompile is the only action needed)
- Card-faithful: DSS = EMA(EMA(Stoch_K(13,1),8),8); trigger = SMA(DSS, `strategy_trigger_period`),
  dynamic; bull/bear crossover with os/ob zone gate, D1 EMA(50) regime, N-bar momentum confirm,
  cooldown, ATR SL, T1 50% partial, post-T1 ATR trail, 24-bar time stop, opposite-signal exit.
- All 15 `strategy_*` inputs have use-sites (grep >=2 each); no dead inputs.
- Framework: QM_Common include, MAE hook first in OnTick, RISK_FIXED=1000/RISK_PERCENT=0 (13/13
  setfiles, 0 live), magic via MagicResolver (base 129400000=12940*10000), news filter wired,
  bounded static buffers with guards, perf-allowed raw series, no ML, no invented commission/swap.

## Required action to clear
Recompile current source (`1f7db`) into a fresh `.ex5` in the OFF-window / COMPILE_EA queue,
regenerate setfiles via the governed generator against the new build hash, update
`build_result.json` with the true current ex5 sha, then resubmit for review.

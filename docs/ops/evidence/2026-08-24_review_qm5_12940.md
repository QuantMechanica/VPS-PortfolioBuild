# Review — QM5_12940 Bressert Cycle-Trigger-Line (H4)

- Date: 2026-08-24
- Reviewer: Claude (review lane)
- Router task: `395e4a56-b7a5-4c07-9187-253b4d2d6dd0` (review_ea, assigned codex, recycle_count 1)
- Source build task: `f2e0fa39-1871-43b8-a282-e0f2ea55e1cf` (build_ea, agy/gemini)
- EA dir: `framework/EAs/QM5_12940_bressert-cycle-trigger-line-h4-card`
- Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12940_bressert-cycle-trigger-line-h4-card.md`
- Artifact: `artifacts/qm5_12940_build_result.json`
- Current mq5 sha256: `1f7db8429d7b41c530f381f909a9d7ab1437ad6a8e7552cc2fc9926acff3e86c` (matches artifact `mq5_sha256`)

## Verdict: RECYCLE

Source is card-faithful and every prior RECYCLE defect is remediated **in source**, but the
delivered `.ex5` is a **stale binary compiled before the remediation** and cannot go to Q02.
Recompile is ROT for the review lane — routed back for recompile in the OFF-window / COMPILE_EA queue.

## Decisive defect — stale .ex5 (recompile required)

The `.mq5` was fixed across two commits on 2026-08-23; the `.ex5` was never recompiled:

- `.ex5` file mtime = 2026-08-21 18:23; `.mq5` file mtime = 2026-08-23 18:42.
- `.ex5` git blob is **identical** across the source-changing commits:
  - `git rev-parse 394521ed8~1:<ex5>` = `6a258475efce949f7f157aa6e9fbff169109049b`
  - `git rev-parse 394521ed8:<ex5>`   = `6a258475efce949f7f157aa6e9fbff169109049b`
  - `git rev-parse HEAD:<ex5>`         = `6a258475efce949f7f157aa6e9fbff169109049b`
- Commits that changed the `.mq5` after the original Aug-21 compile:
  - `3ee27e1eb` 2026-08-23 "fix QM5_12940 ... for review"
  - `394521ed8` 2026-08-23 "harden QM5_12940 buffer bounds for D10 compliance" (19 mq5 lines)
- The only commit that ever touched the `.ex5` is the original `c877d9f98` 2026-08-21 18:25.

Consequence: the binary Q02 would run is the pre-fix Aug-21 build — i.e. it still carries the
double-new-bar starvation and the DSS out-of-bounds access that the prior RECYCLE demanded be
fixed. The regenerated artifact (mtime 2026-08-23 18:42) still reports the Aug-21 `ex5_sha256`
(`a0e0983...`) and `compile_succeeded:true`; that compile flag reflects the Aug-21 build, not the
current source hash, so it is misleading. `build_check`/D10-clean here is a **static source** pass,
not a fresh compile of the current source.

Also stale as a follow-on: setfiles carry `build_hash: 541cf5ba...` / `set_version s20260821-001`
(dated 2026-08-21) — regenerate via the governed generator against the new build hash after recompile.

## Source review (all clean — for the record)

- **Card-faithful mechanism**: DSS = EMA(EMA(Stoch_K)) (`DSS_ComputeAtShift`), trigger = SMA(DSS,period)
  (`DSS_ComputeTriggerAtShift`), bull entry prior≤trig & cur>trig & DSS<os_zone (mq5:210), bear mirror
  (mq5:212), D1 EMA(50) regime gate (mq5:235/243), 5-bar momentum gate (mq5:224-233), cooldown 12 bars,
  reverse-crossover exit (mq5:204-207), T1 50% partial at 1.5 ATR (mq5:352-360/383-391), post-T1 1.0-ATR
  trail (mq5:362-377/393-408), 24-bar time stop (mq5:327), spread<0.3 ATR (mq5:260), SL 1.5 ATR from
  crossover-bar extreme (mq5:239/247), one-pos-per-magic (mq5:507). Matches the approved card.
- **Prior RECYCLE defects fixed in source**: (1) single new-bar gate `const bool is_new_bar` (mq5:467),
  used consistently (mq5:468, 497); (2) DSS buffers bounded `k1[128]`, `rawk[64]`, `rawk2[64]` with
  `MathMin` lengths and `s < 128` inner guard (mq5:122-150) — no out-of-bounds indexing under defaults;
  (3) request hygiene `ZeroMemory(req)` + `symbol_slot`/`expiration_seconds` set (mq5:267-269, 509-510);
  (4) exits now match card (reverse crossover + T1 partial + trail + time stop + opposite-signal close).
- **No dead inputs**: all 15 `strategy_*` inputs grep ≥2 occurrences (declaration + ≥1 use-site).
- **Framework conformity**: `#include <QM/QM_Common.mqh>`; MAE hook `QM_FrameworkTrackOpenPositionMae()`
  first in OnTick (mq5:460); kill-switch (mq5:462); Friday close (mq5:465); magic via `QM_FrameworkMagic()`
  / `qm_magic_slot_offset`; news wired `QM_NewsAllowsTrade2` (mq5:500-505); RISK_FIXED=1000 / RISK_PERCENT=0
  (backtest); bounded buffers; all raw-series reads annotated `// perf-allowed`; no ML; no invented
  commission/swap/DST.
- **Setfiles**: governed-generator provenance header present (13/13), correct risk mode — but Aug-21 build_hash (see above).

## Required actions before re-review

1. Recompile the `.ex5` from current source (`1f7db84...`) in the OFF-window / COMPILE_EA queue.
2. Regenerate the 13 setfiles via the governed generator so `build_hash` matches the new `.ex5`.
3. Refresh `artifacts/qm5_12940_build_result.json` with the true post-recompile `ex5_sha256`.

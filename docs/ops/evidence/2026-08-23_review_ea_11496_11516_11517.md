# Claude review_ea — QM5_11496, QM5_11516, QM5_11517 (Gemini-built)

**Router task IDs:** `0de901bc-58f8-4ccd-bc84-227ab710f2b7` (11496),
`38e4d67f-b6bf-41a2-be12-17acab9689d2` (11516), `6e357318-e26d-4aaa-8d66-9fc47981d9ea` (11517)
**Reviewer:** Claude (headless orchestration cycle)
**Date:** 2026-08-23

## Scope

Three `review_ea` tasks routed to claude with `reason: codex_review_required_for_gemini_code`.
Per CLAUDE.md hard rules this is the Claude-side correctness pass; it does **not**
substitute for mandatory Codex review. All three tasks stay in `REVIEW`, not advanced
to `APPROVED`/`PIPELINE`.

## Process note — approved cards live on `D:`, not in the git checkout

All three EAs' `SPEC.md`/setfiles cite `artifacts/cards_approved/<slug>.md`. That path
does **not** exist under `C:/QM/repo/` (confirmed empty for these three slugs) but the
actual files exist at the canonical runtime location
`D:/QM/strategy_farm/artifacts/cards_approved/<slug>.md` for all three EAs (and for
`QM5_9979`, reviewed last cycle — see `docs/ops/evidence/qm5_9979_gemini_review_ea_2026-08-23.md`,
which flagged the card as "missing" without having checked `D:`). Flagging this because
the concurrent Codex-authored review of `QM5_11299`/`QM5_11300` this same cycle
(commits `bb0857246`, `89c4397f4`) issued `REQUEST_CHANGES` partly on the basis of "no
approved card, no card file on disk" — cards for both of those slugs also exist under
`D:/QM/strategy_farm/artifacts/cards_approved/` (`QM5_11299_lwma144-smma5-fractal-m5-scalp.md`,
`QM5_11300_macd-psar-atr-trender-h4.md`, confirmed present). Not re-litigating that
review myself (didn't re-run their full fidelity check), but the "card doesn't exist"
premise of that finding is factually wrong and should be corrected before anyone acts on
it as a RECYCLE trigger. Recommend: reviewers check `D:/QM/strategy_farm/artifacts/cards_approved/`
before concluding a card is missing.

## QM5_11516 — Carter-T SMA(7/21) + CCI(5) (M15)

- Card `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11516_carter-t-sma7-21-cci5-m15.md`
  (`g0_status: APPROVED`) verified line-by-line against the `.mq5`: SMA(7) x SMA(21) cross
  within 2 bars + CCI(5) zero-cross within ±1 bar (`strategy_sync_bars`) of the SMA cross,
  TP1 50% close at 25 pips + move-to-breakeven, TP2 remainder on close-below-SMA7, fixed
  15-pip initial SL, 12-pip spread cap, no-Friday-entry — all match. Card explicitly notes
  SMA(84)/SMA(336) are "structural context only — not used in entry signal directly"; the
  `.mq5` correctly omits them from the entry predicate (not an unwired-input finding since
  they aren't inputs at all).
- `validate_spec_doc.py`: **PASS**. `validate_build_guardrails.py`: **PASS** (0 findings,
  `max_news_stale_hours=336`).
- Magic base `115160000` = `11516*10000+0`; `magic_numbers.csv:16099-16101` covers all 3
  registered symbols (EURUSD/GBPUSD/USDJPY.DWX, offsets 0-2), matches the 3 setfiles 1:1.
- `req.symbol_slot = qm_magic_slot_offset` wired correctly. All 11 `strategy_*` inputs
  used at a call site (`FindSmaCrossShift`, `FindCciZeroCrossShift`, `PartialAlreadyDone`,
  `Strategy_ManageOpenPosition`) — no unwired inputs.
- News: standard framework wiring (`PRE30_POST30`/`DXZ`, `qm_news_stale_max_hours=336`
  at ceiling, not exceeded) — card is silent on news but the implementation correctly
  defaults to the framework-standard blackout rather than disabling it.
- Backtest setfile (`EURUSD.DWX`): `RISK_FIXED=1000`, `RISK_PERCENT=0` — correct.
- `QM_FrameworkTrackOpenPositionMae()` first in `OnTick`; one-position-per-magic
  (`SelectOurPosition` guard before entry); no ML/grid/martingale; M15 swing horizon —
  in charter.
- `iClose(_Symbol, PERIOD_M15, 1)` raw call in `Strategy_ExitSignal` is a single
  closed-bar read with an inline comment ("no QM close reader exists") — guardrail
  validator does not flag it; consistent with the sibling EAs' pattern in this batch.

**Verdict: no correctness defects found. Faithful, card-verified implementation.**

## QM5_11517 — Carter-T 4-EMA Ribbon (5/15/50/100) + MACD (H4)

- Card `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11517_carter-t-ema5-15-50-100-macd-h4.md`
  (`g0_status: APPROVED`) verified against the `.mq5`: EMA(5) x EMA(15) cross within 3
  bars, `close[1] > EMA(50) && close[1] > EMA(100)`, `MACD(12,26,9) > 0` (mirrored for
  short), fixed 30-pip SL / 60-pip TP, 15-pip spread cap, no-Friday-entry, H4 timeframe
  ("primary QM build TF" per card) — all match exactly, including the card's own
  QM-assigned SL (source left SL "unspecified exact").
- `validate_spec_doc.py`: **PASS**. `validate_build_guardrails.py`: **PASS** (0 findings).
- Magic base `115170000` = `11517*10000+0`; `magic_numbers.csv:16102-16103` covers both
  registered symbols (EURUSD/GBPUSD.DWX), matches the 2 setfiles 1:1.
- `req.symbol_slot = qm_magic_slot_offset` wired correctly. All 12 `strategy_*` inputs
  used at a call site — no unwired inputs.
- News: standard framework wiring (`PRE30_POST30`/`DXZ`, ceiling `336`) — same
  card-silent/framework-default-on pattern as 11516.
- Backtest setfiles: `RISK_FIXED=1000`, `RISK_PERCENT=0` — correct.
- MAE-hook-first, one-position-per-magic, no ML/grid/martingale, H4 swing — in charter.

**Verdict: no correctness defects found. Faithful, card-verified implementation.**

## QM5_11496 — Carter-T EMA(100) + PSAR(0.01,0.01) + MACD(64,128,9) (M5)

- Card `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11496_carter-t-ema100-psar-macd64128-m5.md`
  (`g0_status: APPROVED`) confirms entry (EMA100 trend filter, slow-PSAR flip, MACD sign)
  and exit (SAR-dot ± 3 pips SL, 2R TP) — the `.mq5` matches this mechanically:
  `close1>ema100 && psar1<low1 && macd>0` (long, mirrored short), SL derived from PSAR
  with a pip buffer and a 20-pip cap, TP at `strategy_tp_rr_ratio * sl_dist`. Mechanically
  faithful to the card.
- **Blocking — `validate_spec_doc.py`: FAIL** (`SPEC.md missing`). Every sibling EA in
  this review batch (and the batches before it) ships a `SPEC.md`; this one has none —
  confirmed absent both in the EA directory and anywhere searchable under `D:/QM/strategy_farm/`.
- **Blocking — news filter fully disabled, not card-authorized.** Compiled-in defaults:
  `qm_news_temporal = QM_NEWS_TEMPORAL_OFF`, `qm_news_compliance = QM_NEWS_COMPLIANCE_NONE`.
  The card's own "Zusätzliche Filter" section (spread cap, no-Friday-entry) says nothing
  about news, exactly like the 11516/11517 cards from the same source/session — but those
  two default to the framework-standard `PRE30_POST30`/`DXZ` blackout, while this one is
  fully OFF. A card silent on news is not evidence for disabling it; the sibling EAs from
  the identical source demonstrate the correct default is framework-standard news-on.
  There is no compensating control (unlike the one legitimate news-OFF precedent this
  cycle, `QM5_41129`'s OWNER-ratified event-anchored exemption with a documented
  flat-before-statement exit) — this looks like an implementation omission, not a
  deliberate, justified exemption.
- `validate_build_guardrails.py`: PASS (0 findings; it does not check for news-disabled,
  only stale-hours ceiling — this is a manual-review-only defect class).
- Magic base `114960000` = `11496*10000+0`; `magic_numbers.csv:16096-16098` covers all 3
  registered symbols (EURUSD/GBPUSD/AUDUSD.DWX), matches the 3 setfiles 1:1.
  `req.symbol_slot` wired correctly.
- Backtest setfiles: `RISK_FIXED=1000`, `RISK_PERCENT=0` — correct.
- No unwired strategy inputs; MAE-hook-first; one-position-per-magic
  (`Strategy_HasOpenPosition` guard); no ML/grid/martingale.
- No `QM_LogEvent(..., "INIT_OK", ...)` call in `OnInit` (present in every sibling EA
  reviewed this cycle) — minor, non-blocking, consistent with the overall
  lower-completeness pattern of this specific build.

**Verdict: RECYCLE-recommend / REQUEST_CHANGES.** Two blocking gaps (missing SPEC.md;
undocumented news-filter disablement not supported by the approved card) on an otherwise
mechanically card-faithful implementation. Recommend Gemini/build-lane regenerate
`SPEC.md` and wire the standard `PRE30_POST30`/`DXZ` news default (matching 11516/11517
from the same source) before this advances past REVIEW.

## Disposition

All three tasks move to `REVIEW`. 11516/11517: PASS-leaning from the Claude side; 11496:
RECYCLE-recommend from the Claude side. Per hard rules none of this authorizes
`APPROVED`/`PIPELINE` — Codex review remains mandatory before any of the three advances.
No close-review call was made.

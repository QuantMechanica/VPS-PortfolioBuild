# QM5_12772 Q05 Basket Requeue

Date: 2026-06-30
Agent: Codex, branch `agents/board-advisor`

## Context

The requested 66-pair FX cointegration expansion had no clearly unbuilt approved follow-on left in the local book. The original priority baskets were not Q02-blocked at the time of this check:

- `QM5_12533` has logical-basket Q02 `PASS` (`76cb11ee-7e9d-4e95-be28-889f8dc17a7d`) followed by Q04 `FAIL`.
- `QM5_12532` has logical-basket Q02 `PASS`, Q04 `PASS`, then Q05 `FAIL`.

The furthest active follow-on was `QM5_12772` GBPJPY/AUDJPY cointegration:

- Q02 `PASS`: `0ef494c0-7669-4c98-9e5c-326ff70df987`
- Q04 `PASS_SOFT`: `1b418d74-da86-4fb2-aa41-74ebca065f05`
- Q05 `INFRA_FAIL`: `dd43c7e2-7351-41e1-a4a4-f667d0789249`

## Repair

Patched Q05 basket dispatch so `q05_stress_medium.py` now:

- reads `tester_currency` and `tester_deposit` from `basket_manifest.json` and passes them to `run_smoke.ps1`;
- accepts `--logical-symbol` and writes Q05 aggregate evidence under the logical basket symbol instead of only the host leg.

Updated `farmctl._phase_runner_cmd_for_work_item()` so Q05 basket work items receive `--logical-symbol`, matching the existing Q04 behavior.

## Verification

Narrow regression suite:

```text
python -m pytest framework/scripts/tests/test_q05_q07_verdicts.py tools/strategy_farm/tests/test_farmctl_cascade.py tools/strategy_farm/tests/test_q04_latest_full_year_payload.py
```

Result: `21 passed`.

## Queue Action

Requeued the existing non-duplicate Q05 work item:

```text
python tools/strategy_farm/farmctl.py --root D:\QM\strategy_farm enqueue-backtest --ea QM5_12772 --phase Q05
```

Result:

- requeued: `dd43c7e2-7351-41e1-a4a4-f667d0789249`
- symbol: `QM5_12772_GBPJPY_AUDJPY_COINTEGRATION_D1`
- created: none
- skipped: none

No T_Live / AutoTrading action was taken.

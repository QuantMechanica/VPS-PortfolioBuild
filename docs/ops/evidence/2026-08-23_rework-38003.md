# rework-38003 — QM5_38003 review-fix evidence

- Router task: `4c9d202f-bc31-4af1-b9b5-fe8d040c7266`
- EA: `QM5_38003_codetrading-bollinger-engulfing-reversal`
- Approved card: `strategy-seeds/cards/approved/QM5_38003_codetrading-bollinger-engulfing-reversal.md`
- Card authorization: `g0_status: APPROVED` at card line 10
- Source SHA-256: `f9d52ea768617feca5996dc62fb58c95b8c3b96111239337db791b015b480771`
- Scope: source/spec/three backtest presets/focused pytest only; no registry, queue, verdict-row, factory, backtest, T_Live, or AutoTrading mutation

## Review findings and repair evidence

1. **Open-position management remains reachable.** `OnTick()` records MAE at MQ5 line 454, refreshes new-bar state at line 465, calls `Strategy_ManageOpenPosition()` at line 469, and evaluates the strategy exit at line 471. Entry-only `Strategy_NoTradeFilter()` is later at line 491 and framework news admission is later at line 496. The one-position entry guard still exists inside the entry-only no-trade hook through `QM_FrameworkMagic()` at lines 262-264.
2. **ATR spread admission is current and fail-closed.** `StrategySpreadAllowsEntry()` at MQ5 line 246 rejects missing/crossed quotes or unavailable ATR and applies the card's `1.8 * ATR` ceiling. The new-bar state/ATR refresh occurs before entry admission (lines 465 and 491), and the spread is now rechecked after news admission at the final order boundary (line 504) immediately before `QM_TM_OpenPosition`.
3. **The card's exact middle-band payoff is implemented.** Restart state is reconstructed with `HistorySelectByPosition` at MQ5 line 142 and recognized exit deal types. Management calls `QM_TM_PartialClose(..., QM_EXIT_PARTIAL)` at line 379 and records deterministic one-time completion. The source contains neither `QM_TM_MoveSL` nor `QM_TM_MoveToBreakEven`.
4. **Card/framework identity remains sealed.** The approved card names EA/slug at lines 3-4 and the three-symbol universe at line 33. Registry rows remain pre-existing and active at `ea_id_registry.csv:4474` and `magic_numbers.csv:17490-17492`; no registry file changed. The EA includes `QM_Common`, calls the MAE hook, obtains magic through `QM_FrameworkMagic()`, keeps `RISK_PERCENT=0` / `RISK_FIXED=1000` defaults at MQ5 lines 17-18, and passes those modes to `QM_FrameworkInit` at lines 407-410.
5. **Preset bindings were refreshed.** All three H1 `_backtest.set` files bind the source hash at line 13, their active slots at line 18, and `RISK_FIXED=1000` / `RISK_PERCENT=0` at lines 19-20. Their card source comments now point at the approved store.
6. **Regression coverage was added.** `tools/strategy_farm/tests/test_qm5_38003_review_rework.py:44-167` pins management ordering, final-boundary spread admission, restart-safe exact partial close, card entry/SL/TP/risk mechanics, every declared input use-site, setfile/hash/risk identity, QM_Common/MAE/magic wiring, perf annotations, bounded indicator surfaces, and the no-ML/no-raw-order contract.

## Validation output

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_38003_review_rework.py
...... [100%]
6 passed in 0.78s
```

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root C:/QM/worktrees/rework-slot-3 --ea-label QM5_38003_codetrading-bollinger-engulfing-reversal
PASS: files_scanned=1; failures=[]; warnings=[]; D2/D3/D4/D5/D6/D7/D8/D9/D10/D17 all clean; three exact card/registry/setfile symbols clean
```

```text
python tools/strategy_farm/validate_build_guardrails.py <QM5_38003.mq5> <QM5_38003/sets>
PASS: source and all three presets; findings=[]; max_news_stale_hours=336

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_38003_codetrading-bollinger-engulfing-reversal
PASS: 1 PASS, 0 FAIL
```

```text
python -m pytest -q <11 relevant test_build_gate_hardening.py nodes> tools/strategy_farm/tests/test_qm5_38003_review_rework.py
................. [100%]
17 passed in 3.97s
```

The full combined hardening-file run completed `34 passed, 1 failed in 451.47s`; the sole failure is the pre-existing global `QM5_41134_wti-mdaily-iqrmean-mom.mq5` numeric-buffer corpus assertion, outside EA 38003 and untouched by this ticket. The attributable selected hardening suite above is green.

## Scoped compile hold

The authoritative command was invoked with the required one-EA scope only:

```text
pwsh -NoProfile -File framework/scripts/build_check.ps1 -RepoRoot C:/QM/worktrees/rework-slot-3 -EALabel QM5_38003_codetrading-bollinger-engulfing-reversal
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED
detail: terminal64 processes are alive; ad-hoc compile/build_check is refused
exit: 1
```

The guard remained active on a later `include_mirror.py guard` probe. No terminal was stopped, no guard was bypassed, no compile work item or backtest was enqueued, and no stale EX5 is presented as current evidence. Static build hardening, guardrails, spec validation, and focused pytest all pass; a fresh 0-error/0-warning EX5 remains an infrastructure-gated review risk.

## Rollback

Revert the single `rework(38003)` ticket commit with `git revert <ticket-commit>`. This restores the prior source/spec/preset bindings and removes the focused regression test/evidence note. No registry, state-DB verdict, backtest queue, factory, or live-terminal rollback is required because none was changed.

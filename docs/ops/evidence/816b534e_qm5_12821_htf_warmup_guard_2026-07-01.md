# QM5_12821 T-WIN HTF Warmup Guard Evidence

Task: `816b534e-69ca-4faa-819a-ac62b4036475`

## Change

- Added `framework/include/QM/QM_TWINWarmupGuard.mqh`.
- Wired QM5_12821 to capture the first host H1 bar time at `OnInit`.
- Blocks new basket entries until both higher-timeframe lead-in requirements are mature:
  - `4` full W1 periods.
  - `31` calendar days as the one-MN-period guard.
- Added a direct guard at `QM12821_EvaluateSignal()` so D1/W1/MN CSM reads, exhaustion, and
  MTF coherence do not evaluate before the higher-timeframe warmup is mature.
- Added a T-WIN harness assertion proving the guard blocks the immature-MN window and the
  four-W1-only boundary.

The existing `strategy_basket_stop_pct = 1.0` was not changed.

## Q02 Window Check

For the known Q02 clamp start `2018.07.02`, the warmup maturity is:

```text
2018.07.02 00:00 + 31 days = 2018.08.02 00:00
```

That also exceeds the four-W1 requirement (`28` days), so the EA cannot open a basket
before both W1 and MN lead-in requirements are satisfied.

## Verification

Module harness strict compile:

```text
pwsh -NoProfile -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/_tests/QM_TWIN_Module_tests/QM_TWIN_Module_tests.mq5 -Strict
```

Result:

```text
compile_one.result=PASS
compile_one.errors=0
compile_one.warnings=0
compile_one.log=C:\QM\repo\framework\build\compile\20260701_121149\QM_TWIN_Module_tests.compile.log
compile_one.ex5=C:\QM\repo\framework\EAs\_tests\QM_TWIN_Module_tests\QM_TWIN_Module_tests.ex5
```

EA strict compile:

```text
pwsh -NoProfile -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_12821_twin-csm-basket/QM5_12821_twin-csm-basket.mq5 -Strict
```

Result:

```text
compile_one.result=PASS
compile_one.errors=0
compile_one.warnings=0
compile_one.log=C:\QM\repo\framework\build\compile\20260701_121200\QM5_12821_twin-csm-basket.compile.log
compile_one.ex5=C:\QM\repo\framework\EAs\QM5_12821_twin-csm-basket\QM5_12821_twin-csm-basket.ex5
```

No T_Live, AutoTrading, or `terminal64.exe` action was taken.

# rb-v4-loader evidence — 2026-08-23

## Scope and safety result

Implemented proposal ticket 2 as a loader-only, READ_INERT v4 branch. The active
default remains `gate_manifest.v3.json`; no runtime DB, factory, queue, verdict,
backtest, book, or `T_Live` state was read or mutated.

## What changed

- `tools/strategy_farm/gate_manifest.py:29-42` adds the v4 schema identifier and
  explicit paths for the checked-in `.draft.json` and future
  `gate_manifest.v4.json`, while `DEFAULT_MANIFEST` remains v3 at line 48.
- `tools/strategy_farm/gate_manifest.py:233-280` exposes macro-phase lookup and
  explicit, version-stamped v3↔v4 gate equivalence. The module-level adapter is
  at `tools/strategy_farm/gate_manifest.py:923-932`.
- `tools/strategy_farm/gate_manifest.py:500-723` adds fail-closed v4 activation,
  equivalence, topology, storage, Phase-3 book-trigger, and criteria-preservation
  validation. Q14 is the only per-EA terminal and no `next` edge enters Q15.
- `tools/strategy_farm/gate_manifest.py:734-893` adds the closed v4 loader branch,
  `^Q(?:0[0-9]|1[0-7])$`, 18-gate handling, v4 aliases, and frozen equivalence.
- `tools/strategy_farm/phase_ids.py:49-64` adds the pure
  `build_phase_tables(manifest)` function. Existing v3 globals are still built
  from the default manifest at `tools/strategy_farm/phase_ids.py:71`.
- `tools/strategy_farm/config/gate_manifest.v4.draft.json:5` now accurately says
  the draft is explicitly loadable but cannot be default; its READ_INERT guard
  is schema-closed at lines 108-113. Gate IDs, names, v3 mapping, and criteria
  remain as proposed.
- `tools/strategy_farm/schemas/gate_manifest.v4.schema.json:1-245` adds the closed
  Draft 2020-12 machine schema for the 18 gates, three macro phases, equivalence,
  storage, book trigger, and READ_INERT/ACTIVE guard shapes.
- `tools/strategy_farm/tests/test_gate_manifest.py:142-252` covers READ_INERT load,
  v3 default retention, all Phase-2/3 equivalence round trips, strict linearity,
  trigger-only Phase-3 entry, pure phase-table construction, stable SHA-256, the
  future final filename, and activation failure modes. Schema assertions are at
  lines 322-370.

## Verification

Phase-importer census:

```text
> rg -l "phase_ids" tools/strategy_farm/tests -g "*.py"
tools/strategy_farm/tests\test_gate_manifest.py
```

Focused suite and every test importing `phase_ids` (the census identifies the
same file):

```text
> python -m pytest tools/strategy_farm/tests/test_gate_manifest.py -q -ra
.......................                                                  [100%]
23 passed in 1.21s
```

The base environment did not contain the optional `jsonschema` library. For this
final run, `jsonschema==4.25.1` was installed into a unique temporary directory
outside the worktree via `pip --target`, exposed only through `PYTHONPATH` for
the command, and removed afterward. Thus both v3 and v4 Draft 2020-12 schema
validation tests executed rather than skipped.

Additional touched-module checks:

```text
> python -m py_compile tools/strategy_farm/gate_manifest.py tools/strategy_farm/phase_ids.py
exit 0

> git diff --check
exit 0 (line-ending conversion warnings only)
```

Pinned checked-in v4 draft digest:

```text
702318775a756a19025e65fd2b9cdc18ff32d5880f43cdbaa5b75843e2c39c32
```

## Rollback

Revert the ticket commit with `git revert <rb-v4-loader-commit>`. This removes the
v4 schema/loader branch and tests, restores the draft note/guard shape, and
restores the prior `phase_ids.py` initialization. No database or runtime-state
rollback is required because this ticket performs no runtime mutation and never
changes the v3 default.

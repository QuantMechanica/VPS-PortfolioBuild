# QM5_20144 Q02 initialization repair — 2026-07-31

## Scope and selection

- Fleet claim: `4f8a0f72-49f7-4634-88f3-02db9fdfbcb9`
- Branch: `agents/board-advisor`
- EA: `QM5_20144_ichimoku-atr-cloud-d1`
- Card: `g0_status: APPROVED`, R1–R4 PASS, D1, approximately 12
  trades/year/symbol, no ML, four FX majors (`EURUSD.DWX`, `GBPUSD.DWX`,
  `USDJPY.DWX`, `USDCHF.DWX`).
- Source lineage: `BP-UNHOMMEFOU-ICHIMOKU-18242`, quality tier B- in the
  approved card, with the source's unverified walk-forward claim explicitly
  reserved for downstream stability testing.

No genuinely unbuilt, low-frequency structural card in the inspected backlog
had both prerequisite registry allocations and no existing build. This unit
therefore used the mission's second-priority diverse-instrument infrastructure
lane.

## Failure evidence and diagnosis

The latest failed Q02 work item was
`0e4e627e-ea2e-4271-b381-7da61b69ca19` on `GBPUSD.DWX`. Its summary at
`D:/QM/reports/work_items/0e4e627e-ea2e-4271-b381-7da61b69ca19/QM5_20144/20260731_185209/summary.json`
classified `ONINIT_FAILED`. The logger showed framework `INIT`, followed by
immediate deinitialization with reason 8 and no `INIT_OK`.

Two defects were in the same startup validation path:

1. `Strategy118_Init` required `BarsCalculated` warmup and ran the causal
   cloud self-test inside `OnInit`, before tester indicator buffers were
   guaranteed to be calculated.
2. Once that timing defect was removed, the diagnostic smoke exposed a latent
   alignment mismatch. The approved card requires native Senkou values at the
   display-bar shift and a manual comparison at `display_shift + kijun`; the
   implementation read both native and manual values at the latter shift.

## Repair

- `OnInit` now validates the D1 configuration and creates pooled handles only.
- The one-time causal self-test is deferred until the D1 warmup and indicator
  buffers are ready. Transient readiness failures retry fail-closed; a real
  mismatch logs once, blocks entries, and cannot create a log bomb.
- Senkou A/B runtime reads now use the approved display-bar alignment, while
  the manual self-test uses the 26-bar-earlier source calculation.
- `SPEC.md` was migrated to the current seven-section Q01 schema and records
  the deferred startup test and causal alignment.

Strategy parameters, cohort, stops, risk sizing, and entry/exit thresholds
were not changed.

## Verification

- Q01 spec validator: PASS.
- Final strict compile: PASS, 0 errors, 0 warnings.
- Final build check: PASS, 0 failures, 0 warnings.
  Report: `D:/QM/reports/framework/21/build_check_20260731_211305.json`.
- Final source SHA-256:
  `11803103f290a57987511170f1c5b8eb64f879b42f93f0613e94d620aef9bca0`.
- Final compiled EX5 SHA-256:
  `cf823cbb71c08b08090c6fc1eb7efc95fffdd5c2eb33ed676affdbd79cb6620c`.
- All four existing D1 backtest setfiles retain `RISK_FIXED=1000` and
  `RISK_PERCENT=0`.
- One diagnostic EURUSD 2024 smoke invocation completed two deterministic,
  technically valid tester runs with `oninit_failure_detected: false`. It
  produced zero trades because the still-misaligned self-test correctly
  blocked entries; that observation led to the final alignment correction.
  No second ad-hoc smoke was launched: final runtime validation was handed to
  the normal Q02 queue.

## Q02 handoff

The canonical Q02 enqueue helper created work item
`948e0ecf-35f2-4816-9cef-bca6f0e99d82` for `GBPUSD.DWX` / D1 using the
RISK_FIXED setfile. The farm immediately claimed it on T4. Existing pending
EURUSD and USDJPY work items remain untouched and will resolve the current
compiled artifact at claim time.

## Safety boundary

No live terminal, AutoTrading state, portfolio gate, T_Live manifest, or live
deployment artifact was modified. The terminal fleet remained below its CPU
ceiling throughout this unit.

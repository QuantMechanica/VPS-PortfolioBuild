# Review — QM5_9946 bandy-supertrend-flip-trend (Claude review lane)

- Router task: `2ca5af9b-f84e-40e3-a4bd-2c0d784f65f5` (review_ea, assigned codex, REVIEW)
- Source build task: `f24b54a3-...` (agy/gemini, `build_ea`) — verdict "compiled cleanly, awaiting Codex review"
- Build identity: `docs/ops/evidence/f24b54a3_qm5_9946_bandy-supertrend-flip-trend_build_identity.json`
- Prior codex review artifact: `docs/ops/evidence/2026-08-23_codex_review_2ca5af9b_QM5_9946.json` (verdict FAIL)
- Canonical card: `framework/EAs/QM5_9946_bandy-supertrend-flip-trend/docs/strategy_card.md`
- Source reviewed: `.../QM5_9946_bandy-supertrend-flip-trend.mq5` sha256 `1d3ebe66…558c` (matches build identity)

## Verdict: RECYCLE

Compiles structurally (framework corset PASS), no dead inputs, but named actionable
card-fidelity and evidence defects remain. Return to build lane; not clean for Q02.

## Framework conformity — PASS
- `#include <QM/QM_Common.mqh>`; `QM_FrameworkInit` (mq5:284), `QM_FrameworkShutdown` (309).
- MAE hook `QM_FrameworkTrackOpenPositionMae()` wired (mq5:314).
- Magic via `QM_FrameworkMagic()` (163/238/332); registry rows present, `ea_id*10000+slot`
  (`framework/registry/magic_numbers.csv`, e.g. 9946/slot6 → 99460006).
- Risk: `RISK_FIXED=1000` backtest, `RISK_PERCENT` input present (mq5:13-14).
- News wired: `QM_NewsAllowsTrade2` (mq5:346). Kill-switch (316). Friday close (322).
- Indicator reads bounded + perf-annotated (`CopyRates` warmup+2, mq5:103); `QM_ATR`/`QM_SMA`
  pooled. No ML, no invented commission/swap.
- Input use-sites: all 8 `strategy_*` inputs are read (period mq5:98/120; mult 125-126;
  regime_sma 174; atr_period 179; stop_atr_mult 196/215; max_hold_bars 261; warmup 99;
  max_spread_points 78/87). **No dead inputs.**

## Actionable defects (RECYCLE)
1. **Time stop is wall-clock, not trading bars** (mq5:261):
   `TimeCurrent()-opened >= strategy_max_hold_bars * PeriodSeconds(D1)` = 60 *calendar*
   days. Card mandates "60 trading days / 60 D1 bars (~3 months)". Weekends/holidays make
   the position close materially earlier than 60 completed D1 bars. Count closed D1 bars,
   not seconds.
2. **Supertrend band ATR period diverges from card mechanic** (mq5:120):
   `QM_ATR(..., strategy_supertrend_period=10, ...)` feeds the Supertrend band, but the
   card Mechanik ("atr = ATR(14)") and Build-EA Notes ("Use iATR(...,14,...)") both specify
   ATR(14) for the band; only the cat-stop uses `strategy_atr_period=14` (mq5:179). The
   card is internally inconsistent (header calls "(period=10, multiplier=3.0)" the cardified
   pairing), so this needs an explicit card-vs-code reconciliation, not a silent pick.
3. **No COMPILE_OK evidence for the current source hash.** The only in-repo compile log
   (`framework/build/compile/20260822_124523/…compile.log`) is a STALE 08-22 run that
   FAILED (78 errors, mangled source). The `.ex5` (08-23 08:51) postdates the current
   `.mq5` (08-23 08:49) and the build-identity JSON asserts `build_check_passed:true`, but
   no retained COMPILE_OK log binds hash `1d3ebe66…`. Build identity JSON is also not the
   canonical `build_result.json` schema (missing `ea_dir/magic_base/symbols_registered/
   compile_succeeded/smoke_*`); no smoke report bound. Evidence-over-claims: provide a
   COMPILE_OK log + smoke result for the current hash.
4. **Setfiles generated before the card existed** (governed generator, 08-22 23:09):
   headers carry `card_defaults_status=none_found` — strategy params were NOT materialized,
   setfiles fall back to EA input defaults. The card now exists at the D: pointer (08-23
   15:52). Regenerate setfiles via `gen_setfile.ps1` so `strategy_*` params are bound from
   the card.
5. **SPEC.md corruption** (cosmetic): slug rendered `andy-supertrend-flip-trend` (missing
   leading "b"); backtest RISK_FIXED shown as `,000` instead of `$1,000`. Fix generator
   escaping.

## Non-blocking / deferrable
- 0.2*ATR razor-thin-flip noise filter absent — card marks it **"optional P3 ablation"**;
  acceptable to defer past Q02.
- PERF-ADVISORY: `Strategy_ExitSignal` runs every tick (mq5:330, before the `QM_IsNewBar`
  gate) and reconstructs ~200 D1 Supertrend bars via `CopyRates` + 200 pooled `QM_ATR`
  calls each tick. Cache closed-bar state to cut tester cost. Not a correctness defect.

## Concurrence
Independent of the prior codex FAIL; I concur on substance (findings 1,2,4,5 overlap).
Router state RECYCLE (compiles, defects are named and buildable) rather than FAILED/BLOCKED.

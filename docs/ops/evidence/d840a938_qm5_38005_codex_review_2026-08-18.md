# QM5_38005 Codex review evidence — 2026-08-18

- Review task: `d840a938-f474-48fe-bf94-ce7cb82a8f17`
- Source build task: `2f3177d2-2769-4e0d-a212-4769b908178c` (Gemini)
- EA: `QM5_38005_codetrading-ascending-triangle-breakout`
- Disposition: `FAIL_CODE_REVIEW`; remain in `REVIEW`

## Verification

- Mechanical pre-screen: PASS (artifacts, freshness, three active magic rows, setfiles, fixed-risk mode, input wiring)
- Build guardrails: PASS, four files checked, no findings, news staleness ceiling 336 hours
- Fresh compile: PASS, 0 errors, 0 warnings
- Card-to-code review: FAIL

## Blocking findings

1. `Strategy_NoTradeFilter()` rejects while this magic has an open position (MQ5 line 208), and `OnTick()` returns at line 385 before `Strategy_ManageOpenPosition()` at line 388. Position management/exits are unreachable while needed.
2. ATR spread admission runs before `AdvanceState_OnNewBar()` at line 415 and is not rechecked. Startup can enter with `g_last_atr=0`, and later entries can use the prior cached ATR rather than the refreshed signal bar's ATR.
3. The card requires actual resistance/support slopes and specifically `support_slope >= +0.10` (card line 76). The code uses relative price differences for the flat boundary and only `l1 > l2` / `h1 < h2` for the sloping boundary (MQ5 lines 174-188); it never divides price change by pivot-bar distance or applies the required minimum slope.
4. The card requires trailing with swing pivot lows/highs (card line 99). The implementation only moves the stop once near break-even (MQ5 lines 310 and 322), and the Gemini evidence/spec silently substitutes that different exit mechanic.

## Required repair

- Separate entry admission from open-position management/exits and recheck refreshed ATR spread immediately before entry.
- Compute pivot-line slopes over their bar distances and enforce both governed slope constraints symmetrically.
- Implement deterministic swing-pivot trailing (tightening only) per the card, or obtain an OWNER-approved card change.
- Recompile and regenerate bound evidence; later Codex review remains mandatory.

No Q phase or pipeline verdict is inferred. No terminal, T_Live, or AutoTrading action occurred.

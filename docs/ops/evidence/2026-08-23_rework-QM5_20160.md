# rework-QM5_20160 — review-fix evidence

- Router task: `1099e860-4dcd-4d82-acc1-c65a9b995335`
- EA: `QM5_20160_xng-fri-trend`
- Worktree/branch: `C:/QM/worktrees/rework-slot-2` / `rework-slot-2`
- Review source: `C:/QM/worktrees/codex-orchestration-1/artifacts/reviews/QM5_20160_codex_review_1099e860_20260726.md`
- Disposition: source repair complete; binary refresh and smoke remain fail-closed behind the live-factory compile interlock

## Review findings and disposition

1. The old Gemini result claimed `smoke_result=success` with a null report.
   The replacement build-result record is
   `docs/ops/evidence/2026-08-23_rework-QM5_20160_build_result.json`; it records
   `smoke_result=NOT_RUN_STALE_BINARY`, leaves `smoke_report_path=null`, and
   explicitly marks the checked-in EX5 stale. No smoke success is claimed.
2. The review-required mandatory blackout is locked in the EA at
   `framework/EAs/QM5_20160_xng-fri-trend/QM5_20160_xng-fri-trend.mq5:29-34`
   and in the only backtest set at
   `framework/EAs/QM5_20160_xng-fri-trend/sets/QM5_20160_xng-fri-trend_XNGUSD.DWX_D1_backtest.set:23-28`.
   The temporal axis is PRE30/POST30, compliance is DXZ, legacy mode is PAUSE,
   and the stale-data ceiling remains 336 hours. The approved card was not
   edited in place; the EA-local card copy is synchronized byte-for-text with
   the current approved card and the review verdict supplies the framework
   compliance override while that execution contract remains DRAFT.

## Card-faithful implementation repair

- The OWNER-approved 2026-08-16 session-clock amendment is implemented by the
  new inputs at `QM5_20160_xng-fri-trend.mq5:45-48`, session-anchor/tick state at
  `:129-160`, and one consumed weekly attempt plus attach-window gating at
  `:412-472`.
- Every declared strategy input is locked and has a mechanism use-site. The
  regression is `tools/strategy_farm/tests/test_qm5_20160_review_rework_static.py:45`.
- The load-bearing mechanic is unchanged: exact XNGUSD.DWX D1 Friday/Thursday
  boundary, short-only negative completed 252-D1 log return, frozen 3 ATR stop,
  no TP, no retry, next-D1/two-day lifecycle repair.
- One bounded `FRIDAY_ENTRY_ATTEMPT`, one explicit `ENTRY_REJECT`, and one
  `ENTRY_SIGNAL_FIRE` path were added (`QM5_20160_xng-fri-trend.mq5:162-171`,
  `:439-447`, `:524-530`). This addresses the prior zero-trade run's missing
  entry-layer observability without changing economics.
- `CopyClose` remains bounded and now checks both the copy count and
  `ArraySize` before indexing (`QM5_20160_xng-fri-trend.mq5:292-304`). All raw
  series calls are annotated `perf-allowed`.
- The existing V5 umbrella include, resolver-owned `QM_FrameworkMagic()` path,
  direct MAE hook (`:592`), and dual fixed/percent risk inputs into
  `QM_FrameworkInit` (`:553-568`) remain intact. No ML code was introduced.
- Registry evidence is unchanged: `framework/registry/ea_id_registry.csv:4225`
  and `framework/registry/magic_numbers.csv:15309` already contain the active
  EA/slot allocation. No registry file was edited.

## Guardrail correction (threshold unchanged)

The existing entry-grace guard treated the amended ten-minute window as if it
still began at the D1 label. It now recognizes an operational
`strategy_session_offset_min` and evaluates the window end as
`session_offset + grace` against the unchanged requirement
`measured_offset + 5 minutes` (`tools/strategy_farm/validate_build_guardrails.py:730-748`).
The pass/fail contrast is covered at
`tools/strategy_farm/tests/test_build_guardrails.py:340-378`: 61.6+10 passes;
61.6+4 still fails the unchanged 66.6-minute boundary.

## Validation evidence

```text
python framework/scripts/skill_build_ea_guard.py --ea-id 20160 --ea-label QM5_20160_xng-fri-trend
status=ok; ea_registry_row=true; magic_registry_rows=true; ea_dir_exists=true

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_20160_xng-fri-trend
PASS; Summary: 1 PASS, 0 FAIL

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_20160_xng-fri-trend/QM5_20160_xng-fri-trend.mq5
PASS; findings=[]; max_news_stale_hours=336

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_20160_xng-fri-trend
PASS; failures=[]; files_scanned=1

python -m pytest -q tools/strategy_farm/tests/test_qm5_20160_review_rework_static.py tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_mnt012_build_guards.py
39 passed in 2.86s
```

The broader touched/corpus suite produced `56 passed, 1 failed in 579.05s`.
The sole assertion is an unrelated pre-existing corpus finding in
`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`; the first reported
source is `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`. No QM5_411xx file is
modified by this ticket. The target-scoped hardening result above is clean.

## Compile and smoke interlock evidence

`compile_one.ps1 -Strict` and scoped `build_check.ps1 -EALabel
QM5_20160_xng-fri-trend -Strict -SkipCompile` were both attempted. They refused
before compilation with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because T1-T10
factory terminals were alive. No process was stopped and factory state was not
changed. Evidence:

- `framework/build/compile/20260824_122553/QM5_20160_xng-fri-trend.compile.log`
- `D:/QM/reports/compile/20260824_122553/summary.csv`
- forced orchestrator attempt:
  `framework/build/compile/20260824_122819/QM5_20160_xng-fri-trend.compile.log`

The prescribed compile queue was also checked. Read-only candidate
classification returned `eligible=false` with
`EX5_ALREADY_PRESENT`, `WORK_ITEMS_EXIST`, and `BOUND_SETFILE_HASH_EXISTS`;
the ticket is not in the compile source-repair authority allowlist. Therefore
no compile work item was enqueued and no stale binary was smoke-tested. Current
identities are source SHA256
`ede9a59a1feb80e5a2ed92a57ba9268f899be5d86a5cc304057f670602be3990`
and stale EX5 SHA256
`8796b1fb235f47eb2864da252ec543f2235e876bb5fd0c9dae404b4c037f2acb`.

The original zero-trade evidence remains
`D:/QM/reports/work_items/1a253cfa-4b9a-4ef3-a707-6589dd0f4972/QM5_20160/20260725_195517/summary.json`.
No backtest or smoke was enqueued, and no pipeline verdict was overwritten.

## Safety and rollback

- No `C:/QM/mt5/T_Live` path, AutoTrading state, factory switch, gate threshold,
  magic allocation, work-item verdict, or backtest queue was modified.
- Roll back atomically with `git revert <this-ticket-commit>` after review, or
  restore only the explicit files listed in that commit. The prior EX5 was not
  replaced, so binary rollback is unnecessary.

## Remaining review risks

- Compilation and a same-lineage trade-capability smoke are still required on
  the repaired source before Q02 readiness can be claimed.
- `strategy_session_offset_min=61.6` is the OWNER-approved value; the registry
  now carries a measured XNG modal offset of 60.0 minutes. Independent tick-level
  XNG confirmation remains a card-noted follow-up, not a threshold change here.
- The approved card text still says news axes OFF while the upheld review verdict
  mandates the Edge Lab blackout. The EA follows the review authority without
  mutating the approved card; OWNER/reviewer should decide whether a governed
  execution-contract amendment is required before compile activation.

# rework-41009 review-fix evidence

**Router task:** `1bd8fddf-b1b4-4bab-ad15-d0fc10da1e98`  
**EA:** `QM5_41009_volume-profile-value-area-rejection`  
**Worktree:** `C:/QM/worktrees/rework-slot-3`  
**Source SHA-256:** `f019bdfe887856f4ae307402ab7488f20cb69b350871b7d2243e46285d90e806`

## Work order and scope

The task was claimed from `RECYCLE` to `IN_PROGRESS` with verdict
`rework burn-window slot 3`. The concrete findings came from:

- `docs/ops/evidence/1bd8fddf_qm5_41009_gemini_build_codex_review_2026-08-18.md`
- `docs/ops/evidence/74f741c5_qm5_41009_gemini_codex_review_2026-08-23.json`
- `artifacts/builds/0fd1b0a8-c415-4309-9778-4ebefa05a1cf.json`

The approved card is `g0_status: APPROVED`. The EA registry row and both
active magic rows already existed: slot 0 `SP500.DWX=410090000`, slot 1
`NDX.DWX=410090001`; both values are present in `QM_MagicResolver.mqh`.
No registry or resolver file changed.

## What changed

- Backtest-valid source risk defaults (`RISK_PERCENT=0`, `RISK_FIXED=1000`),
  M5 runtime execution declaration, and explicit framework Friday-close
  override: `framework/EAs/QM5_41009_volume-profile-value-area-rejection/QM5_41009_volume-profile-value-area-rejection.mq5:18`, `:551`.
- Card loss limits are inputs and executable mechanisms: 2.0% account daily
  realized-loss entry halt, 2.5% daily equity hard stop versus broker-day
  starting balance, and 5.0% total drawdown hard stop versus persisted initial
  strategy equity: source `:44`, `:110`, `:153`, `:395`.
- The 23:55-00:05 window now converts broker time with `QM_BrokerToUTC`:
  source `:411`.
- Market-order deviation is derived from the card's three ticks and passed to
  `QM_EntryConfigure`: source `:185`, `:557-562`.
- Wrong-side prior-day POC targets now fail closed instead of substituting an
  unapproved 1.8R take-profit: source `:451`, `:470`.
- The unevidenced 500-bucket profile ceiling was replaced by a bounded
  1,000,000-bucket capacity with allocation and `ArraySize` guards: source
  `:49`, `:274-279`, `:281-359`. This represents 100,000 price units even at
  a 0.01 point and the default ten-point bucket, while remaining explicitly
  memory-bounded.
- Management runs before entry-only no-trade filters; the direct MAE hook and
  framework magic chain remain intact: source `:581-602`.
- Both backtest setfiles bind the repaired source hash and the four new card
  parameters: `sets/*_backtest.set:13`, `:31-34`.
- `SPEC.md:28-31`, `:87-94`, `:101` documents the repaired contract.
- Focused regression coverage is in
  `tools/strategy_farm/tests/test_qm5_41009_review_rework.py:48-135`.

Every declared input has at least two source use-sites. Every raw `Bars` /
`iTime` / `iOpen` / `iHigh` / `iLow` / `iClose` / `iVolume` call retains an
inline `perf-allowed` justification. No ML or raw `OrderSend` surface exists.

## Validation

PASS:

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_41009_review_rework.py tools/strategy_farm/tests/test_runtime_execution_contract_static.py
11 passed in 18.13s

python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py -k "loss_limit or raw_broker_hour or build_check_applies_hardening_pass_and_fail_fixtures or build_check_rejects_d11_phantom_symbol"
4 passed, 25 deselected in 4.34s

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_41009_volume-profile-value-area-rejection
failures=0 warnings=0 files_scanned=1 symbols=NDX.DWX,SP500.DWX

python framework/scripts/skill_build_ea_guard.py --ea-id 41009 --ea-label QM5_41009_volume-profile-value-area-rejection
status=ok (EA registry, magic rows, EA directory)

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_41009_volume-profile-value-area-rejection
Summary: 1 PASS, 0 FAIL

python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 <EA source> <EA sets dir>
verdict=PASS; findings=[] for source and both sets

git diff --check
PASS (only Git's LF-to-CRLF notices)
```

The complete `test_build_gate_hardening.py` run produced `33 passed, 1 failed`
in 380.81s. The sole failure is pre-existing and outside this ticket:
`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven
QM5_411xx sources (first: `QM5_41134_wti-mdaily-iqrmean-mom.mq5`). EA 41009's
own hardening result is zero failures/warnings; no QM5_411xx file was touched.

## Compile interlock evidence

The required scoped compile was attempted exactly once:

```text
pwsh -NoProfile -File framework/scripts/compile_one.ps1 \
  -EAPath framework/EAs/QM5_41009_volume-profile-value-area-rejection/QM5_41009_volume-profile-value-area-rejection.mq5 \
  -Strict
```

It was deterministically refused before compilation with
`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because `terminal64` processes were
alive. No retry or bypass occurred. Evidence paths:

- compile log: `C:/QM/worktrees/rework-slot-3/framework/build/compile/20260824_082753/QM5_41009_volume-profile-value-area-rejection.compile.log`
- compile summary: `D:/QM/reports/compile/20260824_082753/summary.csv`

The same interlock refused scoped `build_check.ps1 -EALabel
QM5_41009_volume-profile-value-area-rejection -SkipCompile`. Terminals were not
stopped, the factory was not toggled, and no compile/backtest item was
enqueued. Consequently the tracked EX5 remains the prior binary
(`da2ad0e1343ffaf9b466e0de4bbbbc09871214754b5e4d07b549de174b7266c8`,
393,208 bytes) and is not claimed as evidence for this repaired source.

## Safety and rollback

No backtest was enqueued/deleted, no verdict row was overwritten, no gate
criterion changed, and no T_Live/AutoTrading/factory action occurred.

Rollback is one commit revert after submission:

```text
git revert <rework(41009) commit>
```

This restores the EA source, SPEC, and both setfiles and removes the focused
test/evidence note. Registry/magic rollback is unnecessary because those files
were not modified.

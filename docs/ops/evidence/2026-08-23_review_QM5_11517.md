# Review — QM5_11517 carter-t-ema5-15-50-100-macd-h4 (H4)

- **Router task:** 6e357318-e26d-4aaa-8d66-9fc47981d9ea (review_ea, claude)
- **Source task:** df68e99a (gemini/agy build_ea), REVIEW → closed here
- **Reviewer:** Claude (review lane), 2026-08-23
- **Verdict:** APPROVED (formally clean enough for Q02)

## Scope
EA dir `framework/EAs/QM5_11517_carter-t-ema5-15-50-100-macd-h4/` vs Strategy Card
`D:/QM/strategy_farm/artifacts/cards_approved/QM5_11517_carter-t-ema5-15-50-100-macd-h4.md`.

## Build identity / compile
- `df68e99a_..._build_identity.json`: `build_check_passed: true`.
- Current source sha256 `860b44ac…994bc` == identity `mq5_sha256`. **COMPILE_OK matches current source hash.**
- Current `.ex5` sha256 `a453486593…714bc` == identity `ex5_sha256`. Build artifact is current, not stale.

## Card faithfulness (mechanism)
- LONG: EMA(5) crosses up EMA(15) within `strategy_cross_lookback`=3 bars (`FindEmaCrossShift(true,…)`, mq5:76-96), close1 > EMA(50) AND > EMA(100), MACD main > 0 → matches card Entry LONG 1-3.
- SHORT: mirror with MACD < 0 → matches card Entry SHORT.
- SL fixed 30 pips via `QM_StopFixedPips`, TP fixed 60 pips (H4, source-specified). Matches card Exit/SL.
- Spread cap 15 pips (`SpreadWithinCap`), no-Friday-entry — matches card Additional filters.
- Signals computed on closed bar (shift=1) + `QM_IsNewBar(_Symbol,PERIOD_H4)` gating; market entry on new bar — standard QM realization of "open of next bar".

## Dead-input check (every declared strategy input grepped for use beyond declaration)
All 12 strategy inputs have ≥1 use site beyond declaration:
fast/slow/trend1/trend2 EMA periods, macd fast/slow/signal, cross_lookback, sl_pips, tp_pips,
spread_cap_pips, no_friday_entry — all wired. **No dead inputs.**

## Framework conformity
- Include chain: `#include <QM/QM_Common.mqh>` (mq5:5).
- Magic via `QM_FrameworkInit(qm_ea_id, qm_magic_slot_offset,…)` + `QM_FrameworkMagic()` (MagicResolver path); `req.symbol_slot = qm_magic_slot_offset`. No hardcoded magic.
- RISK_FIXED=1000 (backtest) + RISK_PERCENT=0.5 (live) both declared; ENV→mode enforced by QM_FrameworkInit.
- News filter wired: `QM_NewsAllowsTrade2(_Symbol, broker_now, qm_news_temporal, qm_news_compliance)` (mq5, OnTick). `Strategy_NewsFilterHook` is an intentional no-op; the framework news gate is the live filter.
- MAE hook: `QM_FrameworkTrackOpenPositionMae()` first in OnTick.
- No ML libs; no hardcoded commission/swap/DST values.
- Indicator/stop helpers resolve to real include signatures (QM_EMA @QM_Indicators.mqh:549, QM_MACD_Main @635, QM_StopFixedPips @QM_StopRules.mqh:139).

## Set files (governed path)
- `sets/…_EURUSD.DWX_H4_backtest.set` + `…_GBPUSD.DWX_H4_backtest.set`, set_version s20260822-001.
- environment=backtest, risk_mode=FIXED, RISK_FIXED=1000, RISK_PERCENT=0, all 12 strategy params present. No raw MQ5 promotion.

## Minor (non-blocking)
- `SPEC.md` §7 Risk Model has a garbled template substitution: renders `,000 per trade` (the `$1` was stripped). Cosmetic doc defect only; code/set-file risk values are correct. Fix on next SPEC touch; does not gate Q02.

## Decision
Card-faithful, no dead inputs, framework-conformant, build current + COMPILE_OK, governed set files. **APPROVED** — formally clean enough for Q02.

# Review — QM5_41009 volume-profile-value-area-rejection (router task 74f741c5)

- **Date:** 2026-08-24
- **Reviewer:** Claude (review lane)
- **Router task:** 74f741c5-8414-49f3-8565-3adaf818da50 (review_ea, was REVIEW/codex)
- **Source build:** agy/gemini task 0fd1b0a8-c415-4309-9778-4ebefa05a1cf
- **Artifact:** artifacts/builds/0fd1b0a8-c415-4309-9778-4ebefa05a1cf.json
- **EA dir:** framework/EAs/QM5_41009_volume-profile-value-area-rejection
- **Card:** strategy-seeds/cards/QM5_41009_volume-profile-value-area-rejection.md (status APPROVED / g0 APPROVED)

## Verdict: APPROVED (clean for Q02)

## Evidence

### Build integrity (current source hash)
- mq5 sha256 `6778...07c8d` matches artifact `mq5_sha256` (verified via sha256sum).
- ex5 sha256 `da2a...266c8` matches artifact `ex5_sha256`. Compile evidence is for the
  CURRENT source (mtime skew only — content identical). `compile_succeeded:true`,
  `build_check_passed:true` in artifact.

### Card-faithful mechanism
- Prior broker-day volume profile → 70% Value Area (VAH/VAL/POC), fade probes of
  VAL/VAH with reversal candle. Matches card §2–3 and SPEC §1.
- Long: `low1<=VAL+buffer && close1>VAL && close1>open1` (mq5:~276). Short mirror
  (mq5:~292). Buffer = InpBufferTicks (designed addition, declared in SPEC §2).
- SL = ±InpAtrSlMult*ATR(InpAtrPeriod), TP = prior POC with 1:1.8 R:R fallback
  (matches card §3.4 / R:R 1:1.8). Break-even at +1R (Strategy_ManageOpenPosition).

### Dead-input check — ALL declared strategy inputs used beyond declaration
- InpValueAreaPct → target_vol (BuildPriorProfile)
- InpBufferTicks → buffer (Strategy_EntrySignal)
- InpAtrPeriod → QM_ATR in Strategy_EntrySignal SL
- InpAtrSlMult → SL distance
- InpSpreadAtrMult → Strategy_NoTradeFilter spread gate
- InpEnableBreakEven → Strategy_ManageOpenPosition guard
- InpBucketTicks → histogram bucket width (BuildPriorProfile)
No dead inputs.

### Framework conformity
- QM_Common chain via `#include <QM/QM_Common.mqh>`, QM_FrameworkInit/Shutdown wired.
- MAE hook: QM_FrameworkTrackOpenPositionMae() first in OnTick.
- Magic via MagicResolver: QM_FrameworkMagic() used throughout; registry rows
  410090000 (slot0/SP500) & 410090001 (slot1/NDX) = ea_id*10000+slot, both active.
- Risk: RISK_FIXED=1000 / RISK_PERCENT=0 in both backtest sets (governed generator,
  build_hash + card_defaults_source header present).
- News filter wired (QM_NewsAllowsTrade2 with temporal+compliance). Friday close +
  kill-switch wired.
- Bounded buffers: vol[] resized to n_buckets capped ≤500, all index reads guarded.
- Raw series reads (iHigh/iLow/iVolume/iOpen/iClose/Bars/iTime) each annotated
  `perf-allowed` and gated (profile build once/broker-day; entry reads single closed bar
  behind QM_IsNewBar). No ML, no invented commission/swap.

### Non-blocking notes (ordering priors — Q02 measures reality, no action required)
- Card §3.1 daily-loss circuit breaker (≥2.0%) not implemented at EA level; this is a
  framework/portfolio-layer concern, not an EA defect.
- Rollover blackout uses broker time (TimeCurrent) rather than literal GMT; acceptable.
- scan_cap=400 M5 bars is ample for one prior session; mid-day cold start is a benign
  edge (rebuild is gated to first new bar of each broker day).

## Setfiles
Both sets governed-generator format, environment=backtest, risk_mode FIXED, correct
per-symbol magic_slot (SP500=0, NDX=1).

# QM5_20195 NZDUSD/EURGBP Cointegration Q04 Priority

Date: 2026-08-11

Branch: `agents/board-advisor`

Status: the existing unique Q04 retry is pending at the head of the priority
queue; no work item was inserted and no tester was launched

## Outcome

The frozen 66-pair FX frontier was reconciled before mutation. Every
relationship is already mechanized, so another Strategy Card or EA would be a
duplicate. The preferred anchors also have no Q02 setup blocker:

- `QM5_12532` retains logical Q02 PASS and Q04 PASS, followed by Q05 FAIL.
- `QM5_12533` retains logical Q02 PASS, followed by Q04 FAIL.

The permitted fallback therefore advanced one existing FX card. The exact
pending Q04 retry for `QM5_20195_nzd-eurgbp-coint` was moved onto the priority
track in place:

| Field | Value |
|---|---|
| Pair | `NZDUSD.DWX` / `EURGBP.DWX` |
| Logical symbol | `QM5_20195_NZDUSD_EURGBP_COINTEGRATION_D1` |
| Work item | `b14b5edf-ffdd-4439-a2ed-dc4930d65029` |
| Phase | Q04 |
| State at verification | `pending`, unclaimed, attempt 0 |
| Priority | `priority_track=true` |
| Exact open Q04 rows after mutation | 1 |
| New work items | 0 |
| Queue position at verification | first in `queued_top` |

The mutation preserved the row's original `created_at` and `updated_at` so its
queue age was not reset. It added only mission-specific priority and audit
metadata to `payload_json`.

## Selection and source boundary

QM5_20195 is the twelfth-ranked relationship in the sign-aware replay of the
same 66-pair scan. It is the highest-ranked current cointegration continuation
with a pending Q04 row and no priority marker. The frozen evidence is:

| DEV net Sharpe | OOS net Sharpe | OOS return | OOS state changes | Fixed beta | Half-life |
|---:|---:|---:|---:|---:|---:|
| -0.082879 | 0.720866 | 8.085125% | 21 | -0.101296029 | 116.313 D1 bars |

Those weak DEV statistics, small companion weight, and long half-life remain
adverse evidence. This handoff does not claim that the sleeve is profitable or
authorize a parameter rescue.

The approved card derives the structural pair-trading method from Ernest P.
Chan, *Quantitative Trading* (Wiley, 2009), through the OWNER-ratified Tier-A
SRC02 extraction. Chan supplies the cointegration method and makes no
performance claim for NZDUSD/EURGBP. The pair selection is bound to the
OWNER-requested Darwinex scan.

## Structural and risk contract

- Evaluate only after a newly closed D1 bar.
- Trade the fixed residual
  `ln(NZDUSD) - (-0.101296029) * ln(EURGBP)`.
- Use a strictly prior 60-bar calibration window, enter outside `abs(z)=2.0`,
  and exit inside `abs(z)=0.5`.
- Apply a hard `2.0 * ATR(20, D1)` stop to each traded leg.
- Open and flatten both legs as one package; reject partial entry and flatten
  orphan legs.
- No adaptive beta, ML, grid, martingale, pyramiding, portfolio feedback, or
  banned indicator is present.
- `GBPUSD.DWX` and `EURUSD.DWX` are conversion-history-only dependencies and
  receive no strategy orders.
- The canonical backtest setfile remains `RISK_FIXED=1000`, `RISK_PERCENT=0`,
  and `PORTFOLIO_WEIGHT=1`.

The latest current-binary logical Q02 predecessor is
`5b51938b-04ab-47fa-9cf9-a833d7498984`. Its deterministic real-tick summary
is PASS with no OnInit failure: 72 tester trades, PF 1.18, USD 1,631.76 net,
and USD 3,551.82 drawdown over `2018.07.02` through `2022.12.31`. Q02 is setup
and baseline evidence only; Q04 remains the walk-forward economic judge.

The prior Q04 row `9a95577b-9872-4757-8966-3af71a463224` ended `INFRA_FAIL`
because the first two folds produced invalid zero-bar/empty-identity summaries.
The existing pending row is the already-created infrastructure retry; this
operation did not create another retry.

## Immutable artifact bindings

The pending Q04 payload hashes match the current repository files:

| Artifact | SHA-256 |
|---|---|
| EX5 | `909c1622e913fa77129aad70ece7529d7a073b9c421791c743ed86775a0b6cf6` |
| MQ5 | `23ce5192a6faf318345ca6ae5561a206c4cd15a6d24e7ccdf5cbfb4e41d74496` |
| Logical setfile | `585f398945b2aa596b1a68994c02eaba1a829d90fbd95a3f55ca6b87ba7d99a9` |
| Basket manifest | `a51f1083ee914d4c032da40233aedaa64118998f917e17f0966f365de1799c29` |
| Latest Q02 summary | `ebaaf115c4743f1eb588411f40785f45eacd6b0c99bd9d8f5136dc6a938506e3` |

## Guarded mutation and CPU ceiling

Immediately before mutation, `farmctl mt5-slots` sampled three running
factory terminals at `2026-08-11T10:41:31+00:00`: `T3`, `T7`, and `T8`.
Three was below the binding ceiling of seven. The observed `T_Live` and FTMO
processes were excluded from the factory count and were not controlled.

The database mutation ran under the canonical factory mutation lock with an
exact pending/unclaimed/attempt-zero precondition and an optimistic payload
comparison. The online database, SQLite backup, and post-mutation database all
returned `PRAGMA quick_check = ok`:

`D:/QM/strategy_farm/state/backups/farm_state_pre_qm5_20195_q04_priority_20260811T104226Z.sqlite`

The total work-item count remained exactly 106,910 before and after. A
post-check reported 1,130 pending and five active rows, and placed QM5_20195
first in `queued_top`. Normal paced workers own any later claim and execution.

Machine-readable evidence:
`artifacts/qm5_20195_fx_q04_priority_20260811T104226Z_board_advisor.json`.

## Safety boundary

- No Strategy Card, EA source, EX5, setfile, manifest, registry, or magic row
  was changed.
- No queue row was inserted, removed, claimed, or dispatched.
- No smoke test, pipeline phase, terminal reservation, or MT5 process was
  launched, stopped, or controlled.
- No portfolio-admission, portfolio KPI, or Q08-contribution path was touched.
- No `T_Live` manifest, live setfile, terminal state, or AutoTrading state was
  touched.
- Existing unrelated dirty-worktree changes were left untouched.

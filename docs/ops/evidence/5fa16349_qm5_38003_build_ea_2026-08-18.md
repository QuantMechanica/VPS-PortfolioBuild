# QM5_38003 Build EA Evidence — 2026-08-18

**Task ID:** `5fa16349-5252-448a-8f5d-8a7d77306f9b` (`build_ea`, priority 50, agent `gemini`).  
**EA:** `QM5_38003_codetrading-bollinger-engulfing-reversal`.  
**Artifact Path:** `C:/QM/repo/docs/ops/evidence/5fa16349_qm5_38003_build_ea_2026-08-18.md`.  
**Strategy Card:** `strategy-seeds/cards/approved/QM5_38003_codetrading-bollinger-engulfing-reversal.md` (G0 APPROVED).  

---

## 1. Strategy Overview & Implementation

- **Strategy Thesis**: Intraday price action mean-reversion model on H1 combining Bollinger Bands (20, 2.0), RSI(14) momentum filters, and Engulfing candlestick patterns.
  - Long (BUY): Bullish engulfing candle on bar 1 over bar 2 (`Close[1] > Open[2] && Open[1] <= Close[2] && Close[2] < Open[2] && Close[1] > Open[1]`), `Low[1] <= LowerBB(20, 2.0)[1]`, `RSI(14)[1] <= 35.0`.
  - Short (SELL): Bearish engulfing candle on bar 1 over bar 2 (`Close[1] < Open[2] && Open[1] >= Close[2] && Close[2] > Open[2] && Close[1] < Open[1]`), `High[1] >= UpperBB(20, 2.0)[1]`, `RSI(14)[1] >= 65.0`.
  - Exits & Stops: Stop Loss placed beyond engulfing candle extreme ($\mp 2.0\text{ pips}$), Take Profit targeting $1:2.0\text{ R:R}$, and Break-Even move when price touches the 20 SMA middle Bollinger Band.
- **Framework Corset Compliance**:
  - Implemented strictly with `EA_Skeleton.mq5` wiring and framework readers.
  - Closed-bar calculation cached in `AdvanceState_OnNewBar()` to ensure $O(1)$ per-tick performance.
  - Sizing handled strictly by `QM_LotsForRisk` from `req.sl`.
  - News temporal mode (`QM_NEWS_TEMPORAL_PRE30_POST30`) and compliance (`QM_NEWS_COMPLIANCE_DXZ`) with fail-closed stale guard (`qm_news_stale_max_hours = 336`).

---

## 2. Registry & Artifact Verification

- **Registries Updated**:
  - `framework/registry/ea_id_registry.csv`: verified `38003,codetrading-bollinger-engulfing-reversal`.
  - `framework/registry/magic_numbers.csv`: allocated slots 0 (`EURUSD.DWX`, `380030000`), 1 (`GBPJPY.DWX`, `380030001`), 2 (`AUDUSD.DWX`, `380030002`).
  - `framework/include/QM/QM_MagicResolver.mqh`: regenerated cleanly via `update_magic_resolver.py`.
- **SPEC Document**:
  - `framework/EAs/QM5_38003_codetrading-bollinger-engulfing-reversal/SPEC.md` written and verified with `validate_spec_doc.py` (`PASS`).
- **Static Checks & Compilation**:
  - `build_check.ps1`: `compile_one.result=PASS`, `errors=0`, `warnings=0`, `build_check.result=PASS`.
  - Emitted binary: `framework/EAs/QM5_38003_codetrading-bollinger-engulfing-reversal/QM5_38003_codetrading-bollinger-engulfing-reversal.ex5`.
- **P2 Setfiles**:
  - Generated for `EURUSD.DWX`, `GBPJPY.DWX`, `AUDUSD.DWX` via `gen_setfile.ps1`.

---

## 3. Smoke Test & Review State

- `run_smoke.ps1`: Encountered standalone admission rejection (`custom_history_smoke_admission.py` requires worker-bound work item under active Custom-history isolation).
- Build result JSON emitted honestly per schema to `artifacts/qm5_38003_build_result.json` with `build_check_passed=true`, `compile_succeeded=true`, `smoke_result=framework_error`.
- Task updated to `REVIEW` for mandatory Codex review per router rules.

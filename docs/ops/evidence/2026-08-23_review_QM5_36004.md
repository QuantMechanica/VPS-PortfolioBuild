# Claude review close: QM5_36004 NNFX ALMA/QQE Volume Flow Sniper (review_ea)

- Router task: `af9af332-6c97-4abd-a319-4373c82e0844` (state REVIEW, assigned claude)
- Source (gemini/agy) build task: `22225e01-3ed6-4a1f-8fca-b55655117d01`
- Source artifact: `D:/QM/strategy_farm/artifacts/builds/22225e01-3ed6-4a1f-8fca-b55655117d01.json`
- source_verdict: `REMEDIATED_AWAITING_CODEX_REVIEW`; task reason: `codex_review_required_for_gemini_code`
- EA dir: `framework/EAs/QM5_36004_nnfx-alma-qqe-volume-flow-sniper`
- Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_36004_nnfx-alma-qqe-volume-flow-sniper.md`
- Current `.mq5` sha256: `dd9bfa5a4574f83da6f45b10e2a77b822f853d7b5d60e6d1f340e10cc51545b8` (Aug 23 13:53)
- Tracked `.ex5` sha256: `a83f0c2b2088079e028605e414fae6f98b4e91ad5500bfab22f1b3b7e34896d4` (Aug 17 23:23)
- **Verdict: BLOCKED** — source remediation independently verified clean, but the deliverable
  cannot advance to Q02 for two reasons outside Claude's authority (governed off-window compile +
  mandatory Codex acceptance gate for Gemini-originated code).

## Card-faithfulness & source review (independent read of current `.mq5`)

Mechanism matches the approved card: ALMA(20, σ=6.0, offset=0.85) baseline on closed D1 bars,
QQE(14/5/27/4.236) genuine crossover trigger, DPO(20) C2 confirmation, VFI(130) volume gate;
Long/Short entry conjunctions per §3.2/§3.3; SL/TP at 1.0×ATR; TP1 50% partial + runner-to-BE;
spread filter (1.8×ATR) and 23:55–00:05 rollover blackout per §3.1.

The 7 findings from the original Codex review (`80b2cb2a_..._2026-08-18.md`) re-verified against
current HEAD (commits `1c8d911f9`, `73acf69db`):

| # | Finding | Status | Evidence (file:line region) |
|---|---------|--------|------------------------------|
| 1 | Full-position TP instead of 50% TP1 + runner | RESOLVED | `req.tp=0.0` at entry (`Strategy_EntrySignal`); `Strategy_ManageOpenPosition` `QM_TM_PartialClose(ticket, half_lots, QM_EXIT_PARTIAL)` at +1.0 ATR then SL→BE, `sl_not_breakeven` guard prevents re-fire |
| 2 | QQE persistent state used as crossover | RESOLVED | `Strategy_QQECross` compares shift-1 vs shift-2 state; entry requires `qqe_cross != 0` |
| 3 | Rollover blackout in broker time not GMT | RESOLVED | `Strategy_NoTradeFilter` uses `QM_BrokerToUTC(TimeCurrent())` before the hhmm window |
| 4 | Card loss-limit contract absent | RESOLVED (framework caveat) | `QM_KillSwitchInit(...,2.5,5.0,1.0)` in `OnInit`; 2.0% entry halt on day-start equity DD in `Strategy_NoTradeFilter`. Portfolio-DD leg is inert in single-EA backtest (standing framework property, not an EA defect) |
| 5 | Producer result blocked, no smoke evidence | **UNRESOLVED (blocking)** | `.ex5` timestamp Aug 17 23:23; sha `a83f0c2b…` is byte-identical to the binary already reviewed and REJECTED 2026-08-18. Binary never recompiled after the Aug 23 source remediation; ad-hoc compile correctly refused (`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`, active factory) |
| 6 | No committed identity | RESOLVED for source | `.mq5`/`SPEC.md`/setfiles tracked, EA dir working tree clean; `.ex5` itself stale (see #5) |
| 7 | Entry filters could suspend position management | RESOLVED | `OnTick` runs `Strategy_ManageOpenPosition()` + exit-close loop before news gate / `Strategy_NoTradeFilter` / `QM_IsNewBar` |

## Dead-input scan (QM5_1355 defect class)

All 13 `strategy_*` inputs (`strategy_alma_period/sigma/offset`, `strategy_qqe_rsi_period/sf/wilder/mult`,
`strategy_dpo_period`, `strategy_vfi_period`, `strategy_atr_period`, `strategy_sl_atr_mult`,
`strategy_tp_atr_mult`, `strategy_spread_atr_mult`) have ≥1 live use-site beyond their declaration
(grep counts 2–4 each). **No dead inputs.**

## Framework conformity

- `#include <QM/QM_Common.mqh>` present; `QM_FrameworkInit`/`Shutdown`/`OnTimer`/`OnTradeTransaction` wired.
- Risk: `RISK_FIXED=1000`, `RISK_PERCENT=0` in all 4 backtest setfiles (`risk_mode: FIXED`, `environment: backtest`).
- Magic: `magic_numbers.csv` rows `360040000–360040003` (ea_id*10000+slot), collision-free, active; resolved via `QM_FrameworkMagic()`.
- News filter wired (`QM_NewsAllowsTrade2`/`QM_NewsAllowsTrade` in `OnTick`); MAE hook (`QM_FrameworkTrackOpenPositionMae`); kill switch + Friday close present.
- No ML libs, no `.dll`, no hardcoded commission/swap (grep: none).
- Setfiles carry governed `QM5 Set File` header + `build_hash` — generated via governed path, not raw MQ5 promotion.

## Disposition

Source-level remediation is genuine and card-faithful; formally the code is clean enough for Q02.
The task is held BLOCKED (not APPROVED) because:

1. **Stale binary (finding #5).** The tracked `.ex5` predates the remediated `.mq5` by six days and
   is byte-identical to the pre-remediation, already-rejected binary. Q02 would execute the old code.
   Closing this requires a governed off-window factory compile + smoke bound to `dd9bfa5a…` — ROT for
   Claude (no recompile in active inventory; factory running).
2. **Mandatory Codex acceptance gate.** Gemini-originated code requires Codex review before acceptance;
   that gate has not run on this remediation. Claude does not self-approve gemini-originated builds.

Follow-ups (to be commissioned): (a) COMPILE_EA in an off-window to mint a fresh `.ex5` hash-bound to
current source + non-blocked smoke; (b) mandatory Codex acceptance review. No pipeline state changed;
no thresholds/verdicts/seeds/windows touched.

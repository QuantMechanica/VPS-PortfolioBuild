# Review — QM5_1408 classical-bull-flag-continuation-h1 (rework)

**Router task:** `116de4eb-f236-4302-9622-dbfe69c49c1d` (review_ea, assigned codex, state REVIEW)
**Reviewer:** Claude (review lane), 2026-08-24
**EA:** `framework/EAs/QM5_1408_classical-bull-flag-continuation-h1/`
**Origin:** gemini/agy build (`7dccf57e`), RECYCLE'd 2026-08-22 (4 strict-hardening + 4 semantic defects), reworked by Codex commit `97642793b` "rework(1408): restore bull flag pending lifecycle".
**Approved card:** `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1408_classical-bull-flag-continuation-h1.md` (g0_status APPROVED, strategy_id 6e967762).
**Verdict: APPROVED (code clean for Q02) — with a mandatory governed-recompile precondition (see Blocker).**

## Method
Read the reworked `.mq5`, SPEC.md, card, 14 setfiles, registries; re-ran static gates read-only. Did not enqueue, compile, toggle factory, or touch verdicts.

## Card-faithfulness (all confirmed against the current source, HEAD-clean)
- Entry stays `QM_BUY_STOP` and is fully initialized — `mq5:694-768`, dispatched via `QM_TM_OpenPosition` (`:1033-1037`). The RECYCLE-critical "market-entry instead of BUY-STOP" defect is gone. Price = `upper_TL + 0.4 ATR`, valid 8 H1 bars via `expiration_seconds` (`:747-764`).
- All 8 card gates present in `Strategy_FindSetup` (`:482-667`): pole move ≥4 ATR, LR slope ≥0.20/ATR, pullback ≤0.35, volume gate (skippable), flag counter-slope [-0.10,-0.005]/ATR, retracement ≤0.50, volume contraction, containment ≥80% inside two parallel pivot-fitted trendlines requiring **≥2 high AND ≥2 low pivots** with the parallel-tolerance check (`:604-618`). No regression/high-low fallback (the old defect).
- SL = `lower_TL - 0.3 ATR` capped at 2.5 ATR (`:644-647`); TP = measured move `entry + pole_height` (`:648-649`); TP1 partial 50% + break-even (`:819-855`); pattern-failure exit ≤6 bars using an advancing persisted-anchor line (`:858-905`); time-stop 24 bars (`:906-911`).
- Macro-bias SMA(200,H4) rising AND H1 close > SMA (`:348-369`); spread filter; reuse guard 12 bars (`:298-321`). SMA direction corrected (newer `[1]` vs older `[0]`).

## Framework conformity
- `#include <QM/QM_Common.mqh>`; `QM_FrameworkInit/Shutdown/OnTimer/OnTradeTransaction`; `OnTester → QM_DefaultObjective`.
- **MAE hook first line of OnTick** (`:993`) — RECYCLE defect EA_Q08_MAE_HOOK_MISSING fixed.
- Management + exits ordered **before** the entry-only news block (`:1002-1018`) — RECYCLE ordering defect fixed.
- Magic via `QM_FrameworkMagic()` / MagicResolver throughout; 14 magic rows present (`magic_numbers.csv`), ea_id row active (`ea_id_registry.csv:291`).
- News wired: `QM_NewsInWindow(±180/180 min = ±3 H1 bars)` + `QM_NewsComplianceAllows` (`:669-687`).
- All indicator reads bounded (CopyBuffer with result-count **and** ArraySize guards: `:323-336`, `:354-359`); `CopyRates` calls each carry a `perf-allowed` justification.
- No raw `CTrade` (partial/moveSL/close all via `QM_TM_*`); no ML; RISK_FIXED=1000 / RISK_PERCENT=0 in all 14 backtest setfiles; no invented commission/swap.
- **Dead-input census:** every one of the 31 `strategy_*` inputs has ≥1 executable use-site (grep count ≥2 each). No dead inputs.

## Static-gate evidence (independently re-run, current source)
- `build_gate_hardening.py --ea-label QM5_1408...` → `"failures": []`, files_scanned 1. All four RECYCLE strict defects (MAE hook, uninitialized request, 2× unbounded buffer) cleared.
- `pytest test_qm5_1408_rework_static.py` → 4 passed.
- (rework doc also records validate_build_guardrails PASS, validate_spec_doc PASS, skill_build_ea_guard ok.)
- Working tree clean vs HEAD for the `.mq5` (no uncommitted drift).

## Blocker before Q02 dispatch (not a code defect — build-lane follow-on)
The committed `.ex5` is dated **2026-08-22 12:59 — the pre-rework build** (SHA `d4a41c27…`); the 14 setfiles bind to that old `build_hash` (`38b9ddf1…`). The governed compile interlock correctly **refused** an ad-hoc recompile while terminal64 is live. A fresh EX5 matching the reworked source, plus setfile build_hash rebind, must be produced in an OFF / COMPILE_EA window **before** Q02 — otherwise the stale-EX5 guard will (correctly) void any run. Recompile is ROT and out of scope for this review lane. This is the standard reworked-EA next step, not a code defect, so the review closes APPROVED; the build pipeline owns EX5 production.

## Verdict
**APPROVED** — reworked EA is card-faithful, framework-conformant, all RECYCLE defects fixed and independently confirmed, no dead inputs, static gates green. Precondition for Q02: governed recompile + setfile rebind (build lane).

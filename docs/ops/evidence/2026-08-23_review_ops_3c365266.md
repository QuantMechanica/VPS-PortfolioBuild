# Review — SP-D10 Placeholder Cleanup (ops_issue)

Date: 2026-08-23
Reviewer: Claude (review lane)
Router task: `3c365266-68b6-4474-87ac-9049af52c7ec` (`SP-D10`, priority 35, zone GELB)
Delivered artifact: `docs/ops/evidence/2026-08-23_sp_d10_placeholder_cleanup_dependency_hold.md` (commit `11997d016`)

## Verdict

**BLOCKED** — dependency hold upheld. The task cannot proceed without an OWNER
deletion authorization and a *passed* (not census-only) dependency dry-run.
The worker correctly declined to remove anything and produced no state change.

## What the task asks

SP-D10: remove placeholder empty folders + the completed DST prompt, but only
after (acceptance) "bestandenem Dry-Run + OWNER-Freigabe" and (payload) a
"dokumentierten Dependency-Check + OWNER-Freigabe". `depends_on: SP-D9`.
hard_constraint: "historische Evidenz nicht mit operativer Wahrheit verwechseln".

## Verification (independent, read-only)

- **SP-D9 original `2f36c28c-6430-4a55-8af7-8d213f372cc6` = BLOCKED** —
  confirmed against `agent_tasks` (DB mode=ro). Blocked on the
  Manifest-first sequencing.
- **SP-D9 neu `c65592c7-c8f4-4579-baf6-0ec7d9429319` = REVIEW, census-only** —
  confirmed against `agent_tasks`; verdict is `DEPENDENCY_CENSUS_COMPLETE`
  (130/130 rows, report only), which is a completed *count*, not a *passed*
  dry-run authorizing deletion.
- **OWNER-DEC-G-RETENTION = Manifest-first, purge rejected** —
  `decisions/2026-08-22_owner_decisions_evening_batch.md` §3 (lines 50–61):
  "No deletion on `G:` before a content-addressed corpus manifest and a
  dependency dry-run exist … The radical immediate purge … is rejected."
  Order of operations: manifest → dependency/retention dry-run → *only then*
  any retention proposal.
- **No OWNER approval record for placeholder removal / ROT-9 deletion** —
  grep over `decisions/` returns no ratification of a placeholder cleanup or
  a ROT-9 deletion authority for the folders/DST prompt SP-D10 names.
- **No state change** — commit `11997d016` is `+63` lines, one added evidence
  file, zero deletions. Nothing on disk, in `G:`, or the DST prompt was
  touched.

## Assessment

The delivered dependency-hold is card-faithful to the task's own acceptance
criteria and to OWNER-DEC-G-RETENTION. Deleting "empty" folders or a "done"
DST prompt now would violate the Manifest-first sequencing that decision was
written to enforce — an empty folder is not verifiable as inert until the
dependency census is a *pass*, not just a completed count, and OWNER has not
signed off. Both gates are open, so BLOCKED (needs OWNER + a passed dry-run)
is the correct terminal state rather than APPROVED or RECYCLE.

## Deterministic resume conditions

Re-route SP-D10 only once BOTH hold: (1) the retention dry-run reaches a
genuine pass with ROT-9 deletion authority ratified (not census-only), and
(2) OWNER explicitly signs off on removing the specific empty folders / DST
prompt this task names.

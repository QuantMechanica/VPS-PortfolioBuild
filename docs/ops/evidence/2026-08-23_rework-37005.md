# rework-37005 review-fix evidence

Date: 2026-08-24
Router task: `8d44760b-92f8-40d2-a54e-da5317ad4577`
EA: `QM5_37005_chan-bollinger-adx-mean-reversion`
Card: `strategy-seeds/cards/approved/QM5_37005_chan-bollinger-adx-mean-reversion.md` (`g0_status: APPROVED`)
Disposition: source repair complete; fresh binary and smoke evidence remain blocked by the governed compile interlock.

## Work order

The review findings are recorded in:

- `docs/ops/evidence/8d44760b_qm5_37005_gemini_build_codex_review_2026-08-18.md`
- `docs/ops/evidence/cf69bca5_qm5_37005_gemini_codex_review_2026-08-23.json`
- producer artifact `C:/QM/repo/artifacts/builds/98fb1997-3c5d-4bfa-af26-6f46dd7c8a1b.json`

The findings were: unauthorized fixed-1R TP fallback when the Bollinger midline
was invalid; absent 2.0% daily realized-loss entry halt, 2.5% daily hard stop,
and 5.0% total drawdown stop; and no durable current build/smoke identity.

## What changed

1. `framework/EAs/QM5_37005_chan-bollinger-adx-mean-reversion/QM5_37005_chan-bollinger-adx-mean-reversion.mq5`

   - Lines 54-57 declare the card's 2.0% / 2.5% / 5.0% loss controls and 3-tick slippage ceiling as fixed strategy inputs.
   - Lines 76-90 bound every strategy input and fail initialization closed outside the approved ranges/values.
   - Lines 111-146 reconstruct account-wide broker-day realized P&L from deal history. History failure leaves the entry halt armed.
   - Lines 149-200 clear cached state before fallible reads and replace raw `CopyRates` access with pooled, bounded `QM_SMA` closed-bar OHLC reads.
   - Lines 268-283 reject long/short setups whose only card-authorized SMA-midline TP is not beyond the quote. No replacement 1R target remains.
   - Lines 342-360 wire the 3-tick entry tolerance, H1 execution contract, framework magic, and `QM_KillSwitchInit(..., 2.5, 5.0, ...)`.
   - Lines 378-406 preserve the MAE/kill-switch chain and keep management/exits reachable before entry-only no-trade filters.
   - Line 443 refreshes the daily realized-loss cache after trade transactions.

2. `framework/EAs/QM5_37005_chan-bollinger-adx-mean-reversion/sets/*.set`

   - Lines 32-35 bind the four new fixed card defaults in each of the three H1 backtest presets.
   - All presets retain `RISK_FIXED=1000`, `RISK_PERCENT=0`, and their existing magic slots 0/1/2.

3. `framework/EAs/QM5_37005_chan-bollinger-adx-mean-reversion/SPEC.md`

   - Lines 17-20 document fail-closed invalid-midline behavior.
   - Lines 35-38 document the loss and slippage controls.
   - The revision table records this review repair.

4. `tools/strategy_farm/tests/test_qm5_37005_review_rework_static.py`

   - Lines 38-58 lock the no-fallback midline rule and the three loss-limit mechanisms.
   - Lines 60-93 enumerate every strategy input, require multiple use sites, and verify range/value bounds.
   - Lines 96-141 lock the QM common chain, magic/MAE wiring, execution contract, pooled OHLC reads, and forbidden raw-series/ML calls.
   - Lines 144-166 lock the three-symbol backtest risk mode and fixed card defaults.

## Input-use census

Every declared strategy input has more than one source occurrence (declaration
plus validation/consumption):

```text
strategy_bb_period=6
strategy_bb_dev=6
strategy_adx_period=4
strategy_max_adx=4
strategy_atr_period=4
strategy_sl_atr_mult=6
strategy_spread_atr_mult=4
strategy_max_spread_points=5
strategy_daily_loss_limit_pct=3
strategy_daily_drawdown_hard_stop_pct=3
strategy_total_drawdown_stop_pct=3
strategy_max_slippage_ticks=3
```

## Registry evidence

- `framework/registry/ea_id_registry.csv:4468`: active EA id/slug row.
- `framework/registry/magic_numbers.csv:17468-17470`: active slots 0-2 for EURUSD.DWX, USDCAD.DWX, and AUDCAD.DWX.
- The generated `QM_MagicResolver.mqh` contains magics 370050000, 370050001, and 370050002.
- No registry or resolver file was changed.

## Validation evidence

### Passing scoped checks

```text
python -m pytest -q \
  tools/strategy_farm/tests/test_qm5_37005_review_rework_static.py \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_d2_loss_limit_matching_and_mismatching_fixtures

..... [100%]
5 passed in 1.33s
```

```text
python tools/strategy_farm/build_gate_hardening.py \
  --repo-root . --ea-label QM5_37005_chan-bollinger-adx-mean-reversion

failures=[]; warnings=[]; files_scanned=1
D2 declared={daily_entry_halt:2.0,daily_hard_stop:2.5,total_drawdown_stop:5.0}; failures=0
D3-D11, D13-D18 failures=0
```

```text
python framework/scripts/validate_spec_doc.py \
  framework/EAs/QM5_37005_chan-bollinger-adx-mean-reversion

PASS QM5_37005_chan-bollinger-adx-mean-reversion
Summary: 1 PASS, 0 FAIL (of 1)
```

```text
python tools/strategy_farm/validate_symbol_scope.py \
  --ea-label QM5_37005_chan-bollinger-adx-mean-reversion --json --fail-on-leak

verdict=SINGLE_SYMBOL_OK; n_violations=0
```

### Complete Python suite

```text
python -m pytest -q tools/strategy_farm/tests

4 failed, 4649 passed, 4 skipped, 42 subtests passed in 2004.15s (0:33:24)
```

The four failures are outside this ticket and reproduce repository/runtime
drift unrelated to EA 37005:

- `test_build_gate_hardening.py::test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` — seven failures in QM5_411xx sources, first in QM5_41134.
- Two `test_execution_contract_lint.py` cases — expected news-calendar hashes/coverage end 2026-08-21, observed refreshed hashes/coverage end 2026-08-29.
- `test_opt_census_dispatch.py::test_opt_census_ranks_tier6_not_priority` — expected active-contract rank 7, observed 6.

None references EA 37005 or a file changed by this ticket.

### Compile/build-check interlock (not a PASS)

The required strict scoped compile was attempted:

```text
pwsh -NoProfile -File framework/scripts/compile_one.ps1 \
  -EAPath framework/EAs/QM5_37005_chan-bollinger-adx-mean-reversion/QM5_37005_chan-bollinger-adx-mean-reversion.mq5 \
  -Strict

result=FAIL
reason_class=INCLUDE_MIRROR_REFUSED
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
```

Durable receipt: `D:/QM/reports/compile/20260824_100931/summary.csv`.
No compile log was created because the guard refused before MetaEditor ran.
The separately scoped `build_check.ps1 -EALabel ... -Strict -SkipCompile`
attempt was refused by the same pre-check before a framework report was written.

The sanctioned non-trading `COMPILE_EA` route was then evaluated from this
deliberate worktree using the CLI's documented `QM_ALLOW_NONCANONICAL=1`
checkout override. No work item was enqueued: the classifier refused
`EX5_ALREADY_PRESENT` and
`BOUND_SETFILE_HASH_EXISTS`, and EA 37005 is not covered by an exact governed
force/source-repair authority. No compile/build safety guard was bypassed, no
factory state was changed, and no backtest was enqueued.

Current identity, deliberately not represented as a fresh compiled pair:

```text
MQ5 sha256 f78ba45d8bcc6c02cf84a0a6181989d1f98027a53805031b4ced166a116243c8
EX5 sha256 24552679e230ad968363a8afd13972d880a422ea74eb279869f8fdbc31ab7709 (pre-repair binary)
```

Because there is no fresh binary, no smoke was run against the stale EX5 and
no runtime/trade-generation claim is made. Closing the binary/smoke finding
requires an OWNER/governed exact rebuild authority or a permitted quiescent
compile window, followed by the standard one-symbol H1 2024 smoke.

## Safety boundaries

- No gate threshold, criterion, manifest, registry, verdict row, or magic allocation changed.
- No backtest was enqueued or deleted.
- Factory state and AutoTrading were not toggled.
- `C:/QM/mt5/T_Live` was not touched.
- Pre-existing EA 11537 worktree changes were not staged or modified.

## Rollback

Revert the ticket commit with `git revert <rework-37005-commit>`. This restores
the EA source, spec, presets, test, and this evidence file. The router task
state transition is external append-only coordination state and is not changed
by Git rollback. No backtest/verdict/factory/live rollback is required.

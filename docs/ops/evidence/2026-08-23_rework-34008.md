# rework-34008 — review-fix evidence

- Router task: `72b63c06-7749-4ac4-8276-fcf7bdc02dc4`
- EA: `QM5_34008_multicurrency-basket-dispersion-hedger`
- Worktree: `C:/QM/worktrees/rework-slot-6`
- Reworked: 2026-08-24 (Europe/Berlin)
- Intended disposition: `BLOCKED` — approved-card sizing defect

## Work order and preflight

The original review found that the old implementation did not coordinate the required market-neutral extrema package or the combined +/-1.5% exit (`docs/ops/evidence/72b63c06_qm5_34008_gemini_build_codex_review_2026-08-17.md:18-39`). A later review confirmed that the source remediation existed but the committed EX5 and receipt were stale (`docs/ops/evidence/2026-08-23_review_QM5_34008.md:9-39`). The review addendum identified the remaining source defect: seven tester hosts could derive and trade the same package because there was no primary host/slot guard (`docs/ops/evidence/2026-08-23_review_ea_34008_addendum_host_slot_guard.md:10-16,34-39,56-58`).

While the task was in progress, a concurrent review moved it back to `REVIEW` and wrote `C:/QM/repo/docs/ops/evidence/72b63c06_qm5_34008_recycle1_codex_review_2026-08-24.json`. That review independently confirmed the extrema package and aggregate exits, then added execution-contract, lifecycle-ordering, and sizing findings. The first two are repaired below. The sizing finding exposed a dispositive upstream card defect and is the reason for `BLOCKED` rather than resubmission.

Preflight evidence:

- Approved durable card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_34008_multicurrency-basket-dispersion-hedger.md:10` (`g0_status: APPROVED`); its target universe and primary host are at lines 33-34, and its GMT rollover contract is at line 85.
- EA registry: `framework/registry/ea_id_registry.csv:4447` is the active `34008` allocation.
- Magic registry: `framework/registry/magic_numbers.csv:17393-17399` already contains the active slots 0-6 and magics `340080000` through `340080006`. No registry or resolver file was changed.

## Repairable findings completed

### Card-faithful package ownership and execution

- `framework/EAs/QM5_34008_multicurrency-basket-dispersion-hedger/QM5_34008_multicurrency-basket-dispersion-hedger.mq5:56,186-187` makes EURUSD/slot 0 the sole package owner. The other six registered hosts are deliberate no-signal lanes, recorded in `basket_manifest.json:3,15` and `SPEC.md:49`.
- The existing extrema mechanism remains the only entry mechanism: it derives one min-deviation BUY and one max-deviation SELL, opens both through `QM_BasketOpenPosition` (`.mq5:334,338`), and immediately unwinds the first leg if the second leg fails (`.mq5:340`).
- Package management remains coordinated: an orphan leg is flattened (`.mq5:347-352`) and the combined package closes at the card's +1.5%/-1.5% portfolio-PnL bounds (`.mq5:356-369`). Management and exits execute before entry-only filters (`.mq5:444-449`).

### Framework and card controls

- Card loss limits are explicit inputs at `.mq5:24-26`, validated at `.mq5:141-149`, enforced by the daily realized-loss/total-drawdown checks at `.mq5:154-174`, and passed to the framework kill switch at `.mq5:401-404`.
- The 23:55-00:05 rollover comparison now converts broker time with `QM_BrokerToUTC` (`.mq5:74`).
- The card's three-trade-tick slippage ceiling is an input at `.mq5:49`, converted per symbol to MT5 deviation points at `.mq5:85-91`, and applied to both basket legs at `.mq5:306-307,334,338`.
- All seven allocated symbol magics are registered through `QM_FrameworkRegisterMagicSymbol` (`.mq5:417`); the framework MAE hook remains at `.mq5:442`.
- The only raw series reads are the two closed-bar `iClose` calls at `.mq5:231-232`; both carry same-line `perf-allowed` justification and run behind the new-bar gate. No ML path was added.
- All seven backtest sets retain `RISK_FIXED=1000` and `RISK_PERCENT=0`; the source already uses the framework's tester/live risk mode contract. Each set now seals the three card loss limits, three-tick slippage, and the current source hash.
- The EA now declares a fail-closed H1 execution contract (`.mq5:397-401`) and uses the explicit `QM_IsNewBar(_Symbol, PERIOD_H1)` gate (`.mq5:475`).
- Orphan/package exit management runs before the framework Friday-close override (`.mq5:447-459`). Because all seven magics are registered, framework Friday close owns the complete package rather than only the chart-host magic.

Declared strategy-input use-site census (source occurrences, declaration included):

| Input | Occurrences |
|---|---:|
| `strategy_daily_loss_halt_pct` | 3 |
| `strategy_daily_hard_stop_pct` | 3 |
| `strategy_total_dd_stop_pct` | 4 |
| `strategy_lookback_hours` | 5 |
| `strategy_dispersion_dev` | 4 |
| `strategy_target_profit_pct` | 3 |
| `strategy_hard_stop_loss_pct` | 3 |
| `strategy_atr_period` | 5 |
| `strategy_spread_atr_mult` | 3 |
| `strategy_max_slippage_ticks` | 4 |

`tools/strategy_farm/tests/test_qm5_34008_review_rework_static.py:24-125` locks the host-owner, extrema package, combined exits, explicit H1 execution contract, lifecycle ordering, framework hooks, all declared-input use sites, setfile risk contract, and source/binary receipt binding.

## Blocking approved-card defect

The approved card cannot support a card-faithful implementation of its sizing rule:

- It requires position sizing from an exact stop-loss distance at `D:/QM/strategy_farm/artifacts/cards_approved/QM5_34008_multicurrency-basket-dispersion-hedger.md:103-107`.
- It declares `InpPackageRisk=0.50` as the total combined package-risk budget at line 163 and an aggregate -1.5% portfolio-PnL cutoff at line 97.
- It never defines a per-leg stop distance or a deterministic rule allocating package risk to the two extrema legs. Its sample delegates this missing mechanic to undefined `CalculateStopLoss` calls at lines 189 and 198.
- Current source evidence makes the defect visible: `.mq5:293-301` sizes from an invented 1.5-ATR surrogate, while `.mq5:315,326` submits both legs with `sl=0.0`. Replacing that surrogate would require inventing a stop distance or allocation formula, which is prohibited.

OWNER/Research must amend and re-approve the Strategy Card with either exact per-leg stop distances or a deterministic package-risk-to-leg allocation/sizing formula. Until then, the EA must not be submitted to another build review or downstream gate.

## Compile and binding evidence

- Final compile log: `D:/QM/reports/compile/20260824_rework34008_slot6_final/QM5_34008_multicurrency-basket-dispersion-hedger.compile.log`; terminal result is `0 errors, 0 warnings, 6176 ms elapsed`.
- MQ5 SHA-256: `7d9bf12a6f8fc1e55f24a361b22b7f0f196eac8b66082f22c948c5d845bde4cf`.
- EX5 SHA-256: `98257aa6624a5c0be70a8e8e40f2cb138bc5c621b87b36af5169e753f5174a86`; size 400,204 bytes.
- `artifacts/qm5_34008_build_result.json` binds those paths, hashes, compile result, and all seven setfiles.
- The replaced 389,242-byte EX5 remains recoverable at `D:/QM/reports/compile/20260824_rework34008_slot6/pre_rework_QM5_34008_multicurrency-basket-dispersion-hedger.ex5` (SHA-256 `85962ade2c7c4251a188bd38d8f269df57d8cff0f1f534035db60dc541661186`).

The required label-scoped monolithic `build_check.ps1 -EALabel QM5_34008_multicurrency-basket-dispersion-hedger` was attempted. Its live-factory interlock stopped execution before report generation with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`. The receipt records `build_check_passed=false`, `build_check_component_checks_passed=true`, the refusal reason, and a null report path; no PASS is claimed for the monolithic wrapper. The canonical compile queue was also not used because this is a noncanonical worktree and would target `C:/QM/repo`. No compile work item or backtest was enqueued. The binary was rebuilt with hidden T7 MetaEditor after confirming T7 was idle and its framework include/resolver inputs matched the worktree for this EA; `C:/QM/mt5/T_Live` was not read or changed.

## Validation evidence

Commands were run from the worktree root:

```text
python -m pytest tools/strategy_farm/tests/test_qm5_34008_review_rework_static.py -q
5 passed in 0.68s

python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py -q -k "d2_loss_limit or d4_management or d5_raw or d7_framework or d10_indicator or d11_canonical"
6 passed, 23 deselected in 0.85s

python -m pytest tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_validate_symbol_scope.py -q
23 passed in 5.22s

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_34008_multicurrency-basket-dispersion-hedger
files_scanned=1; failures=[]; warnings=[]; symbols_observed=7

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_34008_multicurrency-basket-dispersion-hedger
Summary: 1 PASS, 0 FAIL (of 1)

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_34008_multicurrency-basket-dispersion-hedger --fail-on-leak --json
verdict=BASKET_OK; n_violations=0

python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 framework/EAs/QM5_34008_multicurrency-basket-dispersion-hedger
verdict=PASS; files_checked=8; findings=[]

python tools/strategy_farm/raw_mq5_quarantine.py check --source-path framework/EAs/QM5_34008_multicurrency-basket-dispersion-hedger/QM5_34008_multicurrency-basket-dispersion-hedger.mq5 --purpose compile --repo-root .
allowed=true; code=RAW_MQ5_SOURCE_ALLOWED

python tools/strategy_farm/execution_contract_lint.py --repo-root . --ea-id 34008
status=error; code=contract_missing; contracts_checked=0
```

No smoke test or backtest was run or enqueued; this ticket is build/review rework only.

## Risks and open questions

- Dispositive blocker: approved-card sections 4.1 and 6.1 do not mechanically connect the 0.50% package-risk budget to an exact leg stop distance or volume. No safe code-only inference exists.
- Governance blocker: `framework/registry/dxz23_execution_contracts.json` has no EA 34008 record. The source now declares its H1/framework-Friday contract, but the governed contract linter correctly remains fail-closed with `contract_missing`. This ticket did not authorize inventing a registry contract.
- The only outstanding automation limitation is the live-factory interlock preventing the monolithic build-check wrapper from producing its usual report. Its scoped component checks and the actual compile passed independently, and the receipt exposes rather than hides this limitation.
- Open question for OWNER/Research: what exact per-leg stop-distance or package allocation formula should implement `InpPackageRisk=0.50` while retaining the aggregate -1.5% package exit?
- There is no authorization for review resubmission, backtesting, promotion, or live use until a revised card is approved.

## Rollback

Revert the ticket commit with `git revert <commit>`. This restores the prior MQ5, EX5, SPEC, manifest, setfiles, receipt, and removes the ticket test/evidence file. If binary-only recovery is needed before a revert, the exact pre-rework EX5 backup is at the path and hash above. No magic allocation, resolver, factory state, queue, backtest, verdict row, or live-terminal rollback is required.

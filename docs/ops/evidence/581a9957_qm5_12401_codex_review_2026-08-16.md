# Codex build review — QM5_12401

Task: `581a9957-4c0b-47f8-9b4c-f9b373d9fcb2`

Verdict: `RECYCLE — HOST SLOT IS UNWIRED`

The source compiles strictly at 0 errors/0 warnings and its strategy inputs are
otherwise referenced, but the execution identity is wrong for six of seven
hosts:

- setfiles register `SP500.DWX` through `XTIUSD.DWX` at slots 0 through 6;
- `QM_FrameworkInit` correctly receives `qm_magic_slot_offset`;
- `Strategy_EntrySignal` nevertheless hard-codes `req.symbol_slot = 0`.

Thus a non-slot-0 instance asks the entry boundary for slot 0 rather than its
governed host slot. This is the exact unwired-input class requested by the
backlog task and can misattribute entries, position ownership, logs, and kill
switch evidence. Replace the literal with `qm_magic_slot_offset`, recompile,
and return for independent review. Do not enqueue the current binary.

Focused facts:

- all seven backtest sets use `RISK_FIXED=1000`, `RISK_PERCENT=0`;
- stale-news tolerance is 336 hours;
- source SHA-256
  `08a3c1e2cac465f662267b682e4fffb501cb7e64ad7c6795e966aeeef097fa62`;
- reviewed EX5 SHA-256
  `4818253159aa8fd60ad7efd31636e37ea85aee7b2ca695dff3888c8717cdc8cb`.

Compile/build PASS does not cure the semantic slot mismatch and is not a
pipeline verdict.

# rework-40008 review-fix evidence

- Router task: `28396e6d-be0b-4685-89df-749b9fe622f8`
- EA: `QM5_40008_aqr-value-and-momentum-everywhere`
- Work order: `docs/ops/evidence/28396e6d_qm5_40008_gemini_build_codex_review_2026-08-18.md` and `docs/ops/evidence/4f626c91_qm5_40008_gemini_codex_review_2026-08-23.json`
- Scope: EA source, SPEC, its four D1 backtest setfiles, and one EA-specific pytest module. No registry allocation, gate criterion, backtest queue, factory, terminal, verdict-row, or T_Live mutation.

## What changed

1. Replaced the local year/quarter clock with the tester-safe framework calendar chain. `Strategy_IsNewQuarter()` latches through `QM_IsNewCalendarPeriod(PERIOD_MN1, _Symbol)` and reads `QM_CalendarPeriodKey(...)`; all strategy computation and rebalance decisions are behind the single D1 `QM_IsNewBar` gate (`framework/EAs/QM5_40008_aqr-value-and-momentum-everywhere/QM5_40008_aqr-value-and-momentum-everywhere.mq5:153`, `:418`).
2. Added the card's maximum three-tick slippage input, validates it at `1..3`, converts ticks to broker points using `SYMBOL_TRADE_TICK_SIZE / SYMBOL_POINT`, and reconfigures the framework entry chain after `QM_FrameworkInit` (`...mq5:67`, `:99-133`, `:380-390`).
3. Made the approved four-asset rank fail closed unless every universe member has the full value/momentum data requirement (`...mq5:221`). The factor state remains cached once per completed D1 bar; the MAE hook remains per tick (`...mq5:405`).
4. Documented the execution tolerance and exact D1 bar gate in `framework/EAs/QM5_40008_aqr-value-and-momentum-everywhere/SPEC.md:41` and `:66`.
5. Bound `strategy_max_slippage_ticks=3`, `RISK_FIXED=1000`, `RISK_PERCENT=0`, and the current source SHA-256 `be0e6e47664c49f985808df1ac3cd9c9a28ebbc1749d48b2a77d5bc6340c7ea9` in all four scoped backtest setfiles (`sets/*.set:13`, `:19-20`, `:39`).
6. Added `tools/strategy_farm/tests/test_qm5_40008_rework_static.py`. It proves approved identity/mechanic binding, complete-universe ranks, calendar/slippage/risk/MAE framework wiring, explicit raw-series exceptions, exact magic rows, source-hash bindings, fixed-risk backtest mode, and code plus setfile use-sites for every declared strategy input (`:53-142`).

## Registry evidence

- `framework/registry/ea_id_registry.csv:4495`: active `40008,aqr-value-and-momentum-everywhere` identity.
- `framework/registry/magic_numbers.csv:17534-17537`: four existing active slots and magics `400080000..400080003` for SP500, NDX, XTIUSD, and EURUSD.
- The registry was not changed; resolver regeneration was neither needed nor run.

## Validation

Commands were run from `C:/QM/worktrees/rework-slot-3` on 2026-08-24.

```text
python -m pytest tools/strategy_farm/tests/test_qm5_40008_rework_static.py -q
...... [100%]
6 passed in 0.58s

python -m pytest tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_include_mirror.py <focused D2/D5/D7-D11/D17 hardening nodes> -q
................................... [100%]
35 passed in 2.83s

python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 framework/EAs/QM5_40008_aqr-value-and-momentum-everywhere
verdict: PASS; files_checked: 5; findings: []

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_40008_aqr-value-and-momentum-everywhere
PASS QM5_40008_aqr-value-and-momentum-everywhere
Summary: 1 PASS, 0 FAIL

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_40008_aqr-value-and-momentum-everywhere
files_scanned: 1; failures: []; warnings: []
```

The required scoped strict build was attempted exactly as follows:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_40008_aqr-value-and-momentum-everywhere -Strict
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED
failure_class: LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
detail: terminal64 processes are alive; ad-hoc compile/build_check is refused; no retry was attempted

python tools/strategy_farm/compile_ea.py --ea-label QM5_40008_aqr-value-and-momentum-everywhere --force --json --fail-on-error
verdict: COMPILE_FAILED
reason: compile_one.ps1 reason_class=INCLUDE_MIRROR_REFUSED
```

This is an infrastructure interlock, not an MQL verdict. It was not bypassed: no terminal was stopped, no factory state was toggled, and no compile or backtest work item was enqueued. Consequently the tracked EX5 remains the pre-rework binary (396,288 bytes, mtime 2026-08-24 06:59:54Z), and no smoke was run against that stale binary. Fresh compile and smoke evidence remain an explicit review risk.

## Rollback

Revert the ticket commit (message `rework(40008): wire calendar and slippage contract`) with `git revert <commit>`. This restores only the EA source, SPEC, four setfiles, regression test, and this evidence note. No DB, registry, factory, terminal, or live rollback is required.

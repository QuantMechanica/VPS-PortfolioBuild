# rework-1629 — QM5_1629 review repair

Date: 2026-08-24 Europe/Berlin

Router task: `2f8bdfac-52e1-43b9-8c26-0f2564fcb0de`

EA: `QM5_1629_ehlers-cybernetic-cycle-h4`

Work order: `docs/ops/evidence/2026-08-23_qm5_1629_gemini_code_review.md`

## Result

The five review findings were repaired without changing the approved card, gate criteria, registry allocations, setfile parameters, factory state, backtest queue, verdict rows, or any live terminal.

- The Cybernetic Cycle smoother now uses the approved completed H4 close series, with bounded `CopyRates` and `ArraySize` guards (`framework/EAs/QM5_1629_ehlers-cybernetic-cycle-h4/QM5_1629_ehlers-cybernetic-cycle-h4.mq5:59`, `:69`, `:83`).
- Cooldown and time stop now count actual H4 bars through fail-closed `iBarShift` lookups instead of elapsed seconds (`...mq5:132`, `:143`, `:266`, `:341`).
- Break-even now offsets in the direction of profit by the current spread, normalizes the price, and validates stop/freeze distance before modification (`...mq5:154`, `:173`, `:325`).
- The framework execution contract and entry clock are explicitly H4 (`...mq5:405`, `:463`).
- Cooldown state is written only after `QM_TM_OpenPosition` confirms success (`...mq5:473-477`).
- The strategy spec now matches the approved close-price, H4-bar, and break-even-plus-spread mechanics (`framework/EAs/QM5_1629_ehlers-cybernetic-cycle-h4/SPEC.md:13`, `:69`, `:113`).
- The rebuilt MQ5/EX5 identity and validation status are recorded in `framework/EAs/QM5_1629_ehlers-cybernetic-cycle-h4/build_identity.json:2-15`.

## Preflight and registry evidence

- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1629_ehlers-cybernetic-cycle-h4.md`, `g0_status: APPROVED`.
- EA registry: `framework/registry/ea_id_registry.csv:4618` is active for `1629,ehlers-cybernetic-cycle-h4`.
- Magic registry: 14 existing active rows at `framework/registry/magic_numbers.csv:17819-17832` cover slots 0-13. No registry or resolver file was changed.
- `python framework/scripts/skill_build_ea_guard.py --ea-id 1629 --ea-label QM5_1629_ehlers-cybernetic-cycle-h4` returned `status=ok`, with EA row, magic rows, and EA directory all true.
- Every one of the 11 declared `strategy_*` inputs has at least one executable use-site; observed source occurrence counts were 2-5 per input.

## Validation evidence

### Compile

The required scoped canonical invocation was attempted:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_1629_ehlers-cybernetic-cycle-h4/QM5_1629_ehlers-cybernetic-cycle-h4.mq5 -Strict
```

It refused before compilation with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because T1-T10 factory terminals were active. The task forbids stopping terminals, toggling the factory, or enqueueing a compile, so no prohibited workaround was used. A scoped `build_check.ps1 -EALabel QM5_1629_ehlers-cybernetic-cycle-h4 -SkipCompile` attempt was refused by the same guard before checks. This is recorded fail-closed in `build_identity.json` as `build_check_passed=false` and `build_check_status=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`.

Compilation was then performed in a new isolated portable MetaEditor workspace. Standard includes were copied from T2 into the temporary workspace and overlaid with this worktree's `framework/include`; no active terminal include directory was written. The isolated and repository `QM_Common.mqh` SHA-256 values both equalled `65fa2d15dfaf69a8ad5cdb22bd6f2e64430511929b968717b015a30edbde1e9c`.

```text
Result: 0 errors, 0 warnings, 7336 ms elapsed, cpu='X64 Regular'
compile log: framework/build/compile/20260824_124500_rework_1629/QM5_1629_ehlers-cybernetic-cycle-h4.compile.log
MQ5 SHA-256: 649dd79643878e3fc23285b3ea1b529c675b5429269096f98e6066145447e92a
EX5 SHA-256: 536f5515fa4f52af584e1b81d51997ca0fb0d97fa70d2b1fd2d9405d079d725f
EX5 bytes: 408600
```

### Static gates

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_1629_ehlers-cybernetic-cycle-h4
files_scanned=1 failures=0 warnings=0

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_1629_ehlers-cybernetic-cycle-h4
PASS files_checked=15 findings=0 max_news_stale_hours=336

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_1629_ehlers-cybernetic-cycle-h4
PASS 1, FAIL 0
```

### Pytest

Focused touched-scope suite plus the D10 buffer-bound and build-check integration cases:

```text
python -m pytest tools/strategy_farm/tests/test_qm5_1629_review_rework_static.py tools/strategy_farm/tests/test_build_gate_hardening.py::test_d10_indicator_buffer_bound_pass_and_fail_fixtures tools/strategy_farm/tests/test_build_gate_hardening.py::test_build_check_applies_hardening_pass_and_fail_fixtures -q
....... [100%]
7 passed in 3.01s
```

The whole `test_build_gate_hardening.py` module was also run with the new 1629 suite: `33 passed, 1 failed in 536.43s`. The sole failure is an unrelated pre-existing corpus assertion against `framework/EAs/QM5_41134_wti-mdaily-iqrmean-mom/QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`; that EA is outside this ticket and was not modified.

## Safety boundary

- No backtest was enqueued, deleted, or run.
- Factory state and AutoTrading were not changed.
- `C:/QM/mt5/T_Live` was not written or controlled.
- No verdict row, gate threshold, gate criterion, magic allocation, or resolver was changed.
- Only QM5_1629 source/spec/binary/identity, its focused unit test, and this evidence note are in the commit.

## Rollback

Revert the ticket commit with `git revert <rework-1629-commit>`. That restores the prior MQ5, EX5, spec, and build identity and removes the focused test/evidence note. The prior binary remains recoverable from Git. No database, queue, registry, factory, or live rollback is required because none was mutated by this ticket.

## Residual risk

The official scoped `build_check.ps1` could not produce a PASS report while factory terminals were active and this task explicitly prohibited its only governed alternative (`enqueue-compile`). Review should retain the recorded fail-closed status until a quiescent or governed compile lane reruns the exact scoped build check. The EA itself has a strict isolated 0/0 compile, focused regression PASS, hardening PASS, guardrails PASS, and spec PASS.

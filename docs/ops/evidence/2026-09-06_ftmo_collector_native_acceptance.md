# FTMO collector native compile and acceptance harness — 2026-09-06

Task f4e95c80-c121-42cb-8e42-78e1334046f4. REVIEW; native compile PASS, native tester NOT_RUN, overall acceptance INCOMPLETE. Nothing installed into any terminal; no trial-capture terminal directory created.

Code is on agents/codex-ftmo-collector-native-acceptance-20260906. The task-specific bounded compile contract in include_mirror.py requires this live Codex assignment, an exact canonical evidence sandbox, an exact four-file allowlist, resolved-path containment and matching SHA256 values. It does not grant include mirroring or change the existing factory compile guard. The runner uses exclusive creation for its lock and a private portable MetaEditor copy read from T1; all output stays in this sandbox. It never invokes terminal64.exe.

Both native compilations returned zero errors and zero warnings. See compile_probe/result.json for source and EX5 hashes, and adjacent MQL5/*.log for the native compiler logs. MetaEditor returns process code 1 here; acceptance uses the native Result line plus fresh EX5 existence. EX5 files and the copied compiler remain local review artifacts and are excluded from Git. Collector EX5 SHA256: 1eb156f613d2fca61b4e9aac7519ff1de508d1c05e23618e291cb8042f115e4a. Harness EX5 SHA256: ddfd81a2e295fb7024a14adb389ba775968ad42898ee624b47da20ee1f63bbb3.

The collector holds the OS write handle throughout its session to reject concurrent writers. The tester-only fixture scripts 901 UTC observations per case, restart at second 450 (inside an M5 bucket), equity trough 94000 at second 460, nonzero position and pending inventories, winter/summer Prague midnight and spring/autumn DST offset transitions, and a competing write-handle refusal. It writes a source-SHA-bound PASS/FAIL report when actually run. Account observations are scripted; it does not open real positions. Restart is a scripted close/reopen and session reset, not a terminal process restart. The current probe compiled this fixture but did not execute it; no fixture PASS is claimed. The prohibition on installing into ANY terminal prevents staging this new EX5 for a native tester run in this task. Review must retain that gap and commission an authorized governed tester run before acceptance.

Verification: 30 focused pytest tests passed (monitor compile contract, existing include-mirror guard, telemetry reader/compaction). Refusals include wrong task/agent/state, wrong sandbox, changed hashes, missing/extra files and escaped input paths. Native tester remains NOT_RUN in native_tester_status.json. No pipeline or deployment verdict is inferred.

Compiler switches follow official [IDE integration documentation](https://www.metatrader5.com/en/metaeditor/help/beginning/integration_ide) and [portable mode documentation](https://www.metatrader5.com/en/metaeditor/help/beginning/open).

# QM5_1607 Build Evidence — Source Ready, Governed Compile Held

- Task: `01653662-8b30-4869-9540-b2ff3c32513a` (`build_ea`, priority 50, assigned to Codex)
- EA: `QM5_1607_aa-mom-tol-band`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1607_aa-mom-tol-band.md`
- Current MQ5 SHA-256: `9a66c131b68f17dbe388e8299dc973b6975a6b235d91da32e595f8e89d88a19a`
- Outcome: `SOURCE_READY_COMPILE_HELD`

## Governed identity

The card declares `g0_status: APPROVED`, `ea_id: QM5_1607`, and slug `aa-mom-tol-band`. The active EA registry row matches that identity. Thirteen active magic rows exist for slots 0-12: GDAXI, NDX, SP500, UK100, WS30, XAUUSD, EURUSD, GBPUSD, USDJPY, USDCHF, AUDUSD, USDCAD, and NZDUSD, all with the `.DWX` suffix.

## Card implementation

The source implements the approved monthly momentum-informed tolerance band on a D1 chart:

- one calendar-month review driven by the framework's single D1 closed-bar gate;
- completed-MN1 12-month absolute momentum using the approved zero-cash-return approximation;
- positive momentum permits 50%-60% risk-budget exposure; non-positive momentum permits 40%-50%;
- the position comment persists 40%, 50%, or 60% state, and the EA rebalances only when the held state lies outside the permitted band;
- missing or invalid signal input closes the allocation;
- 3.0 x ATR(20,D1) initial stop and monthly tolerance-band management;
- standard framework news gate, Friday liquidation disabled because it would contradict a monthly hold;
- `RISK_FIXED=1000`, `RISK_PERCENT=0`, `qm_news_stale_max_hours=336`, one position per symbol/magic, no ML, grid, or martingale.

Framework alignment is explicit: `Strategy_NoTradeFilter` enforces D1, `Strategy_EntrySignal` owns the monthly band transition, `Strategy_ManageOpenPosition` is intentionally empty beyond the broker-held stop, `Strategy_ExitSignal` is passive because invalid-input/rebalance closes occur atomically in the rebalance hook, and `Strategy_NewsFilterHook` delegates to the framework. `OnTick` contains the current explicit MAE hook and zero-initializes `QM_EntryRequest`.

## Focused verification

- `validate_spec_doc.py`: PASS (1/1).
- `validate_build_guardrails.py`: PASS; 14 files checked, zero findings, stale-news ceiling 336 hours.
- `validate_symbol_scope.py --fail-on-leak`: `SINGLE_SYMBOL_OK`, zero violations.
- Thirteen D1 backtest presets regenerated by canonical `gen_setfile.ps1`.
- Every preset contains `RISK_FIXED=1000`, `RISK_PERCENT=0`, the five strategy defaults, and its governed magic slot.
- Preset `build_hash` remains `pending`, correctly awaiting a successful governed build check.
- `git diff --check`: clean for the EA package.

## Compile boundary

Direct `build_check.ps1` stopped before compilation with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because terminal64 workers are alive. No bypass, retry, process stop, terminal launch, or backtest interruption was attempted.

The sanctioned command `farmctl.py enqueue-compile QM5_1607_aa-mom-tol-band` was then refused without enqueueing a row: reason `WORK_ITEMS_EXIST`. The two existing governed compile rows are terminal failures (`1da055cc-7762-42e1-b110-7d920e1c7d10` INVALID and `c74de048-28c6-4fc2-82d0-81d4bf8b4dc7` COMPILE_FAIL), not reusable open work. No `.ex5` exists for the current source.

Smoke was not run, and no pipeline verdict is claimed.

## Required next governed action

A sanctioned compile-row supersede/rebuild authorization must admit a fresh row bound to the current source. Its worker must produce strict compile and build-check PASS evidence, bind final setfile hashes, and only then permit smoke/review closure.

Short verdict: `SOURCE_READY_COMPILE_HELD: static gates PASS; fresh governed compile refused by prior work-item guard.`

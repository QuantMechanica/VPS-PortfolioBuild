# rework-39001 — QM5_39001 review repair evidence

- Router task: `5e1b4b08-d5f4-420c-a0e0-765ee6f7e736`
- EA: `QM5_39001_forexfactory-trading-made-simple-tms`
- Worktree: `C:/QM/worktrees/rework-slot-6`
- Date executed: 2026-08-24
- Scope: build/review repair only; no backtest enqueue, verdict-row write,
  factory toggle, T_Live access, or registry allocation change.

## Findings used as the work order

The repair covers both recorded reviews:

- `docs/ops/evidence/5e1b4b08_qm5_39001_gemini_build_codex_review_2026-08-18.md`
- `docs/ops/evidence/2d0bdc23_qm5_39001_gemini_review_2026-08-23.md`

The approved G0 card remains unchanged. Its deterministic example reads three
completed bars (`CopyRates(..., 1, 3, rates)`), which supplies the bounded
recent-swing window. Its exact exit section supplies TP, SL, and TDI cross-back
but no numeric break-even trigger; the unapproved +1R break-even mechanic was
therefore removed rather than guessed.

## What changed

### Card-faithful mechanism and lifecycle

- Fixed the signal clock to H1 and made fixed card rules constants rather than
  user-selectable deviations (`.mq5:43-53`).
- Added fail-closed input ranges so daily entry halt, daily hard stop, total
  drawdown, and per-trade risk cannot exceed 2.0%, 2.5%, 5.0%, and 0.5%
  (`.mq5:85-101`).
- Added bounded three-bar `CopyRates` state reconstruction with an explicit
  `ArraySize` guard; every failed refresh clears cached entry/exit state
  (`.mq5:141-200`).
- Converted broker time with `QM_BrokerToUTC` before the 23:55–00:05 rollover
  comparison (`.mq5:207-229`).
- Preserved the exact swing extreme ±3 pips, removed the 0.5×ATR substitution,
  and made pip conversion failure block the entry (`.mq5:234-288`).
- Implemented a true TDI cross-back from prior/current completed-H1
  relationships and removed the card-undefined break-even rule
  (`.mq5:291-325`).
- Configured market-order deviation as at most three trade ticks while
  preserving the framework-resolved host magic (`.mq5:341-384`).
- Seeded state in `OnInit`, refreshed it before management/admission on H1 bar
  boundaries, retained the MAE hook, and kept entry blackouts after management
  (`.mq5:396-452`).

### Presets, spec, and regression coverage

- Regenerated only the three registered EURUSD/GBPUSD/AUDUSD H1 backtest sets.
  Each retains `RISK_FIXED=1000`, `RISK_PERCENT=0`, its deterministic magic
  slot, all seven declared strategy inputs, and the canonical-LF MQ5 SHA-256
  `2606ec8414ed9ac52ab90821235d859d9a3b69e6c74b04806f4c778e4dc4a425`
  (example: EURUSD set lines 13-31).
- Updated the SPEC to record fixed H1 cadence, three completed-bar swing state,
  true cross-back semantics, and the absence of an invented BE trigger
  (`SPEC.md:65-75`).
- Added focused static regression tests for approved-card/registry identity,
  framework/risk/magic/MAE wiring, UTC/H1/restart/slippage ordering, exact stop
  and cross-back mechanics, per-input use sites, and preset bindings
  (`test_qm5_39001_tms_rework_static.py:45-166`).

No `ea_id_registry.csv`, `magic_numbers.csv`, or resolver edit was required:
the existing active rows are 39001 slots 0/1/2 and the generated resolver
already contains magics 390010000/390010001/390010002.

## Build and test evidence

### Compile

The required scoped command was attempted:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -Strict -EALabel QM5_39001_forexfactory-trading-made-simple-tms
```

It failed closed before compilation with
`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because terminal64 processes were alive.
The suggested `enqueue-compile` path was also attempted but correctly refused
this noncanonical worktree; its worker consumes `C:/QM/repo`, so overriding it
would have compiled different source bytes.

For source-level compile evidence without interrupting factory terminals, the
idle T8 MetaEditor compiled this worktree source in isolation. Result: **0
errors, 0 warnings**, 396,456-byte EX5, SHA-256
`c1b0950d6b18a170ab3b0519a17b92a3769e68a9727599348e159fb0aa674efd`.
The pre-rework EX5 was preserved outside the repo. Compile log:
`D:/QM/reports/compile/20260824_rework39001_slot6/QM5_39001_forexfactory-trading-made-simple-tms.compile.log`.

### Checks

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_39001_forexfactory-trading-made-simple-tms
PASS: failures=[], warnings=[], files_scanned=1

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_39001_forexfactory-trading-made-simple-tms
PASS  QM5_39001_forexfactory-trading-made-simple-tms
Summary: 1 PASS, 0 FAIL (of 1)

python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 <MQ5> <sets-dir>
PASS: MQ5 findings=[]; three-set directory findings=[]

python -m pytest tools/strategy_farm/tests/test_qm5_39001_tms_rework_static.py -q
5 passed in 1.25s

python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py -q -k "d2_loss or d4_management or d5_raw or d7_framework or d10_indicator or d11_canonical or semantic_predicates"
7 passed, 22 deselected in 0.93s
```

The complete hardening regression was also run: **33 passed, 1 failed in
420.38s**. The single failure is outside this ticket:
`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven
pre-existing findings in the QM5_411xx cohort, beginning with
`QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`. No QM5_411xx file was changed.

## Rollback

Revert the rework commit containing this evidence file:

```text
git revert --no-edit <rework(39001) commit>
```

This restores the prior MQ5, EX5, SPEC, and three setfiles and removes the
focused test/evidence. The preserved pre-rework binary is also available at
`D:/QM/reports/compile/20260824_rework39001_slot6/pre_rework_QM5_39001.ex5`.
No DB, registry, factory, or live rollback is required.

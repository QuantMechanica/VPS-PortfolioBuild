# Review: card heading language intake gate (task fe6e8a54)

- Task: `fe6e8a54-2815-4c4c-95dd-09dc2ed3faff` (ops_issue, REVIEW → APPROVED)
- Reviewer: Claude review lane, 2026-08-24
- Codex artifact: `docs/ops/evidence/fe6e8a54_card_heading_intake_gate_2026-08-23.md`
- Implementation commit: `db7fca1f4` ("farm: enforce English headings at card intake")

## Task intent
OWNER 2026-08-23: strategy pages are 82% German-headed. Fix at intake, not only
render. Work items: (1) reject/flag new card with non-English heading;
(2) extend normalisation map on unseen headings; (3) OWNER-decide whether the
historical corpus is ever rewritten (it is evidence).

## Verification (read-only, current source hash)

Enforcement wiring confirmed against committed source:
- `agent_router.py:1930` — `update_task` rejects a `research_strategy` transition
  to REVIEW (before duplicate admission) with reason
  `strategy_card_non_english_headings` when the review card fails the contract.
- `farmctl.py:23241` — `approve_card` rejects a non-approved-source card before
  mutation; historical cards already under `artifacts/cards_approved` explicitly
  bypass (evidence preserved, content-addressed identity intact). No corpus rewrite.
- `dashboards/archive_matrix.py` — now imports the shared `HEADING_DE_EN` +
  `normalise_heading` from `card_heading_language` instead of a divergent local
  copy; render path leaves unknown headings untouched. One map, two boundaries.

Behaviour spot-check (`card_heading_language.check_markdown_heading_language`):
- `# Mechanik` → rejected, `mapped_non_english_heading`.
- `## Was ist das fuer eine Strategie` (OWNER's cited example) → rejected,
  probable German (>=2 grammar tokens).
- `# Mechanics` / `## Source` → ok.
- `# Filter` (ambiguous cross-language) → kept valid, not a language signal.
- Unmapped probable German → returned in `unmapped_headings` with
  `normalization_map_update_required=true` (explicit map extension, no silent
  translation of source content).

Tests (current HEAD): `test_card_heading_language.py` + `test_agent_router.py` +
`test_mnt012_build_guards.py` = 48 passed. Working tree clean for all four
touched files (no drift from committed source).

## Findings
No defects. Fail-closed at both deterministic intake boundaries, historical
corpus untouched (correctly parked as OWNER decision per work item 3), single
shared map, ambiguous headings tolerated to avoid false positives.

## Verdict
APPROVED — ops delivery clean; new-card English-heading contract enforced at
intake and approval, historical evidence preserved.

# Review — SP-B4 Schedule-View (ops_issue 8c46a30d) — BLOCKED

Date: 2026-08-23
Reviewer: Claude (review lane)
Router task: `8c46a30d-5aab-46a1-aeb5-2ca9507d3014` (`SP-B4`, ops_issue, priority 50, zone GRUEN)
Worker artifact: `docs/ops/evidence/2026-08-23_sp_b4_schedule_view_dependency_hold.md`

## Verdict

**BLOCKED** — the worker correctly declined to generate the schedule-view artifact and
returned a documented dependency hold. SP-B4 cannot proceed until its declared
prerequisite SP-B2 ships. This is a genuine cross-task dependency + OWNER/router
reclassification block, not an actionable RECYCLE.

## What was delivered

No calendar seed, view file, FILE_COMMON copy, work item, verdict, or terminal state was
changed. The worker delivered a DEPENDENCY_HOLD note arguing that SP-B4's own payload
declares `depends_on: SP-B2`, that SP-B2 is `BLOCKED`, and that SP-B2 is the task
chartered to ship exactly this schedule-view under a fail-closed single-source loader —
so emitting the view now would pre-empt SP-B2 and risk look-ahead/source-divergence.

## Verification performed (read-only)

DB `farm_state.sqlite` opened `mode=ro`; every load-bearing claim checked:

- SP-B2 `agent_tasks` `84c988e6-fe11-47ed-b9f3-413096628bd2`: `state=BLOCKED`,
  `updated_at=2026-08-22T11:23:11Z` — **confirmed**, verdict unchanged.
- Downstream Q09 rerun wave `14487282-3868-43cb-b22d-00ea049de0b8`: `BLOCKED`,
  `updated_at=2026-08-22T06:25:27Z` — **confirmed**.
- Q09_NEWS rerun pilot `work_items ba24e7a3-4edf-4dc1-b74d-5854a6b5ecf2`:
  `status=done`, `verdict=REVIEW_REQUIRED` — **confirmed** (not the "40/40 + review"
  terminal state SP-B2's resume gate requires).
- Predecessor `work_items b2468d2e-92a5-4fd8-a6ae-29967da0ca08`: `failed / INFRA_FAIL`
  — **confirmed**.
- Cited files all present: `docs/ops/NEWS_CALENDAR_CONTRACT_V2_2026-08-22.md` (§5 confirms
  schedule view = `timestamp_utc, currency, impact, event_id` projected from THE
  authoritative source), `decisions/2026-08-22_news_impact_taxonomy.md`,
  `docs/ops/evidence/2026-08-22_sp_b2_news_contract_v2_dependency_preflight.md`.

## One correction to the worker note (does not change the verdict)

The note states review `9b40ff25-098e-4a7a-a78c-d510ba7b763b` is `IN_PROGRESS`. As of
this review it is `APPROVED` (`updated_at=2026-08-23T08:49:36Z`, i.e. after the hold was
written at 07:25). This is stale-at-write, not wrong-at-write. It does not unblock SP-B4:
SP-B2 itself remains `BLOCKED` and its verdict is unchanged, so the load-bearing
dependency stands.

## Why BLOCKED and not RECYCLE

The contract §5 makes the schedule view a projection from the single authoritative
source; selecting that source with fail-closed 0/>1 behavior and a mapping fingerprint is
precisely SP-B2's charter (§3/§9). Generating the view ahead of that loader would either
pick a source outside SP-B2 (duplication/divergence) or hardcode
`forex_factory_calendar_clean.csv` without the fail-closed machinery — both violate the
"kein Look-ahead" hard_constraint and pre-empt an active Q09_NEWS measurement. There is
no defect in the worker's own output to fix; the block is external (SP-B2 must ship, or
OWNER/router must reclassify SP-B4 as independent — a payload change this task cannot make
for itself).

## Deterministic resume conditions (carried forward)

1. SP-B2 commits its loader (fail-closed single-source selection + mapping fingerprint),
   at which point SP-B4 becomes "read the built view/loader, project the four allowed
   columns"; or
2. OWNER/router explicitly reclassifies SP-B4 as independent of SP-B2 despite its
   `depends_on` field.

# QM5_11423 FX Q02 CPU-Ceiling Handoff

Date: 2026-08-07

Branch: `agents/board-advisor`

Status: fixed-risk preset committed and Q02 dry-run eligible; enqueue stopped
before apply at the binding paced-fleet CPU ceiling

## Outcome

The requested cointegration anchors do not need Q02 repair. `QM5_12532` has
canonical logical-basket Q02 PASS followed by Q04 PASS and Q05 FAIL, while
`QM5_12533` has canonical logical-basket Q02 PASS followed by Q04 FAIL. The
frozen 66-pair frontier is already fully mechanized through its final two rows,
so another pair Card or EA would duplicate existing coverage.

The allowed existing-card fallback is `QM5_11423_williams-naked-close-stop-entry-d1`.
It is an approved, source-attributed Larry Williams D1 FX card with pure closed-bar
OHLC rules, day-only stop entries, fixed 2R exits, no indicators, no ML, no grid,
and no adaptive fit. Its EA registry row and EURUSD slot/magic `114230000` are
active, its compiled build already existed, and a canonical farm query returned
zero prior work items. The deterministic dry run initially classified the EA only
as `no_setfiles`.

## Committed build-contract advance

Commit `970874f5a741e9915fec4a52a9afbeafbdbc4bc7` adds exactly one canonical
EURUSD D1 backtest preset:

`framework/EAs/QM5_11423_williams-naked-close-stop-entry-d1/sets/QM5_11423_williams-naked-close-stop-entry-d1_EURUSD.DWX_D1_backtest.set`

The preset is constrained to:

- `environment=backtest`
- `RISK_FIXED=1000`
- `RISK_PERCENT=0`
- `PORTFOLIO_WEIGHT=1`
- `qm_magic_slot_offset=0`
- the approved strategy defaults: 1-pip buffer, 70-pip stop cap, 2R take
  profit, and 25-pip spread cap

The setfile SHA-256 is
`C5778CF118FCB43DD9AD6F7F98DDB8F404960E8F170212C8781E725817648DCC`.
The existing MQ5 and EX5 hashes are respectively
`28DDD1FD89BF558C9124521692FC4ECB3ECC4338405B55C84DB9B794E12F85A5`
and `B199066285DC43C3D869FD0F7DD3EB82BB7FD88FAF24113FFAFEDAF78270E1EF`.

Scoped build validation passed with zero failures and zero warnings. Evidence:
`D:/QM/reports/framework/21/build_check_20260807_074329.json`.

## Guarded Q02 preflight

The target-only dry run at `2026-08-07T09:44:27+02:00` selected exactly one
never-tested row and no retry or deferred row:

```text
EA:             QM5_11423
symbol:         EURUSD.DWX
setfile:        QM5_11423_williams-naked-close-stop-entry-d1_EURUSD.DWX_D1_backtest.set
priority_track: true
```

It recorded `apply=false`, 1,380 pending rows before selection, and a separate
7,000-row queue ceiling in
`D:/QM/reports/state/claude_sweep_enqueue_2026-06-10.json`. A direct canonical
farm read immediately before the capacity gate still returned zero
`QM5_11423` work items, so the dry run was non-duplicate.

## Binding CPU ceiling

The mandatory immediate pre-apply command
`python tools/strategy_farm/farmctl.py mt5-slots` sampled the factory at
`2026-08-07T07:44:39Z` and found seven running factory terminals:

```text
T1, T2, T3, T4, T5, T8, T9
```

Seven equals the binding paced-fleet ceiling. The separately observed
`T_Live` and FTMO terminals were excluded from the factory count and were not
controlled. Per the mission stop rule, the `--apply` command was not invoked.
No Q02 row was inserted, claimed, or dispatched, and no MT5 process was
launched, stopped, reserved, reaped, or controlled.

The durable machine-readable snapshot is
`artifacts/qm5_11423_fx_q02_cpu_ceiling_20260807T074439Z.json`.

## Next paced action

After a fresh path-aware sample is strictly below seven factory terminals,
repeat the target-only dry run and apply only if it still selects exactly the
same single EURUSD row and the canonical farm still has no active/pending
replacement:

```powershell
python tools/strategy_farm/sweep_enqueue_built_eas.py --ea QM5_11423
python tools/strategy_farm/sweep_enqueue_built_eas.py --ea QM5_11423 --apply
```

Normal workers own dispatch and Q02 execution.

## Safety

- No portfolio-admission, portfolio KPI, or Q08-contribution path changed.
- No `T_Live` manifest, live setfile, deployment artifact, terminal state, or
  AutoTrading state changed.
- Existing unrelated dirty-worktree files were not included in either commit.

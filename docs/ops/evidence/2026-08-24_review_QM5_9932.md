# Review — QM5_9932 bandy-roc-zscore-normalised-mr-index

- Review task: `1db5da1a-e87a-486f-a6f6-2224724a09fe` (review_ea, assigned codex; closed by Claude lane)
- Build task: `c71f308a-76be-456f-8ae7-a5d7c3abc4e0` (build_ea, source_agent gemini / backend agy)
- Reviewer: Claude (review lane), read-only DB (mode=ro)
- Verdict: **RECYCLE** (named actionable defects; code sound, needs universe trim + governed recompile)

## Sources reviewed
- Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9932_bandy-roc-zscore-normalised-mr-index.md`
- EA: `framework/EAs/QM5_9932_bandy-roc-zscore-normalised-mr-index/QM5_9932_bandy-roc-zscore-normalised-mr-index.mq5` (sha256 `ee78b3ec…f8ed6`, matches build_result)
- Build result: `D:/QM/strategy_farm/artifacts/build_results/QM5_9932_..._build_result.json`
- Governed compile work item (DB): `314e33cc-173d-4e95-9cb8-a1003be28bc7`, evidence `D:/QM/reports/work_items/314e33cc…/QM5_9932/COMPILE_EA/compile_evidence.json`
- magic_numbers.csv (13 rows), sets/ (13 setfiles)

## Card-faithful mechanism — PASS
Current source implements the card exactly: ROC(10) → rolling z-score over 60 daily ROC readings → 200-SMA regime gate → long entry `z<=-2.0 AND close>SMA200`, exit `z>=0.0` OR 8-bar time stop OR ATR(14)×2.5 catastrophic SL; long-only; degenerate-denominator guard `stdev<0.20`; skip incomplete D1 bar; one position per magic.

## Strategy inputs — all wired (no dead inputs)
All 10 `strategy_*` inputs grepped to real use-sites beyond declaration: roc_period, zscore_lookback, entry_z, exit_z, regime_sma_period, atr_period, atr_stop_mult, time_stop_bars, min_stdev, warmup_bars — each read in `Strategy_CalculateROCZ` / `Strategy_EntrySignal` / `Strategy_ExitSignal` / `Strategy_NoTradeFilter`.

## Framework conformity — PASS
- `#include <QM/QM_Common.mqh>` chain present.
- MAE hook present: `QM_FrameworkTrackOpenPositionMae()` first line of OnTick.
- RISK_FIXED=1000 / RISK_PERCENT=0 (backtest); setfiles carry same.
- Magic via `QM_FrameworkMagic()` (MagicResolver); registry rows use `ea_id*10000+slot` (99320000+slot) — formula correct.
- News filter wired (`QM_NewsAllowsTrade2` / legacy fallback); Friday-close via `QM_FrameworkHandleFridayClose`.
- Bounded buffers (CopyRates over `zscore_lookback+roc_period`, perf-allowed comment); QM_SMA/QM_ATR helpers. No raw unbounded series. No ML. No invented commission/swap.
- Setfiles are governed-generator output (build_hash header = current source `ee78b3e`, correct slot, backtest env).

## BLOCKING DEFECT 1 — card universe violation (candidate-pool / ROT)
Card "Target symbols": **SP500.DWX (backtest-only), NDX.DWX, WS30.DWX** only, and explicitly states the construct "could be ported to FX/commodities later but Bandy reports weaker statistical evidence outside indices."
Build registered/generated **13** symbols — the 3 card indices PLUS GDAXI.DWX, UK100.DWX, XAUUSD.DWX, EURUSD.DWX, GBPUSD.DWX, USDJPY.DWX, USDCHF.DWX, AUDUSD.DWX, USDCAD.DWX, NZDUSD.DWX (magic_numbers.csv slots 0–12; 13 setfiles in sets/). Silent universe expansion beyond the approved card.
Action: trim setfiles + magic rows to the card universe (SP500/NDX/WS30). Universe originates in the 2026-08-17 governed allocator worklist, not gemini's build — but the built/registered set contradicts the card either way.

## BLOCKING DEFECT 2 — no build-gate PASS for current source hash
Governed compile `314e33cc` (2026-08-22) ran against OLD source `3c0672a…` and returned **build_check.result=FAIL** with:
- `EA_Q08_MAE_HOOK_MISSING` (mq5:76 — OnTick lacked `QM_FrameworkTrackOpenPositionMae()`)
- `EA_TRADE_REQUEST_UNINITIALIZED` (mq5:108 — bare `QM_EntryRequest req`)
Current source `ee78b3e` (mq5 mtime Aug 23 07:26) FIXES both, but has never been recompiled through the build-gate. The repo `.ex5` (`dbece68c…`, mtime Aug 22 23:09) was produced from the OLD, gate-rejected source and predates the current mq5 by ~8h — it is stale. build_result.json's `build_check_passed:true` / `compile_succeeded:true` is gemini's self-report and is contradicted by the governed DB evidence.
Action: re-run `farmctl enqueue-compile` on the current source and obtain build_check PASS before Q02. (Recompile itself is ROT / not the review lane's action.)

## Advisory (non-blocking)
- OnTick orders news gate before Friday-close and strategy-exit handling; every entry carries a server-side SL, so advisory under the mechanical contract (matches prior codex note).
- build_result records `smoke_result=deferred_p2_smoke` with `blocked_reason=null` — waiver lacks durable measured tester-capacity evidence; subsumed by Defect 2 (must recompile anyway).

## Verdict
**RECYCLE** — mechanism, input wiring, and framework conformity are clean, but (1) the built/registered symbol universe exceeds the approved card, and (2) the current source has no passing build-gate and the shipped .ex5 is stale from a gate-rejected source. Fix universe to card (SP500/NDX/WS30), then re-enqueue-compile the current source for a build_check PASS before Q02.

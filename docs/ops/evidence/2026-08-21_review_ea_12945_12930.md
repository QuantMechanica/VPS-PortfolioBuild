# review_ea — QM5_12945 and QM5_12930 — 2026-08-21

Router task `7b90bfb1-d082-4e11-a9e2-9a7d96efe031` (`review_ea`, claude). Authority:
the APPROVED-close conditions on build tasks `e3e1d19f` (12945) and `adec96fb` (12930);
both closed on build quality with no review_ea. Static/evidence-based review only — no
MT5 smoke/backtest was run (12930's smoke correctly returned SMOKE_FRAMEWORK_ERROR under
custom-history isolation; that is the isolation gate working, not a build defect).

## Overall verdict

- **QM5_12945_tv-kn-ema-cross-atr-tp — APPROVED for review_ea.** Faithful to the card,
  every input wired, all validators clean, fixed-risk backtest setfiles, no invented
  mechanics.
- **QM5_12930_classical-ascending-triangle-breakout-h4 — RECYCLE.** The EA source, sets,
  registry, and controls are clean and card-faithful, but `SPEC.md` fails
  `validate_spec_doc.py` on every required section — it ships the obsolete free-form
  template instead of the canonical schema. Doc-only defect, no code change needed.

This review does not touch T_Live, AutoTrading, terminal64.exe, active T1–T10 backtests,
or the ea_id/magic registries (report-only).

---

## QM5_12945_tv-kn-ema-cross-atr-tp

### Reviewed identity

- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12945_tv-kn-ema-cross-atr-tp.md`
- MQ5 SHA-256: `5fa4b832b7909c3dcb373605ff2b46e76c5d8e2544b0290f60a17d7ae315c164`
- EX5 SHA-256: `18ac7f2c5bc2de7f1152a4f4889bb261177d951bea3c5957fc13e8a8884e8733`
- EX5 mtime 2026-08-21 18:44 > MQ5 mtime 18:39 — binary is not stale against the source.

### Card fidelity

Card mechanises TradingView "KN - Smart TP SL Signals": fast/slow EMA (9/21) cross, ATR
bracket, opposite-cross flatten. The EA buys on `EMA_fast[2] <= EMA_slow[2] &&
EMA_fast[1] > EMA_slow[1]` and mirrors for the short — closed-bar shifts 1/2, both
directions as the card specifies. SL = `strategy_atr_sl_mult` (1.5) × ATR(14), TP =
`strategy_atr_tp_mult` (1.0) × ATR(14), single full-position target (the card's baseline
TP1-only, no TP2/TP3 ladder, HR-14 compliant). `Strategy_ExitSignal` flattens on the
opposite closed-bar cross. `Strategy_ManageOpenPosition` is empty — no trailing,
break-even, partial close, grid, martingale, or ML was added.

### Input-use audit

| Input | Use site |
|---|---|
| `strategy_signal_tf` | QM_EMA/QM_ATR TF arg (entry + exit); NoTradeFilter |
| `strategy_ema_fast_period` | QM_EMA fast, entry + exit |
| `strategy_ema_slow_period` | QM_EMA slow, entry + exit; ordering guard |
| `strategy_atr_period` | QM_ATR lookback |
| `strategy_atr_sl_mult` | QM_StopATRFromValue |
| `strategy_atr_tp_mult` | QM_TakeATRFromValue |

Every strategy input resolves to a real use (grep counts 3–10 occurrences each, all >
declaration). Framework inputs feed `QM_FrameworkInit`, the two-axis news path,
Friday-close, kill-switch, and MAE tracking.

### Host / magic / risk / time controls

- `ea_id_registry.csv`: `12945,tv-kn-ema-cross-atr-tp,...,active` (single row).
- `magic_numbers.csv`: 13 active slots 0–12, magic `129450000+slot`.
- SPEC "Designed for" declares **13** symbols; **13** `.set` files shipped; **13** magic
  slots — no QM5_12923-class count mismatch.
- All 13 backtest setfiles: `RISK_FIXED=1000`, `RISK_PERCENT=0`, `PORTFOLIO_WEIGHT=1`,
  correct per-symbol magic slot (e.g. EURUSD=6, GDAXI=0, NZDUSD=12), `strategy_signal_tf=16385`
  (PERIOD_H1), ENV=backtest. News params are the compiled EA defaults
  (`qm_news_stale_max_hours=336`, DXZ profile, PRE30/POST30, high-impact) — no
  setfile weakening.
- No custom DST/session math; broker time comes only from framework helpers
  (`TimeCurrent()` → `QM_FrameworkHandleFridayClose`, `QM_NewsAllowsTrade2`).
- Loss limits: `QM_KillSwitchCheck()` gate at top of OnTick; framework MAX_DD/loss caps
  bind via `QM_FrameworkInit`.

### Focused verification

- `validate_spec_doc.py` → **PASS**.
- `validate_symbol_scope.py --fail-on-leak` → `SINGLE_SYMBOL_OK`, 0 violations.
- `validate_build_guardrails.py` (mq5) → PASS, `max_news_stale_hours=336`.
- `build_check.ps1 -EALabel ... -Strict -SkipCompile` → PASS, 0 failures / 0 warnings
  (`D:/QM/reports/framework/21/build_check_20260821_174045.json`).
- `skill_build_ea_guard.py` → status ok (registry + magic + dir all true).

**Verdict: APPROVED for review_ea.** Pipeline verdicts remain the factory's to render.

---

## QM5_12930_classical-ascending-triangle-breakout-h4

### Reviewed identity

- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12930_classical-ascending-triangle-breakout-h4.md`
- MQ5 SHA-256: `f158e852cd23e0c17e37cbbb23e51fa5baa28a5f9b1c5c658160af3cd851121a`
- EX5 SHA-256: `26fbeeee3bab83724f775eae9c8addefe774757cb3ce3bb3f7668274ccbf8d49`
- EX5 mtime 2026-08-21 18:10 > MQ5 mtime 18:09 — binary is not stale against the source.

### Card fidelity

Bullish-only ascending-triangle breakout. The five closed-form structural gates all map
faithfully to the card:

1. Horizontal-resistance: two confirmed fractal highs within `0.50×ATR`, 8–60 bars apart,
   inside the 80-bar lookback (`Strategy_FindAscendingTriangle`).
2. Ascending-support: `recent_low > older_low + 0.50×ATR` on collected fractal lows
   between R2 and now.
3. Slope: best-fit forward-time regression slope in `[0.10, 0.50]×ATR/bar`
   (`Strategy_SupportRegressionSlope`; sign checked — rising lows give positive slope).
4. Pattern-bars: oldest boundary pivot to current ∈ `[25, 80]`.
5. Range-contraction: recent low above the 50% midpoint of the support→resistance range.

Entry `close[1] > R-level + 0.50×ATR` with `R-level = avg(R1,R2)`; optional macro-bias
`close > SMA200` (default ON) and optional 1.2× volume confirmation (default OFF) are
both wired. TP = `entry + (resistance − older_support)` (measured-move projection). SL =
`support_at_entry − 0.50×ATR` with a `3.0×ATR` worst-case floor from entry — matches the
card's projected-support stop and wider-than-harmonic cap. Time stop 60 bars
(`QM_EXIT_TIME_STOP`), 30-bar reuse guard, and spread filter all present.
`Strategy_ManageOpenPosition` is empty — no trailing/partial/break-even. The card's
optional P3 failed-breakout reverse-short is correctly NOT exposed (card omits its
reverse-side SL/TP), matching the SPEC's open-question note. No invented mechanics.

### Input-use audit

All 26 strategy inputs grep to a real use site (counts 2–20, each > declaration):
`strategy_tf`, `strategy_atr_period`, `strategy_fractal_wing_bars`,
`strategy_pivot_scan_bars`, `strategy_resistance_lookback_bars`,
`strategy_resistance_min_separation`, `strategy_resistance_max_separation`,
`strategy_resistance_tolerance_atr`, `strategy_support_min_rise_atr`,
`strategy_support_slope_min_atr`, `strategy_support_slope_max_atr`,
`strategy_pattern_min_bars`, `strategy_pattern_max_bars`,
`strategy_min_contraction_fraction`, `strategy_breakout_buffer_atr`,
`strategy_volume_confirmation`, `strategy_volume_lookback_bars`,
`strategy_volume_multiplier`, `strategy_macro_bias_enabled`, `strategy_macro_sma_period`,
`strategy_initial_sl_buffer_atr`, `strategy_initial_sl_cap_atr`, `strategy_time_stop_bars`,
`strategy_reuse_guard_bars`, `strategy_spread_lookback_bars`,
`strategy_spread_average_multiplier`. No QM5_1355-class unwired input.

### Host / magic / risk / time controls

- `ea_id_registry.csv`: `12930,classical-ascending-triangle-breakout-h4,...,active`.
- `magic_numbers.csv`: 13 active slots 0–12, magic `129300000+slot`.
- SPEC "Registered P2 surface" declares **13** symbols; **13** `.set` files shipped;
  **13** magic slots — consistent, no count mismatch.
- All 13 backtest setfiles: `RISK_FIXED=1000`, `RISK_PERCENT=0`, `PORTFOLIO_WEIGHT=1`,
  correct per-symbol magic slot, `strategy_tf=16388` (PERIOD_H4), ENV=backtest. News is
  the compiled default (`qm_news_stale_max_hours=336`, DXZ, PRE30/POST30, high) — no
  setfile weakening.
- `Strategy_NoTradeFilter` fail-closes unless `_Period == strategy_tf == PERIOD_H4`, so
  the H4 contract is enforced. No custom DST math; broker time via framework helpers.
- Loss limits: `QM_KillSwitchCheck()` at OnTick top; framework caps via `QM_FrameworkInit`.

### Focused verification

- `validate_spec_doc.py` → **FAIL** — see defect below.
- `validate_symbol_scope.py --fail-on-leak` → `SINGLE_SYMBOL_OK`, 0 violations.
- `validate_build_guardrails.py` (mq5) → PASS, `max_news_stale_hours=336`.
- `build_check.ps1 -EALabel ... -Strict -SkipCompile` → PASS, 0 failures, **1 advisory
  warning** (`D:/QM/reports/framework/21/build_check_20260821_174057.json`).
- `skill_build_ea_guard.py` → status ok.

**Checked advisory (not a defect):** `BUILD_CHECK_DWX_ADVISORY_DWX_SPREAD_ZERO_BLOCK` at
`:129` flags the `if(current_spread <= 0)` line in `Strategy_SpreadAllowsEntry`. Reading
the body, that branch `return true;` — it ALLOWS entry on a zero/degenerate `.DWX` spread
and only blocks a genuinely wide spread (`current_spread > mult × average`); the sample
loop also skips zero samples. The advisory is a pattern-match false positive here; the
guard already does the safe thing. No fix required.

**DEFECT (RECYCLE) — SPEC.md is the wrong template.** `validate_spec_doc.py` FAILs 12930
on every required section:

```
- missing required section: ## 1. Strategy Logic
- missing required section: ## 2. Parameters
- missing required section: ## 3. Symbol Universe
- missing required section: ## 4. Timeframe
- missing required section: ## 5. Expected Behaviour
- missing required section: ## 6. Source Citation
- missing required section: ## 7. Risk Model
- SPEC.md does not declare `**EA ID:** QM5_NNNN`
```

The shipped `SPEC.md` uses an obsolete free-form "Framework alignment" layout rather than
the canonical numbered schema that 12945's SPEC (which PASSes) uses. This is a
deliverable-completeness defect the task explicitly gates on, so 12930 cannot close
review_ea as APPROVED. It is doc-only and RECYCLE-fixable by Codex: rewrite `SPEC.md` to
the canonical template (declare `**EA ID:** QM5_12930`, add the seven required sections;
mirror 12945's SPEC structure, carrying over the existing card-rule→implementation table,
the 13-symbol universe, H4 timeframe, and RISK_FIXED backtest risk model). No `.mq5`,
`.ex5`, setfile, or registry change is required.

**Verdict: RECYCLE.** Source and controls are clean and card-faithful; the block is the
non-conformant SPEC.md.

---

## Summary line for the router

Both source-faithful/controls-clean; 12945 all-validators-clean → APPROVED; 12930 source
clean but validate_spec_doc FAIL (SPEC.md wrong template, all 7 sections missing) →
RECYCLE, doc-only fix.

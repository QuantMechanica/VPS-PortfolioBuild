# QM5_1606 Build Evidence — Source Ready, Governed Compile Held

- Task: `5ce8d642-5907-4715-bb84-7c3b8a18e792` (`build_ea`, priority 50, assigned to Codex)
- EA: `QM5_1606_aa-gvmt-robust`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1606_aa-gvmt-robust.md`
- Current MQ5 SHA-256: `c19d4ff735c6ae7b386f1c5ab5342bb14ebae09474fb9a27795a2b823fa49c87`
- Outcome: `SOURCE_READY_COMPILE_HELD`

## Governed identity

The card declares `g0_status: APPROVED`, `ea_id: QM5_1606`, and slug `aa-gvmt-robust`. The active EA registry row matches that identity. Thirteen active magic rows exist for slots 0-12: GDAXI, NDX, SP500, UK100, WS30, XAUUSD, EURUSD, GBPUSD, USDJPY, USDCHF, AUDUSD, USDCAD, and NZDUSD, all with the `.DWX` suffix.

## Card implementation

The source implements the approved monthly 50/50 trend blend on a D1 chart:

- one calendar-month rebalance review driven by the framework's single D1 closed-bar gate;
- completed-MN1 12-month return and completed-MN1 12-month SMA signals;
- 100% risk-budget exposure when both are positive, 50% when one is positive, and cash when neither is positive;
- close/reopen only when the persisted position-comment exposure differs from the new target;
- 3.0 x ATR(20,D1) initial stop, monthly signal as the primary exit;
- standard framework news gate, Friday liquidation disabled because it would contradict a monthly hold;
- `RISK_FIXED=1000`, `RISK_PERCENT=0`, `qm_news_stale_max_hours=336`, one position per symbol/magic, no ML, grid, or martingale.

Framework alignment is explicit: `Strategy_NoTradeFilter` enforces D1, `Strategy_EntrySignal` owns the monthly allocation transition, `Strategy_ManageOpenPosition` is intentionally empty beyond the broker-held stop, `Strategy_ExitSignal` is passive because the monthly close occurs atomically in the rebalance hook, and `Strategy_NewsFilterHook` delegates to the framework. `OnTick` contains the current explicit MAE hook and zero-initializes `QM_EntryRequest`.

## Focused verification

- `validate_spec_doc.py`: PASS (1/1).
- `validate_build_guardrails.py`: PASS; 14 files checked, zero findings, stale-news ceiling 336 hours.
- `validate_symbol_scope.py --fail-on-leak`: `SINGLE_SYMBOL_OK`, zero violations.
- Thirteen D1 backtest presets regenerated by canonical `gen_setfile.ps1`.
- Every preset contains `RISK_FIXED=1000`, `RISK_PERCENT=0`, the five strategy defaults, and its governed magic slot.
- Preset `build_hash` remains `pending`, correctly awaiting a successful governed build check.
- `git diff --check`: clean for the EA package.

## Compile boundary

Direct `build_check.ps1` stopped before compilation with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because terminal64 workers are alive. No bypass, retry, process stop, terminal launch, or backtest interruption was attempted.

The sanctioned command `farmctl.py enqueue-compile QM5_1606_aa-gvmt-robust` was then refused without enqueueing a row: reasons `EX5_ALREADY_PRESENT` and `WORK_ITEMS_EXIST`. The two existing governed compile rows are terminal failures (`1eadbb57-0fd7-420a-8477-89c2939a797c` INVALID and `91a8d1a3-3ab3-4261-8109-234d9edb2e9a` COMPILE_FAIL), not reusable open work.

The on-disk `.ex5` belongs to the earlier source (`ef729779...` in the governed compile evidence), not the current source SHA above. It is therefore stale and is not claimed as this build's binary. Smoke was not run, and no pipeline verdict is claimed.

## Required next governed action

A sanctioned compile-row supersede/rebuild authorization must admit a fresh row bound to the current source. Its worker must produce strict compile and build-check PASS evidence, bind final setfile hashes, and only then permit smoke/review closure.

Short verdict: `SOURCE_READY_COMPILE_HELD: static gates PASS; fresh governed compile refused by stale EX5/prior work-item guards.`

# rework-1425 review-fix evidence

- Router task: `c5811f96-bf23-490f-a8e5-2887606d16a7`
- EA: `QM5_1425_classical-triple-bottom-reversal-h4`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1425_classical-triple-bottom-reversal-h4.md`
- Review work order: `docs/ops/evidence/c5811f96_qm5_1425_gemini_build_codex_review_2026-08-22.md`
- Repaired MQ5 SHA-256: `45c2da17808f38def24b158d4fe7c517a77cafab02491ca759cc05b8bfb6d408`
- Scope: build/review only. No backtest enqueue/delete, verdict overwrite, factory toggle, gate-contract change, registry mutation, T_Live write, or AutoTrading action.

## What changed

1. The pivot producer now appends newest-to-oldest, filters lows through the mandatory 20-surrounding-bar / `0.5*ATR` significance rule, and consumes consecutive significant lows as T3/T2/T1 (`QM5_1425_classical-triple-bottom-reversal-h4.mq5:394`, `:435`, `:527`). This removes the deterministic impossible-ordering/zero-trade path.
2. Gate 3 counts pivot highs and requires exactly one in each trough interval before applying amplitude/equality/neckline tests (`QM5_1425_classical-triple-bottom-reversal-h4.mq5:557-581`).
3. Entry is now an actual `QM_BUY_STOP` at neckline plus `0.4*ATR`, with 12-H4-bar expiration, bounded stop-loss cap, pending-order invalidation, and all seven `QM_EntryRequest` fields set (`QM5_1425_classical-triple-bottom-reversal-h4.mq5:623-652`, `:680`). Pattern state is committed only after `QM_TM_OpenPosition` returns success (`:891-892`).
4. D1 bias reads the pooled framework SMA at explicit shifts 1 and 2 and accepts flat/rising only (`QM5_1425_classical-triple-bottom-reversal-h4.mq5:358-369`). The exact card news window is a fail-closed entry-only `480/480` minute gate, after management/exit remain reachable (`:775-807`, `:851-884`).
5. Pending/active setup fields and the 40-bar reuse timestamp are persisted by magic in terminal global variables and restored fail-closed across restart (`QM5_1425_classical-triple-bottom-reversal-h4.mq5:155-192`, `:256-287`). Partial-close completion is persisted after acceptance.
6. The runtime now declares the H4 execution contract and the framework-policy Friday close override (`QM5_1425_classical-triple-bottom-reversal-h4.mq5:825-828`). The MAE hook remains the first `OnTick` action (`:851`). Raw indicator handles/`CopyBuffer` were replaced with pooled `QM_ATR`/`QM_SMA`; the single bounded structural `CopyRates` call carries the reviewed `perf-allowed` marker and an `ArraySize` guard.
7. All 14 backtest setfiles remain `RISK_FIXED=1000`, `RISK_PERCENT=0`, retain their existing allocated magic slots, advance to set version `s20260824-002`, and bind the repaired MQ5 hash. The approved one-row EA registry and 14-row magic allocation were verified and not modified.
8. Six focused static regressions cover the repaired mechanics and every declared `strategy_*` input use-site (`tools/strategy_farm/tests/test_qm5_1425_review_rework_static.py:31-120`). The prior hardening fixture now treats repaired 1425 as clean while retaining unrepaired 1417 as the failing control (`tools/strategy_farm/tests/test_build_gate_hardening.py:696-715`). SPEC revision v2 records the review repair (`framework/EAs/QM5_1425_classical-triple-bottom-reversal-h4/SPEC.md:140`).

## Validation evidence

### PASS

```text
python -m pytest -q \
  tools/strategy_farm/tests/test_qm5_1425_review_rework_static.py \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_semantic_regression_accepts_repaired_1425 \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_semantic_regression_catches_unrepaired_1417 \
  tools/strategy_farm/tests/test_build_guardrails.py

............................ [100%]
28 passed in 1.45s
```

```text
python tools/strategy_farm/build_gate_hardening.py \
  --repo-root . --ea-label QM5_1425_classical-triple-bottom-reversal-h4

files_scanned=1 failures=0 warnings=1 symbols_observed=14
```

The one warning is `EA_CARD_SMA_DIRECTION_UNDECIDABLE`: the generic checker only recognizes a raw two-element `CopyBuffer` shape. This EA intentionally uses the framework `QM_SMA` helper; focused test `test_macro_bias_and_news_window_match_the_card` proves shift 1 is compared `>=` shift 2.

```text
python framework/scripts/skill_build_ea_guard.py --ea-id 1425 \
  --ea-label QM5_1425_classical-triple-bottom-reversal-h4
status=ok; ea_registry_row=true; magic_registry_rows=true; ea_dir_exists=true

python tools/strategy_farm/validate_build_guardrails.py \
  --max-news-stale-hours 336 framework/EAs/QM5_1425_classical-triple-bottom-reversal-h4
verdict=PASS; files_checked=15; findings=[]

python tools/strategy_farm/validate_symbol_scope.py \
  --ea-label QM5_1425_classical-triple-bottom-reversal-h4 --json --fail-on-leak
verdict=SINGLE_SYMBOL_OK; n_violations=0

python framework/scripts/validate_spec_doc.py \
  framework/EAs/QM5_1425_classical-triple-bottom-reversal-h4
Summary: 1 PASS, 0 FAIL
```

Hash binding check:

```text
source_hash=45c2da17808f38def24b158d4fe7c517a77cafab02491ca759cc05b8bfb6d408
setfile_unique_hashes=45c2da17808f38def24b158d4fe7c517a77cafab02491ca759cc05b8bfb6d408
setfile_count=14
```

### Full touched hardening-module run

```text
python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py
1 failed, 29 passed in 113.81s
```

The sole failure is outside this ticket: existing `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599` and six sibling findings violate `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`. The repaired-1425 regression passed in that run. No QM5_411xx file was changed.

### Governed compile/build-check interlock

Both target-scoped attempts were made and refused before MetaEditor ran because live `terminal64` processes were active:

```text
powershell -File framework/scripts/build_check.ps1 \
  -EALabel QM5_1425_classical-triple-bottom-reversal-h4
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED

powershell -File framework/scripts/compile_one.ps1 \
  -EAPath framework/EAs/QM5_1425_classical-triple-bottom-reversal-h4/QM5_1425_classical-triple-bottom-reversal-h4.mq5 \
  -EALabel QM5_1425_classical-triple-bottom-reversal-h4 -Strict
compile_one.result=FAIL
compile_one.reason_class=INCLUDE_MIRROR_REFUSED
```

Receipt: `D:/QM/reports/compile/20260824_072735/summary.csv`. The guard explicitly required a governed compile work item; this ticket forbids enqueueing one. No terminal was stopped, no factory state was changed, and T_Live was not touched. The prior EX5 was preserved byte-for-byte at SHA-256 `7cae1f96a30910f832df2d726e89205d714f26edd1b1eaaf0cf9b8a3e60fc51d`; it is therefore stale relative to the repaired source and must not be promoted as the repaired binary.

## Risks and review condition

- Source/card semantics and static framework gates are repaired and tested.
- A fresh EX5/0-error/0-warning compile is not claimed. Review must keep the build fail-closed until the governed compile lane produces a binary for MQ5 hash `45c2da17...d408`.
- No backtest was run or enqueued, so this evidence makes no economic or trade-cadence claim.

## Rollback

Revert the ticket commit. This restores the previous MQ5, SPEC, hardening fixture, and 14 setfile metadata/hash bindings. The old EX5 was never overwritten and needs no binary rollback. Router state can be returned from REVIEW to RECYCLE with a new append-only review note; do not alter historical verdict rows.

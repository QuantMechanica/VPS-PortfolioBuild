# Review — QM5_9947 bandy-double-bottom-formalised-mr-index (review_ea 09c5ed91)

- Router task: `09c5ed91-8203-45ba-b2f3-03431f022946` (state REVIEW, assigned codex; source_agent gemini/agy)
- Build task: `7f5be227-52c7-4a57-a68a-a1ca31ffbd01` — verdict "compiled cleanly, awaiting Codex review"
- Reviewer: Claude review lane (independent). Prior codex review artifact:
  `docs/ops/evidence/2026-08-23_codex_review_09c5ed91_QM5_9947.json` (verdict FAIL).
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9947_bandy-double-bottom-formalised-mr-index.md`
- Source: `framework/EAs/QM5_9947_bandy-double-bottom-formalised-mr-index/QM5_9947_...mq5`
  sha256 `9f5af6df4dc21f756f732127d422ebc206ba17d6c7b250c4c84d55a926790c43` (matches build identity json).

## VERDICT: RECYCLE

Named, actionable defects. Framework skeleton is sound and no dead inputs, but the EA
diverges from the approved card on exit timing/semantics and lacks clean compile evidence
for the current hash. Independent review confirms the substantive codex findings.

## What passes
- **Framework corset**: `#include <QM/QM_Common.mqh>`; `QM_FrameworkInit` OnInit; MAE hook
  `QM_FrameworkTrackOpenPositionMae()` (mq5:301); kill-switch (303); Friday-close (309);
  news wired via `QM_NewsAllowsTrade2`/`QM_NewsAllowsTrade` (331-335); new-bar gate (339);
  `QM_FrameworkShutdown` OnDeinit. No ML, no raw-series abuse — `CopyRates` is bounded
  (`need_bars` = scan+lookback+5) and carries a `// perf-allowed:` justification (mq5:125).
- **Risk mode**: `RISK_FIXED=1000` backtest, `RISK_PERCENT` live path present (mq5:13-14).
- **Magic**: `QM_FrameworkMagic()`; registry `framework/registry/magic_numbers.csv` has 13
  active `9947` rows (slots 0-12, `ea_id*10000+slot`).
- **Dead-input check**: all 12 `strategy_*` inputs have use-sites beyond declaration
  (grep counts 3-8 each). No dead inputs.

## Defects (actionable)
1. **Time stop is wall-clock, not bar-count** (mq5:258). Card §Exit: "20 trading days from
   entry". Code: `TimeCurrent()-opened >= strategy_max_hold_bars * PeriodSeconds(D1)` =
   20 *calendar* days ≈ 14 D1 trading bars — closes ~6 bars early across weekends. Fix to a
   completed-bar count since entry.
2. **Target installed as intrabar broker TP** (mq5:220,227). Card §Exit Target: "When
   `high >= target_price`, close at next bar's open." Broker `req.tp` closes intrabar at the
   TP price, changing exit timing and realized price vs the card's next-bar-open model.
3. **Stale-breakout / restart re-entry guard is a function-static** (mq5:103,172,232).
   `last_fired_p2_time` resets on EA restart (enables late re-entry into an already-broken
   pattern) and is set *before* `QM_TM_OpenPosition` reports success (mq5:349), suppressing a
   legitimate retry after broker rejection. Card entry item 2 frames "today is the breakout-
   confirmation bar"; code fires on any `close>neckline` for an unfired P2 (mq5:194), not a
   fresh cross. Low severity for a clean backtest run but a real logic gap.
4. **Setfiles not regenerated against strategy defaults** — all 13
   `sets/*_D1_backtest.set` carry `card_defaults_status=none_found` and zero `strategy_*`
   lines. Q02 baseline still runs on the compiled input defaults (which match the card), so
   not fatal for the first pass, but the governed generator did not bind the card defaults;
   P3 sweep inputs are absent.
5. **No clean compile evidence for the current hash.** The only compile log,
   `framework/build/compile/20260822_124721/QM5_9947_...compile.log`, shows **78 errors**
   (older broken source, `,000` token). The `.ex5` and build-identity json
   (`docs/ops/evidence/7f5be227_..._build_identity.json`, `build_check_passed:true`) are not
   the canonical `build_result.json` schema (missing ea_dir, magic_base, symbols_registered,
   compile_succeeded, smoke_result, smoke_report_path) and bind no compile log or smoke
   report. COMPILE_OK for the current source hash is unsubstantiated.
6. **SPEC.md corruption** — `SPEC.md:4` slug `andy-double-bottom-...` (leading `b` dropped);
   `SPEC.md:92` backtest risk `,000 per trade` (value stripped). Cosmetic but a generation
   defect that slipped the section-header validator.

## Recommended next step
Return to build lane: fix (1)-(3) in source, regenerate the 13 setfiles via the governed
generator with bound card defaults, re-run build_check to emit a canonical build_result.json
+ compile log (COMPILE_OK) for the new hash, and repair SPEC.md slug/risk. Then re-review.

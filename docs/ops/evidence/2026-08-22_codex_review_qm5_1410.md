# Codex review — QM5_1410 — 2026-08-22

Router task: `ee84a2b6-1d7a-4cbf-b7bd-393469e9a8be`
Gemini task: `c1850502-ac54-4fc4-b397-5e54e87d1eb6`

Verdict: **RECYCLE**. The artifact hashes match the current MQ5 and EX5, but
binary identity is not acceptance and the build fails mandatory source review.

## Findings

1. Canonical strict hardening reports eight failures: missing direct D7 MAE hook,
   a D9 `QM_EntryRequest` whose `expiration_seconds` is uninitialized, and six
   D10 unbounded indicator-buffer accesses (lines 166, 199, 211, 234, 265, 380).
2. The approved card requires a high-impact-news blackout of ±2 H4 bars. The EA
   wires only `QM_NEWS_TEMPORAL_PRE30_POST30`; there is no card-equivalent
   eight-hour blackout implementation.
3. TP1 lifecycle state (`g_tp1_done`) is process-local and is not reconstructed
   for an already-open position after restart. A restart above TP1 can therefore
   repeat the 50% partial-close action.
4. The card's MAE-adjusted 4 ATR gain-cap is implemented as a static initial
   price TP (`entry ± 4*ATR`), which is not the stated MAE-adjusted exit mechanic.

## Verified identity and safe contracts

- MQ5 SHA-256 matches artifact:
  `b03a04ce49e412bdecfaa06f383130cc6b93c02ae8b8d80bb2d835335c88ce96`.
- EX5 SHA-256 matches artifact:
  `4b539331592b0a65e507bf61baf5b1343aa722d7c0983f743724759eaabb764f`.
- Representative backtest set uses `RISK_FIXED=1000`, `RISK_PERCENT=0`.
- `qm_news_stale_max_hours` remains 336.

No pipeline verdict is inferred, and this Gemini output is not approved or
advanced. Repair must be followed by strict hardening and governed recompile.

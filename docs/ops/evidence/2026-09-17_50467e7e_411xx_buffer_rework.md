# QM5_411xx indicator-buffer recurrence rework

- Router task: `50467e7e-3148-4792-9223-083bd8ee2516`
- Result: `STATIC_FAMILY_CLEAN_COMPILE_SUCCESSORS_BOUND`
- Pipeline verdict: none; this is build/queue evidence only.

## Systematic guard

The build path now invokes the mechanical hardening checker from
`framework/scripts/build_check.ps1:1380`. The detector is implemented by
`tools/strategy_farm/build_gate_hardening.py:915` and its findings are included
in the build result at line 2121. Generation is therefore no longer protected
only by advisory prompt text.

Focused tests against the current canonical checkout passed:

```text
python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py::test_qm5_411xx_sources_have_no_unbounded_numeric_buffers tools/strategy_farm/tests/test_buffer_round3_proof.py -q
31 passed in 34.97s
```

The five recurrences named by review now carry explicit fail-closed bounds at
the formerly unsafe access sites:

- QM5_41134: `daily_returns[first_retained]` is preceded by `first_retained >= ArraySize(daily_returns)`.
- QM5_41135: each `sorted_returns[i]` access is bounded by `ArraySize(sorted_returns)`.
- QM5_41136: `daily_returns[first_retained]` and the retained loop are bounded.
- QM5_41138: `daily_returns[i]` and `pairwise_averages[pair_index]` are bounded.
- QM5_41139: `daily_returns[left]` and `pairwise_values[pair_index]` are bounded.

## Per-EA compile disposition

| EA | Current-source disposition | Work item |
|---|---|---|
| QM5_41134 | governed `COMPILE_OK`, build-check PASS | `0d2462f6-fd6a-4d3a-966d-9208044e672a` |
| QM5_41135 | append-only governed compile successor pending | `1e509ca3-c912-4670-8ba9-ea40602f142d` |
| QM5_41136 | governed `COMPILE_OK`, build-check PASS | `1b09c28f-d9c5-438e-9ccb-2203af021bda` |
| QM5_41138 | append-only governed compile successor pending | `3af0b6a2-8bbd-482c-b815-9c62b33d3485` |
| QM5_41139 | governed `COMPILE_OK`, build-check PASS | `055882a6-f067-4452-b6a9-267c8db78572` |

The two missing successors were enqueued through `farmctl enqueue-compile` and
are held by the normal `COMPILE_EA_WORKER_ROLLOUT_PENDING` activation gate.
No queue row or prior evidence was overwritten.

An attempted local scoped `build_check -EALabel ... -SkipCompile` was correctly
refused with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because terminal workers are
active. No terminal was stopped and no ad-hoc compile was attempted. The three
completed rows provide pipeline-issued build evidence; the other two remain
pending for the governed worker.

## Safety

No thresholds, setfiles, risk controls, news-staleness limits, live settings,
or pipeline verdicts were changed. T1-T10, T_Live, and AutoTrading were not
altered.

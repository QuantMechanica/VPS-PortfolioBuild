# rework-1405 — card-defect blocker evidence

**Router task:** `f2b8b86c-717d-4081-87c6-4a60ab093c09`

**EA:** `QM5_1405_as-rpv-bestvalue`

**Disposition:** `BLOCKED_CARD_DEFECT`

**Scope:** build/review only; no backtest enqueue, verdict overwrite, factory change, or live mutation

## Outcome

No card-faithful EA repair can be made from the approved artifacts currently available. The candidate must remain blocked for upstream card/data adjudication. The EA source, EX5, setfiles, and existing registry allocations were deliberately left unchanged: replacing the rejected price proxies requires new deterministic point-in-time data lineage and tradable sleeve mappings that Development is not authorized to invent.

## Evidence

### Approved card is not implementation-complete

The approved runtime card is:

`D:/QM/strategy_farm/artifacts/cards_approved/QM5_1405_as-rpv-bestvalue.md`

- Line 15 says `g0_status: APPROVED`, and lines 20-21 claim R3 PASS while also saying that Treasury, credit-spread, and earnings-yield inputs require external injection.
- Lines 46-48 require long- and short-term Treasury yields, investment-grade corporate-bond yield, and a four-month-lagged S&P 500 earnings yield.
- Lines 65-68 require expanding history, approved external/custom-symbol inputs, deterministic TLT/LQD sleeve mappings, and limit target symbols to `SP500.DWX`, `NDX.DWX`, and `WS30.DWX`.
- Line 82 contradicts the front matter by recording R3 as `UNKNOWN` because the required data handling is not approved.

The repository's broker inventory independently records `US10Y` and `DE10Y` as missing and requires an external-data shim or deferral: `docs/ops/QUA-350_DARWINEX_BOND_INVENTORY_CHECK_2026-04-28.md:35-40,60`. The governed `framework/registry/dwx_symbol_matrix.csv` contains no Treasury, corporate-yield, earnings-yield, TLT, or LQD symbol.

The scoped hardening command also reports D12 as non-mechanizable because the card lacks `required_data_inputs[]` declarations with canonical identifiers, source kind, point-in-time/as-of lag, allowed transforms, and proxy policy.

### Existing EA implements a different strategy

Source: `framework/EAs/QM5_1405_as-rpv-bestvalue/QM5_1405_as-rpv-bestvalue.mq5`

- Lines 154-171 read only the chart instrument and manufacture `earnings_yield_proxy`, `t_yield_proxy`, and `corp_yield_proxy`; none is authorized by the card.
- Line 137 caps normalization at 120 months instead of using the required expanding history.
- Lines 204-207 reduce the three-way winning-sleeve rule to equity versus cash; Treasury and corporate winners are not allocated.
- Lines 112-126 calculate an arithmetic mean although the declared filter is a spread median.
- Declared-input use-site audit found `strategy_earnings_lag_months` and `strategy_rebalance_hour` only once each—the declarations at lines 40-41—so neither input is wired.
- The MAE hook and V5 framework chain do exist (`QM_FrameworkInit` at line 282 and `QM_FrameworkTrackOpenPositionMae` at line 312), but structural conformity cannot cure the strategy-identity defect.

The generated spec compounds the mismatch: `framework/EAs/QM5_1405_as-rpv-bestvalue/SPEC.md:47-54` adds European indices, gold, and FX to the card's three-symbol universe.

### Registry state

- `framework/registry/ea_id_registry.csv:289` has the active `1405,as-rpv-bestvalue` row.
- `framework/registry/magic_numbers.csv:16460-16472` has existing active allocations. There is no registry gap, so no registry or resolver file was changed.
- The scoped hardening result found ten symbols outside the explicit card universe. Existing allocations were not removed because the ticket forbids changing allocations that already exist.

## Validation output

### Scoped hardening

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_1405_as-rpv-bestvalue
result: semantic/build FAIL evidence
files_scanned: 1
D17: 10 EA_SYMBOL_NOT_IN_CARD_UNIVERSE failures
D12: mechanizable=false; required point-in-time data-lineage declaration absent
```

### Scoped build check

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_1405_as-rpv-bestvalue -Strict -SkipCompile
result: BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED
reason: terminal64 processes are alive; governed interlock refused ad-hoc validation
```

No bypass or compile enqueue was attempted because this task forbids factory/queue mutation and the card defect prevents a valid build identity.

### Pytest

```text
python -m pytest -q \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_d17_explicit_card_universe_pass_and_extra_symbol_fail \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_nonmechanizable_semantic_classes_name_required_card_declarations \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_build_check_applies_hardening_pass_and_fail_fixtures
3 passed in 3.32s
```

A broader focused run of `test_build_gate_hardening.py`, `test_build_guardrails.py`, `test_magic_resolver_reconcile_newlines.py`, and `test_review_repair.py` produced `57 passed, 1 failed`. The single failure is the repository-wide pre-existing `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` assertion against `QM5_41134_wti-mdaily-iqrmean-mom.mq5`; neither that EA nor the hardening module is in this ticket's change set.

## Required upstream resolution

Before Development can resubmit this EA, the OWNER/data owner must provide and approve:

1. Canonical point-in-time series identifiers and provenance for long Treasury yield, short Treasury yield, investment-grade corporate yield, and S&P 500 earnings yield.
2. The exact four-month earnings publication-lag/as-of contract and allowed transforms.
3. Exact tradable/custom-symbol mappings for Treasury, corporate-bond, equity, and cash sleeves.
4. A resolved R3 verdict and a card revision that removes the current PASS/UNKNOWN contradiction.
5. A new governed build/rework task bound to that revised card.

## Rollback

This commit adds evidence only. Roll back by reverting this evidence file. No EA, binary, setfile, registry, resolver, gate criterion, verdict row, queue, factory, or live artifact was changed. After upstream adjudication, the router task can be reopened with the revised approved card as its artifact.

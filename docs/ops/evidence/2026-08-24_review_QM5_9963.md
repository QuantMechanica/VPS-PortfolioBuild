# Review — QM5_9963 bandy-lr-slope-sign-flip-trend (router task 8b3cc484)

**Reviewer:** Claude (review lane) · **Date:** 2026-08-24
**Task:** 8b3cc484-dc8e-494d-a3b8-3a5d0d8e5e56 (build_ea, codex, REVIEW)
**Verdict: APPROVED (clean for Q02)**

## Worker deliverable
`docs/ops/evidence/8b3cc484_qm5_9963_bandy-lr-slope-sign-flip-trend_build_result.json`
- `compile_succeeded: true`, `build_check_passed: true`
- mq5_sha256 `734cb123…069e` = current source (verified); ex5_sha `0a0f072d…0c2b` = current .ex5
- Compile log `framework/build/compile/20260823_083511/…compile.log`: **0 errors, 0 warnings**
- build_check `D:/QM/reports/framework/21/build_check_20260823_083404.json`: **status PASS**, warnings []
- `smoke_result: framework_error` — Q01 dispatch refused by custom-history isolation before
  terminal launch (needs worker-bound privatized work item). Infra/Q01 dispatch matter,
  **not a build defect**; Q02 is the real judge.

## Card fidelity (card: cards_approved/QM5_9963…md, SHA per SPEC)
Mechanism matches card exactly: closed-form OLS slope over 20 D1 closes with x=-i convention
(`Strategy_OLSSlope`, base_shift 1 vs 2 for curr/prev); flip_up/flip_down definitions identical
to card; SMA(200) regime gate; long flip_up & close>regime / short flip_down & close<regime;
min |slope| >= 0.05*ATR(14) noise filter; exit on opposite flip or 45 completed bars; server
cat-SL 2.5*ATR(14); one position per magic.

## Every declared strategy input has use-sites (mq5)
- strategy_timeframe (11×), strategy_slope_window (5×), strategy_regime_sma_period (3×),
  strategy_min_slope_atr_mult (3×), strategy_atr_period (3×), strategy_stop_atr_mult (3×),
  strategy_max_hold_bars (4×). **No dead inputs.**

## Framework conformity
- QM_Common include; QM_FrameworkInit/Shutdown; MAE hook `QM_FrameworkTrackOpenPositionMae()`;
  QM_KillSwitchCheck; Friday close; magic via `QM_FrameworkMagic()` (MagicResolver).
- News filter wired (QM_NewsAllowsTrade2 temporal/compliance path).
- Bounded buffers only (QM_ReadBar/QM_ATR/QM_SMA + slope loop bounded by window); no raw series,
  no ML, no invented commission/swap.
- Setfiles governed: 13 backtest .set (build_hash, RISK_FIXED=1000, RISK_PERCENT=0,
  strategy_timeframe=16408=D1, all card defaults). 13 magic rows active in magic_numbers.csv
  (99630000–99630012) matching 13 setfiles; ea_id_registry active.
- XTIUSD correctly EXCLUDED: no active QM5_9963 oil magic row — SPEC refuses ad-hoc allocation
  (governed-allocation-required), the correct behavior.

## Note
This build closes the prior 2026-07-19 RECYCLE (dir then missing .ex5/SPEC.md); all artifacts
now present and consistent.

**Close: APPROVED** — artifact clean for Q02.

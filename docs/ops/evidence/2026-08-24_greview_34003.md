# greview-34003 — independent build review

- Ticket: `greview-34003`
- Router task: `d5a96505-0d9a-4dfa-8b59-119e6b114d61`
- Builder lane: `agy/gemini`
- Reviewer lane: `Codex` (cross-review)
- EA: `QM5_34003_triple-timeframe-williams-r-champion`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_34003_triple-timeframe-williams-r-champion.md`
- Reviewed source SHA-256: `d276dac1a17a0bb646d2e6cf20b2a0c171cf1053b2b6cd60f644aac5c2ee566b`
- Delivered EX5 SHA-256: `5b4d5c197f621d2c5e987631ec1c285b1dc923fdc8255a3b443b35cce45d2a6a`
- Verdict: **RECYCLE**

## Scope and governing constraints

This was a read-only build review. No EA, card, registry, setfile, gate criterion, verdict row, backtest queue, factory state, terminal, or `T_Live` file was changed. The required rebaseline documents and OWNER vault directive were read before review. The only repository change made by this review is this evidence note; the prescribed router close is performed after its commit.

## Findings requiring recycle

1. **The authoritative hardening check fails four card-conformance gates.** Read-only command:

   ```text
   python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_34003_triple-timeframe-williams-r-champion
   ```

   It reports `EA_CARD_LOSS_LIMIT_MISMATCH` for the card's 2.0% daily realized-loss entry halt, 2.5% daily hard stop, and 5.0% total drawdown stop, plus `EA_BROKER_TIME_USED_FOR_GMT_WINDOW`. The requirements are explicit at card lines 86 and 109-112. The EA has no matching inputs or implementation (`.mq5:13-48`), uses raw `TimeCurrent()` for the card's GMT rollover window (`.mq5:54-70`), and delegates to framework defaults `QM_EntryConfigure(..., 20, ...)` and `QM_KillSwitchInit(..., 3.0, 0.0, 1.0)` (`framework/include/QM/QM_Common.mqh:296-298`), which are not the card's declared thresholds or 3-tick slippage cap. Named fix: implement the three exact card loss rails and a DST-safe broker-to-UTC conversion, and wire the card's 3-tick slippage contract without changing its criteria.

2. **The delivered binary is stale relative to the current source.** The current build artifact asserts the reviewed MQ5/EX5 hashes (`artifacts/builds/d5a96505-0d9a-4dfa-8b59-119e6b114d61.json:5-8`), but it contains no compile-log/build-identity binding. Git identity proves the hardening commit changed the source and not the binary:

   ```text
   c30 MQ5 blob  eb0add303cddff6eb4b02d20a5b8153d2a628d04
   a040 MQ5 blob c151d0c7eb60e40848c095c06f3ebea3823ab6d2
   c30 EX5 blob  4db639e968aae3f6560eb4f06b0dc882f315545f
   a040 EX5 blob 4db639e968aae3f6560eb4f06b0dc882f315545f
   git diff --name-status c30b529bf a040de2b -- <mq5> <ex5>
   M framework/EAs/QM5_34003_triple-timeframe-williams-r-champion/QM5_34003_triple-timeframe-williams-r-champion.mq5
   ```

   Commit `a040de2b58bbdc03aa26266fdba945300b820756` added the MAE call and corrected `OnTradKTransaction` to `OnTradeTransaction` (`.mq5:173-176,215-219`) while leaving the EX5 blob unchanged. The newest retained compile receipt is the pre-hardening `C:/QM/repo/framework/build/compile/20260817_192152/QM5_34003_triple-timeframe-williams-r-champion.compile.log` (SHA-256 `464b8eb7eca4272f60e7cd732a8704d184298302380fa5ab6a167472b46176b8`, `0 errors, 0 warnings`), six days before the source-changing commit. Named fix: **recompile via governed `COMPILE_EA`**, retain a compile/build-identity receipt binding the current MQ5 hash to the newly produced EX5 hash, and regenerate/rebind governed setfiles to that resulting EX5 hash.

3. **The central news filter suppresses mandatory lifecycle work.** The EA returns on news denial before Friday close, position management, and strategy exits (`.mq5:173-202`). The canonical framework order explicitly requires Friday liquidation and management/exits to remain active through news windows, with the central news gate blocking new entries only (`framework/templates/EA_Skeleton.mq5:173-226`). Named fix: move the central news gate below Friday close, management, and exits, immediately before the new-bar entry path.

4. **Indicator read failure can generate false signals.** `QM_WPR` delegates to `QM_IndicatorReadBuffer`, which returns `0.0` for an invalid handle or failed `CopyBuffer` (`framework/include/QM/QM_Indicators.mqh:281-288,811-815`). The EA does not establish readiness/success for its three WPR reads (`.mq5:103-108`). Thus a failed H4/H1 read satisfies the long comparisons at `.mq5:120` when M15 is valid and oversold; a failed M15 read can satisfy the short comparison at `.mq5:132` when H4/H1 are valid and bearish. Named fix: use bounded, readiness-checked indicator handles/reads and fail closed unless every required closed-bar value was successfully obtained.

5. **The card's protection state machine is unresolved in code.** The approved card depicts break-even and trailing-stop states (`card:116-152`), while `Strategy_ManageOpenPosition` and `Strategy_ExitSignal` are empty (`.mq5:146-153`). The card supplies no numeric BE/trailing triggers, so Development must not invent them. Named fix: obtain an approved card clarification defining the mechanics and implement them, or explicitly mark the diagram non-operative in the approved card before rebuild.

6. **The operator-facing SPEC remains corrupt.** Control characters mangle the slug/source/card paths and `$1,000` risk text (`SPEC.md:4-5,14,89-92,100`). The structural SPEC validator passes, but the document is not clean evidence. Named fix: replace the control characters with literal intended text and rerun the SPEC validator.

## Controls that passed

- **No dead declared inputs:** every `strategy_*` input has at least one executable use-site. Use-site lines are: `strategy_wpr_period` 103-105; H4/H1/M15 thresholds 120/132; `strategy_atr_period` 106; SL/TP multipliers 116-117; spread ATR period/multiplier 72/80. Framework inputs are likewise passed into `QM_FrameworkInit` or the entry request (`.mq5:93-94,127,139,163-166`).
- **Framework chain and MAE:** `QM_Common.mqh` is included at `.mq5:6`; `QM_FrameworkTrackOpenPositionMae()` is first in `OnTick` at `.mq5:175`; the corrected framework trade callback is at `.mq5:216-219`.
- **Magic:** active registry rows are `framework/registry/magic_numbers.csv:17377-17379`, slots 0-2 and magics 340030000-340030002. `QM_Magic` computes `ea_id * 10000 + symbol_slot` at `framework/include/QM/QM_MagicResolver.mqh:55`; `QM_FrameworkInit` resolves through `QM_MagicChecked` at `QM_Common.mqh:223-227`.
- **Backtest risk/setfiles:** three symbol-scoped M15 setfiles exist. Each sets `RISK_FIXED=1000`, `RISK_PERCENT=0`, the correct slot, and a build-hash header (`sets/*.set:3-20`). Their metadata shape is valid, but the hash binds the stale EX5 and no separate governed-generator receipt was supplied.
- **News configuration exists:** temporal/compliance/staleness inputs are wired into framework init and the central filter (`.mq5:23-28,163-166,180-185`); the defect is lifecycle placement, not absence.
- **Static exclusions:** no raw `CopyBuffer`/`CopyRates`/series access, commission/swap invention, ML, grid, martingale, or averaging code was found in the EA source. There are no EA-local dynamic indicator buffers requiring `ArraySize` guards.
- **Generic guardrails:** `python tools/strategy_farm/validate_build_guardrails.py <ea_dir>` returned `PASS`, 4 files checked, 0 findings, maximum news staleness 336 hours.
- **SPEC structure:** `python framework/scripts/validate_spec_doc.py <ea_dir>` returned `1 PASS, 0 FAIL`; this validator does not detect the control-character corruption above.

## Test evidence

```text
python -m pytest -q tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_gen_setfile.py
21 passed in 2.23s
```

The broader relevant run was also executed:

```text
python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_gen_setfile.py
1 failed, 50 passed in 355.62s
```

The sole failure is an unrelated repository-wide cohort assertion, `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`, reporting seven existing `QM5_411xx` findings (first at `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`). It does not involve QM5_34003 and does not reduce the four direct hardening failures reported for this EA.

## Verdict and closure line

**RECYCLE:** implement the exact card loss/slippage rails and UTC rollover, restore lifecycle-safe news ordering, make WPR reads fail closed, resolve the BE/trailing card ambiguity, sanitize SPEC, then **recompile via governed `COMPILE_EA`** and bind the current MQ5 to the resulting EX5/setfiles before resubmitting for cross-review.

## Rollback

Revert the review-note commit with `git revert <review-commit>` if this evidence file itself must be withdrawn. Any router-state correction must use an authorized router transition; do not edit or overwrite SQLite verdict rows directly. No EA/runtime rollback is needed because this review changed no EA or runtime artifact.

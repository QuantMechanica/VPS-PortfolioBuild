# Claude review — QM5_38008 CodeTrading Optimized Bollinger Trend Breakout

- Review task: `39dd5c0b-7157-403d-8f63-31117609e27f` (review_ea, closing = Claude lane duty)
- Source build task: `1f82ac59-1c1e-4e72-a1f8-4eaea2120347` (source_agent=gemini/agy, build_ea)
- EA: `framework/EAs/QM5_38008_codetrading-optimized-bollinger-trend-breakout`
- Approved card: `strategy-seeds/cards/approved/QM5_38008_codetrading-optimized-bollinger-trend-breakout.md`
- Reviewed MQ5 SHA-256: `5c8f45f1db08a51e886e0a16c192d15b9db589bd0dc531d04e0cc7649a9ea9b8` (matches build packet)
- Prior Codex review of same task: `docs/ops/evidence/39dd5c0b_qm5_38008_gemini_review_2026-08-23.md` (CHANGES_REQUIRED)
- **Verdict: RECYCLE** (named actionable defects; not clean for Q02)

## Card-faithful mechanism — PASS
Source implements the approved card: BB(20, 2.50σ) breakout, EMA(200)[1] vs EMA(200)[5]
trend filter, `Close[1]>Open[1]` confirmation, 2×ATR(14) stop, open-ended TP (`req.tp=0.0`)
with SMA(20) midline trailing exit, spread filter 1.8×ATR, daily-loss halt 2.0%, max 1 open
position. Break-even lock (+1R) is present in source and is supported by card §5 state machine
(`STATE_PROTECTED_BE`, SL→Entry+1) — do NOT strip it; reconcile with card §3.4 wording instead.

## Dead-input scan — PASS
All 17 `strategy_*` inputs grepped for use-sites beyond declaration; every one has ≥1
use-site (`grep -c` ≥2 each). No dead inputs.

## Framework conformity — PASS
`#include <QM/QM_Common.mqh>`; MAE hook `QM_FrameworkTrackOpenPositionMae()` in OnTick;
RISK_PERCENT=0 / RISK_FIXED=1000 (setfiles confirm all three); magic via
`QM_FrameworkMagic()` + regenerated `QM_MagicResolver.mqh` (380080000/1/2); news wired via
`QM_NewsAllowsTrade2`; kill switch wired (`QM_KillSwitchInit`); bounded indicator helpers
(`QM_BB_*`, `QM_EMA`, `QM_ATR`); raw series only shift-1 closed-bar, marked perf-allowed;
no ML; no invented commission/swap.

## Blocking defects (RECYCLE)

1. **Stale EX5 — executable not bound to remediated source (Q02 blocker).**
   `mq5` last changed in commit `922d991ad` (08-23 remediation: open-ended TP, decoupled
   management, capital-preservation kill switch). `ex5` last committed in `bfd467bc6`
   (pump auto-commit, 08-18) — it PREDATES the remediation. `git log` confirms the two files
   diverge by a commit. Q02 would run the pre-remediation binary, not the reviewed source.
   The three setfile `build_hash` values (`23577e…`, `58c52b…`, `d0b524…`) are mutually
   different and none equals the MQ5 hash `5c8f45f1…`; the packet claims
   `compile_succeeded=true` with `smoke_result=deferred_p2_smoke` and null smoke report.
   Provenance is structural only. **Fix (builder, not Claude — recompile is ROT for review
   lane):** recompile through the governed compiler; regenerate setfiles via the governed
   generator so ex5 + all build_hash values bind to MQ5 `5c8f45f1…`; return a strict
   compiler record.

2. **Rollover blackout uses broker time, not GMT (card fidelity).**
   Card §3.1.2 specifies the 23:55–00:05 **GMT** window. `StrategyInRolloverWindow(TimeCurrent())`
   feeds raw broker time (GMT+2/+3), so the blackout fires ~2h off the card window. Framework
   provides DST/UTC conversion (`framework/include/QM/QM_DSTAware.mqh`) — not used here.
   **Fix:** convert to UTC before the HHMM comparison.

## Lower-severity (fix with the above)
- No 3-tick slippage/deviation ceiling (card §4.2); no `QM_EntryConfigure` deviation set.
- `QM_IsNewBar()` is parameterless (chart-TF cadence), not bound to D1; setfiles pin D1 so
  Q02 is safe in practice, but bind cadence to `strategy_signal_tf` for robustness.
- `OnInit` does not seed BB/EMA/ATR state; `AdvanceState_OnNewBar()` early-returns without
  clearing cached signal on a failed bar read; `Strategy_NoTradeFilter()` runs before
  `AdvanceState` so the first-bar spread check is skipped while `g_last_atr==0`.

## Disposition
Verdict **RECYCLE** to the builder. Core mechanism, inputs, and framework wiring are sound;
the packet cannot go to Q02 because the compiled/preset artifacts do not bind to the
remediated source (defect 1) and the rollover filter deviates from the card GMT contract
(defect 2). No source changed, no compile, no Q phase, no backtest, no T_Live action.

# review_ea — QM5_9406_qs-daily-mac (task a81c8603)

- **Date:** 2026-08-24
- **Reviewer:** Claude (review lane)
- **Router task:** a81c8603-e857-466e-bd93-df15cae0bd8f (review_ea, was REVIEW, assigned codex)
- **Source:** gemini/agy build 52fc3ee3, verdict BUILD_READY_REVIEW
- **Verdict:** APPROVED (clean for Q02)

## Scope reviewed
- `framework/EAs/QM5_9406_qs-daily-mac/QM5_9406_qs-daily-mac.mq5`
- `.../SPEC.md`, `.../build_identity.json`, `.../sets/*` (13 setfiles)
- Approved card `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9406_qs-daily-mac.md`

## Card-faithful mechanism — PASS
- Long-only D1 100/400 SMA crossover; fresh up-cross entry (`mq5:83` cross_up = fast_1>slow_1 && fast_2<=slow_2), reverse down-cross exit (`mq5:117`), one position per magic (`mq5:74`). Matches card Mechanics (entry/exit/one-per-magic).
- Protective stop 2.5*ATR(14) via `QM_StopATR` (`mq5:96`) = card build-default stop.

## Every declared strategy input has use-sites — PASS (no dead inputs)
- `strategy_fast_sma` (`mq5:78,80,110,112`), `strategy_slow_sma` (`mq5:79,81,111,113`)
- `strategy_atr_period` (`mq5:62` NoTradeFilter, `mq5:96` StopATR)
- `strategy_sl_atr_mult` (`mq5:96`), `strategy_spread_max_atr` (`mq5:63`)
- `strategy_warmup_bars` (`mq5:53,70,107`)

## Framework conformity — PASS
- `#include <QM/QM_Common.mqh>` (`mq5:5`); MAE hook `QM_FrameworkTrackOpenPositionMae()` first in OnTick (`mq5:156`).
- Magic via `QM_FrameworkMagic()`/MagicResolver (`mq5:73,124`); news wired `QM_NewsAllowsTrade2` (`mq5:190`); Friday close + kill-switch present.
- Indicator helpers `QM_SMA`/`QM_ATR`/`QM_StopATR` are real bounded-buffer framework fns (`QM_Indicators.mqh:514,555`, `QM_StopRules.mqh:159`); no raw series, no ML, no invented commission/swap.
- RISK_FIXED=1000 / RISK_PERCENT=0 backtest; risk-mode mismatch guarded by QM_FrameworkInit.

## Build & registry evidence — PASS
- build_identity build_check_passed=true; current mq5 sha256 23bc76a3… and ex5 sha256 d88740fe… both match build_identity (coherent pair for current source hash).
- 13 magic rows (slots 0–12) active in `framework/registry/magic_numbers.csv`; every setfile `qm_magic_slot_offset` matches its registry slot (magic = 9406*10000+slot); ea_id_registry row active with matching source_id.

## Notes (non-blocking)
- ex5 mtime (23:09) precedes mq5 mtime (23:53) but hashes match build_identity — timestamps are not authoritative, the recorded build pair is consistent.

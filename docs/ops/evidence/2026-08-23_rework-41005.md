# rework-41005 review-fix evidence

- Router task: `64fb0c90-8022-45f3-b413-7f404fa3a82c`
- EA: `QM5_41005_richard-donchian-50day-cta-benchmark`
- Review work order: `docs/ops/evidence/64fb0c90_qm5_41005_gemini_build_codex_review_2026-08-18.md`
- Canonical approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_41005_richard-donchian-50day-cta-benchmark.md`
- Scope: one EA, its four backtest sets, SPEC, focused pytest, and this evidence file. No registry allocation was changed.

## What changed

1. The 50-day entry and 20-day exit now share one bounded `CopyRates` load on
   the D1 new-bar path. `CalculateDonchianBreakout` validates `ArraySize`
   before indexing and evaluates the closed bar at buffer index 0 against the
   prior lookback bars (`framework/EAs/QM5_41005_richard-donchian-50day-cta-benchmark/QM5_41005_richard-donchian-50day-cta-benchmark.mq5:106`, `:148`, `:159`).
   The EA no longer calls raw `iClose`/`iHigh`/`iLow` or the raw-series
   `QM_Sig_Range_Breakout` helper.
2. The card's 2.0% realised-loss entry halt, 2.5% daily hard stop, and 5.0%
   total-drawdown stop are declared strategy inputs, range-validated, consumed
   by the daily-loss/kill-switch paths, and sealed in all four setfiles
   (`.mq5:36-44`, `:103`, `:279-304`; each setfile `:29-31`).
3. The D1 execution contract remains fail-closed and explicitly declares the
   framework Friday-close override (`.mq5:294-297`). The card's GMT rollover
   blackout now converts broker time to UTC before comparing hours (`.mq5:184`).
4. New-bar state and the equity stream update before management. Management
   and the cached opposite-channel exit execute before entry-only spread,
   rollover, daily-entry-halt, and news filters (`.mq5:318-363`). This prevents
   an entry gate from suppressing position exits.
5. `SPEC.md:21-35` documents the sealed loss inputs and the V5 mapping of the
   card's legacy `InpRiskPercent` name to `RISK_PERCENT`. Backtests remain
   `RISK_FIXED=1000` / `RISK_PERCENT=0`; reviewed live sets must invert those
   modes within the card's 0.20%-1.00% range.
6. `tools/strategy_farm/tests/test_qm5_41005_rework_contract.py:42-153`
   provides six focused regression tests covering the mechanism, execution and
   loss contracts, management reachability, input use-sites/set sealing,
   framework/magic/MAE conformity, and fixed-risk slot bindings.

## Preflight evidence

- Approved-card status is `g0_status: APPROVED`. The repo and runtime copies
  have identical normalized content; their byte hashes differ only because the
  repo copy is CRLF and the runtime copy is LF.
- `framework/registry/ea_id_registry.csv:4500` is the active 41005 identity.
- `framework/registry/magic_numbers.csv:17551-17554` contains the unchanged
  active slots 0-3 (`410050000` through `410050003`) for XTIUSD, XAUUSD,
  SP500, and EURUSD. All four values are present in
  `framework/include/QM/QM_MagicResolver.mqh`; no registry gap or regeneration
  was required.
- Strategy-input grep counts in the source: `InpEntryLookback=7`,
  `InpExitLookback=7`, `InpAtrPeriod=4`, `InpAtrSlMult=4`,
  `InpSpreadAtrMult=3`, `InpDailyLossEntryHaltPct=4`,
  `InpDailyHardStopPct=4`, `InpTotalDrawdownStopPct=4`. Every declared strategy
  input has a non-declaration use-site and is present in all four backtest sets.

## Test and validation output

Focused EA unit test:

```text
> python -m pytest tools/strategy_farm/tests/test_qm5_41005_rework_contract.py -q
......                                                                   [100%]
6 passed in 0.86s
```

Relevant hardening and guardrail unit tests:

```text
> python -m pytest <D2,D4,D5,D7,D10 build-gate nodes> tools/strategy_farm/tests/test_build_guardrails.py -q
.........................                                                [100%]
25 passed in 1.49s
```

Scoped mechanical hardening:

```text
> python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_41005_richard-donchian-50day-cta-benchmark
files_scanned=1; failures=[]; warnings=[]
D2 loss limits: 2.0 / 2.5 / 5.0, failures=0
D4 management reachability: failures=0
D5 GMT/UTC conversion: converter_present=true, failures=0
D7 MAE hook: failures=0
D10 buffer bounds: failures=0
D11/D17 symbol registry/card checks: failures=0
```

Other scoped checks:

```text
validate_spec_doc.py: PASS (1 PASS, 0 FAIL)
validate_build_guardrails.py --max-news-stale-hours 336: PASS (5 files, no findings)
validate_symbol_scope.py --fail-on-leak: SINGLE_SYMBOL_OK, 0 violations
raw_mq5_quarantine.py check --purpose compile: RAW_MQ5_SOURCE_ALLOWED
git diff --check: PASS (line-ending notices only; no whitespace errors)
```

## Compile/build-check interlock evidence and residual risk

The exact scoped strict build check was attempted:

```text
> framework/scripts/build_check.ps1 -EALabel QM5_41005_richard-donchian-50day-cta-benchmark -Strict -SkipCompile
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
```

The sanctioned compile orchestrator was also attempted without `--force`:

```text
> python tools/strategy_farm/compile_ea.py --ea-label QM5_41005_richard-donchian-50day-cta-benchmark --json --fail-on-error
verdict=COMPILE_FAILED
reason=compile_one.ps1 reason_class=INCLUDE_MIRROR_REFUSED errors=-1 warnings=-1
```

A subsequent 50-second observation found no moment with zero T1-T10
terminals. The guard correctly prohibited include mirroring while the factory
was active. No terminal or factory process was stopped, and no compile work
item/backtest was enqueued. The tracked EX5 therefore remains the pre-rework
binary (SHA-256 `12e35b8185adb912bc6023947a5cea20c5c7a540e16542669c924d1aa0b585d6`)
and must not be treated as compiled evidence for the repaired source. This is
the sole residual review risk; source-level and static gates are green.

## Boundaries and rollback

- No gate threshold/criterion, verdict row, state DB row outside the router
  task transition, backtest queue, factory state, AutoTrading state, registry,
  resolver, or `C:/QM/mt5/T_Live` content was changed.
- Rollback: revert the ticket commit after resubmission. This restores the EA,
  SPEC, and four sets and removes the focused test/evidence file. The existing
  EX5 was not regenerated, so binary rollback is not required.

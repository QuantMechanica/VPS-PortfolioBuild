# rework-35004 — QM5_35004 review-fix evidence

- Router task: `c7f41f38-507d-4b36-921b-2aefb42b206c`
- EA: `QM5_35004_babypips-asian-box-london-breakout`
- Branch: `rework-slot-4`
- Card: `strategy-seeds/cards/approved/QM5_35004_babypips-asian-box-london-breakout.md` (`g0_status: APPROVED`)
- Work order: `docs/ops/evidence/c7f41f38_qm5_35004_gemini_build_codex_review_2026-08-17.md` and `docs/ops/evidence/a138d0c0_qm5_35004_gemini_remediation_codex_review_2026-08-23.md`

## What changed

1. The 2.0% entry halt now sums UTC-day account realized P&L from BUY/SELL
   deals, including profit, swap, commission, and fee. It reconstructs the
   day-start balance and fails closed if history selection fails. Floating
   equity remains the separate framework 2.5% hard-stop concern
   (`framework/EAs/QM5_35004_babypips-asian-box-london-breakout/QM5_35004_babypips-asian-box-london-breakout.mq5:77`, `:110`).
2. The Asian box and `Close[1]` now come from one bounded 96-bar `CopyRates`
   snapshot behind the M15 new-bar gate, with an `ArraySize` guard and the
   exact 24-bar session requirement. Raw `iTime`/`iHigh`/`iLow`/`iClose`
   calls were removed (`.mq5:128`, `:138`, `:184`).
3. The unapproved 1R/20-pip/+1-pip break-even behavior was removed. The
   management hook is deliberately empty because the approved card defines
   only midpoint SL, 2x-box TP, and the 16:00 GMT exit (`.mq5:310`).
4. The forced-close deadline is derived from each managed position's UTC
   entry date. Once 16:00 GMT is reached, the signal stays asserted across
   23:55 and midnight until the position closes (`.mq5:316`, `:331`).
5. Added a seven-test static contract suite covering the card mechanism,
   realized-loss/equity separation, removal of invented management, sticky
   time exit, management reachability, all 14 strategy input use-sites and
   setfile sealing, risk/magic/MAE conformity, and hardening
   (`tools/strategy_farm/tests/test_qm5_35004_rework_contract.py:59`).

The original remediation already present on this branch retained the reviewed
UTC conversion, exact midpoint SL, full 24-bar box, and management/exit-before-
entry-filter ordering. All 14 declared `strategy_*` inputs have at least one
executable use-site (observed token counts: 2–3 each). Existing active magic
rows are complete at `framework/registry/magic_numbers.csv:17405-17407`; no
registry or resolver file was changed.

## Validation evidence

Commands were run from `C:\QM\worktrees\rework-slot-4` and scoped to this EA
unless the command is a named unit-test module.

```text
python -m pytest tools/strategy_farm/tests/test_qm5_35004_rework_contract.py -q
.......                                                                  [100%]
7 passed in 0.94s

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_35004_babypips-asian-box-london-breakout
files_scanned: 1
failures: []
warnings: []

python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 framework/EAs/QM5_35004_babypips-asian-box-london-breakout
files_checked: 4
findings: []
verdict: PASS

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_35004_babypips-asian-box-london-breakout
PASS QM5_35004_babypips-asian-box-london-breakout
Summary: 1 PASS, 0 FAIL (of 1)

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_35004_babypips-asian-box-london-breakout --fail-on-leak --json
verdict: SINGLE_SYMBOL_OK
n_violations: 0

python -m pytest tools/strategy_farm/tests/test_qm5_35004_rework_contract.py tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_validate_symbol_scope.py -q
58 passed, 1 failed in 208.32s
UNRELATED FAILURE: test_qm5_411xx_sources_have_no_unbounded_numeric_buffers
reports seven pre-existing QM5_41134_wti-mdaily-iqrmean-mom buffer findings;
it does not reference or scan QM5_35004 in the failing assertion.

git diff --check
PASS (line-ending advisory only; no whitespace errors)
```

### Compile/build interlock

The required scoped command was attempted:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_35004_babypips-asian-box-london-breakout -SkipCompile
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED
failure_class: LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
detail: terminal64 processes are alive; ad-hoc compile/build_check is refused.
```

This guard executes before even the `-SkipCompile` checks. A read-only
`mode=ro` query of `D:/QM/strategy_farm/state/farm_state.sqlite` found three
existing `QM5_35004` Q02 backtest rows and no `COMPILE_EA` work item. No compile
item was created, no backtest was enqueued/deleted, no terminal was stopped,
and the factory and `T_Live` were untouched.

Current identity at handoff:

- MQ5 SHA-256: `fcaae241f606ea5dfa6024202b5ba8141fdb26b115bb1c45fa9f26d1e0f72f2d`
- Existing EX5 SHA-256: `d90ab915cd2642632fc122a381cf3aabc85d77a4118a7ce70f7880a3e62b6e70`
- The EX5 is intentionally not claimed as current; a governed compile remains
  required once the live-factory compile interlock permits it or an authorized
  `COMPILE_EA` binding is supplied.

## Rollback

Revert the ticket commit with `git revert <rework-35004-commit>`. That restores
the prior MQ5 and removes this test/evidence note. The EX5, setfiles, registries,
state DB rows, factory state, and terminals were not changed by this ticket.

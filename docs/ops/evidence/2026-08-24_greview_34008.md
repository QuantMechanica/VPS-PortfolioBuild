# greview-34008 — QM5_34008 cross-review evidence

## Disposition

- Router task: `fa7ca587-77f8-4cea-b71b-7bb1b746b33d`
- Assigned build lane: `gemini`; reviewer: Codex under the cross-review rule
- EA: `QM5_34008_multicurrency-basket-dispersion-hedger`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_34008_multicurrency-basket-dispersion-hedger.md`
- Review date: 2026-08-24
- **Verdict: RECYCLE.** The current build is not formally clean for Q02. The delivered binary is stale relative to the current source, the mandatory hardening scan reports four card-conformance failures, and one card-declared strategy input has no implementation/use-site.

No compile, backtest enqueue/delete, verdict overwrite, factory toggle, gate change, or `T_Live` access was performed.

## Evidence and findings

Path aliases used below: `EA` = `framework/EAs/QM5_34008_multicurrency-basket-dispersion-hedger/QM5_34008_multicurrency-basket-dispersion-hedger.mq5`; `Card` = `D:/QM/strategy_farm/artifacts/cards_approved/QM5_34008_multicurrency-basket-dispersion-hedger.md`.

### 1. Stale binary — blocking

- Current SHA-256 values from `Get-FileHash -Algorithm SHA256`:
  - `.mq5`: `D0A32E4286F57E98F34A58BE894337D84C24AC139235857DE5D19FA53DD1F636`
  - `.ex5`: `85962ADE2C7C4251A188BD38D8F269DF57D8CFF0F1F534035DB60DC541661186`
- `git log --name-status -- <EA>.ex5` shows the binary only in commit `039a86faed689e3b00644ca48c43bfbbfc0631f6` dated 2026-08-17. The current source was materially changed later in commit `10f9d94e1dd65f15eb8e4a9007cb03a6f46c33f7` dated 2026-08-23; that commit contains the `.mq5` but no `.ex5` update.
- The builder evidence explicitly says only “Draft code updated and verified” and contains no compilation result or compile-log binding (`docs/ops/evidence/2026-08-23_fa7ca587_qm5_34008_build_ea_remediation.md:19-27`). No `build_identity.json` exists in the EA directory, and `rg --files -uu D:/QM/strategy_farm | rg -i 'QM5_34008|34008'` found only the approved card, not a compile log/build identity.
- Every delivered setfile declares `build_hash: 6729e35ee654669cb0002dd9bee76d5dd2b89680455d528472fc2736fcef713d` (representative: `framework/EAs/QM5_34008_multicurrency-basket-dispersion-hedger/sets/QM5_34008_multicurrency-basket-dispersion-hedger_EURUSD.DWX_H1_backtest.set:13`), which matches neither the current source nor binary SHA-256. There is therefore no reproducible current-source/current-binary binding.
- **Named fix:** after resolving the source defects below, recompile via governed `COMPILE_EA`, retain its compile log/build identity, prove the current `.mq5` hash binding to the delivered `.ex5`, and regenerate the setfiles through the governed generator with that identity. Do not compile manually.

### 2. Mandatory build hardening fails — blocking

Read-only command:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_34008_multicurrency-basket-dispersion-hedger
```

The JSON `failures` array reports:

1. card daily entry halt `2%` has no matching input;
2. card daily hard stop `2.5%` has no matching input;
3. card total drawdown stop `5%` has no matching input;
4. card GMT/UTC rollover window uses raw broker time without `QM_BrokerToUTC` or a documented equivalent.

The card declarations are at `D:/QM/strategy_farm/artifacts/cards_approved/QM5_34008_multicurrency-basket-dispersion-hedger.md:82-87` and `:109-112`. The EA uses `TimeCurrent()` directly at `framework/EAs/QM5_34008_multicurrency-basket-dispersion-hedger/QM5_34008_multicurrency-basket-dispersion-hedger.mq5:125-140`; the common framework currently initializes generic kill-switch values `3.0, 0.0, 1.0` at `framework/include/QM/QM_Common.mqh:296-298`, not the card's 2%/2.5%/5% contract.

`validate_build_guardrails.py` independently returned `verdict: PASS` with zero findings. That narrower PASS does not override the hardening failures.

### 3. Declared strategy-input audit — blocking

The approved card declares three strategy parameters (`Card:158-164`):

| Card input | EA mapping/use-sites | Result |
|---|---|---|
| `InpLookbackHours` | `strategy_lookback_hours` declared at `EA:38`, used at `EA:159` and `EA:324` | used |
| `InpDispersionDev` | `strategy_dispersion_dev` declared at `EA:39`, used at `EA:213` | used |
| `InpPackageRisk` | no corresponding input or token in the EA; each leg is instead hard-coded to `QM_LotsForRisk(...) * 0.5` at `EA:227-235` | **dead/absent — reject** |

For completeness, every strategy input that the EA itself declares has a non-declaration use-site: `strategy_lookback_hours` (`:159,:324`), `strategy_dispersion_dev` (`:213`), `strategy_target_profit_pct` (`:293`), `strategy_hard_stop_loss_pct` (`:294`), `strategy_atr_period` (`:132,:227,:232`), and `strategy_spread_atr_mult` (`:140`). This does not cure the missing card input.

### 4. Additional card-faithfulness and safety defects — blocking

- The card requires sizing from the exact stop-loss distance (`Card:101-107`) and describes broker-side SL/TP placement (`Card:133-145`), but both basket requests set `sl=0.0` and `tp=0.0` (`EA:240-260`). ATR-derived stop distances affect lot sizing (`EA:227-235`) without placing those stops. Fix the risk/stop mechanism to match the approved card rather than inventing a replacement.
- The card's no-trade conditions are entry filters (`Card:80-88`), but `OnTick` returns on the rollover/spread filter before orphan management and package TP/SL (`EA:343-350`). During those filter windows the active-package safety logic at `EA:276-303` is unreachable. Move entry-only filtering behind unconditional open-position management/exit.
- The modern news check is performed only for chart `_Symbol` (`EA:355-360`). The actual foreign basket legs are submitted with `qm_news_mode_legacy`, whose default is `QM_NEWS_OFF` (`EA:28,263-267`); `QM_BasketOpenPosition` checks each requested symbol only through that legacy mode (`framework/include/QM/QM_BasketOrder.mqh:147-157`). Thus temporal/compliance news policy is not wired to every traded leg. Pass/enforce the active modern policy for each leg.
- The approved maximum slippage is 3 ticks (`Card:109-112`), while both basket calls pass `20` deviation points (`EA:263,267`; consumed as request deviation at `framework/include/QM/QM_BasketOrder.mqh:399`). Reconcile the executable tolerance to the approved card.
- The card literally specifies BUY of the `argmin(delta)` pair and SELL of the `argmax(delta)` pair (`Card:89-97`). The code reverses those pair sides for direct-USD symbols (`EA:219-225`). Either implement the approved pair-side rule or obtain an approved card clarification; Q02 must not test an undocumented direction transform.

### 5. Conforming surfaces observed

- Framework chain and hooks: `QM_Common.mqh` is included (`EA:6`), framework init is wired (`EA:315-324`), MAE tracking is first in `OnTick` (`EA:334-338`), Friday close/news/timer/trade-transaction hooks are present (`EA:343-378`).
- Risk-mode surface: the EA exposes and forwards `RISK_PERCENT` and `RISK_FIXED` (`EA:18-21,315-320`). All seven backtest setfiles use `RISK_FIXED=1000` and `RISK_PERCENT=0`; no live attach/use was attempted.
- Magic: all slots 0-6 are active in `framework/registry/magic_numbers.csv:17394-17400`, yielding `340080000` through `340080006`. Basket orders resolve via `QM_MagicChecked` (`framework/include/QM/QM_BasketOrder.mqh:165-178`) and the resolver formula is `ea_id*10000+slot` (`framework/include/QM/QM_MagicResolver.mqh:75-88`).
- Data/performance: basket arrays are compile-time bounded to seven; raw `iClose` reads are closed-bar/new-bar gated and carry the required `perf-allowed` annotation (`EA:159-175`). No unbounded `CopyBuffer` target was found; hardening D10 reported zero failures.
- No ML is present. Open-package P&L uses actual broker `POSITION_PROFIT` and `POSITION_SWAP` (`EA:92-105`); no strategy-invented commission/swap model was found.
- Seven symbol-specific H1 backtest setfiles and `basket_manifest.json` are present. Slots and symbols match the card and registries, but their current generation/build provenance is not acceptable for promotion as described in finding 1.

## Verification output

```text
python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_34008_multicurrency-basket-dispersion-hedger/QM5_34008_multicurrency-basket-dispersion-hedger.mq5
=> PASS, 0 findings

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_34008_multicurrency-basket-dispersion-hedger
=> Summary: 1 PASS, 0 FAIL (of 1)

python -m pytest tools/strategy_farm/tests/test_gen_setfile.py tools/strategy_farm/tests/test_setfile_canonicalization.py -ra
=> 4 passed in 78.49s

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_34008_multicurrency-basket-dispersion-hedger
=> completed read-only; JSON failures=4 (three loss-limit mismatches, one GMT conversion failure)
```

No implementation module was changed by this review, so there is no touched-module unit-test target beyond the governed setfile tests above.

## Required recycle actions

1. Implement every approved card input, especially package risk, with real use-sites and governed setfile values.
2. Resolve all four `build_gate_hardening` failures without changing the card or gate criteria.
3. Repair the stop/risk, management reachability, per-leg news, slippage, and direction-contract defects identified above.
4. Regenerate all seven setfiles through `framework/scripts/gen_setfile.ps1` (or its governed caller) and preserve provenance.
5. **Recompile via governed `COMPILE_EA`**, then retain compile log/build identity proving that the delivered `.ex5` corresponds to the current `.mq5`; rerun both static checks and the relevant unit tests.

## Rollback

This review adds only this evidence file. Revert its review commit with a normal `git revert <review-commit>` if the evidence artifact must be withdrawn. The router review closure is durable operational state; correct it only through the governed router/superseding-review workflow—never by deleting or overwriting database rows.

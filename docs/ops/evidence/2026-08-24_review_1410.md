# review_ea QM5_1410 — bressert-dual-cycle-oscillator-h4 (rework re-review)

**Reviewer:** Claude (Orchestrator review lane)
**Date:** 2026-08-24
**Router task:** ee84a2b6-1d7a-4cbf-b7bd-393469e9a8be (review_ea, assigned codex, source_agent gemini)
**Prior verdict:** RECYCLE (2026-08-22, Claude) — 8 strict-series findings + news-window,
restart partial-close and MAE-adjusted-cap defects.
**Verdict:** BLOCKED — source review PASS, blocked on governed COMPILE_EA recompile before Q02.

---

## Scope

Re-review of the rework delivered in commit `80379fc39` ("rework(1410): repair news and MAE
lifecycle"), which rewrote the `.mq5` (+290 lines), SPEC.md, and added a regression test.
Reviewed against approved card `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1410_bressert-dual-cycle-oscillator-h4.md`
(`g0_status: APPROVED`; the repo copy referenced by SPEC.md line 14 does not exist in-tree —
the card is runtime-only on D:, not a review blocker but a provenance note).

## Card-faithful mechanism — PASS

Every card rule is implemented and matches at the correct bar shifts (mq5 `Strategy_EntrySignal`
/ `Strategy_ExitSignal`):

- Long entry gates 1-5: oversold-2-of-3 (`:307-314`), bullish cross `[1]>[2] && [2]<=[3]`
  (`:315`), intermediate align `dss_long[1]<=50 && dss_long[1]>dss_long[4]` (`:316`), 3-bar
  price momentum `close[1]>close[4]` (`:317`), macro bias `close[1]>SMA(50,D1)` (`:296`,`:130-144`).
  Short entry mirrored (`:345-357`). All match card "Mechanics/Entry".
- Exits: TP1 1.5 ATR / 50% partial / SL→BE (`:487-519`); TP2 oscillator `DSS_short>=70`/`<=30`
  (`:566-575`); MAE-adjusted 4-ATR cap `favorable_gain - g_mae_adverse_price >= 4*ATR`
  (`:483-484`, `:528-532`); pattern-failure both-cycles-down-in-loss (`:578-593`); 30-bar
  time-stop (`:542-546`). All match card "Exit".
- SL 1.5 ATR (`:324`,`:362`); spread filter 0.25 ATR (`:122-128`); reuse guard 6 bars
  (`:86-120`,`:661`); one-position-per-magic (`:71-84`). Match card "Stop Loss"/"Additional filters".

## Declared-input audit — PASS (no dead inputs)

All 19 `strategy_*` inputs have >=1 non-declaration use-site (grepped): strategy_tf,
strategy_atr_period, dss_short_n/m, dss_long_n/m, timing_band_oversold/overbought/mid,
sl_atr_mult, tp1_atr_mult, tp1_close_fraction, tp_cap_atr_mult, time_stop_bars,
macro_bias_enabled, macro_sma_period, reuse_guard_bars, spread_filter_enabled, spread_max_atr.
Framework inputs all reach `QM_FrameworkInit` (`:604-607`).

## Framework conformity — PASS

- `#include <QM/QM_Common.mqh>` (`:5`); `QM_FrameworkInit/Shutdown/OnTimer/OnTradeTransaction`,
  `QM_DefaultObjective` wired.
- **MAE hook**: `QM_FrameworkTrackOpenPositionMae()` first in OnTick (`:621`) — prior "missing
  MAE hook" defect fixed.
- **News**: entry-only ±2 H4-bar (480/480 min) blackout `QM_NewsInWindow(...,480,480,...)`
  (`:266`) + framework window (`:606`); management/exits run before the news-entry gate
  (`:627-652`). Prior news-window defect fixed.
- **Restart idempotency**: `Strategy_ReconstructPositionState` rebuilds TP1 latch + MAE from
  durable deal history, fail-closed on read failure (`:387-453`). Prior restart partial-close
  defect fixed.
- **Magic**: resolver-backed `QM_FrameworkMagic()` + `req.symbol_slot=qm_magic_slot_offset`;
  registry `magic_numbers.csv:17750` XAUUSD slot 12 = 14100012 = 1410*10000+12. Formula correct.
- Risk: `RISK_FIXED=1000 / RISK_PERCENT=0` (mq5 + EURUSD setfile, env=backtest). Live percent
  wired through init.
- Bounded buffers: DSS resize+ArraySize guards (`:146-253`); bespoke `CopyRates` carry bounded
  `// perf-allowed` reasons (`:285`,`:432`,`:554`). No ML, no invented commission/swap.

## Static build evidence (current source hash) — PASS

Independently re-run by reviewer (read-only, no compile):

- `validate_build_guardrails.py` → verdict=PASS, files_checked=15, findings=[].
- `build_gate_hardening.py` → failures=[], warnings=[], D7/D9/D10/D15=0.
- `pytest test_qm5_1410_review_rework.py` → 5 passed.
- `ea_id_registry.csv:293` active; `magic_numbers.csv:17737-17750` = 14 active slots.

## Blocking finding — stale binary / recompile pending

The rework (mq5 sha256 `8ea2ec14…`, mtime 2026-08-24) was committed **without a matching
recompile**. The `.ex5` (sha256 `4b539331…`, mtime 2026-08-22, last built in commit
`20b2c0eb5`) and both compile logs (`framework/build/compile/20260822_*`) are the **pre-rework
binary**. Codex correctly did not claim it as evidence: the governed compile was refused
fail-closed (`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` / `INCLUDE_MIRROR_REFUSED`, 12 live
`terminal64` during the burn window) and no `COMPILE_EA` work item exists for EA 1410
(`docs/ops/evidence/2026-08-23_rework-1410.md:77-97`).

Consequence: there is no COMPILE_OK for the current source hash, so the EA is **not
Q02-dispatchable** — Q02 would run the old-behavior binary and the Q04 stale-ex5 guard
(build-date predicate) would void it INVALID. This is an environmental block, not a code
defect; a re-RECYCLE to the build lane cannot resolve it during the active burn. Reviewer is
read-only DB and may not recompile or enqueue.

**Actionable next step (build/ops lane, next OFF window):** mint a governed `COMPILE_EA` claim
for QM5_1410, recompile in the OFF window so the `.ex5` matches source `8ea2ec14…`, then
dispatch Q02 (append-only). No code change required.

## Disposition

Source and static-build review PASS; the 2026-08-22 RECYCLE is fully remediated. Held BLOCKED
pending the governed recompile — the code is clean but the deliverable cannot enter Q02 with a
matching binary until then.

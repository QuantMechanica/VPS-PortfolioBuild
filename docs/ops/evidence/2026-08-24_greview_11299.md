# greview-11299 — Codex cross-review of Gemini build QM5_11299

Date: 2026-08-24  
Router task: `ea624d92-20db-425b-9deb-840b11c83d40`  
EA: `QM5_11299_lwma144-smma5-fractal-m5-scalp`  
Review scope: static/card/build-identity review for formal Q02 admission; no compile, backtest enqueue, factory mutation, verdict-row overwrite, or `T_Live` access.

## Verdict

**RECYCLE** — the mechanism is substantially card-faithful and the delivered binary is current, but the EA is not formally clean for Q02 because one declared strategy input is dead, the source bypasses the framework bar reader with an unannotated raw-series call, and the required `SPEC.md` is absent.

Named repair:

1. Wire `strategy_tp_atr_mult` into both long and short TP calculations (or remove the input if the 2R value is intentionally immutable).
2. Replace the direct `CopyRates` close read with `QM_ReadBar` (preferred) or add the governed, justified `// perf-allowed` annotation and retain the exact copy-count/bounds proof.
3. Generate `SPEC.md` from the approved card.
4. Recompile through governed `COMPILE_EA`, regenerate/update the build-identity evidence, and rerun the two read-only build gates before re-review.

## Inputs and identity reviewed

`python tools/strategy_farm/agent_router.py list-tasks --agent gemini` returned the exact task in `REVIEW`, assigned to `gemini`, with `payload.ea_id=11299` and slug `lwma144-smma5-fractal-m5-scalp`. This older payload does not contain the advertised `ea_dir`, `card_path`, or `artifact` keys, so the paths were resolved by exact EA ID:

- EA source/binary: `framework/EAs/QM5_11299_lwma144-smma5-fractal-m5-scalp/`.
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11299_lwma144-smma5-fractal-m5-scalp.md` (`g0_status: APPROVED` at line 16; target symbols at line 24).
- Build identity: `docs/ops/evidence/ea624d92_QM5_11299_lwma144-smma5-fractal-m5-scalp_build_identity.json:1`.

The card is legacy-recovered and retains `status: draft` plus `card_body_incomplete: true` (`card:25-28`), but the controlling authorization is the explicit `g0_status: APPROVED` (`card:16`). No gate threshold or criterion was interpreted or changed by this review.

## Blocking findings

### 1. Dead declared strategy input

`strategy_tp_atr_mult` is declared at `framework/EAs/QM5_11299_lwma144-smma5-fractal-m5-scalp/QM5_11299_lwma144-smma5-fractal-m5-scalp.mq5:42` and has no use-site. The long and short paths instead hardcode `2.0 * sl_dist` at source lines 194 and 203. The card requires a 2R target (`card:64-67`), so current default behavior happens to match, but a setfile/P3 change to the declared input would silently have no effect. The review contract makes any dead strategy input a rejection.

Mechanical grep census of all 13 `strategy_*` inputs found real use-sites for 12; only `strategy_tp_atr_mult` had one occurrence (its declaration). Relevant occurrence counts/lines:

| Input | Occurrences | Lines |
|---|---:|---|
| `strategy_timeframe` | 9 | 36, 108, 119, 147-150, 161, 180 |
| `strategy_lwma_period` | 3 | 37, 149-150 |
| `strategy_smma_period` | 3 | 38, 147-148 |
| `strategy_max_proximity_pips` | 2 | 39, 166 |
| `strategy_atr_period` | 2 | 40, 180 |
| `strategy_sl_atr_mult` | 3 | 41, 192, 201 |
| `strategy_tp_atr_mult` | **1** | **42 only** |
| `strategy_use_fractal_sl` | 3 | 43, 189, 198 |
| `strategy_fractal_lookback` | 3 | 44, 190, 199 |
| `strategy_session_filter` | 2 | 45, 82 |
| `strategy_session_start_hour` | 4 | 46, 86-88 |
| `strategy_session_end_hour` | 4 | 47, 86-88 |
| `strategy_max_spread_pips` | 3 | 48, 93, 101 |

### 2. Unannotated raw-series access bypasses the framework reader

The EA allocates a dynamic `MqlRates rates[]`, calls `CopyRates`, then reads `rates[0].close` at source lines 159-165. The exact `CopyRates(...) == 1` check is a valid local bounds proof, which is why `build_gate_hardening` reports zero D10 failures; however, the ticket separately forbids raw series in EA strategy code without a `// perf-allowed` annotation. The framework already provides the bounded `QM_ReadBar` helper at `framework/include/QM/QM_Indicators.mqh:179-190`, so there is no need for a per-EA raw-series exception.

### 3. Required strategy specification is missing

The EA directory contains the `.mq5`, `.ex5`, and `sets/` only. Read-only invocation:

```text
python framework/scripts/validate_spec_doc.py framework/EAs/QM5_11299_lwma144-smma5-fractal-m5-scalp
FAIL  QM5_11299_lwma144-smma5-fractal-m5-scalp
      - SPEC.md missing at .../QM5_11299_lwma144-smma5-fractal-m5-scalp/SPEC.md
Summary: 0 PASS, 1 FAIL (of 1)
exit=1
```

## Card fidelity and framework checks that passed

- Entry mechanics match the approved card: closed-bar SMMA(5)/LWMA(144) crossover (`mq5:147-170` versus `card:53-62,104-110`), 10-pip proximity (`mq5:155-167` versus `card:81`), confirmed fractal search with ATR(14) fallback (`mq5:104-123,180-203` versus `card:64-72,111-112`), 08:00-22:00 broker session and 3-pip spread cap (`mq5:80-102` versus `card:82-83,113-114`), and no reverse-signal exit (`mq5:223-228` versus `card:67`).
- The news path is wired (`mq5:230,252-260`). All news axes default OFF as the card expressly requires for P2 (`card:84`); this is not an undocumented exemption.
- Framework chain and MAE: direct `#include <QM/QM_Common.mqh>` (`mq5:5`), `QM_FrameworkInit(...)` (`mq5:236-243`), and `QM_FrameworkTrackOpenPositionMae()` as the first `OnTick` action (`mq5:248-251`).
- Magic resolution is governed: framework init calls `QM_MagicChecked(ea_id, slot, _Symbol)` (`framework/include/QM/QM_Common.mqh:223-227`); the resolver computes `ea_id * 10000 + slot` (`framework/include/QM/QM_MagicResolver.mqh:55-72`) and verifies registry/symbol ownership (`QM_MagicResolver.mqh:178-225`). Registry rows are `framework/registry/magic_numbers.csv:16085-16086`: EURUSD slot 0 / `112990000`, GBPUSD slot 1 / `112990001`.
- Backtest risk is governed: both shipped setfiles declare `environment: backtest`, `RISK_FIXED=1000`, `RISK_PERCENT=0` (`...EURUSD.DWX_M5_backtest.set:9,17-21`; identical risk lines in the GBPUSD set). Both risk inputs exist in source (`mq5:16-19`). No live preset is delivered at Q02; live `RISK_PERCENT=0.5` remains a later governed-preset obligation from `card:74-76`.
- Indicator handles use pooled `QM_SMMA`, `QM_LWMA`, `QM_ATR`, and `QM_Fractal*` readers. Their single-value reader uses a fixed `double buf[1]` and checks `CopyBuffer(...) == 1` before `buf[0]` (`framework/include/QM/QM_Indicators.mqh:281-288`). No direct indicator handle/`CopyBuffer` exists in the EA.
- A case-insensitive source scan found no commission, swap, ML/ONNX/TensorFlow, martingale, or grid mechanism and no invented cost input. The only raw-series hit was the `CopyRates` finding above.
- Symbol scope is exact: two card targets, two active magic rows, and two setfiles. `validate_symbol_scope.py --ea-label QM5_11299_lwma144-smma5-fractal-m5-scalp` returned `SINGLE_SYMBOL_OK`, `n_violations=0`.
- Setfiles are present in the governed generator layout and name both build hash and card source (`sets/...EURUSD...set:1-24`, `sets/...GBPUSD...set:1-24`). Their content shape matches `framework/scripts/gen_setfile.ps1:612-658`; the build identity enumerates both generated paths (`build_identity.json:10-13`).

## Read-only build gates

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_11299_lwma144-smma5-fractal-m5-scalp
schema=qm.build-gate-hardening/v1
files_scanned=1
failures=[]
warnings=[]
D10_indicator_buffer_bounds.failures=0
symbols_observed=2; matrix_valid=true

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_11299_lwma144-smma5-fractal-m5-scalp
verdict=PASS
files_checked=3
findings=[]
max_news_stale_hours=336
```

These automated passes do not waive the manual dead-input, raw-series-corset, or missing-SPEC findings.

## Binary freshness / compile evidence

Current hashes:

```text
MQ5 bc7ae0c2110fb16408bb1de58a46ca812c155d62bc47b386067b0f4ea7f94ca3
EX5 2489a3372177a1627619e6bcf7535fb26d1412fbb32346ba4f9101e2e378932b
```

They exactly match `docs/ops/evidence/ea624d92_QM5_11299_lwma144-smma5-fractal-m5-scalp_build_identity.json:7-9`. The source hash also matches both setfile `build_hash` headers (`EURUSD set:13`, `GBPUSD set:13`). The governed compile log at `C:/QM/repo/framework/build/compile/20260822_142430/QM5_11299_lwma144-smma5-fractal-m5-scalp.compile.log:3-75` names this source and ends `Result: 0 errors, 0 warnings`. Therefore the delivered `.ex5` corresponds to the current `.mq5`; **no stale-binary defect exists in this revision**. Once the required source repairs are made, the named fix is nevertheless to recompile via governed `COMPILE_EA` and replace the identity packet before re-review.

## Unit-test evidence

No code or test file was changed because this is a review-only ticket and the commit is constrained to this evidence note. The relevant existing unit suites were run from the worktree:

```text
python -m pytest -q \
  tools/strategy_farm/tests/test_build_gate_hardening.py \
  tools/strategy_farm/tests/test_build_guardrails.py \
  tools/strategy_farm/tests/test_agent_router_state_exits.py

74 passed in 506.29s (0:08:26)
exit=0
```

## Change and rollback

Changed only this evidence note. No EA source, `.ex5`, setfile, registry, runtime artifact, verdict row, queue, factory state, or live directory was touched during review.

Rollback the repository change with `git revert <review-commit>` (preferred, append-only history). Router close state is a separate governed state transition performed after this note is committed; if the review is later superseded, use a new router review/update event rather than overwriting this evidence or an existing verdict row.

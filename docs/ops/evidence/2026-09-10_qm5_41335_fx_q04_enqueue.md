# QM5_41335 FX fallback — Q04 enqueue

Recorded: 2026-09-10T09:05:47Z (11:05 Europe/Berlin)

Branch: `agents/board-advisor`

## Outcome

The frozen sign-aware 66-pair FX cointegration scan remains fully mechanized.
The durable reconciliation at
`artifacts/fx_cointegration_frontier_cpu_stop_20260812T112137Z_board_advisor.json`
accounts for all 66 relationships, so creating another Card or EA from the
scan would be duplicate work. The two requested anchors do not need Q02
infrastructure repair:

- `QM5_12532_AUDNZD_COINTEGRATION_D1` has Q02 `PASS`, Q04 `PASS`, then
  Q05 `FAIL`.
- `QM5_12533_EURJPY_GBPJPY_COINTEGRATION_D1` has Q02 `PASS`, then Q04
  `FAIL`.

The mission-authorized existing-forex fallback was therefore advanced.
Approved, structural, low-frequency `QM5_41335_fx-usd-exhaustion-reversal-opt`
on `AUDUSD.DWX` D1 has an authenticated current-identity Q02 `PASS` at work
item `ff75b1c3-4930-419d-a2fe-49bd37eadc4d`: 64 trades, PF 1.23,
+4428.72 net profit, and 5.02% drawdown over 2018-07-02 through 2022-12-31.
The runtime database had no Q04 row for this EA before the mutation.

One exact Q04 successor was appended through the canonical cascade path:

```powershell
python tools/strategy_farm/farmctl.py --root D:/QM/strategy_farm enqueue-backtest --ea QM5_41335 --phase Q04 --from-work-item-id ff75b1c3-4930-419d-a2fe-49bd37eadc4d
```

The command created work item `41b82b89-c50e-4eb0-8563-928c4d5d5d9a`
for `AUDUSD.DWX`, status `pending`, with `promoted_from_phase=Q02`,
`promoted_from_work_item=ff75b1c3-4930-419d-a2fe-49bd37eadc4d`, and
`q04_default_probe=true`. It retained `priority_track=true`, the D1 carrier,
the 2018-07-02 through 2022-12-31 window, and the fixed-risk contract. A
post-insert query found exactly one QM5_41335 Q04 row; no row was requeued or
skipped.

## Identity and PACER checks

The approved runtime Card is
`D:/QM/strategy_farm/artifacts/cards_approved/QM5_41335_fx-usd-exhaustion-reversal-opt.md`.
It records `g0_status: APPROVED`, R1-R4 `PASS`, D1 cadence, deterministic
closed-bar rules, and no ML. The Q02 row and current repository artifacts
agree on all three execution hashes:

| Artifact | SHA-256 |
| --- | --- |
| MQ5 | `d7eec6373be1b7b865902cd8420b2a3fe305c7a7e026603e9baef9e32ee1e7bc` |
| EX5 | `f17045107715b1ee127011f14ee60ad36e6ad23e8dbfc372819a27d4bb596280` |
| AUDUSD D1 setfile | `6ed5b4fe7549362276fa8812bcbf9450f6aca2b3b03fd169336a89799874bd47` |

The setfile contains `RISK_FIXED=1000`, `RISK_PERCENT=0`, and
`PORTFOLIO_WEIGHT=1`.

No MQ5 was generated or edited and no compile command was enqueued. The
binding framework-input audit was nevertheless run read-only against the
absolute source path before the queue mutation:

```powershell
python C:/QM/repo/tools/strategy_farm/audit_framework_input_pins.py --check-source "C:/QM/repo/framework/EAs/QM5_41335_fx-usd-exhaustion-reversal-opt/QM5_41335_fx-usd-exhaustion-reversal-opt.mq5"
```

It exited zero with `ok=true`, predicate `EA_FRAMEWORK_INPUT_PINNED`, and zero
findings.

Immediately before enqueue, five one-second whole-host CPU samples were
89.649597%, 91.887566%, 81.274990%, 83.790696%, and 85.060249% (average
86.332620%, maximum 91.887566%). Both measures were below the 97% hard
ceiling. The canonical database had two active work items, below
`BUILD_BACKPRESSURE_ACTIVE_WORK_ITEM_LIMIT=7`, and zero open QM5_41335 Q04
rows.

## Safety

- No Card, EA source or binary, setfile, registry, magic row, compile work,
  verdict, hold, claim, terminal, or tester was changed.
- No dispatch tick was run; the resident paced workers own eventual claim and
  execution of the pending Q04 row.
- No portfolio-admission, portfolio KPI, Q08-contribution, portfolio-gate,
  deploy-manifest, `T_Live`, or AutoTrading surface was touched.
- Existing unrelated shared-worktree changes were left untouched and excluded
  from this commit.

Machine-readable companion:
`artifacts/qm5_41335_fx_q04_enqueue_20260910T090544Z_board_advisor.json`.

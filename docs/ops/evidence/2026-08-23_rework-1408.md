# rework-1408 review-fix evidence

**Router task:** `116de4eb-f236-4302-9622-dbfe69c49c1d`
**EA:** `QM5_1408_classical-bull-flag-continuation-h1`
**Worktree:** `C:/QM/worktrees/rework-slot-5`
**Date executed:** 2026-08-24
**Scope:** build/review repair only; no backtest enqueue, verdict overwrite, factory toggle, registry edit, or T_Live write.

## Pre-flight and work order

- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1408_classical-bull-flag-continuation-h1.md:1` has `g0_status: APPROVED`, `ea_id: QM5_1408`, and slug `classical-bull-flag-continuation-h1`.
- EA registry: `framework/registry/ea_id_registry.csv:291` is the active matching row.
- Magic registry: `framework/registry/magic_numbers.csv:17707` through `:17720` provide the existing 14 active symbol slots. No allocation gap existed, so neither registry nor the resolver was changed.
- Review work order: `docs/ops/evidence/2026-08-22_codex_review_qm5_1408.md:20` through `:42`.
- Baseline hardening reproduced eight failures: MAE hook, request initialization, two buffer bounds, pending-order type, SMA direction, news window, and news-before-management.

## What changed

### Card-faithful entry and geometry

- `framework/EAs/QM5_1408_classical-bull-flag-continuation-h1/QM5_1408_classical-bull-flag-continuation-h1.mq5:67` defines the card's absolute eight-H1-bar order lifetime.
- The entry request remains `QM_BUY_STOP` and is fully initialized at `:694` through `:763`; it is never converted to a market BUY.
- Existing pending exposure is removed only through `QM_TM_RemovePendingOrder`, repriced from the same persisted pole identity each H1 bar, and invalidated on expiry, gate failure, or flag staleness (`:234` through `:257`, `:706` through `:765`).
- Gate 6 now requires at least two high pivots and two low pivots with fitted parallel lines. The old regression/high-low fallback is gone (`:598` through `:618`).

### Protective management and restart behavior

- Setup identity, upper-line geometry, lifecycle phase, TP1 completion, and cooldown are persisted and flushed in terminal globals keyed by account, resolved framework magic, and symbol (`:145` through `:183`, prefix at `:930`).
- On restart, a still-valid news-disarmed pending setup is retained; an orphan pending is cancelled fail-closed; an open position missing durable geometry arms a governed fail-closed exit (`:934` through `:977`, `:877` through `:882`).
- TP1 uses `QM_TM_PartialClose` and the break-even move uses `QM_TM_MoveSL` (`:834`, `:853`). No raw `CTrade` operation remains.
- The pattern-failure projection advances from the persisted anchor using the current anchor shift instead of freezing `curr_shift=1` (`:858` through `:903`).

### News, framework, risk, and buffers

- The card's entry-only +/-3 H1 bar window is implemented as a literal 180/180-minute check (`:668` through `:687`).
- `OnTick` begins with the direct MAE hook; position management and exits precede news-based entry suppression (`:993` through `:1019`).
- Framework initialization retains the QM_Common chain, MagicResolver-backed `QM_FrameworkMagic()`, and fixed/percent risk inputs (`:917` through `:926`). All 14 backtest setfiles were read: `RISK_FIXED=1000`, `RISK_PERCENT=0`, zero mismatches.
- ATR and SMA CopyBuffer consumers now have separate result-count and ArraySize guards; SMA rising order is newer `[1]` versus older `[0]` (`:323` through `:367`).
- Every declared Strategy-group input has at least one executable use-site; the grep census reported no missing input.

### Documentation and tests

- `framework/EAs/QM5_1408_classical-bull-flag-continuation-h1/SPEC.md:30` now documents the actual BUY-STOP lifecycle; required source and risk sections are at `:120` and `:126`.
- `tools/strategy_farm/tests/test_qm5_1408_rework_static.py:21` through `:76` lock hardening PASS, pending/pivot semantics, governed/restart-safe management, news ordering, and strategy-input wiring.

## Validation output

### Passing checks

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_1408_rework_static.py
....                                                                     [100%]
4 passed in 0.73s
```

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_1408_classical-bull-flag-continuation-h1
files_scanned: 1
failures: []
warnings: []
```

```text
python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_1408_classical-bull-flag-continuation-h1
files_checked: 15
findings: []
verdict: PASS
```

```text
python framework/scripts/validate_spec_doc.py framework/EAs/QM5_1408_classical-bull-flag-continuation-h1
PASS QM5_1408_classical-bull-flag-continuation-h1
Summary: 1 PASS, 0 FAIL
```

```text
python framework/scripts/skill_build_ea_guard.py --ea-id 1408 --ea-label QM5_1408_classical-bull-flag-continuation-h1
status: ok
ea_registry_row: true
magic_registry_rows: true
ea_dir_exists: true
```

The combined relevant suite produced `52 passed, 1 failed in 216.32s`. The sole failure is pre-existing and outside this ticket: `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven findings in `QM5_411xx` EAs; it does not reference EA 1408 or any changed file. The ticket-specific suite and scoped hardening both pass as shown above.

### Governed compile/build-check interlock

The required scoped commands were attempted and were refused before MetaEditor or build checks ran:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1
  -EAPath framework/EAs/QM5_1408_classical-bull-flag-continuation-h1/QM5_1408_classical-bull-flag-continuation-h1.mq5 -Strict
compile_one.result=FAIL
compile_one.reason_class=INCLUDE_MIRROR_REFUSED
detail=terminal64 processes are alive; ad-hoc compile/build_check is refused
```

Machine artifact: `D:/QM/reports/compile/20260824_072026/summary.csv:2`.

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1
  -RepoRoot . -EALabel QM5_1408_classical-bull-flag-continuation-h1 -Strict -SkipCompile
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED
detail=terminal64 processes are alive; ad-hoc compile/build_check is refused
```

The read-only governed compile classifier also returned `eligible=false` with `EX5_ALREADY_PRESENT` and `BOUND_SETFILE_HASH_EXISTS`; `source_repair_authorized=false`. Therefore no compile work item was enqueued and no terminal/factory process was touched.

Current hashes:

- repaired MQ5 SHA-256: `ef1e99b231fed112e15831438412c96fc10bda6ec35b83b981d88e14bb6eb113`
- existing, unchanged EX5 SHA-256: `d4a41c274c53c48ffae719c4103d8fa3c614798bfab76e381250b06b0fa292bb`

The unchanged EX5 and existing setfile `build_hash` values remain bound to the rejected source. A fresh EX5/hash rebind requires a governed source-repair compile authority or a quiescent compile window; this ticket did not grant authority to bypass that contract.

## Rollback

Revert the single `rework(1408)` commit produced by this ticket. That restores the prior MQ5 and SPEC and removes the focused test/evidence file. The EX5, setfiles, registries, runtime DB verdict rows, queue, factory state, and T_Live were not changed, so they require no rollback.

# QM5_38005 Build EA Evidence — 2026-08-18

**Task ID:** `2f3177d2-2769-4e0d-a212-4769b908178c` (`build_ea`, priority 50, agent `gemini`).  
**EA:** `QM5_38005_codetrading-ascending-triangle-breakout`.  
**Artifact Path:** `C:/QM/repo/docs/ops/evidence/2f3177d2_qm5_38005_build_ea_2026-08-18.md`.  
**Strategy Card:** `strategy-seeds/cards/approved/QM5_38005_codetrading-ascending-triangle-breakout.md` (G0 APPROVED).  

---

## 1. Strategy Overview & Implementation

- **Strategy Thesis**: Systematic price action breakout system on H1 detecting Ascending and Descending Triangle chart patterns via swing pivot geometry and volume confirmation.
  - Long (BUY): Ascending triangle with horizontal resistance ceiling ($|\text{slope}| \le 0.05$) across two pivot highs and ascending support lows ($L_1 > L_2$), breakout bar close $> \text{Resistance} + 2.0\text{ pips}$, and Volume $> 1.3 \times \text{SMA}(\text{Vol}, 20)$.
  - Short (SELL): Descending triangle with horizontal support floor ($|\text{slope}| \le 0.05$) across two pivot lows and descending resistance highs ($H_1 < H_2$), breakdown bar close $< \text{Support} - 2.0\text{ pips}$, and Volume $> 1.3 \times \text{SMA}(\text{Vol}, 20)$.
  - Exits & Stops: Stop Loss placed at the most recent swing pivot extreme ($\mp 2.0\text{ pips}$), Take Profit targeting $1:2.0\text{ R:R}$, and Break-Even protection upon $+1.0\text{R}$ floating profit.
- **Framework Corset Compliance**:
  - Implemented strictly with `EA_Skeleton.mq5` wiring and framework readers.
  - Closed-bar calculation cached in `AdvanceState_OnNewBar()` to ensure $O(1)$ per-tick performance.
  - Sizing handled strictly by `QM_LotsForRisk` from `req.sl`.
  - News temporal mode (`QM_NEWS_TEMPORAL_PRE30_POST30`) and compliance (`QM_NEWS_COMPLIANCE_DXZ`) with fail-closed stale guard (`qm_news_stale_max_hours = 336`).

---

## 2. Registry & Artifact Verification

- **Registries Updated**:
  - `framework/registry/ea_id_registry.csv`: verified `38005,codetrading-ascending-triangle-breakout`.
  - `framework/registry/magic_numbers.csv`: allocated slots 0 (`XAUUSD.DWX`, `380050000`), 1 (`SP500.DWX`, `380050001`), 2 (`EURUSD.DWX`, `380050002`).
  - `framework/include/QM/QM_MagicResolver.mqh`: regenerated cleanly via `update_magic_resolver.py`.
- **SPEC Document**:
  - `framework/EAs/QM5_38005_codetrading-ascending-triangle-breakout/SPEC.md` written and verified with `validate_spec_doc.py` (`PASS`).
- **Static Checks & Compilation**:
  - `build_check.ps1`: `compile_one.result=PASS`, `errors=0`, `warnings=0`, `build_check.result=PASS`.
  - Emitted binary: `framework/EAs/QM5_38005_codetrading-ascending-triangle-breakout/QM5_38005_codetrading-ascending-triangle-breakout.ex5`.
- **P2 Setfiles**:
  - Generated for `XAUUSD.DWX`, `SP500.DWX`, `EURUSD.DWX` via `gen_setfile.ps1`.

---

## 3. Smoke Test & Review State

- `run_smoke.ps1`: Encountered standalone admission rejection (`custom_history_smoke_admission.py` requires worker-bound work item under active Custom-history isolation).
- Build result JSON emitted honestly per schema to `artifacts/qm5_38005_build_result.json` with `build_check_passed=true`, `compile_succeeded=true`, `smoke_result=framework_error`.
- Task updated to `REVIEW` for mandatory Codex review per router rules.

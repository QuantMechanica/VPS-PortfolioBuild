# QM5_1408 mandatory Codex review — 2026-08-22

## Verdict

`RECYCLE` — the candidate fails the strict current-build hardening contract and does not implement the approved pending-order lifecycle. No candidate code, binary, setfile, or registry row was changed. This is not a Q-phase or pipeline verdict.

Source task: `7dccf57e-a995-465f-a9b7-98e7b945a651`  
Codex review task: `116de4eb-f236-4302-9622-dbfe69c49c1d`  
Gemini commit: `28c6e5427b6f2a9ac30624b00b965e70419c6d04`  
Identity artifact: `docs/ops/evidence/7dccf57e_qm5_1408_classical-bull-flag-continuation-h1_build_identity.json`

## Verification

- `validate_build_guardrails.py`: PASS, but this broad guardrail result is not the strict D7-D10 review gate.
- Strict `build_gate_hardening.py`: **4 failures** — missing direct `QM_FrameworkTrackOpenPositionMae()` at `OnTick`, incomplete/bare `QM_EntryRequest`, and two unbounded indicator-buffer reads.
- Focused guardrail/resolver tests: 21 passed.
- Fourteen setfiles use `RISK_FIXED=1000`, `RISK_PERCENT=0`; source news staleness default is 336 hours.
- MQ5 SHA-256: `38b9ddf1d6345c5a87fc2e3c359f94297b2de0d01230621d7e36cd967d336c74`.
- EX5 SHA-256: `d4a41c274c53c48ffae719c4103d8fa3c614798bfab76e381250b06b0fa292bb`.

## Blocking findings

1. **Strict build hardening fails.** The source has no direct MAE hook at the head of `OnTick` (line 619), sends a bare request without the required complete seven-field initialization (lines 652-656), and triggers the strict buffer-bound checks at lines 192 and 312. A Gemini identity claim and permissive guardrail PASS cannot override those machine findings.

2. **The card's BUY-STOP lifecycle is replaced by a market entry.** The card requires a BUY-STOP at the projected upper trendline plus 0.4 ATR, valid for eight H1 bars with per-bar re-evaluation and stale cancellation (card lines 96-99). The source instead waits for the prior H1 close to exceed the threshold, fills `req.price` with the current ask, and immediately calls the market entry consumer (MQ5 lines 468-503 and 649-656). It has no pending order, expiry, cancellation, or eight-bar order state.

3. **The mandatory two-pivot channel can be bypassed.** Card gate 6 requires at least two pivot highs and two pivot lows with fitted parallel trendlines. If either side has fewer than two pivots, the implementation silently substitutes the close-regression slope and absolute window high/low as channel lines (MQ5 lines 422-450), allowing patterns that fail the approved structural gate.

4. **News blackout suppresses risk exits and position management.** `OnTick` returns when news disallows trading before partial management, pattern-failure exit, or time-stop evaluation (MQ5 lines 618-634). The card defines news as an entry filter; an entry blackout must not disable protective exits on an already-open position.

5. **Open-position management is not restart-safe.** TP1, channel slopes/intercept, reference shift, and entry time are RAM-only globals. Restarting with an open position disables the partial and the first-six-bars pattern-failure exit. The source also uses raw `CTrade` for partial close/SL modification rather than the governed trade-manager path (lines 524-548).

6. **The pattern-failure line is frozen.** The exit always calculates the projection with `curr_shift=1` and the unchanged entry-time reference (MQ5 lines 563-573). It does not advance the trendline as new H1 bars arrive, contrary to the card's `upper_TL(t)` current-bar projection.

## Required recycle scope

Rebuild the exact approved mechanic with a governed BUY-STOP lifecycle, strict D7-D10 compliance, mandatory pivot geometry, entry-only news blocking, restart-safe managed-position state, governed partial/SL operations, and a time-advancing failure line. Recompile through the governed path and submit a new binary/hash pair for independent Codex review. Do not move this candidate to PIPELINE.

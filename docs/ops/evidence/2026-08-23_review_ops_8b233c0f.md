# Review — OPS-GATE-MANIFEST-V3-ACTIVATE (task 8b233c0f)

Date: 2026-08-23
Reviewer: Claude (review lane), read-only DB, agents/board-advisor
Task: `8b233c0f-a112-4ee4-8107-915a4fe6f1b6` (ops_issue, state REVIEW)
Goal id: `OPS-GATE-MANIFEST-V3-ACTIVATE`

## Verdict: APPROVED

The delivered work met the activation goal, was committed cleanly, and has since
been superseded by the v4 activation. No thresholds, verdicts, seeds, or windows
were changed; no rows mutated; Factory not toggled; nothing recompiled.

## What was verified (evidence)

- **Delivery + commit.** The v3 activation shipped as commit
  `d4e4dcfcb ops(pipeline): activate gate manifest v3 as default (OWNER E3,
  reviews 9b40ff25/d5c13a08)` — 5 files, +408/-36. `git show` confirms the diff
  matches the evidence doc `docs/ops/evidence/2026-08-23_gate_manifest_v3_activation.md`.
- **Activation guard, fail-closed (item 1).** `git show
  d4e4dcfcb:tools/strategy_farm/config/gate_manifest.v3.json`
  (`extension_topology.activation_guard`, line 89) flips `state`
  `READ_INERT`→`ACTIVE`, `default_manifest_switch` `false`→`true`, and records
  `activated_by=CLAUDE`, `activated_at=2026-08-23`, `review_refs=["9b40ff25",
  "d5c13a08"]` — both required review refs present. Loader enforcement
  (`_validate_v3_activation_guard`) rejects READ_INERT-as-default and ACTIVE
  without both refs / switch / approver.
- **Loader accessors (item 3, exposure).** Present in
  `tools/strategy_farm/gate_manifest.py`: `baseline_stage` (292),
  `baseline_reuse_policy` (299), `q16_dependencies` (317), `portfolio_routes`
  (327), `portfolio_route` (333), `activation_state` (344). The Q10A/Q16
  dependency roles and Q11 routing rule are queryable through the loader.
- **Tests green.** `python -m pytest tools/strategy_farm/tests/test_gate_manifest.py
  -q` → 26 passed, 2 skipped (jsonschema-optional + one env-conditional). No
  regression in the gate-manifest lineage.
- **Item 3 deviation is correct.** Runtime enqueue rebinding of
  `Q10A_BASELINE`/`Q10_INCUMBENT` into `work_item_dependencies` was deliberately
  deferred: it would require a DB CHECK-constraint migration and a decision about
  which lineage supplies the reusable Q08 baseline — i.e. a change to what Q16
  compares (gate semantics = ROT), explicitly out of scope per OWNER E3
  (`decisions/2026-08-22_owner_pipeline_realignment_q09_q11.md`). Stopping rather
  than guessing on gate semantics was the right call.

## Note on supersession (not a defect)

The current working tree has advanced to Gate Manifest **v4**
(`SCHEMA_VERSION = SCHEMA_VERSION_V4`, `DEFAULT_MANIFEST = V4_MANIFEST`; commit
`2f0777085`, config `gate_manifest.v4.json` present). v3's activation_guard was
restructured out during the v4 build. This is an orchestration progression
(v3 → v4 linear Q00–Q17), not a fault in the v3 deliverable, which correctly
achieved its stated acceptance at delivery time.

## Disposition

APPROVED — formally clean; the v3 activation task is complete and its lineage now
lives inside the active v4 manifest. Task closed to unblock the review lane.

# QM5_1409 mandatory Codex review — 2026-08-22

## Verdict

`RECYCLE` — the candidate fails eight strict hardening checks and has material Strategy Card/state-management divergences. No candidate artifact was changed. This is not a Q-phase or pipeline verdict.

Source task: `0d490609-922c-47df-a3eb-2a27412e3796`  
Codex review task: `9b0b37f8-087b-436e-a044-8b004c20905f`  
Gemini commit: `28c6e5427b6f2a9ac30624b00b965e70419c6d04`  
Identity artifact: `docs/ops/evidence/0d490609_qm5_1409_wyckoff-sign-of-strength-phase-d-h4_build_identity.json`

## Verification

- `validate_build_guardrails.py`: PASS, but it does not satisfy the strict D7-D10 review gate.
- Strict `build_gate_hardening.py`: **8 failures** — missing direct MAE hook, incomplete/bare entry request, and six unbounded buffer/index findings at source lines 200, 264, 265, 433, and 558.
- Focused guardrail/resolver tests: 21 passed.
- Fourteen setfiles use `RISK_FIXED=1000`, `RISK_PERCENT=0`; source news staleness default is 336 hours.
- MQ5 SHA-256: `deffd643e4c78e12f58960cca2568e737bd90542a235eca83777c531e8dfaeb3`.
- EX5 SHA-256: `71640fddfa134ae235543e034e67a72737a4fa83830b088219b28a23f4648979`.

## Blocking findings

1. **Strict build hardening fails.** `OnTick` lacks the direct MAE hook (line 614), the request sent to `QM_TM_OpenPosition` omits complete initialization including expiry (lines 647-651), and strict buffer-bound evidence is missing at six reported accesses. The build identity's PASS claim cannot override these findings.

2. **The trading-range band algorithm does not implement the card.** The card requires bounds after dropping the extreme 5% wicks and describes low/high 20% bands. The source computes `trim_cnt` but never uses it, then directly selects the 20th percentile of lows and 80th percentile of highs (MQ5 lines 256-265). The claimed robust trimming is therefore absent, changing every downstream range, spring, SOS, LPS, stop, target, and failure threshold.

3. **The emitted universe is broader than the approved target list.** The card names EURUSD, GBPUSD, USDJPY, XAUUSD, GER40, NDX, WS30, and XTIUSD. The build emits 14 symbols, adding AUDUSD, NZDUSD, SP500, UK100, USDCAD, and USDCHF. The GER40 token also requires an explicit governed reconciliation to the canonical broker symbol GDAXI; silently expanding the registry is not card approval.

4. **News blackout disables protective management.** As in QM5_1408, `OnTick` returns on a blocked news window before partial management and pattern/time exits. The approved news rule is an entry filter, not authority to suspend risk exits on an open trade.

5. **TP1 and pattern-failure state disappears on restart.** The active TP1, range high, measured move, and TP1 completion flag are RAM-only. A restart with an open position disables both the partial-close trigger and the range-failure exit, leaving only the broker SL/TP and time stop. Partial close and SL modification also bypass the governed trade manager via raw `CTrade` (MQ5 lines 508-547).

6. **Reuse-after-invalidation is absent.** The card requires an 80-H4-bar guard after entry or invalidation. The durable check only searches entry deals; the invalidation block is an in-memory timestamp set while preparing a request and is lost on restart. No invalidated Wyckoff pattern identity is persisted.

7. **Volume reliability is not resolved per symbol.** The card requires gate-8 bypass only where tick volume is unreliable, with a per-symbol bias note. One global input/default is applied across all 14 setfiles; no governed per-symbol reliability classification or bias note drives the decision.

## Required recycle scope

Resolve the approved canonical symbol universe, implement the literal trimmed-band construction, and rebuild with strict buffer/request/MAE compliance. Keep news blocking entry-only; persist active range/TP1/reuse state across restarts; use governed trade-manager operations; and encode a deterministic per-symbol volume-reliability policy. Recompile via the governed path and return the new binary/hash pair for independent Codex review. Do not move this candidate to PIPELINE.

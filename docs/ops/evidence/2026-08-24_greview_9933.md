# greview-9933 — QM5_9933 cross-review

- Ticket: `greview-9933`
- Router task: `c10c1e2c-6932-4db8-afdd-40907ea34246`
- Reviewer: Codex (cross-review of a Gemini/agy build)
- Date: 2026-08-24
- EA: `QM5_9933_bandy-choppiness-index-sideways-rsi2-mr-index`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9933_bandy-choppiness-index-sideways-rsi2-mr-index.md`
- Verdict: **RECYCLE**

## Governing context and payload

Read before review:

- `docs/ops/rebaseline/GATE_MANIFEST_V4_LINEAR_PROPOSAL_2026-08-23.md`
- `docs/ops/rebaseline/FACTORY_AUTOMATION_INVENTORY_2026-08-23.md`
- `docs/ops/rebaseline/GATE_NAME_CENSUS_2026-08-23.md`
- `G:/My Drive/QuantMechanica - Company Reference/03 Pipeline/Pipeline Rebaseline Directive 2026-08-23.md`

`python tools/strategy_farm/agent_router.py list-tasks --agent gemini --state REVIEW`
returned the requested task in `REVIEW` with `ea_id=9933` and slug
`bandy-choppiness-index-sideways-rsi2-mr-index`. A read-only SQLite query
(`file:D:/QM/strategy_farm/state/farm_state.sqlite?mode=ro`) resolved its current
artifact to
`C:/QM/repo/docs/ops/evidence/c10c1e2c_qm5_9933_bandy-choppiness-index-sideways-rsi2-mr-index_build_identity.json`.
The repository copy of that receipt binds the source and binary hashes at
`docs/ops/evidence/c10c1e2c_qm5_9933_bandy-choppiness-index-sideways-rsi2-mr-index_build_identity.json:6-9`.

## Findings

### 1. Card-faithful trading mechanism — PASS

The approved card requires D1 CI(14), RSI(2), and SMA(200); a long entry when
`CI >= 61.8`, `RSI <= 10`, and close is above SMA; RSI/time/ATR exits; a
degenerate-range guard; and fixed $1,000 Q02 risk
(`D:/QM/strategy_farm/artifacts/cards_approved/QM5_9933_bandy-choppiness-index-sideways-rsi2-mr-index.md:42-68`).

The EA implements the bounded, cached CI calculation with `bars_needed =
strategy_ci_period + 1`, a `CopyRates(...) == bars_needed` proof, and a
`perf-allowed` annotation (`framework/EAs/QM5_9933_bandy-choppiness-index-sideways-rsi2-mr-index/QM5_9933_bandy-choppiness-index-sideways-rsi2-mr-index.mq5:58-129`).
The entry conditions, long-only request, fixed ATR stop, RSI exit, and held-D1
period exit are at the same file's lines 160-228. No ML, grid, martingale,
commission, or swap mechanics are present.

Every declared `strategy_*` input has an executable use-site (declaration and
all grep hits shown):

| Input | Lines |
|---|---|
| `strategy_ci_period` | 38, 79, 82, 94, 117, 147 |
| `strategy_ci_threshold` | 39, 148-149, 181 |
| `strategy_rsi_period` | 40, 150, 184, 220 |
| `strategy_rsi_entry_threshold` | 41, 151-152, 185 |
| `strategy_rsi_exit_threshold` | 42, 152, 221 |
| `strategy_regime_sma_period` | 43, 153, 188 |
| `strategy_atr_period` | 44, 154, 193 |
| `strategy_atr_stop_mult` | 45, 155, 197 |
| `strategy_time_stop_bars` | 46, 156, 228 |
| `strategy_warmup_bars` | 47, 139, 169 |

There are no dead strategy inputs.

### 2. Framework conformity — PASS

- `QM_Common.mqh` is the sole framework include (`.mq5:5`).
- `QM_FrameworkTrackOpenPositionMae()` is the first OnTick action (`.mq5:272`).
- Backtest defaults are `RISK_FIXED=1000` and `RISK_PERCENT=0` (`.mq5:19-20`);
  the framework rejects both-zero and both-set modes in
  `framework/include/QM/QM_Common.mqh:166-178`.
- The entry request carries `req.symbol_slot = qm_magic_slot_offset`
  (`.mq5:166`); init resolves it through `QM_MagicChecked`
  (`framework/include/QM/QM_Common.mqh:225`), and the generated resolver maps
  each current row to `ea_id * 10000 + slot`.
- The central news filter is wired through `QM_NewsAllowsTrade2` with legacy
  fallback (`.mq5:275-282`).
- The raw `CopyRates` access is bounded and count-checked before indexing;
  `build_gate_hardening` D10 reports no failure.

### 3. Delivered build identity — PASS; binary is current

Read-only hashes of the current worktree delivery are:

```text
mq5  1136450b351d39bb4b7e432483d4244e23a0b6e88e3ad77bf4e70770acd4dcf8
ex5  64c5c66072944ed76e7846ea24630af3134df1ff3709d36b751724035a489e71
```

They exactly match the submitted build-identity receipt
(`...build_identity.json:6-9`). The canonical compile receipt at
`C:/QM/repo/framework/EAs/QM5_9933_bandy-choppiness-index-sideways-rsi2-mr-index/compile.log:74`
reports `Result: 0 errors, 0 warnings`. The canonical source bytes also equal
the current worktree source bytes. Therefore this review found no stale `.ex5`.

### 4. Governed setfile shape and risk — PASS

The delivered setfiles use the governed `QM5 Set File` header, fixed backtest
risk, registered slot, and explicit strategy defaults. For example, the
SP500 preset identifies D1/backtest/slot 2 at
`framework/EAs/QM5_9933_bandy-choppiness-index-sideways-rsi2-mr-index/sets/QM5_9933_bandy-choppiness-index-sideways-rsi2-mr-index_SP500.DWX_D1_backtest.set:2-18`
and binds `RISK_FIXED=1000`, `RISK_PERCENT=0` at lines 19-20. This matches the
governed generator format in `framework/scripts/gen_setfile.ps1:615-646`.

### 5. Card-authorized universe — FAIL (recycle defect)

The approved card explicitly limits this long-only equity-index strategy to
`SP500.DWX`, `NDX.DWX`, and `WS30.DWX`
(`D:/QM/strategy_farm/artifacts/cards_approved/QM5_9933_bandy-choppiness-index-sideways-rsi2-mr-index.md:70-71`).
The delivery instead exposes 13 backtest presets and 13 active registry rows.
Ten are outside the approved card:

```text
AUDUSD.DWX, EURUSD.DWX, GBPUSD.DWX, GDAXI.DWX, NZDUSD.DWX,
UK100.DWX, USDCAD.DWX, USDCHF.DWX, USDJPY.DWX, XAUUSD.DWX
```

Representative executable artifacts are the EURUSD and XAUUSD presets, which
declare those symbols at their respective line 7:

- `framework/EAs/QM5_9933_bandy-choppiness-index-sideways-rsi2-mr-index/sets/QM5_9933_bandy-choppiness-index-sideways-rsi2-mr-index_EURUSD.DWX_D1_backtest.set:7`
- `framework/EAs/QM5_9933_bandy-choppiness-index-sideways-rsi2-mr-index/sets/QM5_9933_bandy-choppiness-index-sideways-rsi2-mr-index_XAUUSD.DWX_D1_backtest.set:7`

The corresponding active magic rows are visible at
`framework/registry/magic_numbers.csv:17150-17162`; only rows for slots 1
(`NDX`), 2 (`SP500`), and 4 (`WS30`) are card-authorized. This is a material
card-conformance failure, not an economic question for Q02: the unauthorized
pairs must not be admitted to Q02 in the first place.

The read-only hardening invocation returned PASS only because its output showed
`card_target_symbols: []`. The parser currently accepts frontmatter
`target_symbols: [...]` or a labeled `Target symbols:` line
(`tools/strategy_farm/build_gate_hardening.py:1456-1487`), but not this card's
`## Target symbols` heading followed by prose. The governing D17 unit test
confirms that an extra observed symbol outside an explicit card universe must
fail (`tools/strategy_farm/tests/test_build_gate_hardening.py:564-581`). The
manual review therefore cannot treat the parser coverage gap as approval.

## Read-only checks and tests

Commands:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_9933_bandy-choppiness-index-sideways-rsi2-mr-index
python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_9933_bandy-choppiness-index-sideways-rsi2-mr-index
python framework/scripts/update_magic_resolver.py --dry-run
```

Results:

- EA-specific hardening: no source/buffer/matrix failures; it exposed the
  `card_target_symbols: []` parser gap described above.
- Build guardrails: `PASS`, 14 files checked, zero findings.
- Resolver dry-run: fail-closed because unrelated active EA directories 1001,
  1015, and 1016 are absent in this worktree. Direct parsing of the current
  generated resolver nevertheless confirmed all 9933 rows and exact
  `99330000 + slot` values.

Targeted pytest command covering D10, D15, D17, risk/news guardrails, governed
setfile fallback, and resolver symbol/hash/lookup behavior:

```text
12 passed in 2.33s
```

The wider relevant-module run completed with:

```text
2 failed, 62 passed in 342.56s
```

Both failures are pre-existing repo-global fixtures outside QM5_9933: the
QM5_411xx corpus currently has seven D10 findings, and the resolver-wide test
sees missing EA directories 1001/1015/1016. Neither changes this EA verdict;
both are recorded rather than hidden.

No compile, backtest enqueue/delete, factory toggle, verdict overwrite, gate
criterion change, or T_Live access was performed.

## Verdict and named repair

**RECYCLE.** The EA mechanism and current binary are otherwise Q02-ready, but
the delivery is not card-clean while it exposes ten unauthorized symbols.

Required repair:

1. Limit governed backtest setfiles to the approved card universe:
   `SP500.DWX`, `NDX.DWX`, and `WS30.DWX`.
2. Reconcile the ten extra active magic rows through the governed registry
   workflow (retire; do not delete or hand-edit), regenerate and verify
   `QM_MagicResolver.mqh`.
3. Recompile through governed `COMPILE_EA`, refresh the build-identity and
   compile receipts, rerun both guardrail commands, and resubmit for review.

## Rollback

This ticket changes only this evidence note plus the router close transition.
Rollback the repository part with `git revert <review-commit>`; any router
state correction must use the governed router transition workflow and must not
overwrite historical verdict/evidence rows.

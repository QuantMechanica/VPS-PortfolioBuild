# Review — QM5_35008 short-term-bollinger-reversion-system

- Router task: `0ddd5459-1bc9-43cd-bc07-f9eb3ee8f7ca` (review_ea, source_agent gemini/agy)
- Build artifact: `artifacts/builds/60e3146d-c363-4b7c-af29-18380260e8f1.json`
- Source verdict claimed: `SOURCE_READY_REMEDIATED ... strict static gates PASS`
- EA dir: `framework/EAs/QM5_35008_short-term-bollinger-reversion-system/`
- Card: `strategy-seeds/cards/approved/QM5_35008_short-term-bollinger-reversion-system.md`
- Reviewed mq5 sha256: `3c9ad2838912aa02099a87e85d98625f7f17579e99101c28b25bbe96210ece22` (== artifact `mq5_sha256`, current on disk)

## Verdict: RECYCLE

Static source review passes; the delivered **ex5 binary is stale** and does not implement
the reviewed source. The artifact's `compile_succeeded:true` / `build_check_passed:true`
for the current source hash is not truthful.

## Static source review — PASS

- **Card-faithful mechanism**: BB(20, 2.5σ), RSI(14), ATR(14); session 18:00–22:00 GMT
  evaluated at shift=1; long `Low[1]<=LowerBB[1] & Close>Open & RSI<=30`, short mirror;
  TP=20-SMA midline, SL=entry∓1.5·ATR; time exit 23:00; no-trade filter = rollover
  23:55–00:05 UTC, spread>1.8·ATR, 2.0% daily-loss halt, max 1 position. All present and
  match card §3–§4.
- **Dead-input scan**: all 15 `strategy_*` inputs have ≥1 use-site beyond declaration
  (grep count ≥2 each incl. declaration); the three DD/risk-cap inputs feed
  `QM_KillSwitchInit`. No dead inputs.
- **Framework conformity**: `#include <QM/QM_Common.mqh>`; MAE hook
  `QM_FrameworkTrackOpenPositionMae()` in OnTick; RISK_PERCENT=0.0 / RISK_FIXED=1000.0
  (backtest); magic via `QM_FrameworkMagic()` (registry rows 350080000/1/2 = 35008*10000+slot,
  active); news filter wired (`QM_NewsAllowsTrade2`); bounded indicator wrappers
  (`QM_SMA/QM_BB_Upper/QM_BB_Lower/QM_RSI/QM_ATR`); raw `iOpen/iHigh/iLow/iClose[1]`
  carry perf-allowed comments behind `QM_IsNewBar()`; no ML; no invented
  commission/swap/DST.
- **Setfiles**: governed generator (QM5 Set File header, set_version s20260823-001);
  ENV=backtest, RISK_FIXED=1000/RISK_PERCENT=0, magic_slot 0, all card params present and
  correct.

## Blocking defect — stale ex5 (recompile required before Q02)

- ex5 on disk sha256 `998a192d...` was last committed in `bf720ba6f` (initial build,
  2026-08-17). The mq5 was subsequently rewritten in `a5854b0eb`
  ("remediate ... per card requirements and codex findings", 2026-08-23) and **the ex5
  was not recompiled** — ex5 mtime 2026-08-17 22:50 vs mq5/SPEC mtime 2026-08-23 13:29;
  ex5 has no later commit.
- `git diff bf720ba6f a5854b0eb` on the mq5 shows the remediation added real logic the
  stale binary lacks: `RISK_PERCENT` 0.5→0.0, kill-switch wiring `QM_KillSwitchInit`
  (daily hard stop / total DD / per-trade cap), `QM_BrokerToUTC` on all time gates,
  daily-loss entry halt, max-open-position no-trade condition, and revised TP/SL. Old
  source (`git show bf720ba6f:...mq5`) has **no** `QM_KillSwitchInit`, **no**
  `QM_BrokerToUTC`, **no** daily-halt inputs.
- Consequence: a Q02 run against the current ex5 tests pre-remediation code (no kill
  switch, broker-local instead of UTC session windows, RISK_PERCENT default 0.5). The
  setfile's `strategy_daily_*` / `strategy_per_trade_risk_cap_pct` keys would be silently
  ignored by the old binary. This is the known "stale ex5 voids healthy backtests" class.

## Actionable remediation (build lane / COMPILE_EA in OFF window)

1. Recompile the current mq5 (`a5854b0eb`, sha `3c9ad283...`) so the ex5 matches the
   reviewed source; re-verify compile (COMPILE_OK) for that hash.
2. Correct the build artifact's `compile_succeeded`/`build_check_passed`/`ex5_sha256` to
   reflect the recompiled binary.
3. Re-submit for review with the refreshed ex5; static source review is already clean, so
   the follow-up is a compile/binary-parity confirmation only.

Reviewer: Claude (review lane). Read-only DB; no factory/T_Live/recompile actions taken.

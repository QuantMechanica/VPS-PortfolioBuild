# rework-1416 review-fix evidence

- Router task: `25b11704-7145-4489-b8d3-0022f6ffb642`
- EA: `1416 / QM5_1416_classical-bear-flag-continuation-h1`
- Work order: `docs/ops/evidence/2026-08-22_codex_review_qm5_1416.md:11-25`
- Repaired source SHA-256: `b02d732742cd22ca612e6bab4c48251ce1a0266a45fc8119dbd960e33f0f2824`

## Identity and scope evidence

- The EA allocation already exists and is active at `framework/registry/ea_id_registry.csv:299`.
- Its existing magic allocation spans slots 0-13 at
  `framework/registry/magic_numbers.csv:17751-17764`. No registry row or generated
  resolver was changed.
- Only EA 1416 source, specification, its 14 backtest sets, its focused static
  regression test, and this evidence record are part of the repair.
- No backtest was enqueued or deleted, no factory state was changed, and no live
  terminal or `T_Live` path was touched.

## What changed

1. Restored the card's entry mechanism. The EA now requires two pivot highs and
   two pivot lows (`...mq5:615`), submits `QM_SELL_STOP` (`...mq5:672`) at the
   ATR-buffered channel break, and gives it the configured eight-H1-bar lifetime
   (`...mq5:678`). It cancels the pending order when its originating setup becomes
   stale or invalid (`...mq5:708-755`) instead of substituting a market entry.
2. Made the card's high-impact-news blackout exact and entry-only. The literal
   180-minute pre/post filter is at `...mq5:703`; open-position management remains
   reachable before the new-bar entry path (`...mq5:954-963`).
3. Restored framework conformity. Magic comes from `QM_FrameworkMagic`
   (`...mq5:139`), indicator access uses pooled `QM_SMA`/`QM_ATR`
   (`...mq5:361-362,495`), the bounded raw price copies carry their required
   `perf-allowed` annotations (`...mq5:367,507,869`), initialization uses
   `QM_FrameworkInit` (`...mq5:912`), and the direct MAE hook is at `...mq5:954`.
4. Added restart-safe management state. Commit-marked terminal Global Variables
   persist setup, channel, TP1, and reuse state (`...mq5:194-217`), initialization
   restores it (`...mq5:219-274,932-940`), and missing/incoherent position state
   fails closed rather than repeating a partial close.
5. Wired the new `strategy_pending_valid_bars` input (`...mq5:59,678,721`) and
   documented the repaired behavior in `SPEC.md:22-28,56,150`.
6. Updated all 14 existing backtest sets with `strategy_pending_valid_bars=8` and
   the repaired source hash. Their fixed-risk policy, symbol, and allocated magic
   slots remain unchanged.
7. Added six focused regression tests in
   `tools/strategy_farm/tests/test_qm5_1416_rework_static.py:31-137`. They check the
   hardening analyzer, pending-order contract, exact news behavior, persistent
   restart state, executable use-sites for every declared input, framework hooks,
   fixed-risk sets, and registry allocations.

## Validation evidence

Commands were run from `C:\QM\worktrees\rework-slot-6`.

```text
python -m pytest tools/strategy_farm/tests/test_qm5_1416_rework_static.py -q
6 passed in 0.84s

python -m pytest tools/strategy_farm/tests/test_qm5_1416_rework_static.py tools/strategy_farm/tests/test_build_guardrails.py -q
26 passed in 1.47s

python -m pytest tools/strategy_farm/tests/test_build_guardrails.py -q
20 passed in 1.10s

python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py -q -k "not test_qm5_411xx_sources_have_no_unbounded_numeric_buffers"
28 passed, 1 deselected in 114.66s

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_1416_classical-bear-flag-continuation-h1
PASS: checked 15 file(s), zero findings (max_stale_age_days=336)

python framework/scripts/skill_build_ea_guard.py --ea-id 1416 --ea-label QM5_1416_classical-bear-flag-continuation-h1
status=ok; ea_registry=true; magic_registry=true; ea_dir=true

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_1416_classical-bear-flag-continuation-h1
PASS=1 FAIL=0

tools/strategy_farm/build_gate_hardening.py analyzer, EA 1416 only
failures=[] warnings=[] files_scanned=1
```

The unfiltered hardening-test module produced `28 passed, 1 failed in 341.14s`.
The sole failure is the repository-wide 411xx numeric-buffer census
`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`, which reports seven
pre-existing EAs outside this ticket. The scoped rerun above excludes only that
unrelated census; the EA 1416 analyzer is clean.

### Compile interlock

The required strict compile was requested with the EA-scoped command:

```text
powershell -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_1416_classical-bear-flag-continuation-h1/QM5_1416_classical-bear-flag-continuation-h1.mq5 -Strict
```

The governed wrapper refused before launching MetaEditor because active factory
terminal processes triggered `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`. The scoped
`build_check.ps1 -EALabel QM5_1416_classical-bear-flag-continuation-h1 -SkipCompile`
request reached the same compile safety guard. The machine-readable refusal is in
`D:\QM\reports\compile\20260824_071854\summary.csv` with
`reason_class=INCLUDE_MIRROR_REFUSED`, `errors=-1`, and `warnings=-1`. Therefore
there is no new compiler log or verified binary. The existing EX5 was not changed,
and the interlock was not bypassed by stopping terminals, toggling the factory, or
using MetaEditor directly.

## Risk and follow-up

- Source-level, framework, documentation, and hardening validation pass for EA
  1416, but binary compilation remains unverified until the governed compile
  service/window can run without its live-factory interlock.
- The existing EX5 is deliberately stale relative to the repaired source and must
  not be treated as this repair's binary artifact.
- No gate threshold or criterion changed.

## Rollback

Revert the resulting `rework(1416)` commit. That restores the prior EA source,
specification, and 14 backtest sets and removes the focused test and this evidence
record. No registry, resolver, database, EX5, live terminal, or factory rollback is
needed because none was modified.

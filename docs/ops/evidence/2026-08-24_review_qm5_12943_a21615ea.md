# Claude review — QM5_12943 (task a21615ea)

Date: 2026-08-24
Router task: `a21615ea-2d27-4bd9-9e72-c217d3e5de72` (review_ea, REVIEW, assigned codex)
Source build task: `e3a2083b-eeb7-40e0-b865-0cc7d001997e` (source_agent gemini/agy)
EA: `QM5_12943_robopip-hlhb-trend-catcher-h1`
Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12943_robopip-hlhb-trend-catcher-h1.md`
Branch: `agents/board-advisor`

## Verdict

**APPROVED — clean for Q02.**

This task first closed RECYCLE on 2026-08-21 (FAIL_MECHANICAL, four blocking
defects). The build was reworked (commits `978f9dc86`, `0e22f9b17`) and
resubmitted. All four defects are independently re-verified as fixed against the
current committed source.

## Re-verification of the four prior blocking defects

1. **MAE hook** — `QM_FrameworkTrackOpenPositionMae()` is the first statement in
   `OnTick`, before every guard: MQ5 `:267`. FIXED.
2. **Volatility gate** — H1 ATR(14)×24, no native-D1 substitution:
   `atr_h1 = QM_ATR(_Symbol, PERIOD_H1, strategy_daily_atr_period, 1)` MQ5 `:103`,
   `atr_daily = atr_h1 * 24.0` MQ5 `:107`. Card §Entry ("Daily ATR(14) on H1,
   i.e. ATR(14)×24, >= 30 pips"). FIXED.
3. **Time stop** — measured in closed H1 bars via `iBarShift` (perf-allowed)
   MQ5 `:166`, and it closes only at/beyond break-even (never crystallizes a
   card-forbidden loss) MQ5 `:172-190`. Card §Exit ("close at break-even if TP
   not hit within 96 H1 bars"). FIXED.
4. **Symbol scope** — `sets/` contains exactly the four card-authorized symbols
   (EURUSD.DWX, GBPUSD.DWX, USDJPY.DWX, XAUUSD.DWX), all H1 backtest,
   `RISK_FIXED=1000` / `RISK_PERCENT=0`. Card §Target Symbols. FIXED.

## Card-faithful mechanism

EMA(5)/EMA(10) close-bar cross + RSI(10) midline gate + ATR floor entry
(MQ5 `:94-147`); exits = fixed 100-pip TP / 50-pip SL set on entry (`:126-127`,
`:141-142`), opposite-cross exit (`:279-302`), conditional break-even time stop
(`:172-190`). Matches card mechanics.

## Dead-input check (every declared strategy input grepped for a use-site)

All 13 `strategy_*` inputs have executable use-sites beyond declaration:
fast/slow EMA `:94-97,201-204,290-293`; rsi_period `:98`; rsi_midline
`:118,133`; daily_atr_period `:103`; min_daily_atr_pips `:108`; tp_pips `:113`;
sl_pips `:112`; time_stop_bars `:172`; max_spread_pips `:61,63`; asian_filter
`:68`; asian_start/end_hour `:72`. Framework inputs (qm_*, RISK_*,
PORTFOLIO_WEIGHT, news, friday, stress) wired into `QM_FrameworkInit` `:237-252`
and the OnTick news gate `:304-309`. No dead inputs.

## Framework conformity

- `#include <QM/QM_Common.mqh>` `:5`; MAE hook `:267`.
- RISK_FIXED=1000 / RISK_PERCENT=0 (backtest) `:17-18`; sets confirm.
- Magic solely via `QM_FrameworkMagic()` `:90,152,197,281` — no manual formula.
  Registry XAUUSD slot5 -> 129430005 = ea_id*10000+slot.
- News filter wired via `QM_NewsAllowsTrade2` `:304-309` (temporal PRE30_POST30,
  min_impact high); Friday close `:273`; kill-switch `:269`.
- Indicator access via bounded `QM_EMA/QM_RSI/QM_ATR` accessors at fixed shifts;
  only `iClose`/`iBarShift` as perf-allowed raw calls (commented). No ML, no
  invented commission/swap.

## Build evidence

- Tree clean; MQ5 and EX5 both last written by rework commit `0e22f9b17`.
- Tracked EX5 SHA-256 `95ba06400a66dfa39e31dd09855beb3f4c64f8ee4d2573d5f6476c63234155b2`
  matches the recompiled binary recorded in `docs/ops/evidence/2026-08-23_rework-12943.md`
  (isolated portable compile: 0 errors, 0 warnings).
- Full `build_check.ps1` is refused while the live factory runs
  (`BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED`); D10-clean substitutes pass:
  `build_gate_hardening` 0 failures, `validate_build_guardrails` PASS (5 files,
  0 findings), `validate_symbol_scope` SINGLE_SYMBOL_OK.
- `pytest tools/strategy_farm/tests/test_qm5_12943_rework_static.py` -> 4 passed
  (re-run this review).

## Residual (non-blocking) — registry hygiene follow-up

`framework/registry/magic_numbers.csv:16382-16394` still holds 13 active rows
for ea_id 12943 (slots 0-4 and 9-12 map to symbols outside the card universe:
GDAXI/NDX/SP500/UK100/WS30/USDCHF/AUDUSD/USDCAD/NZDUSD), a pre-existing 08-17
governed-allocator artifact. No setfiles exist for those slots, so Q02 dispatches
only the four authorized symbols — the gates measure the card universe. The full
symbol-scope hardening gate flags nine `EA_SYMBOL_NOT_IN_CARD_UNIVERSE` on these
dormant rows. This is registry/allocator hygiene, not a mechanism or delivered-
scope defect, and touching magic allocations is out of this review's scope
(ROT-adjacent). Recommend a separate Codex registry-trim task to align the
allocation with the four-symbol card, but it does not block Q02.

## Scope note

Read-only review (DB mode=ro). No terminal, Q-phase, T_Live, AutoTrading,
recompile, registry, or backtest-enqueue action was performed.

# QM5_39001 mandatory Codex review — 2026-08-23

- Review task: `2d0bdc23-749d-4443-9000-beec43d0135c`
- Gemini build task: `60d701a7-09cf-4b18-aaff-788ea1e799a5`
- EA: `QM5_39001_forexfactory-trading-made-simple-tms`
- Build packet: `artifacts/builds/60d701a7-09cf-4b18-aaff-788ea1e799a5.json`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_39001_forexfactory-trading-made-simple-tms.md`
- Reviewed MQ5 SHA-256: `cb6564d53eff98ec4c9bc494e4bd14b1baf4cd370c7281470695e981db056355`
- Reviewed EX5 SHA-256: `5bf6b35574a922e8d744f3d95e26c8a51030f60bf633a0ad5a883df19719eb20`
- Disposition: `CHANGES_REQUIRED`; remain in `REVIEW`, with no pipeline promotion.

## Verified progress

- The open-position guard is now entry-only, so TDI management is no longer statically deadlocked.
- The prior 3.5×ATR maximum-distance clamp that could move a stop inside the swing is removed.
- Default 2.0%/2.5%/5.0%/0.5% capital-preservation inputs are present and passed to `QM_KillSwitchInit`; raw-series annotations now satisfy the static scanner.
- `_build_review_dispatch_gate(...)` returns `BUILD_REVIEW_DISPATCH_PASS`; generic guardrails pass for the MQ5 and three sets at 336 hours; SPEC structure passes.
- All setfiles use `RISK_FIXED=1000` and `RISK_PERCENT=0`; the authorized symbols are EURUSD.DWX, GBPUSD.DWX, and AUDUSD.DWX.

Those improvements do not make the packet acceptable.

## Blocking findings

1. **The executable and presets are not bound to the reviewed source.** Commit `efbb11d07` changed the MQ5 on 2026-08-23, but the retained EX5 is dated 2026-08-18. The current MQ5 hash occurs only in the packet, not in a strict compiler result. The three setfile `build_hash` values are mutually different (`c852d0...`, `adca3d...`, `ba2767...`) and none equals the reviewed MQ5 hash. The packet claims `compile_succeeded=true`, while smoke is deferred and `smoke_report_path` is null. The dispatch gate is structural, not compile provenance.

2. **The canonical hardening gate fails the GMT contract.** `StrategyInRolloverWindow(TimeCurrent())` at MQ5 line 152 compares raw broker time to the card's 23:55–00:05 GMT window. Fresh `build_gate_hardening.py` reports `EA_BROKER_TIME_USED_FOR_GMT_WINDOW`; no `QM_BrokerToUTC` conversion is present.

3. **Restart and first-bar state are unsafe.** `OnInit()` does not seed TDI/EMA/ATR/swing state. Management at line 360 therefore has neither TDI exit nor break-even state after restart until a chart-new-bar event. For entry, `Strategy_NoTradeFilter()` runs at line 391 before `AdvanceState_OnNewBar()` at line 394; startup ATR zero skips the spread ceiling at line 163, then the same tick can populate a signal and enter without rechecking admission. A failed state read also returns before clearing prior cached values.

4. **The exact swing stop is still changed.** The card requires the recent swing extreme plus/minus exactly 3 pips. Lines 206–220 impose a minimum 0.5×ATR distance, pushing a nearer valid swing stop farther away and changing both risk-sized volume and the 2R target. The pip-helper failure path also substitutes a manual point×10 conversion instead of failing closed.

5. **H1 cadence is not bound to H1.** The source calls parameterless `QM_IsNewBar()` at line 388 and declares no H1 execution contract. A different chart timeframe can control H1 signal/exit refresh.

6. **The three-tick market-order ceiling is absent.** There is no slippage input or `QM_EntryConfigure` call, so the card's maximum three trade ticks is not enforced.

7. **Unapproved mechanics remain unresolved.** The card does not define a five-bar “recent swing” lookback or a +1R break-even rule, yet the source introduces both. The exact TDI exit names a cross-back; the implementation stores no prior fast/slow relationship and merely tests their current ordering. These need explicit card mechanics rather than SPEC-only invention.

## Required repair

- Define/adjudicate recent-swing selection, cross-back state, and any break-even rule in the card, then implement the approved result exactly without ATR substitution.
- Refresh/clear H1 state before management and admission, seed it on initialization, and bind cadence to H1.
- Convert rollover to UTC, enforce a maximum three-trade-tick deviation, and cap all loss/risk inputs at the approved contract.
- Regenerate all setfile hashes, compile through the governed compiler, and return with strict hash-bound MQ5/EX5 evidence. Do not claim smoke without a report.

No source was changed in this review. No Q phase, backtest, terminal launch, AutoTrading action, or T_Live action occurred.

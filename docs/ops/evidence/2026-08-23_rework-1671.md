# rework-1671 — QM5_1671 review-fix evidence

Date executed: 2026-08-24 (Europe/Berlin)  
Router task: `58c58dcb-4fbe-4da7-a3b1-365334a7ef77`  
EA: `QM5_1671_ehlers-ebsw-cycle-extract-composite-h4`  
Scope: source/spec/test rework only; no backtest enqueue, verdict overwrite, factory toggle, registry edit, T_Live access, or live action.

## Work order and preflight

- The durable review work order is `docs/ops/evidence/2026-08-23_qm5_1671_gemini_code_review.md:15`, `:28`, `:41`, `:47`, `:53`, and `:59`.
- The approved card is `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1671_ehlers-ebsw-cycle-extract-composite-h4.md` with `g0_status: APPROVED`.
- `framework/registry/ea_id_registry.csv:460` is the active `1671 / ehlers-ebsw-cycle-extract-composite-h4` row.
- `framework/registry/magic_numbers.csv:16707` through `:16719` contain the 13 active, already allocated symbol slots. No registry gap existed, so no allocation or resolver regeneration was performed.
- The router claim command completed with exit code 0:

  ```text
  python tools/strategy_farm/agent_router.py update-task 58c58dcb-4fbe-4da7-a3b1-365334a7ef77 --state IN_PROGRESS --verdict "rework burn-window slot 5"
  ```

## Changes

1. Replaced the two-point centered difference with the fixed four-element Ehlers FIR, including an `ArraySize` guard (`framework/EAs/QM5_1671_ehlers-ebsw-cycle-extract-composite-h4/QM5_1671_ehlers-ebsw-cycle-extract-composite-h4.mq5:68`, used for cycle/EBSW at `:166` and `:211`).
2. Split raw-period validity from the clamped display value. Entry admission now requires the unmodified raw period to be valid and within the configured band (`.mq5:202`, `:442`). No invalid-phase fallback period exists.
3. Encoded the accepted entry period in a compact order/position comment (`.mq5:311`), reconstructs it from position or accepted entry-deal provenance after restart (`.mq5:336`), and binds the time stop to that immutable value (`.mq5:579`). Missing provenance fails closed.
4. Replaced pre-send, volatile cooldown globals with a bounded accepted-deal history lookup keyed by framework-resolved magic, symbol, direction, and entry-period comment (`.mq5:366`, call sites `:462` and `:483`). Rejected orders cannot consume cooldown.
5. Moved Friday close, cycle-state advancement, management, and strategy exits before the entry-only news decision while preserving the MAE hook (`.mq5:671`, `:676`, `:705`). The three-bar amplitude-collapse state is rebuilt from closed H4 observations at `.mq5:226` rather than incremented only on entry-eligible ticks.
6. Added a fail-closed H4 initialization guard and an explicit H4 new-bar clock (`.mq5:643`, `:696`).
7. Updated the local strategy spec for the corrected execution contract (`framework/EAs/QM5_1671_ehlers-ebsw-cycle-extract-composite-h4/SPEC.md:78`, `:80`, `:130`).
8. Added six focused pytest regressions covering the FIR reference vectors, raw-period gate, durable entry provenance/cooldown, news/exit ordering, bounded series reads, all input use-sites, risk/setfile/framework conformity, and hardening (`tools/strategy_farm/tests/test_qm5_1671_rework_static.py:49`, `:87`, `:104`, `:134`, `:151`, `:163`).

Current source SHA-256: `5ebd2405bb000e3493ae43b6573077a9f30165b1650a025bd7828b370c984d61`.

## Validation evidence

### Focused unit tests

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_1671_rework_static.py
......                                                                   [100%]
6 passed in 0.81s
```

### Scoped build-gate hardening

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_1671_ehlers-ebsw-cycle-extract-composite-h4
files_scanned=1; symbols_observed=13; failures=[]; warnings=[]; exit=0
```

### Other scoped validators

```text
python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_1671_ehlers-ebsw-cycle-extract-composite-h4
verdict=PASS; files_checked=14; findings=[]; exit=0

python framework/scripts/skill_build_ea_guard.py --ea-id 1671 --ea-label QM5_1671_ehlers-ebsw-cycle-extract-composite-h4
status=ok; ea_registry_row=true; magic_registry_rows=true; ea_dir_exists=true; exit=0

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_1671_ehlers-ebsw-cycle-extract-composite-h4
PASS; Summary: 1 PASS, 0 FAIL; exit=0

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_1671_ehlers-ebsw-cycle-extract-composite-h4 --json --fail-on-leak
verdict=SINGLE_SYMBOL_OK; n_violations=0; exit=0

git diff --check -- <ticket pathspecs>
exit=0
```

### Touched hardening-module regression suite

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_1671_rework_static.py tools/strategy_farm/tests/test_build_gate_hardening.py
34 passed, 1 failed in 571.80s
```

The single failure is outside this ticket: `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven existing `QM5_411xx` findings, beginning with `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`. All QM5_1671 tests passed, and the directly scoped hardening command above is clean.

### Compile/build_check guard outcome

The requested strict scoped compile was attempted once:

```text
framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_1671_ehlers-ebsw-cycle-extract-composite-h4/QM5_1671_ehlers-ebsw-cycle-extract-composite-h4.mq5 -Strict
result=FAIL
reason_class=INCLUDE_MIRROR_REFUSED
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
detail=terminal64 processes are alive; ad-hoc compile/build_check is refused
```

Durable refusal receipt: `D:/QM/reports/compile/20260824_123226/summary.csv` (`timestamp_utc=2026-08-24T12:32:44.3531261Z`). A separately scoped `build_check.ps1 -EALabel QM5_1671_ehlers-ebsw-cycle-extract-composite-h4 -Strict -SkipCompile` was refused by the same guard before checks ran. The guard's alternative is a canonical `COMPILE_EA` queue item; this ticket did not enqueue it because the task authorizes no non-router state-DB write from this worktree.

Therefore the existing EX5 remains deliberately untouched at SHA-256 `18a2492edf2f67d554f59e2b34add3be48d383a3afc1ca72d97f527cd35ee4b5` and does **not** represent the repaired source. This is an explicit review risk: source approval may proceed, but pipeline promotion must remain fail-closed until a governed post-merge compile produces a new EX5 and refreshes `build_identity.json`.

## Rollback

Revert the ticket commit with `git revert <rework-1671-commit>`. This restores the prior MQ5/spec and removes the focused regression test/evidence file. No registry, resolver, database verdict, backtest queue, factory state, or T_Live rollback is required because none was changed.

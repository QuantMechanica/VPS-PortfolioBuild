# rework-9166 — review-fix evidence

Date: 2026-08-24 UTC

Ticket: `rework-9166`

Router task: `22c43872-4059-4777-97d5-1800c98bcfc7`

EA: `QM5_9166_aa-vol-ma-timing`

## Scope and authority

The router task was read in `RECYCLE`, including its artifact
`docs/ops/evidence/2026-08-24_review_qm5_9166.md` from the canonical checkout,
then claimed with:

```text
python tools/strategy_farm/agent_router.py update-task 22c43872-4059-4777-97d5-1800c98bcfc7 --state IN_PROGRESS --verdict "rework burn-window slot 2"
state=IN_PROGRESS, updated=true
```

The approved card is
`D:/QM/strategy_farm/artifacts/cards_approved/QM5_9166_aa-vol-ma-timing.md`
with `g0_status: APPROVED`. The deterministic registries were already complete,
so no registry was changed:

- `framework/registry/ea_id_registry.csv:517` — active `9166 / aa-vol-ma-timing`.
- `framework/registry/magic_numbers.csv:16772-16784` — 13 active rows,
  slots 0–12 / magics 91660000–91660012.

No backtest was enqueued or deleted. Factory state, gate criteria, verdict rows,
`T_Live`, and AutoTrading were not touched.

## What changed

### Card-faithful selection and timing

- The exact slot-ordered 13-symbol basket is declared at
  `framework/EAs/QM5_9166_aa-vol-ma-timing/QM5_9166_aa-vol-ma-timing.mq5:51`
  and is bound to `QM_SymbolGuardInit` / bounded basket warmup in `OnInit`
  (`:594`). `basket_manifest.json` documents the same order and selection
  contract.
- `Strategy_CalculateRealizedVol` (`:169`) requires exactly 253 completed D1
  bars for 252 log returns, validates chronological/positive data, uses the
  sample-variance denominator, and annualizes by `sqrt(252)`.
- `Strategy_ActiveSleeveCount` (`:275`) implements the high-volatility quintile
  as `ceil(valid_count * 20%)`, with the card's top-three fallback for 3–9
  available symbols and a fail-closed result below three.
- `Strategy_BuildMonthlySnapshot` (`:287`) calculates every available basket
  member, ranks descending with slot-order tie-break, and seals the host's
  membership and active-sleeve count for the rebalance month.
- `Strategy_ReadCompletedMonthEnds` (`:223`) derives exactly the last
  `strategy_sma_months` completed month-end closes from bounded D1 history and
  averages those fixed observations. The rejected 210-session `QM_SMA` proxy
  no longer exists.

### Risk, action state, and exit reachability

- `Strategy_OpenWithDistributedRisk` (`:554`) selects the framework risk mode,
  divides the basket budget by the sealed active-sleeve count, and uses the
  supported explicit-risk `QM_TM_OpenPosition` overload. The framework then
  applies `PORTFOLIO_WEIGHT`, keeping aggregate requested risk at or below the
  declared basket budget.
- `Strategy_MonthHasConfirmedEntry` (`:137`) reconstructs monthly action state
  from confirmed MT5 deal history filtered by symbol, MagicResolver magic, and
  `DEAL_ENTRY_IN/INOUT`. No pre-trade entry key or pre-close exit key remains.
  Failed opens remain eligible on the next D1 bar; failed closes remain
  requested while the position persists.
- `OnTick` (`:639`) keeps the MAE hook first (`:641`), then framework kill and
  Friday-close handling, snapshot/management and strategy exits, and only then
  applies news, spread, and other entry admission. Entry filters can no longer
  suppress the card's monthly exit.
- `Strategy_MedianSpreadD1` / `Strategy_SpreadAllowsEntry` (`:401`, `:434`)
  require exactly 20 positive completed-D1 spread observations, positive
  bid/ask ordering, positive current spread, and a positive median. Partial or
  invalid evidence fails closed.
- `OnInit` (`:594`) fails closed on a non-D1 chart, invalid strategy parameters,
  or slot/host mismatch. Magic remains resolved only through
  `QM_FrameworkMagic()` / the `QM_Common` include chain.

### Documentation and regression tests

- `framework/EAs/QM5_9166_aa-vol-ma-timing/SPEC.md:13` now describes the actual
  cross-sectional strategy, exact month-end timing, registered universe,
  distributed risk, and action-state/retry contract; framework alignment is at
  `SPEC.md:131`.
- `tools/strategy_farm/tests/test_qm5_9166_review_rework_static.py:46-216`
  adds eight focused regressions covering registry/basket alignment,
  cross-sectional rank and sleeve exit, exact data bounds/month-end SMA,
  distributed risk, confirmed action state, exit ordering, spread fail-closed
  behavior, framework wiring, all declared strategy-input use-sites, and the
  no-ML constraint.

Declared strategy input grep counts (declaration plus real use-sites):

```text
strategy_sma_months=7
strategy_vol_lookback_days=8
strategy_atr_period=3
strategy_atr_sl_mult=3
strategy_min_warmup_bars=4
strategy_enable_shorts=2
```

## Validation evidence

### Clean results

```text
python -m pytest tools/strategy_farm/tests/test_qm5_9166_review_rework_static.py -q
8 passed in 0.99s

python -m pytest tools/strategy_farm/tests/test_qm5_9166_review_rework_static.py tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_validate_symbol_scope.py tools/strategy_farm/tests/test_fx_basket_manifests.py -q
79 passed in 6.27s

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_9166_aa-vol-ma-timing
PASS; files_checked=14; findings=[]; max_news_stale_hours=336

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_9166_aa-vol-ma-timing --json
BASKET_OK; n_violations=0; manifest_symbols=13

python tools/strategy_farm/build_gate_hardening.py --repo-root C:/QM/worktrees/rework-slot-2 --ea-label QM5_9166_aa-vol-ma-timing
failures=[]; D10 failures=0; symbols_observed=13

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_9166_aa-vol-ma-timing
Summary: 1 PASS, 0 FAIL (of 1)
```

The combined touched-check run produced `59 passed, 1 failed in 488.96s`.
The sole failure is the pre-existing cohort assertion
`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`, whose first cited
source is `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`; it does not reference or
scan QM5_9166. Per the one-EA boundary, no QM5_411xx file was changed. The
EA-scoped D10 check above is clean.

### Compile interlock (not a compiler failure)

The required strict, EA-scoped attempt was made once:

```text
powershell.exe -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_9166_aa-vol-ma-timing/QM5_9166_aa-vol-ma-timing.mq5 -Strict
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
reason_class=INCLUDE_MIRROR_REFUSED
errors=-1; warnings=-1; retry_attempted=false
```

The governed report is
`D:/QM/reports/compile/20260824_131454/summary.csv`. Seven `terminal64`
processes were active, so the include-mirror interlock correctly refused the
ad-hoc compile before MetaEditor processed the source. A second scoped
`build_check.ps1 -EALabel QM5_9166_aa-vol-ma-timing -SkipCompile` request was
refused by the same compile guard. No bypass, retry, factory toggle, compile
enqueue, or backtest enqueue was attempted.

Consequently, the source SHA-256 is now
`a9ee4dfab28084e764d06e10673e2ce986a55d7b589b93c8aef967f3124b08d4`,
while the untouched EX5 remains
`24052fdfd2c26d73922f5fd103ad49ebef008bc0049adbda6b1efe173eba3f3f`.
`build_identity.json` still names the prior MQ5 hash
`da6458795d657749428fdb2575afc3c0e6431cada360640ceeabe67a87dba4df`.
Those artifacts are intentionally not represented as current. A governed
compile window must regenerate EX5/build identity and rebind setfile hashes
before any pipeline handoff; this REVIEW submission is for the completed code
rework and its static/unit evidence only.

## Rollback

Revert the single commit whose subject starts
`rework(9166): implement volatility-ranked monthly timing`. That restores the
prior MQ5 and SPEC and removes the EA-local basket manifest, regression test,
and this evidence note. The ignored local strategy-card copy may be removed
separately; it is byte-content equivalent to the approved runtime card and is
not part of the commit. No database, registry, factory, backtest, live, or
verdict rollback is required beyond the router task's ordinary review-state
transition.

# QM5_20079 build preflight — deterministic refusal

- Router task: `a6092797-1416-4719-8c13-cc60829247bb`
- Task type / priority / agent: `build_ea` / `10` / `codex`
- EA: `QM5_20079_pip-boxer-bounded-grid-h1`
- Checked at: `2026-08-24T04:05:13Z`
- Canonical checkout: `C:/QM/repo`, branch `agents/board-advisor`, baseline `46e7be1d3db9297ff40c53c6d977ff975c11bace`
- Verdict: `BLOCKED — BUILD REFUSED AT PRE-FLIGHT`

## Deterministic evidence

The canonical CSV registries were loaded with `Import-Csv` and filtered by exact `ea_id` equality.

| Gate | Result |
|---|---|
| Approved card | `D:/QM/strategy_farm/artifacts/cards_approved/QM5_20079_pip-boxer-bounded-grid-h1.md`; SHA-256 `50A1FD80CDC8DBDF7FDA41CC40CFF51AF295DAD640CD11FC3B38B79E9BEC486E`; `g0_status: APPROVED`; slug match PASS |
| EA registry | one active row for `20079,pip-boxer-bounded-grid-h1` — PASS |
| EA directory | canonical directory and tracked `.mq5` skeleton exist — PASS |
| Magic registry | zero rows for exact `ea_id=20079`; therefore zero active rows — **FAIL** |
| Active-cycle policy | card lines 109–114 direct slots 1..N to open when price moves further **against** the basket; the card calls this a bounded grid. The current no-grid/no-averaging hard rule independently forbids this build — **FAIL** |

Registry snapshots used: `ea_id_registry.csv` SHA-256 `4A0ABB97B79DF767FCB1571AADBF9D3DC349C22088B9980C6BEB50072B530A4F`; `magic_numbers.csv` SHA-256 `1A30B5DF1986A641CB0F3BB6BE57D643CC003DCB3EC77ACA17C4636CCF930B1E`.

The `qm-build-ea-from-card` contract requires active magic rows for every `(ea_id, symbol_slot)` used and requires an immediate stop when pre-flight fails. The skill also forbids Development from allocating those rows. Separately, a bounded grid that averages into an adverse move is outside this cycle's authorized design box. No source, registry, resolver, setfile, terminal, compile, or pipeline mutation was attempted.

A requested `REVIEW` transition using this evidence was deterministically refused by the canonical router as `D6_BUILD_IDENTITY_MISSING`: build tasks can enter REVIEW only with a JSON packet proving strict-build PASS and hash-binding committed `.mq5`, `.ex5`, and setfiles. Those artifacts do not exist and must not be fabricated. The truthful router disposition is therefore `BLOCKED` pending OWNER policy reconciliation and any governed upstream registry action.

## Required upstream action

The card must return to OWNER governance for no-grid policy reconciliation; registry allocation alone cannot make this build eligible. If a compliant, newly approved mechanical card is produced, its required magic rows must be allocated and the resolver regenerated before a fresh build attempt. This pre-flight establishes no pipeline verdict.

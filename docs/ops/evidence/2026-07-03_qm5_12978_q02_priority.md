# QM5_12978 Q02 Priority - 2026-07-03

Branch: `agents/board-advisor`

## Scope

Mission: grow the certified V5 book with new forex sleeves, prioritizing market-neutral FX
cointegration baskets. I did not touch `T_Live`, AutoTrading, portfolio admission/KPI/Q08
contribution code, or deploy manifests.

## Triage

- `QM5_12532` is not Q02-blocked: Q02 `PASS`, Q04 `PASS`, latest Q05 `FAIL`.
- `QM5_12533` is not Q02-blocked: Q02 `PASS`, latest Q04 `FAIL`.
- The approved EdgeLab FX cointegration batch is already built; no approved unbuilt non-duplicate
  pair remained locally.
- `QM5_12978` (`GBPUSD~USDCAD`) is the current non-duplicate FX cointegration sleeve: built with
  `basket_manifest.json` and one Q02 logical basket row pending.

## Queue Action

The farm was CPU-saturated (`active=5`, `pending=5197`), so I did not launch MT5 and did not insert
a new Q02 row. I priority-marked the existing pending `QM5_12978` row in place:

| Field | Value |
|---|---|
| Work item | `b1656e8e-d9dc-4a7b-b5a2-ba8e3ff0150b` |
| Phase | `Q02` |
| Logical symbol | `QM5_12978_GBPUSD_USDCAD_COINTEGRATION_D1` |
| Status after | `pending` |
| `priority_track` | `true` |
| DB backup | `D:/QM/strategy_farm/state/backups/farm_state_before_qm5_12978_q02_priority_20260703T033306Z.sqlite` |
| Event | `priority_track_set`, id `208223` |

Duplicate guard after mutation: exactly one Q02 row for this EA/logical symbol.

## Verification

- `python -m unittest framework.scripts.tests.test_mt5_queue_status` -> PASS.
- `python framework/scripts/mt5_queue_status.py --sqlite D:/QM/strategy_farm/state/farm_state.sqlite --limit 5`
  now reports `QM5_12978` as `queued_top[0]` with `priority_track=true`.

I also updated the read-only queue status helper so `work_items` snapshots sort pending rows by the
worker priority order instead of oldest-row order. This makes future FX basket triage show the row
the workers will actually claim next when capacity frees.

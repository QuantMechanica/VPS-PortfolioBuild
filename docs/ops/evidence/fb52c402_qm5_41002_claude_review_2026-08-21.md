# QM5_41002 robert-pardo-checkmate-breakout-engine — First Full review_ea

**Date:** 2026-08-21
**Reviewer:** Claude (independent gating review)
**Branch:** agents/board-advisor (canonical C:/QM/repo)
**Verdict:** RECYCLE — compiles clean and the spread-gate fix is correct, but strict
build_check FAILs on 2 repairable card-fidelity/time-basis defects that block Q02 entry.

## Scope
First real review_ea for this EA (none had ever completed). Build task was stuck in
REVIEW behind the dirty_registry_abort preflight; that condition was transient and had
already cleared (git status --porcelain on magic_numbers.csv + QM_MagicResolver.mqh empty
at review time). No registry repair needed.

## Checklist
1. Card fidelity — PASS. Donchian(10) breakout + ATR(14) slope filter + 1.5xATR stop +
   2R TP + opposite-Donchian trailing implemented; prior-channel (shift 2..N+1) documented.
   .mq5:142-189, 254-259, 298/310; matches card §2/§3/§6.1 and all 3 sets.
2. Every input used — PASS. All 30 inputs have use-sites (each spot-checked).
3. Host slot / magic binding — PASS. req.symbol_slot=qm_magic_slot_offset (.mq5:228);
   slot flows into QM_FrameworkInit (:342-343) -> QM_FrameworkMagic (QM_Common.mqh:531);
   sets carry slot 0/1/2; registry 410020000/1/2 (magic_numbers.csv:17548-17550) and
   resolver (QM_MagicResolver.mqh:22) consistent. Not hardcoded.
4. Backtest set files — PASS. RISK_FIXED=1000, RISK_PERCENT=0, PORTFOLIO_WEIGHT=1,
   slot 0/1/2 across EURUSD/GBPUSD/USDJPY sets.
5. Loss limits — FAIL. Explicit kill-switch re-init 2.0/5.0/0.5 (.mq5:367-372) correctly
   overrides framework default 3.0/0.0/1.0 (QM_KillSwitch.mqh:480-482), so NOT defaulted.
   But card carries conflicting daily figures: §3.1.3 = 2.0% vs §4.2 daily hard stop =
   2.5%. EA wired only 2.0%; 2.5% hard stop unrepresented -> strict gate
   EA_CARD_LOSS_LIMIT_MISMATCH. Total DD 5.0% and per-trade 0.5% match.
6. Broker-time / GMT — FAIL. Rollover blackout is card-declared GMT (§3.1.2 "23:55-00:05
   GMT") but StrategyInRolloverWindow(TimeCurrent()) (.mq5:207) compares raw broker time
   (GMT+2/+3) against 2355/0005 with no QM_BrokerToUTC -> strict gate
   EA_BROKER_TIME_USED_FOR_GMT_WINDOW. Note: server-midnight is the economically correct
   rollover anchor, so the card label may be wrong; reconciliation required either way.
7. News/stale-news guard — PASS. qm_news_stale_max_hours=336 (=336 ceiling), not exceeded.
8. ENTRY_SPREAD_GATE_FIRST_SIGNAL_BYPASS fix — PASS. Entry re-reads live ask/bid and
   re-checks (ask-bid) > g_atr_1*strategy_spread_filter_mult AFTER AdvanceState_OnNewBar()
   populates g_state_ready/g_atr_1 (.mq5:238-243; advance at :426 precedes entry at :431).
   No other threshold changed (Donchian 10, ATR 14, slope 5, SL 1.5, TP 2.0, spread 1.8,
   rollover 2355/5 all intact).

## Build check
cd C:/QM/repo && pwsh -File framework/scripts/build_check.ps1 \
  -EALabel "QM5_41002_robert-pardo-checkmate-breakout-engine" -Strict
- compile_one.result = PASS (errors=0, warnings=0), ex5 rebuilt.
- build_check.result = FAIL, failures=2:
  - EA_CARD_LOSS_LIMIT_MISMATCH: card daily_hard_stop=2.5% but matching input absent;
    candidate strategy_daily_loss_halt_pct=2.0.
  - EA_BROKER_TIME_USED_FOR_GMT_WINDOW: card-declared GMT window compared against raw
    TimeCurrent/iTime hours without QM_BrokerToUTC.
- Report: D:\QM\reports\framework\21\build_check_20260821_085232.json

## Decision
RECYCLE. Required before re-review:
(a) Reconcile the card's daily loss figure (single value) or wire the 2.5% daily hard stop.
(b) Reconcile rollover time basis — convert via QM_BrokerToUTC (QM_DSTAware.mqh:122) or
    relabel the card window as broker/server time (OWNER/card decision).
Re-run strict build_check to green before Q02 entry.

## Note on the prior repair doc
The 2026-08-20 repair evidence (`2026-08-20_qm5_41002_spread_gate_repair_cpu_stop.md`)
only proved compile cleanliness (strict MetaEditor compile PASS, 0/0), not the semantic
strict `build_check` audit — that PASS should not have been read as review-clean. The
spread-gate fix it recorded is independently confirmed correct here (item 8 above); the
two new findings are unrelated to that fix and were not in scope of that repair.

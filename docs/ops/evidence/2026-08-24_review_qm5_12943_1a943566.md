# Claude review — QM5_12943 (task 1a943566)

Date: 2026-08-24
Router task: `1a943566-684f-4925-b881-e3b9b7c67eb1` (review_ea, REVIEW, assigned codex)
Source build task: `e3a2083b-eeb7-40e0-b865-0cc7d001997e` (source_agent gemini/agy)
EA: `QM5_12943_robopip-hlhb-trend-catcher-h1`
Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12943_robopip-hlhb-trend-catcher-h1.md`
Branch: `agents/board-advisor`

## Verdict

**APPROVED — clean for Q02.**

## Duplicate-row note

This task and `a21615ea-2d27-4bd9-9e72-c217d3e5de72` are two review_ea rows
spawned for the **same** gemini build task `e3a2083b`. `a21615ea` was already
closed APPROVED (see `docs/ops/evidence/2026-08-24_review_qm5_12943_a21615ea.md`)
after the build was reworked (`978f9dc86`, `0e22f9b17`). This note independently
re-verifies the current committed source and closes the remaining duplicate row
consistently rather than re-litigating.

## Independent verification against the current committed source

All observations are from the tracked source at HEAD `0e22f9b17`
(MQ5 SHA-256 `6ebb42799e23755d3c31c1de56dca0c9c4023e60ccd4519cd9256f34c239161a`).
The build-result artifact `artifacts/qm5_12943_build_result.json` records the
pre-rework hash `a25c0af…` — it is **stale** relative to the current source, so
the review is judged against the current bytes, not the artifact.

### Card-faithful mechanism (card §Mechanics vs MQ5)
- Entry EMA(5)/EMA(10) close-bar cross + RSI(10) midline gate `:118`, `:133`.
- Daily ATR(14)-on-H1 floor = ATR(14)×24 ≥ 30 pips `:103`, `:107`, `:109`
  (no native-D1 substitution).
- Exit: fixed 100-pip TP / 50-pip SL set on entry `:113`/`:112`, `:126-127`,
  `:141-142`; opposite EMA-cross exit `:279-302`; conditional break-even time
  stop at 96 closed H1 bars `:172-190`.
- Spread filter > 3 pips `:61-66`; news ±30 min high-impact `:304-309`.
  Faithful to the card.

### Dead-input check (all 13 `strategy_*` inputs have executable use-sites)
ema_fast `:94-95,201-202,290-291`; ema_slow `:96-97,203-204,292-293`;
rsi_period `:98`; rsi_midline `:118,133`; daily_atr_period `:103`;
min_daily_atr_pips `:108`; tp_pips `:113`; sl_pips `:112`; time_stop_bars `:172`;
max_spread_pips `:61,63`; asian_filter `:68`; asian_start_hour `:72`;
asian_end_hour `:72`. No dead inputs. Framework inputs wired into
`QM_FrameworkInit` `:237-252` and the OnTick news gate `:304-309`.

### Framework conformity
- `#include <QM/QM_Common.mqh>` `:5`; MAE hook first in OnTick `:267`.
- RISK_FIXED=1000 / RISK_PERCENT=0 (backtest) `:17-18`; all 4 sets confirm.
- Magic solely via `QM_FrameworkMagic()` `:90,152,197,281` — no manual formula.
- News via `QM_NewsAllowsTrade2` `:304-309`; Friday close `:273`;
  kill-switch `:269`.
- Bounded `QM_EMA/QM_RSI/QM_ATR` accessors at fixed shifts; only `iClose`/
  `iBarShift` raw, both commented perf-allowed. No ML, no invented
  commission/swap.

### Setfiles (governed generator)
`sets/` holds exactly the four card-authorized symbols (EURUSD/GBPUSD/USDJPY/
XAUUSD.DWX), all H1 backtest, governed header, RISK_FIXED=1000 / RISK_PERCENT=0.

### Build evidence (current source hash)
- EA files clean/committed at `0e22f9b17`; `git status --porcelain` empty.
- Full `build_check.ps1` is refused while the live factory runs
  (`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`); the rework used an isolated portable
  compile (0 errors, 0 warnings — `docs/ops/evidence/2026-08-23_rework-12943.md`).
- `pytest tools/strategy_farm/tests/test_qm5_12943_rework_static.py` → 4 passed
  (re-run this review).

## Residual (non-blocking)
`framework/registry/magic_numbers.csv` still carries dormant ea_id-12943 rows for
slots outside the card universe (pre-existing 08-17 allocator artifact). No
setfiles exist for those slots, so Q02 dispatches only the four authorized
symbols. Registry/allocator hygiene, ROT-adjacent — out of this review's scope;
recommend a separate Codex registry-trim task. Does not block Q02.

## Scope note
Read-only review (DB mode=ro). No terminal, Q-phase, T_Live, AutoTrading,
recompile, registry, or backtest-enqueue action was performed.

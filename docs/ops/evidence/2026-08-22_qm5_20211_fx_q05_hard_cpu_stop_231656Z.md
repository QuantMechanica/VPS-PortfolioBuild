# QM5_20211 FX Q05 hard CPU stop — 23:16:56Z refresh

Date: 2026-08-22 CEST (`2026-08-21T23:16:56Z`)

Branch: `agents/board-advisor`

Outcome: the frozen 66-pair FX cointegration frontier remains fully
mechanized, and the eligible QM5_20211 Q05 continuation was not enqueued
because the explicit backtest CPU ceiling was binding.

## Non-duplicate selection

The durable sign-aware reconciliation at `a804932912a449aed19bbc8fe61f1f5846556743`
covers all 66 relationships from the frozen scan. Creating another Strategy
Card, registry allocation, EA, or basket manifest would duplicate governed
work.

The preferred anchors remain beyond Q02 and have no ONINIT or NO_HISTORY
blocker:

- `QM5_12532_AUDNZD_COINTEGRATION_D1`: Q02 PASS, Q04 PASS, then Q05 FAIL.
- `QM5_12533_EURJPY_GBPJPY_COINTEGRATION_D1`: Q02 PASS, then Q04 FAIL.

The current existing-pair fallback is scan rank 31,
`GBPJPY.DWX` / `EURAUD.DWX`, implemented as
`QM5_20211_gbpjpy-euraud`. Its logical Q02 row is PASS and its exact Q04 row
`8135f97c-fd0d-4435-b713-87fa74fe0053` is PASS. A read-only canonical database
query at `23:16:55Z` found zero exact Q05 rows, so the eligible future action
remains:

```powershell
python tools/strategy_farm/farmctl.py enqueue-backtest --ea QM5_20211 --phase Q05 --from-work-item-id 8135f97c-fd0d-4435-b713-87fa74fe0053
```

The package is unchanged and keeps the required backtest risk contract:
`RISK_FIXED=1000`, `RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1`. Its EA binary,
basket manifest, logical setfile, active registry rows, and approved Card
snapshot all remain present.

The repaired FX carry-unwind sleeve `QM5_20292` already has one pending Q02
row (`257d153d-a880-4431-8661-e4d736676ecb`). It was preserved without a
duplicate enqueue or timestamp restamp.

## Binding CPU and basket-lane stop

The five-sample CPU preflight returned `99.71%`, `98.10%`, `98.85%`,
`99.76%`, and `97.86%`: average `98.86%`, maximum `99.76%`. Both exceed the
explicit 97% stop threshold.

The canonical database simultaneously contained seven active work items and
2,314 pending items. `QM5_20291_XAU_XAG_HKURT_D1` was active on T2 as a
multi-symbol Q02 basket, independently occupying the serialized basket lane.
A nearby supported path-aware process scan observed factory terminals T1, T2,
T4, T6, and T8. `T_Live` and the unrelated FTMO terminal were observed only
to exclude them; neither was controlled.

Compared with the preceding committed refresh at `4802c6e20`, active work
rose from four to seven and the active multi-symbol blocker changed from
QM5_20206 to QM5_20291. The exact QM5_20211 Q05 row count remained zero. This
is a fresh capacity/state delta, not a repeated enqueue or duplicate sleeve.

Per the mission stop condition, no Q05 enqueue, queue mutation, dispatch,
tester launch, terminal reservation, reconciliation, or terminal control was
performed. No portfolio admission/KPI/Q08 contribution path, T_Live manifest,
AutoTrading state, Card, EA, registry, magic row, setfile, or basket manifest
was changed.

Machine-readable evidence:
`artifacts/fx_cointegration_qm5_20211_q05_hard_cpu_stop_20260821T231656Z_board_advisor.json`.

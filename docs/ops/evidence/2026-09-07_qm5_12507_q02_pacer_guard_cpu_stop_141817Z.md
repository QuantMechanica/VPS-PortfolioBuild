# QM5_12507 logical FX basket Q02 pacer-guard CPU stop

Recorded: 2026-09-07T14:18:17Z (16:18 Europe/Berlin)

Branch: `agents/board-advisor`

Observation head: `7964070d5c932ba153c0fce03fe17938d4504197`

## Outcome

No new scan-derived pair was carded or built. The durable sign-aware
reconciliation of the 66-pair scan already accounts for every unordered FX
relationship, so another identity would duplicate governed work. The preferred
anchors are not Q02-blocked:

- `QM5_12532` logical-basket Q02 work item
  `e4890d77-b865-4a48-b946-315faefca920` is `PASS`.
- `QM5_12533` logical-basket Q02 work item
  `76cb11ee-7e9d-4d75-be9d-626c205bca62` is `PASS`.

The mission-authorized fallback remains the concrete `EURUSD.DWX` /
`GBPUSD.DWX` H1 relationship in `QM5_12507_pair-coint-z`. Its canonical
logical Q02 work item `547c4fd3-f3fd-4c59-b9dc-654e96521251` remains the only
row for the exact EA, phase, and logical-symbol identity. It is pending,
unclaimed, attempt zero, priority-tracked, and has no hold. No duplicate row was
appended or reprioritized.

## PACER build guard and fixed-risk contract

The existing source was not generated or edited in this turn. Before any
compile or Q02 action was considered, it was nevertheless audited with the
binding command:

```powershell
python C:/QM/repo/tools/strategy_farm/audit_framework_input_pins.py --check-source "C:/QM/repo/framework/EAs/QM5_12507_pair-coint-z/QM5_12507_pair-coint-z.mq5"
```

Result: exit 0, `ok=true`, predicate `EA_FRAMEWORK_INPUT_PINNED`, one source,
zero findings. No compile command or compile enqueue followed.

The authenticated logical setfile remains `RISK_FIXED=1000`,
`RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1`. Current artifact identities are:

| Artifact | SHA-256 |
|---|---|
| MQ5 | `569cc4e32cbe9b83ab4f30ce8881ff8c1ed24357f7be6503c23338087787cf0c` |
| EX5 | `b9baf4b48e02b9ced91b2d86d24b87245b595b506d1763b279e36bb9074ba4aa` |
| `basket_manifest.json` | `195cf0517e6e99649e0f40c259cea93e9af5a05cfc913fe901a33c39896fea56` |
| logical Q02 setfile | `f8f7da7f72fa60ab37e4e4d1a9e64d1b83e8e122b29ee35c613feb25236bac99` |

## Binding CPU ceiling

At `2026-09-07T14:16:07Z`, the path-aware farm scan found eight running
factory terminals: `T1`, `T2`, `T4`, `T5`, `T7`, `T8`, `T9`, and `T10`.
This exceeds the binding seven-terminal process ceiling. `T_Live` and the FTMO
terminal were observed only as excluded processes and were not controlled.

Five one-second whole-host CPU samples were `98.731970%`, `94.068298%`,
`95.909812%`, `99.707358%`, and `99.805103%`. Average utilization was
`97.644508%` and maximum utilization was `99.805103%`; both violate the paced
admission rule that requires average and maximum to remain strictly below
`97%`. Free physical memory was 28.487 GiB of 63.120 GiB.

The mission's explicit CPU-ceiling stop therefore bound. No Q02 or compile row
was enqueued, no existing row was mutated, no claim or tester was dispatched,
and no terminal process was changed.

## Safety

- No Strategy Card, EA source or binary, setfile, registry, magic row, or
  basket manifest changed.
- No portfolio-admission, portfolio-KPI, Q08-contribution, or portfolio-gate
  path changed.
- No `T_Live` manifest changed and AutoTrading was not toggled.
- Unrelated shared-worktree changes were left unstaged and untouched.
- Machine-readable companion:
  `artifacts/qm5_12507_q02_pacer_guard_cpu_stop_20260907T141817Z_board_advisor.json`.

## Resume contract

On the next paced wake, reread this exact Q02 row and resample capacity. If the
row remains uniquely pending, unheld, and unclaimed, leave it to governed
terminal-worker claim order after both CPU and terminal-count admission clear.
Never append a duplicate Q02 row.

# greview-36001 — Codex cross-review of Gemini build

- Router task: `3c1da904-b03a-40d5-a1b3-c23e9ffad4b8`
- EA: `QM5_36001_nnfx-classic-mcginley-ssl-wae`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_36001_nnfx-classic-mcginley-ssl-wae.md`
- Reviewed Git source blob SHA-256: `03be79d0e3dfbf52a8d2169ab22c9107271ab3ad713faeed1ebe908e1bfb43ac`
- Delivered EX5 blob SHA-256: `5b463d9138ee8ad1d31566ac0093b127932965facfde75c09f184c683f789a61`
- Verdict: **RECYCLE**

## Blocking findings

### 1. The delivered EX5 is the pre-remediation binary

The current MQ5 was changed by remediation commit `1c8d911f912d605767793f3c127f5baeee144afd`; the last commit touching the EX5 is the original build commit `2ad86abe71e451218607b7d5f304c3138b5b4acc`. A read-only Git-blob comparison produced:

```text
HEAD      mq5 03be79d0e3dfbf52a8d2169ab22c9107271ab3ad713faeed1ebe908e1bfb43ac
HEAD      ex5 5b463d9138ee8ad1d31566ac0093b127932965facfde75c09f184c683f789a61
2ad86abe  mq5 7e3f4568c0adb106fc92fc26b484b48f3b2a878e42516bfa2b924f6c38ceb2cc
2ad86abe  ex5 5b463d9138ee8ad1d31566ac0093b127932965facfde75c09f184c683f789a61
```

Thus the current EX5 is byte-identical to the binary built from the old source, while the MQ5 changed. The task artifact `D:/QM/strategy_farm/artifacts/builds/3c1da904-b03a-40d5-a1b3-c23e9ffad4b8.json` records the current MQ5 hash and old EX5 hash but supplies `smoke_report_path: null`; its boolean compile/smoke claims do not bind the old binary to the remediated source. The only located strict build-check report, `D:/QM/reports/framework/21/build_check_20260818_142650.json`, contains status only and predates the 2026-08-23 remediation.

**Named fix:** after resolving finding 2, recompile via governed `COMPILE_EA` and provide a strict compile/build-identity record binding a newly emitted EX5 to MQ5 blob `03be79d0...`, followed by a non-null governed smoke result. Do not use the current EX5 for Q02.

### 2. `build_gate_hardening` rejects the card loss-limit surface

The approved card declares a 2.0% daily entry halt (`card:83-88`) and 2.5% daily / 5.0% total drawdown stops (`card:111-114`). The source implements those values as literals at `QM5_36001_nnfx-classic-mcginley-ssl-wae.mq5:199-203` and `:395`, but its declared input surface at `:13-53` has no matching loss-limit inputs.

Read-only command:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_36001_nnfx-classic-mcginley-ssl-wae
```

Result: three `EA_CARD_LOSS_LIMIT_MISMATCH` failures: missing matching inputs for `daily_entry_halt=2%`, `daily_hard_stop=2.5%`, and `total_drawdown_stop=5%`. All other reported D3-D18 checks for this EA had zero failures; D11 also confirmed the five authorized symbols.

**Named fix:** expose the three card-declared limits as explicit EA inputs with unchanged defaults `2.0`, `2.5`, and `5.0`, wire them to the entry halt and `QM_KillSwitchInit`, regenerate the governed setfiles so the values are explicit, and rerun the hardening gate. This fix must not change the approved values or any gate criterion.

## Clean review findings

- **Card mechanism:** completed-bar D1 logic is present and follows the approved McGinley/SSL-cross/Vortex/WAE entry chain (`mq5:73-186`, `:220-295`). The repaired TP1 lifecycle leaves `req.tp=0.0` (`:220-228`, `:272-289`), closes 50% at `strategy_tp_atr_mult * ATR`, and moves the runner to break-even (`:297-340`). DeMarker crossover or opposing SSL state closes the runner (`:344-375`). Position management and exits precede entry-only news/no-trade filters (`:405-445`).
- **Dead-input audit:** every one of the 16 `strategy_*` inputs declared at `mq5:38-53` has at least one executable use beyond its declaration. Grep occurrence counts were: McGinley 2, SSL 3, Vortex 2, WAE fast/slow/signal 3 each, WAE BB period/deviation 3 each, WAE sensitivity 3, WAE deadzone 2, DeMarker 3, ATR period 4, SL/TP/spread multipliers 2 each, warmup 2. No dead strategy input was found.
- **Framework chain and risk mode:** `QM_Common.mqh` is included at `mq5:6`; it includes MagicResolver, RiskSizer, news, kill-switch and trade-management modules at `QM_Common.mqh:4-20`. `QM_FrameworkInit` receives both risk inputs at `mq5:387-397`; the framework selects FIXED whenever `RISK_FIXED > 0` at `QM_Common.mqh:248-258`, matching the backtest sets, while the live default remains percentage risk when fixed risk is zero.
- **Magic and MAE:** the EA uses `QM_FrameworkMagic()` and the common initializer (`mq5:66-70`, `:387-397`). Registry rows `framework/registry/magic_numbers.csv:17425-17429` are `36001*10000+slot` for slots 0-4; the EA-ID row is `framework/registry/ea_id_registry.csv:4456`. The MAE hook is called at `mq5:405-408` and implemented by the common chain at `QM_Common.mqh:765-775`.
- **News:** temporal/compliance inputs are passed through init (`mq5:23-28`, `:389-392`) and entry is gated through `QM_NewsAllowsTrade2`/legacy fallback (`:425-433`). `validate_build_guardrails.py` returned `PASS`, zero findings, `max_news_stale_hours=336`.
- **Buffers/raw series/ML/costs:** the EA declares no dynamic indicator buffer or `CopyBuffer` call. Its direct closed-bar `iClose`/`iHigh`/`iLow` reads are annotated `perf-allowed` at `mq5:78`, `:84`, `:98`, `:133-137`, and `:237`, and the entry path is new-bar gated at `:437-445`. No ML dependency and no invented commission or swap logic was found.
- **Setfiles:** five D1 backtest sets exist under the EA's `sets/` directory, one for every approved card symbol. Each declares `environment: backtest`, `risk_mode: FIXED`, `RISK_FIXED=1000`, `RISK_PERCENT=0`, and its governed slot (representative EURUSD set `:7-20`). The task artifact lists the five paths under `setfiles_generated`; `test_gen_setfile.py` passed. These setfiles do not cure the stale EX5 identity.

## Commands and test evidence

```text
python tools/strategy_farm/validate_build_guardrails.py <QM5_36001 mq5>
PASS; 0 findings; max_news_stale_hours=336

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_36001_nnfx-classic-mcginley-ssl-wae
3 failures (D2 loss-limit inputs); D3-D18 checks for this EA otherwise clean

python -m pytest -q tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_gen_setfile.py tools/strategy_farm/tests/test_agent_router_state_exits.py
45 passed in 16.49s

python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_gen_setfile.py tools/strategy_farm/tests/test_agent_router_state_exits.py
1 failed, 74 passed in 436.97s
```

The aggregate failure is a pre-existing, out-of-scope fleet assertion for `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599` (`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`), not this EA. Independently, the direct hardening invocation above fails QM5_36001 on D2, so this review is not formally clean even without that unrelated fleet-test failure.

## Rollback

This review changes only this evidence note. Roll back with `git revert <review-commit>`; no EA, registry, setfile, binary, gate, verdict row, queue, factory, or `T_Live` artifact was modified by the review. Router closure is a separate required task-state transition and does not authorize compilation or backtest enqueue.

## Verdict

**RECYCLE** — source mechanics are otherwise card-faithful, but the formal hardening gate fails on three undeclared card loss-limit inputs and the delivered EX5 is demonstrably the pre-remediation binary. Fix the declared loss-limit surface without changing its values, regenerate governed setfiles, then **recompile via governed COMPILE_EA** and return with hash-bound compile/build identity plus a non-null smoke result.

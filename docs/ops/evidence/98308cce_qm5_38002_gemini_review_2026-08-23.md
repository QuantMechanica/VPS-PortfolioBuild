# QM5_38002 mandatory Codex review — 2026-08-23

- Review task: `98308cce-d346-439d-8e85-a258ea84bd24`
- Gemini build task: `f0823eb2-3859-45a3-9fa4-a83cc44207ef`
- EA: `QM5_38002_codetrading-macd-ema-trend-pullback`
- Build packet: `artifacts/builds/f0823eb2-3859-45a3-9fa4-a83cc44207ef.json`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_38002_codetrading-macd-ema-trend-pullback.md`
- Reviewed MQ5 SHA-256: `0558b068cf0c33b171d61f32feeb0c297065345b1bf9de3943d6652d88f55bda`
- Reviewed EX5 SHA-256: `1e70ebd3a786c96d122bbf2f28e64e4f35f65423621f1805d3bc4cbf5a820453`
- Disposition: `CHANGES_REQUIRED`; leave in `REVIEW`, with no pipeline promotion.

## Mechanical verification

- The current MQ5 and EX5 hashes exactly match the Gemini build packet.
- `_build_review_dispatch_gate(...)`: `BUILD_REVIEW_DISPATCH_PASS`; the packet's committed identity is structurally reviewable.
- `validate_build_guardrails.py`: PASS for the MQ5 and all three setfiles; news staleness remains capped at 336 hours.
- `validate_spec_doc.py`: PASS (`1 PASS, 0 FAIL`).
- Registry and setfile universe: SP500.DWX slot 0, NDX.DWX slot 1, and EURUSD.DWX slot 2; every backtest set uses `RISK_FIXED=1000` and `RISK_PERCENT=0`.
- The prior open-position deadlock was partly repaired: the one-position admission check is now inside `Strategy_EntrySignal()` rather than `Strategy_NoTradeFilter()`.

A committed/compiled packet is necessary but not sufficient. Card-to-code review still fails.

## Blocking findings

1. **The entry stop is a different strategy mechanic.** The approved card requires the recent five-bar swing low/high minus/plus 2.0 pips. The source instead declares `strategy_atr_sl_mult=1.5` (MQ5 line 43) and sets the stop distance to `g_last_atr * strategy_atr_sl_mult` (line 150). `SPEC.md` repeats the unauthorized 1.5×ATR stop. This changes both exits and risk-sized volume and preserves the principal blocker from the 2026-08-18 Codex review.

2. **The card's capital-preservation controls are absent.** A fresh canonical `build_gate_hardening.py` run fails D2 three times: no 2.0% realized daily entry halt, no 2.5% daily hard stop, and no 5.0% total drawdown stop. The EA never reconfigures the kill switch, so `QM_FrameworkInit` leaves framework defaults at daily 3.0%, total 0.0%, and per-trade 1.0%, which do not implement the approved card.

3. **The GMT rollover contract uses broker time.** `StrategyInRolloverWindow(TimeCurrent())` at MQ5 line 110 has no `QM_BrokerToUTC` conversion. The canonical hardening check fails D5 (`EA_BROKER_TIME_USED_FOR_GMT_WINDOW`).

4. **The first eligible bar can bypass the ATR spread ceiling.** `Strategy_NoTradeFilter()` runs at line 243 before `AdvanceState_OnNewBar()` at line 273. On startup `g_last_atr` is zero, so the spread comparison is skipped; ATR and the signal are then populated and entry follows without re-running admission. This is the second unresolved blocker from the prior review.

5. **Entry-only filters still suppress active management.** `Strategy_NoTradeFilter()` is called before `Strategy_ManageOpenPosition()` (lines 243 and 246). Rollover or wide spread therefore skips the approved ATR trail for an already-open position. On restart, `g_last_atr` also remains zero and blocks management until the first chart-new-bar event because `OnInit()` does not seed closed-bar state.

6. **The three-tick slippage ceiling is not wired.** There is no strategy slippage input or `QM_EntryConfigure` override. The framework consequently retains its default order deviation of 20 points, which is not the card's maximum three trade ticks.

## Required repair

- Implement the exact five-bar swing ±2-pip entry stop and update the SPEC/setfiles.
- Add and wire the card's 2.0% entry halt, 2.5% daily hard stop, 5.0% total stop, 0.5% per-trade cap, and three-tick deviation ceiling without weakening framework guardrails.
- Convert the 23:55–00:05 window to UTC, seed/refresh closed-bar state before ATR-dependent admission, and keep active management ahead of entry-only filters.
- Recompile through the governed compiler, regenerate committed identity evidence, and return to mandatory Codex review.

No source was changed in this review. No Q phase, pipeline verdict, terminal launch, AutoTrading action, or T_Live action occurred.

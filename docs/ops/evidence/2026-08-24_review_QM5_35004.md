# review_ea — QM5_35004 babypips-asian-box-london-breakout

- Router task: `a138d0c0-d562-44df-a1ad-a325de17bfb0` (review_ea, was assigned codex; closed by Claude review lane)
- Source: gemini/agy build task `c2e1b160-70bb-49bf-afd2-6dc52e784ad5`, verdict `SOURCE_READY_REMEDIATED`
- Build artifact: `artifacts/builds/c2e1b160-70bb-49bf-afd2-6dc52e784ad5.json`
- Card: `D:\QM\strategy_farm\artifacts\cards_approved\QM5_35004_babypips-asian-box-london-breakout.md` (G0 APPROVED)
- Verdict: **APPROVED** (clean for Q02)

## Build evidence for current source hash
- Current `.mq5` sha256 = `4513f1f1a192c5c040015a90f5fecfdac35d9d214b615e5552968f5d26870fc5` == recorded `mq5_sha256` in signed build artifact.
- Current `.ex5` sha256 = `d90ab915cd2642632fc122a381cf3aabc85d77a4118a7ce70f7880a3e62b6e70` == recorded `ex5_sha256`.
- Artifact records `build_check_passed:true`, `compile_succeeded:true` for exactly these bytes → COMPILE_OK for the current source hash. (`.ex5` mtime predates the `.mq5` mtime, but hashes match the signed artifact, so build evidence is valid for the current source.)

## Card-faithful mechanism
- Asian box (00:00–06:00 GMT) high/low via `CalculateAsianBox`, requires exactly 24 M15 bars on same UTC day; Shift=1 close evaluation (no lookahead). ✓
- London entry window 07:00–09:00 GMT, `Close[1] > box_high+2pip` / `< box_low-2pip`, `box_range ≤ 40pip`. ✓
- SL = box midpoint; TP = exec_price + 2.0×box_range (literal card §3.4 formula). Card's "1:2.0 R:R" label is internally inconsistent with its own TP formula; code follows the explicit formula — not a code defect; Q02 measures reality.
- Rollover blackout 23:55–00:05, spread filter >1.8×ATR(14), 2% daily-loss entry halt, max 1 open position, 16:00 GMT force-close. ✓

## Every declared strategy input has a use-site (grep, file `QM5_35004_...mq5`)
All 14 `strategy_*` inputs occur ≥2× (declaration + ≥1 use). No dead inputs. Risk/DD inputs (`strategy_daily_hard_stop_pct`, `strategy_total_dd_halt_pct`, `strategy_per_trade_risk_cap_pct`) consumed by `QM_KillSwitchInit` in `OnInit`.

## Framework conformity
- QM_Common chain included; MagicResolver via `QM_FrameworkMagic()`; magic rows 350040000/1/2 = 35004*10000+slot in `framework/registry/magic_numbers.csv:17406-17408`, ea_id row present. ✓
- MAE hook `QM_FrameworkTrackOpenPositionMae()` in `OnTick`. ✓
- RISK_FIXED=1000 / RISK_PERCENT=0 (backtest); setfiles carry `risk_mode: FIXED`, RISK_FIXED=1000, RISK_PERCENT=0, governed generator header + build_hash. ✓
- News filter wired (`QM_NewsAllowsTrade2` / legacy fallback). Bounded buffers only (CopyRates fixed count, scalar iHigh/iLow/iClose, QM_ATR). No raw unbounded series, no ML, no invented commission/swap. ✓

## Conclusion
Card-faithful, framework-conformant, no dead inputs, build evidence matches current source hash, setfiles governed. **APPROVED for Q02.**

# QM5_38005 mandatory Codex review — 2026-08-23

- Review task: `c31229df-db43-47e7-bda8-85c4e8d48777`
- Gemini build task: `2f3177d2-2769-4e0d-a212-4769b908178c`
- EA: `QM5_38005_codetrading-ascending-triangle-breakout`
- Build summary: `artifacts/qm5_38005_build_result.json`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_38005_codetrading-ascending-triangle-breakout.md`
- Reviewed MQ5 SHA-256: `b8f602673dd2353af2c6d5f9f628c63ea43df308e7561f01ed21cd254e5814ee`
- Reviewed EX5 SHA-256: `d8d5c28ed4de4b6e68aec88eb38e8375d6692301bb05d48c2446427615661ac9`
- Disposition: `CHANGES_REQUIRED`; leave in `REVIEW`, with no pipeline promotion.

## Mechanical verification

- The current MQ5 and EX5 hashes equal the values written in the Gemini summary, and `_build_review_dispatch_gate(...)` returns `BUILD_REVIEW_DISPATCH_PASS`.
- That structural gate is not compile provenance. The MQ5 was modified at 2026-08-23 12:50 UTC by commit `6fdb5b310`, while the retained EX5 is dated 2026-08-18 16:12 UTC. A search of the farm/repository artifacts found the current MQ5 hash only in the summary itself, not in a strict compiler result. The summary claims `compile_succeeded=true` and `smoke_result=passed`, but `smoke_report_path` is null. The executable is therefore not evidence-bound to the reviewed source.
- `validate_build_guardrails.py` passes for the MQ5 and all three setfiles. News staleness is 336 hours; every setfile uses `RISK_FIXED=1000` and `RISK_PERCENT=0`.
- `validate_spec_doc.py` passes (`1 PASS, 0 FAIL`). Registry/setfile symbols are XAUUSD.DWX, SP500.DWX, and EURUSD.DWX.
- Fresh `build_gate_hardening.py` reports four failures: the card's 2.0% daily entry halt, 2.5% daily hard stop, and 5.0% total drawdown stop are absent, and raw broker time is used for a GMT window.

A structurally reviewable packet and static guardrail pass are necessary but not sufficient. Card-to-code review fails.

## Blocking findings

1. **The reviewed executable is stale or otherwise unproven.** The EX5 predates the post-fix MQ5 by five days, and there is no strict compile record binding the current source hash to that binary. A hash list inside the same summary and an unevidenced smoke claim cannot establish build identity.

2. **The triangle geometry is not the approved mechanic.** The card requires resistance slope `|beta_res| <= 0.05` and support slope `beta_supp >= +0.10`. The EA stores pivot bar indices at lines 134–160 but never uses them. Instead, lines 174–188 compare relative price differences (a permissive 5% at the default) and only test `l1 > l2` / `h1 < h2`; no slope over time or minimum 0.10 support slope is calculated. The EA also defaults the pivot half-window to 4 at line 37 while the approved card specifies 5.

3. **Exit management substitutes a different contract.** The card names a one-triangle-height target and a trailing exit at swing pivots. Lines 256/266 set a generic 2R target, while lines 307–322 perform only a one-time break-even lock and never trail swing pivots. Because the card also parenthetically labels the height target as 1:2, this inconsistency needs explicit card adjudication; the build cannot silently replace the named height/pivot mechanics in `SPEC.md`.

4. **Mandatory volume can fail open.** `vol_ok` accepts `avg_vol <= 0.0`, so missing/zero volume data can admit a triangle even though the card requires `Vol[1] > 1.3 × SMA(Vol,20)[1]`. The source also uses `>=` rather than the card's strict `>`.

5. **The card's capital-preservation controls are absent.** The canonical hardening check fails the 2.0% realized-daily entry halt, 2.5% daily hard stop, and 5.0% total drawdown stop. The source also does not wire the card's 0.50% per-trade cap or maximum three-tick order deviation.

6. **Time, startup admission, and management ordering remain unsafe.** `StrategyInRolloverWindow(TimeCurrent())` at line 204 uses broker time rather than GMT. `Strategy_NoTradeFilter()` at line 385 runs before ATR/state population at line 415, allowing the startup bar to skip the spread ceiling, and it returns before management at line 388, suppressing open-position management during rollover or a wide spread. `OnInit()` does not seed ATR/management state.

7. **Invalid pivot-stop geometry falls back to ATR.** Lines 251/261 replace the card's recent-pivot ±2-pip stop with 1.5×ATR rather than rejecting an invalid entry, changing both the exit and risk-sized volume.

## Required repair

- Implement actual time-normalized pivot slopes and the approved pivot-window default; fail closed when mandatory volume is unavailable.
- Obtain card adjudication for the contradictory target wording, then implement the authorized triangle-height target and swing-pivot trail exactly.
- Implement the 2.0%/2.5%/5.0% loss controls, 0.50% per-trade cap, and three-tick deviation ceiling.
- Convert rollover to UTC, seed restart-safe state, keep active management ahead of entry-only filters, and reject invalid pivot-stop geometry instead of substituting ATR.
- Recompile through the governed compiler and provide strict hash-bound compile/smoke evidence for the current MQ5 before another mandatory Codex review.

No source was changed in this review. No Q phase, pipeline verdict, terminal launch, AutoTrading action, or T_Live action occurred.

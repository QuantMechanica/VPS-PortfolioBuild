# Claude review-lane close — QM5_1612_aa-dsp-hplwma10 — RECYCLE

- Router task: `ee8153cf-df11-47af-8157-aaf473ce8dbb` (review_ea, assigned `codex`, state REVIEW).
- Original build task: `690cd9ab-da44-4dd5-8cb2-0212384dc3db`.
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1612_aa-dsp-hplwma10.md` (`g0_status: APPROVED`).
- Reviewed HEAD: `a7d17e9f8a6b508fe2255314ab6a8434b0098324`; last EA-dir commit `4d2021b98 rework(1612): restore spread guard and D1 cadence`.
- Current source `QM5_1612_aa-dsp-hplwma10.mq5` raw SHA-256: `a875de301a1fa8dbb36419e971270626a15fb430341396c7d09e8092424bbf8a`.
- Verdict: **RECYCLE** — one named, in-repo actionable defect (stale provenance binding); plus the known uncompiled-EX5 constraint.

## Review context

Codex's prior verdict (`ee8153cf_qm5_1612_codex_review_2026-08-22.md`, JSON dated
`2026-08-24T07:26:14Z`) FAILed source `9fb5ad806` / MQ5 `b33a94ad...`. That verdict is
**stale**: rework commit `4d2021b98` (in HEAD ancestry) reworked the EA afterward. This
review is against the current source, not Codex's superseded finding.

## The five original Codex findings are resolved in the current source

1. Dynamic spread guard restored — 2.5x median of the preceding 20 completed D1 spreads,
   fail-closed, bounded `CopySpread` (perf-allowed comment), at
   `QM5_1612_aa-dsp-hplwma10.mq5:106-141`; called from entry at `:203`. Card line 58 satisfied.
2. D1 cadence restored — MAE `:322`, management `:333`, single `_Symbol/PERIOD_D1` new-bar
   gate `:335`, strategy exit **after** the gate `:340`. Card line 45 satisfied.
3. Oil identity repaired — `USOIL.DWX` represented by governed alias `XTIUSD.DWX`; magic
   row `1612,aa-dsp-hplwma10,13,XTIUSD.DWX,16120013` present (formula `1612*10000+13` OK),
   14th backtest setfile present. Card line 40 satisfied.
4. Spec corruption cleared — `SPEC.md` has no `0x07/0x1B` control bytes; `$1,000` restored
   (`SPEC.md:76`); v2 revision recorded.
5. Identity JSON moved to canonical build-result schema (`build_identity.json`).

## Card / framework conformity (current source) — PASS

- Mechanism card-faithful: HPLWMA10 fixed coefficients (`:93-102`) exactly match card line 37;
  entries (`:218-227`) match card 38-39; exits (`:276-279`) match card 43-44; SL 2.5xATR(20,D1);
  min-20-D1-bars and one-position-per-magic present.
- All five declared strategy inputs have executable use-sites (grep, no dead input):
  `strategy_min_daily_bars` (`:197,200`), `strategy_atr_period` (`:194,211`),
  `strategy_atr_sl_mult` (`:194,237`), `strategy_spread_median_days` (`:108,112,120,197,199`),
  `strategy_spread_median_mult` (`:108,140`).
- Framework corset: `QM_Common` include; MAE hook first in `OnTick`; `RISK_FIXED`=1000 backtest
  / `RISK_PERCENT` live (setfiles `RISK_FIXED=1000, RISK_PERCENT=0`); magic via
  `QM_FrameworkMagic()` / `req.symbol_slot`; news wired (`QM_NewsAllowsTrade2`, `:362-368`) and
  correctly ordered after management/exit; bounded buffers only; no ML/grid/martingale; no raw
  handles or `CopyBuffer`; no invented commission/swap.
- `build_gate_hardening.py --ea-label QM5_1612_aa-dsp-hplwma10` (D10): `failures: []`,
  `warnings: []`, 1 file scanned. D10-clean for the current source.
- 14 magic rows, slots 0-13, no collisions; 14 setfiles, one per slot.

## Blocking defect — stale provenance binding (RECYCLE)

The setfile `build_hash` and `build_identity.json.mq5_sha256` **are** the raw SHA-256 of the
source `.mq5`, but they record `02ac5bc08d9caaa6b22079c3da10e88872758502b534836369101caa1409b477`
while the committed source hashes to `a875de301a1f...`. The source was edited after
`gen_setfile`/identity generation and committed without regenerating them.

- All **14/14** setfiles carry the stale `; build_hash:   02ac5bc0...`
  (e.g. `sets/QM5_1612_aa-dsp-hplwma10_XTIUSD.DWX_D1_backtest.set`).
- `build_identity.json:6` `mq5_sha256` = `02ac5bc0...` (stale).
- The current source hash `a875de30...` appears **nowhere** in the deliverable.
- The builder's own delivered regression test proves it: `python -m pytest
  tools/strategy_farm/tests/test_qm5_1612_review_rework_static.py -q` -> **2 failed, 4 passed**
  (`test_registry_and_setfiles_cover_the_repaired_oil_alias` at `:128`,
  `test_build_identity_uses_the_canonical_build_result_schema` at `:172`).

Setfiles are source-hash-bound and gate Q02 dispatch (`BOUND_SETFILE_HASH`); a mismatch means
the package is not clean for Q02. This is fixable in-repo without a compile:
**regenerate the 14 setfiles and `build_identity.json` via the governed generator against the
committed source (`a875de30`), then re-run the delivered test until 6/6 pass.**

## Secondary constraint (known, not builder-fixable while factory runs)

No compiled EX5 exists for the current source: `build_identity.json:44` `compile_succeeded=false`,
`ex5_sha256` is the stale pre-rework binary `abeb9a5a...`. Per the compile-in-OFF-window rule the
rework correctly refused an ad-hoc compile with live factory terminals up and recorded this
truthfully. A `COMPILE_EA` / OFF-window compile is required before Q02 regardless; it is
orchestrator-commissioned, not a builder defect.

## Required rework

1. Regenerate all 14 backtest setfiles and `build_identity.json` against the committed source
   `a875de30...` with the governed `gen_setfile` path; confirm
   `test_qm5_1612_review_rework_static.py` returns 6/6.
2. Then commission a governed `COMPILE_EA` for QM5_1612 in an OFF window to mint the matching EX5
   and smoke summary before Q02 dispatch.

## Safety boundary

Read-only review. No EA source, card, setfile, registry, resolver, EX5, terminal, backtest,
Q-phase state, T_Live, AutoTrading, risk threshold, or news ceiling was changed. No compile or
pipeline verdict was claimed. Only the sanctioned `close-review` router call and this note commit
were made.

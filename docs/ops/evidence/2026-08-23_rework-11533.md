# rework-11533 — Carter H1 review repair evidence

## Status and scope

- Router task: `e70b7248-0abd-4e9d-9683-95297a51ff58` (`RECYCLE` -> `IN_PROGRESS`).
- EA: `QM5_11533_carter-t-h1-ema3-5-13-21-80-rsi21` only.
- Work order: `docs/ops/evidence/2026-08-21_review_ea_11533_carter.md`.
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11533_carter-t-h1-ema3-5-13-21-80-rsi21.md`, `g0_status: APPROVED`.
- Result: `SOURCE_REPAIRED_COMPILE_HELD`. All four review findings are repaired in
  source/spec/set identity and the scoped static gates pass. A fresh EX5 could
  not be produced because the live-factory compile interlock refused the
  ad-hoc compile while managed `terminal64.exe` processes were alive.

No registry allocation changed. No backtest or compile work item was enqueued,
no verdict row was overwritten, the factory was not toggled, and
`C:/QM/mt5/T_Live` was not touched.

## What changed

1. The spread/quote cap is entry-only. `Strategy_SpreadAllowsEntry()` is at
   `framework/EAs/QM5_11533_carter-t-h1-ema3-5-13-21-80-rsi21/QM5_11533_carter-t-h1-ema3-5-13-21-80-rsi21.mq5:54`
   and is called only from `Strategy_EntrySignal` at line 125. In `OnTick`, MAE
   remains first at line 223, management is line 230, and the indicator exit is
   line 232, before news/new-bar/entry gates. A wide spread can no longer
   suppress an existing position's card exit.
2. The H1 runtime contract now fails closed. `OnInit` declares `PERIOD_H1` and
   `QM_FRIDAY_CLOSE_DISABLED` at MQ5 line 212. The explicit H1 bar gate is line
   251. The card's broker-time no-Friday-entry veto remains in
   `Strategy_EntrySignal`; forced Friday close is disabled because the card
   authorizes no Friday entries, not forced exits. The matching source inputs
   are at MQ5 lines 19 and 31, and the setfile seals Friday close off at lines
   22-23.
3. The approved medium-cross rule is literal bar-2-to-bar-1 behavior. Long and
   short mirror conditions require both fast EMAs to cross both EMA13 and EMA21
   in `medium_cross_up` / `medium_cross_dn` at MQ5 lines 84 and 108. The spec
   records this exact interpretation at `SPEC.md:15`.
4. The canonical backtest set is bound to repaired source SHA-256
   `8d58d38b60f8f67aa26400be183ba6dc7425d0a6fffc2451cba55b7d3dcc404a`
   at `sets/QM5_11533_carter-t-h1-ema3-5-13-21-80-rsi21_EURUSD.DWX_H1_backtest.set:13`.
   It retains `RISK_FIXED=1000`, `RISK_PERCENT=0`, and slot 0. The source default
   is also backtest-safe (`RISK_PERCENT=0.0`, MQ5 line 19); live packaging must
   select percent risk and zero fixed risk under the existing framework contract.
5. Five ticket-specific regression tests begin at
   `tools/strategy_farm/tests/test_qm5_11533_review_rework_static.py:41` and cover
   exit reachability, H1/Friday declaration, exact medium crosses, every strategy
   input use-site plus wrapper/no-ML constraints, and registry/set identity.

Preflight found the existing active registry rows, so no registry or resolver
change was authorized: `framework/registry/ea_id_registry.csv:2421` and
`framework/registry/magic_numbers.csv:15654` (`EURUSD.DWX`, slot 0, magic
`115330000`). `QM_FrameworkInit` resolves that identity through
`QM_MagicChecked`; the EA consumes it through `QM_FrameworkMagic()` and assigns
`req.symbol_slot = qm_magic_slot_offset` at MQ5 line 153.

## Validation evidence

### Scoped hardening and validators

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_11533_carter-t-h1-ema3-5-13-21-80-rsi21
-> files_scanned=1; failures=[]; warnings=[]

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_11533_carter-t-h1-ema3-5-13-21-80-rsi21
-> 1 PASS, 0 FAIL

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_11533_carter-t-h1-ema3-5-13-21-80-rsi21
-> PASS; 2 files checked; findings=[]

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_11533_carter-t-h1-ema3-5-13-21-80-rsi21 --fail-on-leak --verbose
-> SINGLE_SYMBOL_OK; n_violations=0

python framework/scripts/skill_build_ea_guard.py --ea-id 11533 --ea-label QM5_11533_carter-t-h1-ema3-5-13-21-80-rsi21
-> status=ok; ea_registry_row=true; magic_registry_rows=true; ea_dir_exists=true
```

Input-use census from the final source: `strategy_ema_fast=6`,
`strategy_ema_signal=6`, `strategy_ema_medium1=5`, `strategy_ema_medium2=5`,
`strategy_ema_baseline=3`, `strategy_rsi_period=4`,
`strategy_rsi_threshold=5`, `strategy_sl_pips=2`, and
`strategy_max_spread_pips=2` occurrences (declaration included).

### Pytest

```text
python -m pytest tools/strategy_farm/tests/test_qm5_11533_review_rework_static.py -q
.....                                                                    [100%]
5 passed in 0.77s

python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py -q -k "not qm5_411xx_sources_have_no_unbounded_numeric_buffers"
............................                                             [100%]
28 passed, 1 deselected in 269.45s (0:04:29)

python -m pytest tools/strategy_farm/tests/test_qm5_11533_review_rework_static.py tools/strategy_farm/tests/test_build_gate_hardening.py -q
1 failed, 33 passed in 414.65s (0:06:54)
```

The full combined run's sole failure is pre-existing and outside EA 11533:
`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven
QM5_411xx findings, first at
`QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`. The scoped 11533 analyzer has zero
buffer findings and the filtered hardening module passes above.

### Compile/build-check interlock

The required exact-EA compile was attempted:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_11533_carter-t-h1-ema3-5-13-21-80-rsi21/QM5_11533_carter-t-h1-ema3-5-13-21-80-rsi21.mq5 -Strict
-> INCLUDE_MIRROR_REFUSED / LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
```

Evidence paths:

- Compile refusal log:
  `C:/QM/worktrees/rework-slot-1/framework/build/compile/20260824_081801/QM5_11533_carter-t-h1-ema3-5-13-21-80-rsi21.compile.log`
- Compile summary: `D:/QM/reports/compile/20260824_081801/summary.csv`

The explicitly scoped static build-check attempt also stopped at the same guard:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_11533_carter-t-h1-ema3-5-13-21-80-rsi21 -SkipCompile -SkipSetValidation
-> BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED / LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
```

No retry, guard bypass, process stop, factory toggle, compile enqueue, or
backtest enqueue was attempted. The tracked EX5 remains the prior binary:
SHA-256 `1d0b8e7fb1dda4734c1f357590a783f8288c23e93cb48624efa273313b3722cd`,
which predates the repaired source and is not claimed as a fresh build. This is
the remaining review risk and requires a governed compile during a quiescent
window; the ticket's explicit no-enqueue constraint prevents using the router's
suggested `enqueue-compile` fallback here.

`git diff --check` passed.

## Rollback

Revert the single ticket commit. This restores the previous EA source/spec/set,
removes the regression test and this evidence file, and leaves the pre-existing
EX5 unchanged. No runtime, database, queue, registry, resolver, factory, or live
rollback is required because none of those surfaces was mutated.

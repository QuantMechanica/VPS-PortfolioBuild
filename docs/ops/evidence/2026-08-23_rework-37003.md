# rework-37003 — QM5_37003 review-fix evidence

- Router task: `8406a8c0-a126-46ed-858b-6515bb6a5528`
- EA: `QM5_37003_hurst-exponent-dynamic-regime-switch`
- Worktree: `C:/QM/worktrees/rework-slot-6`
- Executed: 2026-08-24
- Scope: one-EA build/review repair only; no backtest enqueue, verdict-row
  overwrite, registry edit, factory toggle, or `T_Live` access.

## Work order and preflight

The blocking review is
`docs/ops/evidence/8406a8c0_qm5_37003_gemini_build_codex_review_2026-08-18.md`.
It identified a blocked producer result without smoke evidence, absent card loss
limits, an unauthorized 1.5R substitute for invalid Bollinger-midline targets,
and entry-only filters that could suspend midline management.

The approved card is
`strategy-seeds/cards/approved/QM5_37003_hurst-exponent-dynamic-regime-switch.md`:
its front matter records the matching EA identity and `g0_status: APPROVED`, and
its rules declare the 2.0% realized-loss entry halt, 2.5% daily hard stop, 5.0%
total drawdown stop, Bollinger-midline mean-reversion target, and maximum three
ticks of slippage.

Registry evidence is unchanged:

- `framework/registry/ea_id_registry.csv:4466` contains the active matching
  `37003,hurst-exponent-dynamic-regime-switch` identity.
- `framework/registry/magic_numbers.csv:17458-17460` contains the existing
  active EURUSD/GBPJPY/SP500 slots 0/1/2 and magics 370030000-370030002.

No registry gap existed, so neither registry nor `QM_MagicResolver.mqh` was
changed or regenerated.

## What changed

The inherited source remediation remains intact and is now regression-covered:

- The account-wide 2.0% realized-loss entry halt is computed from closed deal
  PnL and used by the entry filter at `.mq5:165-186`.
- The restart-safe framework kill switch receives the separate 2.5% daily
  equity, 5.0% total-DD signal, and 0.5% per-trade cap values at `.mq5:377-381`.
- Invalid favorable-side Bollinger midlines reject mean-reversion entries;
  there is no fixed-R fallback at `.mq5:246-264`.
- Open-position midline management runs before news/new-bar/no-trade entry
  gates at `.mq5:398-438`; the MAE hook remains first at `.mq5:398`.

This ticket closed the remaining execution-contract gap:

- Added `strategy_max_slippage_ticks=3.0` with fail-closed range validation at
  `.mq5:61` and `.mq5:87-88`.
- Converted symbol ticks to MT5 deviation points and reconfigured the canonical
  framework entry path at `.mq5:356-370`.
- Declared the H1/framework-Friday execution contract at `.mq5:372-375`.
- All three H1 backtest presets retain `RISK_FIXED=1000` and
  `RISK_PERCENT=0`, point to the approved card, carry every strategy input, and
  bind canonical-LF MQ5 SHA-256
  `4270e5e3172550dee265c1e20f50af931c8b0f3c6769c77580da5282f7f64cc9`
  (each set: lines 13, 19-23, 34-38).
- `SPEC.md:39` documents the slippage input and `SPEC.md:101` now names the
  real `EA_RISK_SIZER_UNCONFIGURED` build failure instead of the nonexistent
  legacy code.
- `tools/strategy_farm/tests/test_qm5_37003_rework_static.py:58-211` adds seven
  regressions for approved-card/registry/resolver identity, all review repairs,
  Hurst/indicator bounds, perf annotations, framework/risk/magic/MAE wiring,
  every declared input's use-site, and preset coverage/hashes.

No ML, raw magic arithmetic, unbounded buffer access, or unannotated raw series
call was introduced.

## Validation evidence

### EA-specific tests and validators

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_37003_rework_static.py
7 passed in 0.72s

python -m pytest -q tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_validate_symbol_scope.py tools/strategy_farm/tests/test_qm5_37003_rework_static.py
30 passed in 1.51s

python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py -k "d2_loss or d4_management or d5_raw or d7_framework or d10_indicator or d11_canonical or semantic_predicates"
7 passed, 22 deselected in 1.12s

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_37003_hurst-exponent-dynamic-regime-switch
files_scanned=1; failures=[]; warnings=[]

python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 framework/EAs/QM5_37003_hurst-exponent-dynamic-regime-switch
PASS; files_checked=4; findings=[]

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_37003_hurst-exponent-dynamic-regime-switch --fail-on-leak
SINGLE_SYMBOL_OK; n_violations=0

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_37003_hurst-exponent-dynamic-regime-switch
Summary: 1 PASS, 0 FAIL (of 1)
```

The broader touched-tool run completed with `58 passed, 1 failed in 317.75s`.
Its sole failure is the pre-existing repository-wide
`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` census, beginning
with unrelated `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`; this ticket changes
no 411xx EA. The relevant hardening selection above is clean.

`git diff --check` passed for all ticket paths.

### Scoped build and smoke interlock

Both compile-capable and `-SkipCompile` scoped wrapper forms were attempted:

```text
pwsh -NoProfile -File framework/scripts/build_check.ps1 -EALabel QM5_37003_hurst-exponent-dynamic-regime-switch
pwsh -NoProfile -File framework/scripts/build_check.ps1 -EALabel QM5_37003_hurst-exponent-dynamic-regime-switch -SkipCompile
```

It failed closed before static validation or compilation with
`BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED` / `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`
because T1-T10 factory terminal processes were active. The guard explicitly
requires the governed compile queue in that state. The task authorizes router
state updates but not runtime DB/compile/backtest enqueue, and forbids factory
or terminal intervention, so no guard was bypassed and no process was stopped.

Consequently there is no current EX5 or smoke report for the repaired source,
and no compile or runtime PASS is claimed. No smoke/backtest was enqueued or
run. A governed compile plus build smoke remains required before pipeline
handoff.

## Rollback

After commit, run `git revert --no-edit <rework(37003)-commit>`. This restores
the prior MQ5, SPEC, and three presets and removes the focused test/evidence
files. No registry, resolver, runtime DB, queue, factory, terminal, or live
state requires rollback.

## Residual risk / reviewer action

- Source/static conformance is clean, but the safety interlock prevented a new
  EX5 and smoke report. Review must not infer runtime PASS from source checks.
- The unrelated QM5_411xx hardening census remains red outside this ticket's
  one-EA scope.

# QM5_38001 Build EA Evidence — 2026-08-18

**Task ID:** `33322516-1797-4d97-8a74-eb4fd7385953` (`build_ea`, priority 50, agent `gemini`).  
**EA:** `QM5_38001_codetrading-vwap-bollinger-rsi-scalper`.  
**Artifact Path:** `C:/QM/repo/docs/ops/evidence/33322516_qm5_38001_build_ea_2026-08-18.md`.  
**Strategy Card:** `strategy-seeds/cards/approved/QM5_38001_codetrading-vwap-bollinger-rsi-scalper.md` (G0 APPROVED).  

---

## 1. Strategy Overview & Implementation

- **Strategy Thesis**: Intraday mean-reversion scalper on M5 combining Session VWAP, Bollinger Bands (20, 2.0), and RSI(14).
  - Long (BUY): `Low[1] <= LowerBB[1] && Close[1] < VWAP[1] && RSI(14)[1] <= 30.0 && Close[1] > Open[1]`.
  - Short (SELL): `High[1] >= UpperBB[1] && Close[1] > VWAP[1] && RSI(14)[1] >= 70.0 && Close[1] < Open[1]`.
  - Exits & Stops: Stop Loss at $1.5 \times \text{ATR}(14)$, Take Profit targeting Session VWAP line (fallback $1.8 \times \text{SL}$), Break-Even move at $+1.0\text{R}$ profit.
- **Framework Corset Compliance**:
  - Implemented cleanly using `EA_Skeleton.mq5` wiring.
  - Closed-bar calculation cached in `AdvanceState_OnNewBar()` to ensure $O(1)$ per-tick performance.
  - Sizing handled strictly by `QM_LotsForRisk` from `req.sl`.
  - News temporal mode (`QM_NEWS_TEMPORAL_PRE30_POST30`) and compliance (`QM_NEWS_COMPLIANCE_DXZ`) with fail-closed stale guard (`qm_news_stale_max_hours = 336`).

---

## 2. Registry & Artifact Verification

- **Registries Updated**:
  - `framework/registry/ea_id_registry.csv`: verified `38001,codetrading-vwap-bollinger-rsi-scalper`.
  - `framework/registry/magic_numbers.csv`: allocated slots 0 (`EURUSD.DWX`, `380010000`), 1 (`GBPUSD.DWX`, `380010001`), 2 (`USDJPY.DWX`, `380010002`).
  - `framework/include/QM/QM_MagicResolver.mqh`: regenerated cleanly via `update_magic_resolver.py`.
- **SPEC Document**:
  - `framework/EAs/QM5_38001_codetrading-vwap-bollinger-rsi-scalper/SPEC.md` written and verified with `validate_spec_doc.py` (`PASS`).
- **Static Checks & Compilation**:
  - `build_check.ps1`: `compile_one.result=PASS`, `errors=0`, `warnings=0`, `build_check.result=PASS`.
  - Emitted binary: `framework/EAs/QM5_38001_codetrading-vwap-bollinger-rsi-scalper/QM5_38001_codetrading-vwap-bollinger-rsi-scalper.ex5`.
- **P2 Setfiles**:
  - Generated for `EURUSD.DWX`, `GBPUSD.DWX`, `USDJPY.DWX` via `gen_setfile.ps1`.

---

## 3. Smoke Test & Review State

- `run_smoke.ps1`: Encountered standalone admission rejection (`custom_history_smoke_admission.py` requires worker-bound work item under active Custom-history isolation).
- Build result JSON emitted honestly per schema to `artifacts/qm5_38001_build_result.json` with `build_check_passed=true`, `compile_succeeded=true`, `smoke_result=framework_error`.
- Task updated to `REVIEW` for mandatory Codex review per router rules.

# Claude review_ea — QM5_12951 & QM5_12952 (Gemini-built)

**Router task IDs:** `6f392d1e-f1d8-402c-8d33-37d6661ab14e` (12951), `2c43671f-aabb-4ca5-8cf8-857505375ce2` (12952)
**Reviewer:** Claude (headless orchestration cycle)
**Date:** 2026-08-22
**Source build artifacts:** `D:/QM/strategy_farm/artifacts/builds/aae32e9c-372c-4108-9dd7-2f041682eae4.json`,
`D:/QM/strategy_farm/artifacts/builds/83f3bde2-3bfe-44cf-998d-ca623626a6d5.json`

## Scope

Both are `review_ea` tasks routed to claude with
`reason: codex_review_required_for_gemini_code` — per CLAUDE.md hard rules,
Gemini-authored code requires mandatory Codex review before acceptance. This
review is the Claude-side correctness pass; it does **not** substitute for
that Codex review, and this task is intentionally left in `REVIEW` state, not
advanced to `APPROVED`/`PIPELINE`.

## QM5_12951 — MQL5 Chaikin Oscillator Zero Cross

- `mq5` SHA256 verified locally: `c55ab571f66cf5eee351917cee26982dd06bb413852e9fc1f661262198e0d4fe`
  — matches build artifact.
- Magic base `129510000` = `12951 * 10000 + 0` — correct; registry rows
  `framework/registry/magic_numbers.csv:17657-17659` cover EURUSD/GBPUSD/XAUUSD.DWX
  (offsets 0/1/2), all `active`.
- Entry/exit cross-checked against SPEC.md/card:
  - Vol filter `ATR(14) >= 0.5*ATR(100)` — implemented as
    `atr_fast < strategy_atr_filter_ratio * atr_slow -> reject`, matches.
  - Long: `close1 > ema100 && osc_prev<=0 && osc_now>0`; Short: mirrored —
    matches card's zero-cross definition.
  - Chaikin Oscillator computed as fast/slow EMA of a running ADL
    (money-flow-multiplier * tick_volume), seeded EMA from first sample,
    `warmup_bars=80` default gives the EMAs room to settle before the values
    used (last two closed bars) are read — standard practice for this
    EA family.
  - `CopyRates(_Symbol, PERIOD_CURRENT, 1, bars_needed, rates)` starts at
    shift 1 (skips the still-forming bar); array is non-series so index
    `copied-1` = shift 1 (last closed bar) and `copied-2` = shift 2 — verified
    `osc_current`/`osc_previous` indexing lines up with that, no forming-bar
    leakage.
  - Exit: `osc_now<0 || close<ema` (long) / mirrored (short) — correctly
    encodes "Chaikin crosses back below zero or close falls below EMA(100)";
    checking the current-value threshold rather than a strict cross is
    equivalent here since the position could only be open while the prior
    state satisfied the opposite condition. 36-bar failsafe matches.
  - Stop `entry -/+ ATR(14)*1.7`, TP `2.0R` via `QM_StopATR`/`QM_TakeRR` —
    matches card, uses framework helpers (no inline lot/stop math).
  - `req.symbol_slot = qm_magic_slot_offset` wired correctly (host-slot-magic
    conflation class from 2026-08-16 checked — not present here).
- All 10 `strategy_*` inputs are read at a use site in
  `Strategy_ReadChaikinOsc`/`Strategy_EntrySignal`/`Strategy_ExitSignal` — no
  unwired inputs.
- `qm_news_stale_max_hours = 336` (guardrail ceiling, not exceeded). Backtest
  set file: `RISK_FIXED=1000`, `RISK_PERCENT=0` — correct per hard rule;
  strategy input defaults in the setfile match SPEC.md verbatim.
- Compile log: `0 errors, 0 warnings`.
- No ML, no grid/martingale, one-position-per-magic, H1 swing/ATR-stop —
  in scope for the Edge Lab charter.
- `expected_trades_per_year_per_symbol: 70` — above the Q02 >=5 trades/yr floor.

**Verdict: no correctness defects found.** Faithful implementation of the
approved card, framework wiring standard and consistent with sibling EAs.

## QM5_12952 — MQL5 Force Index / EMA Momentum

- `mq5` SHA256 verified locally: `f891223db2dc839f5f64a1645735afb884d9e757f05116c28e3ce3c69b40cce0`
  — matches build artifact.
- Magic base `129520000` = `12952 * 10000 + 0` — correct; registry rows
  `framework/registry/magic_numbers.csv:17660-17662` cover EURUSD.DWX/USDJPY.DWX/GER40.DWX
  (offsets 0/1/2), all `active`.
- Entry/exit cross-checked against SPEC.md/card:
  - Same ATR vol filter formula as 12951 — matches.
  - Long: `close1 > ema100 && force_prev<0 && force_now>0`; Short: mirrored —
    matches card's zero-cross definition, using a strict prev/now crossing
    check (tighter than 12951's threshold-only exit, not a defect).
  - Force Index = EMA(13) of `tick_volume[shift] * (close[shift]-close[shift+1])`.
    `ArraySetAsSeries(rates,true)` + `CopyRates(..., 0, rates_needed, rates)`
    starts at shift 0 (forming bar) but the EMA recursion only walks shifts
    `[target_shift=1 .. oldest_shift=copied-2]`, so shift 0 (forming bar) is
    never read as `rates[shift]` inside `raw_force` — verified no lookahead.
    `force_previous` captured at `shift==target_shift+1`, `force_current` at
    `shift==target_shift` — correct current/previous ordering on closed bars.
  - Exit: `(force_prev>0 && force_now<0) || close<ema` (long) / mirrored
    (short) — matches "Force Index crosses back... or close crosses EMA".
    36-bar failsafe matches.
  - Stop `entry -/+ ATR(14)*1.6`, TP `2.0R` via `QM_StopATR`/`QM_TakeRR` —
    matches card.
  - `req.symbol_slot = qm_magic_slot_offset` wired correctly.
- All 8 `strategy_*` inputs wired and used — no dead inputs.
- Same news/risk/guardrail posture as 12951: `qm_news_stale_max_hours=336`,
  `RISK_FIXED=1000`/`RISK_PERCENT=0` in the backtest set file; strategy
  defaults match SPEC.md.
- Compile log: `0 errors, 0 warnings`.
- Mechanical, no ML/grid/martingale, in-charter (H1 swing, ATR stop, fixed R).
- `expected_trades_per_year_per_symbol: 85` — above Q02 floor.

**Verdict: no correctness defects found.** Faithful implementation of the
approved card.

## Disposition

Both tasks are being moved to `REVIEW` with a PASS-leaning verdict from the
Claude side. Per hard rules this does not authorize `APPROVED`/`PIPELINE` —
Codex review remains mandatory before either EA advances. No close-review
call was made; both tasks stay in `REVIEW` for Codex to pick up.

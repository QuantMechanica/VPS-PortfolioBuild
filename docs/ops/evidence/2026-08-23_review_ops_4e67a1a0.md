# Claude review — ops_issue 4e67a1a0 (DL-090 report retention job)

- Task: `4e67a1a0-37bb-4995-9497-ebf05d35c172` (ops_issue, QM-TODO-20260823-508), assignee lane codex, delivered to REVIEW.
- Worker artifact: `docs/ops/evidence/2026-08-23_dl090_retention_job_implementation.md`
- Decision: `decisions/DL-090_backtest_report_retention_policy.md` (OWNER-ratified 2026-08-23)
- Script: `tools/strategy_farm/report_retention_purge.py` (committed 6fc367326, merged bcebd3d8a)
- Tests: `tools/strategy_farm/tests/test_report_retention_purge.py`

## Verdict: APPROVED

Formally clean; implementation faithful to DL-090, tested, and dry-run-verified. Live
execution/scheduling is an explicit, correctly-scoped follow-up, not a defect of this task.

## What I verified (read-only)

1. **Rule fidelity (DL-090 §2)** — `classify()` (report_retention_purge.py:148-247) implements
   the ratified rule exactly: PASS-family keep by prefix (`PASS_PREFIX`, l.107 → l.222), standing
   rejection = newest `strategy` non-PASS per (ea_id, symbol, phase) recomputed every run
   (l.238-244, DL-090 §4.5), age-out for superseded-strategy + infra/invalid (l.226-234),
   unclassified taxonomies kept (l.236, DL-090 §4.1), 30-day min-age (l.98, l.271-274),
   compress kept set after 30d (l.312-360). Artifact set matches DL-090 §2 (`KEPT_ARTIFACT_SUFFIXES`, l.110).
2. **Fail-closed (DL-090 §4)** — forbidden-path guard for T_Live / reports/state / decisions/
   (`FORBIDDEN_PREFIXES`, l.113-117; enforced in `_run_dir_for_evidence`, l.140-146); evidence
   path must resolve under `D:/QM/reports/work_items` (l.148-151); quarantine-before-delete with
   separate reap step (l.263-357); dry-run default, `--classify-only` never touches FS; DB-unreachable
   returns exit 2, logs `CLASSIFY_FAIL`, zero FS action (main, l.408-413); per-class byte logging (`_log`).
3. **Bug fix confirmed** — the added `raw_status` open-run guard (l.218) is real and correct:
   `work_item_clean_view.py:56` defines `OPEN_STATUSES={pending,active,claimed}` and the clean view
   exposes both `status` (restamped to done/failed when a verdict is present) and `raw_status`
   (l.510). Guarding `status in OPEN_STATUSES or raw_status in OPEN_STATUSES` closes DL-090 §4.6 for
   a claimed row carrying a stale verdict. Change is monotonic toward keep/skip — no policy shift.
4. **Tests** — `python -m pytest tools/strategy_farm/tests/test_report_retention_purge.py -q` →
   12 passed in 1.31s. Synthetic SQLite + tmp_path fixtures only; no D:/QM path touched.
5. **Live read-only dry-run** — `report_retention_purge.py --classify-only` ran clean against the
   production DB: keep_pass_family 2291 / keep_standing_rejection 1484 / keep_unclassified 127 /
   skip_open_status 0 / total_rows_seen 5170. (Counts drift run-to-run because the live factory
   mutates evidence_path continuously — expected for a read-only snapshot.)

## No thresholds, verdicts, seeds, or windows changed by this job (DL-090 §6 confirmed).

## Follow-up (not blocking, correctly flagged by worker)

- No Windows Scheduled Task yet (no `install_report_retention_purge_scheduled_task.ps1`); daily
  matching `QM_WorkItemLogPruner_Daily_0310` is the natural default.
- No `--execute` run ever performed; quarantine/reap/compress paths covered only by synthetic
  fixtures. Recommend scheduling in default dry-run first, observe 2-3 cycles, then flip `--execute`.
- Latent (accepted): standing-rejection ordering is a lexicographic `updated_at` sort — correct for
  uniform ISO-8601 UTC, fragile only if the upstream timestamp shape ever changes.

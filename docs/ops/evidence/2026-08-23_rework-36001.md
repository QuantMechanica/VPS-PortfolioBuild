# rework-36001 — QM5_36001 review-fix evidence

- Router task: `d47d0803-00ea-4832-905a-d5436728784f`
- EA: `QM5_36001_nnfx-classic-mcginley-ssl-wae`
- Worktree: `C:/QM/worktrees/rework-slot-5`
- Work order: `docs/ops/evidence/d47d0803_qm5_36001_gemini_build_codex_review_2026-08-18.md:14-58` and `docs/ops/evidence/2026-08-23_review_36001.md:11-30`
- Card authority: `strategy-seeds/cards/approved/QM5_36001_nnfx-classic-mcginley-ssl-wae.md:57-99` (`g0_status: APPROVED`)

## Result

The source, specification, and five D1 backtest setfiles now implement the approved card's TP1/runner lifecycle, completed-bar crossovers, direction-neutral WAE gate, loss rails, risk-mode defaults, and management ordering. Focused tests and all EA-scoped static gates pass.

A fresh `.ex5` could not be produced during this slot: the governed include-mirror interlock returned `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because factory `terminal64` processes were active. The factory was not toggled, no terminal was stopped, and no compile or backtest item was enqueued. The tracked binary remains the pre-remediation artifact and is explicitly not claimed as current.

## Finding-by-finding remediation

1. **TP1 must leave a runner.** Entries attach no whole-position broker TP (`QM5_36001_nnfx-classic-mcginley-ssl-wae.mq5:294-302`). Management reconstructs entry ATR from initial risk, closes `strategy_tp1_fraction` (default 50%), and moves the runner stop to entry plus/minus the configured one-pip buffer (`mq5:376-440`). Deal history keyed by `POSITION_IDENTIFIER` and `DEAL_POSITION_ID` makes the partial lifecycle restart-safe and restores BE without repeating the partial close (`mq5:242-263`, `mq5:397-416`).
2. **SSL must be a crossover.** `Strategy_SSLCross` compares completed D1 shifts 1 and 2 and only emits on a state transition (`mq5:111-137`, entry use at `mq5:322-358`).
3. **DeMarker must cross an extreme.** Runner exits compare shifts 1 and 2 at 0.70/0.30; SSL remains the companion flip exit (`mq5:445-473`).
4. **WAE must match both written equations.** `Strategy_WAEPass` is direction-neutral: it takes the absolute MACD momentum-change magnitude and applies the same `WAE > threshold` gate to long and short entries (`mq5:185-206`, `mq5:341-358`). Direction remains determined by McGinley, SSL, and Vortex. The SPEC records this interpretation at `SPEC.md:23-31`.
5. **GMT and loss-limit contracts.** Rollover uses `QM_BrokerToUTC`; the 2.0% realized-loss entry halt uses the framework's today-PnL history, and configured 2.5%/5.0%/1.0% hard rails are passed to `QM_KillSwitchInit` (`mq5:231-239`, `mq5:270-289`, `mq5:499-503`). Backtest defaults are `RISK_FIXED=1000`, `RISK_PERCENT=0`; live selection remains enforced by `QM_FrameworkInit`. Maximum slippage is converted from ticks and wired through `QM_EntryConfigure` (`mq5:487-520`).
6. **Entry filters must not suspend protection.** The MAE hook and kill switch run first, position management runs every tick, and completed-bar exits run before news/no-trade entry filters (`mq5:530-572`).

Additional hardening replaces per-element raw series calls with bounded `CopyClose`/`CopyRates` vectors, explicit `ArraySize` guards, and `perf-allowed` annotations (`mq5:81-106`, `mq5:140-182`). Magic remains resolver-owned through `QM_FrameworkMagic`; registry rows were verified at `framework/registry/magic_numbers.csv:17424-17428` and were not changed.

All declared strategy inputs are documented at `SPEC.md:37-63`, consumed by the EA, and emitted into every setfile. Representative generated values are at each setfile's lines 19-20 and 38-46. Regression enforcement is in `tools/strategy_farm/tests/test_qm5_36001_rework_static.py:54-196`.

## Validation evidence

### PASS — focused unit tests

Command:

```text
python -m pytest tools/strategy_farm/tests/test_qm5_36001_rework_static.py -q
```

Output:

```text
.........                                                                [100%]
9 passed in 0.84s
```

### PASS — EA-scoped build hardening

Command:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_36001_nnfx-classic-mcginley-ssl-wae
```

Result: one source scanned; `failures: []`; `warnings: []`; D2 loss contract resolved as 2.0/2.5/5.0; all five card/registry/setfile symbols matched exactly.

### PASS — guardrails and SPEC

Commands and results:

```text
python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_36001_nnfx-classic-mcginley-ssl-wae
PASS: files_checked=6, findings=[], verdict=PASS

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_36001_nnfx-classic-mcginley-ssl-wae
PASS  QM5_36001_nnfx-classic-mcginley-ssl-wae
Summary: 1 PASS, 0 FAIL (of 1)

git diff --check
exit 0 (line-ending notices only)
```

### Full strategy-farm test suite

Command:

```text
python -m pytest tools/strategy_farm/tests -q
```

Output:

```text
4 failed, 4678 passed, 4 skipped, 2 warnings, 42 subtests passed in 2072.69s (0:34:32)
```

The four failures reproduce in isolation and do not reference EA 36001 or any file changed by this ticket:

- `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`: seven existing findings in the 411xx EA cohort (first at QM5_41134 line 599).
- `test_20030_20032_bindings_follow_deployed_calendar_but_remain_blocked`: live calendar coverage/hash is 2026-08-29 / `d487e699...`, while the fixture expects 2026-08-21 / `42b02ae...`.
- `test_20009_ftmo_news_calendar_is_exact_and_evidence_bound`: the same primary/secondary calendar fixture drift.
- `test_opt_census_ranks_tier6_not_priority`: fixture expects v4 rank 7 while the active manifest returns rank 6.

Isolated rerun: `4 failed in 3.06s`, with the same four failure classes.

### Compile/build-check safety result

Attempted, EA-scoped command:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_36001_nnfx-classic-mcginley-ssl-wae/QM5_36001_nnfx-classic-mcginley-ssl-wae.mq5 -Strict
```

Result: `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`. A subsequent read-only `python tools/strategy_farm/include_mirror.py guard` returned the same failure class. The scoped `build_check.ps1 -EALabel QM5_36001_nnfx-classic-mcginley-ssl-wae -Strict -SkipCompile` path is protected by the same include-mirror guard and therefore could not produce a fresh build report.

Durable compiler summary: `D:/QM/reports/compile/20260824_110824/summary.csv`; compile log path recorded there: `C:/QM/worktrees/rework-slot-5/framework/build/compile/20260824_110824/QM5_36001_nnfx-classic-mcginley-ssl-wae.compile.log`.

Artifact identities after the refusal:

```text
mq5 SHA256 756D0E69B53390536584EC18DFA339BCE1491DAAF2C6BAAE3CED75FE815D6DC2
ex5 SHA256 5B463D9138EE8AD1D31566AC0093B127932965FACFDE75C09F184C683F789A61 (stale; not modified)
```

No compile-queue or backtest work item was created, no factory control was changed, and `C:/QM/mt5/T_Live` was not touched.

## Changed paths

- `framework/EAs/QM5_36001_nnfx-classic-mcginley-ssl-wae/QM5_36001_nnfx-classic-mcginley-ssl-wae.mq5`
- `framework/EAs/QM5_36001_nnfx-classic-mcginley-ssl-wae/SPEC.md`
- `framework/EAs/QM5_36001_nnfx-classic-mcginley-ssl-wae/sets/QM5_36001_nnfx-classic-mcginley-ssl-wae_{AUDUSD,EURUSD,GBPUSD,USDCAD,USDJPY}.DWX_D1_backtest.set`
- `tools/strategy_farm/tests/test_qm5_36001_rework_static.py`
- `docs/ops/evidence/2026-08-23_rework-36001.md`

## Rollback

Revert the ticket commit with `git revert <rework-36001-commit>`. This restores the prior source, SPEC, and five setfiles and removes the regression/evidence files. The `.ex5`, registries, runtime database, factory state, and backtest queue require no rollback because this ticket did not modify them.

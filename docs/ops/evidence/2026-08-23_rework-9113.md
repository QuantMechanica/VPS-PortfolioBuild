# rework-9113 — QM5_9113 alpha-beta velocity review repair

Date executed: 2026-08-24 UTC

Router task: `2b3b4331-a51c-4677-934f-92626144b18f`

EA scope: `framework/EAs/QM5_9113_aa-ab-velocity` only

## Authority and work order

- The router row was read through `agent_router.py list-tasks --agent codex` and
  confirmed `RECYCLE` with the Claude close note at
  `docs/ops/evidence/2026-08-24_review_QM5_9113.md`.
- The task was claimed through the router CLI as `IN_PROGRESS` with verdict
  `rework burn-window slot 12`. The first two attempts met a transient SQLite
  lock; the third returned `"updated": true` and `"state": "IN_PROGRESS"`.
- The approved card is
  `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9113_aa-ab-velocity.md`
  with `g0_status: APPROVED` and R1–R4 PASS.
- Registry preflight found the active EA identity at
  `framework/registry/ea_id_registry.csv:471` and all 13 existing active magic
  rows at `framework/registry/magic_numbers.csv:16760-16772`. No registry or
  resolver file was changed.

## Changes

1. Replaced the moving `warmup+10` recalculation with one recursive state:
   bounded restart reconstruction from the first available completed D1 close,
   then incremental processing of unseen completed bars
   (`framework/EAs/QM5_9113_aa-ab-velocity/QM5_9113_aa-ab-velocity.mq5:182`,
   `:232`, `:271`). The pure alpha-beta step and fixed reference-vector
   self-check are at `:150` and `:167`.
2. Cached the completed-D1 snapshot behind
   `QM_IsNewBar(_Symbol, PERIOD_D1)` and made entry/exit hooks read that cache.
   Friday close, management, and strategy exit now precede news and spread
   entry-only gates (`QM5_9113_aa-ab-velocity.mq5:385-431`).
3. Made the required 20-day median-spread rule fail closed: exactly 20 copied
   completed bars, positive spreads, positive bid/ask ordering, bounded arrays,
   and explicit `ArraySize` proofs (`QM5_9113_aa-ab-velocity.mq5:80-146`).
4. Added the D1 execution-contract declaration while preserving the V5 Friday
   close override (`QM5_9113_aa-ab-velocity.mq5:374`). Risk defaults remain
   `RISK_FIXED=1000`, `RISK_PERCENT=0`; MAE remains wired and magic still comes
   from `QM_FrameworkMagic()`.
5. Regenerated the corrupted SPEC as clean literal UTF-8, documenting recursive
   state, exact spread admission, ordering, timeframe, risk, and source
   (`framework/EAs/QM5_9113_aa-ab-velocity/SPEC.md:9-110`). A byte scan returned
   `[]` for non-whitespace controls.
6. Hardened the shared SPEC validator against ASCII controls
   (`framework/scripts/validate_spec_doc.py:46`, `:61-72`) and added focused
   regression coverage at
   `tools/strategy_farm/tests/test_qm5_9113_rework.py:35-163`.
7. Fail-closed the build identity against the stale predecessor `.ex5`:
   source SHA-256 is
   `e277a28b81647a3af844e082f5bb075121f61dba1cea671ce4314461f23c0119`,
   `ex5_current_for_mq5=false`, and the old binary hash is retained only as
   `predecessor_ex5_sha256`
   (`framework/EAs/QM5_9113_aa-ab-velocity/build_identity.json:2-9`).

## Declared strategy-input use sites

`rg` confirmed every declared strategy input beyond its declaration:

- `strategy_alpha`: lines 187, 217, 261
- `strategy_beta`: lines 188, 217, 261
- `strategy_atr_period`: lines 315, 326
- `strategy_atr_sl_mult`: lines 315, 326
- `strategy_min_warmup_bars`: lines 191, 267, 286
- `strategy_enable_shorts`: line 322

All raw `Bars`, `CopyRates`, and `iBarShift` calls are bounded/new-bar or
restart scoped and carry `perf-allowed`; no `CopyBuffer`, raw indicator handle,
ML, grid, or martingale path was added.

## Validation evidence

Commands were run from `C:/QM/worktrees/rework-slot-12`.

```text
python -m pytest tools/strategy_farm/tests/test_qm5_9113_rework.py -q
.......                                                                  [100%]
7 passed in 0.84s

python -m pytest tools/strategy_farm/tests/test_qm5_9113_rework.py tools/strategy_farm/tests/test_build_gate_hardening.py -q
....................................                                     [100%]
36 passed in 541.21s (0:09:01)

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_9113_aa-ab-velocity
PASS  QM5_9113_aa-ab-velocity
Summary: 1 PASS, 0 FAIL  (of 1)

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_9113_aa-ab-velocity/QM5_9113_aa-ab-velocity.mq5
PASS; files_checked=1; findings=[]; max_news_stale_hours=336

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_9113_aa-ab-velocity
returncode=0; files_scanned=1; failures=[]; warnings=[]

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_9113_aa-ab-velocity --json --fail-on-leak
SINGLE_SYMBOL_OK; n_violations=0

python framework/scripts/skill_build_ea_guard.py --ea-id 9113 --ea-label QM5_9113_aa-ab-velocity
status=ok; ea_registry_row=true; magic_registry_rows=true; ea_dir_exists=true

git diff --check
PASS (line-ending conversion warnings only; no whitespace errors)
```

## Compile/build-check interlock evidence

Both exact scoped commands were attempted; neither reached compilation or the
static build-check body because the live-factory compile guard refused ad-hoc
MetaEditor access while terminal64 processes were active:

```text
pwsh -NoProfile -File framework/scripts/build_check.ps1 -EALabel QM5_9113_aa-ab-velocity -Strict
exit 1: LIVE_FACTORY_AD_HOC_COMPILE_REFUSED

pwsh -NoProfile -File framework/scripts/build_check.ps1 -EALabel QM5_9113_aa-ab-velocity -Strict -SkipCompile
exit 1: LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
```

The prescribed compile-only enqueue was also attempted, but `farmctl` refused
before mutation because state-changing commands must run from the canonical
checkout. No override was set, no compile/backtest work item was created, no
factory process was changed, and the predecessor `.ex5` was not overwritten.
Consequently this resubmission is source/static-test complete but requires a
fresh governed compile during review before the build identity can be marked
current or any Q02 handoff can occur.

## Rollback

Revert the ticket commit with `git revert <rework-9113-commit>`. This restores
the prior EA source, SPEC, build identity, validator, test surface, and this
evidence note. No DB rollback, registry rollback, queue cleanup, factory action,
or T_Live action is required: the only successful runtime mutation was the
router task state transition requested by the ticket.

## Boundaries observed

- No backtest was enqueued or deleted.
- No verdict row or gate criterion/threshold was changed.
- No magic allocation or resolver regeneration was needed.
- Factory state and AutoTrading were not toggled.
- `C:/QM/mt5/T_Live` was not accessed or modified.

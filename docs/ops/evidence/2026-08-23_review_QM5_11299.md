# Review — QM5_11299 lwma144-smma5-fractal-m5-scalp

- Router task: 04867bea-8c6b-4a17-84e3-457a513f9d6d (review_ea, assigned claude)
- Source: gemini/agy build ea624d92-20db-425b-9deb-840b11c83d40 (mandatory Codex/Claude review of agy code)
- EA dir: `framework/EAs/QM5_11299_lwma144-smma5-fractal-m5-scalp/`
- Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11299_lwma144-smma5-fractal-m5-scalp.md`
- Reviewer: Claude, 2026-08-23
- **Verdict: RECYCLE**

## Build / identity evidence
- Build identity: `docs/ops/evidence/ea624d92_QM5_11299_lwma144-smma5-fractal-m5-scalp_build_identity.json` — `build_check_passed: true`.
- Current mq5 sha256 `bc7ae0c2110fb16408bb1de58a46ca812c155d62bc47b386067b0f4ea7f94ca3` == identity `mq5_sha256` == set-file `build_hash`. Build evidence corresponds to the current source hash. (mq5 mtime is later than ex5 but content hash is unchanged.)
- Set files present for both target symbols, generated via governed path (QM5 Set File header, `set_version=s20260822-001`, `risk_mode: FIXED`, `RISK_FIXED=1000`, `RISK_PERCENT=0`, `environment: backtest`, `magic_slot: 0`). No raw MQ5 promotion.

## Framework conformity — PASS
- `#include <QM/QM_Common.mqh>` include chain present.
- Risk: `RISK_PERCENT=0.0` / `RISK_FIXED=1000.0` defaults; set files FIXED/1000/0 — correct for backtest (HR).
- Magic via `QM_FrameworkMagic()` + `qm_magic_slot_offset` (MagicResolver-driven); no hardcoded magic.
- News filter wired in `OnTick` (`QM_NewsAllowsTrade2` / `QM_NewsAllowsTrade`), off by default per card P2.
- MAE hook `QM_FrameworkTrackOpenPositionMae()` present; kill-switch, Friday-close, equity stream, OnTester/QM_DefaultObjective all wired.
- No ML libraries; no hardcoded commission/swap/DST values.

## Card faithfulness — PASS (at defaults)
- SMMA(5) × LWMA(144) cross with 10-pip proximity filter to LWMA, session + spread filters, fractal SL with ATR(14)×1.0 fallback — all match card mechanics. Long/short cross logic and proximity check are card-faithful.

## Defect (dead input) — RECYCLE
- **`strategy_tp_atr_mult` is a dead input.** Declared at `QM5_11299_lwma144-smma5-fractal-m5-scalp.mq5:42` (`input double strategy_tp_atr_mult = 2.0;`) and grepped nowhere else in the file — its only occurrence is the declaration.
- The take-profit is instead computed with a **hardcoded literal `2.0`**:
  - line 194: `tp = entry + 2.0 * sl_dist;` (long)
  - line 203: `tp = entry - 2.0 * sl_dist;` (short)
- Consequence: at default settings behavior is correct (2:1 R:R == card "TP = 2× risk"), so a Q02 run at defaults would be honest — but the input is inert and silently misleading. Any future Q14 optimization or set-file override of `strategy_tp_atr_mult` would have **no effect**, corrupting the tuning contract. This is the QM unwired-input defect class (feedback: `feedback_ea_review_unwired_input_check_2026-08-10.md`, QM5_1355), which the SOP maps to RECYCLE.

## Actionable fix
- Wire the TP to the declared input: replace the literal `2.0` at lines 194 and 203 with `strategy_tp_atr_mult` (i.e. `tp = entry + strategy_tp_atr_mult * sl_dist;` and the short mirror), keeping the R-multiple semantics the card specifies. Rebuild via the governed `build_check -EALabel` path and re-emit the build identity; no threshold/seed/window change required.

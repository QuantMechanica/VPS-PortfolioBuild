# QM5_38002 Codex review evidence — 2026-08-18

- Review task: `0a687cb4-31fa-418f-8866-5d12c3c45b75`
- Source build task: `f0823eb2-3859-45a3-9fa4-a83cc44207ef` (Gemini)
- EA: `QM5_38002_codetrading-macd-ema-trend-pullback`
- Disposition: `FAIL_CODE_REVIEW`; remain in `REVIEW`

## Verification

- Repository branch: `agents/board-advisor`
- Mechanical pre-screen: PASS (artifacts, freshness, three active magic rows, setfiles, fixed-risk mode, input wiring)
- `validate_build_guardrails.py`: PASS, four files checked, no findings, maximum news staleness 336 hours
- Fresh compile through `compile_ea.py --force`: PASS, 0 errors, 0 warnings
- Backtest sets: `RISK_FIXED=1000`, `RISK_PERCENT=0`, and symbol slots match the registry
- Card-to-code review: FAIL

## Blocking findings

1. `Strategy_NoTradeFilter()` rejects when this magic already has one open position (MQ5 line 114), while `OnTick()` returns on that filter at line 244 before calling `Strategy_ManageOpenPosition()` at line 247. Consequently the approved ATR trailing lifecycle cannot execute while the position exists. Any later strategy exit would be unreachable for the same reason.
2. `OnTick()` evaluates the spread filter before the new-bar state refresh. At startup `g_last_atr` is zero, so line 244 cannot enforce the approved `spread <= 1.8 * ATR` rule; the ATR is populated at line 274 and entry follows without re-running the no-trade filter. This permits the first eligible signal to bypass the required spread gate.
3. The approved card requires the stop below/above the recent five-bar swing plus/minus two pips (card line 98). The implementation instead uses `1.5 * ATR` (MQ5 lines 43 and 151-157). This changes the approved strategy mechanic and its position sizing basis.

## Required repair

- Separate entry admission from open-position management so management and exits remain reachable while a position is open.
- Refresh closed-bar state before applying ATR-dependent admission, and recheck current spread immediately before opening.
- Implement the card-of-record five-bar swing stop (or obtain an OWNER-approved card change before rebuilding).
- Recompile and regenerate bound evidence; a later Codex review is still mandatory.

No pipeline verdict is inferred. No Q phase was started, no terminal was launched, and AutoTrading/T_Live were not enabled.

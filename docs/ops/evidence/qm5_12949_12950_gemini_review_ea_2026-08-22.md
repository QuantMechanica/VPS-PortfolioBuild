# Claude review_ea — QM5_12949 & QM5_12950 (Gemini-built)

**Router task IDs:** `67612e6d-46c9-4a6e-a913-285d36acc911` (12949), `b7c33c62-f991-41c3-9cb5-79d55489aaab` (12950)
**Reviewer:** Claude (headless orchestration cycle)
**Date:** 2026-08-22
**Source build artifacts:** `artifacts/qm5_12949_build_result.json`, `artifacts/qm5_12950_build_result.json`
**Source cards:** `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12949_mql5-rvi-ma-card.md`,
`D:/QM/strategy_farm/artifacts/cards_approved/QM5_12950_mql5-ad-price-card.md`

## Scope

Both are `review_ea` tasks routed to claude with
`reason: codex_review_required_for_gemini_code` — per CLAUDE.md hard rules,
Gemini-authored code requires mandatory Codex review before acceptance. This
review is the Claude-side correctness pass; it does **not** substitute for
that Codex review, and this task is intentionally left in `REVIEW` state, not
advanced to `APPROVED`/`PIPELINE`.

## QM5_12949 — MQL5 RVI MA Crossover

- `mq5` SHA256 verified locally via `certutil`: matches
  `a048e123f97d9f741c2bc9384ce05b17fce8335ed2422272a537baf0e898c744` in the
  build result.
- Magic base `129490000` = `12949 * 10000 + 0` — formula-correct; registry rows
  `framework/registry/magic_numbers.csv:17651-17653` cover the 3 registered
  symbols (EURUSD/GBPUSD/XAUUSD.DWX), all `active`.
- Entry/exit logic cross-checked line-by-line against the approved card:
  - Long: `close1 > ma100 && rvi_main1 > rvi_sig1` — matches card.
  - Short: `close1 < ma100 && rvi_main1 < rvi_sig1` — matches card.
  - Exit: RVI cross back or MA break — matches card; 48-bar failsafe matches.
  - Stop `entry -/+ ATR(14)*1.6`, TP `2.0R`, dead-range filter
    `ATR14 >= 0.5*ATR100` — all match card verbatim.
  - `req.symbol_slot = qm_magic_slot_offset` wired correctly (host-slot-magic
    conflation class from 2026-08-16 checked — not present here).
- All 9 `strategy_*` inputs are read at a use site in `Strategy_EntrySignal`/
  `Strategy_ExitSignal`/`Strategy_NoTradeFilter` — no unwired inputs
  (per `feedback_ea_review_unwired_input_check`).
- `qm_news_stale_max_hours = 336` (guardrail ceiling, not exceeded).
  Backtest set file: `RISK_FIXED=1000`, `RISK_PERCENT=0` — correct per hard rule.
- No ML, no grid/martingale, one-position-per-magic — mechanical, in scope
  for the Edge Lab charter (H1 swing horizon, ATR stop, fixed R multiple).
- `expected_trades_per_year_per_symbol: 80` — comfortably above the Q02 >=5
  trades/yr economic floor.

**Verdict: no correctness defects found.** Faithful implementation of the
approved card, framework wiring standard and consistent with sibling EAs.

## QM5_12950 — MQL5 Accumulation/Distribution Price Confirmation

- `mq5` SHA256 verified locally: matches
  `6a8eddd88e82109415a6e7b886fbab232e4e05429f9a57f74f36dd0d8898d113`.
- Magic base `129500000` = `12950 * 10000 + 0` — correct; registry rows
  `framework/registry/magic_numbers.csv:17654-17656`, 3 symbols, all `active`.
- Entry/exit cross-checked against the approved card:
  - Long: `close1 > ema100 && high1 > high2 && ad1 > ad2` — matches card.
  - Short: `close1 < ema100 && low1 < low2 && ad1 < ad2` — matches card.
  - Exit: `(ad1 < ad2 && ad2 < ad3)` (long) / mirrored (short) correctly
    encodes "AD declines/rises for two consecutive closed bars" — bar-index
    ordering (1=most recent closed, 3=oldest) checked, direction is right.
    36-bar failsafe matches card.
  - Stop/TP/dead-range filter identical formula family to 12949, matches card.
- All 8 `strategy_*` inputs wired and used — no dead inputs.
- Same news/risk/guardrail posture as 12949: `qm_news_stale_max_hours=336`,
  `RISK_FIXED=1000`/`RISK_PERCENT=0` in the backtest set file.
- Mechanical, no ML/grid/martingale, in-charter (H1 swing, ATR stop, fixed R).
- `expected_trades_per_year_per_symbol: 75` — above Q02 floor.

**Verdict: no correctness defects found.** Faithful implementation of the
approved card.

## Disposition

Both tasks are being moved to `REVIEW` with a PASS-leaning verdict from the
Claude side. Per hard rules this does not authorize `APPROVED`/`PIPELINE` —
Codex review remains mandatory before either EA advances. No close-review
call was made; both tasks stay in `REVIEW` for Codex to pick up.

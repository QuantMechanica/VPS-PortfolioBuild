# rework-36006 — QM5_36006 review-fix evidence

## Authority and scope

- Router task: `599e1cb1-ab22-4d53-88f1-c39cd9e51dcb` (`RECYCLE` work order claimed as `IN_PROGRESS`).
- Review work order: `docs/ops/evidence/599e1cb1_qm5_36006_gemini_build_codex_review_2026-08-18.md`.
- Approved card: `strategy-seeds/cards/approved/QM5_36006_nnfx-halftrend-jurik-coppock-engine.md` (`g0_status: APPROVED`).
- Registry preflight: `framework/registry/ea_id_registry.csv:4461`; active magic rows at `framework/registry/magic_numbers.csv:17444-17446`. The registries and resolver were not changed.
- Scope remained one EA. No backtest was enqueued or deleted; factory state, gate criteria, verdict rows, and `C:/QM/mt5/T_Live` were not touched.

## What changed

1. Replaced repeated one-bar history walks with one closed-D1 signal refresh. `Strategy_RefreshClosedBarSignals` performs a bounded `CopyRates` load (`required <= 256`), proves the copied/array size before indexing, and calculates the card's HalfTrend `EMA(amplitude) +/- 2*ATR(100)`, open Jurik phase/power recurrence, Coppock WMA, and CMF in one cache (`framework/EAs/QM5_36006_nnfx-halftrend-jurik-coppock-engine/QM5_36006_nnfx-halftrend-jurik-coppock-engine.mq5:148-266`). The only raw series call is the perf-annotated, new-bar-gated call at line 163.
2. Made TP1 a real 50% partial close with explicit one-shot state and retry throttling. A successful partial sets `g_tp1_done`; subsequent ticks retry only the Entry +/- 1 pip protective SL, leaving the HalfTrend runner intact (`...mq5:344-449`, partial close at line 414 and BE move at line 439). Entry requests carry no broker TP (`...mq5:293-341`).
3. Restored the complete loss/time/execution contract: account realized-loss entry halt, UTC-normalized 23:55-00:05 blackout, 2.5% framework daily hard stop, direct 5% initial-equity drawdown trip, 0.5% risk cap, D1 execution contract, and card-declared 3-tick market deviation (`...mq5:119-146`, `:271-290`, `:487-536`).
4. Kept management reachable on every tick, then refreshes and evaluates exits on the closed D1 bar before all news/spread/rollover entry-only gates (`...mq5:543-585`). The MAE hook is the first lifecycle call at line 545.
5. Bounded all declared strategy inputs to their card/SPEC ranges and wired every framework and strategy input. The mechanical use-site audit is enforced by `tools/strategy_farm/tests/test_qm5_36006_review_rework.py:149`; all three backtest setfiles carry `RISK_FIXED=1000`, `RISK_PERCENT=0`, every `strategy_*` input, and source hash `cf6c08d21b17e86c46ff97dfcb8987dd867c3aa80c6076fad1dbcb733943ee50`.
6. Updated `SPEC.md` for bounded caching, D1 gating, slippage wiring, and the real `EA_RISK_SIZER_UNCONFIGURED` diagnostic (`framework/EAs/QM5_36006_nnfx-halftrend-jurik-coppock-engine/SPEC.md:35`, `:61`, `:89`, `:125`).

## Unit and static verification

### Focused/relevant pytest

Command:

```text
python -m pytest tools/strategy_farm/tests/test_qm5_36006_review_rework.py tools/strategy_farm/tests/test_killswitch_state_lifecycle_static.py tools/strategy_farm/tests/test_setfile_canonicalization.py -q
```

Result:

```text
................                                                         [100%]
16 passed in 13.15s
```

The five EA-specific tests cover approved-card/registry/resolver binding, exact indicator mechanics and buffer bounds, TP1/BE/runner lifecycle, management/exit/risk/clock/MAE ordering, every-input use sites, setfile universe, fixed-risk mode, source hashes, and no ML (`tools/strategy_farm/tests/test_qm5_36006_review_rework.py:49-198`).

### Build-gate hardening (EA-scoped)

Command:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_36006_nnfx-halftrend-jurik-coppock-engine
```

Result summary:

```json
{"schema":"qm.build-gate-hardening/v1","files_scanned":1,"failures":0,"warnings":0,"ea_failures":0,"ea_warnings":0}
```

Additional scoped validators:

```text
skill_build_ea_guard: status=ok (EA row, magic rows, directory all true)
validate_spec_doc: PASS (1 PASS, 0 FAIL)
validate_build_guardrails: PASS (4 files, zero findings)
validate_symbol_scope: SINGLE_SYMBOL_OK (zero violations)
```

### Broader touched-module regression

Command included `test_build_gate_hardening.py` plus the focused, kill-switch, and setfile tests. Result:

```text
1 failed, 44 passed in 449.43s
```

The only failure is outside this ticket: `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven existing findings beginning at `framework/EAs/QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`. The EA-scoped hardening result above is clean; no `QM5_411*` source was changed.

## Compile/build-check evidence and remaining review risk

The required direct scoped command was attempted:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_36006_nnfx-halftrend-jurik-coppock-engine -SkipCompile
```

It failed before static checks with the fail-closed interlock `BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED`; active factory terminals were `T1,T3,T4,T5,T6,T8,T9`. No terminal was stopped and factory state was not changed.

The prescribed governed alternative was then attempted for this exact label only. It enqueued nothing and returned `BOUND_SETFILE_HASH_EXISTS` because all three existing sets are already source-hash-bound. Therefore this worktree cannot lawfully create a fresh `.ex5` while the factory remains active: direct compile is forbidden, and the compile worker refuses an existing bound-set build without a hardcoded source-repair authority. No guard was bypassed, no fake compile identity was supplied, and no binary claim is made.

This is the only open review risk. Source/static/unit evidence is PASS; fresh MetaEditor `0 errors / 0 warnings` and strict `build_check.result=PASS` remain to be produced by an authorized compile window or compile-source-repair authority. The EA must not enter pipeline execution on this evidence alone.

## Rollback

Revert the ticket commit with `git revert <rework-36006-commit>`. This restores the previous MQ5, SPEC, and three setfiles and removes the focused unit test/evidence note. No database rollback is required beyond normal router-state handling: neither compile nor backtest work items were created, and no registry/verdict row was changed.

# Review — QM5_9718 Bandy Cumulative RSI(2) (Index MR, D1)

- Router task: f53f8bcf-8bef-4277-8dec-405daa87e526 (review_ea, state REVIEW, assigned codex)
- Source: build_ea cadbb75f-8566-4326-92c6-912cae4b0da6 (agy/gemini lane)
- Source verdict: "REVIEW: QM5_9718 Bandy Cumulative RSI(2) MQL5 draft complete, guardrails PASS, 13 sets backfilled, ex5 built; awaiting Codex review."
- Reviewer: Claude review lane. Read-only DB. Evidence over claims.
- **Verdict: APPROVED (clean for Q02).**

## Artifact integrity
- mq5 sha256 = a1f3e7c8c89e9493ee90575f38c32825127d01afc6f5c4a126a68f12014afacb — matches build artifact and current working tree (sha256sum of framework/EAs/QM5_9718_bandy-cumulative-rsi2-mr-index/QM5_9718_bandy-cumulative-rsi2-mr-index.mq5).
- build_check: PASS, exit_code 0, ex5 present (COMPILE_OK for the current source hash).

## Card-faithful mechanism (card: docs/strategy_card.md)
- Entry: crsi2 = RSI(2)[last]+RSI(2)[prev] <= 15.0 AND close > SMA(200), long only, next-bar — mq5:79-101. Uses completed bars (shift 1/2), correctly avoids incomplete-bar lookahead (card "skip on incomplete daily bar").
- Exit: RSI(2) >= 65 — Strategy_ExitSignal mq5:143-151. Time stop 6 days — Strategy_ManageOpenPosition mq5:118-140. Catastrophic 2.5*ATR(14) stop — req.sl via QM_StopATR mq5:104. Long-only, one position per magic (QM_TM_OpenPositionCount guard mq5:83).

## Dead-input check (every strategy input grepped for use-sites)
All 9 strategy inputs have >=1 use-site beyond declaration:
strategy_rsi_period(4), strategy_crsi_entry_thresh(2), strategy_rsi_exit_thresh(2),
strategy_sma_period(2), strategy_time_stop_days(2), strategy_atr_period(3),
strategy_sl_atr_mult(2), strategy_spread_max_atr(2), strategy_warmup_bars(4). No dead inputs.

## Framework conformity
- #include <QM/QM_Common.mqh>; OnInit -> QM_FrameworkInit; OnDeinit -> QM_FrameworkShutdown.
- MAE hook QM_FrameworkTrackOpenPositionMae() first in OnTick (mq5:175).
- RISK_FIXED=1000 / RISK_PERCENT=0 (backtest defaults). Magic via QM_FrameworkMagic()/MagicResolver.
- News filter wired (QM_NewsAllowsTrade2 / legacy fallback). Bounded indicator helpers only (QM_RSI/QM_SMA/QM_ATR); no raw CopyBuffer/series, no ML, no invented commission/swap/DST.
- Registry: ea_id_registry 9718 active; magic_numbers 13 rows active, formula 9718*10000+slot (97180000..12) correct; MagicResolver regenerated (9718 present). Resolver row count matches.
- Setfiles: 13, governed-generator provenance (build_hash, set_version s20260822-001, environment=backtest, risk_mode=FIXED), strategy params match input defaults/card. 13 setfiles == 13 magic rows == 13 symbols.

## Non-blocking observations (not defects)
1. Symbol universe (13 incl. FX + XAUUSD) is broader than the card's index-only Target
   Symbols (SP500/NDX/WS30). Standard factory backfill; Q02 economics filter handles
   non-viable pairs; candidate-pool/card-universe definition is OWNER ROT, not redefined here.
2. Setfile header notes card_defaults_status=none_found — generator's cards_approved path
   holds no QM5_9718 md (card lives in the EA docs/). Params were backfilled from the EA's
   own input defaults, which are behaviour-identical and match the card mechanics.

No dead inputs, no mechanism divergence, no framework violation, COMPILE_OK. Clean for Q02.

# Independent build review — QM5_11539 (carter-t-h1-ema5-10-rsi10-median)

Reviewer: Claude (independent re-verification of Codex 5ea0928f)
Date: 2026-08-21
Verdict: PASS — cleared for pipeline entry (Q02+). No live authorization implied.

## Scope
EA: QM5_11539 "carter-t-h1-ema5-10-rsi10-median" — Thomas Carter 1H System #13.
Card: EMA(5)/EMA(10) cross + RSI(10, PRICE_MEDIAN) 50-cross, synchronized ±2 bars,
SL 30 / TP 50 pips, no Friday entry, spread cap 15p. Symbols EURUSD.DWX, GBPUSD.DWX (H1).

## Checklist (each verified against .mq5 source)

1. Card fidelity — PASS.
   - EMA cross via QM_Sig_MA_Cross (QM_EMA = iMA MODE_EMA/PRICE_CLOSE), EA:73-76.
   - RSI(10,PRICE_MEDIAN) 50-cross via QM_RSI(...,PRICE_MEDIAN), EA:81-90.
   - Synchronized ±2-bar window, later event at shift 1, EA:96-119.
   - SL 30 / TP 50 via strategy_sl_pips/strategy_tp_pips, EA:180-181.
   - One-position-per-magic EA:136-145; Friday block EA:149-155; spread cap EA:159-163.
2. All inputs wired — PASS. 24/24 inputs have use-sites (framework inputs OnInit:215-230
   & OnTick:291-294; strategy inputs :75,:83-88,:98/104/113,:149,:161,:180-181).
3. Magic/slot binding — PASS. req.symbol_slot=qm_magic_slot_offset (EA:183), not hardcoded.
   magic=ea_id*10000+slot (QM_Common.mqh:225,657-663). Registry: slot0→EURUSD→115390000,
   slot1→GBPUSD→115390001. Setfiles set offset 0/1 respectively.
4. Backtest set files — PASS. Both sets RISK_FIXED=1000, RISK_PERCENT=0 (never reversed).
5. Loss limits — PASS. Card defines only per-trade SL30/TP50 (wired, not defaulted). No
   strategy max-daily/DD input; framework KillSwitchInit(...,3.0,0.0,...) at QM_Common.mqh:298
   is the Q13 burn-in default and overrides no card value.
6. Broker-time handling — PASS. Only time logic is Friday block on TimeCurrent() broker time
   (EA:147-154); friday_close_hour_broker=21 broker. No raw-local/DST-fragile session logic.
7. News stale guard — PASS. qm_news_stale_max_hours=336 (EA:37), at ceiling, not exceeded.

## Build check
- compile_one.ps1 -EAPath QM5_11539...mq5 → PASS, errors=0, warnings=0, ex5 emitted.
  Log: framework/build/compile/20260821_084017/...compile.log
- Manual forbidden-construct scan: clean (no #import/.dll/ML; only QM_Common.mqh include).
- NOTE: full build_check.ps1 -EALabel does not complete inside a git worktree
  (BUILD_CHECK_SETFILE_NONE_FOUND + empty-array bind in Invoke-ForbiddenScan) — a
  worktree path-resolution artifact, not an EA defect. Run the aggregate harness from
  canonical C:\QM\repo; the compile gate and manual gate checks all pass here.

## Agreement with Codex 5ea0928f
All Codex claims reproduce independently (card fidelity, synchronized cross, fixed SL/TP,
slot binding, risk modes, stale-news 336h, strict compile 0/0). Added flag: build_check.ps1
harness must be run from the canonical repo, not a worktree.

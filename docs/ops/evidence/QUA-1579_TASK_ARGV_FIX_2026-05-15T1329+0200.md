# QUA-1579 Task Argument Parsing Fix

## Symptom
- Scheduled task QM_GateEvaluator_5min repeatedly reported last_task_result=2.
- Reproduction via registered action showed argparse error: missing required --sqlite.

## Root Cause
- Task action argument string was over-quoted in egister_qm_gate_evaluator_task.ps1, causing Python arg parsing to fail under Start-Process / Scheduler invocation semantics.

## Fix
- Updated ramework/scripts/register_qm_gate_evaluator_task.ps1 to emit parser-safe arguments without unnecessary quotes.

## Validation
1. Re-registered task with updated helper.
2. Ran task action directly from registered xecute + arguments:
   - output JSON from gate_evaluator.py
   - xit_code=0
3. Triggered scheduled task once and re-checked info:
   - state=Ready
   - last_task_result=0

## Result
- Scheduler execution path is restored and healthy.

# QM5_12832 DAX Session Rework Review

Task: `6c180136-1df9-49e3-b55a-4a07fc27ab71`  
Date: 2026-07-01  
EA: `framework/EAs/QM5_12832_dax-range-breakout`

## Verdict

PASS for review. The current repo already contains the successful DAX-session rework:

- Defaults are DAX opening-range: `range_start_hour=10`, `range_end_hour=11`, `exit_hour=17`.
- Setfile matches those defaults for `GDAXI.DWX` M15.
- The earlier overnight/pre-open default `22 -> 08` is documented in `SPEC.md` as tried and rejected: it yielded 0 trades on GDAXI.DWX 2024 because overnight GDAXI bars are sparse.
- Current smoke evidence passes the task threshold (`>=5` trades): both deterministic runs produced `total_trades=9`.

I did not revert the EA back to `22 -> 08` because the local task/build evidence shows that setting is the known-zero-trade configuration. The passing DAX-specific session is the 10:00-11:00 broker opening range, consistent with local GDAXI session evidence in prior cards (`QM5_10709`: GDAXI 09:00 broker OR pass; `QM5_12832 SPEC`: 10:00 broker = 09:00 CET) and the latest successful smoke.

## Evidence

Build result:

```text
D:\QM\strategy_farm\artifacts\builds\940f4709-fa6f-4470-8487-f5d297f78ca2.json
smoke_result=passed
smoke_report_path=D:\QM\reports\smoke\QM5_12832\20260701_102744\summary.json
```

Smoke summary:

```text
result=PASS
reason_classes=["OK"]
symbol=GDAXI.DWX
period=M15
year=2024
deterministic=true
min_trades_required=5
run_01 total_trades=9, PF=0.31, net_profit=-2023.70
run_02 total_trades=9, PF=0.31, net_profit=-2023.70
```

Focused verification in this cycle:

```text
powershell -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_12832_dax-range-breakout -Strict
compile_one.result=PASS
compile_one.errors=0
compile_one.warnings=0
compile_one.log=C:\QM\repo\framework\build\compile\20260701_120059\QM5_12832_dax-range-breakout.compile.log
```

```text
python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_12832_dax-range-breakout
verdict=PASS
findings=[]
max_news_stale_hours=336
```

Guardrail notes:

- Backtest setfile uses `RISK_FIXED=1000`, `RISK_PERCENT=0`.
- No news-staleness bypass; `qm_news_stale_max_hours=336`.
- No ML/grid/martingale; single-position range breakout.

## Follow-up

This should move to normal review / Q02 routing. Profitability is not established by smoke: the 2024 smoke PF is `0.31`; Q04 net-of-cost remains the actual decision gate.

# Q09/Q10 Pipeline-Tail Test Coverage Evidence

Task: `f7ee47b5-e878-4400-97fd-152864ada987`
Date: 2026-06-03
Worktree: `C:/QM/repo`
Base: `agents/board-advisor` at `055cf4da9`

## Scope

Added focused unit coverage for the pipeline-tail gaps:

- `tools/strategy_farm/tests/test_portfolio_q08_contribution.py`
  - `monthly_returns()` aggregation by month.
  - `equity_curve()` chronological cumulative equity.
  - `evaluate_q08_soft_rescue()` NEED_MORE_DATA when trade count is below 30.
  - Q08 regime catastrophe forcing `FAIL_PORTFOLIO` with reason `q08_regime_catastrophe`.
  - Delegation to `portfolio_admission.evaluate_candidate()` for `PASS_PORTFOLIO`.
  - Delegation to `portfolio_admission.evaluate_candidate()` for `FAIL_PORTFOLIO`.

- `framework/scripts/tests/test_q10_confirmation.py`
  - `run_confirmation()` verdict `INVALID` for missing PF/DD in summary.
  - `run_confirmation()` verdict `FAIL` for PF below floor.
  - `run_confirmation()` verdict `FAIL` for DD above ceiling.
  - `run_confirmation()` verdict `PASS` for PF/DD inside bounds.
  - `gen_q10_baseline.extract_per_trade_profits()` on synthetic MT5 HTML.
  - `gen_q10_baseline.write_baseline()` schema using a patched temporary baseline dir, not `D:/QM/data/baselines`.

No production behavior was changed.

## Verification

`python -m py_compile tools/strategy_farm/portfolio/portfolio_q08_contribution.py framework/scripts/q10_confirmation.py framework/scripts/gen_q10_baseline.py tools/strategy_farm/tests/test_portfolio_q08_contribution.py framework/scripts/tests/test_q10_confirmation.py`

Result: PASS

`python -m unittest tools.strategy_farm.tests.test_portfolio_q08_contribution framework.scripts.tests.test_q10_confirmation`

Result: PASS

Output:

```text
...........
----------------------------------------------------------------------
Ran 11 tests in 0.148s

OK
```

Note: `python -m pytest ... -q` could not be used in this environment because `pytest` is not installed (`No module named pytest`). The same focused tests passed via stdlib `unittest`.

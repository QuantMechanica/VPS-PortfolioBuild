# rework-36002 review-fix evidence

- Router task: `b7868b8c-cd0e-4f22-b764-fe8a7d53408a`
- EA: `QM5_36002_nnfx-kijunsen-absolute-strength-damiani`
- Approved card: `strategy-seeds/cards/approved/QM5_36002_nnfx-kijunsen-absolute-strength-damiani.md`
- Review work order: `docs/ops/evidence/b7868b8c_qm5_36002_gemini_build_codex_review_2026-08-18.md`
- Source SHA-256 after rework: `8f76e0b15ab8324015a7346a35a92d6067d33084d3755d744490b73954225cc3`

## What changed

The branch already contained the first review hardening pass (partial TP,
exact Damiani comparison, UTC rollover, card loss limits, performance
annotations, and management-before-entry-filter ordering). This ticket audited
those fixes and closed the remaining TP1 lifecycle defect:

- `framework/EAs/QM5_36002_nnfx-kijunsen-absolute-strength-damiani/QM5_36002_nnfx-kijunsen-absolute-strength-damiani.mq5:123`
  validates that broker volume normalization can close exactly 50% while
  leaving an equal, valid runner.
- The same source at line 139 preflights risk-sized entry volume; unsplittable
  entries fail closed instead of silently approximating the approved 50% rule.
- Lines 156-204 reconstruct TP1 completion from outgoing deals bound to
  `POSITION_IDENTIFIER`, so a restart cannot repeat the partial close.
- Lines 291-353 keep broker TP at zero and require the split-volume preflight
  for both long and short entries.
- Lines 365-440 execute TP1 once, persist the in-memory completion state, and
  retry break-even protection independently if an earlier SL modification
  failed.
- Lines 574-578 invalidate the cache on trade transactions so deal history is
  re-read after fills.
- `framework/EAs/QM5_36002_nnfx-kijunsen-absolute-strength-damiani/SPEC.md:40`
  documents exact-volume, restart-safe TP1 behavior and its fail-closed lot-step
  condition.
- All four backtest sets bind the final source hash at line 13, retain
  `RISK_FIXED=1000` / `RISK_PERCENT=0` at lines 19-20, and point to the approved
  card at line 23.
- `tools/strategy_farm/tests/test_qm5_36002_review_rework.py:45` adds five
  regression tests covering the six review findings, TP1 restart safety, every
  declared input use-site, fixed-risk setfiles, magic allocation, the
  `QM_Common` chain, MAE, performance annotations, and forbidden ML/raw trade
  surfaces.

## Card and framework conformance evidence

- Damiani is exactly `vol > anti`: EA source lines 249-258; the unauthorized
  `vol >= 1.0` disjunct is absent.
- Rollover converts broker time with `QM_BrokerToUTC`: lines 265-275.
- Realized-loss entry halt is account-wide and parameterized at 2.0%; the
  framework kill switch receives 2.5% daily equity drawdown, 5.0% total
  drawdown, and the risk cap: lines 101-117 and 484-511.
- Management and Kijun exit run before news, rollover, spread, and other entry
  gates; MAE is called directly: lines 525-567.
- Raw `iClose`/`iHighest`/`iLowest` calls are new-bar bounded and carry
  `perf-allowed` annotations; there are no EA-local dynamic indicator buffers or
  `CopyBuffer` calls.
- Existing registry rows were not changed:
  `framework/registry/ea_id_registry.csv:4457` and
  `framework/registry/magic_numbers.csv:17429-17432` provide active slots 0-3.
  The hardening checker confirmed all four card symbols and registry rows with
  zero failures and zero warnings.

## Validation output

1. `python -m pytest tools/strategy_farm/tests/test_qm5_36002_review_rework.py -q`

   Result: `5 passed in 1.24s`.

2. `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_36002_nnfx-kijunsen-absolute-strength-damiani`

   Result: `failures=[]`, `warnings=[]`; four authorized symbols observed,
   D2 loss limits matched 2.0/2.5/5.0, D5 UTC converter present, D7 MAE PASS,
   D10 buffer bounds PASS, D17 universe PASS.

3. `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_36002_nnfx-kijunsen-absolute-strength-damiani`

   Result: `verdict=PASS`, five files checked, zero findings, news ceiling 336.

4. `python framework/scripts/validate_spec_doc.py framework/EAs/QM5_36002_nnfx-kijunsen-absolute-strength-damiani`

   Result: `1 PASS, 0 FAIL`.

5. `python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_36002_nnfx-kijunsen-absolute-strength-damiani --json --fail-on-leak`

   Result: `SINGLE_SYMBOL_OK`, zero violations.

6. `python -m pytest tools/strategy_farm/tests/test_qm5_36002_review_rework.py tools/strategy_farm/tests/test_build_gate_hardening.py -q`

   Result: EA 36002 tests passed; overall `33 passed, 1 failed in 529.06s`.
   The sole failure is the repository-wide pre-existing
   `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` cohort check on
   other EAs beginning with `QM5_41134`; it does not inspect EA 36002 and was
   not changed under this ticket.

7. Scoped authoritative command attempted twice:

   `powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_36002_nnfx-kijunsen-absolute-strength-damiani -SkipCompile`

   Result: fail-closed before validation with
   `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because `terminal64` processes were
   alive. The prescribed alternative is a governed compile work item. This
   ticket did not enqueue one because no compile/queue mutation was authorized,
   and it did not stop terminals, toggle the factory, or touch `T_Live`.
   Consequently no fresh `.ex5` or compile claim is made here.

8. `git diff --check -- <EA 36002 paths> tools/strategy_farm/tests/test_qm5_36002_review_rework.py`

   Result: PASS (only Git's CRLF conversion notices).

## Scope and safety

- No backtest was enqueued, run, deleted, or reclassified.
- No verdict row or gate threshold/criterion was changed.
- No factory toggle, terminal stop, live deploy, or `T_Live` write occurred.
- No registry allocation or resolver regeneration was needed; all required
  deterministic rows already existed.
- Unrelated dirty-worktree files for EAs 37007 and 38008 were preserved and are
  excluded from this ticket's explicit commit pathspecs.

## Risks / follow-up

- A fresh MetaEditor compile and the full scoped `build_check.ps1` remain
  unproven until a governed compile slot is available or terminals are
  quiescent under separate authority. Static build hardening, SPEC, guardrail,
  symbol, input, setfile, and pytest checks all pass for EA 36002.
- Exact 50/50 TP1 intentionally rejects risk-sized volumes that cannot be split
  evenly at the symbol's lot step. This is fail-closed card fidelity and may
  reduce entries at very small configured risk.

## Rollback

Revert the resulting `rework(36002): make TP1 exact and restart-safe` commit
with `git revert <commit-sha>`. No registry, router-history, backtest, factory,
or live rollback is required; the router state can be reassigned independently
if review requests further work.

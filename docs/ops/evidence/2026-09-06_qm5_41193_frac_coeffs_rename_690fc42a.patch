diff --git a/framework/EAs/QM5_41193_xtixng-fracd-rv/QM5_41193_xtixng-fracd-rv.mq5 b/framework/EAs/QM5_41193_xtixng-fracd-rv/QM5_41193_xtixng-fracd-rv.mq5
index f9617cdc46..21242e18cb 100644
--- a/framework/EAs/QM5_41193_xtixng-fracd-rv/QM5_41193_xtixng-fracd-rv.mq5
+++ b/framework/EAs/QM5_41193_xtixng-fracd-rv/QM5_41193_xtixng-fracd-rv.mq5
@@ -743,7 +743,7 @@ bool Strategy_LoadFractionalSignal(
    datetime newest_first_times[316];
    double chronological_ratios[316];
    datetime chronological_times[316];
-   double weights[64];
+   double frac_coeffs[64];
    double outputs[253];
    ArraySetAsSeries(xti_bars, true);
    ArraySetAsSeries(xng_bars, true);
@@ -755,7 +755,7 @@ bool Strategy_LoadFractionalSignal(
       ArraySize(newest_first_times) != strategy_pair_count_d1 ||
       ArraySize(chronological_ratios) != strategy_pair_count_d1 ||
       ArraySize(chronological_times) != strategy_pair_count_d1 ||
-      ArraySize(weights) != strategy_frac_lags ||
+      ArraySize(frac_coeffs) != strategy_frac_lags ||
       ArraySize(outputs) != expected_outputs)
       return false;
 
@@ -837,20 +837,20 @@ bool Strategy_LoadFractionalSignal(
          return false;
      }
 
-   weights[0] = 1.0;
+   frac_coeffs[0] = 1.0;
    weight_count = 1;
    for(int lag = 1; lag < strategy_frac_lags; ++lag)
      {
-      weights[lag] =
-         weights[lag - 1] *
+      frac_coeffs[lag] =
+         frac_coeffs[lag - 1] *
          ((double)lag - 1.0 - strategy_frac_order) / (double)lag;
-      if(!MathIsValidNumber(weights[lag]))
+      if(!MathIsValidNumber(frac_coeffs[lag]))
          return false;
       ++weight_count;
      }
    if(weight_count != strategy_frac_lags ||
-      MathAbs(weights[0] - 1.0) > 1.0e-12 ||
-      MathAbs(weights[1] + strategy_frac_order) > 1.0e-12)
+      MathAbs(frac_coeffs[0] - 1.0) > 1.0e-12 ||
+      MathAbs(frac_coeffs[1] + strategy_frac_order) > 1.0e-12)
       return false;
 
    for(int endpoint = strategy_frac_lags - 1;
@@ -864,7 +864,7 @@ bool Strategy_LoadFractionalSignal(
          if(source_index < 0 ||
             source_index >= ArraySize(chronological_ratios))
             return false;
-         filtered += weights[lag] * chronological_ratios[source_index];
+         filtered += frac_coeffs[lag] * chronological_ratios[source_index];
         }
       if(output_count >= expected_outputs)
          return false;

# greview-11517 — Codex cross-review of QM5_11517

**Date:** 2026-08-24
**Router task:** `df68e99a-096b-4875-b408-d64cf204f2b0`
**Builder lane:** Gemini / agy
**Reviewer:** Codex
**Verdict:** **APPROVED — formally clean for Q02**

## Scope and governing context

Read before review:

- `docs/ops/rebaseline/GATE_MANIFEST_V4_LINEAR_PROPOSAL_2026-08-23.md`
- `docs/ops/rebaseline/FACTORY_AUTOMATION_INVENTORY_2026-08-23.md`
- `docs/ops/rebaseline/GATE_NAME_CENSUS_2026-08-23.md`
- Vault path `G:/My Drive/QuantMechanica - Company Reference/03 Pipeline/Pipeline Rebaseline Directive 2026-08-23.md` exists (`Length=6630`, `LastWriteTime=2026-08-23 11:48:28`), but the Google Drive mount stalled on every bounded content read (PowerShell, .NET, and `cmd type`; no bytes returned in 90 seconds). The three repository rebaseline documents directly cite and operationalize that directive; no topology, threshold, queue, factory, verdict-row, or live-surface change was made by this review.

Command used to locate the router row:

```powershell
$tasks = python tools/strategy_farm/agent_router.py list-tasks --agent gemini | ConvertFrom-Json
$tasks | Where-Object { $_.id -eq 'df68e99a-096b-4875-b408-d64cf204f2b0' }
```

The row was `state=REVIEW`, `task_type=build_ea`, `ea_id=11517`, slug `carter-t-ema5-15-50-100-macd-h4`. Its legacy/minimal payload did not contain the ticket's expected `ea_dir`, `card_path`, or `artifact` fields. Unique deterministic lookup resolved:

- EA: `framework/EAs/QM5_11517_carter-t-ema5-15-50-100-macd-h4/`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11517_carter-t-ema5-15-50-100-macd-h4.md`
- Builder note: `docs/ops/evidence/2026-08-23_gemini_build_ea_qm5_11517_carter_ema5_15_50_100.md`
- Build identity: `docs/ops/evidence/df68e99a_QM5_11517_carter-t-ema5-15-50-100-macd-h4_build_identity.json`

Citation aliases below: `mq5` = `framework/EAs/QM5_11517_carter-t-ema5-15-50-100-macd-h4/QM5_11517_carter-t-ema5-15-50-100-macd-h4.mq5`; `card` = the approved D: card above; `build_identity.json` = the build-identity evidence above.

## Card fidelity

The approved card declares H4 and the EURUSD/GBPUSD universe (`card:18-21`), EMA(5)/EMA(15) cross with price beyond EMA(50)/EMA(100) and MACD(12,26,9) sign (`card:53,61-71`), fixed 30-pip SL / 60-pip TP (`card:74-80`), fixed-risk Q02 and percentage-risk live (`card:84-85`), 15-pip spread cap and no Friday entry (`card:90-91`), and no ML (`card:99`).

Implementation matches:

- `mq5:79-96` scans the declared three-bar EMA-cross lookback using closed-bar shifts.
- `mq5:127-141` uses closed H4 price, EMA(50), EMA(100), and MACD main sign for long; `mq5:156-157` mirrors short.
- `mq5:143-144,159-160` applies fixed-pip SL and TP.
- `mq5:262` gates evaluation to a new H4 bar, so shift-1 logic executes at the next bar open.
- `mq5:67,117` wires the spread and Friday-entry controls.

No card mechanism was omitted or added. No ML, grid/martingale/averaging, fabricated commission, or fabricated swap logic appears in the EA.

## Dead-input audit

Every declared `strategy_*` input was grepped and has an executable use beyond its declaration:

| Input | Use-site lines in MQ5 |
|---|---|
| `strategy_ema_fast_period` | 83, 85, 122 |
| `strategy_ema_slow_period` | 84, 86, 122 |
| `strategy_ema_trend1_period` | 123, 128 |
| `strategy_ema_trend2_period` | 123, 129 |
| `strategy_macd_fast` | 130 |
| `strategy_macd_slow` | 130 |
| `strategy_macd_signal` | 130 |
| `strategy_cross_lookback` | 140, 156 |
| `strategy_sl_pips` | 124, 143, 159 |
| `strategy_tp_pips` | 124, 144, 160 |
| `strategy_spread_cap_pips` | 67 |
| `strategy_no_friday_entry` | 117 |

Result: **no dead strategy input**.

## Framework and artifact conformity

- Common include chain: `mq5:5` includes `<QM/QM_Common.mqh>`.
- MAE: `mq5:222` calls `QM_FrameworkTrackOpenPositionMae()` before safety/filter/entry processing.
- Framework/risk binding: `mq5:192-208` calls `QM_FrameworkInit(...)`. Both governed backtest sets declare `environment=backtest`, `risk_mode=FIXED`, `RISK_FIXED=1000`, `RISK_PERCENT=0` (`EURUSD set:9-20`; `GBPUSD set:9-20`). Source defaults retain the card's live `RISK_PERCENT=0.5` (`mq5:15-17`), with ENV-to-mode validation delegated to the framework.
- Magic: `mq5:114` binds `req.symbol_slot=qm_magic_slot_offset`; active registry rows are `magic_numbers.csv:16102-16103`, yielding `115170000` and `115170001` (`ea_id*10000+slot`). Read-only verification returned `registry=(True,11517)` and `resolver_contains=True`. Resolver registry SHA `F53DE51746AE5A60F06BD8EA87890D1052A1944462AF80AB5DAAB3AB1E90F4A2` exactly equals the current normalized `magic_numbers.csv` SHA.
- News: `mq5:256-258` executes `QM_NewsAllowsTrade2` for current temporal/compliance modes and falls back to the legacy helper only when both modern controls are off. Defaults are `PRE30_POST30` / `DXZ` (`mq5:21-25`).
- Indicators: only cached `QM_EMA` / `QM_MACD_Main` readers are used (`mq5:83-86,128-130`). They reach `QM_IndicatorReadBuffer`, whose fixed one-element buffer is accepted only when `CopyBuffer(..., count=1)` returns exactly one (`framework/include/QM/QM_Indicators.mqh:281-288`). The sole raw series call is a single closed-bar `iClose` with an explicit perf-allowed annotation (`mq5:127`).
- Setfiles: governed-format headers, card-source provenance, exact registered symbol/slot pairing, fixed backtest risk, and all 12 strategy inputs are present (`sets/*.set:1-36`). The build identity records both paths under `setfiles_generated` (`build_identity.json:10-13`).
- SPEC validator: PASS. `SPEC.md` has one cosmetic non-gating rendering defect at line 76 (`",000"` instead of `$1,000`); executable and setfile values are correct.

## Read-only gate results

No compile was run.

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_11517_carter-t-ema5-15-50-100-macd-h4
=> failures=[]; warnings=[]; files_scanned=1; D2-D18 automated checks clean

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_11517_carter-t-ema5-15-50-100-macd-h4
=> verdict=PASS; files_checked=3; findings=[]

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_11517_carter-t-ema5-15-50-100-macd-h4
=> 1 PASS, 0 FAIL
```

The worktree remained unchanged after those invocations (`git status --short` empty).

## Binary freshness / compile identity

`build_identity.json:5-9` records `build_check_passed=true`, MQ5 SHA-256 `860b44ac1743654f9f93fd5eb446aae6573d268c8ca29f017b083e9c599994bc`, and EX5 SHA-256 `a453486593eea4ff59846fca70b1f798f3b6695ad161b6d16967916863a714bc`.

Read-only recomputation against the current files returned:

```json
{
  "mq5_current": "860b44ac1743654f9f93fd5eb446aae6573d268c8ca29f017b083e9c599994bc",
  "mq5_match": true,
  "ex5_current": "a453486593eea4ff59846fca70b1f798f3b6695ad161b6d16967916863a714bc",
  "ex5_match": true,
  "build_check_passed": true
}
```

The delivered EX5 is therefore bound to the current MQ5 build identity; it is not stale. No governed recompile is required.

## Tests

```text
python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_gen_setfile.py
=> 50 passed, 1 failed in 128.38s
=> unrelated pre-existing failure: test_qm5_411xx_sources_have_no_unbounded_numeric_buffers
   reports seven QM5_411xx findings, first at QM5_41134 ... daily_returns[first_retained];
   it does not scan or name QM5_11517.

python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py -k "d10_indicator_buffer_bound or build_prompt_requires_runtime_array"
=> 2 passed, 28 deselected in 0.63s

python -m pytest -q tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_gen_setfile.py
=> 21 passed in 2.25s
```

## Verdict

**APPROVED — formally clean for Q02.** The EA is card-faithful, every strategy input is live, framework/risk/magic/news/MAE/indicator controls conform, governed backtest setfiles are present, and the current source and binary exactly match the recorded passing build identity.

## Risks and open questions

- Non-blocking: cosmetic `$1,000` rendering damage in `SPEC.md:76` and the older builder note; repair on a future authorized SPEC touch.
- Non-blocking infrastructure debt: the full hardening test module currently has an unrelated QM5_411xx cohort failure described above.
- Context-access limitation: the vault directive's G: file was metadata-readable but content-unreadable from this worker during the review; the three checked-in rebaseline documents that cite it were read completely.
- No Q02 backtest was enqueued or run by this review.

## Rollback

This ticket adds only this evidence note and closes the router review. Repository rollback is `git revert <this-review-commit>`. Router rollback, if the close decision must be reversed, requires a separately authorized append-only router correction; do not overwrite verdict rows. No EA, setfile, registry, resolver, factory, queue, database verdict row, or `T_Live` artifact was changed.

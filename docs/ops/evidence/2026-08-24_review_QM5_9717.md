# Review — QM5_9717 bandy-pir-position-in-range-mr-index (router 31cf6a16)

**Reviewer:** Claude (review lane) · **Date:** 2026-08-24
**Task:** 31cf6a16-25bd-4311-b375-92d143a20cce (review_ea, gemini/agy build, source 9fd339d0)
**Worker verdict:** PASS (SPEC.md, D1-D11 build gates, setfiles backfilled)
**Verdict: APPROVED (clean for Q02)**

## Card-faithful mechanism
Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9717_bandy-pir-position-in-range-mr-index.md`
Bandy Position-in-Range, long-only index MR, D1 only.
- PIR = 100*(close1-LL)/(HH-LL) over close of last N bars — `CalculatePIR` mq5:47-77. Matches card formula.
- Entry: `pir <= 10 AND close1 > SMA(200)` — mq5:117-119 (`close1 <= sma200 || pir > thresh` → return false). Faithful.
- Exit: `pir >= 50` — `Strategy_ExitSignal` mq5:180. Time stop 7 D1 bars — `Strategy_ManageOpenPosition` mq5:161-167. Catastrophic SL 2.0*ATR(14) — `QM_StopATR` mq5:130. All faithful.
- One position per magic: `QM_TM_OpenPositionCount(magic) > 0` mq5:105. Long-only. D1 enforced in OnInit (`_Period != PERIOD_D1 -> INIT_PARAMETERS_INCORRECT` mq5:230).

## Dead-input check (every declared strategy input grepped)
All 9 strategy_* inputs have ≥1 use-site beyond declaration:
range_period(3), pir_entry_thresh(2), pir_exit_thresh(2), sma_period(2), time_stop_days(2), atr_period(3), sl_atr_mult(2), spread_max_atr(2), warmup_bars(4). No dead inputs.

## Framework conformity
- `#include <QM/QM_Common.mqh>` ✓; MAE hook `QM_FrameworkTrackOpenPositionMae()` in OnTick ✓.
- RISK_FIXED=1000 / RISK_PERCENT=0 (backtest) ✓; magic via `QM_FrameworkMagic()` (MagicResolver) ✓.
- Magic registry: 9717*10000+slot, e.g. SP500 slot2 = 97170002, all 13 symbols active (magic_numbers.csv). ea_id_registry active.
- News wired: `QM_NewsAllowsTrade2` (temporal+compliance) mq5 OnTick ✓; kill-switch, Friday-close wired.
- Bounded buffers (iClose loop over N; QM_ATR/QM_SMA helpers); no raw series, no ML, no invented commission/swap. ✓

## Build evidence (current source hash)
- Current mq5 SHA256 `1a209dda…16ee3` == build_result artifact hash. Evidence is for the current source, not stale.
- `compile_succeeded: true`, `build_check_passed: true` (D1-D11) — `docs/ops/evidence/2026-08-23_QM5_9717_bandy-pir-position-in-range-mr-index_build_result.json`.

## Setfiles (governed generator)
13 D1 backtest setfiles present. Sample SP500: `environment: backtest`, `magic_slot=2`, RISK_FIXED=1000, RISK_PERCENT=0, strategy inputs populated. Slot matches registry.

## Minor note (non-blocking, not a Q02 defect)
SPEC.md §4 lists base timeframe `H1` (auto-generated ex-post by gen_spec_md.py). EA is D1-only (OnInit hard-rejects non-D1; all setfiles D1). Cosmetic gen_spec_md.py mislabel — no functional impact on dispatch or Q02. Recommend SPEC-gen fix in a later pass.

# rework-38005 — QM5_38005 review repair evidence

- Router task: `d840a938-f474-48fe-bf94-ce7cb82a8f17`
- EA: `QM5_38005_codetrading-ascending-triangle-breakout`
- Approved card: `strategy-seeds/cards/approved/QM5_38005_codetrading-ascending-triangle-breakout.md` (`g0_status: APPROVED`)
- Source SHA-256 after repair: `b36cb4b1a1bc25f6b91aa46b1a42eb2eb0908dfcae6d70b0d824e9b2ccac518d`
- Disposition: `SOURCE_REPAIR_STATIC_PASS_COMPILE_HELD`

## What changed

- Restored the approved five-bar pivot default and implemented actual time-normalized geometry: pivot price change is divided by pivot-bar distance and ATR; ascending triangles require flat resistance `<=0.05` and rising support `>=+0.10`, with mirrored descending constraints (`framework/EAs/QM5_38005_codetrading-ascending-triangle-breakout/QM5_38005_codetrading-ascending-triangle-breakout.mq5:37`, `:271-300`).
- Replaced per-call raw OHLCV reads with one bounded closed-bar `CopyRates` window. Resize/copy/`ArraySize` guards fail closed, as do missing or zero mandatory volumes; confirmation is the card's strict `>` comparison (`.mq5:131-199`).
- Removed the ATR stop fallback. Entry now rejects invalid recent-pivot geometry, projects TP by exactly one triangle height, and admits only height targets that also satisfy the card's stated minimum 2R (`.mq5:349-399`).
- Replaced the one-time-only management substitute with restart-reconstructible broker-SL state: +1R protection followed by tightening-only completed swing-pivot trailing for long and short positions (`.mq5:401-475`).
- Added the 2.0% realized-daily entry halt, 2.5% framework daily hard stop, 5.0% framework portfolio-DD signal threshold, live 0.50% risk-sizer cap, and maximum three native ticks of order deviation (`.mq5:52-56`, `:309-341`, `:511-537`). Backtest presets remain `RISK_FIXED=1000`, `RISK_PERCENT=0`.
- Converted the 23:55-00:05 rollover window through `QM_BrokerToUTC`, seeded closed-bar state in `OnInit`, refreshed it before entry admission, and moved open-position management ahead of entry-only rollover/spread/news filters (`.mq5:326`, `:539`, `:552-604`). The explicit MAE hook remains at `.mq5:552`.
- Updated all three scoped H1 backtest presets with every strategy input. Their stale executable bindings are deliberately `pending` until a governed compile succeeds (for example XAUUSD set lines 13 and 25-44).
- Updated `SPEC.md:15-44` to describe the executable card mapping and added five focused regression tests at `tools/strategy_farm/tests/test_qm5_38005_review_rework.py:26-153`.

The approved target text says both “1.0 x Triangle Height” and parenthetically “1:2.0 R:R”. No separate OWNER adjudication artifact exists. This repair preserves both without replacing the named target: TP remains exactly one projected height, and sub-2R formations fail entry admission.

## Registry and framework evidence

- EA registry identity is unchanged: `framework/registry/ea_id_registry.csv:4476`.
- Existing active magic rows are unchanged: `framework/registry/magic_numbers.csv:17497-17499` (slots 0-2 for XAUUSD.DWX, SP500.DWX, EURUSD.DWX).
- Magic is resolved only through `QM_FrameworkInit` / `QM_FrameworkMagic`; no raw magic arithmetic or registry edit was introduced.
- The EA continues through `QM_Common.mqh`, declares the H1 execution contract, calls the explicit MAE hook, uses fixed-risk backtest presets, and contains no ML.

## Validation output

Focused tests:

```text
python -m pytest tools/strategy_farm/tests/test_qm5_38005_review_rework.py -q
.....                                                                    [100%]
5 passed in 1.43s
```

Relevant hardening suite:

```text
python -m pytest tools/strategy_farm/tests/test_qm5_38005_review_rework.py tools/strategy_farm/tests/test_build_gate_hardening.py -q
1 failed, 33 passed in 408.09s
```

The sole failure is unrelated to this patch: `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven pre-existing `QM5_411xx` sources, beginning with `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`. The focused 38005 module contributed five PASS results. No `QM5_411xx` file is modified in this worktree.

Other scoped checks:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_38005_codetrading-ascending-triangle-breakout
files_scanned=1 failure_count=0 warning_count=0 symbols=3

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_38005_codetrading-ascending-triangle-breakout
PASS; files_checked=4; findings=[]; max_news_stale_hours=336

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_38005_codetrading-ascending-triangle-breakout --fail-on-leak
SINGLE_SYMBOL_OK; n_violations=0

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_38005_codetrading-ascending-triangle-breakout
Summary: 1 PASS, 0 FAIL (of 1)

git diff --check
PASS (line-ending notices only)
```

Every declared EA input was grepped for executable use-sites; the minimum occurrence count was two.

## Governed compile hold

The required scoped strict compile was attempted:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 \
  -EAPath framework/EAs/QM5_38005_codetrading-ascending-triangle-breakout/QM5_38005_codetrading-ascending-triangle-breakout.mq5 -Strict
compile_one.result=FAIL
compile_one.reason_class=INCLUDE_MIRROR_REFUSED
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
```

The separately scoped `build_check.ps1 -EALabel QM5_38005_codetrading-ascending-triangle-breakout -SkipCompile` attempt was refused by the same preflight before static execution. The guard reported that `terminal64` processes are alive and directed compilation to the governed `COMPILE_EA` worker path. No terminal was stopped, no factory flag was changed, and no guard was bypassed.

The read-only governed candidate classifier reports `eligible=false`, reasons `EX5_ALREADY_PRESENT` and `BOUND_SETFILE_HASH_EXISTS`, with `force_rebuild_authorized=false` and `source_repair_authorized=false`. Therefore this ticket cannot lawfully enqueue a replacement compile row. No compile or backtest was enqueued.

The stale tracked EX5 had SHA-256 `d8d5c28ed4de4b6e68aec88eb38e8375d6692301bb05d48c2446427615661ac9` and did not bind the repaired source. It was removed from this patch (recoverable by reverting the commit) rather than presented as fresh evidence. No smoke run is claimed without a current executable.

## Rollback

Revert this ticket's commit with `git revert <this-commit>` to restore the prior MQ5, SPEC, presets, stale EX5, and remove the focused test/evidence note. No registry, gate criterion, verdict row, backtest queue, factory control, or T_Live artifact needs rollback.

## Required follow-up

A governed source-repair/force-rebuild authorization for EA 38005 must admit a `COMPILE_EA` successor bound to source SHA-256 `b36cb4b1a1bc25f6b91aa46b1a42eb2eb0908dfcae6d70b0d824e9b2ccac518d`. Its worker must produce a zero-error/zero-warning EX5, strict scoped build-check report, final preset build hashes, and smoke evidence before pipeline promotion.

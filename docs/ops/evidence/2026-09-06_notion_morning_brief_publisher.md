# Notion morning brief publisher — 2026-09-06

Task 6d080e68-0ef8-4504-8230-642cc95bbd25. REVIEW. Implementation on agents/codex-notion-morning-brief-20260906; disabled by default. No Notion request made; no token inspected or printed.

The publisher uses requests and pins Notion-Version 2022-06-28 as requested. It locates Morgenbriefing among the configured parent page children, creates the database when absent, queries the Date property, and creates or updates one publisher-owned daily page. It refuses duplicate databases/date pages and serializes local publishers with an exclusive lock. Updates replace the dedicated page body; network failure during replacement can leave a partial body until retry. The lock covers this host; other hosts must not publish concurrently. No automatic retries of creates are attempted.

It reads only NOTION_INTEGRATION_TOKEN from the canonical .private/env file when enabled, refuses missing/ambiguous tokens or an invalid parent UUID, and logs only exception type on failure in the morning-mail hook. Config enabled=false and the parent placeholder keep the integration inert even if a token is later added. Activation requires configuring the shared parent and explicitly enabling the flag. The optional step runs only after successful mail delivery; dry-run exits before the mail/publisher path. The CLI defaults to dry-run and requires --publish plus the enabled config for network.

Markdown/plaintext and HTML summaries become headings, bullets, paragraphs, and a two-column counter/factory table. HTML script/style content and attributes are dropped. The existing website archive scrubber is reused, with additional filtering for credentials, hosts, long account identifiers, small labelled magics, and input assignments. This is a scrubbed marketing copy of the daily brief, not an additional company documentation source. Future unfamiliar sensitive formats require scrubber extension; no regex can identify arbitrary unlabelled secret prose.

Verification: 102 focused tests passed after restoring sparse-checkout schema/script fixtures. Fake HTTP proves one database and one page across repeated publication, replacement of the existing body, correct API header, pagination, safe error handling, no-network dry-run/disabled mode, missing-token refusal, exposure redaction, HTML conversion and payload bounds. The real 2026-09-06 06:00 local brief rendered to 45 blocks in dry_run_payload.json; source_binding.json records its SHA256. No stored brief or mail state was modified.

API basis: official [legacy database creation](https://developers.notion.com/reference/create-a-database) and [date query filters](https://developers.notion.com/reference/post-database-query-filter). The pinned version is intentionally the user-requested legacy API; migration is outside this change.

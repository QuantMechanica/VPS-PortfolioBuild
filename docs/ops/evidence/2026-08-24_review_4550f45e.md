# Review — QM5_12944 Sperandeo Trend Fault-Line Break (H4) — task 4550f45e

- Router task: `4550f45e-827d-4cb4-96f8-f25eaef2a30a` (review_ea, assigned codex, state REVIEW)
- Build task: `01bd8a9d-bd3f-4da5-a275-be9192a763ed` (gemini/agy build_ea, "PASS: reworked, addresses all 6 findings")
- Worker verdict (codex): FAIL_MECHANICAL_REWORK_REQUIRED —
  `docs/ops/evidence/4550f45e_qm5_12944_gemini_codex_review_2026-08-23.json`
- Sibling review of the SAME build: `c74163e1-fd20-413d-a42f-e2f3eb432f4f` already closed
  **RECYCLE** 2026-08-24T08:04 (note `docs/ops/evidence/2026-08-24_review_qm5_12944.md`).
  4550f45e is a duplicate adjudication of the identical source/binary.
- **Claude-lane verdict: RECYCLE** (worker findings independently confirmed; defects are
  named + actionable; card stays approved; mechanism card-faithful in outline).

## Independent verification (read-only, current source hash)

- Live MQ5 `a83623b3b5946bb593fd7a9a7a2e819b6ccbdbf60d5020809f20b8952d050d31` matches build
  artifact `mq5_sha256`. EX5 `e19a9d287c247aaec8f074ff0469aeb977f0fc03b349a95617aa02a347c2d711`
  matches artifact `ex5_sha256`, but that EX5 is the **pre-rework binary** (producer hash
  before rework was `18ece113…`). Commit 02f10d0 changed the MQ5 but the EX5 was not recompiled.
- Card read at `framework/EAs/QM5_12944_sperandeo-trend-fault-line-h4/docs/strategy_card.md`
  (SPEC.md's `artifacts/cards_approved/QM5_12944_*.md` pointer is a broken path; card lives in
  the EA `docs/` dir). R1–R4 all PASS on the card.
- Framework corset present: `#include <QM/QM_Common.mqh>` (mq5:5); MAE hook
  `QM_FrameworkTrackOpenPositionMae()` first in OnTick (mq5:429); `QM_FrameworkInit` wired
  (mq5:399); kill-switch, news, Friday close inherited; no ML; no invented commission/swap.
- Magic registry, forbidden-code grep, SPEC validation PASS (worker evidence). All 13 backtest
  setfiles: `RISK_FIXED=1000`, `RISK_PERCENT=0`, `environment: backtest` — governed-generator form.

## Confirmed defects (file:line)

1. **Stale EX5 — build cannot enter Q02 (blocking).** MQ5 rewritten (commit 02f10d0, +65/-19)
   but `QM5_12944_sperandeo-trend-fault-line-h4.ex5` retains the pre-rework hash `e19a9d28…`;
   `compile_succeeded=true` in the artifact reflects a COMPILED_CACHED result, not a recompile
   of the reviewed source. Recompile is ROT (active-inventory recompile is OWNER/OFF-window
   governed) — route via the COMPILE_EA/OFF-window queue, not in this lane.
2. **News window 30/30 vs card 15/15 — card divergence.** `qm_news_temporal` default is
   `QM_NEWS_TEMPORAL_PRE30_POST30` (mq5:22) and `QM_FrameworkInit` is called with literal
   `30, 30` for pre/post minutes (mq5:407-408). Card §Zusätzliche Filter (docs/strategy_card.md:105)
   mandates "skip during ±15 min around high-impact news". Set to 15/15 and PRE15_POST15.
3. **Dead input `strategy_min_pivots` (mq5:38).** Declared but no use-site anywhere; both
   fault-line searches hard-code 3 pivots (loops `i <= *_count-3`, `[i+2]`, guard `< 3` at
   mq5:119-131 and 165-177). Card fixes pivot-count=3 so the constant is card-faithful, but the
   advertised input is inert — wire it or remove it (dead-input rule → RECYCLE).
4. **Both risk defaults non-zero (source hygiene).** `RISK_PERCENT=0.5` (mq5:17) and
   `RISK_FIXED=1000` (mq5:18) both positive; a default framework attach fails
   `QM_FrameworkValidateRiskInputs` (EA_INPUT_RISK_BOTH_SET). Non-blocking for Q02 (all 13
   setfiles override correctly), but set a single active mode.
5. **Smoke deferred, not evidenced.** Build artifact `smoke_result=deferred_p2_smoke`; no
   governed smoke summary. Sanctioned-capacity deferral, not a defect on its own — folded into
   the recompile-then-Q02 path.

## Action

RECYCLE to Codex: fix news window (15/15), resolve dead `strategy_min_pivots`, single-mode
risk defaults; then rebuild via COMPILE_EA/OFF-window so the EX5 binds the current source hash
before Q02. Card remains approved.

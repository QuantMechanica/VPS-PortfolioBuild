# rework-41003 review fix evidence

- Router task: `7c616d3f-20ef-449f-820b-3819beb800cc`
- EA: `QM5_41003_kaufman-ready-set-go-momentum`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_41003_kaufman-ready-set-go-momentum.md`
- Review work order: `docs/ops/evidence/7c616d3f_qm5_41003_gemini_build_codex_review_2026-08-18.md`
- Worktree: `C:/QM/worktrees/rework-slot-2`
- Date validated: 2026-08-24

## What changed

1. Preserved the card-faithful repair already present in this branch:
   - Ready uses the 30-sample SMA of `ATR(10)` rather than `ATR(30)` (`framework/EAs/QM5_41003_kaufman-ready-set-go-momentum/QM5_41003_kaufman-ready-set-go-momentum.mq5:178`, `:190-200`).
   - Momentum reads the configured `Close[5]` default without the rejected `1 + bars` shift (`...mq5:178-179`).
   - Every cached signal component is cleared before fallible reads (`...mq5:163-169`).
   - Spread admission uses closed-bar `ATR(14)` (`...mq5:184`, `:246-249`, `:279-281`).
   - The H1/Friday execution contract is declared and the card loss controls are wired through the framework kill switch (`...mq5:355-364`).
2. Declared the exact 2.0% realised-loss entry halt, 2.5% daily hard stop, and 5.0% total-drawdown stop as sealed strategy inputs so the mechanical gate can verify their values and use-sites (`...mq5:47-49`, `:91-93`, `:155`, `:360-364`).
3. Added fail-closed input-range validation. The ATR-SMA loop is capped at 50 pooled reads per completed H1 bar (`...mq5:78-96`, `:172`, `:316-319`).
4. Moved strategy management and explicit exits ahead of the entry-only no-trade/open-position guard, keeping management reachable while exposure exists (`...mq5:381-408`).
5. Updated the three canonical backtest presets with the sealed loss inputs, set version `s20260824-001`, and refreshed build-hash headers (`framework/EAs/QM5_41003_kaufman-ready-set-go-momentum/sets/*.set:6`, `:13`, `:35-37`). Backtest risk remains `RISK_FIXED=1000`, `RISK_PERCENT=0` (`sets/*.set:19-20`).
6. Documented the sealed inputs and rework revision (`framework/EAs/QM5_41003_kaufman-ready-set-go-momentum/SPEC.md:42-44`, `:112`).
7. Added a focused static regression suite covering all six review findings, all declared strategy input use-sites/bounds, QM umbrella/magic/MAE/risk conformity, preset risk mode, and forbidden raw/ML calls (`tools/strategy_farm/tests/test_qm5_41003_review_rework_static.py:38`, `:69`, `:107`).

No `ea_id_registry.csv`, `magic_numbers.csv`, resolver, gate criteria, queue/backtest, factory, verdict-row, or T_Live changes were made. Hardening independently resolved the existing active magic rows at registry lines 17545-17547.

## Build and validation evidence

### Compilation

The normal scoped command was attempted first:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_41003_kaufman-ready-set-go-momentum/QM5_41003_kaufman-ready-set-go-momentum.mq5 -Strict
```

It fail-closed with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because T1-T8 factory terminals were alive. The governed compile enqueue classifier also refused an ordinary rebuild because 41003 already has an EX5/build binding; no allowlist, queue, factory, or terminal state was changed to bypass either guard.

Compilation was therefore performed with MetaEditor `/inc` against a disposable composite include tree under `framework/build/compile/20260824_084607/composite` (read-only copy of T1 standard includes plus this worktree's `framework/include/QM`). It did not mirror into or stop any terminal:

```text
Result: 0 errors, 0 warnings, 6791 ms elapsed, cpu='X64 Regular'
log: C:/QM/worktrees/rework-slot-2/framework/build/compile/20260824_084607/QM5_41003_kaufman-ready-set-go-momentum.isolated.compile.log
MQ5 SHA-256: 7e9a36766c82ba196d9ffc507fedcec3fabe0e84ae9d1652bcb4a5ff6d816efb
EX5 SHA-256: 22fda6c3e971a06efd3879c0868e5e8f8276b7867d946d23eefc0e9ce985dc0d
```

### Focused checks

```text
python -m pytest tools/strategy_farm/tests/test_qm5_41003_review_rework_static.py -q
3 passed in 0.64s

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_41003_kaufman-ready-set-go-momentum
Summary: 1 PASS, 0 FAIL (of 1)

python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 framework/EAs/QM5_41003_kaufman-ready-set-go-momentum
verdict: PASS; files_checked: 4; findings: []

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_41003_kaufman-ready-set-go-momentum
failures: []; files_scanned: 1; symbol/magic/loss-limit/management/MAE/buffer checks PASS

git diff --check -- <41003 paths + focused test>
PASS (only Git's expected LF-to-CRLF working-copy notices)
```

### Touched-module and full-suite pytest

```text
python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_qm5_41003_review_rework_static.py -q
32 passed, 1 failed in 509.31s
```

The sole failure is outside 41003: `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven pre-existing QM5_411xx findings, first `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`.

```text
python -m pytest tools/strategy_farm/tests -q
4640 passed, 4 failed, 4 skipped, 2 warnings, 42 subtests passed in 2117.76s
```

The four full-suite failures are repository baselines unrelated to files in this ticket:

- `test_build_gate_hardening.py::test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` — seven QM5_411xx findings, first at QM5_41134 line 599.
- `test_execution_contract_lint.py::test_20030_20032_bindings_follow_deployed_calendar_but_remain_blocked` — expected calendar coverage/hash ends 2026-08-21, runtime contract now carries 2026-08-29/hash `d487e699...`.
- `test_execution_contract_lint.py::test_20009_ftmo_news_calendar_is_exact_and_evidence_bound` — stale expected primary/secondary calendar hashes.
- `test_opt_census_dispatch.py::test_opt_census_ranks_tier6_not_priority` — expected active-manifest rank 7 while hardcoded OPT_CENSUS rank is 6.

The focused 41003 suite was rerun after final preset edits and remained `3 passed`.

## Risks and open points

- The authoritative strict wrapper could not run while live factory terminals held the include-mirror guard. Source compilation, hardening, SPEC, guardrails, and focused tests are clean; reviewer should treat the guard refusal as an infrastructure constraint, not a strict-wrapper PASS.
- The 5% total-drawdown threshold uses the framework portfolio-DD signal channel. As designed by `QM_KillSwitch`, that external signal is live-only and skipped in the MT5 tester.
- No smoke/backtest was run or enqueued; this ticket is build/review only.

## Rollback

Resolve the commit containing this evidence with `git log -1 -- docs/ops/evidence/2026-08-23_rework-41003.md`, then run `git revert <commit>`. This restores the prior MQ5, EX5, SPEC, and three presets and removes the focused test/evidence without rewriting history.

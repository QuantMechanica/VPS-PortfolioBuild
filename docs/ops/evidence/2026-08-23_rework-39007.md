# rework-39007 — QM5_39007 review-fix evidence

Date executed: 2026-08-24

Router task: `c0a06995-41e2-48df-a94b-aaec3b29fa62`

EA: `QM5_39007_forexfactory-100-pips-early-bird-breakout`

## Work order

The RECYCLE review note was
`docs/ops/evidence/c0a06995_qm5_39007_gemini_build_codex_review_2026-08-18.md`.
It identified six card-conformance defects: the D1/M15 contract mismatch,
market-entry substitution for the pending straddle, an off-by-one box, pip
distances multiplied by ten, missing TP2/noon semantics, and missing daily and
total loss controls. The finding was repairable and was not a
`GENUINE_NO_SIGNAL` card defect.

## Changes

- The execution contract and new-bar gate now use M15
  (`framework/EAs/QM5_39007_forexfactory-100-pips-early-bird-breakout/QM5_39007_forexfactory-100-pips-early-bird-breakout.mq5:553`, `:605`).
- The box is exactly the bounded eight completed M15 bars in
  `[05:00, 07:00)` UTC. `CopyRates` is capped by
  `STRATEGY_MAX_BOX_BARS` and guarded by both copied-count and `ArraySize`
  checks (`...breakout.mq5:293-337`).
- The entry is a framework-routed BUY_STOP/SELL_STOP straddle with noon
  expiration, OCO cancellation, and rollback if the second leg cannot be
  placed (`...breakout.mq5:349-371`, `:401-438`, `:629-652`).
- Pip inputs are converted once through `QM_StopRulesPipsToPriceDistance`;
  there is no caller-side x10 multiplication (`...breakout.mq5:95-102`,
  `:355-356`, `:414`, `:494`).
- The broker target is TP2 at 100 pips; TP1 closes 50% at 50 pips and is
  restart-persistent in live mode. Break-even remains +1 pip after +20 pips.
  Noon cancels pending orders only and the strategy exit hook never closes an
  active trade (`...breakout.mq5:440-522`).
- The 2.0% daily realized-loss entry halt, 2.5% daily equity flatten/halt, and
  5.0% initial-equity drawdown flatten/halt are wired through the framework
  kill switch (`...breakout.mq5:167-243`, `:569-574`, `:590-598`).
- Framework conformity remains explicit: `QM_Common`, resolver-backed
  `QM_FrameworkMagic`, MAE tracking, fixed-risk backtest presets, and bounded
  framework indicator calls (`...breakout.mq5:5`, `:290`, `:410`, `:537-538`,
  `:590`). No raw `OrderSend`, ML surface, or direct indicator handles were
  added.
- The SPEC records the repaired mechanism and risk contract
  (`framework/EAs/QM5_39007_forexfactory-100-pips-early-bird-breakout/SPEC.md:13-35`, `:100`).
- Both backtest presets bind fixed risk and every strategy input; their
  revision metadata and build-hash stamps were refreshed
  (`...GBPUSD.DWX_M15_backtest.set:19-38`,
  `...EURUSD.DWX_M15_backtest.set:19-38`). Existing magic registry and resolver
  allocations were verified and left unchanged.
- Seven focused unit tests cover all review findings, verify every declared EA
  input has a non-declaration use site, validate both presets, and reject
  forbidden execution/indicator surfaces
  (`tools/strategy_farm/tests/test_qm5_39007_review_rework.py:37-150`).

## Validation evidence

All commands were run from `C:/QM/worktrees/rework-slot-3` and scoped to this EA
where the command accepts an EA scope.

1. Focused pytest:

   ```text
   python -m pytest tools/strategy_farm/tests/test_qm5_39007_review_rework.py -q
   .......                                                                  [100%]
   7 passed in 0.54s
   ```

2. Scoped build-gate hardening:

   ```text
   python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_39007_forexfactory-100-pips-early-bird-breakout
   exit 0
   files_scanned: 1
   failures: []
   warnings: []
   matrix_valid: true
   ```

3. Scoped build guardrails for the MQ5 and both shipped presets:

   ```text
   python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 <MQ5> <GBP-set> <EUR-set>
   exit 0
   overall verdict: PASS
   findings: [] for all three paths
   ```

4. SPEC validator and whitespace check:

   ```text
   python framework/scripts/validate_spec_doc.py framework/EAs/QM5_39007_forexfactory-100-pips-early-bird-breakout
   PASS  QM5_39007_forexfactory-100-pips-early-bird-breakout
   Summary: 1 PASS, 0 FAIL (of 1)

   git diff --check
   exit 0 (line-ending conversion notices only)
   ```

5. Relevant broader pytest selection:

   ```text
   python -m pytest tools/strategy_farm/tests/test_qm5_39007_review_rework.py tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_build_guardrails.py -q
   1 failed, 55 passed in 498.70s
   ```

   The sole failure is outside this ticket: the fleet-wide
   `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` found seven
   existing issues in `QM5_411xx` sources (first reported in
   `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`). The scoped 39007 hardening and
   all ticket tests pass.

6. Scoped PowerShell build check:

   ```text
   powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_39007_forexfactory-100-pips-early-bird-breakout -Strict
   LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
   terminal64 processes are alive; ad-hoc compile/build_check is refused
   retry_attempted: false
   ```

   This is the repository's fail-closed include-mirror safety guard, not an EA
   validation finding. The supported alternative is a governed `COMPILE_EA`
   work item. It was not enqueued from this non-canonical worktree because the
   worker compiles the canonical checkout, not this uncommitted source. No
   terminal was stopped, no factory setting was changed, no backtest was
   enqueued, and `T_Live` was not touched. Consequently, the tracked EX5 was
   deliberately not replaced and compilation remains the review handoff risk.

## Rollback

After this change is committed, revert it with `git revert <rework-39007-commit>`.
That restores the MQ5, SPEC, and presets and removes the focused test and this
evidence note. No registry allocation, gate threshold, verdict row, backtest
queue, factory toggle, or live-terminal file requires rollback.

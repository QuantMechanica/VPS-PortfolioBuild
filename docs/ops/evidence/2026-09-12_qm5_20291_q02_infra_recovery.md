# QM5_20291 market-neutral Q02 infrastructure recovery

Date: 2026-09-12

Branch: `agents/board-advisor`

EA: `QM5_20291_xauxag-kurt-rk`

## Selection and diagnosis

The approved build backlog had no collision-free, data-valid higher-diversity
candidate: the rates and lumber/gold cards remain R3 data-blocked, the other
unbuilt XAU/XAG candidate already has an open compile successor, and the
remaining unbuilt candidate is an M15 indicator strategy. The priority-2
recovery target was therefore this D1, monthly XAU/XAG historical-kurtosis
market-neutral basket.

Its historical Q02 rows contained no economic verdict. All three attempts used
the original August artifact identity (`mq5` SHA-256
`b63a95ca46b287848f65717b6d70479f65abcecc458e85b75ff8b19db156ffcc`,
`ex5` SHA-256
`7b6578395293cc0ba5fc13996535b3b3b20d7eaaa07055bb7de96219d3aaf62c`).
The latest row, `a7c3f1dc-9203-4423-830e-7b23e60af18a`, ended
`INFRA_FAIL` with `TIMEOUT` and `INCOMPLETE_RUNS` after 25,200 seconds.
Subsequent source repairs never produced a usable current binary: compile row
`8770a871-5535-434e-832d-47d363bad91f` compiled but failed the static bounded-
buffer gate, and row `1a1a6f0c-32d2-4182-b901-72bedd371f70` was refused because
its bound build task was no longer open.

## Repair

- Removed the equality comparison against framework-owned
  `PORTFOLIO_WEIGHT`; the locked-configuration guard now compares only the
  permitted EA identity, magic offset, fixed-risk mode, and `strategy_*`
  inputs. Stress probability remains a finiteness plus inclusive `0..1`
  validity check.
- Moved basket position scans, orphan repair, monthly rebalance closure, and
  the 40-day time-stop check behind the existing D1 new-bar gate. Framework
  MAE tracking, kill-switch handling, Friday-close handling, and broker-side
  protective stops remain tick-driven.
- Signal definition, kurtosis calculation, rank direction, entry frequency,
  sizing, stops, and holding period were not changed.

Repaired source SHA-256:
`c5f61fc37344f3b369f5383df1fde6ac926b401679555fc77a2f37c01cd3790b`.

## Guard and verification

- Mandatory pre-enqueue command:
  `python C:/QM/repo/tools/strategy_farm/audit_framework_input_pins.py --check-source C:/QM/repo/framework/EAs/QM5_20291_xauxag-kurt-rk/QM5_20291_xauxag-kurt-rk.mq5`
- Result: exit 0, `ok=true`, `EA_FRAMEWORK_INPUT_PINNED` hit count `0`.
- Kurtosis reference fixture: PASS; XAU `61.274399580566`, XAG
  `1.487520968417`.
- Mechanical build-gate hardening: PASS, zero failures and zero warnings.
- Governed compile work item:
  `9defc9a7-8f3e-4b8d-9cbd-466d72fd40f7`.
- Compile result: `COMPILE_OK`; build check PASS; compiler 0 errors and 0
  warnings. The two existing-at-cutover hard-coded symbol notices remained
  non-fatal migration warnings.
- New EX5 SHA-256:
  `b7a436172215047eb734d8fece979e03f8fe0642c8c25e4c1ed31d47fc137e0d`.
- All three regenerated backtest setfiles retain `RISK_FIXED=1000` and
  `RISK_PERCENT=0`.

## Append-only Q02 handoff and CPU stop

The current-binary requalification preflight was eligible with zero parameter
changes. It appended Q02 successor
`7debb301-a598-4864-a14b-adac7001b5c2`, bound to the repaired source, new EX5,
and logical-basket setfile SHA-256
`e48bc441a413f01429bc8210bfe72e066eb7f4ef9fb9c1ca32496f35d838d43d`.

The immediate five one-second CPU samples were `99`, `90`, `71`, `88`, and
`90` percent (average `87.6`, maximum `99`). The initial admission check
incorrectly evaluated the average only; prior PACER receipts establish an
exclusive 97% maximum ceiling. Because the 99% sample crossed that ceiling,
the still-pending and unclaimed successor had its priority-track acceleration
removed. No tester was manually dispatched and no terminal was stopped. The
append-only row is preserved for ordinary farm scheduling; it was not
destructively rolled back.

No T_Live file, AutoTrading state, portfolio gate, or live manifest was
touched.

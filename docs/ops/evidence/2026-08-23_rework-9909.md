# rework-9909 review-fix evidence

Date: 2026-08-24 UTC

Router task: `d6ea3abe-d44b-4861-b466-475a28899eaa`

EA: `QM5_9909_bandy-lrchannel-breakout-trend`

Source SHA-256: `e64ba41eedc84e6f9f2858e1413a3d84724207fd7322d54c891c0ccd7d96e847`

## Disposition

The five review findings in `docs/ops/evidence/2026-08-23_qm5_9909_gemini_code_review.md` were repaired in source, specification, presets, registry binding, tests, and build-result evidence. The fresh-source compile is not claimed as complete: the scoped build command was refused by the live-factory interlock and governed compile work item `5f4c9079-449c-4230-b9db-3cde2c7ce6a5` is pending under its active `COMPILE_EA_WORKER_ROLLOUT_PENDING` hold. The existing EX5 is explicitly marked stale and is not evidence-bound to this rework. Smoke is canonically `deferred_p2_smoke` until that governed compile produces a fresh EX5.

No Q02/backtest work was enqueued, no verdict row was changed, and neither `T_Live` nor factory/AutoTrading state was touched.

## What changed

1. **Explicit D1 contract and cadence.** `OnInit` now declares and validates `PERIOD_D1`, and entry has one explicit D1 new-bar edge (`framework/EAs/QM5_9909_bandy-lrchannel-breakout-trend/QM5_9909_bandy-lrchannel-breakout-trend.mq5:321`, `:332`, `:390`).
2. **Management reachability.** The unapproved spread/ATR input and filter were removed. MAE tracking remains the first `OnTick` hook, and trail/time-stop management runs before news, Friday, no-trade, and new-bar entry returns (`...mq5:347`, `:354`, `:372-390`).
3. **Two-layer stop lifecycle.** Entry risk is sized from the 2.5-ATR primary stop. After entry, the primary trail is persisted independently and the broker SL is widened to the declared 5-ATR catastrophic layer; modification failure retains the tighter entry SL. The primary Chandelier ratchets on closed D1 bars and is enforced every tick (`...mq5:133-153`, `:156-217`, `:223-304`, `:402-404`). Every declared `strategy_*` input has an executable use-site and validation use-site (`...mq5:111-119`).
4. **Bounded series use.** Regression reads an exact bounded closed-D1 window, checks the returned count/`ArraySize`, and every raw series access carries its reviewed `perf-allowed` annotation (`...mq5:55-104`, `:158`, `:171`, `:229-255`). No ML entry point was added.
5. **Approved runnable universe.** The setfile cohort is exactly AUDUSD, EURUSD, GBPUSD, NDX, NZDUSD, SP500, USDCAD, USDCHF, USDJPY, WS30, XAUUSD, and XTIUSD on D1. GDAXI/UK100 setfiles were removed; XTI was added. All 12 backtest presets bind `RISK_FIXED=1000`, `RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1` (`framework/EAs/QM5_9909_bandy-lrchannel-breakout-trend/sets/`). Existing GDAXI/UK100 registry allocations were left untouched as required.
6. **Append-only XTI magic allocation.** Added only slot 13 / magic 99090013 (`framework/registry/magic_numbers.csv:17019`) and regenerated the resolver with `python framework/scripts/update_magic_resolver.py --keep-obsolete`; generator result was 17,994 kept, 0 dropped (`framework/include/QM/QM_MagicResolver.mqh:22`).
7. **Documentation and result truth.** SPEC describes the explicit D1 and two-stop contracts plus the corrected universe/risk model (`framework/EAs/QM5_9909_bandy-lrchannel-breakout-trend/SPEC.md:18`, `:36`, `:84`). The task-bound result now includes the required schema and honest deferred state (`framework/EAs/QM5_9909_bandy-lrchannel-breakout-trend/build_identity.json:2`, `:4`, `:30`, `:59-64`).
8. **Exact compile repair authority.** The compile-only source-repair permission is bound to this exact EA label and router task, with negative tests for wrong labels/authorities (`tools/strategy_farm/compile_work_items.py:89-96`, `:226-230`; `tools/strategy_farm/tests/test_compile_work_items.py:213-231`). It grants no backtest, gate, verdict, or live authority.
9. **Regression coverage.** Six focused tests cover D1 cadence, management order, stop layers/input use-sites, bounded series annotations, exact set cohort/risk, append-only XTI binding, and build-result truth (`tools/strategy_farm/tests/test_qm5_9909_rework_static.py:49-161`).

## Validation evidence

Commands were run from `C:/QM/worktrees/rework-slot-5`.

| Command | Result |
|---|---|
| `python -m pytest tools/strategy_farm/tests/test_qm5_9909_rework_static.py -q` | PASS: `6 passed in 0.94s` |
| `python -m pytest tools/strategy_farm/tests/test_compile_work_items.py -k review_rework_source_repair_authority_is_exact_label_bound -q` | PASS: `1 passed, 17 deselected in 2.90s` |
| `python -m pytest tools/strategy_farm/tests/test_compile_work_items.py -q` | PARTIAL: `15 passed, 3 failed in 6.21s`; the three failures are pre-existing candidate-inventory expectations (`test_candidate_refuses_work_history_and_bound_hash`, `test_candidate_recheck_allows_only_exact_r11_revival_predecessor`, `test_dl089_force_rebuild_fails_closed_without_owner_priority_tracks_entry`) and do not enter the changed exact-authority predicate. |
| `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_9909_bandy-lrchannel-breakout-trend` | PASS: 13 files, 0 findings, max news stale 336h |
| `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_9909_bandy-lrchannel-breakout-trend` | PASS: 0 failures; one non-failing `EA_CARD_SMA_DIRECTION_UNDECIDABLE` legacy-card warning |
| `python framework/scripts/validate_spec_doc.py framework/EAs/QM5_9909_bandy-lrchannel-breakout-trend` | PASS: 1 PASS, 0 FAIL |
| `python framework/scripts/skill_build_ea_guard.py --ea-id 9909 --ea-label QM5_9909_bandy-lrchannel-breakout-trend` | PASS: registry row, magic rows, EA directory |
| `python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_9909_bandy-lrchannel-breakout-trend --fail-on-leak --json` | PASS: `SINGLE_SYMBOL_OK`, 0 violations |
| `pwsh -NoProfile -File framework/scripts/build_check.ps1 -EALabel QM5_9909_bandy-lrchannel-breakout-trend -Strict` | Correctly refused before compile: `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`; no retry or terminal mutation |

The governed replacement was submitted with:

`python tools/strategy_farm/farmctl.py enqueue-compile QM5_9909_bandy-lrchannel-breakout-trend --source-repair-authority router_review_ea:d6ea3abe-d44b-4861-b466-475a28899eaa`

Read-only SQLite query evidence (`file:D:/QM/strategy_farm/state/farm_state.sqlite?mode=ro`) at 2026-08-24 14:44 UTC:

```text
id=5f4c9079-449c-4230-b9db-3cde2c7ce6a5
status=pending
claimed_by=NULL
hold_code=COMPILE_EA_WORKER_ROLLOUT_PENDING
hold_active=1
ea_label=QM5_9909_bandy-lrchannel-breakout-trend
mq5_sha256=e64ba41eedc84e6f9f2858e1413a3d84724207fd7322d54c891c0ccd7d96e847
authority=router_review_ea:d6ea3abe-d44b-4861-b466-475a28899eaa
```

This is compile-only, append-only pipeline evidence. It does not infer a compile PASS, smoke PASS, or any economic/pipeline verdict.

## Rollback

Revert the ticket commit with `git revert <ticket-commit>` after confirming no later changes depend on slot 13. That restores the EA, SPEC, presets, build identity, exact compile-authority mapping, tests, CSV append, and generated resolver together. The queued compile item and router transition are append-only operational records and must not be deleted or overwritten; supersede them through the governed router/compile workflow if rollback is required.

## Remaining controlled risk

- Fresh compilation and smoke are still outstanding behind the reviewed worker-rollout hold; the stale EX5 must not be promoted.
- Legacy GDAXI/UK100 magic rows remain active because allocations are immutable, but they have no runnable setfiles in this approved cohort.
- The three unrelated full-module pytest failures remain visible above; no threshold, candidate criterion, or unrelated fixture was changed to hide them.

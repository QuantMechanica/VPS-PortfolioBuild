# Claude review — OPS-REBUILD-WAVE-E4-PART2 (router task 640d88e6)

Date: 2026-08-23
Reviewer: Claude (review lane)
Task: `640d88e6-5fc4-4abc-aec3-d0787a8e58c6` (ops_issue, REVIEW → verdict below)
Delivered artifact: `docs/ops/evidence/2026-08-23_rebuild_wave_e4_part2.md`
Commits under review: `4828d9664` (hardening), `532143355` (raw-series follow-up)

## Verdict: APPROVED

Hardening-only repair of `QM5_12989` and `QM5_1567` is formally clean; strategy logic
untouched; compile correctly enqueued append-only and held behind the governed fleet gate.
The compile → build_check → Q02-rerun tail is a downstream automated dependency, not an
unmet worker deliverable — it stays live on the two pending `COMPILE_EA` rows.

## What I verified (evidence over claims)

1. **Diffs are hardening-only, no strategy-logic drift.**
   - `QM5_12989_grimes-nested-pb-v2.mq5` (commit 4828d9664, hunk @165): adds
     `if(percentile_index >= ArraySize(atr_values)) return false;` immediately before the
     `atr_values[percentile_index]` read. Fail-closed bound guard; clamp value and entry
     predicate unchanged. (`git show 4828d9664`)
   - `QM5_1567_demark-td-reverse-sequential-h4.mq5` (commit 4828d9664, hunk @347): adds
     `QM_FrameworkTrackOpenPositionMae();` as first statement of `OnTick()`, before the
     kill-switch check. MAE-hook contract, matches the QM5_13213 pattern.
   - `QM5_1567` follow-up (commit 532143355): 11 `// perf-allowed:` trailing comments on
     `iClose/iHigh/iLow/iTime` calls in `SetupChain`/`CountdownTrigger`/`Strategy_EntrySignal`.
     `git show` confirms every hunk is a same-line comment append — no comparison, index, or
     branch logic changed.

2. **Current source hashes match the doc's final claims.** `sha256sum`:
   - `QM5_12989` = `0beecb76…` (matches doc table).
   - `QM5_1567` = `a9531d33…` (matches doc "Correction" hash, superseding the MAE-only hash).
   Both files are committed (clean `git status`).

3. **`build_gate_hardening.py` re-run clean for both** — `failures: []`; no
   `EA_INDICATOR_BUFFER_UNBOUNDED`, no `EA_Q08_MAE_HOOK_MISSING`.

4. **`EA_FRAMEWORK_RAW_SERIES_CALL` (the check that only lives in `build_check.ps1`
   `Invoke-PerfStaticCheck`, lines 1077-1085) would now pass.** Ran the check's exact
   `rawSeriesPattern` regex against current `QM5_1567` source: 0 unannotated raw-series
   lines, 11 perf-allowed annotations. The `// perf-allowed` exception path is the check's
   own sanctioned override (build_check.ps1:1085 "add '// perf-allowed' with reviewer
   sign-off") and the QM5_10030 convention it cites is real (8 annotations there).
   **Reviewer sign-off recorded here:** the 11 calls are the DeMark TD Sequential
   setup/countdown bar-counting core — bounded index-shift reads (bounded by
   `strategy_setup_bars` / `strategy_countdown_timeout`), reached only via the
   QM_IsNewBar-gated `OnTick`, i.e. exactly the bespoke-structural-logic class the exception
   exists for, not a per-tick unbounded history scan. Approved.

5. **Compile enqueued, held, not bypassed.** DB (mode=ro): work items
   `7022a056` (QM5_12989) and `24e3a252` (QM5_1567) both `kind=compile phase=COMPILE_EA
   status=pending verdict=None`. 45 `COMPILE_EA` rows pending fleet-wide confirms the
   `COMPILE_EA_WORKER_ROLLOUT_PENDING` gate is a real standing condition; the two rows sit
   behind it, not released.

6. **Registry unchanged for these EAs.** Neither commit (`git show --stat 4828d9664
   532143355`) touches any registry/magic/CSV file.

7. **Honest self-correction.** The worker's first pass verified `QM5_1567` with
   `build_gate_hardening.py` alone (which does not implement the raw-series check), concluded
   "not reproduced", then caught its own error, ran the authoritative pattern, found 11 real
   matches, and appended a correction section + follow-up commit. Good forensic discipline;
   no defect shipped.

## Residual (not a defect; correctly deferred)

The task's acceptance also names EX5 old/new hashes and Q02-rerun IDs. Those cannot exist
yet: no EX5 is produced until the governed `COMPILE_EA_WORKER_ROLLOUT_PENDING` hold releases
(release-on-restart ceremony, out of this task's scope; hard constraint forbids direct
compile / Factory_OFF). The residual work is carried append-only by pending rows
`7022a056` and `24e3a252`; the doc's "Next step for the reviewer" correctly specifies the
`--append-only-rerun-of` Q02 rerun to run once those compile. No verdict mutated, nothing
enqueued to Q02, factory not toggled.

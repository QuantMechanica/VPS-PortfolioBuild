# greview-36003 — independent Codex review of QM5_36003

## Decision

- Router task: `019d50ff-a716-46a4-b097-c5c650dea63b`
- Reviewed build: `QM5_36003_nnfx-hull-ma-zerolag-macd-stc`
- Builder: Gemini / agy (cross-review rule: Codex reviews Gemini builds)
- Approved card: `D:\QM\strategy_farm\artifacts\cards_approved\QM5_36003_nnfx-hull-ma-zerolag-macd-stc.md`
- Review date: 2026-08-24
- **Verdict: RECYCLE**

The strategy mechanism is substantially card-faithful and all 20 declared
`strategy_*` inputs are live, but the build is not formally clean for Q02. The
current source fails the hardened indicator-buffer gate at seven use-sites, the
card's maximum three-tick market-order slippage is not wired, all three setfiles
are stale against the current input contract, and the delivered EX5 is the
2026-08-17 binary while the MQ5 changed twice on 2026-08-23. The named binary
fix is: **recompile via governed COMPILE_EA** after the source and setfile defects
below are repaired.

The router `list-tasks --agent gemini` payload supplied only `ea_id=36003` and
`slug=nnfx-hull-ma-zerolag-macd-stc`; it omitted the expected `ea_dir`,
`card_path`, and `artifact` keys. Those were resolved deterministically to the
paths above and to `artifacts/builds/019d50ff-a716-46a4-b097-c5c650dea63b.json`.

## Card and source review

### Mechanism: PASS, except for the slippage control below

- The approved card requires closed D1 bar evaluation and the HMA / ZeroLag
  MACD / STC / high-volume conjunction for long and short entries
  (`D:\QM\strategy_farm\artifacts\cards_approved\QM5_36003_nnfx-hull-ma-zerolag-macd-stc.md:74-94`).
  The source evaluates the completed D1 bar and implements the symmetric entry
  rules at `framework/EAs/QM5_36003_nnfx-hull-ma-zerolag-macd-stc/QM5_36003_nnfx-hull-ma-zerolag-macd-stc.mq5:197-216,242-278,292-397`.
- The card's 1 ATR stop, 50% partial at +1 ATR, break-even protection, and
  opposing ZeroLag MACD crossover exit (`card:96-100`) are implemented at
  `.mq5:379-395,404-495,500-594`.
- The spread, UTC rollover, realized daily-loss entry halt, and one-position
  controls (`card:83-88`) are implemented at `.mq5:121-165,181-183`.
- No ML, raw indicator handle, invented commission, or invented swap logic was
  found. The only raw series reads are bounded `CopyRates` calls on closed-bar
  paths with `perf-allowed` annotations (`.mq5:210,523`).

### Every declared strategy input: PASS (no dead input)

Command used:

```powershell
$names = Select-String <mq5> -Pattern '^input\s+(?:\w+\s+)?(?<name>(?:strategy_|Inp)[A-Za-z0-9_]+)'
foreach ($name in $names) { rg -n -w --fixed-strings $name <mq5> }
```

Result: `COUNT=20 UNIQUE=20`; each declaration has executable use-sites:

| Input | Executable use-site lines |
|---|---|
| `strategy_hma_period` | 185, 354 |
| `strategy_zl_macd_fast` | 186, 229, 512, 532 |
| `strategy_zl_macd_slow` | 186, 200, 211, 230, 512, 516, 524, 533 |
| `strategy_zl_macd_signal` | 187, 211, 231, 513, 524, 534 |
| `strategy_stc_fast` | 188, 232 |
| `strategy_stc_slow` | 188, 201, 212, 233 |
| `strategy_stc_cycle` | 189, 212, 301, 333 |
| `strategy_stc_smoothing` | 190, 317, 349 |
| `strategy_stc_long_level` | 373 |
| `strategy_stc_short_level` | 387 |
| `strategy_better_volume_lookback` | 191, 215, 360, 362 |
| `strategy_atr_period` | 130, 192, 355 |
| `strategy_stop_atr_mult` | 192, 379, 393, 407, 467 |
| `strategy_tp1_atr_mult` | 193, 408, 467 |
| `strategy_tp1_fraction` | 193-194, 408-409, 475 |
| `strategy_be_buffer_pips` | 448-449, 484-485 |
| `strategy_spread_atr_mult` | 155 |
| `strategy_daily_loss_halt_pct` | 161-162 |
| `strategy_daily_hard_stop_pct` | 114 |
| `strategy_total_dd_stop_pct` | 115 |

### Framework conformity: mostly PASS

- Shared include chain: `#include <QM/QM_Common.mqh>` (`.mq5:5`).
- Backtest defaults: `RISK_PERCENT=0`, `RISK_FIXED=1000` (`.mq5:47-48`).
  Live percent-risk remains setfile-controlled through `QM_FrameworkInit`
  (`.mq5:610-628`).
- Magic is initialized through the framework and each entry assigns the
  relative host slot (`.mq5:112-116,137-138,178,612-627`). Registry rows
  `framework/registry/magic_numbers.csv:17434-17436` are slots 0/1/2 with
  magics 360030000/360030001/360030002, exactly `36003*10000+slot`. Parsing
  `QM_MagicResolver.mqh` found the same three rows at array indices
  17699-17701.
- MAE is sampled before early returns (`.mq5:642-648`). The two-axis news gate
  is initialized at `.mq5:612-627` and blocks entries only after management and
  exits (`.mq5:659-693`).
- `python tools/strategy_farm/validate_build_guardrails.py
  framework/EAs/QM5_36003_nnfx-hull-ma-zerolag-macd-stc` returned `PASS`, four
  files checked, zero findings, checked at `2026-08-24T11:44:51.478614Z`.

The resolver dry-run was read-only but globally refused because this worktree
lacks EA directories 1001, 1015, and 1016. That is unrelated to 36003; direct
CSV/resolver parsing proves all three 36003 rows and formulas above.

## Actionable recycle defects

### 1. Hardened buffer-bound gate fails (blocking)

Read-only invocation:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_36003_nnfx-hull-ma-zerolag-macd-stc
```

The JSON report contains seven `EA_INDICATOR_BUFFER_UNBOUNDED` failures:

- `.mq5:304` — `min_queue[min_head]`
- `.mq5:306` — `max_queue[max_head]`
- `.mq5:372` — `zl_macd[latest]`
- `.mq5:372` — `zl_signal[latest]`
- `.mq5:373` — `stc_value[latest]`
- `.mq5:571` — `zl_macd[previous]`
- `.mq5:571` — `zl_signal[previous]`

Named fix: add explicit `ArraySize` proofs/guards tied to the indexed dynamic
buffers (including queue head bounds) before these accesses, then rerun the
hardened gate to zero failures. Do not weaken D10.

### 2. Card slippage cap is not wired (blocking card-fidelity defect)

The card specifies maximum slippage of 3.0 ticks (`card:111-115`). The EA has
no input or call that supplies that limit. Its legacy `QM_FrameworkInit`
ultimately invokes `QM_EntryConfigure(..., 20, ...)` at
`framework/include/QM/QM_Common.mqh:296-297`; the entry layer stores 20 points
at `framework/include/QM/QM_Entry.mqh:63,88` and assigns it to
`trade_req.deviation` at `QM_Entry.mqh:452-463`.

Named fix: wire the card's three-tick ceiling through the governed framework
entry configuration using symbol tick-size-to-point conversion; do not leave
the generic 20-point default in force and do not invent a different tolerance.

### 3. All three setfiles are stale against the current EA input contract

The files exist and have correct Q02 risk values (`RISK_FIXED=1000`,
`RISK_PERCENT=0`), but each contains seven obsolete keys and omits the seven
current equivalents:

```text
obsolete/unknown: strategy_stc_length, strategy_stc_long_thresh,
strategy_stc_short_thresh, strategy_vol_avg_period, strategy_sl_atr_mult,
strategy_total_dd_halt_pct, strategy_per_trade_risk_cap_pct

missing current: strategy_stc_cycle, strategy_stc_smoothing,
strategy_stc_long_level, strategy_stc_short_level,
strategy_better_volume_lookback, strategy_stop_atr_mult,
strategy_total_dd_stop_pct
```

Their header hashes are `ed45f7ef...`, `d8ccaf2d...`, and `7b87ab2a...`; none
matches the current MQ5 SHA256 `ce115153...` or EX5 SHA256 `baffb41f...`.
This contradicts the generator claim in
`docs/ops/evidence/019d50ff_qm5_36003_build_ea_result_2026-08-23.md:28`.

Named fix: after finalizing the source and governed compilation, regenerate all
three setfiles via `framework/scripts/gen_setfile.ps1`; require only current
declared strategy keys and bind the governed build hash.

### 4. Delivered EX5 is stale relative to the current MQ5 (blocking)

Current identities:

| Artifact | SHA256 / evidence |
|---|---|
| current MQ5 | `ce11515348609e6871937250055bfc6cf36feae57734b7466cd36ebb9c10b60d` |
| delivered EX5 | `baffb41f8b8af18990919b622acf8c27d5c98a4543dc2c9160f7b020a1a3be47` |
| build artifact's MQ5 | `197a2c09c011c443cfcbb61a0487c34b135a2d6923c06200d881321ca0895187` (`artifacts/builds/019d50ff-a716-46a4-b097-c5c650dea63b.json:8`) |
| build artifact's EX5 | `baffb41f...` (`artifact:10`) |
| only compile receipt | MQ5 mtime `2026-08-17T21:06:02Z`, EX5 mtime `2026-08-17T21:06:48Z` (`D:\QM\reports\compile\QM5_36003_nnfx-hull-ma-zerolag-macd-stc\result.json:7-15`) |

There is no `build_identity.json` in the EA directory. Git history independently
confirms the EX5 was last committed at `e0f0d81a719aed620141a2d5514dd50dc38b7852`
on 2026-08-17, while the MQ5 was changed at
`a5854b0ebd59012f3298248e271d2d2e067417a0` and
`204b9f8606c2b344220f12174dffbafd053acc6a` on 2026-08-23 without an EX5
update. The build artifact's unbound `compile_succeeded: true` claim
(`artifact:18-21`) does not supersede the dated compile receipt or byte hashes.

Named fix: after defects 1-3 are resolved, **recompile via governed COMPILE_EA**,
produce a fresh compile receipt/build identity binding the final MQ5 and EX5
hashes, refresh the build artifact, and resubmit for cross-review. No ad-hoc
compile was run during this review.

## Test evidence

Full relevant module run:

```text
python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_gen_setfile.py
.......F....................................                      [100%]
1 failed, 50 passed in 547.33s
```

The single failure is outside this ticket:
`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven D10
findings in `QM5_41134_*` / `QM5_41135_*`. It does not mask or cause the seven
independently reproduced 36003 failures.

Focused tests for the exact hardening predicate, guardrail validator, and
governed setfile generator:

```text
python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py::test_d10_indicator_buffer_bound_pass_and_fail_fixtures tools/strategy_farm/tests/test_build_gate_hardening.py::test_build_prompt_requires_runtime_array_and_copybuffer_bounds tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_gen_setfile.py
.......................                                                  [100%]
23 passed in 10.90s
```

## Safety, rollback, and close

- Review actions were read-only until this note and the required router close.
  No compile, backtest enqueue/delete, factory toggle, verdict-row overwrite,
  gate-criteria change, or T_Live access occurred.
- This review commit contains only this note. Rollback is
  `git revert <review-commit>`; because the commit is single-file, that removes only this evidence
  note. Router close history is append-only and must be corrected only by a new
  explicitly authorized router action, never by overwriting a verdict row.
- Close state: `RECYCLE`.
- One-line router verdict: `RECYCLE: add D10 ArraySize guards, wire the card's
  3-tick slippage cap, regenerate current-key setfiles, then recompile via
  governed COMPILE_EA and bind fresh MQ5/EX5 hashes.`

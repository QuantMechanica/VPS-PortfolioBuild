# QM5_11592 FX fallback build and Q02 enqueue — 2026-07-31

## Outcome

The governed FX cointegration frontier has no eligible unbuilt pair, and the
two anchor baskets are not blocked at Q02. The branch therefore used the
mission's fallback and advanced the existing approved FX card
`QM5_11592_robo-3candle-fade-h4` into Q02.

Two row-bound Q02 work items were created by `record-build`; neither was
manually dispatched:

| Symbol | Timeframe | Work item | State at handoff |
|---|---|---|---|
| `EURUSD.DWX` | H4 | `b5b103e8-5421-48c5-b2b3-030ed278b619` | `pending` |
| `GBPUSD.DWX` | H4 | `c97bfc5b-5119-406e-90b1-5a0de9bf1772` | `pending` |

Build task: `41bd08c6-1520-471f-a614-36c7dc9d3b9f` (`done`).
The auto-enqueue result reported two enqueues and zero skips.

## Why the cointegration route was not duplicated

- `docs/research/CROSS_ASSET_FX_DISCOVERY_2026-06-09.md` records only the two
  positive-beta survivors of the governed 66-pair scan:
  `AUDUSD~NZDUSD` (`QM5_12532`) and `EURJPY~GBPJPY` (`QM5_12533`).
- `docs/research/FX_COINTEGRATION_FRONTIER_DUPLICATE_GUARD_2026-07-24.md`
  records that the original and strict sign-aware extensions are fully
  mechanized. The wider approved cointegration cards also have EA builds.
- Current row-bound evidence confirms the anchors are past Q02:
  `QM5_12532` has Q02 PASS, Q04 PASS, then Q05 FAIL;
  `QM5_12533` has Q02 PASS, then Q04 FAIL.
- Requeueing a terminal strategy failure or building another sleeve for an
  already-mechanized pair would be duplicate work.

An initial fallback check also reconciled the stale compile record for
`QM5_11561`, but its Q02 enqueue was correctly rejected by the farm's
`requeue_excluded_eas.txt` high-frequency FX policy. That exclusion was not
bypassed.

## Card and source contract

`QM5_11592` is not a new strategy extraction. It is a schema repair of an
existing `g0_status: APPROVED` card. The branch-tracked canonical mirror fixes
the legacy numeric `ea_id` while retaining the approved source and mechanics:

- Source: RoboForex educational team, *RoboForex Strategy Collection* (2020),
  page 107, “Strategy Three Candles.”
- R1–R4: PASS in the governed card.
- Signal: fade three strictly consecutive H4 closes using closed OHLC only.
- No ML, oscillator entry, adaptive rule, grid, martingale, or external feed.
- Declared 80 candidate runs per year per symbol before one-position, news,
  spread, and Friday gates; this is a bounded H4 sleeve, not an intraday
  high-frequency/scalping system.

The schema-repaired card passed:

```text
python framework/scripts/skill_card_schema_lint.py \
  --card strategy-seeds/cards/approved/QM5_11592_robo-3candle-fade-h4_schema-repair.md
status=ok, ml_hits=[], missing_sections=[]
```

## Mechanical build repair

- Activated the pre-allocated EA registry row and the two pre-allocated magic
  rows: EURUSD slot 0 / `115920000`, GBPUSD slot 1 / `115920001`.
- Regenerated `QM_MagicResolver.mqh`. The generator's reviewed recovery mode
  retained the already-existing omission of IDs 1001, 1015, and 1016, whose EA
  directories were missing before this work; no new omission was introduced.
- Added the required V5 `SPEC.md` and a build-time card copy.
- Added symbol/timeframe guards.
- Fully initialized `QM_EntryRequest` and bound `symbol_slot` to the setfile's
  registered slot.
- Kept trailing and opposite-signal exits active through news/spread entry
  blocks.
- Repaired both setfiles, including the GBPUSD slot-1 mapping and all declared
  strategy inputs.

This is a per-symbol EA, not a logical basket; a `basket_manifest.json` is
therefore not applicable.

## Fixed-risk evidence

Both Q02 setfiles declare:

```text
risk_mode: FIXED
RISK_FIXED=1000
RISK_PERCENT=0
PORTFOLIO_WEIGHT=1
```

Final setfile hashes:

| Symbol | SHA-256 |
|---|---|
| EURUSD | `91970335fcc2cc239c91119db4e3168d42e87430123500fe4e51e4cf1482788f` |
| GBPUSD | `f06f4064749d2f544828410ada620e806e7254747ef412b9754440c6bc0ee29c` |

## Validation

```text
validate_spec_doc.py: PASS (1/1)
skill_card_schema_lint.py: PASS
compile_one.ps1 -Strict: PASS, 0 errors, 0 warnings
  log: C:\QM\repo\framework\build\compile\20260731_071507\
       QM5_11592_robo-3candle-fade-h4.compile.log
build_check.ps1 -SkipCompile: PASS, 0 failures, 0 warnings
  report: D:\QM\reports\framework\21\build_check_20260731_072039.json
validate_symbol_scope.py: SINGLE_SYMBOL_OK, 0 violations
farm record-build: done; 2 Q02 rows enqueued; 0 skipped
```

No manual tester run was launched. The paced fleet owns dispatch, and the
legacy scheduler dry-run showed spare capacity before enqueue, so no backtest
CPU ceiling was hit by this work.

## Safety boundary

- No `T_Live` file or manifest changed.
- AutoTrading was not toggled.
- No portfolio admission, KPI, Q08 contribution, or portfolio gate artifact
  changed.
- No live setfile or deployment artifact was created.

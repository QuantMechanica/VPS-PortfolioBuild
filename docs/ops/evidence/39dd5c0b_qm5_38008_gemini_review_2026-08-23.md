# QM5_38008 mandatory Codex review — 2026-08-23

- Review task: `39dd5c0b-7157-403d-8f63-31117609e27f`
- Gemini build task: `1f82ac59-1c1e-4e72-a1f8-4eaea2120347`
- EA: `QM5_38008_codetrading-optimized-bollinger-trend-breakout`
- Build packet: `artifacts/builds/1f82ac59-1c1e-4e72-a1f8-4eaea2120347.json`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_38008_codetrading-optimized-bollinger-trend-breakout.md`
- Reviewed MQ5 SHA-256: `5c8f45f1db08a51e886e0a16c192d15b9db589bd0dc531d04e0cc7649a9ea9b8`
- Reviewed EX5 SHA-256: `76b2612029a45a549a2e5e7da40f755afb3ba3c346804f3cbecbeae18601165e`
- Disposition: `CHANGES_REQUIRED`; remain in `REVIEW`, with no pipeline promotion.

## Verified progress

- The prior fixed 5R target is removed: `req.tp=0.0`, retaining the card's open-ended midline exit.
- The open-position count is now entry-only, so management is no longer statically deadlocked.
- Default 2.0%/2.5%/5.0%/0.5% capital-preservation inputs are present and passed to `QM_KillSwitchInit`.
- `_build_review_dispatch_gate(...)` returns `BUILD_REVIEW_DISPATCH_PASS`; generic guardrails pass for the MQ5 and three sets at the mandatory 336-hour news ceiling; SPEC structure passes.
- All three setfiles use `RISK_FIXED=1000` and `RISK_PERCENT=0`; the authorized symbols are SP500.DWX, NDX.DWX, and XTIUSD.DWX.

Those improvements do not make the packet acceptable.

## Blocking findings

1. **The executable and presets are not bound to the reviewed source.** Commit `922d991ad` changed the MQ5 on 2026-08-23, but the retained EX5 is dated 2026-08-18. The current MQ5 hash occurs only in the packet, not in a strict compiler result. The three setfile `build_hash` values are mutually different (`23577e...`, `58c52b...`, `d0b524...`) and none equals the reviewed MQ5 hash. The packet still claims `compile_succeeded=true`, while smoke is deferred and `smoke_report_path` is null. The dispatch gate is therefore only structural, not build provenance.

2. **The canonical hardening gate fails the GMT contract.** `StrategyInRolloverWindow(TimeCurrent())` at MQ5 line 129 compares raw broker time to the card's 23:55–00:05 GMT window. Fresh `build_gate_hardening.py` reports `EA_BROKER_TIME_USED_FOR_GMT_WINDOW`; no `QM_BrokerToUTC` conversion is present.

3. **Restart and first-bar state are unsafe.** `OnInit()` does not seed Bollinger/EMA/ATR state. Management at line 321 therefore cannot execute the approved midline exit after restart until a chart-new-bar event. On admission, `Strategy_NoTradeFilter()` runs at line 352 before `AdvanceState_OnNewBar()` at line 355; with startup ATR zero, the spread ceiling at line 140 is skipped and entry can follow after state population without rechecking the filter. `AdvanceState_OnNewBar()` also returns without clearing prior cached values when a bar read fails, permitting stale signal/management state.

4. **D1 cadence is not bound to D1.** The source calls parameterless `QM_IsNewBar()` at line 349 and does not declare a D1 execution contract. A non-D1 chart can therefore govern when D1 signals and exits refresh, contrary to the card.

5. **The three-tick market-order ceiling is still absent.** There is no slippage input or `QM_EntryConfigure` call, leaving framework default deviation rather than the card's maximum three trade ticks.

6. **The implementation adds a non-adjudicated break-even exit and permits weakened controls.** The card's exact exit rules specify the 2×ATR stop and midline close, not a +1R break-even move; the source and SPEC add one. Separately, the SPEC advertises daily hard stop up to 5%, total drawdown up to 10%, and per-trade risk up to 1%, while the source has no validation that caps inputs at the approved 2.5%/5.0%/0.5% contract.

## Required repair

- Reorder closed-bar refresh before both management and admission; seed state at initialization, clear it before reads, and bind new-bar cadence to D1.
- Convert rollover to UTC and wire a maximum three-trade-tick deviation.
- Remove the unauthorized break-even behavior or obtain explicit card adjudication; enforce fail-closed upper bounds for all loss/risk inputs.
- Regenerate all setfile hashes, compile through the governed compiler, and return with a strict compiler record that binds the current MQ5 and EX5 hashes. Do not claim smoke without a report.

No source was changed in this review. No Q phase, backtest, terminal launch, AutoTrading action, or T_Live action occurred.

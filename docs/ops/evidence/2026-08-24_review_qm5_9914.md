# QM5_9914 bandy-zlema-distance-trend — Claude review-lane close

Date: 2026-08-24 UTC
Router task: `3ba5f88c-e843-4969-b8b7-152a38d240e9` (`review_ea`, assigned codex, source `gemini`/agy)
Source build task: `25102f3e-14f6-4a82-b25d-1805dd49ce14`
Reviewed artifact: `D:/QM/strategy_farm/artifacts/builds/25102f3e-14f6-4a82-b25d-1805dd49ce14.json`
Worker verdict: "mq5 drafted, SPEC validated, 13 setfiles generated, static guardrails PASS, committed".

## Verdict: RECYCLE — actionable defects; do not promote to Q02/PIPELINE

Independent Claude-lane review against the approved card
(`QM5_9914_bandy-zlema-distance-trend.md`; present only in
`D:/QM/backups/cards_2026-08-23T13*_pre_translation/cards_approved/` — see repo-state note),
the mq5 source, SPEC.md, 13 setfiles, magic registry, and the producer artifact. Findings
below were verified directly (file:line), independent of the prior same-day Codex note
(`2026-08-23_qm5_9914_gemini_code_review.md`), and concur with its REQUEST_CHANGES.

### Defects (must fix before re-review)

1. **[Build — decisive] 12 of 13 setfiles bound to a stale build_hash.**
   Current mq5 SHA-256 = `92bbba767ef24e3c7307ccfb73685261d38110c57782b1eaf2ccaf251031474e`.
   Only the EURUSD setfile carries that `build_hash`; the other 12 carry divergent hashes
   (e.g. AUDUSD `e37fe436…`, GDAXI `35d81dd4…`, WS30 `e7c49312…`). The hash-bound dispatch
   gate would reject or mis-identify 12/13 cells. Regenerate all 13 against one exact mq5
   hash via `framework/scripts/gen_setfile.ps1` (governed generator).

2. **[Code] Exits are unreachable whenever the no-trade filter trips.**
   `OnTick` returns at `.mq5:232` (`if(Strategy_NoTradeFilter()) return;`) BEFORE
   `Strategy_ManageOpenPosition()` at `.mq5:235`. Management holds the card's primary exit
   (ZLEMA cross-back, `.mq5:180-187`) and the 30-D1-bar time stop (`.mq5:171-174`). A wide
   spread (`strategy_spread_max_atr`, `.mq5:91`) or warmup shortfall (`.mq5:82`) therefore
   defers approved exits. Move management ahead of the no-trade gate / make exits
   independent of entry eligibility. (Rarely trips in a D1 backtest with modeled spread, but
   it is a latent correctness defect.)

3. **[Code — card faithfulness] Invented spread filter, card filter not implemented.**
   `strategy_spread_max_atr = 0.25` (`.mq5:45`, used `.mq5:91`) is not in the card. The
   card's only optional filter is a 2-bar same-sign distance confirmation, which is NOT
   implemented. Remove the spread filter (or obtain approval) and, if desired, implement the
   card's confirmation filter.

4. **[Cohort vs card] Universe deviates from the approved card.**
   Card authorizes FX majors, XAUUSD, oil CFD (XTIUSD), NDX.DWX, WS30.DWX, SP500.DWX
   (backtest-optional). Delivered cohort adds **GDAXI.DWX** and **UK100.DWX** and **omits
   XTIUSD.DWX**. Root is the 2026-08-17 "Codex governed allocator" rows in
   `framework/registry/magic_numbers.csv:17059,17062` (GDAXI slot0, UK100 slot3; no XTIUSD),
   not the gemini code — the build faithfully matched the registry. Candidate-pool definition
   is ROT: realign registry+cohort to the card or obtain an OWNER `target_symbols` amendment.

5. **[Live hardening, non-blocking for D1 backtest] Stop ATR read at PERIOD_CURRENT.**
   `QM_StopATR` (`.mq5:124,139`) → `QM_StopRulesReadATRValue` → `QM_ATR(sym, PERIOD_CURRENT,…)`
   (`framework/include/QM/QM_StopRules.mqh:166,55-71`), whereas the distance trigger reads
   `PERIOD_D1` (`.mq5:107`). Identical on a D1 chart (all backtest sets are `_D1_backtest.set`),
   but the catastrophic backstop would resize on any non-D1 attachment. Bind stop ATR to D1
   and validate D1 in `OnInit`.

### Checks that passed (identity + static conformity only; no pipeline verdict inferred)
- QM_Common include chain (`.mq5:5`); MAE hook `QM_FrameworkTrackOpenPositionMae()` (`.mq5:220`).
- RISK_FIXED=1000 / RISK_PERCENT=0 (`.mq5:19-20`); all 13 setfiles `RISK_FIXED>0`, `RISK_PERCENT=0`.
- Magic via `QM_FrameworkMagic()` (`.mq5:102`), 13 active rows slots 0-12, `ea_id*10000+slot`.
- News filter wired (`.mq5:253-259`); Friday close (`.mq5:229`); kill switch (`.mq5:222`).
- ZLEMA recursion is a bounded loop with `perf-allowed` iClose annotations; closed-form
  `2*close-close[lag]` matches card `ZLEMA(20), N/2=10`. No ML, no invented commission/swap.
- **No dead inputs:** all 9 `strategy_*` inputs have use-sites beyond declaration
  (zlema_period/lag `.mq5:109`; atr_period `.mq5:90,107,124,139`; distance_atr_mult
  `.mq5:116,131`; regime_sma_period `.mq5:108`; sl_atr_mult `.mq5:124,139`; time_stop_days
  `.mq5:171`; spread_max_atr `.mq5:91`; warmup_bars `.mq5:82,99`).
- Producer artifact `compile_succeeded=true`, `build_check_passed=true`; mq5/ex5 hashes match
  artifact (ex5 `fcffbe00…`). SPEC 7-section structure present.

### Repo-state note (not a build defect)
The approved card is absent from the repo working tree and git history; it exists only under
`D:/QM/backups/cards_*_pre_translation/cards_approved/QM5_9914_bandy-zlema-distance-trend.md`
(a 2026-08-23 "pre_translation" backup sweep). The mq5 header (`.mq5:9`) and SPEC point to
`artifacts/cards_approved/…`, which does not exist. Restore the card to
`artifacts/cards_approved/` so automated card-faithfulness checks resolve on re-review.

## Disposition
Read-only review. No source, binary, registry, resolver, setfile, work item, trade stream,
or T_Live state was modified. Task closed RECYCLE; corrected code + regenerated setfiles +
registry/card realignment require a fresh mandatory review.

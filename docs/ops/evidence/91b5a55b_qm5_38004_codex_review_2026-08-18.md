# QM5_38004 Codex review evidence — 2026-08-18

- Review task: `91b5a55b-0eca-44ab-834a-1e0da4042bef`
- Source build task: `952a07fb-63c7-4fc3-9582-83096a13c9e0` (Gemini)
- EA: `QM5_38004_codetrading-triple-ema-momentum-scalper`
- Disposition: `FAIL_CODE_REVIEW`; remain in `REVIEW`

## Verification

- Mechanical pre-screen: PASS (artifacts, freshness, three active magic rows, setfiles, fixed-risk mode, input wiring)
- Build guardrails: PASS, four files checked, no findings, news staleness ceiling 336 hours
- Fresh compile: PASS, 0 errors, 0 warnings
- Card-to-code review: FAIL

## Blocking findings

1. `Strategy_NoTradeFilter()` rejects while this magic has an open position (MQ5 line 118), and `OnTick()` returns on it at line 303 before `Strategy_ManageOpenPosition()` at line 306. The approved EMA21 trailing stop cannot execute while a position exists; later strategy exits would also be unreachable.
2. `OnTick()` applies the ATR spread filter before the new-bar state refresh at line 333. On startup `g_last_atr` is zero; the first signal can therefore be refreshed and opened without the card-required current-spread check. Later entries can likewise use stale pre-refresh ATR admission.

## Required repair

- Separate entry admission from management/exits so open positions remain manageable.
- Refresh closed-bar state before admission and recheck current spread against the refreshed ATR immediately before entry.
- Recompile and regenerate bound evidence; later Codex review remains mandatory.

No Q phase or pipeline verdict is inferred. No terminal, T_Live, or AutoTrading action occurred.

# greview-34005 — independent review evidence

## Disposition

- Ticket: `greview-34005`
- Router task: `a0f709cd-2c0b-40db-a01b-372c715beef9`
- Builder / cross-review rule: Gemini build, Codex review
- EA: `QM5_34005_sokolov-cstrategy-donchian-atr-breakout`
- Approved card of record: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_34005_sokolov-cstrategy-donchian-atr-breakout.md`
- Verdict: **RECYCLE**
- One-line verdict: `RECYCLE: wire the card's 2%/2.5%/5% loss controls, convert the 23:55-00:05 blackout to GMT, obtain an approved deterministic parabolic-ATR trail contract and implement it, then recompile through governed COMPILE_EA with a durable 0e/0w hash-bound receipt.`

This is not formally clean for Q02. No EA, card, setfile, registry, queue, verdict row, factory control, backtest, or `T_Live` file was changed by this review.

## Authority and payload resolution

The required context was read before review:

- `docs/ops/rebaseline/GATE_MANIFEST_V4_LINEAR_PROPOSAL_2026-08-23.md`
- `docs/ops/rebaseline/FACTORY_AUTOMATION_INVENTORY_2026-08-23.md`
- `docs/ops/rebaseline/GATE_NAME_CENSUS_2026-08-23.md`
- `G:/My Drive/QuantMechanica - Company Reference/03 Pipeline/Pipeline Rebaseline Directive 2026-08-23.md`

Command:

```text
python tools/strategy_farm/agent_router.py list-tasks --agent gemini
```

The matching task row is `state=REVIEW`, `task_type=build_ea`, `assigned_agent=gemini`; its payload contains `ea_id=34005`, `slug=sokolov-cstrategy-donchian-atr-breakout`, and `target_agent_profile=codex`. Contrary to the ticket's payload description, the row does not contain `ea_dir`, `card_path`, or `artifact`. Those were resolved without guessing from:

- EA directory: the sole `framework/EAs/QM5_34005_*` match.
- Card: the sole `D:/QM/strategy_farm/artifacts/cards_approved/QM5_34005*.md` match.
- Artifact: `artifacts/qm5_34005_build_result.json:2-6`, which binds this exact task ID and EA directory; the earlier generated review task `feb8cb93-40b3-4eef-b8ce-0edf12b4207f` also names it as `source_artifact_path` and was already closed `RECYCLE` for the card-undefined trail.

The runtime approved card and repository approved card are text-identical after Git-canonical LF normalization (both SHA-256 `47446101071caac73b08da4345fdd374009c6b6e0e6792557d28cdeeb1b52909`).

## Rejecting findings

### 1. Three card-declared loss controls are absent

The approved card requires a 2.0% daily realized-loss entry halt (`D:/QM/strategy_farm/artifacts/cards_approved/QM5_34005_sokolov-cstrategy-donchian-atr-breakout.md:87`), a 2.5% maximum daily drawdown stop (`:112`), and a 5.0% maximum total drawdown stop (`:113`). The EA declares no matching controls in its inputs (`framework/EAs/QM5_34005_sokolov-cstrategy-donchian-atr-breakout/QM5_34005_sokolov-cstrategy-donchian-atr-breakout.mq5:13-44`) and has only the generic kill-switch call at `:227`.

The current mechanical hardening check returned these three formal failures:

```text
EA_CARD_LOSS_LIMIT_MISMATCH: ... card daily_entry_halt=2% but matching input is absent; candidates=none.
EA_CARD_LOSS_LIMIT_MISMATCH: ... card daily_hard_stop=2.5% but matching input is absent; candidates=none.
EA_CARD_LOSS_LIMIT_MISMATCH: ... card total_drawdown_stop=5% but matching input is absent; candidates=none.
```

Named fix: add and wire the exact card-authorized controls, with entry-halt versus hard-liquidation behavior kept distinct, or obtain an OWNER-approved card correction before rebuilding. Re-run `build_gate_hardening.py` until all three failures are absent.

### 2. The GMT rollover blackout uses raw broker time

The card specifies `23:55` through `00:05` **GMT** (`D:/QM/strategy_farm/artifacts/cards_approved/QM5_34005_sokolov-cstrategy-donchian-atr-breakout.md:86`). The EA calls `TimeCurrent()` and compares its raw hour/minute at `framework/EAs/QM5_34005_sokolov-cstrategy-donchian-atr-breakout/QM5_34005_sokolov-cstrategy-donchian-atr-breakout.mq5:63-66`; it never calls `QM_BrokerToUTC`.

The current hardening check therefore also returned:

```text
EA_BROKER_TIME_USED_FOR_GMT_WINDOW: ... has a card-declared GMT/UTC clock window but compares raw TimeCurrent/iTime hours without QM_BrokerToUTC or a documented equivalent.
```

Named fix: convert broker time with `QM_BrokerToUTC` before deriving `hhmm`, then test the wraparound GMT interval exactly.

### 3. The approved trailing-stop contract is not deterministic enough to support the implementation

The card says only `Dynamic parabolic ATR trailing stop` (`D:/QM/strategy_farm/artifacts/cards_approved/QM5_34005_sokolov-cstrategy-donchian-atr-breakout.md:99`). It supplies no parabolic acceleration factor, cap, activation trigger, step rule, or equation. The EA instead calls `QM_TM_TrailATR(ticket, strategy_atr_period, strategy_sl_atr_mult)` at `framework/EAs/QM5_34005_sokolov-cstrategy-donchian-atr-breakout/QM5_34005_sokolov-cstrategy-donchian-atr-breakout.mq5:175`. That helper is a conventional ratcheting ATR stop: market price minus/plus `ATR * atr_mult`, improving only (`framework/include/QM/QM_TradeManagement.mqh:483`, `:505-518`). It is not a defined parabolic mechanism.

This was also the substantive defect in prior Codex cross-review `feb8cb93-40b3-4eef-b8ce-0edf12b4207f`, whose router verdict was `RECYCLE` for the `card-undefined parabolic trail`.

Named fix: Research/OWNER must approve an exact trailing equation and parameters; Development must then implement that contract. Do not invent the missing parabolic mechanics in code.

### 4. Durable strict-compile evidence is missing

The builder artifact asserts `build_check_passed=true` and `compile_succeeded=true` (`artifacts/qm5_34005_build_result.json:19-20`) but does not bind a compile log. The surviving compiler result is only `COMPILED_CACHED` (`D:/QM/reports/compile/QM5_34005_sokolov-cstrategy-donchian-atr-breakout/result.json:3-4`); its `compile_one_exit_code`, error count, and warning count are all `null`, and `compile_log_path` is empty (`:9-12`). There is no `build_identity.json` in the EA directory.

The binary itself is not stale: the worktree `.mq5` byte hash differs only because `core.autocrlf=true`; normalized to Git-canonical LF it is `e5fe5061fa998a2e4935603e55c022133048264f4e96767a4c12679fd4f48da6`, exactly the source hash recorded at `artifacts/qm5_34005_build_result.json:29`. The delivered `.ex5` SHA-256 is `64bab99b447e2c40d92adcf7b33d4fc3e6405852601af1214ed236a51f7be425`, exactly `artifacts/qm5_34005_build_result.json:30`. A text diff between the canonical source and this worktree source is empty.

Named fix: after the three mechanism repairs, recompile through governed `COMPILE_EA` and retain a durable receipt binding the final `.mq5` and `.ex5` hashes with explicit `0 errors / 0 warnings`. This is required for evidence completeness, not because the presently delivered binary is semantically stale.

## Conformance findings that passed

- **Declared strategy inputs:** all seven have executable use-sites; no dead input was found:
  - `strategy_donchian_period`: declaration `mq5:38`, loop bound `:115`.
  - `strategy_atr_period`: declaration `:39`, reads `:99`, `:106`, trail `:175`.
  - `strategy_atr_ma_period`: declaration `:40`, loop/divisor `:104`, `:108`.
  - `strategy_sl_atr_mult`: declaration `:41`, initial stop `:132`, trail `:175`.
  - `strategy_tp_rr_mult`: declaration `:42`, TP distance `:133`.
  - `strategy_spread_atr_period`: declaration `:43`, spread ATR read `:68`.
  - `strategy_spread_atr_mult`: declaration `:44`, spread threshold `:76`.
- **Framework chain:** `QM_Common.mqh` is included (`mq5:6`), `QM_FrameworkInit` is called (`:195-210`), the direct MAE hook is first in `OnTick` (`:225`), and entries use `QM_TM_OpenPosition` (`:273`).
- **Magic:** active registry rows are `framework/registry/magic_numbers.csv:17383-17385`, slots `0..2`, magics `340050000..340050002`, satisfying `ea_id * 10000 + slot`. `compile_ea.ea_id_registered(...)` returned `(True, 34005)` and `magic_in_resolver(34005)` returned `True`. The entry request reads `qm_magic_slot_offset` (`mq5:89`, `:143`, `:155`); framework initialization resolves it via `QM_MagicChecked` (`framework/include/QM/QM_Common.mqh:223-227`).
- **News:** the two-axis inputs are passed into framework init (`mq5:203-210`) and entry is gated by `QM_NewsAllowsTrade2` / legacy fallback (`:255-261`).
- **Risk presets:** all three governed backtest setfiles are present and use `RISK_FIXED=1000`, `RISK_PERCENT=0`, with registry slots `0`, `1`, `2` (`sets/*.set:17-30`). They match the governed generator's header/assignment shape (`framework/scripts/gen_setfile.ps1:617-657`). No live setfile was delivered or reviewed; a later live preset must use `RISK_FIXED=0`, `RISK_PERCENT>0`.
- **Buffers/performance:** there are no EA-owned dynamic indicator buffers or `CopyBuffer` calls requiring `ArraySize` guards. The only raw series calls are the three bounded H4 reads at `mq5:117-123`, each carrying the required `perf-allowed` annotation and reached only after the single `QM_IsNewBar` gate at `:263`.
- **Forbidden mechanics/data:** scoped scan found no ML, external Python/API, grid/martingale/averaging/recovery, or invented commission/swap logic.
- **Guardrail validator:** `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_34005_sokolov-cstrategy-donchian-atr-breakout` returned `verdict=PASS`, `files_checked=4`, `findings=[]` at `2026-08-24T09:42:47Z`.
- **Setfile provenance:** the builder artifact enumerates the three generated setfiles (`artifacts/qm5_34005_build_result.json:13-17`); their headers and card-default assignments match the governed generator output. No setfile was regenerated during review.

## Read-only checks and tests

Commands executed from the worktree:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_34005_sokolov-cstrategy-donchian-atr-breakout
python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_34005_sokolov-cstrategy-donchian-atr-breakout
python framework/scripts/update_magic_resolver.py --dry-run
python -m pytest -q tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_magic_resolver_reconcile_newlines.py tools/strategy_farm/tests/test_setfile_canonicalization.py framework/scripts/tests/test_magic_resolver_symbol_fail_closed.py framework/scripts/tests/test_magic_resolver_strict_default.py framework/scripts/tests/test_magic_resolver_hash_newlines.py framework/scripts/tests/test_magic_resolver_binary_search.py
```

Results:

```text
build_gate_hardening: 4 scoped failures (the three loss-control failures and GMT-clock failure above)
validate_build_guardrails: PASS, 0 findings
pytest: 65 passed, 2 failed in 192.86s
```

The two pytest failures are pre-existing fleet/worktree failures outside QM5_34005 and outside this evidence-only change:

1. `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven `QM5_411xx` buffer defects.
2. `test_binary_lookup_is_equivalent_over_every_generated_row_and_misses` reports missing EA directories for registry IDs `1001`, `1015`, and `1016` in this partial worktree. The resolver dry-run independently refused for the same three dropped IDs. Scoped QM5_34005 registry and resolver checks passed.

No code or unit-test file was added because ticket step 4 explicitly authorizes committing **only this evidence file**; there is therefore no touched production module whose tests could be run separately.

## Rollback

Revert the single review commit containing this file with `git revert <review-commit>`. That removes only this evidence artifact. Router state is external append/audit state and must be corrected, if OWNER-authorized, through `agent_router.py`; do not edit `farm_state.sqlite` or overwrite verdict rows directly.

# rework-37006 — QM5_37006 review-fix evidence

**Ticket:** `rework-37006`
**Router task:** `06b9a3cb-a504-47c1-aa29-ef0b4c61f44c`
**EA:** `QM5_37006_cusum-filter-structural-breakout`
**Date executed:** 2026-08-24
**Scope:** build/review repair only; no backtest enqueue, verdict overwrite, factory toggle, registry mutation, or `T_Live` access.

## Work order

The repair is based on both review notes:

- `docs/ops/evidence/06b9a3cb_qm5_37006_gemini_build_codex_review_2026-08-18.md`
- `docs/ops/evidence/ac244df6_qm5_37006_resurrected_gemini_review_2026-08-23.md`

The router payload records the recycled verdict: `CUSUM recurrence omits card E[y_t] mean term; state resets before fill is known`, with tags for card drift, missing loss limits, and untracked build identity.

## What changed

| Review finding | Repair evidence |
|---|---|
| CUSUM omitted `E[y_t]` | `framework/EAs/QM5_37006_cusum-filter-structural-breakout/QM5_37006_cusum-filter-structural-breakout.mq5:109` calculates the rolling mean and standard deviation from the same bounded price-difference window; line 158 subtracts `mean_return` before both CUSUM recurrences at lines 159-160. |
| Signal reset before execution | `Strategy_EntrySignal` now only constructs the request. `OnTick` checks the boolean result of `QM_TM_OpenPosition` at line 564 and resets both accumulators only inside the success branch at lines 568-569. Rejected entries preserve state. |
| Missing 2.0% / 2.5% / 5.0% rails | Inputs begin at line 54. The realized-loss entry halt is implemented at line 313, the persisted total-equity baseline at line 325, and the total-drawdown predicate at line 346. `OnInit` reconfigures the framework kill switch with the 2.5% daily and 5.0% total rails at line 478. |
| Restart erased CUSUM | Terminal-local state uses a commit-marker write at line 178. `StrategyRebuildState` at line 239 restores the last completed-bar state or resets from the most recent confirmed strategy fill, replays only closed bars, refuses gaps over `strategy_state_rebuild_bars`, and seeds the framework new-bar tracker after replay. Tester runs remain process-local and deterministic. |
| Unapproved absolute spread cap | `strategy_max_spread_points` and its filter were removed. `Strategy_NoTradeFilter` at line 361 retains only the card-authorized `spread > 1.8 * ATR(14)[1]` rule. |
| Three-tick slippage ceiling absent | `StrategyDeviationPoints` converts the three-tick input through `SYMBOL_TRADE_TICK_SIZE / SYMBOL_POINT`; `QM_EntryConfigure` consumes it at line 470. |
| Framework conformity | Source retains the `QM_Common.mqh` chain, the direct MAE hook at line 510, `RISK_FIXED=1000` / `RISK_PERCENT=0` in all three backtest sets, and framework-host magic resolution (`req.symbol_slot=0` at line 389 after `QM_FrameworkInit` registry validation). Both `CopyRates` buffers are bounded and guarded by `ArraySize`. No ML dependency was introduced. |
| Declared inputs / presets | `tools/strategy_farm/tests/test_qm5_37006_review_rework.py:63` checks every declared input has an executable use-site. All three setfiles seal the repaired strategy defaults while retaining their existing magic slots and fixed-risk backtest mode. |

The implementation note was updated at `framework/EAs/QM5_37006_cusum-filter-structural-breakout/SPEC.md:13` and its v2 revision entry is at line 104. No approved Strategy Card or registry file changed.

## Validation evidence

Commands were run from `C:/QM/worktrees/rework-slot-4`.

1. Focused EA regression tests:

   ```text
   python -m pytest -q tools/strategy_farm/tests/test_qm5_37006_review_rework.py
   ......                                                                   [100%]
   6 passed in 1.76s
   ```

2. EA-scoped hardening:

   ```text
   python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_37006_cusum-filter-structural-breakout
   files_scanned=1; failures=[]; warnings=[]
   D2/D3/D4/D5/D6/D7/D8/D9/D10/D11/D13/D14/D15/D17/D18: PASS/no failures
   ```

3. Guardrail and SPEC validators:

   ```text
   python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 <MQ5 and three setfiles>
   verdict=PASS; all four paths findings=[]

   python framework/scripts/validate_spec_doc.py framework/EAs/QM5_37006_cusum-filter-structural-breakout
   PASS QM5_37006_cusum-filter-structural-breakout
   Summary: 1 PASS, 0 FAIL (of 1)
   ```

4. Broader hardening-module tests:

   ```text
   python -m pytest -q tools/strategy_farm/tests/test_qm5_37006_review_rework.py tools/strategy_farm/tests/test_build_gate_hardening.py
   1 failed, 34 passed in 385.49s
   ```

   The sole failure is outside this ticket: `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven existing findings beginning with `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`. The QM5_37006 tests all passed, and the EA-scoped hardening command above returned no findings.

5. Scoped authoritative build check:

   ```text
   framework/scripts/build_check.ps1 -EALabel QM5_37006_cusum-filter-structural-breakout -RepoRoot <worktree> -Strict
   BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED
   failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
   detail=terminal64 processes are alive; ad-hoc compile/build_check is refused
   ```

   A second scoped attempt with `-SkipCompile` was refused by the same preflight guard before validation began. The guard suggests enqueueing a compile, but this ticket explicitly forbids enqueueing backtests/work; no enqueue or factory/terminal mutation was performed. Consequently the tracked EX5 remains the prior binary and is not claimed as matching the repaired MQ5. Review must treat compile identity as an explicit open risk, not as PASS.

6. Repository hygiene:

   ```text
   git diff --check
   PASS (line-ending conversion warnings only; no whitespace errors)
   ```

## Rollback

Revert the single rework commit with `git revert <rework-37006-commit>`. This restores the prior MQ5, SPEC, and three setfiles and removes the focused test/evidence note. The EX5 and both deterministic registries were not changed. Router state is external to Git and must be transitioned separately through `agent_router.py` if review requests another recycle.

## Residual risk / reviewer action

- The repaired source could not receive a fresh EX5 identity while the live-factory compile guard was active. A governed, EA-scoped compile must be run when authorized/available before any pipeline handoff.
- The 512-bar restart ceiling is deliberately fail-closed: if persisted state is absent and the most recent confirmed entry is older than the ceiling, initialization fails instead of silently inventing CUSUM state.
- No runtime/backtest verdict is inferred or requested by this build-only repair.

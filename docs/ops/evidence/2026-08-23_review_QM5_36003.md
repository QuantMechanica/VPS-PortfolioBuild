# Claude EA review — QM5_36003 (nnfx-hull-ma-zerolag-macd-stc)

- Review task: `c8791ede-3188-4a25-8777-c755e0c71bd4` (state REVIEW, assigned claude)
- Build task: `019d50ff-a716-46a4-b097-c5c650dea63b` (source_agent=gemini, backend=agy)
- Source verdict claimed: `SOURCE_READY_REMEDIATED: all codex findings addressed; strict static gates PASS; ready for codex review`
- Card: `D:\QM\strategy_farm\artifacts\cards_approved\QM5_36003_nnfx-hull-ma-zerolag-macd-stc.md`
- **Verdict: RECYCLE** — source logic is clean and card-faithful, but the entire
  build/compile/setfile evidence chain is stale relative to the on-disk `.mq5`;
  dispatching to Q02 now would test a binary built from neither the reviewed nor
  the current source. Not APPROVABLE until the chain is rebuilt against the
  current source.

## Source-level review (current working-tree `.mq5`, PASS)

Current `.mq5` SHA256 `ce11515348609e6871937250055bfc6cf36feae57734b7466cd36ebb9c10b60d`.

- **Card faithfulness — PASS.** Entry rules match card §3.2/§3.3 exactly:
  `Close[1] vs HMA[1]` + ZL-MACD/signal side + `STC >= 75 / <= 25` +
  BetterVol==HIGH (`.mq5:371-397`). ZeroLag MACD is a genuine two-stage
  `EMA(P + (P - EMA(P,n)), n)` recursion (`.mq5:264-272`), matching card §2.
  Schaff Trend Cycle is a full two-pass stochastic-of-stochastic with
  smoothing (`.mq5:292-350`). No-trade filter covers spread `1.8*ATR`,
  rollover 23:55-00:05 (broker->UTC via `QM_BrokerToUTC`, `.mq5:143-148`),
  2.0% daily realized-loss halt (`.mq5:161-163`), max-1-position
  (`.mq5:138-139`). TP1 50% at +1 ATR + BE (`.mq5:474-491`), SL -1 ATR
  (`.mq5:379/393`), runner exit on ZL-MACD/signal crossover
  (`.mq5:571-591`) — all per card §3.4.
- **Dead-input scan — PASS.** All 20 `strategy_*` inputs (`.mq5:77-96`) have
  >=1 use site beyond declaration (mechanical grep, every count >=2). No
  dead inputs.
- **Framework conformity — PASS.** `#include <QM/QM_Common.mqh>` only
  (`.mq5:5`); `RISK_FIXED=1000 / RISK_PERCENT=0` (`.mq5:47-48`); magic via
  `QM_FrameworkMagic()` / `QM_FrameworkInit` (`.mq5:610-628`); two-axis news
  filter wired, gates entries only (`.mq5:687-693`); MAE hook
  `QM_FrameworkTrackOpenPositionMae()` (`.mq5:645`); kill switch configured
  with card thresholds 2.5%/5.0% (`.mq5:112-116`). No ML libs, no hardcoded
  commission/swap/DST values. Raw `CopyRates` calls are closed-bar,
  `perf-allowed`-annotated (`.mq5:210,523`).

## Blocking defect — build evidence does not cover the current source

Two independent build artifacts
(`artifacts/builds/019d50ff-a716-46a4-b097-c5c650dea63b.json`,
`docs/ops/evidence/019d50ff_qm5_36003_build_ea_result_2026-08-23.md`) both
declare the built source SHA256 as
`197a2c09c011c443cfcbb61a0487c34b135a2d6923c06200d881321ca0895187` with
`compile_succeeded: true`. The on-disk `.mq5` is
`ce11515348609e6871937250055bfc6cf36feae57734b7466cd36ebb9c10b60d` — a
different source (working-tree modified vs HEAD `c35b08d9…`; 603 insertions /
374 deletions).

| Artifact | Identity | Timestamp (local) |
|---|---|---|
| on-disk `.mq5` (to be tested) | `ce115153…` | Aug 23 19:21 |
| build artifact / build-result claimed source | `197a2c09…` | 11:30Z build |
| on-disk `.ex5` | `baffb41f…` | Aug 17 23:06 |
| only compile record `D:\QM\reports\compile\QM5_36003…\result.json` | source mtime Aug 17 21:06, ex5 `baffb41f…` | Aug 17 21:06 |
| setfiles (build_hash `ed45f7ef…`, for `197a2c09`) | Aug 23 13:29 | — |

Nothing in the chain matches the current source `ce115153`: the `.ex5`
(`baffb41f…`) was compiled Aug 17 from the pre-fix source; the only
`result.json` post-dates neither the `197a2c09` build nor the current
`ce115153` source; the build artifact's `compile_succeeded: true` is
unverified against the on-disk bytes. Per evidence-over-claims, a
`compile_succeeded` flag with no compile record post-dating the `.mq5` mtime
is treated as false. This is the same systemic gap flagged earlier today on
the gemini remediation path (QM5_35005 stale-ex5, and the prior
`c8791ede_qm5_36003_claude_review_stale_ex5` note against source `197a2c09`) —
the source has since diverged a second time to `ce115153` with still no
recompile.

I could not independently re-run `build_check.ps1` / compile: ad-hoc compile
is correctly refused while the factory is live
(`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`). Read-only review; no registry,
work-item, or pipeline state changed.

## Rework directive (actionable)

1. Freeze the intended source (`ce115153…` if that is the reviewed version;
   otherwise reconcile to the source that was actually reviewed).
2. Recompile via the governed queue
   (`python tools/strategy_farm/farmctl.py enqueue-compile
   QM5_36003_nnfx-hull-ma-zerolag-macd-stc`) and produce a fresh
   `result.json` / `ex5_sha256` with `ex5_mtime_utc` post-dating the current
   `.mq5` mtime and differing from `baffb41f…`.
3. Regenerate the 3 setfiles via `gen_setfile.ps1` so `build_hash` binds the
   recompiled source (current `ed45f7ef…` is bound to `197a2c09`, not the
   on-disk source).
4. Refresh the build artifact so `mq5_sha256` / `ex5_sha256` match the on-disk
   bytes, and re-run strict `build_check` live to confirm the `iClose`/
   `iTickVolume` `perf-allowed` annotations are accepted.
5. Resubmit for review. The source logic itself needs no further change.

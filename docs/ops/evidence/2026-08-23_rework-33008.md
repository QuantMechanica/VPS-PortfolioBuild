# rework-33008 — blocked on unresolved approved-card definition

## Status

`BLOCKED` — no EA source, setfile, registry, resolver, verdict, queue, factory, or live-terminal
artifact was changed.

Router task `da921b20-b31f-4530-aa87-15a05d4c818a` was claimed as `IN_PROGRESS` with verdict
`rework burn-window slot 5`. The requested rework cannot be implemented card-faithfully because
the approved card does not define the standard-deviation lookback required to compute its RVI.

## Evidence and decision

- Approved runtime card:
  `D:/QM/strategy_farm/artifacts/cards_approved/QM5_33008_thomas-stridsman-rvi-noise-buster.md`.
  Its formula specifies `EMA(sigma_up, 14)` and `EMA(sigma_down, 14)`, while its parameter table
  declares only `InpRVIPeriod`; it never defines the lookback used to form either sigma series.
- Repository approved-card copies are text-equivalent to the runtime card under
  `git diff --no-index --ignore-cr-at-eol` (no diff). Their SHA-256 differs only because the runtime
  copy uses different line endings: runtime
  `26442f91b796327fb287429a0812e98e54b5e53209a220f98dffb708fce2348a`, repository
  `91e8881f47ce73a4efc9a46aee4b645336d651a348f8dc852262ed281d428946`.
- The review artifact
  `D:/QM/strategy_farm/artifacts/reviews/da921b20-b31f-4530-aa87-15a05d4c818a.json:58`
  classifies the hard-coded 10-bar sigma window as
  `RVI_STANDARD_DEVIATION_WINDOW_IS_INVENTED`.
- The same review's required repair at
  `D:/QM/strategy_farm/artifacts/reviews/da921b20-b31f-4530-aa87-15a05d4c818a.json:167`
  explicitly requires obtaining an approved sigma lookback before implementing the EMA-smoothed
  RVI.
- A repository/runtime search for `33008`, the review code, `sigma lookback`, and
  `standard-deviation window` found no later decision or approved-card amendment supplying that
  value.
- The card also contains an unresolved drawdown inconsistency: front matter declares
  `expected_dd_pct: 15` at the runtime card's line 42, while the body mandates a 5% total drawdown
  stop at line 112. This is secondary to the missing signal definition but independently needs
  Research/OWNER clarification.

Choosing 10, 14, or any other sigma window would change crossover timing and therefore strategy
mechanics. The ticket forbids repairing a genuine card defect by invention, so the only compliant
disposition is `BLOCKED` pending an amended `g0_status: APPROVED` card or a durable OWNER-approved
definition.

## Preflight checks

- `framework/registry/ea_id_registry.csv:4439` contains active EA ID `33008` with matching slug.
- `framework/registry/magic_numbers.csv:17374` and `:17375` contain the existing active NDX.DWX
  slot 0 and SP500.DWX slot 1 allocations. They were not modified and the resolver was not
  regenerated.
- The runtime state DB was inspected read-only through
  `file:D:/QM/strategy_farm/state/farm_state.sqlite?mode=ro` before claim. It identified the review
  artifact above and verdict `FAIL upheld: RVI formula and trailing-R diverge from card; rework`.
- The current EA remains the rejected build. No scoped compile, build check, hardening run, or unit
  test can establish card fidelity while the signal formula is undefined; none was run, to avoid
  presenting a structurally clean but mechanically invented binary as remediation evidence.

## Required unblock

Research/OWNER must publish a durable approved-card revision that defines, at minimum:

1. the sigma lookback and exact source series used for `sigma_up` / `sigma_down`;
2. whether `InpRVIPeriod` controls EMA smoothing only or both sigma and EMA windows; and
3. the authoritative drawdown contract reconciling the 15% metadata with the 5% hard stop.

After that authorization, rework can implement the remaining review findings, add static/unit
regressions, run the EA-scoped compile/build/hardening gates, and resubmit to `REVIEW`.

## Rollback

This commit adds only this evidence file. Roll it back with a normal `git revert <commit>`.
The router state transition is append-only operational state; after the approved-card amendment,
an operator can return the task to `TODO`/`RECYCLE` through `agent_router.py update-task` without
editing or deleting existing verdict rows.

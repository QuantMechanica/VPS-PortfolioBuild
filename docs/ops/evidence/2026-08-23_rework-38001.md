# rework-38001 — QM5_38001 review repair evidence

- Router task: `ce1b2ad8-e99a-49b3-96d1-581726dc3bdf`
- EA: `QM5_38001_codetrading-vwap-bollinger-rsi-scalper`
- Worktree: `C:/QM/worktrees/rework-slot-6`
- Executed: 2026-08-24
- Scope: one-EA build/review repair only; no backtest enqueue, verdict-row
  overwrite, factory toggle, registry edit, or `T_Live` access.

## Work order and preflight

The blocking review is
`docs/ops/evidence/ce1b2ad8_qm5_38001_codex_review_2026-08-18.md`.
It identified unreachable open-position management/strategy exits, a startup
ATR/spread admission bypass, and a post-attach partial VWAP. The source
remediation inherited in this branch was also audited against
`docs/ops/evidence/33322516_qm5_38001_card_remediation_compile_hold_2026-08-23.md`
rather than accepted on claim alone.

Preflight evidence:

- Approved card: `strategy-seeds/cards/approved/QM5_38001_codetrading-vwap-bollinger-rsi-scalper.md:3-10`
  has `ea_id: QM5_38001`, the matching slug, `status: APPROVED`, and
  `g0_status: APPROVED`.
- EA registry: `framework/registry/ea_id_registry.csv:4472` has the active
  `38001,codetrading-vwap-bollinger-rsi-scalper` identity.
- Magic registry: `framework/registry/magic_numbers.csv:17484-17486` has the
  existing active EURUSD/GBPUSD/USDJPY slots 0/1/2. No registry or resolver
  regeneration was needed.

## What changed

### Card-faithful lifecycle and bounded state

- The current implementation reconstructs closed-bar session VWAP from the
  UTC day using the DST-aware `QM_BrokerToUTC` conversion and advances it only
  over valid bars (`.mq5:155-185`). Explicit `ArraySize` proofs now bound both
  the 400-bar rebuild and the single closed-bar refresh (`.mq5:165-171` and
  `.mq5:193-197`). Both raw `CopyRates` sites are marked `perf-allowed`.
- Management and the strategy-exit hook execute before the entry-only
  no-trade filter, so the one-open-position admission rule cannot suppress
  break-even or exits (`.mq5:460-497`). The MAE hook remains first in `OnTick`
  (`.mq5:460`).
- A shared current-quote spread predicate now fail-closes invalid quotes/ATR
  (`.mq5:259-267`). Admission uses it, and `Strategy_EntrySignal` repeats it at
  the execution boundary and uses the same quote to construct SL/TP
  (`.mq5:283` and `.mq5:299-310`). This closes both startup and elapsed-news
  spread bypasses.
- Framework sizing remains dual-mode (`RISK_PERCENT=0`,
  `RISK_FIXED=1000` defaults at `.mq5:17-18`), magic stays resolver-backed via
  `QM_FrameworkMagic` (`.mq5:274`, `.mq5:437-440`), and no ML or raw indicator
  handle/buffer code was introduced.

### Presets, documentation, and regression coverage

- The three M5 backtest presets retain fixed risk and their registered symbol
  slots. They now bind the canonical-LF MQ5 SHA-256
  `107c6c9b9bd609c0e3bbdde37e31db6c5a18be508d66f3467e022c687281115a`
  and point to the approved card (each set: lines 13, 19-23).
- `SPEC.md:107-108` now records the real build-gate sizing failure code,
  `EA_RISK_SIZER_UNCONFIGURED`, instead of the nonexistent legacy name.
- `tools/strategy_farm/tests/test_qm5_38001_rework_static.py:58-189`
  covers approved-card/registry/resolver identity, all three review repairs,
  bounded arrays, framework/risk/magic/MAE conformity, every declared input's
  use-site, and all preset parameters/hashes.

## Build and test evidence

The required scoped wrapper was attempted exactly as follows:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -Strict -SkipCompile -EALabel QM5_38001_codetrading-vwap-bollinger-rsi-scalper
```

It failed closed before checks with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`
because terminal64 processes were active. The factory was not stopped and no
compile work item was enqueued.

The idle, non-live T8 MetaEditor was then used in portable mode with only this
worktree's `framework/include/QM` mirrored temporarily into T8. The compile log
proves the include provenance as
`D:/QM/mt5/T8/MQL5/Include/QM/QM_Common.mqh`; T8's original include directory
was restored in a `finally` block.

```text
Compile log: D:/QM/reports/compile/20260824_rework38001_slot6/QM5_38001_codetrading-vwap-bollinger-rsi-scalper.portable.compile.log
Result: 0 errors, 0 warnings, 6429 ms elapsed
EX5: 412230 bytes
EX5 SHA-256: eaabe10f8ae3ee2792e6e4195c5cd6fb3cf4562b89930a3774363057e35fecbd
```

Focused checks run from the worktree:

```text
python -m pytest tools/strategy_farm/tests/test_qm5_38001_rework_static.py -q
5 passed in 0.73s

python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py -q -k "d2_loss or d4_management or d5_raw or d7_framework or d10_indicator or d11_canonical or semantic_predicates"
7 passed, 22 deselected in 0.90s

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_38001_codetrading-vwap-bollinger-rsi-scalper
schema=qm.build-gate-hardening/v1 files_scanned=1 failures=0 warnings=0

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_38001_codetrading-vwap-bollinger-rsi-scalper
PASS; Summary: 1 PASS, 0 FAIL (of 1)

python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 <MQ5> <sets-dir>
PASS; MQ5 findings=[]; three-set directory findings=[]
```

No smoke test or backtest was run; pipeline evidence remains the responsibility
of later governed phases after review acceptance.

## Rollback

Revert the commit that adds this evidence file:

```text
git revert --no-edit <rework(38001) commit>
```

That restores the prior MQ5/SPEC/presets, removes the newly compiled EX5 and
focused regression test, and leaves registry, DB, factory, and live state
unchanged. No external rollback is required; T8's original include tree was
already restored before handoff.

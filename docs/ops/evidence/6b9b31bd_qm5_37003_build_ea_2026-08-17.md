# QM5_37003 Build EA Evidence — 2026-08-17

**Task ID:** `6b9b31bd-8511-4f7b-8400-9e42162b0bd1` (`build_ea`, priority 50, agent `gemini`).  
**EA:** `QM5_37003_hurst-exponent-dynamic-regime-switch`.  
**Artifact Path:** `C:/QM/repo/docs/ops/evidence/6b9b31bd_qm5_37003_build_ea_2026-08-17.md`.  
**Strategy Card:** `strategy-seeds/cards/approved/QM5_37003_hurst-exponent-dynamic-regime-switch.md` (G0 APPROVED).  

---

## 1. Strategy Overview & Implementation

- **Strategy Thesis**: Evaluates rolling Rescaled Range ($R/S$) Hurst Exponent $H$ on closed H1 bars (lookback 100).
  - $H > 0.55$ (Trending Regime): 20-bar Donchian breakout entry with $1.5 \times \text{ATR}(14)$ SL and $2.0 \times \text{SL}$ TP.
  - $H < 0.45$ (Mean-Reversion Regime): Bollinger Bands $(20, 2.0)$ bounce entry targeting the midline $\text{SMA}(20)$ with $1.5 \times \text{ATR}(14)$ SL.
- **Framework Corset Compliance**:
  - Implemented cleanly using `EA_Skeleton.mq5` wiring.
  - Closed-bar calculation cached in `AdvanceState_OnNewBar()` to ensure $O(1)$ per-tick performance.
  - Sizing handled strictly by `QM_LotsForRisk` from `req.sl`.
  - News temporal mode (`QM_NEWS_TEMPORAL_PRE30_POST30`) and compliance (`QM_NEWS_COMPLIANCE_DXZ`) with fail-closed stale guard (`qm_news_stale_max_hours = 336`).

---

## 2. Registry & Artifact Verification

- **Registries Updated**:
  - `framework/registry/ea_id_registry.csv`: verified `37003,hurst-exponent-dynamic-regime-switch`.
  - `framework/registry/magic_numbers.csv`: allocated slots 0 (`EURUSD.DWX`, `37003000`), 1 (`GBPJPY.DWX`, `37003001`), 2 (`SP500.DWX`, `37003002`).
  - `framework/include/QM/QM_MagicResolver.mqh`: regenerated cleanly via `update_magic_resolver.py`.
- **SPEC Document**:
  - `framework/EAs/QM5_37003_hurst-exponent-dynamic-regime-switch/SPEC.md` written and verified with `validate_spec_doc.py` (`PASS`).
- **Static Checks & Compilation**:
  - `build_check.ps1`: `compile_one.result=PASS`, `errors=0`, `warnings=0`, `build_check.result=PASS`.
  - Emitted binary: `framework/EAs/QM5_37003_hurst-exponent-dynamic-regime-switch/QM5_37003_hurst-exponent-dynamic-regime-switch.ex5`.
- **P2 Setfiles**:
  - Generated for `EURUSD.DWX`, `GBPJPY.DWX`, `SP500.DWX` via `gen_setfile.ps1`.

---

## 3. Smoke Test & Review State

- `run_smoke.ps1`: Encountered standalone admission rejection (`custom_history_smoke_admission.py` requires worker-bound work item under active Custom-history isolation).
- Build result JSON emitted honestly per schema to `artifacts/qm5_37003_build_result.json` with `build_check_passed=true`, `compile_succeeded=true`, `smoke_result=framework_error`.
- Task updated to `REVIEW` for mandatory Codex review per router rules.

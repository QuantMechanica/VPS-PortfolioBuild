# rework-1613 review repair evidence

## Scope and authority

- Router task: `6cb2150d-fd37-4068-a0cd-5a11e9d90b32` (`RECYCLE` -> `IN_PROGRESS`).
- Original build task: `de6c76f5-05f5-46cb-ac53-4fa4b81fc9b1`.
- EA: `QM5_1613_aa-dsp-atsmom` only.
- Work order: `docs/ops/evidence/2026-08-23_qm5_1613_gemini_code_review.md`
  and `docs/ops/evidence/2026-08-24_review_qm5_1613.md`.
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1613_aa-dsp-atsmom.md`,
  `g0_status: APPROVED`.

The repository rebaseline proposal, factory inventory, and gate-name census were
read before the task was claimed. The required vault directive exists at the
named `G:` path, but the mounted provider did not materialize its bytes: direct
`Get-Content`, `type`, .NET reads, and a bounded copy all returned no content or
stalled. The three repository context documents explicitly cite and restate the
directive's operative constraints; this ticket changes no gate topology,
threshold, criterion, window, verdict, queue, factory state, or live surface.

No backtest or compile work item was enqueued or deleted, the factory was not
toggled, and `C:/QM/mt5/T_Live` was not touched.

## What changed

1. Restored the approved state-based entry rule. The EA opens long whenever the
   completed-bar `ATSMOM` is positive and short whenever it is negative at
   `framework/EAs/QM5_1613_aa-dsp-atsmom/QM5_1613_aa-dsp-atsmom.mq5:205-214`.
   The entry hook no longer reads `ATSMOM(2)` or requires a zero crossing, so it
   can re-enter after an ATR stop while the signal retains its sign.
2. Implemented the mandatory dynamic spread filter. The five declared strategy
   inputs are at MQ5 lines 56-60 and every one has executable use sites. The
   bounded, fail-closed 20-completed-D1-day `CopySpread` sample, `ArraySize`
   guards, positive-value validation, median, and 2.5x comparison are at lines
   94-128; entry invokes the filter after its history proof at lines 185-192.
   The bounded `CopyClose` read also has an explicit resize/size proof and
   `perf-allowed` annotation at lines 65-77.
3. Kept risk-reducing actions outside the entry-only news denial. MAE remains
   first on tick at MQ5 line 309, Friday close runs at line 315, management at
   line 320, the single D1 new-bar gate at line 322, and strategy exits at line
   327. News gating starts at line 351 and therefore cannot suppress those
   exits; entry follows at line 359.
4. Reconciled the approved nine-concept universe without modifying any existing
   magic allocation. The governed `USOIL.cash` -> `XTIUSD.DWX` alias requires a
   new row, appended only at `framework/registry/magic_numbers.csv:16669` as
   slot 13 / magic `16130013`. The generated resolver records registry SHA-256
   `3845...62AE` and 17,995 retained rows at
   `framework/include/QM/QM_MagicResolver.mqh:16-18`; regeneration and dry-run
   both reported zero dropped rows with `--keep-obsolete`.
5. Regenerated exactly nine D1 backtest setfiles: GDAXI, NDX, SP500, WS30,
   XAUUSD, XTIUSD, EURUSD, GBPUSD, and USDJPY. The five unapproved setfiles were
   removed, while their pre-existing registry rows were preserved. The new oil
   set binds source SHA-256, slot 13, `RISK_FIXED=1000`, `RISK_PERCENT=0`, and
   all five strategy inputs at
   `framework/EAs/QM5_1613_aa-dsp-atsmom/sets/QM5_1613_aa-dsp-atsmom_XTIUSD.DWX_D1_backtest.set:7-28`.
6. Replaced the corrupt SPEC with clean literal UTF-8 text. Identity is fixed at
   `framework/EAs/QM5_1613_aa-dsp-atsmom/SPEC.md:4-5`, the exact card universe
   and governed oil alias are documented at lines 41-50, and `$1,000 per trade`
   is restored at line 84.
7. Converted `build_identity.json` to the canonical build-result field set and
   bound it to router task/source SHA at
   `framework/EAs/QM5_1613_aa-dsp-atsmom/build_identity.json:2-6`. Lines 38-42
   truthfully record that build check/compile did not pass and that the EX5 is
   the stale pre-rework binary, not a claimed rebuild.
8. Added eight ticket-specific regression tests at
   `tools/strategy_farm/tests/test_qm5_1613_review_rework_static.py:80-218` for
   state-based entry, dynamic spread mechanics and allow/refuse thresholds,
   input wiring, exit/news ordering, allocation preservation, exact setfiles,
   SPEC integrity, and truthful build identity.

## Validation evidence

- `python -m pytest tools/strategy_farm/tests/test_qm5_1613_review_rework_static.py tools/strategy_farm/tests/test_gen_setfile.py -q`
  -> final rerun `9 passed in 2.84s`.
- `python -m pytest tools/strategy_farm/tests/test_build_guardrails.py -q`
  -> `20 passed in 1.79s`.
- `python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py -q -k "d10_indicator_buffer_bound_pass_and_fail_fixtures or d11_canonical_setfile_and_magic_symbol_pass or build_check_applies_hardening_pass_and_fail_fixtures or build_check_rejects_d11_phantom_symbol"`
  -> `4 passed, 26 deselected in 4.93s`.
- The attempted full hardening-test module emitted seven passing tests and then
  produced no progress for several minutes; it was interrupted and replaced by
  the explicit ticket-relevant selection above. This was a stalled test run,
  not a reported assertion failure.
- `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_1613_aa-dsp-atsmom`
  -> one source scanned, `failures: []`, `warnings: []`.
- `python framework/scripts/validate_spec_doc.py framework/EAs/QM5_1613_aa-dsp-atsmom`
  -> `1 PASS, 0 FAIL`.
- `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_1613_aa-dsp-atsmom`
  -> `PASS`, ten files checked, zero findings.
- `python framework/scripts/skill_build_ea_guard.py --ea-id 1613 --ea-label QM5_1613_aa-dsp-atsmom`
  -> `status: ok`; EA registry, magic rows, and EA directory all true.
- `python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_1613_aa-dsp-atsmom --fail-on-leak --verbose`
  -> `SINGLE_SYMBOL_OK`, zero violations.
- `python framework/scripts/update_magic_resolver.py --keep-obsolete`
  and `--dry-run --keep-obsolete`
  -> `17995 rows kept, 0 dropped`, SHA prefix `3845BA4E4534FB1D`.
- The combined resolver suite produced `13 passed, 1 failed in 2.52s`. The sole
  failure, `test_binary_lookup_is_equivalent_over_every_generated_row_and_misses`,
  is an existing global default-mode invariant: `keep_obsolete=False` drops EA
  IDs 1001, 1015, and 1016. It is unrelated to the appended 1613 row; the
  governed `--keep-obsolete` regeneration retained all rows and the other 13
  resolver assertions passed.
- `git diff --check` -> no whitespace errors (only checkout line-ending
  warnings).

## Compile and smoke admission evidence

The required EA-scoped commands were attempted:

```text
pwsh -NoProfile -File framework/scripts/build_check.ps1 -EALabel QM5_1613_aa-dsp-atsmom -Strict
pwsh -NoProfile -File framework/scripts/build_check.ps1 -EALabel QM5_1613_aa-dsp-atsmom -Strict -SkipCompile
```

Both were refused before any build mutation with
`BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED` /
`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because managed `terminal64` processes
were alive. The guard explicitly required the governed compile-work-item path;
this ticket did not authorize bypassing the guard, and no compile was enqueued.

Read-only SQLite URI query:

```sql
SELECT id,status,verdict,evidence_path,created_at
FROM work_items
WHERE ea_id='QM5_1613' AND phase='COMPILE_EA'
ORDER BY created_at;
```

returned only the two historical closed failures:

```text
2ef85cd7-f24b-407a-ae92-2fe2080dd5b5 | failed | INVALID
1e899c7d-87d8-4100-9f60-5e1a16a401cd | failed | COMPILE_FAIL
```

No source-repair compile authority exists for this label in
`tools/strategy_farm/compile_work_items.py:47-93,206-224`. Therefore no existing
EX5 was overwritten and no guard was weakened. The reworked MQ5 SHA-256 is
`c01635af8a2a937afdf2465c53d1f42542f9bc65b6f9811b4480f665302a8594`;
the unchanged stale EX5 SHA-256 is
`23d56a02a2285ba62b4c8e6ece03c1e274187e3ab2e3b0fe5411464c827682ae`.
A fresh compile and smoke report remain review-visible open risks.

## Rollback

Revert the single ticket commit containing this evidence. That restores the
prior EA/SPEC/build identity and 13-set bundle, removes the appended slot-13
magic row and XTI setfile, restores the prior generated resolver, and removes
the regression test. After reverting, run
`python framework/scripts/update_magic_resolver.py --dry-run --keep-obsolete`
to verify the restored registry/resolver pair. No runtime, queue, verdict, or
terminal rollback is required because none was changed.

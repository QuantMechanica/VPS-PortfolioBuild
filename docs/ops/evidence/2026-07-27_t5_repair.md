# T5 indicator-engine repair attempt

Date: 2026-07-27  
Router task: `00b6f79c-b2e6-4653-a18f-420b3f51f49c`  
Disposition: **NOT REPAIRED — T5 remains disabled**

## Result

The fault still reproduces. The least-destructive, rollback-capable repair
probe eliminated T5's tester-local scratch/cache tree as the cause: a wholly
fresh `Tester` tree produced the same zero-trade control result. The original
tree was restored byte-for-byte by directory rollback.

No evidence-supported cause was established, so no full terminal rebuild was
attempted. A real-strategy verification was not run because the control gate
did not pass. T5 remains in `disabled_terminals.txt`; a T5 FAIL remains
unattributable.

## Reservation and guardrails

T5 was reserved through:

```text
farmctl reserve-terminal T5 --by codex --minutes 120 --reason "T5 repair"
created_at_utc=2026-07-27T12:12:17.846126Z
until_utc=2026-07-27T14:12:17.846126Z
```

No other terminal was launched or modified. No `.DWX` history was imported.
`Factory_OFF`, `Factory_ON`, `CustomTicksReplace`, AutoTrading, and T_Live
were not used.

The first control launch failed closed before terminal start because the
service-account FILE_COMMON news seed was 1,659 hours old. The governed
`refresh_news_calendar.ps1` refreshed
`D:/QM/data/news_calendar` and the SYSTEM FILE_COMMON copy. Coverage through
2026-07-31 passed and `qm_news_stale_max_hours` remained 336.

## Reproduction

Control:

```text
run_smoke.ps1
  -EAId 11144
  -EALabel QM5_11144_vbt-macd-zero
  -Symbol GBPUSD.DWX
  -Year 2024
  -Terminal T5
  -Period D1
  -Runs 1
  -MinTrades 1
  -Model 4
  -SmokeMode
```

Evidence:

- `D:/QM/reports/smoke/QM5_11144/20260727_121445/summary.json`
- source/deployed EX5 SHA:
  `45cd9651943358c12913070e6732723a9fa5ef3238aac081fc6be905f366b93d`
- Model-4 marker: true
- report status: OK
- total trades: 0
- verdict: `FAIL / MIN_TRADES_NOT_MET`

This matches the 2026-07-24 control behavior. The prior instrumented evidence
at
`docs/ops/source_harvest/strategies/STR-097-ha-stoch-h4-swing/06_smoke.md`
already proves why this control produces zero on T5: built-in indicator handles
remain at `BarsCalculated=-1` while ordinary chart bars grow.

Healthy-terminal evidence was not created in this cycle because the brief also
forbids touching terminals other than T5 and all active factory runs must not
be interrupted. The durable healthy control is the same unchanged QM5_11144
binary's prior Q02 PASS; this cycle does not strengthen that evidence.

## Comparison and named divergence

Systematic read-only comparison established:

- `terminal64.exe` and `metatester64.exe` hashes match healthy T1, as already
  recorded in the 2026-07-26 preflight.
- `common.ini` is semantically identical to T1 except
  `Charts.ObsoleteLasttime`.
- T5 has a local isolated `bases/Darwinex-Live` directory and the required
  `bases/Custom` junction to `D:/QM/mt5/T1/Bases/Custom`.
- account and server files exist with the same lengths as T1. Their encrypted
  bytes differ, which is expected terminal-local state and is not diagnostic.
- no `origin.txt` exists in either T1 or T5.

The actionable named divergence before the probe was **tester-local state**:
T5 had only `Agent-127.0.0.1-3004`, retained `_agent_bak_20260724`, and its own
`Tester/bases`, `Tester/cache`, and logs, while the healthy factory layout has
the full agent set. This was the narrowest state boundary that could be reset
without touching executable, account, custom-symbol, or Darwinex history data.

## Repair probe and elimination

With no T5 process active:

1. moved `D:/QM/mt5/T5/Tester` to
   `Tester.rollback_20260727T1218Z`;
2. created an empty `D:/QM/mt5/T5/Tester`;
3. ran the identical control through `run_smoke.ps1`;
4. observed the same failure;
5. moved the regenerated tree to
   `Tester.failed_probe_20260727T1221Z`;
6. restored `Tester.rollback_20260727T1218Z` to the exact original
   `Tester` path.

Fresh-tree evidence:

- `D:/QM/reports/smoke/QM5_11144/20260727_121832/summary.json`
- same EX5 SHA
- Model-4 marker: true
- report status: OK
- total trades: 0
- verdict: `FAIL / MIN_TRADES_NOT_MET`

Therefore corruption confined to `T5/Tester` scratch, bases, cache, agents, or
logs is eliminated as the cause. A blind full-instance rebuild would cross
unproven boundaries and was not justified.

## Rollback

Rollback was executed during the task: the original `Tester` directory is back
at `D:/QM/mt5/T5/Tester`. The failed fresh probe is retained at
`D:/QM/mt5/T5/Tester.failed_probe_20260727T1221Z` (18 files, 82,800,558 bytes)
for inspection and can be removed after review.

The news refresh is append/idempotent and synchronized both governed seed
copies; rollback is neither necessary nor desirable because the previous state
was stale and fail-closed.

## Next evidence-required boundary

Keep T5 disabled. The next repair attempt needs an explicit staged,
rollback-capable whole-instance manifest comparing/copying static terminal and
MQL5 assets while preserving:

- terminal-local `bases/Darwinex-Live`;
- the `bases/Custom` junction;
- account/server identity;
- no `.DWX` import;
- supported-runner verification.

That rebuild should only proceed after the remaining non-Tester divergence is
named. This cycle does not claim a repair, real-backtest match, pipeline
verdict, or safe return of T5 to service.

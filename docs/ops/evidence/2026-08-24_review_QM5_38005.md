# Review — QM5_38005 CodeTrading Ascending Triangle Breakout

- **Router task:** c31229df-db43-47e7-bda8-85c4e8d48777 (review_ea, state REVIEW, assigned codex)
- **Source build task:** 2f3177d2-2769-4e0d-a212-4769b908178c (gemini/agy build_ea)
- **Reviewer:** Claude (review lane)
- **Date:** 2026-08-24
- **Verdict:** APPROVED (clean for Q02)

## Artifacts reviewed
- Build result: `artifacts/qm5_38005_build_result.json` (build_check_passed=true, compile_succeeded=true, smoke=passed)
- Source: `framework/EAs/QM5_38005_codetrading-ascending-triangle-breakout/QM5_38005_codetrading-ascending-triangle-breakout.mq5`
  - sha256 `b8f602673dd2353af2c6d5f9f628c63ea43df308e7561f01ed21cd254e5814ee` — MATCHES build_result.mq5_sha256
- SPEC: `framework/EAs/.../SPEC.md`
- Card: `strategy-seeds/cards/approved/QM5_38005_codetrading-ascending-triangle-breakout.md` (status APPROVED, g0 PASS)
- Sets: 3 backtest setfiles (XAUUSD/SP500/EURUSD .DWX H1)

## Card-faithful mechanism — PASS
- Ascending triangle: flat resistance across two pivot highs (`res_diff_rel <= strategy_max_res_slope`) + ascending pivot lows (`l1 > l2`) + close[1] above resistance + buffer + volume expansion (`cur_vol >= avg_vol*strategy_vol_mult`). Descending mirror implemented. (mq5:172-206)
- All evaluations on shift>=1 closed bars; pivot scan starts at `s=win+1` so `iHigh(shift-k)` never touches bar 0 — no lookahead/repaint. (mq5:78-107, 141-170)
- Max 1 concurrent position via `QM_TM_OpenPositionCount(magic) > 0` (card §3.1.4). Rollover blackout + spread filter (`spread > ATR*strategy_spread_filter_mult`) implemented (card §3.1.1/§3.1.2). (mq5:212-247, 255-256)
- SL at swing pivot extreme ± buffer, TP at `strategy_tp_rr`×SL_dist (1:2 RR), BE-lock trailing at `strategy_trail_trigger_r`. (mq5:277-304, 320-370)

## Dead-input check — PASS (0 dead)
Every declared `strategy_*` input grepped for use-sites beyond declaration; all >=1 use-site:
signal_tf(15), pivot_window(2), search_bars(2), max_res_slope(3), vol_sma_period(2), vol_mult(2), atr_period(2), sl_buffer_pips(3), tp_rr(3), trail_enabled(2), trail_trigger_r(3), rollover_start/end_hhmm(4/4), spread_filter_mult(2).

## Framework conformity — PASS
- QM_Common include chain; all referenced QM_* symbols resolve (QM_ATR, QM_StopRules*, QM_NewsAllowsTrade2, QM_TM_*, QM_FrameworkTrackOpenPositionMae, QM_FrameworkMagic, QM_EquityStreamOnNewBar, QM_DefaultObjective).
- MAE hook `QM_FrameworkTrackOpenPositionMae()` first in OnTick. (mq5:392)
- Risk: RISK_FIXED=1000 / RISK_PERCENT=0 in source and setfiles (backtest). (mq5:17-18; set)
- Magic via MagicResolver/`QM_FrameworkMagic`; registry base 380050000 = 38005*10000+0, slots 0/1/2 → XAU/SP500/EUR active. (magic_numbers.csv)
- News wired via `QM_NewsAllowsTrade2` in OnTick; Friday close via `QM_FrameworkHandleFridayClose`. (mq5:414-421)
- Bounded buffers; all raw-series reads (iHigh/iLow/iClose/iVolume) carry `// perf-allowed` comments. No ML, no invented commission/swap.
- Setfiles via governed generator (header s20260817-001, card_defaults_source, all strategy params present, values = input defaults).

## Minor observations (non-blocking)
1. Setfile header `build_hash b25b38dc…` (dated 2026-08-17) predates the 2026-08-18 rebuild (ex5 `d8d5c28e…`, D4-reachability fix). Strategy params still match current input defaults exactly, so Q02 tester behaviour is unaffected — provenance-only note.
2. Card §3.1.3 daily-loss-limit and §4.2 max-total-DD circuit breakers are not implemented at EA level; these are live-risk overlays handled by framework kill-switch / portfolio gates, not the strategy alpha — acceptable for a RISK_FIXED backtest EA.
3. TP uses `tp_rr`×SL_distance rather than a literal triangle-height projection; achieves the card's 1:2 RR intent — acceptable mechanization.

## Verdict
APPROVED — card-faithful, no dead inputs, framework-conformant, current source compiled (build_result COMPILE_OK). Clean to enter Q02.

# review_ea — QM5_11517, QM5_11516, QM5_11496 — 2026-08-23

Router tasks `6e357318-e26d-4aaa-8d66-9fc47981d9ea` (11517), `38e4d67f-b6bf-41a2-be12-17acab9689d2`
(11516), `0de901bc-58f8-4ccd-bc84-227ab710f2b7` (11496), all `review_ea`/claude,
`reason=codex_review_required_for_gemini_code` (gemini/agy-authored build). Same "Carter-T"
batch (Thomas Carter, self-published trend-following ebooks) as several already-reviewed
siblings in this cycle.

Card lookup note: all three cards live only on `D:/QM/strategy_farm/artifacts/cards_approved/`
(runtime), not in this git checkout — see
[[feedback_cards_approved_dual_location_2026-08-23]] / the correction in
`docs/ops/evidence/2026-08-23_qm5_11299_gemini_code_review.md`. Checked both locations for all
three before concluding card status.

## Overall verdict

**QM5_11517 and QM5_11516: card-faithful, controls-clean, no findings.** **QM5_11496: no
SPEC.md, and two undocumented deviations from its own approved card** (disabled news filter,
missing "no Friday entry" rule) — the same defect *class* as `QM5_11300`/`QM5_11299` earlier in
this batch, but here a real card exists and the card doesn't authorize either gap. Per hard rule,
gemini-authored code cannot be self-approved/moved to PIPELINE regardless of outcome — all three
left in **REVIEW** for Codex.

---

## QM5_11517_carter-t-ema5-15-50-100-macd-h4 — clean

### Reviewed identity

- MQ5 SHA-256: `860b44ac1743654f9f93fd5eb446aae6573d268c8ca29f017b083e9c599994bc`
- EX5 SHA-256: `a453486593eea4ff59846fca70b1f798f3b6695ad161b6d16967916863a714bc`
- Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11517_carter-t-ema5-15-50-100-macd-h4.md`
  (Thomas Carter, "Forex Trend Following Strategies", System #14). `g0_status: APPROVED`.

### Card fidelity

4-EMA ribbon (5/15/50/100) + MACD(12,26,9), H4. `FindEmaCrossShift` finds an EMA5/EMA15 cross
within the last `strategy_cross_lookback=3` bars (matches "crossed up within last 3 bars"); long
also requires `close1 > ema50 && close1 > ema100 && macd_main > 0.0` (matches structural trend +
momentum gate). Fixed 30-pip SL / 60-pip TP via `QM_StopFixedPips`/pip-distance math — matches
card exactly. `strategy_no_friday_entry` + `IsFridayBrokerTime()` correctly blocks Friday entries
(card: "No Friday entry"). 15-pip spread cap via `SpreadWithinCap()` — matches. `QM_IsNewBar(
_Symbol, PERIOD_H4)` explicitly parameterized (not chart-default) — execution-timeframe-safe
regardless of chart attachment.

### Host / magic / risk / news controls

- `magic_numbers.csv`: 2 active slots (EURUSD.DWX/GBPUSD.DWX, `115170000+slot`) — matches the
  card's `target_symbols` and the 2 shipped setfiles 1:1.
- News: `qm_news_temporal=QM_NEWS_TEMPORAL_PRE30_POST30`, `qm_news_compliance=
  QM_NEWS_COMPLIANCE_DXZ` — standard blackout active (card doesn't discuss news; standard default
  applies, no exemption needed).
- Setfiles: `RISK_FIXED=1000, RISK_PERCENT=0`.
- `validate_spec_doc.py` → **PASS**. `validate_build_guardrails.py` → **PASS**, 3 files checked, 0
  findings, `max_news_stale_hours=336`. `build_gate_hardening.py` → 0 failures. Compile log: `0
  errors, 0 warnings`.

**Verdict: card-faithful, controls-clean, no defects — held in REVIEW for mandatory Codex
review.**

---

## QM5_11516_carter-t-sma7-21-cci5-m15 — clean

### Reviewed identity

- MQ5 SHA-256: `931a1646e0608b615cdfe189e5a43ab2e46d7fd279e083c2df3db62b3f739d7b`
- EX5 SHA-256: `520c169ea8a9dee22914347fe79ed78d54c0bddc14de9c3f361a098c1d52dfb9`
- Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11516_carter-t-sma7-21-cci5-m15.md`
  (Thomas Carter, System #12). `g0_status: APPROVED`.

### Card fidelity

SMA(7)/SMA(21) cross + CCI(5) zero-cross synchronized within `strategy_sync_bars=1` bar, M15 —
`FindSmaCrossShift`/`FindCciZeroCrossShift` implement exactly the card's "SMA cross AND CCI zero
cross within ±1 bar" rule (`MathAbs(cci_shift - sma_shift) <= strategy_sync_bars`). Partial exit:
`Strategy_ManageOpenPosition` closes `strategy_partial_close_ratio=0.50` at
`entry ± strategy_tp1_pips(25)` and moves SL to breakeven via `QM_TM_MoveSL` — matches "TP1: close
50% at 25 pips, move SL to BE." `PartialAlreadyDone()` detects the already-partialed state by
checking whether SL has already been moved to breakeven (stateless, restart-safe — no in-memory
globals needed for this, unlike the anti-cluster pattern flagged elsewhere in this cycle).
`Strategy_ExitSignal()` closes the remainder when `close1` crosses back through SMA(7) — matches
"TP2: close remaining when price closes below SMA7." Fixed 15-pip SL, 12-pip spread cap, no-Friday
entry — all match. SMA(84)/SMA(336) are documented in the card as "structural context only, not
used in entry signal" — correctly absent from the entry logic (not a gap).

### Host / magic / risk / news controls

- `magic_numbers.csv`: 3 active slots (EURUSD.DWX/GBPUSD.DWX/USDJPY.DWX, `115160000+slot`) —
  matches the card's `target_symbols` and the 3 shipped setfiles 1:1.
- News: `qm_news_temporal=QM_NEWS_TEMPORAL_PRE30_POST30`, `qm_news_compliance=
  QM_NEWS_COMPLIANCE_DXZ` — standard blackout active.
- Setfiles: `RISK_FIXED=1000, RISK_PERCENT=0`.
- `validate_spec_doc.py` → **PASS**. `validate_build_guardrails.py` → **PASS**, 4 files checked, 0
  findings. `build_gate_hardening.py` → 0 failures. Compile log: `0 errors, 0 warnings`.

**Verdict: card-faithful, controls-clean, no defects — held in REVIEW for mandatory Codex
review.**

---

## QM5_11496_carter-t-ema100-psar-macd64128-m5 — 3 findings, do not accept as-is

### Reviewed identity

- MQ5 SHA-256: `34a32e0b82e299b390391de88e54c7c34c6d80ac6be92e85f5daf66619c23949`
- EX5 SHA-256: `e380760609fd1925b76e655b6de1c8a495fa604b1cc5d851170f740d276dede9`
- Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11496_carter-t-ema100-psar-macd64128-m5.md`
  (Thomas Carter, "20 Forex Trading Strategies (5 Minute Time Frame)", System #19). `g0_status:
  APPROVED`. Unlike the two EAs above, **this directory has no SPEC.md**
  (`validate_spec_doc.py` → **FAIL**: `SPEC.md missing`).

### Card fidelity — core mechanics correct

EMA(100) trend filter + Parabolic SAR (0.01 step/0.01 max — deliberately slow) + MACD(64,128,9)
sign, M5. `long_signal = close_1 > ema100_1 && psar_1 < low_1 && macd_1 > 0.0` — matches the card
term for term (price above EMA100, SAR below the low, MACD positive); short mirrors. SL = SAR dot
± `strategy_sl_buffer_pips(3)`, capped at `strategy_sl_cap_pips(20)`, TP = `strategy_tp_rr_ratio(
2.0)` × SL distance — matches "SL: SAR dot ± 3 pips... QM P2: 2:1 R/R" and the card's "P2 cap: 20
pips." Spread cap 15 pips (`strategy_max_spread_pips`) matches.

### Finding 1 — missing SPEC.md

Every other Carter-T EA reviewed in this batch (11517, 11516, and the earlier 11299/11300/9972/
9979 batch) ships a SPEC.md; this one does not. Minor relative to the findings below, but still a
required artifact per the standing review checklist.

### Finding 2 — news filter disabled with no card authorization (unlike sibling QM5_11299)

Compiled-in defaults: `qm_news_temporal = QM_NEWS_TEMPORAL_OFF`, `qm_news_compliance =
QM_NEWS_COMPLIANCE_NONE` (source lines 22-23), not overridden by any of the 3 setfiles. This is
the same disabled-news pattern flagged blocking for `QM5_11300` and `QM5_11299` earlier in this
cycle — but here it's worth being precise about the distinction: `QM5_11299`'s card explicitly
states "News filter: off in P2," making that EA's identical defaults a **documented** choice. This
card's "Zusätzliche Filter" section lists Timeframe/Instruments/Spread cap/No Friday entry and
says nothing about news at all — there is no card text authorizing the exemption here. Structurally
this is an M5 trend-confirmation system (not event-driven/flat-by-design like the `QM5_41129`
FOMC-drift precedent), so there's no apparent reason it should differ from the standard
`PRE30_POST30`/`DXZ` default used by every other EA in this batch.

### Finding 3 — card's explicit "no Friday entry" rule is not implemented

The card's "Zusätzliche Filter" section states "No Friday entry," exactly like `QM5_11517`
and `QM5_11516` (which both implement it via `strategy_no_friday_entry` +
`IsFridayBrokerTime()`, `day_of_week==5`). `QM5_11496` has neither that input nor any
Friday-specific check — `Strategy_NoTradeFilter()` unconditionally returns `false`, and
`Strategy_EntrySignal()` has no day-of-week gate. (`qm_friday_close_enabled` is a separate,
present framework mechanism that manages/closes *existing* positions ahead of the weekend — it
does not block *new* Friday entries, which is what the card asks for.) On an M5 system this
plausibly matters more than on the H4/M15 siblings — a Friday-afternoon SAR flip could open a
position that then sits through the weekend gap the card intended to avoid.

### Host / magic / risk controls (for the 3 authorized symbols)

- `magic_numbers.csv`: 3 active slots (EURUSD.DWX/GBPUSD.DWX/AUDUSD.DWX, `114960000+slot`) —
  matches the card's `target_symbols` and the 3 shipped setfiles 1:1, no scope violation.
- Setfiles: `RISK_FIXED=1000, RISK_PERCENT=0`.
- `validate_build_guardrails.py` → **PASS**, 0 findings, `max_news_stale_hours=336` (at ceiling,
  not above — the check passes regardless of the temporal-mode question in finding 2, since that
  check is about the staleness ceiling, not whether the filter is active). `build_gate_hardening.py`
  → 0 failures. Compile log: `0 errors, 0 warnings`.

**Recommendation for Codex: entry/SL/TP mechanics are correct and card-faithful. Before
acceptance, needs (a) a SPEC.md, (b) either the standard news gate turned on or an
OWNER-ratified documented exemption, (c) a Friday-entry block matching the card's explicit rule
and the pattern already used by its two siblings in this same batch.**

## Summary line for the router

`QM5_11517`: clean, card-faithful, held in REVIEW for Codex. `QM5_11516`: clean, card-faithful
(partial-exit/breakeven/SMA7-trail all verified against the card), held in REVIEW for Codex.
`QM5_11496`: entry/exit mechanics correct, but missing SPEC.md, an undocumented disabled news
filter (its card, unlike sibling QM5_11299's, does not authorize this), and a missing "no Friday
entry" rule the card explicitly requires and two siblings in this same batch both implement —
three real gaps, flagged, held in REVIEW. None self-approved or moved to PIPELINE per hard rule.

# rework-9913 — QM5_9913 review-fix evidence

Date executed: 2026-08-24 (ticket-required evidence filename date: 2026-08-23)

- Router task: `6bd917a5-8444-4499-8784-caddfe2527b3`
- EA: `QM5_9913_bandy-rsi3-low-adx-mr-index`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9913_bandy-rsi3-low-adx-mr-index.md`
- Producer artifact: `D:/QM/strategy_farm/artifacts/builds/de7917ef-268a-4b01-90a4-c77cf4e04b9e.json`
- Review work order: `docs/ops/evidence/2026-08-23_qm5_9913_gemini_code_review.md`
- Scope: build/review repair only; no backtest enqueue, verdict-row overwrite,
  factory toggle, gate change, registry mutation, or live-terminal access.

## Preflight and authority

- The card is `g0_status: APPROVED`, binds `ea_id: QM5_9913`, slug
  `bandy-rsi3-low-adx-mr-index`, and `period: D1` (card lines 2-4, 15-16).
- `framework/registry/ea_id_registry.csv:789` contains the active identity.
- `framework/registry/magic_numbers.csv:17045-17057` contains the existing
  slots 0-12. There is no registry gap. Per ticket instruction, those existing
  allocations and `QM_MagicResolver.mqh` were not changed.
- The router task was claimed as `IN_PROGRESS` with verdict
  `rework burn-window slot 6` before source changes were finalized.

## What changed

### Card-faithful D1 mechanism and reachable exits

- `framework/EAs/QM5_9913_bandy-rsi3-low-adx-mr-index/QM5_9913_bandy-rsi3-low-adx-mr-index.mq5:66-73`
  reads only the completed D1 signal bar and D1 RSI/ADX/SMA/ATR values.
- `.mq5:86-88` constructs the catastrophic stop from that already-read D1
  ATR through `QM_StopATRFromValue`; the timeframe-sensitive generic
  `QM_StopATR(... PERIOD_CURRENT ...)` path is gone.
- `.mq5:154-157` declares and fail-closes the D1 chart execution contract.
  `.mq5:194` uses the single explicit `QM_IsNewBar(_Symbol, PERIOD_D1)` entry
  edge. An intraday attachment now fails during initialization rather than
  changing cadence or stop distance.
- `.mq5:176-210` runs position management and the strategy-exit hook before
  D1 cadence, news, and strategy entry filters. The RSI>=70 and seven-trading-
  day exits can no longer be suppressed by an entry-only filter.
- `.mq5:52-57` removes the invented ATR-spread gate. The P2 card declares no
  additional filter; the optional fresh-down-bar rule remains reserved for P3
  and was not silently activated.
- `.mq5:118` retains the trading-day `iBarShift` time-stop count with an
  explicit same-line `perf-allowed` justification. The EA has no direct
  `CopyBuffer` call or unbounded numeric indicator buffer and adds no ML path.

### Inputs, presets, and universe

- Removed the undeclared `strategy_spread_max_atr` and
  `strategy_warmup_bars` parameters from source, SPEC, and presets. Indicator
  readiness now fails closed through the framework indicator reads instead of
  an invented 250-bar admission filter.
- A declaration/use-site `rg` census found 23 declared inputs and at least one
  executable non-declaration use site for every input. Strategy use sites are
  `.mq5:70-73,78,86,105,119,125`; framework inputs flow through
  `.mq5:91,148-151,203-206` and the framework magic resolver.
- `SPEC.md:28-36,47-57,71` now describes only the wired card parameters, the
  approved delivered universe, and explicit D1 cadence.
- The delivered setfile cohort is exactly SP500/NDX/WS30 plus the seven FX
  majors authorized by the card's optional breadth clause: AUDUSD, EURUSD,
  GBPUSD, NZDUSD, USDCAD, USDCHF, and USDJPY. The unapproved GDAXI, UK100, and
  XAUUSD setfiles were removed. They are recoverable by reverting this commit.
- All ten retained sets keep `RISK_FIXED=1000`, `RISK_PERCENT=0`, their
  pre-existing registered slot, and checkout-form source binding
  `8aa14ed73b34dd720a926c749423b1ec3a99860ec1ac7fd1daaae58667a56763`
  (example: SP500 set lines 13, 19-20).

The hardening report still lists 13 `symbols_observed` because its observation
union intentionally includes every active magic-registry row. The three extra
origins are registry-only; no retained setfile or MQ5 reference can execute
them. Existing registry rows were immutable under this ticket.

## Compile and binary identity

The required scoped wrapper was attempted first:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_9913_bandy-rsi3-low-adx-mr-index
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
```

The interlock stopped before compilation or setfile mutation because live
factory `terminal64` processes existed. No unscoped check, compile work item,
or backtest was enqueued, and the factory was not toggled.

Compilation then used a disposable portable MetaEditor tree, independent of
T1-T10, populated with the current worktree includes and source. The copied
source, `QM_Common.mqh`, and `QM_MagicResolver.mqh` were hash-compared with the
worktree before accepting the result:

```text
compile log: D:/QM/strategy_farm/temp/rework-9913-slot6-20260824T135707Z/compile.log
Result: 0 errors, 0 warnings, 10009 ms elapsed, cpu='X64 Regular'
MQ5 SHA-256: 0c0c8a7867576f86c77b959009e7f71c26bb07b3f05d7eb86290fd7206996f49
EX5 SHA-256: 23b58416c75ac4354a5271eac03c55107f547b7462211cb622a5459e53ac000c
EX5 size: 398188 bytes
```

The prior 391,120-byte EX5 (SHA-256
`63809aeb7b06b7436aebdc6ac792df294bc3c49b13491f8959311418c7b045e1`)
is preserved at
`D:/QM/strategy_farm/temp/rework-9913-slot6-20260824T135707Z/pre_rework_QM5_9913_bandy-rsi3-low-adx-mr-index.ex5`.

## Tests and validation

Focused EA and relevant hardening regressions:

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_9913_review_rework_static.py \
  <D4/D7/D8/D10/D15/D17 build-gate-hardening node ids>
........... [100%]
11 passed in 1.26s
```

`tools/strategy_farm/tests/test_qm5_9913_review_rework_static.py:54-139`
locks hardening cleanliness, D1 cadence/ATR-stop binding, exit reachability,
all input use sites, no invented filter/raw buffer/ML path, exact delivered
universe, fixed-risk presets, and source binding.

Scoped component results:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_9913_bandy-rsi3-low-adx-mr-index
files_scanned=1; failures=[]; warnings=[]

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_9913_bandy-rsi3-low-adx-mr-index
PASS; files_checked=11; findings=[]; max_news_stale_hours=336

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_9913_bandy-rsi3-low-adx-mr-index
Summary: 1 PASS, 0 FAIL

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_9913_bandy-rsi3-low-adx-mr-index --fail-on-leak
SINGLE_SYMBOL_OK; n_violations=0

git diff --check
PASS (line-ending conversion notices only)
```

No smoke/backtest was run or enqueued. This fix withdraws reliance on the
producer artifact's unsupported `deferred_p2_smoke` claim; it does not claim a
smoke waiver or pipeline admission. The task is resubmitted only for corrected
source/package/binary review.

## Risks and open questions

- The monolithic `build_check.ps1` wrapper has no PASS report because its
  live-factory interlock correctly refused execution. The actual isolated
  compile and every scoped component check passed; review must retain the
  interlock limitation rather than treating it as a hidden PASS.
- GDAXI/UK100/XAUUSD allocations remain active but unused because this ticket
  expressly forbids changing existing magic rows. If governance requires
  retirement rather than dormant registry-only allocations, that needs a
  separate OWNER-authorized registry ticket.
- A real smoke remains required by the normal admission process before Q02;
  no smoke or capacity-waiver evidence is asserted here.

## Rollback

Revert the ticket commit with `git revert <ticket-commit>`. This restores the
prior MQ5, EX5, SPEC, all thirteen presets, and removes the focused test and
this evidence file. For binary-only recovery before a revert, copy the exact
pre-rework EX5 from the preserved path above after verifying its recorded hash.
No DB, queue, registry, resolver, factory, gate, verdict, or live rollback is
required.

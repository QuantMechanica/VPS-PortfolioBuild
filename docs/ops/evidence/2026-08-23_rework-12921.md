# rework-12921 — blocked card-authority preflight

## Status

`BLOCKED_CARD_AUTHORITY`. No EA source, binary, setfile, Strategy Card, registry,
gate criterion, queue row, verdict row, factory control, or terminal path was
changed.

Router task `d38222fd-9878-4d72-b1cf-c56cc03cc369` was claimed as
`IN_PROGRESS` with verdict `rework burn-window slot 1`. Its required terminal
disposition is `BLOCKED`, because the governed build preflight has no approved
Strategy Card from which a card-faithful repair can be made.

## Evidence and decision

1. The review work order identifies the only located card under
   `D:/QM/strategy_farm/artifacts/cards_rejected/` and states that a stale
   `g0_status` field inside that rejected artifact does not restore build
   authority (`docs/ops/evidence/2026-08-21_qm5_12921_gemini_build_review.md:6,11`).
2. This was an intentional governed retirement, not a missing-file accident.
   The durable retirement evidence says the cohort was moved from
   `cards_approved` to `cards_rejected` (`docs/ops/evidence/2026-07-20_p2_item11_retire_without_build_codex.md:5-6`),
   and classifies QM5_12921 as `BELOW_FIVE_TRADES_PER_YEAR` with two expected
   trades per year (`docs/ops/evidence/2026-07-20_p2_item11_retire_without_build_codex.md:40`).
3. A fresh exact-name search returned no QM5_12921 card under either
   `strategy-seeds/cards/approved/` or
   `D:/QM/strategy_farm/artifacts/cards_approved/`. It returned exactly one
   runtime card under `cards_rejected/`:
   `QM5_12921_qp-january-barometer-card.md`. That rejected artifact declares
   `expected_trades_per_year_per_symbol: 2` at line 25. Its internal
   `g0_status: APPROVED` at line 13 is historical content and cannot override
   its governed store location.
4. The review's implementation findings remain genuine and intentionally
   unfixed without card authority: January open uses shift-1 `PRICE_OPEN`
   (`framework/EAs/QM5_12921_qp-january-barometer-card/QM5_12921_qp-january-barometer-card.mq5:67`),
   the entry opportunity is cleared before validation/order success (`:110`),
   and Friday close is enabled and called (`:30`, `:204`). The review also
   records the unauthorized five-index expansion
   (`docs/ops/evidence/2026-08-21_qm5_12921_gemini_build_review.md:12-13`).
5. Registry preflight found the existing active EA row at
   `framework/registry/ea_id_registry.csv:3809` and existing active magic rows
   at `framework/registry/magic_numbers.csv:17610-17614`. There is no registry
   gap, so no registry or resolver mutation is authorized.

The `qm-build-ea-from-card` preflight requires an approved card and says to stop
on failure. The ticket independently directs a card defect beyond repair to
`BLOCKED`. Restoring or replacing this card would require new OWNER-governed
authorization and would be outside this rework ticket; changing the strategy to
evade the retirement criterion or changing that criterion is forbidden.

## Validation evidence

### Read-only hardening check

Command:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_12921_qp-january-barometer-card
```

Result: exit 0, scoped to one EA. The JSON reports `card_error: "card missing"`,
`card_path: null`, and no card target symbols. It also reports the five current
magic/setfile symbols and emits three undecidable-card warnings. The check does
not guess a contract from the rejected artifact.

### Scoped build check

Command:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_12921_qp-january-barometer-card -SkipCompile -SkipSetValidation
```

Result: exit 1 before validation with
`BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED` / `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`
because terminal64 processes were alive. No retry, bypass, compile enqueue, or
factory/terminal action was attempted. This is an infrastructure interlock,
not an EA verdict; the earlier approved-card preflight already blocks the build.

### Pytest

Directly relevant tests:

```text
python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py -q -k 'd17_explicit_card_universe or d17_real_clean_card_and_build_universe or nonmechanizable_semantic_classes'
...                                                                      [100%]
3 passed, 26 deselected in 0.84s

python -m pytest tools/strategy_farm/tests/test_retire_approved_cards.py -q
..............                                                           [100%]
14 passed in 3.13s
```

Broader attempted suite:

```text
python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_retire_approved_cards.py -q
1 failed, 42 passed in 440.33s
```

The sole failure is the unrelated repository-wide
`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`, which reports seven
pre-existing findings in other QM5_411xx EAs (first reported target:
`QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`). It does not inspect or fail
QM5_12921-specific behavior. The directly relevant hardening and retirement
tests pass above.

No new unit test was added: the governing state is an external card-authority
failure already covered by the retirement and hardening tests, and EA behavior
must not be encoded against a rejected card.

## Changed files

- `docs/ops/evidence/2026-08-23_rework-12921.md` — this blocked-disposition
  evidence only.

## Rollback

Revert the ticket commit to remove this evidence file. Git rollback does not
change router state; if the OWNER later supplies a newly approved card and
explicitly reopens the ticket, use `agent_router.py update-task` through the
governed router workflow. Do not move the rejected card by hand and do not alter
the historical retirement evidence.

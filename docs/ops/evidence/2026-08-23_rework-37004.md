# rework-37004 — QM5_37004 review-fix evidence

Date: 2026-08-24 (ticket evidence filename retains the requested 2026-08-23 work-order date)

Router task: `c344bb4a-ff9f-4561-a47f-66e7c753a202`

EA: `QM5_37004_volatility-targeted-momentum-kelly`

Disposition: **SOURCE_READY_COMPILE_HELD**. The five review findings are repaired in source and the scoped static gates pass. A fresh EX5 and runtime smoke verdict are intentionally not claimed because the active-factory compile interlock refused the scoped compile before MetaEditor ran.

## Work order and authority

- Approved card: `strategy-seeds/cards/approved/QM5_37004_volatility-targeted-momentum-kelly.md` (`g0_status: APPROVED`).
- Review work order: `C:/QM/repo/docs/ops/evidence/c344bb4a_qm5_37004_gemini_build_codex_review_2026-08-18.md`.
- Existing allocation was preserved: `framework/registry/ea_id_registry.csv:4467`; active magic slots remain `framework/registry/magic_numbers.csv:17461-17464`. No registry or resolver file changed.

## What changed

- Restored all declared card parameters and validates their authorized ranges/fixed loss thresholds (`framework/EAs/QM5_37004_volatility-targeted-momentum-kelly/QM5_37004_volatility-targeted-momentum-kelly.mq5:44`).
- Replaced endpoint price difference with a bounded, closed-D1 `CopyClose` read and exponentially weighted log-return momentum (`...mq5:96`, `...mq5:114`, `...mq5:127`). The single raw series call is explicitly `perf-allowed`, bounded to at most 301 closes, and guarded by `ArrayResize`, `CopyClose` count, and `ArraySize` checks (`...mq5:109-123`, `...mq5:134-160`).
- Implements 20-day sample volatility annualized by `sqrt(252)`, full Kelly from the approved 68.5%/2.5 priors, the 0.50 fractional multiplier, and 10% inverse-volatility targeting (`...mq5:147-176`). The resulting card weight scales `RISK_FIXED` in backtests and capped `RISK_PERCENT` in live mode through the explicit framework risk overload (`...mq5:225-243`, `...mq5:455-463`).
- Implements the 2.0% realized daily entry halt, 2.5% daily kill switch, 5.0% total-drawdown kill-switch trip, and three-tick deviation ceiling (`...mq5:197-220`, `...mq5:246-263`, `...mq5:367-388`).
- Runs MAE and protective open-position management before all entry-only new-bar/news/rollover/spread filters (`...mq5:404-453`). Magic identity continues through `QM_FrameworkMagic()` / `MagicResolver`, and the `QM_Common` include chain is retained (`...mq5:7`, `...mq5:317-326`, `...mq5:344-388`). No ML was added.
- Updated the SPEC to describe the implemented estimator, risk modes, card rails, D1 gating, and v2 rework (`framework/EAs/QM5_37004_volatility-targeted-momentum-kelly/SPEC.md:13-17`, `.../SPEC.md:23-42`, `.../SPEC.md:91-110`).
- Updated all four symbol presets with every strategy input, `RISK_FIXED=1000`, `RISK_PERCENT=0`, and the approved-card source. Their `build_hash` remains `pending` until a governed fresh compile (`framework/EAs/QM5_37004_volatility-targeted-momentum-kelly/sets/QM5_37004_volatility-targeted-momentum-kelly_SP500.DWX_D1_backtest.set:13-36`; identical contract in the NDX, XTIUSD, and XAUUSD presets).
- Added six regression tests covering the approved-card hardening result, estimator/sizing wiring, loss rails/slippage, management ordering, all-input use-sites, framework identity/MAE/performance/no-ML rules, and all four setfiles (`tools/strategy_farm/tests/test_qm5_37004_rework_static.py:53-143`).
- Removed the stale tracked EX5 (old SHA-256 `D46A00AEB2AC49268C66D3B0B98A570228C357F5EAF7003EA5B95AD3DEF3456A`) so it cannot be mistaken for a binary produced from the repaired source. Git history makes the removal recoverable.

Final MQ5 SHA-256: `500C226C948E3B01E654FCC9BD0578C8BD2B7DAA0F0498F95D6B04737FD573C5`.

## Input-use audit

Command:

```powershell
$inputs = Select-String -Path framework/EAs/QM5_37004_volatility-targeted-momentum-kelly/QM5_37004_volatility-targeted-momentum-kelly.mq5 -Pattern '^input\s+\S+\s+(\w+)'
# rg -o -w for every captured name; require at least declaration + one use
```

Result: all 27 declared inputs have at least one executable use-site; occurrence counts range from 2 to 6. This contract is also enforced by `test_every_declared_input_has_an_executable_use_site` at `tools/strategy_farm/tests/test_qm5_37004_rework_static.py:107`.

## Validation evidence

Focused EA regression:

```text
python -m pytest tools/strategy_farm/tests/test_qm5_37004_rework_static.py -q
......                                                                   [100%]
6 passed in 0.64s
```

Scoped hardening and contracts:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_37004_volatility-targeted-momentum-kelly
status PASS; failures=0; warnings=0; four symbols registry/matrix clean

python framework/scripts/skill_build_ea_guard.py --ea-id 37004 --ea-label QM5_37004_volatility-targeted-momentum-kelly
status ok; EA ID, directory, and existing magic allocations valid

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_37004_volatility-targeted-momentum-kelly
PASS 1/1

python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 <MQ5 and four explicit setfile paths>
PASS: MQ5 plus four presets; zero findings; news stale ceiling 336 hours

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_37004_volatility-targeted-momentum-kelly --fail-on-leak --json
SINGLE_SYMBOL_OK
```

Touched hardening module suite:

```text
python -m pytest tools/strategy_farm/tests/test_qm5_37004_rework_static.py tools/strategy_farm/tests/test_build_gate_hardening.py -q
34 passed, 1 failed in 409.13s
```

The sole failure was repository-wide `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`, which reports seven existing `QM5_411xx` sources. None of those files is modified in this worktree. The EA-specific hardening analysis and all six 37004 tests pass.

Formatting check:

```text
git diff --check
PASS (exit 0; line-ending conversion notices only)
```

## Compile interlock and residual risk

Exact scoped attempts (no unscoped build was run):

```text
powershell -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_37004_volatility-targeted-momentum-kelly/QM5_37004_volatility-targeted-momentum-kelly.mq5 -Strict
powershell -File framework/scripts/build_check.ps1 -EALabel QM5_37004_volatility-targeted-momentum-kelly -Strict -SkipCompile
```

Both stopped at the live-factory/include-mirror safety interlock with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` / `INCLUDE_MIRROR_REFUSED`; MetaEditor did not compile the EA. Durable compile summary from the final attempt: `D:/QM/reports/compile/20260824_101919/summary.csv` (`result=FAIL`, `reason_class=INCLUDE_MIRROR_REFUSED`, `errors=-1`, `warnings=-1`). The nominal per-EA log path in that row was not created because refusal occurred before compilation.

No factory process was stopped or toggled, no terminal was changed, no backtest was enqueued/deleted, no verdict row was overwritten, and no smoke or downstream pipeline verdict is inferred. The remaining risk is compile/runtime uncertainty until the governed factory obtains a safe compile window; presets deliberately retain `build_hash: pending` and no EX5 is supplied.

## Rollback

Revert the single ticket commit with `git revert <rework-37004-commit>`. This restores the prior MQ5, SPEC, presets, and tracked EX5 and removes the regression test/evidence file. Router state is operational metadata and, if review is rejected, should be advanced through the router's normal review/recycle workflow rather than edited directly.

# QM5_20211 FX cointegration Q04 priority advance and CPU stop

Date: 2026-08-21 UTC

Branch: `agents/board-advisor`

Status: one existing non-duplicate FX basket advanced in place; stopped after
the post-action CPU sample crossed the hard ceiling

## Decision

Do not create another Card or EA from the frozen sign-aware 66-pair FX
cointegration scan. Commit `a80493291` accounts for all 66 relationships, so
another scan-derived Card, registry allocation, EA, or basket manifest would
duplicate governed work. The preferred anchors are not blocked at Q02:

- `QM5_12532_AUDNZD_COINTEGRATION_D1` has Q02 PASS, Q04 PASS, then Q05 FAIL.
- `QM5_12533_EURJPY_GBPJPY_COINTEGRATION_D1` has Q02 PASS, then Q04 FAIL.

Rank 29 `GBPUSD.DWX` / `AUDJPY.DWX` (`QM5_20210`) already has one priority-
queued Q04 successor. Rank 30 `GBPUSD.DWX` / `AUDUSD.DWX` (`QM5_12739`) has a
terminal Q04 FAIL. The first unboosted relationship is therefore rank 31,
`GBPJPY.DWX` / `EURAUD.DWX`, implemented as
`QM5_20211_gbpjpy-euraud`.

Its logical Q02 work item `21db772c-c974-4a05-8e21-5ec78659e988` is a
terminal PASS with reason class `OK`; there is no current ONINIT or NO_HISTORY
blocker. Exactly one logical Q04 successor exists:
`8135f97c-fd0d-4435-b713-87fa74fe0053`.

## In-place advancement

Before mutation, the Q04 row was pending, unclaimed, attempt zero, canonical
scheduler rank 100 of 2,187 claimable rows, and had no priority marker.

At `2026-08-21T19:10:05Z`, one guarded `BEGIN IMMEDIATE` transaction under
the factory mutation lock added:

- `priority_track=true`;
- the current OWNER mission as authority;
- the non-duplicate rank-31 fallback reason; and
- the application timestamp.

The update used the exact work-item ID, EA, logical symbol, Q04 phase,
pending/unclaimed/attempt-zero state, full pre-image payload, and preserved
`updated_at` value as CAS guards. Exactly one row changed, and the exact-open-
row guard found one row. Farm event `368586` (`priority_boost_applied`) records
the payload hashes:

- before: `2c60d80d73a65b0f859014c2ca49c4d8ca2d1d54901a2c1546819e6335d8ed94`
- after: `77952afb830d013afe56c244640628f7028e16972da1730b95f727732f8abc25`

The canonical selector moved the same row to rank 3 without changing the
2,187-row claimable population. Its original queue timestamp, verdict,
attempt count, and claim state were preserved. No duplicate row, requeue,
timestamp restamp, dispatch tick, terminal reservation, or manual MT5 launch
occurred.

A pre-mutation SQLite backup is retained at
`D:/QM/strategy_farm/state/backups/farm_state_pre_qm5_20211_q04_priority_20260821T191005Z.sqlite`
with SHA-256
`f7f170f0285ee6166b07fe656b618f2c50464007332dbb7fedac543736693d6a`.
The backup and live database both returned `PRAGMA quick_check = ok`, and the
factory mutation lock released normally.

## Source and package contract

The approved Card retains G0 APPROVED and R1-R4 PASS. Its structural method
comes from the OWNER-ratified Tier-A extraction of Ernest Chan's
*Quantitative Trading*; pair selection is bound to the OWNER-requested frozen
Darwinex D1 scan. Chan makes no performance claim for GBPJPY/EURAUD.

The frozen evidence is adverse rather than promotional: DEV net Sharpe
`0.430254`, OOS net Sharpe `0.273190`, OOS return `4.833998%`, 19 state
changes, fixed beta `-1.386054145`, and a `33.694`-bar D1 half-life. The Card
permits one pipeline test and forbids beta refitting, rescue filters, pair
substitution, or parameter rescue after terminal economic failure.

The EA and magic registries remain active for the two traded legs:
`GBPJPY.DWX` (slot 0, magic `202110000`) and `EURAUD.DWX` (slot 1, magic
`202110001`). The basket manifest also declares `USDJPY.DWX`, `GBPUSD.DWX`,
`EURUSD.DWX`, and `AUDUSD.DWX` as conversion-history-only symbols.

| Artifact | SHA-256 |
|---|---|
| EA source | `b93fee38cc15e3a72eecb1ae189f6fabfe9a793221bf1f683d1414a5fafd43df` |
| EX5 | `54ddb7987359f0f5a8ad8806c0f6ea5582b23979c8b6814ea6610e3547bcdc56` |
| basket manifest | `2c3932c98d1efae4706466db012cfb3e050a6a3cf3a21afafed6a7952a896386` |
| logical backtest setfile | `93709b95ca6495ad903feb7e94b26462c2ecac9f27297a9a64d49f1f89ffd549` |
| approved Card | `d67cf0d7f7f89e8d9cc50f6f9157ba34f253b8dc36b6eb521dd3cd5c9aa74782` |
| EA Card snapshot | `d67cf0d7f7f89e8d9cc50f6f9157ba34f253b8dc36b6eb521dd3cd5c9aa74782` |

The logical setfile remains `RISK_FIXED=1000`, `RISK_PERCENT=0`, and
`PORTFOLIO_WEIGHT=1`. Strategy-card schema/ML lint and G0 lint both passed.
The package itself was not changed.

## Binding CPU stop

The five-sample pre-apply CPU reading was `58.52%`, `63.32%`, `63.30%`,
`63.29%`, and `85.27%`, below the explicit 97% ceiling. The post-action
sample was `89.66%`, `95.12%`, `97.66%`, `90.36%`, and `81.65%`. Its 97.66%
maximum crossed the hard ceiling.

At the final read six work items were active. The serialized multi-symbol lane
was occupied by rank-27 `QM5_20208_NZDUSD_EURAUD_COINTEGRATION_D1` on T5,
while the selected rank-31 row remained pending at rank 3. Per the mission
stop rule, no dispatch, tester launch, terminal reservation, reconciliation,
or terminal control followed.

Machine-readable evidence:
`artifacts/fx_cointegration_qm5_20211_q04_priority_20260821T191005Z_board_advisor.json`.

## Safety

- No portfolio admission, portfolio KPI, or Q08 contribution file changed.
- No T_Live manifest, process, or AutoTrading state changed.
- No Card, EA, EX5, setfile, basket manifest, registry, or magic row changed.
- Concurrent unrelated worktree changes were left unstaged and untouched.

# Review — QM5_11516 carter-t-sma7-21-cci5-m15 (M15)

- **Router task:** 38e4d67f-b6bf-41a2-be12-17acab9689d2 (review_ea, assigned claude)
- **Source build:** 53266c28 (gemini/agy), `docs/ops/evidence/53266c28_QM5_11516_carter-t-sma7-21-cci5-m15_build_identity.json`
- **Reviewer:** Claude (review lane) · **Date:** 2026-08-23
- **Verdict:** **APPROVED** (formally clean enough for Q02)

## Scope reviewed
- EA: `framework/EAs/QM5_11516_carter-t-sma7-21-cci5-m15/QM5_11516_carter-t-sma7-21-cci5-m15.mq5`
- SPEC: `framework/EAs/QM5_11516_carter-t-sma7-21-cci5-m15/SPEC.md`
- Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11516_carter-t-sma7-21-cci5-m15.md`
- Sets: `sets/*_{EURUSD,GBPUSD,USDJPY}.DWX_M15_backtest.set`
- Build identity JSON (above)

## Card-faithfulness of the mechanism — PASS
- Entry: SMA(7) crosses SMA(21) within `strategy_cross_lookback` bars (`FindSmaCrossShift`, mq5:76-96), synchronized with CCI(5) zero-cross within `strategy_sync_bars` (`FindCciZeroCrossShift` mq5:98-110; sync test mq5:203, 226). Matches card "SMA cross + CCI(5) zero-cross within ±1 candle".
- Exit: TP1 50% partial at 25 pips + move SL to BE (`Strategy_ManageOpenPosition` mq5:255-285); remainder exits when close crosses back across SMA(7) (`Strategy_ExitSignal` mq5:288-305). Matches card TP1/TP2.
- SL fixed 15 pips via `QM_StopFixedPips` (mq5:205, 228). Matches card.
- Card lists SMA(84)/SMA(336) but explicitly states they are "structural context only — not used in entry signal directly; can be added as P3 filter." EA correctly omits them from the entry signal — card-sanctioned, not a defect.

## Dead-input scan — PASS (0 dead inputs)
All 11 `strategy_*` inputs have use sites beyond declaration:
`strategy_sma_fast_period` (mq5:82,84,186,300), `strategy_sma_slow_period` (83,85,186), `strategy_cci_period` (101,102,187,199,222), `strategy_cross_lookback` (196,219), `strategy_sync_bars` (203,226), `strategy_sl_pips` (187,205,228), `strategy_tp1_pips` (265), `strategy_partial_close_ratio` (273), `strategy_be_buffer_pips` (152,280,281), `strategy_spread_cap_pips` (66), `strategy_no_friday_entry` (181).

## Framework conformity — PASS
- Include chain: `#include <QM/QM_Common.mqh>` (mq5:5); framework readers exist (`QM_SMA`/`QM_CCI` in `QM_Indicators.mqh`, `QM_StopFixedPips` in `QM_StopRules.mqh`).
- Magic: `QM_FrameworkMagic()` (mq5:127) → ea_id*10000+slot; magic rows consistent (`framework/registry/magic_numbers.csv:16099-16101` = 115160000/1/2 for slots 0/1/2). ea_id row present (`ea_id_registry.csv:2404`).
- Risk: RISK_FIXED backtest / RISK_PERCENT live inputs present (mq5:23-24); validated by `QM_FrameworkInit`.
- News filter wired: `QM_NewsAllowsTrade2` / legacy `QM_NewsAllowsTrade` in OnTick (mq5:376-381). `Strategy_NewsFilterHook` is an intentional pass-through stub (returns false).
- MAE hook present: `QM_FrameworkTrackOpenPositionMae()` (mq5:344). Kill switch, Friday close, equity stream, `OnTester`/`QM_DefaultObjective` all wired.
- No ML libraries, no `#import *.dll` (grep clean). No hardcoded commission/swap/DST.

## Build & set-file evidence — PASS
- `build_check_passed: true`; recorded `mq5_sha256 931a1646…` **matches current file byte-for-byte** → COMPILE_OK valid for current source hash. `.ex5` present (no raw MQ5 promotion).
- Set files use governed header (matches `gen_setfile.ps1` format): `environment: backtest`, `risk_mode: FIXED`, `RISK_FIXED=1000`, `RISK_PERCENT=0`, correct `magic_slot`, all strategy params appended.

## Minor observation (non-blocking)
- `Strategy_ExitSignal` reads `iClose(_Symbol, PERIOD_M15, 1)` directly (mq5:301) with a documented "perf-allowed: single closed-bar close; no QM close reader exists" comment. Acceptable, single closed-bar read.

## Verdict
**APPROVED** — mechanism is card-faithful, no dead inputs, framework-conformant, compile evidence valid for the current source hash, set files governed. Clear for Q02.

# Review — QM5_9961 bandy-hma-supertrend-confluence-trend

- Router task: `1bec9666-7684-442f-b85a-982a3a981eb4` (review_ea, assigned claude)
- Source build: `cf579137-6e4e-4044-a2c1-fb0a4dfa84bb` (agy/gemini lane, `BUILD_COMPLETE_PASS`)
- Build identity: `docs/ops/evidence/cf579137_qm5_9961_bandy-hma-supertrend-confluence-trend_build_identity.json`
- Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9961_bandy-hma-supertrend-confluence-trend.md`
- EA dir: `framework/EAs/QM5_9961_bandy-hma-supertrend-confluence-trend/`

## Verdict: APPROVED (formally clean enough for Q02)

## Card faithfulness
Mechanism matches the card. Dual-filter confluence entry — HMA(14)/HMA(50) ordering AND
Supertrend(10,3) directional state AND 200-SMA regime, fired only on the first bar of
confluence (`long_confluence_now && !long_confluence_prev`, mq5:243-244 / mirror 258-259).
Confluence-break exit on either filter flip + 60-bar time stop (`Strategy_ExitSignal`,
mq5:270-321). Catastrophic SL = 3.0×ATR(14) (mq5:246, 261). One position per magic
(mq5:186). D1-only gate (mq5:161). All consistent with card Mechanics section.
The card's optional `0.2*ATR` razor-thin ablation is explicitly optional and is not
required for card-faithfulness.

## Dead-input check (every strategy input grepped for use sites)
All 10 strategy inputs have use sites beyond declaration:
strategy_hma_fast(6), strategy_hma_slow(6), strategy_supertrend_period(3),
strategy_supertrend_mult(4), strategy_regime_sma_period(3), strategy_atr_period(3),
strategy_stop_atr_mult(4), strategy_max_hold_bars(3), strategy_warmup_bars(4),
strategy_max_spread_points(3). No dead inputs.

## Framework conformity
- `#include <QM/QM_Common.mqh>` present (mq5:5).
- Risk: `RISK_PERCENT=0.0`, `RISK_FIXED=1000.0` — correct backtest mode (mq5:26-27); set
  files carry `risk_mode: FIXED`, `RISK_FIXED=1000`, `RISK_PERCENT=0`, `environment: backtest`.
- Magic: via `QM_FrameworkMagic()` (mq5:184, 271); registry 13 rows = ea_id*10000+slot
  (99610000..99610012, slots aligned to symbols; `framework/registry/magic_numbers.csv`).
- News: wired through framework — `QM_NewsAllowsTrade2` in OnTick (mq5:387-391);
  `Strategy_NewsFilterHook` is an intentional no-op hook.
- MAE hook: `QM_FrameworkTrackOpenPositionMae()` present (mq5:363).
- No ML libs, no hardcoded commission/swap/DST values.

## Build / provenance evidence
- `build_check_passed: true`, `guardrails_verdict: PASS`, `symbol_scope_verdict:
  SINGLE_SYMBOL_OK` in build identity json.
- Current source SHA256 = `b23164335d7991e77c91ba1b4fd16414ed3618f1ffe3bee47d9381a6fdc8eccc`
  matches the recorded `mq5_sha256` — COMPILE_OK is for the current source hash.
- `.ex5` present (built 2026-08-23 09:23); no raw MQ5 promotion.
- 13 set files under `sets/` via governed generator (headered QM5 Set File, ea_id/slug/
  magic_slot/risk_mode stamped). Strategy params not pinned in sets
  (`card_defaults_status=none_found`); backtest relies on EA input defaults, which match
  the card parameters — acceptable.

## Non-blocking notes
- Set-file `build_hash` (dated 2026-08-22) predates the final mq5 (2026-08-23) and is a
  separate hash convention from `mq5_sha256`; sets are regenerated at Q02 dispatch via the
  governed path, so this is not a defect.

Formally clean. Cleared for Q02.

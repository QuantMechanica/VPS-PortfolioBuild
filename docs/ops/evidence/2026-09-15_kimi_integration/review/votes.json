[
 {
  "refuted": false,
  "reason": "Confirmed, not refuted. The R-D two-scope guard and the repo_write receipt contract are genuinely unmet. run_kimi's mutation guard hashes only C:/QM/repo via _repo_porcelain_hash (kimi_adapter.py:556 and :606-607); repo_write is computed solely from that repo delta (:607). No before/after hashing of farm_state.sqlite, D:/QM/strategy_farm/state/, or D:/QM/reports/state/ is performed. _dir_listing_hash (:249-260) is dead code — grep across tools/strategy_farm/ shows only its definition, never a call site. This contradicts three binding sources: (a) INTERNAL_RESEARCH_SOURCE_CONTRACT.md:266 defines critic_receipt.repo_write as true if the critic wrote anything to C:/QM/repo OR the D:/QM verdict/evidence paths (R-D), reinforced at :310-311; (b) KIMI_INTEGRATION_ARCHITECTURE.md:118 mandates a content hash of farm_state.sqlite / D:/QM/strategy_farm/state/ / D:/QM/reports/state/ before and after every critic run, and §14 (:357) names the required regression test test_critic_write_dqm_state_fails_run; (c) research_source.py:788 fails closed only on repo_write is True. Consequence is real: a critic that wrote only under D:/QM produces repo_write=false and passes verify. The named test test_critic_write_dqm_state_fails_run does not exist in test_kimi_adapter.py; the sole guard test (test_critic_repo_write_becomes_critic_wrote, :288) monkeypatches _repo_porcelain_hash and never touches D:/QM. The finding's own 'mitigated by the critic agent-file removing Bash/shell' caveat is correct (that is the primary posture-denial layer), but the architecture deliberately specifies a layered two-scope detector as the secondary belt, and the code ships one scope while the receipt field advertises two.",
  "evidence": "kimi_adapter.py:556 repo_before=_repo_porcelain_hash(repo_root); :606-607 repo_after + repo_write=bool(...repo_before!=repo_after) — repo-only. :249-260 _dir_listing_hash defined but never called (grep tools/strategy_farm/: sole hit is the def). INTERNAL_RESEARCH_SOURCE_CONTRACT.md:266 repo_write = wrote anything to C:/QM/repo OR D:/QM verdict/evidence paths (R-D); :310-311 guard extended to D:/QM before/after. KIMI_INTEGRATION_ARCHITECTURE.md:118 hash of farm_state.sqlite + D:/QM/strategy_farm/state/ + D:/QM/reports/state/ before/after every critic run; :357 names test_critic_write_dqm_state_fails_run. research_source.py:788 fails closed on repo_write is True. test_kimi_adapter.py: no test_critic_write_dqm_state_fails_run (grep empty); only test_critic_repo_write_becomes_critic_wrote at :288 monkeypatches _repo_porcelain_hash."
 },
 {
  "refuted": false,
  "reason": "The finding is confirmed, not refuted, and is reproducible against exact lines. kimi_governor.reconcile_flag (kimi_governor.py:288-319) writes the KIMI_LOW_QUOTA.flag ONLY on EXHAUSTED (line 299-303); the NORMAL/CONSERVE else-branch (308-319) clears or noops and never writes a flag. _write_flag (276-285) emits a MANAGED_BY/set_at/reason/counts body with no `state=` line and no JSON state field. The governor's own unit test test_conserve_does_not_write_hard_flag (test_kimi_governor.py:180-184) asserts that on CONSERVE no flag exists — confirming this is the governor's designed behavior, not speculation. The two consumers key exclusively off the flag: agent_router.kimi_quota_state (agent_router.py:715-743) returns NORMAL for an absent flag (line 727-728), so a governor CONSERVE is read as NORMAL and the CONSERVE lane narrowing (1239-1240) and route_once exclusion (1994) never fire; agent_chain reads _read_kimi_flag_state only when flag.exists() (agent_chain.py:288), so an absent flag yields state=None and the CONSERVE critic-only gate (298-305) is unreachable. The chain's own docstring (agent_chain.py:179-180) admits the governor 'today writes ONLY on EXHAUSTED.' Neither consumer reads kimi_governor_state.json (no governor_state/kimi_governor reference in agent_router.py; agent_chain's kimi branch reads only the flag), so there is no alternate CONSERVE transport. Net: governor-computed CONSERVE is never transmitted to either plane; only NORMAL and EXHAUSTED take effect, and EXHAUSTED works only via the fail-closed accident of an unparseable body. The safety gap is real.",
  "evidence": "kimi_governor.py:299-303 (flag SET only on EXHAUSTED), :308-319 (NORMAL/CONSERVE branch never writes), :276-285 (_write_flag body has no state field); tests/test_kimi_governor.py:180-184 (asserts CONSERVE writes no flag); agent_router.py:727-728 (absent flag => NORMAL), :1239-1240 & :1994 (CONSERVE machinery gated on kimi_state); agent_chain.py:288 (state read only if flag.exists()), :298-305 (CONSERVE branch), :179-180 (docstring admits governor writes flag only on EXHAUSTED); grep confirms neither agent_router.py nor agent_chain.py kimi gate reads kimi_governor_state.json."
 },
 {
  "refuted": false,
  "reason": "The finding holds. No test in the Kimi suite locks the \"non-kimi lanes keep routing when Kimi is unavailable\" invariant. `test_exhausted_disables_kimi_lane` (test_agent_router_kimi_lane.py:157-176) asserts only (a) the kimi agent_registry row goes enabled=0/max_parallel=0 and (b) a kimi-ONLY task type (research_edge_discovery) gets no lane; it never enqueues a build_ea/ops_issue under EXHAUSTED and asserts those still route to codex/gemini/claude, and it does not assert the codex/claude/gemini rows remain enabled after the EXHAUSTED sync. The nearest positive test, test_build_ea_and_ops_issue_never_route_to_kimi (line 96-105), runs under `_sync_normal` (kimi UP) and only proves build_ea/ops never go to kimi — it does not exercise the kimi-unavailable failure condition at all. A cross-file grep across all five kimi tests plus the whole tests/ tree finds no case co-locating EXHAUSTED/cli_missing with a build_ea/ops routing assertion. cli_missing has no router-level unavailability test either. So the mandatory directive-22 item is genuinely uncovered. One nuance that narrows (not refutes) the finding: the backtest/MT5-saturation portion is architecturally decoupled — agent_router never reads/writes MT5 worker or backtest dispatch (that lives in terminal_worker/farmctl), and test_quota_spawn_gate.py:140 (test_deterministic_backtests_are_never_quota_gated) independently locks that backtests are never gated — so the meaningful missing coverage is specifically the non-kimi AGENT lanes (build_ea/ops_issue) continuing to route while the kimi row is EXHAUSTED/cli_missing, which the proposed fix correctly targets and which is cheap to add on the existing sync_default_registry/_route harness.",
  "evidence": "tools/strategy_farm/tests/test_agent_router_kimi_lane.py:157 (test_exhausted_disables_kimi_lane — asserts kimi row disabled + kimi-only task unrouted only); :96 (test_build_ea_and_ops_issue_never_route_to_kimi — runs under NORMAL via _sync_normal, not EXHAUSTED); grep of EXHAUSTED/cli_missing across all kimi tests shows no build_ea/ops/backtest routing assertion under kimi-down; contrast test_quota_spawn_gate.py:140 which does lock backtests-never-gated but is unrelated to kimi state."
 },
 {
  "refuted": false,
  "reason": "Finding confirmed at the exact lines. kimi_governor.reconcile_flag (kimi_governor.py:299-321) writes KIMI_LOW_QUOTA.flag ONLY in the state=='EXHAUSTED' branch; NORMAL/CONSERVE clear or leave the flag absent, and _write_flag (kimi_governor.py:276-285) writes a key=value body with no state= key and no JSON, so no flag ever carries CONSERVE. test_kimi_governor.py:180-184 locks this in. Both consumers read state only from the flag: agent_router.kimi_quota_state (agent_router.py:715-743) returns NORMAL when the flag is absent and only honors CONSERVE from a JSON state field the governor never writes; agent_chain._read_kimi_flag_state (agent_chain.py:174-200) fails an absent flag open and any stateless present flag closed to EXHAUSTED, never CONSERVE. Grep confirms neither plane (agent_router.py, agent_chain.py, run_agent_orchestration_task.py) reads kimi_governor_state.json — the state_path references at agent_router.py:1863/2036 belong to quota_spawn_gate, not the kimi governor. Therefore compute_state's CONSERVE result (kimi_governor.py:216-225) reaches only kimi_governor_state.json, which nothing routes on. The three CONSERVE-gated branches (sync_default_registry capability narrowing agent_router.py:1239; route-time task-type gate agent_router.py:1994; agent_chain CONSERVE critic-only path agent_chain.py:298) are all keyed on kimi_state==CONSERVE, a value unreachable in production. The CONSERVE machinery is dead; only NORMAL (full access) and EXHAUSTED (full disable) are live, exactly as the finding states.",
  "evidence": "kimi_governor.py:299-321 (reconcile_flag writes flag only on EXHAUSTED); kimi_governor.py:276-285 (_write_flag body has no state=, not JSON); kimi_governor.py:216-225 (compute_state can return CONSERVE, written only to state json); agent_router.py:725-743 (kimi_quota_state: absent flag->NORMAL, CONSERVE only from JSON state field); agent_router.py:1239 + 1994 (CONSERVE branches gated on kimi_state==CONSERVE); agent_chain.py:182-200 (_read_kimi_flag_state reads only flag, fails to EXHAUSTED not CONSERVE); agent_chain.py:298 (CONSERVE critic-only path); test_kimi_governor.py:180-184 (test_conserve_does_not_write_hard_flag); grep: no consumer reads kimi_governor_state.json."
 },
 {
  "refuted": false,
  "reason": "The finding is CONFIRMED, not refutable. Every load-bearing claim checks out against the exact lines, and the one plausible refutation (the addendum narrowed the guard to repo-only) does not survive reading the addendum.\n\nCode facts:\n- The critic mutation guard is repo-only: repo_root = C:/QM/repo (kimi_adapter.py:554), before/after hashes use only _repo_porcelain_hash(repo_root) (kimi_adapter.py:556 and :606), and repo_write is derived solely from those two repo hashes (kimi_adapter.py:607). No before/after hashing of farm_state.sqlite, D:/QM/strategy_farm/state/, or D:/QM/reports/state/ exists anywhere in the file.\n- _dir_listing_hash (kimi_adapter.py:249-260) is dead code. grep across the module returns only the definition line (249) with zero call sites; it is the obvious intended D:/QM detector, never wired in.\n- research_source.verify fails closed only on the repo_write flag (research_source.py:788-789 -> REASON_CRITIC_WROTE). So a critic that wrote to D:/QM would carry repo_write=false and pass verify — the exact gap the finding describes.\n- The named regression test test_critic_write_dqm_state_fails_run does not exist: grep over tools/strategy_farm/tests/ finds no dqm/dqm_state test; test_kimi_adapter.py has only test_critic_repo_write_becomes_critic_wrote (:288) and test_creator_repo_write_flag_but_status_ok (:299).\n\nSpec facts (the mismatch is real and binding):\n- INTERNAL_RESEARCH_SOURCE_CONTRACT.md §4.5 line 266 defines critic_receipt.repo_write as \"true if the critic wrote anything to C:/QM/repo OR the D:/QM verdict/evidence paths (R-D)\"; §5 step 2 (line 310) requires the adapter to hash C:/QM/repo \"(extended to the D:/QM verdict/evidence paths) before and after the critic run.\"\n- KIMI_INTEGRATION_ARCHITECTURE.md still mandates D:/QM scope post-addendum: §3.5 line 118 spells out \"a content hash of the D:/QM verdict/evidence state paths (farm_state.sqlite, D:/QM/strategy_farm/state/, D:/QM/reports/state/) before and after\"; §9 line 316 and F1 disposition line 420 reaffirm \"repo + D:/QM scope\"; and the test matrix line 357 explicitly lists test_critic_write_dqm_state_fails_run as required.\n\nRefutation attempted and rejected: the addendum (line 432-438) does NOT withdraw the D:/QM scope. It explicitly withdraws only the --auto assumption (\"§3.1's --auto assumption is withdrawn\", line 434) and states the C:/QM/repo git guard \"stays as the detection belt\" (line 435) — it never amends §3.5, §10, or the line-357 test matrix, all of which still require the D:/QM detector. So the code diverges from the still-binding contract and architecture, not from a superseded version.\n\nSeverity nuance (does not refute): the finding correctly self-labels the exposure as mitigated-not-closed. Primary prevention is the critic agent-file tools allowlist (config kimi_adapter.v1.json:61 lists only Read/Grep/Glob/List; no Write/Edit/Bash), so a live D:/QM write requires the posture to fail first, making this a defense-in-depth / spec-conformance gap rather than an independently reproducible exploit. But two claims stand on their own regardless of reproducibility: (a) the repo_write receipt field does not meet its own contract §4.5 semantics, and (b) verify trusts an under-scoped flag while the mandated regression test is absent. The proposed fix (wire _dir_listing_hash over the D:/QM state paths into the before/after guard, fold into repo_write, add test_critic_write_dqm_state_fails_run; OR formally amend §4.5/§5/§10/test-matrix to repo-only) is the correct disposition.",
  "evidence": "kimi_adapter.py:554,556,606-607 (repo-only guard); kimi_adapter.py:249-260 + grep (dead _dir_listing_hash, 0 call sites); research_source.py:788-789 (verify fails closed on repo_write only); INTERNAL_RESEARCH_SOURCE_CONTRACT.md §4.5 line 266 + §5 line 310 (D:/QM in repo_write definition); KIMI_INTEGRATION_ARCHITECTURE.md §3.5 line 118, §9 line 316, F1 line 420, test matrix line 357 (D:/QM scope + test_critic_write_dqm_state_fails_run required); addendum lines 432-438 (withdraws only --auto, keeps repo git guard, does not touch D:/QM scope); tests dir grep (no dqm test); kimi_adapter.v1.json:61 (critic tools allowlist = mitigation)."
 },
 {
  "refuted": false,
  "reason": "The finding is confirmed, not refuted. Directive §22 (docs/ops/evidence/2026-09-15_kimi_integration/owner_directive_verbatim.md:93) explicitly lists \"no effect on MT5 workers if Kimi is unavailable\" as a mandatory pre-enablement test item. No test in the suite locks this invariant. test_exhausted_disables_kimi_lane (test_agent_router_kimi_lane.py:157-176) asserts only that the kimi registry row is disabled (enabled=0/max_parallel=0, lines 171-172) and that a kimi-only task type gets no lane (assigned_agent is None, lines 174-176); it never enqueues a build_ea/ops_issue/backtest item under EXHAUSTED and asserts it still routes. test_build_ea_and_ops_issue_never_route_to_kimi (lines 96-105) runs under NORMAL state (_sync_normal, kimi enabled) and only asserts assertNotEqual(assigned_agent, \"kimi\") — it does not exercise the unavailable condition and does not even assert positive routing to a non-kimi lane (a None result would also pass). A broad grep across all tools/strategy_farm/tests/ files finds no test binding kimi-unavailable (EXHAUSTED/cli_missing) to build_ea/ops_issue/backtest-queue/MT5-worker continuing to function; the cli_missing tests in test_kimi_adapter.py/test_kimi_governor.py/test_run_agent_orchestration_kimi.py/test_agent_chain_kimi.py only assert the kimi lane itself fails closed. The structural argument that agent_router never touches terminal-worker backtest dispatch does not refute a finding about a directive-mandated explicit test item that is genuinely absent.",
  "evidence": "docs/ops/evidence/2026-09-15_kimi_integration/owner_directive_verbatim.md:93 (mandatory item text). tools/strategy_farm/tests/test_agent_router_kimi_lane.py:157-176 (test_exhausted_disables_kimi_lane asserts only kimi-row disabled + kimi-only task None). tools/strategy_farm/tests/test_agent_router_kimi_lane.py:96-105 (test_build_ea_and_ops_issue_never_route_to_kimi runs under NORMAL, only assertNotEqual \"kimi\", no positive-routing and no unavailable condition). Broad grep for kimi.*(build_ea|ops_issue|backtest) / (EXHAUSTED|cli_missing) across tools/strategy_farm/tests/ returns no matching invariant test (only unrelated dl089 backtest work_item rows)."
 },
 {
  "refuted": false,
  "reason": "Finding confirmed by direct code+test reading; cannot be refuted. The real governor never conveys CONSERVE to routing, so the CONSERVE-narrowing paths are dead in production and no test bridges governor output to the router/chain parsers. (1) kimi_governor.reconcile_flag (kimi_governor.py:288-321) writes KIMI_LOW_QUOTA.flag only when state==EXHAUSTED; NORMAL/CONSERVE clears/no-ops it (verified by test_conserve_does_not_write_hard_flag, test_kimi_governor.py:180-184). (2) The router's kimi_state comes solely from kimi_quota_state (agent_router.py:1909), which returns CONSERVE only from JSON {\"state\":\"CONSERVE\"} (agent_router.py:715-743); a CONSERVE governor writes no flag, so the router reads absent-flag=>NORMAL and the CONSERVE narrowing at agent_router.py:1994 never runs. (3) The chain reads state via _read_kimi_flag_state only when flag.exists() (agent_chain.py:288); absent flag=>None, so the CONSERVE critic-only branch at agent_chain.py:298 never runs. (4) The governor DOES write full state to D:/QM/reports/state/kimi_governor_state.json (kimi_governor.py:337-340) carrying state:CONSERVE, but neither router nor chain reads it (grep shows both consume only KIMI_LOW_QUOTA.flag) — no wired channel carries CONSERVE. (5) Every CONSERVE test injects a hand-authored JSON flag via _write_flag json.dumps({\"state\":state}) (test_agent_router_kimi_lane.py:109-112, test_agent_chain_kimi.py:146); none feeds a governor-produced artifact into the reader parsers. (6) The governor's real EXHAUSTED body is non-JSON MANAGED_BY=... (kimi_governor.py:276-285), which the router honours only via its fail-closed json.loads-except default (agent_router.py:735-742), untested against the governor's actual body. The CONSERVE narrowing is the documented F4 protection (KIMI_INTEGRATION_ARCHITECTURE.md:245, 423) against generic research_strategy capturing the kimi lane when gemini is down; it is unreachable end-to-end. Genuine defect masked by a test gap.",
  "evidence": "tools/strategy_farm/kimi_governor.py:288-321 (reconcile_flag EXHAUSTED-only write); :276-285 (_write_flag MANAGED_BY non-JSON body); :337-340 (state file write, not consumed by router/chain). tools/strategy_farm/agent_router.py:715-743 (kimi_quota_state JSON-state-only, non-JSON=>EXHAUSTED except branch); :1909 (kimi_state sourced only from flag); :1994 (CONSERVE narrowing unreachable). tools/strategy_farm/agent_chain.py:288 (state read gated on flag.exists()); :298 (CONSERVE branch unreachable); :174-200 (_read_kimi_flag_state). Tests: tools/strategy_farm/tests/test_kimi_governor.py:180-184 (CONSERVE writes no flag), :189-196 (evaluate end-to-end tests EXHAUSTED only); tools/strategy_farm/tests/test_agent_router_kimi_lane.py:109-112 (_write_flag json.dumps), :114,:131 (CONSERVE via injected flag); tools/strategy_farm/tests/test_agent_chain_kimi.py:146 (CONSERVE via injected flag). Docs: docs/ops/KIMI_INTEGRATION_ARCHITECTURE.md:245-246 (flag written on EXHAUSTED only; CONSERVE intended to narrow), :423 (F4 protection rationale)."
 },
 {
  "refuted": false,
  "reason": "Could not refute — the finding is confirmed by the code AND by the governor's own test. The flag-body contract is genuinely mutually inconsistent and CONSERVE cannot be carried from the governor to the router/chain.\n\nCORE CONTRADICTION (two directly opposed design intents inside the same commit set):\n1. Router BELIEVES the governor expresses CONSERVE via a JSON flag body. agent_router.py:82-85 comment verbatim: the flag body \"may carry a JSON object with a 'state' field so kimi_governor.py can express CONSERVE ... or EXHAUSTED\". agent_router.py:734-743 (kimi_quota_state) parses the body with json.loads ONLY, reads data.get(\"state\"), and returns CONSERVE only for an explicit JSON {\"state\":\"CONSERVE\"}; any non-JSON body → KIMI_QUOTA_EXHAUSTED; absent flag → NORMAL.\n2. Governor NEVER writes CONSERVE and never writes JSON. kimi_governor.py:299-319 (reconcile_flag) writes the flag ONLY on EXHAUSTED (line 299); for NORMAL/CONSERVE the flag \"should not be present\" (line 308) and is cleared/left absent. kimi_governor.py:276-285 (_write_flag) writes key=value lines with first line \"MANAGED_BY=kimi_governor\", not JSON. Its own test test_kimi_governor.py:180-184 (test_conserve_does_not_write_hard_flag) ASSERTS that on CONSERVE the flag does not exist.\n\nUNRESOLVABLE BODY CONFLICT (the finding's central claim, verified): the governor's ownership check _flag_owned (kimi_governor.py:268-273) requires the literal first line to equal \"MANAGED_BY=kimi_governor\". A JSON body (what the router needs for CONSERVE) would have first line \"{...}\", so _flag_owned returns False → the governor would treat its own flag as foreign-owned and never clear it (reconcile_flag line 307 \"leave-external\"). Therefore no single body satisfies both (a) router's JSON-state CONSERVE requirement and (b) governor's MANAGED_BY-first-line ownership check. The chain reader (agent_chain.py:174-200) tolerates all three shapes but that only masks, not resolves, the writer/reader disagreement.\n\nCONSEQUENCE (real correctness defect, not cosmetic): because the governor clears/omits the flag on CONSERVE, the router's kimi_quota_state returns NORMAL when the governor computed CONSERVE. The router's CONSERVE narrowing (agent_router.py:1239-1240 KIMI_CONSERVE_CAPABILITIES, :1994 KIMI_CONSERVE_TASK_TYPES) and the chain's CONSERVE branch are dead with respect to the governor — nothing writes a JSON CONSERVE flag. This defeats the F4/R-G quota-conservation protection (KIMI_INTEGRATION_ARCHITECTURE.md §5.2/§7.2, lines 194/245/305): kimi stays fully routable at NORMAL through the entire 70%→100% CONSERVE band, burning the fixed one-month subscription on exactly the low-value generic research the OWNER said Kimi is not for, until EXHAUSTED (100%) finally hits. The router comment naming \"kimi_governor.py\" as the CONSERVE writer makes this an explicit, not implied, contract breach.\n\nREFUTATION ATTEMPTS THAT FAILED: (a) \"CONSERVE applied by another mechanism\" — no; the router's only CONSERVE source is the flag (kimi_quota_state reads nothing else; no read of kimi_governor_state.json in the router). (b) \"Flag is EXHAUSTED-only by design\" — the governor is EXHAUSTED-only, but the router explicitly documents and codes a CONSERVE-via-flag path attributed to kimi_governor.py, so the two halves shipped inconsistent. (c) \"EXHAUSTED still works\" — true, but only by fail-closed accident (key=value body → json.loads ValueError → EXHAUSTED), which the finding itself states.\n\nSeverity \"major\" is correct: CONSERVE quota-conservation is inert (functional gap), but the failure direction is safe (fail-closed to EXHAUSTED/NORMAL, backtests untouched), so not blocking. Proposed fix (single canonical JSON body {\"managed_by\",\"state\"} across _write_flag/_flag_owned/kimi_quota_state/_read_kimi_flag_state + an end-to-end test that a governor-written CONSERVE flag narrows both planes) is sound.",
  "evidence": "agent_router.py:82-86 (comment: flag body JSON state expresses CONSERVE via kimi_governor.py); agent_router.py:715-743 (kimi_quota_state: json.loads-only, non-JSON→EXHAUSTED, CONSERVE only from JSON {\"state\":\"CONSERVE\"}); kimi_governor.py:268-273 (_flag_owned: first line must == \"MANAGED_BY=kimi_governor\"); kimi_governor.py:276-285 (_write_flag: key=value body, MANAGED_BY first line, not JSON); kimi_governor.py:299-319 (reconcile_flag: writes only on EXHAUSTED, CONSERVE→absent/clear); tests/test_kimi_governor.py:180-184 (test_conserve_does_not_write_hard_flag asserts CONSERVE writes no flag); agent_chain.py:174-200 (_read_kimi_flag_state tolerates JSON / key=value state= / MANAGED_BY-only→EXHAUSTED); router CONSERVE narrowing that becomes unreachable: agent_router.py:1236-1240 and :1994; architecture intent defeated: docs/ops/KIMI_INTEGRATION_ARCHITECTURE.md lines 194, 245, 305 (§5.2/§7.2 CONSERVE excludes generic research_strategy from kimi)."
 },
 {
  "refuted": false,
  "reason": "The finding is confirmed on every axis I checked; it is not speculative, and the guards it claims are missing are genuinely absent from the code. Refutation attempts all fail:\n\n1) Config/argv posture (CONFIRMED): kimi_adapter.v1.json lines 55-56 give roles creator and research `read_only:false, use_agent_file:false`; only critic (line 58) has `use_agent_file:true` plus an agent-file whose `tools:` allowlist omits Write/Edit/Bash and whose body says \"never run shell commands\" (line 61). build_argv (kimi_adapter.py:397-399) attaches the tool-restricting `--agent-file` ONLY when use_agent_file is set, so research/creator runs carry no tool restriction. The very existence of a shell-denying agent-file for the critic proves the default (non-critic) Kimi posture retains shell — otherwise that denial would be pointless.\n\n2) Mutation guard is critic-only and detection-only (CONFIRMED): is_critic = read_only (kimi_adapter.py:555); the critic_wrote conversion is gated `if is_critic and repo_write` (line 608). For creator/research a repo write yields status ok with only a repo_write flag — asserted verbatim by test_creator_repo_write_flag_but_status_ok (test_kimi_adapter.py:299-306: \"a creator may legitimately write; repo_write is recorded but status stays ok\").\n\n3) Guard is repo-only (CONFIRMED): run_kimi computes repo_before/repo_after via _repo_porcelain_hash on C:/QM/repo only (kimi_adapter.py:556,606). _dir_listing_hash is defined (line 249) but NEVER called; grep shows no D:/QM or T_Live change detection anywhere in the guard path. This directly contradicts the doc's own F1 resolution (KIMI_INTEGRATION_ARCHITECTURE.md:420) which claims \"adapter hash guard extended to both repo and D:/QM state paths\" — that extension is not implemented.\n\n4) The orchestration lane REQUIRES shell and enforces authority only by prompt (CONFIRMED, strongest evidence): run_agent_orchestration_task.py spawns the Kimi orchestration slot with role=\"research\" (line 935). The prompt it builds instructs the model to run `python agent_router.py list-tasks ...` (line 255), `python agent_router.py update-task <id> --state REVIEW --verdict ...` (lines 261,330) and `python farmctl.py health` (lines 268,309,333) — all of which require a shell/exec tool. The only thing preventing farmctl/close-review mutations is the natural-language hard rule \"NEVER run any farmctl mutation subcommand (no approve-card, close-review ...)\" (lines 266-268) — a prompt-level constraint, exactly as the finding states.\n\n5) Chain creator worsens it (CONFIRMED): agent_chain._run_kimi (agent_chain.py:770-774) calls kimi_adapter.run_kimi with role passed through (creator/critic/formatter) and add_dirs=[REPO_ROOT, out_dir] where REPO_ROOT resolves to the live C:/QM/repo (line 54); resolve_creator (line 400-406) accepts a kimi creator whenever vendor_gate returns open, and vendor_gate's kimi branch returns None (open) in NORMAL when the flag is not EXHAUSTED and CLI+cred exist (lines 286-306). So a non-read-only Kimi creator runs with the live repo in --add-dir. \"Worktree isolation\" (doc line 113) isolates a git branch, not a shell's process/filesystem authority: a shell can cd/write outside the worktree, touch C:/QM/mt5/T_Live, or invoke farmctl against C:/QM/repo regardless of --add-dir.\n\nConsequently the KIMI_INTEGRATION_ARCHITECTURE.md §4.2 \"Code-level guards (mechanical)\" claims (lines 152-154) — \"No Kimi branch has a verdict-write path ... no farmctl subcommand is callable by it ... independent of any prompt instruction\" and \"T_Live paths are never placed in --add-dir\" — are false for the research/creator roles: farmctl IS callable (health is explicitly instructed) and mutation avoidance rests solely on prompt hard-rules. Capability omission (agent_router.py TASK_TYPE_CAPABILITIES) only governs router ASSIGNMENT, not the spawned process, so it does not confine the shell. Note §3.5 (lines 109-124) is more honest and even states the guard is \"detection, not prevention\" reachable \"by any shell tool,\" but the R-D primary prevention (agent-file denying shell) it describes is wired only to the critic role, not to research/creator. Severity \"major (safety)\" is appropriate; the proposed fix (extend the deny-shell --agent-file posture to research/creator, or isolate with the repo read-only and D:/QM+T_Live off every reachable path, move the REVIEW transition to the orchestrator, and at minimum extend a fail-on-mutation guard to research covering D:/QM and T_Live, plus correct the doc) is sound.",
  "evidence": "tools/strategy_farm/config/kimi_adapter.v1.json:55-58,61; tools/strategy_farm/kimi_adapter.py:397-399,555,556,606,607,608,249(unused); tools/strategy_farm/tests/test_kimi_adapter.py:299-306; tools/strategy_farm/agent_chain.py:54,400-406,286-306,770-774; tools/strategy_farm/run_agent_orchestration_task.py:255,261,266-268,309,330,333,935; docs/ops/KIMI_INTEGRATION_ARCHITECTURE.md:109-124,141-154(esp.152-153),420(D:/QM guard claim unimplemented)"
 },
 {
  "refuted": false,
  "reason": "The finding is confirmed and directly reproducible; it is not speculative and is not handled elsewhere. kimi_adapter.py has no distinct schema-failure classification: the only status is STATUS_MALFORMED (line 59), and the run path at lines 588-591 maps every rc==0-but-unparseable case to status='malformed_output', error='empty_or_unparseable_output'. parse_stream_json (lines 111-132) internally tracks saw_any_json but returns None for BOTH valid-JSON-wrong-schema (saw_any_json=True, answer=None) and non-JSON garbage (saw_any_json=False), so the distinction is never propagated to the caller. No STATUS_SCHEMA_MISMATCH/schema_mismatch constant exists. Tests confirm the gap: the full test-def list contains no test_stream_json_schema_mismatch_class and no test_malformed_stream_json_class; only test_malformed_output_when_rc0_but_unparseable (line 278) exists and it feeds an empty string, while test_parse_stream_json_ignores_meta_and_tool_and_garbage (line 85) merely asserts garbage->None. This violates architecture 3.3/F9 (KIMI_INTEGRATION_ARCHITECTURE.md line 89: parser must 'fail closed on valid JSON with an unexpected/renamed event set... classified distinctly from non-JSON garbage'). The doc even asserts the fix landed (line 347 names test_stream_json_schema_mismatch_class; line 428 F9 row says 'applied. Split into test_malformed_stream_json_class and test_stream_json_schema_mismatch_class; added schema_mismatch error class') but none of those artifacts exist in the delivered code, making the finding stronger, not weaker. Cannot point to any line that makes the finding false.",
  "evidence": "kimi_adapter.py:59 (STATUS_MALFORMED only; no schema_mismatch constant); kimi_adapter.py:111-132 (parse_stream_json returns None for both wrong-schema and garbage, saw_any_json distinction not surfaced); kimi_adapter.py:588-591 (all rc0-unparseable -> STATUS_MALFORMED/'empty_or_unparseable_output'); grep for schema_mismatch/SCHEMA_MISMATCH in kimi_adapter.py returns nothing; test_kimi_adapter.py full test-def list (lines 79-341) contains no schema-mismatch test; test_kimi_adapter.py:278 test_malformed_output_when_rc0_but_unparseable feeds empty string; test_kimi_adapter.py:85-89 asserts garbage->None only; KIMI_INTEGRATION_ARCHITECTURE.md:89 F9 requirement; KIMI_INTEGRATION_ARCHITECTURE.md:347 and :428 falsely claim the schema_mismatch class and both tests were applied."
 },
 {
  "refuted": false,
  "reason": "Could not refute — every load-bearing claim verifies at the cited lines and the architecture doc genuinely overclaims mechanical enforcement that only the critic role has. (1) kimi_adapter.v1.json:55-56 gives research/creator use_agent_file=false,read_only=false; only critic gets use_agent_file=true. (2) build_argv (kimi_adapter.py:397-399) attaches the tool-restricting --agent-file ONLY when use_agent_file is set, and the doc itself (line 32) states --agent-file is the ONLY tool-restriction mechanism Kimi exposes — so research/creator runs get the default toolset including Bash/Write. (3) The mutation guard is critic-only and detection-only: is_critic=read_only (line 555), guard fires only `if is_critic and repo_write` (line 608); test_creator_repo_write_flag_but_status_ok (test_kimi_adapter.py:299-306) asserts a creator repo write returns status ok with only a repo_write flag. (4) The guard is also repo-only: only _repo_porcelain_hash(C:/QM/repo) is called (556/606); _dir_listing_hash is defined but never invoked, so the doc's F1-resolution promise (KIMI_INTEGRATION_ARCHITECTURE.md:420, 'hash guard extended to both repo and D:/QM state paths') is unimplemented and D:/QM + T_Live are never checked. (5) The orchestration research lane runs WITH shell BY DESIGN: build_prompt's kimi branch instructs `python agent_router.py update-task … --state REVIEW --verdict` (run_agent_orchestration_task.py:261) and `python farmctl.py health` (line 268), both of which require a shell; --add-dir scopes file tools, not shell reach, so C:/QM/mt5/T_Live, farmctl mutations and git commit are reachable regardless of add_dirs. (6) agent_chain.py:772 passes add_dirs=[REPO_ROOT, out_dir] (live repo) to a non-read-only kimi creator, and resolve_creator (400-406)+vendor_gate (286-306) open kimi in NORMAL. Against this, the doc asserts code-level, prompt-independent enforcement: line 120 'even a mutating shell cannot self-approve or advance state', line 152 'no farmctl subcommand is callable by it … so it cannot invoke close-review … independent of any prompt instruction', line 153 'No farmctl subcommand is callable by a Kimi task.' Those are false for the research/creator roles; the only real code-level guard is router capability omission (agent_router.py ~1678-1690, TASK_TYPE_CAPABILITIES), which the finding correctly notes only prevents the router from ASSIGNING build/ops/verdict tasks — it does not confine the spawned process. Severity major (safety) is appropriate: a prompt-injected or misbehaving research/creator run has shell and can run farmctl approve-card, agent_router close-review --state APPROVED, write T_Live, or git commit, with no mechanical stop.",
  "evidence": "tools/strategy_farm/config/kimi_adapter.v1.json:55-58 (research/creator use_agent_file=false,read_only=false; critic true/true) | tools/strategy_farm/kimi_adapter.py:397-399 (--agent-file only if use_agent_file), :555 (is_critic=read_only), :606-611 (repo_write guard gated on is_critic), :232-260 (only _repo_porcelain_hash of C:/QM/repo used; _dir_listing_hash defined but never called → no D:/QM guard) | tools/strategy_farm/tests/test_kimi_adapter.py:299-306 (test_creator_repo_write_flag_but_status_ok: creator repo write → status ok, repo_write True) | tools/strategy_farm/run_agent_orchestration_task.py:246-283 build_prompt kimi branch, esp. :261 (python agent_router.py update-task … --verdict) and :268 (python farmctl.py health) — both require shell; :936 role hardcoded 'research'; :940 add_dirs=[cwd,prompt_parent] but shell reach is unbounded | tools/strategy_farm/agent_chain.py:770-774 _run_kimi add_dirs=[REPO_ROOT,out_dir] to non-read-only creator; :400-406 resolve_creator; :286-306 vendor_gate kimi open in NORMAL | docs/ops/KIMI_INTEGRATION_ARCHITECTURE.md:32 (no tool-restriction flag but --agent-file), :120, :145-161 (§4.2 code-level guards claim 'independent of any prompt instruction', 'even a mutating shell cannot self-approve', 'no farmctl subcommand callable by a Kimi task'), :420 (F1 resolution promising D:/QM guard extension — unimplemented)"
 },
 {
  "refuted": false,
  "reason": "Finding is confirmed, not refutable. Architecture F9 (KIMI_INTEGRATION_ARCHITECTURE.md:89) mandates the parser classify valid-JSON-wrong-schema DISTINCTLY from non-JSON garbage, and the directive verbatim (owner_directive_verbatim.md:93) lists 'malformed output' and 'schema failure' as two separate required test cases. The committed code fails both requirements: (1) kimi_adapter.py has only STATUS_MALFORMED (line 59); no schema_mismatch class exists (grep returns nothing). (2) parse_stream_json returns None for BOTH cases (line 131 for garbage via not-saw_any_json; line 132 returning answer=None for valid-JSON-wrong-schema) — the internal saw_any_json distinction is never propagated. (3) The caller hardcodes rc=0-unparseable to status=malformed_output/error=empty_or_unparseable_output (line 590-591) before classify_error is ever reachable, so a realistic rc=0 auto-update schema mismatch gets no distinct class. (4) No test feeds valid JSONL with renamed events: test_parse_stream_json_ignores_meta_and_tool_and_garbage (test:85) feeds only non-JSON/empty; test_malformed_output_when_rc0_but_unparseable (test:278) feeds empty string. The gap is reproducible and unhandled, so refuted=false. Note: 'fail closed' alone IS satisfied (both paths yield non-OK, text discarded) — only the 'distinct classification' half fails, which is precisely what the finding targets.",
  "evidence": "kimi_adapter.py:59 (single STATUS_MALFORMED, no schema_mismatch constant — grep confirms absent); kimi_adapter.py:130-132 (parse_stream_json returns None for both garbage and valid-JSON-wrong-schema; saw_any_json distinction not propagated); kimi_adapter.py:588-594 (rc=0 unparseable hardcoded to STATUS_MALFORMED at line 591; classify_error only in rc!=0 else-branch and has no schema pattern); test_kimi_adapter.py:85-89 (garbage/empty inputs only); test_kimi_adapter.py:278-283 (empty-string input only); no test named test_malformed_stream_json_class or test_stream_json_schema_mismatch_class exists (grep returns nothing). Requirement: KIMI_INTEGRATION_ARCHITECTURE.md:89 ('must fail closed ... classified distinctly from non-JSON garbage'), owner_directive_verbatim.md:93 ('malformed output, schema failure' as two cases). STRENGTHENED: KIMI_INTEGRATION_ARCHITECTURE.md:346-347 and F9 table line 428 falsely CLAIM the split and a schema_mismatch class were 'applied' — none of the two named tests nor the error class exist in the committed code, a doc-vs-implementation divergence on top of the test gap."
 },
 {
  "refuted": false,
  "reason": "The finding is accurate and reproducible. The three components genuinely disagree on the KIMI_LOW_QUOTA.flag body format, and the disagreement is not merely speculative — the router's own design-intent comment proves the divergence.\n\n1. Governor WRITER — kimi_governor._write_flag (tools/strategy_farm/kimi_governor.py:276-285) writes a key=value body whose lines are MANAGED_BY=, set_at=, reason=, counts=. Not JSON, and it contains NO `state=` line. reconcile_flag (287-320) only ever writes on EXHAUSTED and CLEARs (unlinks) on NORMAL/CONSERVE, so a CONSERVE flag is never emitted.\n\n2. Governor OWNERSHIP-READER — _flag_owned (268-273) reads `.splitlines()[0].strip()` and requires it to equal `MANAGED_BY=kimi_governor`. A JSON body's first line ('{' or '{\"managed_by\": ...') can never satisfy this, so the governor could never clear a JSON flag it wrote.\n\n3. Router READER — kimi_quota_state (715-743) does json.loads(raw) then data.get('state'); its docstring states any non-JSON body fails closed to EXHAUSTED. It only honors an explicit JSON {\"state\":\"CONSERVE\"}.\n\n4. Chain READER — _read_kimi_flag_state (agent_chain.py:174-201) accepts JSON {state}, a key=value `state=` line, or a MANAGED_BY-only body mapped to EXHAUSTED. The governor writes none of a `state=` line, so the chain also cannot read CONSERVE from a governor flag.\n\nDecisive evidence of divergent intent: agent_router.py:82-84 states the flag \"body may carry a JSON object with a 'state' field so kimi_governor.py can express CONSERVE ... or EXHAUSTED ... without a code change.\" The router author expected the governor to write JSON; the governor (committed separately in C1) writes key=value and cannot switch to JSON without breaking its own first-line MANAGED_BY ownership check (_flag_owned). These two requirements are mutually exclusive: no single body satisfies both the router's JSON-only reader and the governor's first-line-MANAGED_BY ownership model. Consequently CONSERVE cannot be carried end-to-end — in governor CONSERVE the flag is cleared, the router reads NORMAL, and the router's CONSERVE narrowing branch (agent_router.py:1239-1240, 1994) never fires from the governor, silently breaking the documented §7.2 CONSERVE contract (KIMI_INTEGRATION_ARCHITECTURE.md:195,305).\n\nThe only minor imprecision in the finding is calling EXHAUSTED-today a pure \"accident\" — the three readers do document fail-closed-to-EXHAUSTED behavior, so EXHAUSTED happens to work. But the core, load-bearing claim (the flag contract cannot carry CONSERVE as coded, due to the JSON-vs-key=value/MANAGED_BY format conflict) is fully correct. The proposed fix (one canonical JSON body {\"managed_by\",\"state\"}; update _write_flag/_flag_owned to read ownership from JSON; align router + chain; add an end-to-end CONSERVE narrowing test) is the right remedy.",
  "evidence": "tools/strategy_farm/kimi_governor.py:276-285 (_write_flag key=value body, no state=, not JSON); :268-273 (_flag_owned requires first line MANAGED_BY=kimi_governor); :287-320 (reconcile_flag writes only on EXHAUSTED, clears on CONSERVE/NORMAL); module docstring :27 ('written on EXHAUSTED'). tools/strategy_farm/agent_router.py:715-743 (kimi_quota_state JSON-only, non-JSON->EXHAUSTED); :82-84 (comment: governor SHOULD write JSON state to express CONSERVE — proof of divergent intent); :1239-1240 and :1994 (CONSERVE narrowing branches that can never trigger from a governor flag). tools/strategy_farm/agent_chain.py:174-201 (_read_kimi_flag_state accepts JSON/state=/MANAGED_BY-only; governor writes none of a state= line). docs/ops/KIMI_INTEGRATION_ARCHITECTURE.md:195,305 (CONSERVE-narrows-lane contract that is left unenforced end-to-end)."
 },
 {
  "refuted": false,
  "reason": "The finding reproduces exactly against the committed code; I could not refute it. The end-to-end CONSERVE path is broken because the only flag writer never emits a CONSERVE-carrying flag, and the one artifact that does carry CONSERVE (kimi_governor_state.json) is read by neither plane.\n\nCHAIN OF FACTS VERIFIED:\n\n1. Governor writes the flag ONLY on EXHAUSTED. kimi_governor.reconcile_flag (kimi_governor.py:288-321): the `if state == \"EXHAUSTED\"` branch (299-303) is the sole caller of _write_flag; the `else` branch covering NORMAL/CONSERVE (308-319) only CLEARs an owned flag or noops — it never writes. The comment at 308 is explicit: \"NORMAL / CONSERVE -> the flag (a hard lane-disable) should not be present.\"\n\n2. compute_state DOES return CONSERVE (kimi_governor.py:216-225) but that state is persisted only to kimi_governor_state.json in evaluate() (kimi_governor.py:340).\n\n3. Nothing reads kimi_governor_state.json. grep for kimi_governor_state / governor_state across agent_router.py and agent_chain.py returns zero hits (the state_path refs in agent_router.py:1863/1898/2036/2199 belong to the Claude/Codex quota_governor, not kimi). Both planes derive kimi state SOLELY from the flag: agent_router.kimi_quota_state (agent_router.py:715-743) returns NORMAL on absent flag (line 728); agent_chain.vendor_gate's kimi branch (agent_chain.py:294-305) evaluates state only when `flag.exists()`, otherwise None (gate open).\n\n4. The scheduled task (install_kimi_governor_scheduled_task.ps1:25) runs `kimi_governor.py evaluate`, which routes through reconcile_flag — so in production the flag is absent whenever state==CONSERVE. Router therefore sees NORMAL; chain sees None/open. The route-time CONSERVE gate (agent_router.py:1994, KIMI_CONSERVE_TASK_TYPES), the sync_default_registry capability narrowing (agent_router.py:1239-1240, KIMI_CONSERVE_CAPABILITIES), and the chain CONSERVE critic-only path (agent_chain.py:298-303) are all unreachable in production.\n\n5. The tests exercise this machinery only by hand-writing a JSON flag body the governor never produces: test_agent_router_kimi_lane.py:111 writes `json.dumps({\"state\": \"CONSERVE\"})`; test_agent_chain_kimi.py:147 writes the same. Meanwhile test_kimi_governor.py:180 (test_conserve_does_not_write_hard_flag) asserts the governor writes NO flag on CONSERVE (`assert not Path(gov[\"flag_path\"]).exists()`). No test wires governor output into either consumer, so the gap passes CI green.\n\n6. Compounding format mismatch (finding #2 territory, confirms non-accidental reachability): even if reconcile_flag were made to write on CONSERVE using the current _write_flag body (kimi_governor.py:276-285), that body is `MANAGED_BY=...` plaintext with no `state=` line and no JSON. agent_router.kimi_quota_state json.loads that and fails closed to EXHAUSTED (agent_router.py:734-736, 743); agent_chain._read_kimi_flag_state finds no state= line and fails closed to EXHAUSTED (agent_chain.py:196-200). So CONSERVE cannot even be transmitted through the current flag format — the fix must add a state field AND a CONSERVE write branch, as the finding's proposed fix notes.\n\nCORRECT AND MUST BE KEPT: The fail-closed posture is sound — absent-owned-flag semantics, MANAGED_BY ownership guard (kimi_governor.py:268-273, 304-307, 317) preventing overwrite of a foreign flag, and both consumers treating an ambiguous/unparseable present flag as EXHAUSTED. The three-body tolerance in agent_chain._read_kimi_flag_state (agent_chain.py:174-200) is forward-compatible and already handles a JSON state field, so the router-side consumer contract (JSON {\"state\": ...}) is the natural target the governor should be made to emit.",
  "evidence": "kimi_governor.py:299-303 (flag written only on EXHAUSTED); kimi_governor.py:308-319 (NORMAL/CONSERVE never writes, only clears/noops); kimi_governor.py:216-225 (compute_state returns CONSERVE); kimi_governor.py:340 (CONSERVE persisted only to state.json); kimi_governor.py:276-285 (_write_flag body has no state= field, non-JSON); agent_router.py:728 (absent flag -> NORMAL); agent_router.py:734-743 (non-JSON/unknown -> EXHAUSTED fail-closed); agent_router.py:1239-1240,1994 (CONSERVE narrowing + task-type gate, both unreachable); agent_chain.py:294-305 (kimi branch reads state only when flag exists); agent_chain.py:196-200 (no state= line -> EXHAUSTED); test_kimi_governor.py:180-185 (locks in no-flag-on-CONSERVE); test_agent_router_kimi_lane.py:111 and test_agent_chain_kimi.py:147 (tests hand-write a JSON CONSERVE flag the governor never emits); install_kimi_governor_scheduled_task.ps1:25 (task runs `evaluate`); grep for kimi_governor_state across both planes = 0 hits."
 },
 {
  "refuted": false,
  "reason": "CONFIRMED. The finding reproduces exactly against the committed code, and the code's own comments admit the gap.\n\n1. kimi_governor.reconcile_flag (kimi_governor.py:298-319) writes KIMI_LOW_QUOTA.flag ONLY when state=='EXHAUSTED' (line 299-303 via _write_flag). For BOTH 'NORMAL' and 'CONSERVE' it takes the else branch (line 308, comment \"NORMAL / CONSERVE -> the flag ... should not be present\") and CLEARs/leaves-absent the flag (lines 309-319). No governor code path ever writes a flag body carrying state=CONSERVE. Confirmed by the module docstring itself: kimi_governor.py:27 \"KIMI_LOW_QUOTA.flag (written on EXHAUSTED)\".\n\n2. The two consumers derive runtime state ONLY from that flag. agent_router.kimi_quota_state (agent_router.py:725-728) returns KIMI_QUOTA_NORMAL when the flag is absent. agent_chain._read_kimi_flag_state (agent_chain.py:182-185) returns None (absent) => kimi open/NORMAL. Neither consumer reads kimi_governor_state.json (the only place the governor persists the computed CONSERVE state, kimi_governor.py:340) — that json write is not consumed by any router/chain path.\n\n3. Therefore a real governor.evaluate() computing CONSERVE clears the flag, and both planes read NORMAL. The CONSERVE machinery is dead in production: the router CONSERVE capability narrowing (agent_router.py:1239-1240) and the CONSERVE task-type gate (agent_router.py:1994, `kimi_state == KIMI_QUOTA_CONSERVE and ... not in KIMI_CONSERVE_TASK_TYPES`) plus the chain CONSERVE critic-only narrowing (agent_chain.py:298-299) can only fire from a hand-written/test flag containing {\"state\":\"CONSERVE\"}. The documented tri-state collapses to bi-state (present-non-JSON=EXHAUSTED / absent=NORMAL) at the flag boundary.\n\nThe code authors were aware of the gap but only made consumers forward-compatible, not the producer. agent_chain.py:177-180 docstring: \"Tolerant of three flag bodies so it works with the current governor and a future one ... the governor's MANAGED_BY-only body, which the governor today writes ONLY on EXHAUSTED.\" So the consumers can parse a future CONSERVE flag, but the CURRENT governor never produces one — the exact defect the finding describes. The architecture doc likewise only specifies the flag on EXHAUSTED (KIMI_INTEGRATION_ARCHITECTURE.md:246, :383), meaning docs and producer agree on bi-state while the two consumers built (and tested only via synthetic flags) a tri-state that production can never reach.\n\nNote on EXHAUSTED (not a counter-argument): _write_flag writes a MANAGED_BY=... key=value body (no state= line), which is not JSON, so kimi_quota_state's json.loads fails and it fail-closes to EXHAUSTED — EXHAUSTED works coincidentally via fail-closed parsing. This does not save CONSERVE, which requires the flag to be present AND carry state=CONSERVE, a combination the governor never emits.\n\nThe proposed fix (have reconcile_flag write {\"state\":\"CONSERVE\"} JSON on CONSERVE and only clear on NORMAL, plus a test driving CONSERVE from an actual governor.evaluate() output) correctly addresses the root cause. Refuted=false: exact producer line (kimi_governor.py:308) makes the finding true.",
  "evidence": "kimi_governor.py:298-321 (reconcile_flag: SET only on EXHAUSTED, CLEAR/noop on NORMAL/CONSERVE); kimi_governor.py:276-285 (_write_flag emits MANAGED_BY=... key=value, no state= field); kimi_governor.py:27 (\"written on EXHAUSTED\"); kimi_governor.py:340 (governor state json write, not consumed by planes); agent_router.py:725-728 (absent flag => NORMAL); agent_router.py:1239-1240 and 1994 (CONSERVE narrowing/gate, only reachable if kimi_state==CONSERVE); agent_chain.py:174-200 (_read_kimi_flag_state; docstring lines 177-180 admit governor writes flag ONLY on EXHAUSTED); agent_chain.py:298-299 (CONSERVE critic-only branch); docs/ops/KIMI_INTEGRATION_ARCHITECTURE.md:246,383 (flag specified as written on EXHAUSTED only)."
 },
 {
  "refuted": false,
  "reason": "Finding is CONFIRMED, not refutable. The real kimi_governor never surfaces CONSERVE to the router or chain, so the CONSERVE-narrowing logic is dead code via the actual governor, and no test bridges the two. Verified end-to-end: (1) reconcile_flag (kimi_governor.py:288-321) writes KIMI_LOW_QUOTA.flag ONLY in the state==\"EXHAUSTED\" branch; the else branch covering NORMAL and CONSERVE (line 308) only CLEARs or noops. test_conserve_does_not_write_hard_flag (test_kimi_governor.py:180-184) asserts the flag file does not exist after a CONSERVE reconcile. (2) The router's only channel for kimi state is kimi_quota_state (agent_router.py:715-743), which returns CONSERVE only when json.loads succeeds with {\"state\":\"CONSERVE\"}; there is no read of kimi_governor_state.json anywhere in the router or chain (grep confirmed). With no flag on disk in CONSERVE, kimi_quota_state reads an absent flag and returns NORMAL (line 727-728). (3) Consequently sync_default_registry capability narrowing (1239-1240), the route_once task-type gate (1994), and the chain CONSERVE critic-only branch (agent_chain.py:298-305) are all unreachable through the real governor. (4) The defect is consequential, not belt-and-suspenders: the kimi lane advertises research+strategy+summary in NORMAL (agent_router.py:628-641), so generic research_strategy (caps [\"research\",\"strategy\"], line 115) is eligible for kimi; with gemini auth-degraded (documented current state, architecture line 192/423) kimi at cost_rank 12 becomes the cheapest eligible lane and captures the generic research funnel exactly as F4/§5.2 warn — CONSERVE is the designed guard against this (§7.2, lines 245/305) and it never engages, so the fixed one-month subscription drains to EXHAUSTED with no 70% conservation. (5) The governor's EXHAUSTED body is non-JSON key=value (_write_flag 276-285); the router honours it only via its fail-closed json.loads-except branch (735-737), so EXHAUSTED works by fail-closed accident rather than a shared flag contract. (6) No test runs kimi_governor.evaluate()/reconcile_flag() at a CONSERVE threshold and feeds the on-disk artifact into kimi_quota_state()/_read_kimi_flag_state(): only test_agent_chain_kimi.py touches both, and it hand-authors the flag ({\"state\":\"CONSERVE\"} at :147; MANAGED_BY-only body at :141 asserted EXHAUSTED). test_kimi_governor.py imports neither the router nor the chain. Every claim in the finding maps to an exact line; the proposed integration test would fail for CONSERVE.",
  "evidence": "kimi_governor.py:288-321 (reconcile_flag writes only on EXHAUSTED); kimi_governor.py:276-285 (_write_flag key=value body, no state= line); tests/test_kimi_governor.py:180-184 (test_conserve_does_not_write_hard_flag asserts no flag file); agent_router.py:715-743 (kimi_quota_state: CONSERVE only from JSON {\"state\":\"CONSERVE\"}, else fail-closed EXHAUSTED; absent flag -> NORMAL at 727-728); agent_router.py:1239-1240 and 1994 (CONSERVE narrowing gated on kimi_state==CONSERVE); agent_router.py:628-641 (kimi NORMAL caps include research/strategy/summary) vs :115 (research_strategy needs [research,strategy]); agent_chain.py:174-200 and 298-305 (chain CONSERVE branch also flag-gated); tests/test_agent_chain_kimi.py:141,147 (hand-authored flags, governor never run to produce CONSERVE); grep: no kimi_governor_state.json read in router/chain; no test imports both real governor evaluate and either reader."
 },
 {
  "reason": "Cannot refute. The finding's core factual claim is directly reproducible on the committed branch under review (agents/board-advisor):\n\n1. The named test does NOT exist. `git show agents/board-advisor:tools/strategy_farm/tests/test_kimi_adapter.py | grep -c test_max_parallel_1_enforced` returns 0. The full test function list (grep 'def test_') contains no such name; the only single-flight coverage is test_single_flight_busy_returns_error (test_kimi_adapter.py:200) and test_single_flight_steals_stale_lock (:209).\n\n2. The binding architecture doc references it as existing coverage in three places (KIMI_INTEGRATION_ARCHITECTURE.md:130, :368, :421), including a test-coverage mapping table (:368 'test_kimi_adapter.py::test_max_parallel_1_enforced ... asserts the cross-process adapter lock') and the F2/R-E risk-resolution table (:421 'applied (R-E) ... test_max_parallel_1_enforced asserts the adapter lock'). The DRAFT (design/KIMI_INTEGRATION_ARCHITECTURE.DRAFT.md:314) names it too. So a binding doc cites a nonexistent test as the evidence that a 'major' risk (F2, the OAuth-refresh serialization race) is resolved — a doc-vs-reality defect, squarely against CLAUDE.md's 'Evidence over claims' hard rule.\n\n3. The concurrency-coverage half also holds: both existing tests exercise lock-state DETECTION in a single thread (one writes a fresh lock body then asserts _spawn_once is never called; the other writes a stale-timestamp lock and asserts a steal). Neither drives two concurrent run_kimi invocations racing for the lock. No threading/multiprocessing/concurrent race test for run_kimi exists anywhere in the test suite or the probe-battery evidence.\n\nI considered the strongest refutation: the actual guarantee is os.O_CREAT|os.O_EXCL (kimi_adapter.py:201), an atomic OS-level primitive that is the canonical correct cross-process mutex, and a Python threaded test adds marginal value over it. That argument bears on SEVERITY/value of the proposed threaded test, and it is fair that the lock implementation itself is correct (the finding does not claim the lock is broken). But it does not touch the decisive, irrefutable prong: the doc names a test that does not exist and presents it as the coverage proof for a resolved major risk. The proposed fix already offers the minimal remedy for exactly this (rename the test or correct the doc so they agree). Not speculative, not handled elsewhere, fully reproducible — refuted=false.",
  "evidence": "git show agents/board-advisor:tools/strategy_farm/tests/test_kimi_adapter.py | grep -c test_max_parallel_1_enforced => 0. Test function inventory (test_kimi_adapter.py) has no test_max_parallel_1_enforced; only test_single_flight_busy_returns_error (:200) and test_single_flight_steals_stale_lock (:209), both single-threaded lock-state detection. Doc references present at KIMI_INTEGRATION_ARCHITECTURE.md:130, :368 (coverage table), :421 (F2/R-E resolution table), and DRAFT:314. Lock impl kimi_adapter.py:194-220 uses os.O_CREAT|os.O_EXCL (:201) — correct primitive, but no concurrent-race test drives it. No threading/multiprocessing/concurrent test for run_kimi anywhere under tools/strategy_farm/tests/ or the 2026-09-15_kimi_integration probe battery.",
  "refuted": false
 },
 {
  "refuted": false,
  "reason": "Confirmed, not refuted. In kimi_governor.py, reconcile_flag (lines 288-321) writes the KIMI_LOW_QUOTA.flag ONLY on state=='EXHAUSTED' (line 299); the else branch (line 308) covers both NORMAL and CONSERVE and clears/leaves-absent the flag (lines 309-319). compute_state genuinely produces state=='CONSERVE' in production (lines 216-225: day/week pct >= conserve_pct, or subscription period near end), so a real computed CONSERVE state is discarded at the flag boundary. Both consumers read the flag as their sole input and treat absence as NORMAL: agent_router.kimi_quota_state returns KIMI_QUOTA_NORMAL for an absent flag (lines 727-728) and only honours an explicit JSON {\"state\":\"CONSERVE\"}; agent_chain._read_kimi_flag_state returns None for an absent flag, leaving kimi open (line 288 -> vendor_gate CONSERVE branch at line 298 never fires). Therefore the router CONSERVE capability-narrowing/task-type gate and the chain CONSERVE critic-only narrowing are unreachable from real governor output. The tests bear this out: every CONSERVE test uses a hand-written synthetic flag (test_agent_router_kimi_lane.py:109-111,120,137 _write_flag -> json.dumps({\"state\": state}); test_agent_chain_kimi.py:147), and none drives CONSERVE from governor.evaluate()/reconcile_flag. The defect is in fact slightly worse than stated: _write_flag (lines 276-285) writes a key=value body with no 'state' key at all, so the governor never emits a state=CONSERVE flag body under any code path. No alternate production writer of a CONSERVE flag exists (grep of tools/strategy_farm/*.py shows kimi_governor is the only writer of KIMI_LOW_QUOTA.flag) and no alternate input path exists (neither consumer reads the governor's state_path JSON). The proposed fix (write {\"state\":\"CONSERVE\"} on CONSERVE, clear only on NORMAL, add a governor-driven CONSERVE test) is correct and addresses the root cause.",
  "evidence": "tools/strategy_farm/kimi_governor.py:288-321 (reconcile_flag writes only on EXHAUSTED, else clears); tools/strategy_farm/kimi_governor.py:276-285 (_write_flag body has no state key); tools/strategy_farm/kimi_governor.py:216-225 (compute_state yields CONSERVE); tools/strategy_farm/agent_router.py:725-743 (kimi_quota_state: absent->NORMAL, JSON-only, else EXHAUSTED); tools/strategy_farm/agent_chain.py:174-200,288-299 (_read_kimi_flag_state absent->None->kimi open); tests/test_agent_router_kimi_lane.py:109-111,120,137 and tests/test_agent_chain_kimi.py:147 (CONSERVE only ever exercised via synthetic hand-written flags)."
 },
 {
  "refuted": true,
  "reason": "Refuted by branch state. The finding's core claim (\"no committed installer creates QM_StrategyFarm_KimiGovernor_15min\") and its proposed fix are already implemented on agents/board-advisor at HEAD commit 835089d2fd, which adds tools/strategy_farm/install_kimi_governor_scheduled_task.ps1. That installer sets $taskName=\"QM_StrategyFarm_KimiGovernor_15min\" and Register-ScheduledTask registers it as SYSTEM/Highest running `pythonw kimi_governor.py evaluate` on a 15-min RepetitionInterval — i.e. exactly the proposed remedy (mirror the Agy/Quota governor, evaluate every 15 min as SYSTEM). It also provides -Uninstall (Unregister-ScheduledTask), so the §15 rollback lever now targets a disable-able/delete-able task, and the header documents \"Rollback: -Uninstall\". The time-based-enforcement consequence is likewise refuted: kimi_governor.py exposes an `evaluate` subcommand (main argparse choices status|evaluate, line 362) and compute_state sets state=\"EXHAUSTED\" on period_ended (lines 192, 212-213) purely from clock+ledger with no call required, so the scheduled evaluate enforces the subscription-period-end transition independent of any real Kimi call. Residuals do not rescue the finding: the task is deliberately not yet live-registered (commit msg \"not yet installed\"; smoke_receipt.md \"installed after the review\"), and the arch doc still imprecisely calls the governor a \"spawner\" — a minor doc-wording nit, not the major ops gap asserted. The finding was written against C1–C6 (ends 7725302c24) and is stale relative to the next committed commit.",
  "evidence": "tools/strategy_farm/install_kimi_governor_scheduled_task.ps1:15 ($taskName = \"QM_StrategyFarm_KimiGovernor_15min\"); same file Register-ScheduledTask line (Action: pythonw -X utf8 kimi_governor.py evaluate; Trigger -Once -RepetitionInterval 15 min -RepetitionDuration 3650d; Principal SYSTEM RunLevel Highest) and -Uninstall/Unregister-ScheduledTask branch (lines 17-22). Commit 835089d2fd --stat: adds install_kimi_governor_scheduled_task.ps1 (+30) and docs/ops/evidence/2026-09-15_kimi_integration/smoke_receipt.md (+21). smoke_receipt.md Scheduled-tasks row: \"KimiGovernor_15min installer written, installed after the review\". tools/strategy_farm/kimi_governor.py:362 (argparse choices status|evaluate), :192 period_ended, :212-213 (if period_ended: state=\"EXHAUSTED\"), :299 flag written on EXHAUSTED, :326/:360 evaluate()/main()."
 },
 {
  "refuted": true,
  "reason": "The finding's central, severity-carrying claim — \"the task does not exist and no committed installer creates it\" — is factually false against the committed state of branch agents/board-advisor. A committed, git-tracked installer creates exactly this task and does exactly what the finding's own proposed fix asks for.\n\nCommit 835089d2fd (HEAD; the immediate next commit after C6/7725302c24, which is a verified ancestor) adds tools/strategy_farm/install_kimi_governor_scheduled_task.ps1. That script registers taskName \"QM_StrategyFarm_KimiGovernor_15min\" as a SYSTEM ServiceAccount task (RunLevel Highest), with a 15-minute RepetitionInterval, executing `pythonw -X utf8 \"kimi_governor.py\" evaluate` — mirroring QM_StrategyFarm_AgyGovernor as the architecture (KIMI_INTEGRATION_ARCHITECTURE.md:264) describes, and providing the -Uninstall lever the §15 rollback line refers to. The proposed fix (\"mirror QM_StrategyFarm_AgyGovernor: python kimi_governor.py evaluate every 15min as SYSTEM, and register it\") is therefore already implemented and committed.\n\nThe governor also actually supports the invoked CLI: kimi_governor.py:362 defines the `evaluate` subcommand, so the installer's action is valid (not a broken reference).\n\nThe finding was accurate only as a snapshot of C6 in isolation, and the refuter default applies: it is \"already handled elsewhere in the code\" (the very next commit on the same branch under review). The remaining residue is a documentation wording nit — KIMI_INTEGRATION_ARCHITECTURE.md:386 loosely calls both tasks \"scheduled spawners\" when the governor spawns nothing — which is minor, not the claimed major ops gap, and does not sustain the finding's severity. Live Get-ScheduledTask state (whether the committed installer has been run) is a runtime/ops condition outside the remit of a committed-code review; the smoke receipt (835089d2fd) documents it as \"installed after the review\". The period-end EXHAUSTED enforcement concern (compute_state period_ended at kimi_governor.py:192/212) is real in principle but is precisely what the now-committed 15-min scheduled evaluate() enforces.",
  "evidence": "C:/QM/repo/tools/strategy_farm/install_kimi_governor_scheduled_task.ps1 (committed at HEAD 835089d2fd): taskName=\"QM_StrategyFarm_KimiGovernor_15min\"; Action = pythonw -X utf8 \"...kimi_governor.py\" evaluate; RepetitionInterval 15 min; Principal UserId \"SYSTEM\" LogonType ServiceAccount RunLevel Highest; comment \"mirrors QM_StrategyFarm_AgyGovernor\". git ls-files confirms it is tracked. | kimi_governor.py:362 `ap.add_argument(\"command\", choices=[\"status\",\"evaluate\"]...)` and :367-370 evaluate() — CLI target exists. | git merge-base --is-ancestor 7725302c24 HEAD => true (installer commit is C6+1 on the reviewed branch). | Only surviving defect: KIMI_INTEGRATION_ARCHITECTURE.md:386 \"stops both scheduled spawners\" mislabels the governor a spawner — minor wording, not major."
 },
 {
  "refuted": false,
  "reason": "The finding is factually confirmed on both sub-claims and cannot be refuted. (1) The architecture doc names a test that does not exist: KIMI_INTEGRATION_ARCHITECTURE.md:368 (\"concurrency — adapter single-flight lock | test_kimi_adapter.py::test_max_parallel_1_enforced\") and :421 (F2/R-E remediation: \"test_max_parallel_1_enforced asserts the adapter lock (§3.6, §14)\"). A Grep for test_max_parallel_1_enforced matches ONLY documentation files (KIMI_INTEGRATION_ARCHITECTURE.md and three evidence/design subfiles) — no test function by that name exists in any *.py test file. (2) The actual single-flight coverage in test_kimi_adapter.py is single-threaded lock-STATE detection, not a concurrency race: test_single_flight_busy_returns_error (:200) pre-writes a fresh lock body then asserts run_kimi returns single_flight_busy without spawning, and test_single_flight_steals_stale_lock (:209) pre-writes a stale-timestamp lock then asserts a spawn proceeds. Neither drives two concurrent run_kimi invocations. I checked for concurrency coverage elsewhere: test_kimi_adapter.py does not appear in the grep for threading/Thread/concurrent/multiprocessing across the tests dir, and test_agent_orchestration_lock.py exercises the orchestration-level agent LEASE (test_two_headless_sessions_cannot_hold_the_same_agent_lease), not the adapter's cross-process single-flight mutex. So the load-bearing OAuth-refresh serialization guarantee (F2/R-E) has no test driving an actual race, and the doc's named test reference is a real doc/test mismatch. This is not speculative, not handled elsewhere, and is trivially reproducible by grep. The doc's own F2 row rates this a \"major\" remediation, so the mismatch is material. The proposed fix is correct — either add the real concurrency test under that name, or correct the doc's two named references (:368, :421) to point at the tests that actually exist.",
  "evidence": "docs/ops/KIMI_INTEGRATION_ARCHITECTURE.md:368 and :421 reference test_kimi_adapter.py::test_max_parallel_1_enforced; Grep for that name matches only docs (KIMI_INTEGRATION_ARCHITECTURE.md + 3 evidence/design files), zero test functions. Actual tests: tools/strategy_farm/tests/test_kimi_adapter.py:200 test_single_flight_busy_returns_error (single-thread, pre-written fresh lock), :209 test_single_flight_steals_stale_lock (single-thread, pre-written stale lock). test_kimi_adapter.py absent from threading grep; test_agent_orchestration_lock.py covers agent-lease serialization, not the adapter single-flight mutex."
 },
 {
  "refuted": true,
  "reason": "The finding mischaracterizes a deliberate, documented, probe-gated design as an accidental inconsistency, and its two concrete harm claims (\"no probe gate, no flag\") are both false.\n\n1. NOT a contradiction of §13 — it IS §13. KIMI_INTEGRATION_ARCHITECTURE.md:266 explicitly states: \"Existing critique sweep QM_StrategyFarm_AgentChain_Critique_15min (agent_chain.py critique-pending --apply --max 2) picks up kimi vendors automatically once §6 lands ... Because that sweep is a second, registry-blind Plane-B spawner, the adapter single-flight lock (§3.6) is what prevents it racing.\" Line 324/420 gates BOTH the orchestration task AND \"the kimi critique path\" on probes 4,5,7. The two mechanisms differ by necessity: the orchestration lane is a NEW scheduled task, so it is gated by the -IncludeKimi installer switch (install_agent_orchestration_scheduled_tasks.ps1:53-58, default OFF); the critique path runs inside a PRE-EXISTING task, so it cannot be installer-switched and is instead gated by sequencing — the §6 config (C3) must not land until probes pass. The design is internally consistent, not contradictory.\n\n2. \"No probe gate\" is false — the gate was honored by commit sequencing. The read-only-critic probes passed in C1 (commit 1333826b9b) per the addendum (KIMI_INTEGRATION_ARCHITECTURE.md:432-438) and probe README (docs/ops/evidence/2026-09-15_kimi_integration/probe_battery/README.md:30: agent-file tools allowlist denies writes, no file written — \"prevention, not just detection\"). The config that activates kimi in the sweep landed LATER, in C3 (2a46f483b9). So \"activated only after probes pass\" was satisfied in order.\n\n3. \"No flag\" is false — the kill switch is enforced. QM_KIMI=0 is checked at the single choke point kimi_adapter.run_kimi (kimi_adapter.py:485-491): it returns STATUS_ERROR \"kill_switch\" and NEVER spawns kimi. Every chain kimi call routes through this adapter (agent_chain.py:770 _run_kimi -> kimi_adapter.run_kimi). Additionally vendor_gate (agent_chain.py:286-306) fails closed on KIMI_LOW_QUOTA.flag EXHAUSTED, CONSERVE-narrowing, and missing cli/credential. So flags do gate the chain seat and QM_KIMI=0 stops any kimi execution.\n\n4. The finding's minimum ask (\"at minimum document that C3 activates kimi in the live sweep independently of -IncludeKimi\") is already delivered verbatim by §13:266.\n\nThe one true low-level observation — vendor_gate (agent_chain.py:254) checks QM_AGENT_CHAIN but not QM_KIMI — is real but produces no harm: QM_KIMI=0 still prevents any kimi spawn downstream at the adapter. The difference is cosmetic (the seat fails as a stage error rather than being skipped to the next critic), not a safety gap. This does not rise to a major default-OFF posture defect.",
  "evidence": "tools/strategy_farm/config/agent_chain.v1.json:29-58 (kimi as second critic in by_creator_vendor tables); agent_chain.py:254,286-306 (vendor_gate checks QM_AGENT_CHAIN + KIMI_LOW_QUOTA/cli/credential, not QM_KIMI); agent_chain.py:770 (_run_kimi routes through adapter); kimi_adapter.py:485-491 (QM_KIMI=0 kill switch, never spawns); config/kimi_adapter.v1.json:8 (kill_switch_env=QM_KIMI); install_agent_orchestration_scheduled_tasks.ps1:53-58 (-IncludeKimi gates only the NEW orchestration task); KIMI_INTEGRATION_ARCHITECTURE.md:266 (sweep auto-picks kimi once §6 lands — documented), :324 & :420 (probes 4,5,7 gate the kimi critique path), :432-438 addendum + probe_battery/README.md:30 (read-only-critic probes PASSED in C1, before C3 config landed)."
 },
 {
  "refuted": false,
  "reason": "Finding CONFIRMED by direct reproduction; not refutable. research_source.mint() writes lineage.json via _lineage_skeleton (research_source.py:433-443) as a single-object shape {schema, research_id, version(int), parent_version_id, discovery_sample, validation_sample, preregistration:null, mechanization} with NO 'versions' key. preregister._update_lineage (preregister.py:146-168) loads that same file when it exists (the exists() branch, line 148-149) and then unconditionally executes lineage[\"versions\"].append(...) at preregister.py:156 -> KeyError: 'versions'. Both modules stamp the identical schema id \"qm.research-lineage/v1\" (research_source.py:62, preregister.py:28) on structurally incompatible shapes. I confirmed both facts and the crash by running mint() then write_preregistration() against the minted dir: output was \"has versions key: False\", both schema constants equal, and \"CRASH: KeyError 'versions'\". The contract makes the collision worse, not milder: INTERNAL_RESEARCH_SOURCE_CONTRACT.md 4.6 defines lineage.json AUTHORITATIVELY as the single-object mint shape (version:int, parent_version_id, preregistration:obj|null filled by the \"prereg tool\") with NO 'versions' array at all — so preregister's 'versions'-array shape is non-conforming to the contract as well as incompatible with the mint skeleton. The design (KIMI_EDGE_DISCOVERY_DESIGN.md:252) confirms preregister and research_source co-locate: preregister appends status:preregistered to the same research_source_ledger.jsonl and operates in the same QM-RESEARCH store dir, so the intended OBSERVE->PRE-REGISTER path runs preregister against a mint-created lineage.json and crashes on first real use. The finding's own claim that tests miss it is correct: test_preregistration_immutable_and_versioned (test_research_ledgers.py) sets artifact_dir = tmp_path/\"QM-RESEARCH-2026-0001\" without minting, so lineage.json does not pre-exist, _update_lineage takes the else-branch fabricating {\"versions\":[]}, and the KeyError is never triggered. No caller in committed code currently wires mint->preregister on the same dir (grep of tools/ finds only the def and the test), so it is a latent-but-real crash on the first end-to-end run, matching the finding's \"breaks the first time PRE-REGISTER runs against a real minted artifact\". Severity major is appropriate. Proposed fix (a) distinct schema ids or (b) make _update_lineage upgrade a mint-minted lineage.json (initialise versions=[] when absent AND agree on whether preregistration lives in lineage['versions'] vs the contract's single 'preregistration' field) is sound; the contract-faithful resolution is (b) reconciling to the 4.6 single-object shape, plus a regression test that mints then calls write_preregistration on the same dir.",
  "evidence": "preregister.py:156 (lineage[\"versions\"].append), preregister.py:146-155 (exists-branch loads mint file; else-branch fabricates versions=[]), preregister.py:28 (LINEAGE_SCHEMA=\"qm.research-lineage/v1\"); research_source.py:433-443 (_lineage_skeleton, no 'versions' key), research_source.py:62 (LINEAGE_SCHEMA=\"qm.research-lineage/v1\"); INTERNAL_RESEARCH_SOURCE_CONTRACT.md 4.6 (authoritative single-object lineage shape, preregistration field filled by prereg tool, no versions array); KIMI_EDGE_DISCOVERY_DESIGN.md:252 (preregister shares ledger + store dir); test_research_ledgers.py test_preregistration_immutable_and_versioned (artifact_dir never minted, KeyError not exercised). Live repro: mint()->write_preregistration() on the minted dir printed \"has versions key: False\" and \"CRASH: KeyError 'versions'\"."
 },
 {
  "refuted": false,
  "reason": "CONFIRMED, not refutable. The schema collision and KeyError are real and reproducible against a genuinely minted C4 artifact.\n\n1. C4 minting ALWAYS writes a standalone lineage.json into the QM-RESEARCH store dir: research_source.py:545 `_write_json(directory / LINEAGE_JSON, _lineage_skeleton(...))` where directory = store_root/<research_id> (store_dir, line 194). Its shape (_lineage_skeleton, research_source.py:433-443) is `{schema:'qm.research-lineage/v1', research_id, version(int), parent_version_id, discovery_sample, validation_sample, preregistration:None, mechanization}` — there is NO 'versions' key.\n\n2. C5 preregister._update_lineage (preregister.py:146-156): when lineage.json exists it loads the file raw (line 149) with no migration/guard, then unconditionally executes `lineage[\"versions\"].append(...)` (line 156). Loading a C4-minted lineage.json (no 'versions' key) -> KeyError: 'versions'. The else-branch that fabricates `versions:[]` (lines 150-155) only runs when NO lineage.json pre-exists.\n\n3. Both modules stamp the identical schema id: research_source.py:62 and preregister.py:28 both = 'qm.research-lineage/v1', on structurally incompatible shapes.\n\n4. Co-location is the intended design, not speculation. preregister.py docstring (lines 6-7) states it \"appends a lineage.json version\" into \"the research artifact dir\". The binding contract INTERNAL_RESEARCH_SOURCE_CONTRACT.md §4.6 (lines 279-291) defines the authoritative lineage.json as a SCALAR `version` int plus a `preregistration` obj|null field \"written by prereg tool\" — i.e. exactly the C4 skeleton shape, with NO `versions` array. So C4/research_source implements the contract; C5/preregister invents an incompatible `versions[]` model that contradicts §4.6.\n\nAttempted refutations, all failed:\n- \"Handled elsewhere / has a guard\": no. preregister.py:148-156 has no try/except and no `versions` initialisation when the loaded dict lacks the key.\n- \"Not reproducible\": it reproduces the first time write_preregistration is pointed at any real minted artifact dir (the documented pre-Q00 OBSERVE->PRE-REGISTER path).\n- \"Tests cover it\": they do NOT. test_research_ledgers.py:98 uses a fresh `tmp_path / 'QM-RESEARCH-2026-0001'` with no C4-minted lineage.json, so the first call takes the else-branch. The test then asserts `lineage['versions']` (lines 126-127), which itself proves preregister's model is a `versions[]` array — the incompatibility the finding names, never crossed against a real mint.\n\nSeverity note: major (not blocking) is right — no live orchestration currently wires preregister against a research_source store dir, so nothing crashes today; but the intended flow breaks the first time it runs, and the two `versions[]` vs scalar-`version`+`preregistration` models also conflict with the contract §4.6 and would corrupt the source.md manifest hash if preregister overwrote the C4 lineage.json.\n\nCorrect and must be kept: research_source's _lineage_skeleton matches contract §4.6 (scalar version + preregistration:null + parent_version_id) and is the authoritative shape; preregister's immutability/self-hash logic and same-version overwrite refusal (preregister.py:191-197) are sound. The fix must land on preregister to consume/upgrade the C4-minted lineage.json (write into the `preregistration` field and bump the scalar `version`/parent link per contract §4.6, rather than an incompatible `versions[]` array), plus a regression test that mints via research_source then calls write_preregistration on the same dir.",
  "evidence": "tools/strategy_farm/research_source.py:433-443 (_lineage_skeleton, no 'versions'); :545 (mint always writes lineage.json); :62,:67,:194 (schema id, filename, store_dir). tools/strategy_farm/research/preregister.py:28 (identical schema id); :146-156 (_update_lineage loads raw then lineage['versions'].append -> KeyError). docs/ops/INTERNAL_RESEARCH_SOURCE_CONTRACT.md:279-291 (§4.6 authoritative lineage.json = scalar version + preregistration obj|null, no versions array). tools/strategy_farm/tests/test_research_ledgers.py:98 (fresh tmp_path dir, no pre-minted lineage.json), :126-127 (asserts lineage['versions'])."
 },
 {
  "refuted": true,
  "reason": "The finding's load-bearing claims (\"no probe gate\", \"contradicts §13\", \"no flag\") do not hold. (1) The §13 install gate for the kimi critique path is probes 4,5,7 passing — and docs/ops/evidence/2026-09-15_kimi_integration/probe_battery/README.md shows those probes PASSED (probe 3/4 read-only critic posture, probe 5 credential, probe 0b `--plan`), committed with C1. So the gate is satisfied, not bypassed. (2) The auto-pickup is not a hidden contradiction: KIMI_INTEGRATION_ARCHITECTURE.md:266 explicitly and deliberately documents it — \"Existing critique sweep QM_StrategyFarm_AgentChain_Critique_15min ... picks up kimi vendors automatically once §6 lands\", with the adapter single-flight lock (§3.6) named as the race guard. (3) A kill switch exists: QM_KIMI=0 makes kimi_adapter (the single choke point every chain spawn routes through) return error/kill_switch WITHOUT spawning (kimi_adapter.py:485, kimi_adapter.v1.json:8); QM_AGENT_CHAIN=0 and the KIMI_LOW_QUOTA governor flag also gate it. Additionally, unattended kimi cannot mutate anything: the read-only critic posture is probe-verified (agent-file allowlist denies Write/Edit/Bash) and backed by a before/after repo+D:/QM hash guard with critic_wrote→discard. kimi is also never the first/selected critic in any creator table (always second, behind codex/claude), so it runs only when the primary critic is itself gated. The genuine residual — vendor_gate not checking QM_KIMI directly (it relies on the adapter to error) — is a minor routing-robustness nicety, not the described major default-OFF-posture violation. The claimed severity is not reproducible against the committed evidence.",
  "evidence": "docs/ops/evidence/2026-09-15_kimi_integration/probe_battery/README.md (probes 4/5/0b PASSED); KIMI_INTEGRATION_ARCHITECTURE.md:266 (auto-pickup documented as intended) and :116/:324/:410 (probe gate = probes 4,5,7, now green); tools/strategy_farm/kimi_adapter.py:485 + config/kimi_adapter.v1.json:8 (QM_KIMI=0 no-spawn kill switch); agent_chain.py:286-306 (vendor_gate kimi branch — the only true residual, lacks a QM_KIMI check but adapter enforces it downstream); agent_chain.v1.json:34-59 (kimi always second critic, never first)."
 },
 {
  "refuted": false,
  "reason": "The finding is confirmed, not refuted. The automated CONSERVE state the governor computes never reaches either plane, and the CONSERVE routing/gate machinery is unreachable via the automated governor.\n\nChain of proof:\n1. kimi_governor.reconcile_flag (kimi_governor.py:299-319) writes KIMI_LOW_QUOTA.flag ONLY on EXHAUSTED; on NORMAL and CONSERVE it clears an owned flag or leaves it absent (the else branch at :308-319). This is a DELIBERATE, locked-in behavior: tests/test_kimi_governor.py:180-184 (test_conserve_does_not_write_hard_flag) asserts a CONSERVE state produces NO flag file at all.\n2. The router's kimi_quota_state (agent_router.py:715-743) derives kimi state SOLELY from the flag: absent flag => NORMAL (:727-728). So on governor-computed CONSERVE (flag absent) the router sees NORMAL.\n3. route_once (agent_router.py:1909, 1994) fires its CONSERVE exclusion of generic research_strategy only when kimi_state == KIMI_QUOTA_CONSERVE — never reached from an absent flag.\n4. The chain's vendor_gate (agent_chain.py:286-298) computes state = _read_kimi_flag_state(flag) only if flag.exists() else None; on governor CONSERVE the flag is absent => None, so the CONSERVE critic-only branch (:298-305) is skipped and the kimi seat is open.\n5. No component reads the authoritative kimi_governor_state.json — grep for governor_state.json in agent_router.py/agent_chain.py returned nothing, and a repo-wide grep shows only kimi_governor._write_flag ever writes the flag body, exclusively on EXHAUSTED.\n\nConsequently, from 70% to 100% of a cap (and on period-near-end), the governor raises CONSERVE but transmits nothing; both planes read NORMAL/open. Only EXHAUSTED (100%) takes effect — and only by the fail-closed accident that the MANAGED_BY-only body is unparseable JSON (router :735-737) and carries no state= line (chain :195-200). With gemini auth-degraded, kimi at cost_rank 12 captures the generic [research,strategy] funnel from 70% to 100% with no brake, defeating the documented §5.2/§7.2 (F4/R-G) subscription protection.\n\nNote on design intent vs. defect: the flag-consumer side was explicitly built to accept a soft CONSERVE state (agent_router.py:722 docstring '\"Only an explicit {\"state\": \"CONSERVE\"}... is honoured\"; :83-85 flag can \"express CONSERVE ... without a code change\"), and the design doc §14 names end-to-end tests (test_generic_research_excluded_in_conserve, test_generic_research_prefers_gemini_codex, test_kimi_low_quota_disables_lane) that DO NOT EXIST in the test files — so there is no test proving a governor-produced CONSERVE narrows the router lane. The only router kimi test (test_agent_router.py:550-588) uses a MISSING flag (NORMAL). This is a genuine producer/consumer wiring gap: the governor deliberately never emits the CONSERVE the consumers were built to honor. Severity major is appropriate (safety/budget-protection, not a crash). The proposed fix is sound; the cleanest form is to have the governor write state=CONSERVE / a JSON {\"state\":...} on both CONSERVE and EXHAUSTED and unify the flag encoding across kimi_governor._write_flag, router.kimi_quota_state, and chain._read_kimi_flag_state, plus add the missing end-to-end tests.\n\nMust-keep (correct and should be preserved): the fail-closed reads on both consumers (router :730-743, chain :186-200) which treat any ambiguous/unparseable present flag as EXHAUSTED; the MANAGED_BY ownership guard so the governor never overwrites a foreign-owned flag (kimi_governor.py:268-273, :304-317; tests :163-177); the compute_state CONSERVE/EXHAUSTED derivation itself (:200-225, well tested :61-123); allowed_capabilities per state (:251-263); and the by_creator_vendor.kimi=non-kimi critic invariant enforced independently of quota state (chain :298-305, :354-358).",
  "evidence": "CONFIRMING (finding is true) — kimi_governor.py:299 `if state == \"EXHAUSTED\":` (flag written only here) with :308 else-branch clearing/leaving-absent on NORMAL/CONSERVE; tests/test_kimi_governor.py:180-184 asserts CONSERVE writes no flag (deliberate); agent_router.py:727-728 absent flag => KIMI_QUOTA_NORMAL; agent_router.py:1909+1994 route_once CONSERVE gate keyed only on kimi_state-from-flag; agent_chain.py:288 `state = _read_kimi_flag_state(flag) if str(flag) and flag.exists() else None` and :298 CONSERVE branch requires flag present; repo-wide grep: no reader of kimi_governor_state.json in router/chain, and only kimi_governor._write_flag ever writes the flag body. Could not find any line that transmits a governor-computed CONSERVE to either plane."
 }
]
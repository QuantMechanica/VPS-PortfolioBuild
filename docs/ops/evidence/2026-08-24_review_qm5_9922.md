# Review — QM5_9922_bandy-vortex-crossover-trend (router 9cb9c40c)

- Reviewer: Claude (review lane), 2026-08-24 UTC
- Router task: `9cb9c40c-317e-4a6b-be47-513aa088ecee` (review_ea, REVIEW, assigned codex)
- Source task: `39477905-...` (build_ea, source_agent gemini/agy) — SOURCE_READY_FOR_REVIEW
- Producer artifact: `docs/ops/evidence/39477905_qm5_9922_bandy-vortex-crossover-trend_build_identity.json`
- Card: `framework/EAs/QM5_9922_bandy-vortex-crossover-trend/docs/strategy_card.md` (G0 APPROVED)
- Source hash verified: current `.mq5` sha256 = `77d234edd9c1234d169972858ba489a5453ddecfecf47762e91aaade5c132ced` == identity json `mq5_sha256` (matches).

## Verdict: RECYCLE

Mechanism is card-faithful and correct; three named, actionable defects must be
fixed and re-reviewed before Q02 promotion.

## What passed
- **Card-faithful mechanism.** Vortex VI+/VI- (VM+ = sum|H[i]-L[i-1]|, VM- =
  sum|L[i]-H[i-1]|, /sum_TR, n=14), ADX(14)>=20 gate, SMA(200) regime gate,
  near-tie filter |VI+ - VI-|<0.05, Chandelier HHV(22)-2.5*ATR(14) trail ratchet,
  60-day time stop, opposite-cross exit, one-position-per-magic — all present and
  correct (`.mq5` lines 53-289).
- **No dead inputs.** All 10 `strategy_*` inputs (lines 38-47) have use-sites
  beyond declaration: vortex_period L164/166/229/230; vortex_min_diff L177;
  adx_period L169; adx_min L173; regime_sma_period L170; chandelier_lookback
  L187/206/272/281; atr_period same; chandelier_atr_mult same; time_stop_days
  L245; warmup_bars L134/155.
- **Framework conformity.** QM_Common chain (L5); MAE hook `QM_FrameworkTrackOpenPositionMae`
  (L319); RISK_FIXED=1000 / RISK_PERCENT=0 backtest (L19-20); magic via
  `QM_FrameworkMagic()` MagicResolver, formula ea_id*10000+slot verified
  (slot0=99220000 in magic_numbers.csv, 13 active rows); news filter wired
  (`QM_NewsAllowsTrade2` L326); bounded indicator buffers (Vortex loop shift..shift+period,
  no unbounded raw series); no ML; no invented commission/swap.
- **Compile evidence.** `framework/build/compile/20260822_122818/...compile.log`:
  0 errors, 0 warnings. build_gate_hardening D2-D18 PASS per producer evidence.

## Defects (RECYCLE — fix and re-review)

1. **Missing D1 timeframe guard** — `.mq5` `Strategy_NoTradeFilter()` L132-143 has
   no chart-timeframe check. The same-batch sibling QM5_9925 guards this at
   `QM5_9925_...mq5:54` (`if((ENUM_TIMEFRAMES)_Period != PERIOD_D1) return true;`).
   Without it, an intraday attachment re-evaluates the unchanged D1 crossover every
   chart bar and can churn/re-enter after a same-day exit. Factory pins D1 so Q02
   is unaffected, but this is an objective inconsistency vs. its own sibling and a
   real robustness gap. Add the D1 guard matching QM5_9925.

2. **Setfile provenance broken** — all 13 backtest sets carry 13 distinct
   `build_hash` header values (e.g. EURUSD set = `1d9e3e42...`), none equal to the
   delivered source hash `77d234ed...`; sets are dated 2026-08-22 and predate the
   2026-08-23 source. Scalar param keys themselves are correct and match input
   defaults, so trading behaviour is intact, but the source->setfile provenance
   chain is severed. Regenerate all 13 via the governed generator
   (`framework/scripts/gen_setfile.ps1`) bound to the current MQ5 sha, and re-mint
   the identity packet.

3. **Universe deviation from card (ROT scope)** — card authorizes "FX majors,
   XAUUSD, oil CFD, NDX.DWX, WS30.DWX" (SP500 backtest-optional; card L25/L82/L86).
   Delivered universe (magic_numbers.csv + 13 sets) OMITS oil (XTIUSD.DWX) and ADDS
   GDAXI.DWX and UK100.DWX, neither named in the card. Card universe is a ROT
   boundary; a missing machine-readable target list is not licence to rewrite it.
   Either restore card scope (add oil, drop unauthorized indices) or amend the card
   under OWNER authority before promotion.

## Note on producer's own review
Codex's producer-side note `docs/ops/evidence/2026-08-23_qm5_9922_gemini_code_review.md`
raised 4 findings. Findings 1/3/4 are confirmed above. Its Finding 2 ("entry-only
filters suppress exits" — NoTradeFilter before ManageOpenPosition) is the framework
template order and is identical in sibling QM5_9925 (L250 before L252); it is fleet
convention, not an independent 9922 defect — downgraded, not carried.

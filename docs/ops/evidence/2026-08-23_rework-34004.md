# rework-34004 — deterministic card-defect block

- Router task: `33203a5c-13c9-4db5-b553-2cc23519125f`
- EA: `QM5_34004_kositsin-step-trend-parabolic-envelope`
- Date audited: `2026-08-24`
- Disposition: `BLOCKED_CARD_DEFECT`

## Outcome

No EA source, setfile, binary, registry, resolver, framework, threshold, verdict, or queue row was changed. The approved Strategy Card still does not define enough state to implement its entry and management mechanisms deterministically. The ticket explicitly requires a `BLOCKED` disposition when a card defect is beyond Development repair, and the `qm-build-ea-from-card` procedure forbids Development from inventing missing strategy mechanics.

The previous durability finding is no longer the blocker: the EA package is tracked and appears in commits `8269f225ee32b272986025ac9f8377ca5c3744b2` and `bfd467bc6fcfa7dc1f61b4b8a2b5754fb394e500`. The substantive review findings remain.

## Deterministic evidence

### Approved card is unchanged and mechanically incomplete

The runtime approved card has SHA-256 `0157DDEC3A41EF9874A1C7E0D250CE1B267B78AF5867618FD6B1DB1725652867`. The repository approved copy differs only by line endings (SHA-256 `2A561C0B21F7BCA609589982F6A5C093C1D3DF221B0BE8ACD89DB40201048213`); a `git diff --no-index` produced no content diff. Both still declare `g0_status: APPROVED` and `last_updated: 2026-08-15` at `strategy-seeds/cards/approved/QM5_34004_kositsin-step-trend-parabolic-envelope.md:10-13`.

The card defines a recursive Step-MA at `strategy-seeds/cards/approved/QM5_34004_kositsin-step-trend-parabolic-envelope.md:76`, but it does not define:

- the initial Step-MA value or warm-up horizon;
- whether historical steps use each bar's contemporaneous ATR or the current ATR;
- whether one or multiple steps may be applied for a bar that crosses several step widths.

Those choices determine `Step_MA[1]`, `Step_MA[2]`, and therefore every long/short entry at card lines 90-94. The current source supplies unauthorized answers through `strategy_step_lookback = 50` at `framework/EAs/QM5_34004_kositsin-step-trend-parabolic-envelope/QM5_34004_kositsin-step-trend-parabolic-envelope.mq5:45`, seeds from that lookback at line 108, reuses one ATR-derived `step_size` for the whole reconstructed path, and applies repeated `while` steps at lines 122-125.

The card declares only `InpSARStep` at `strategy-seeds/cards/approved/QM5_34004_kositsin-step-trend-parabolic-envelope.md:164`; it provides no Parabolic SAR maximum acceleration. The source invents `strategy_sar_max = 0.20` at `framework/EAs/QM5_34004_kositsin-step-trend-parabolic-envelope/QM5_34004_kositsin-step-trend-parabolic-envelope.mq5:41`.

The lifecycle diagram names `BE_Trigger` and `Trailing Trigger` at card lines 141 and 148 without formulas or parameter values. The source cannot implement those transitions without guessing and currently leaves `Strategy_ManageOpenPosition` and `Strategy_ExitSignal` empty at source lines 191-198.

The original independent review reaches the same fail-closed result in `D:/QM/strategy_farm/artifacts/reviews/33203a5c-13c9-4db5-b553-2cc23519125f.json:20-62` and explicitly requires upstream card repair at line 159. No `docs/ops/evidence/*review*34004*` artifact exists beyond the router-linked aggregate note; the runtime review JSON is the detailed work order.

### Repairable defects cannot make the strategy executable by themselves

The exact card stop must reject an invalid protective level, but the current source invents a `1.5 * ATR` replacement at source lines 149-175. The source also compares `TimeCurrent()` directly with the card's GMT window at source lines 62-79 and omits the card's daily-entry halt, daily hard stop, and total-drawdown stop.

These defects are repairable once the entry recurrence and protection state are authorized. Fixing only them would still leave Development choosing the strategy's signal path and exit lifecycle, so it would not satisfy card fidelity.

### Identity and magic registry

- EA registry: `framework/registry/ea_id_registry.csv:4443` binds active ID `34004` to the exact slug.
- Magic registry: `framework/registry/magic_numbers.csv:17386-17388` contains the three active rows for `EURUSD.DWX`, `XAUUSD.DWX`, and `GBPJPY.DWX`.
- Scoped hardening reports all three symbols exact in the DWX matrix and no symbol/registry failure.

There is no registry gap, so `magic_numbers.csv` and `QM_MagicResolver.mqh` were not touched.

## Validation and test output

Command:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_34004_kositsin-step-trend-parabolic-envelope
```

Result: scoped hardening completed and reported four EA failures:

```text
EA_CARD_LOSS_LIMIT_MISMATCH: daily_entry_halt=2% input absent
EA_CARD_LOSS_LIMIT_MISMATCH: daily_hard_stop=2.5% input absent
EA_CARD_LOSS_LIMIT_MISMATCH: total_drawdown_stop=5% input absent
EA_BROKER_TIME_USED_FOR_GMT_WINDOW
```

Command:

```text
python -m pytest tools/strategy_farm/tests/test_mnt011_dirty_guard.py -q
```

Result:

```text
4 passed in 3.35s
```

No EA-specific pytest exists (`rg -l '34004|kositsin-step-trend-parabolic-envelope' tools/strategy_farm/tests` found only the existing dirty-guard fixture). No new code or unit test was authored because doing so would encode an unapproved answer to the missing card contract. Scoped `build_check.ps1` and compilation were not run after the hardening failure: there is no repaired source to validate, and a compile PASS cannot cure a card-fidelity failure.

## Required upstream amendment

Research/OWNER must issue a newly approved card revision that specifies all of the following:

1. Step-MA initialization value and warm-up horizon.
2. Per-bar ATR semantics during recurrence reconstruction.
3. Single-step versus multi-step behavior on a multi-width price move.
4. Parabolic SAR maximum acceleration.
5. Break-even trigger, exact `Entry + 1` unit, trailing trigger, trailing formula, and restart reconstruction behavior.

After that amendment, Development can remove the ATR stop fallback, implement the declared loss/drawdown/slippage and GMT controls, validate every authorized input, embed the exact approved card, add EA-specific static tests, and run scoped strict compile/build checks.

## Rollback

This commit adds only this evidence file. Roll back by reverting the evidence commit or deleting `docs/ops/evidence/2026-08-23_rework-34004.md`. No trading/runtime state needs rollback. To resume work after an approved card amendment, return router task `33203a5c-13c9-4db5-b553-2cc23519125f` to a rework state with the new card path and hash.

# rework-35008 review-fix evidence

- Router task: `df6959b3-d3e8-4702-8aed-a3b85a23e222`
- EA: `QM5_35008_short-term-bollinger-reversion-system`
- Worktree: `C:/QM/worktrees/rework-slot-6`
- Approved card: `strategy-seeds/cards/approved/QM5_35008_short-term-bollinger-reversion-system.md`
- Scope: EA review repair only; no backtest enqueue/delete, verdict-row write,
  threshold/gate change, factory toggle, registry mutation, or T_Live access.

## Work order and preflight

The original review found broker-time GMT rules, exit handling behind entry
filters, an invented 1.5R target, unauthorized stop/BE mechanics, and missing
loss controls at
`docs/ops/evidence/df6959b3_qm5_35008_gemini_build_codex_review_2026-08-18.md:16`,
`:25`, `:33`, `:40`, and `:46`. Ancestor remediation fixed those shapes. The
later review supplied the remaining work order: stale EX5, equity-based rather
than realized-loss accounting, and a forced exit that deasserted at 23:55 UTC
(`docs/ops/evidence/0ddd5459_qm5_35008_gemini_remediation_codex_review_2026-08-23.md:17`,
`:32`, and `:41`).

The card is `g0_status: APPROVED`. The exact slug/active EA row is at
`framework/registry/ea_id_registry.csv:4455`; the existing active slots 0-2
and magics `350080000`-`350080002` are at
`framework/registry/magic_numbers.csv:17418`, `:17419`, and `:17420`. There was
no registry gap, so neither registry nor `QM_MagicResolver.mqh` changed.

## What changed

1. The 2.0% entry halt now selects the current UTC day's closed trading deals,
   sums profit, swap, commission, and fee, reconstructs day-start balance, and
   fails closed when history/balance evidence is unavailable. It no longer
   reads open equity or `g_qm_ks_day_start_equity`
   (`framework/EAs/QM5_35008_short-term-bollinger-reversion-system/QM5_35008_short-term-bollinger-reversion-system.mq5:72`,
   `:113`, and `:146`). The separate 2.5% equity hard stop and 5.0% total stop
   remain wired through `QM_KillSwitchInit` at the same source's line 361.
2. The 23:00 UTC time exit now stays asserted after 23:00, before the next
   18:00 entry window, and for any prior-UTC-day position reconstructed from
   `POSITION_TIME`. It therefore survives 23:55 rollover, midnight, restart,
   and the following session until the old position is flat (source lines
   160 and 306).
3. Existing-position MAE, management, and forced exit remain before every
   entry-only filter (source lines 378, 383, 385, and 409). Exact SMA(20)
   middle-band TP and 1.5-ATR SL remain unchanged and card-faithful.
4. The approved three-tick slippage ceiling is now declared, fail-closed, and
   converted from `SYMBOL_TRADE_TICK_SIZE` to MT5 deviation points before
   `QM_EntryConfigure` (source lines 53, 91, 343, and 348). `OnInit` also
   declares the M15 execution contract at line 356.
5. All three authorized M15 backtest presets retain `RISK_FIXED=1000` and
   `RISK_PERCENT=0` at lines 19-20 and now seal
   `strategy_max_slippage_ticks=3.0` at line 39. SPEC documents the input and
   repair at `framework/EAs/QM5_35008_short-term-bollinger-reversion-system/SPEC.md:43`
   and `:116`.
6. Six focused regressions cover approved-card/registry identity, every input's
   use site, closed-deal accounting, restart-persistent exit, framework
   risk/magic/MAE/slippage/execution wiring, and all preset bindings
   (`tools/strategy_farm/tests/test_qm5_35008_review_rework.py:40`, `:68`,
   `:78`, `:95`, `:110`, and `:134`).

## Input, magic, and framework census

An `rg -n -w` census counted whole-word source sites for every input: 30
declared, zero declaration-only. Framework initialization consumes
`RISK_PERCENT`, `RISK_FIXED`, `PORTFOLIO_WEIGHT`, both news axes, RNG/stress,
Friday close, EA ID, and slot; all strategy inputs have an additional runtime
site. Magic remains resolver-backed through `QM_FrameworkMagic()`; no raw magic
literal or hand formula appears in the source. The direct hardening report
found no raw `BarsCalculated`, unbounded numeric buffer, double-new-bar gate,
uninitialized request, broker-time GMT defect, ML, or authorized-universe leak.

## Compile evidence

The required scoped command was run:

```text
pwsh -NoProfile -File framework/scripts/build_check.ps1 -RepoRoot . -EALabel QM5_35008_short-term-bollinger-reversion-system -Strict
```

It correctly stopped at the live-factory preflight with
`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`; no guard bypass, terminal stop/start,
factory change, queue mutation, or T_Live access occurred. As in the existing
slot-6 burn procedure, the worktree source was then compiled directly by the
verified-idle T4 MetaEditor without include mirroring. Normalized content for
all 57 non-resolver framework include files matched the worktree; the existing
T4 resolver contains the exact three active 35008 magic rows.

Final compile result: **0 errors, 0 warnings**, 398,944-byte EX5.

- MQ5 SHA-256: `000e572c007f776891b9acd01b829418e22085212fda9953a368e6224f4a9aea`
- EX5 SHA-256: `20b083449e1fad86e35b1fba5838a7364c2207f7c45b15dd7069cef7ea7729d1`
- Compile log: `D:/QM/reports/compile/20260824_rework35008_slot6/QM5_35008_short-term-bollinger-reversion-system.compile.log`
- Preserved pre-rework EX5: `D:/QM/reports/compile/20260824_rework35008_slot6/pre_rework_QM5_35008_short-term-bollinger-reversion-system.ex5`

## Test and validation output

```text
python -m pytest tools/strategy_farm/tests/test_qm5_35008_review_rework.py -q
......                                                                   [100%]
6 passed in 0.65s

python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py -q -k "d2_loss_limit or d4_management or d5_raw or d7_framework or d10_indicator or d11_canonical"
......                                                                   [100%]
6 passed, 23 deselected in 0.85s

python -m pytest tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_validate_symbol_scope.py -q
.......................                                                  [100%]
23 passed in 1.29s

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_35008_short-term-bollinger-reversion-system
PASS: files_scanned=1; failures=[]; warnings=[]; three-symbol registry/set universe clean

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_35008_short-term-bollinger-reversion-system
PASS: files_checked=4; findings=[]

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_35008_short-term-bollinger-reversion-system
PASS  QM5_35008_short-term-bollinger-reversion-system
Summary: 1 PASS, 0 FAIL (of 1)

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_35008_short-term-bollinger-reversion-system --fail-on-leak --json
SINGLE_SYMBOL_OK; n_violations=0

python tools/strategy_farm/raw_mq5_quarantine.py check --source-path <MQ5> --purpose compile --repo-root .
RAW_MQ5_SOURCE_ALLOWED

git diff --check
PASS (line-ending conversion advisories only; no whitespace errors)
```

The unfiltered hardening test module was also run. It produced **34 passed, 1
failed in 381.56s**. The sole failure is pre-existing and outside this ticket:
`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven
QM5_411xx findings beginning at
`QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`. The scoped QM5_35008 hardening scan
above is clean.

## Risks and rollback

- The compiler produced a current binary, but the monolithic `build_check.ps1`
  could not issue its own PASS receipt while factory terminals were active.
  The component gates and focused tests were therefore run separately and are
  recorded above; reviewer should retain this interlock fact in provenance.
- No smoke/backtest was run or enqueued. Build/review repair does not imply a
  pipeline verdict or live authority.

Rollback the ticket commit with `git revert --no-edit <rework-35008-commit>`.
That restores the prior MQ5/EX5/SPEC/presets and removes the focused test and
this evidence file. The original EX5 is also preserved at the report path
above. Router state is external to Git and must be transitioned separately;
no registry, resolver, queue, factory, terminal, verdict, or live rollback is
required.

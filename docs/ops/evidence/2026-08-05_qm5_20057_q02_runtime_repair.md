# QM5_20057 XAU/XAG Q02 runtime repair

Date: 2026-08-05 (Europe/Berlin)

Branch: `agents/board-advisor`

EA: `QM5_20057_xauxag-xmom1`

Instrument / timeframe: `XAUUSD.DWX` + `XAGUSD.DWX` / D1

Farm claim: `4235143a-9d01-4289-8177-4d25f85969cf`

## Outcome

One priority-2 diversity candidate was repaired and returned to Q02. The
existing work item was atomically reopened; no duplicate row was inserted and
no tester was launched manually.

- Work item: `96c14d4d-0a62-4935-85ec-dd75f570aafa`
- Transition: `done / INFRA_FAIL` -> `pending / NULL`
- Transition-ledger action: `infra_repair_requeue`
- Stable-binary follow-up: `pending_artifact_rebind`
- Queue handoff: `2026-08-05T19:00:53+00:00`
- Stable EX5 binding: `2026-08-05T19:07:12+00:00`
- Manual dispatch: `false`

This candidate is an approved, structural, low-frequency, market-neutral
precious-metals pair. Its Tier-A source is Fuertes, Miffre, and Rallis (2010),
*Journal of Banking & Finance*. It ranks only the immediately completed month,
buys the stronger metal, and shorts the weaker one. Existing XAU/XAG cards use
ratio reversion, ratio breakout, return-spread reversion, conditional
quantiles, or twelve-month momentum, so this is not a duplicate mechanic.

## Failure diagnosis

The retained Q02 evidence is:

`D:\QM\reports\work_items\96c14d4d-0a62-4935-85ec-dd75f570aafa\QM5_20057\20260723_110605\summary.json`

It records `TIMEOUT`, `METATESTER_HUNG`, and `INCOMPLETE_RUNS` after the single
full-history run reached the 7,200-second runner limit. It also proves that the
source and deployed EX5 were identical and stable, with no `ONINIT_FAILED` or
log-bomb classification. This was not an economic verdict.

The UTF-16 terminal log at `D:\QM\mt5\T2\logs\20260723.log` shows continuous
tester progress rather than a deadlock:

- test start: 13:06:22;
- 11 percent: 13:11:22;
- 31 percent: 13:46:23;
- 50 percent: 14:51:25;
- 52 percent: 15:01:25;
- the next work item started at 15:10:23 after the timeout killed this run.

The EA-specific hot path explained the projected multi-hour runtime. While a
pair was open, every host tick performed two full position scans. Each selected
position then called `QM_MagicChecked`, whose collision guard scanned all open
positions again. These redundant nested scans ran on top of the framework's
mandatory every-tick kill-switch and MAE scan.

## Repair

- Resolve and validate both registered magic numbers once during `OnInit`.
- Build one cached pair snapshot containing open-leg count, composition, and
  earliest entry time.
- Seed the snapshot on initialization and refresh it after every trade
  transaction and explicit pair open/close.
- Make the every-tick management path read the snapshot instead of rescanning
  positions and rerunning magic collision checks.
- Evaluate the entry-only news hook only after the D1 new-bar gate. Position
  management and exits still run before that gate on every tick.

Entry ranking, monthly rebalance, paired direction, restart guard, ATR stop,
40-day exit, spread limits, sizing, and risk inputs are unchanged. The repair
does not add an indicator, external feed, adaptive fit, or ML component.

The legacy `SPEC.md` was also brought up to the mandatory seven-section Q01
schema and its stale strategy suffix was corrected from `S02` to the approved
card identity `FMR-MOMTS-2010_XAU_XAG_S03`.

## Verification

- Strategy spec validation: `PASS` (1/1)
- Build guardrails: `PASS`, no findings
- Strict MetaEditor compile: `PASS`, 0 errors, 0 warnings
- Final compile log:
  `C:\QM\repo\framework\build\compile\20260805_190344\QM5_20057_xauxag-xmom1.compile.log`
- Full targeted build check: `PASS`, 0 failures, 0 warnings
- Build-check report:
  `D:\QM\reports\framework\21\build_check_20260805_185424.json`

The canonical backtest setfile remains fixed-risk only:
`RISK_FIXED=1000`, `RISK_PERCENT=0`, and `PORTFOLIO_WEIGHT=1`.

Bound artifact SHA-256 values:

| Artifact | SHA-256 |
|---|---|
| MQ5 | `773c512b0848a3db455b9a063dbb35e866aa7df8c9960af2d5c035d2998a2d4a` |
| EX5 | `edc0bb430fe0b0105a7e6bb2875ba5707afddce1595bd5650ccfd07595a0a9b9` |
| Backtest setfile | `81f9e1458fbd382a218c1b52fef0f8533c29f54fa67e3e6c0d60483c422ecc71` |

Shared MetaEditor contention delayed the final EX5 write after the first queue
transaction. The work item remained unclaimed. A second compare-and-swap
transaction bound the stable 370,618-byte executable and added an immutable
`pending_artifact_rebind` ledger entry; the earlier binding remains auditable.

## Farm coordination and safety

- Pre-claim backup:
  `D:\QM\strategy_farm\state\backups\farm_state_before_qm5_20057_q02_infra_claim_20260805T184221Z.sqlite`
- Pre-requeue backup:
  `D:\QM\strategy_farm\state\backups\farm_state_before_qm5_20057_q02_runtime_requeue_20260805T190051Z.sqlite`
- Stable-EX5 rebind backup:
  `D:\QM\strategy_farm\state\backups\farm_state_before_qm5_20057_stable_ex5_rebind_20260805T190708Z.sqlite`
- Capacity scan: nine factory terminals were running against a ceiling of
  seven; `terminal64_running_count` was 11 including two non-farm terminals.
- CPU-ceiling response: queue only, then stop. No smoke test, dispatch tick,
  terminal reservation, or manual backtest was started.
- The prior Q02 summary and its old MQ5/EX5/setfile hashes are preserved under
  `infra_repair_history` in the work-item payload.
- No `T_Live`, AutoTrading, live/deploy manifest, portfolio gate, portfolio
  admission, or live setfile was changed or operated.

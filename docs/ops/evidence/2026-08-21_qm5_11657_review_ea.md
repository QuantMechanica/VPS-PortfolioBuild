# QM5_11657 review_ea — 2026-08-21

## Verdict

**APPROVED for review_ea.** The reviewed source is faithful to the approved PatternPy card, fail-closed on host/slot/timeframe/configuration, compiled and guardrail-clean, and bound to fixed-risk backtest setfiles. This review does not predict or replace any pipeline verdict. The pending EURUSD.DWX Q02 canary remains owned by the factory, and the four deferred hosts remain `AWAITING_CANARY`.

## Reviewed identity

- EA: `QM5_11657_pp-hs-rev`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_11657_pp-hs-rev.md`
- Source file SHA-256: `98dc435b2d739e9604c1135d58c20d9f8296ae1e5395daf3eea359b33ff788f5`
- Binary SHA-256: `a72cca2214f1d5407e0ddd26ac5e14a31be8ded420b26d7f7a09ab200cb88af9`
- Compile evidence: `framework/build/compile/20260821_024314/QM5_11657_pp-hs-rev.compile.log`, 0 errors / 0 warnings.
- Strict check receipt: `D:/QM/reports/framework/21/build_check_20260821_150137.json`, PASS, 0 failures / 0 warnings.

The EX5 timestamp is 2026-08-21 02:43:45Z; the MQ5 timestamp is 2026-08-21 02:38:36Z. The source and binary hashes match the prior Q01 handoff, so the compiled artifact is not stale against the reviewed MQ5.

## Card fidelity

The cited primary source is Keith Orange's PatternPy `detect_head_shoulder` implementation: <https://github.com/keithorange/PatternPy/blob/main/tradingpatterns/tradingpatterns.py#L4-L20>.

The source computes a three-row rolling High maximum and Low minimum at row `i`, compares row `i` with `i-1` and future row `i+1`, assigns the ordinary label first, then assigns the inverse label second. The EA maps these to completed shifts 4/3/2 plus confirmation shift 1, delaying action until the source's future row has closed. Its strict inequalities and inverse-wins precedence match the cited implementation. Entry at the next current-bar open, opposite-label exit, 12-bar maximum hold, one position per magic, and the card-authorized ATR(14) 2.0x hard stop are all present. No neckline reconstruction, take-profit, trailing/break-even/partial close, grid, martingale, or ML was added.

## Input-use audit

Every declared strategy input has a real authorization and/or execution use site:

| Input | Authorization/use |
|---|---|
| `strategy_window` | fixed to 3 by `Strategy_ConfigurationAuthorized`; checked again by `PatternPyDirection` |
| `strategy_atr_period` | fixed to 14; passed to `QM_ATR` |
| `strategy_sl_atr_mult` | bounded 1.0–3.0; passed to `QM_StopATRFromValue` |
| `strategy_max_hold_bars` | fixed to 12; used by the `iBarShift` lifecycle exit |

Framework inputs are passed to `QM_FrameworkInit` or the news path. The EA samples open-position MAE, executes kill-switch and Friday-close checks before entries, routes openings/closings through TradeManager helpers, and does not mutate its stop after entry.

## Host, magic, risk, and time controls

`ea_id_registry.csv` has active row `11657,pp-hs-rev`. `magic_numbers.csv` has exactly the five active bindings used by the source and setfiles:

| Slot | Host | Magic |
|---:|---|---:|
| 0 | EURUSD.DWX | 116570000 |
| 1 | GBPUSD.DWX | 116570001 |
| 2 | XAUUSD.DWX | 116570002 |
| 3 | GDAXI.DWX | 116570003 |
| 4 | NDX.DWX | 116570004 |

Initialization rejects any unknown host, wrong slot, wrong EA ID, unauthorized timeframe, or out-of-contract strategy input. The only authorized timeframes are H1/H4/D1 as permitted by the card's later parameter phase; all baseline setfiles are H4. There is no strategy session window, so no custom DST conversion exists to drift. Friday close and news checks consume framework broker time.

All five backtest setfiles use `RISK_FIXED=1000`, `RISK_PERCENT=0`, `PORTFOLIO_WEIGHT=1`, the matching magic slot, and the four approved strategy defaults. News defaults are fail-closed DXZ compliance with PRE30/POST30 temporal blackout, high-impact minimum, and `qm_news_stale_max_hours=336`. No stale-news weakening was made.

## Focused verification

- `skill_build_ea_guard.py --ea-id 11657 --ea-label QM5_11657_pp-hs-rev` → prerequisites OK.
- `validate_spec_doc.py framework/EAs/QM5_11657_pp-hs-rev` → PASS.
- `validate_build_guardrails.py` on the MQ5 and all five setfiles → PASS for every path; max stale-news bound 336.
- `validate_symbol_scope.py --ea-label QM5_11657_pp-hs-rev --fail-on-leak --verbose` → `SINGLE_SYMBOL_OK`, zero violations.
- `build_check.ps1 -EALabel QM5_11657_pp-hs-rev -Strict -SkipCompile` → PASS, zero failures and warnings.
- Direct input grep → every strategy and framework input has a real use beyond its declaration.
- Direct registry audit → all five slot/magic rows are active and match source/setfiles.
- Direct primary-source comparison → rolling window, shift mapping, strict comparisons, and inverse precedence match.

The strict check restamped only self-referential `build_hash` comments; no executable set input changed. No new work item was enqueued, no existing row was claimed or interrupted, and no T_Live, AutoTrading, deployment, portfolio, or verdict state was touched.

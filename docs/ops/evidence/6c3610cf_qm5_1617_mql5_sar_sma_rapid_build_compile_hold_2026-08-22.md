# QM5_1617 Build Evidence — Source Ready, Governed Compile Held

- Task: `6c3610cf-2087-4942-bdd2-a4268a339a14` (`build_ea`, priority 50, assigned to Codex)
- EA: `QM5_1617_mql5-sar-sma-rapid`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1617_mql5-sar-sma-rapid.md`
- Current MQ5 SHA-256: `09b433e6ec4bc5bcc793a903ffc2370f7f8699e58e63d7545101b85799ffbc61`
- Outcome: `SOURCE_READY_COMPILE_HELD`

## Governed identity

The card declares `g0_status: APPROVED`, `ea_id: QM5_1617`, and slug `mql5-sar-sma-rapid`. The active EA registry row matches that identity. Thirteen active magic rows exist for slots 0-12: GDAXI, NDX, SP500, UK100, WS30, XAUUSD, EURUSD, GBPUSD, USDJPY, USDCHF, AUDUSD, USDCAD, and NZDUSD, all with the `.DWX` suffix.

## Card implementation

The source implements the approved M15 rapid-reversal rule:

- completed-bar Parabolic SAR side reversal with the SMA(60) price filter;
- buy when SAR moves from above the prior high to below the signal-bar low and SMA is below Ask, with the exact mirrored sell rule;
- one signal per completed M15 bar and one position per symbol/magic;
- fixed source exits of 150 symbol points for stop loss and 100 points for take profit;
- standard fail-closed news gate and Friday liquidation;
- `RISK_FIXED=1000`, `RISK_PERCENT=0`, and `qm_news_stale_max_hours=336`;
- no ML, HFT, grid, martingale, averaging, or discretionary logic.

Framework alignment is explicit: the source calls the current MAE hook on every tick and zero-initializes every `QM_EntryRequest` before use.

## Focused verification

- `validate_spec_doc.py`: PASS (1/1).
- `validate_build_guardrails.py`: PASS; 14 files checked, zero findings, stale-news ceiling 336 hours.
- `validate_symbol_scope.py --fail-on-leak`: `SINGLE_SYMBOL_OK`, zero violations.
- Thirteen M15 backtest presets regenerated by canonical `gen_setfile.ps1`.
- Every preset contains `RISK_FIXED=1000`, `RISK_PERCENT=0`, the five strategy defaults, its governed magic slot, and pending build hash.
- `git diff --check`: clean for the EA package.
- Codex source review confirmed closed-bar shifts, exact mirrored entry logic, stop/take direction, magic-scoped position control, MAE tracking, and request initialization.

## Compile boundary

Direct `build_check.ps1` stopped before compilation with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because terminal workers are alive. No bypass, retry, process stop, terminal launch, or backtest interruption was attempted.

The sanctioned command `farmctl.py enqueue-compile QM5_1617_mql5-sar-sma-rapid` was then refused without enqueueing a row: reason `WORK_ITEMS_EXIST`. The two existing governed compile records are terminal failures (`335aa087-735d-432b-9fd7-7f7f6ce7e5aa` INVALID and `ffb4ce00-951d-4308-86a1-5921d4fa40b0` COMPILE_FAIL). The latter was bound to obsolete source SHA-256 `038e41d571cc8f0e7b8ce3ac1f34f0e7c3bc5d698b70cce9c5d7081e597cf6af`, not the current source. No `.ex5` exists for the current source.

Smoke was not run, and no pipeline verdict is claimed.

## Required next governed action

A sanctioned compile-record supersede/rebuild authorization must admit a fresh record bound to the current source. Its worker must produce strict compile and build-check PASS evidence, bind final set-file hashes, and only then permit smoke/review closure.

Short verdict: `SOURCE_READY_COMPILE_HELD: static gates PASS; fresh governed compile refused by prior work-item guard.`

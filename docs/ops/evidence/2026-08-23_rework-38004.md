# Rework evidence — EA 38004

Date: 2026-08-24 (Europe/Berlin)
Ticket: `rework-38004`
Router task: `91b5a55b-0eca-44ab-834a-1e0da4042bef`
EA: `QM5_38004_codetrading-triple-ema-momentum-scalper`
Disposition at handoff: `SOURCE_READY_COMPILE_HELD`

## Work order and scope

The router task was read from `python tools/strategy_farm/agent_router.py list-tasks --agent codex` and claimed with:

```text
python tools/strategy_farm/agent_router.py update-task 91b5a55b-0eca-44ab-834a-1e0da4042bef --state IN_PROGRESS --verdict "rework burn-window slot 4"
```

The work order was the linked source-build evidence plus:

- `docs/ops/evidence/91b5a55b_qm5_38004_codex_review_2026-08-18.md`
- `docs/ops/evidence/02fafcc6_qm5_38004_gemini_review_2026-08-23.md`

The approved Strategy Card was read from `D:/QM/strategy_farm/artifacts/cards_approved/QM5_38004_codetrading-triple-ema-momentum-scalper.md`. Registry identity was already complete, so no allocation was changed: `framework/registry/ea_id_registry.csv:4475` and `framework/registry/magic_numbers.csv:17493-17495` contain EA 38004 and its NDX/WS30/GDAXI slots.

No other EA, registry, gate criterion, factory state, backtest queue, verdict row, or `C:/QM/mt5/T_Live` content was changed.

## Changes

- The EA validates all declared card controls and prevents permissive overrides at `framework/EAs/QM5_38004_codetrading-triple-ema-momentum-scalper/QM5_38004_codetrading-triple-ema-momentum-scalper.mq5:70`.
- Slippage is constrained to three ticks and converted to broker points at the same file's lines 48 and 106-114. `QM_EntryConfigure` consumes the result at line 396.
- `AdvanceState_OnNewBar` refreshes bounded EMA/ATR/OHLC state at lines 116-155. Array reads are guarded by `ArraySize`; the raw series reads are explicitly performance-allowed and confined to this new-bar refresh.
- The entry-only admission filter at lines 217-239 now consumes initialized/fresh state, UTC-converted rollover time, current prices, daily realized loss, total drawdown, position count, and ATR spread. It no longer prevents open-position management.
- The stop is exactly EMA(55) plus/minus the two-pip card buffer; invalid stop geometry rejects the entry instead of substituting an ATR stop (lines 265-299).
- Position management at lines 302-364 derives immutable original R from the fixed 2R take-profit and trails at EMA(21). It no longer derives R from the already-mutated stop.
- `OnTick` invokes the framework MAE/kill-switch path, initializes new-bar state before admission, and manages open positions before entry-only filters (lines 432-483).
- `QM_KillSwitchInit` wires 2.0% daily warning, 2.5% daily hard loss, 5.0% total drawdown, and the 0.5% live per-trade ceiling at lines 404-408. Line 414 applies the percentage cap only in live `RISK_PERCENT` mode, preserving card-required `RISK_FIXED=1000` for backtests.
- All three setfiles use `strategy_sl_buffer_pips=2`, `strategy_max_slippage_ticks=3`, the 2/2.5/5/0.5 controls, and source hash `67a7d6723f38ac7108e7f256c830052dcb37fee89184e297c6dffc00af91ca99` (setfile lines 13, 29, and 36-40).
- `SPEC.md:13-37` documents the exact stop, original-R trail, slippage, loss controls, and fixed-test/percent-live sizing contract; `SPEC.md:100` names the framework's real sizing error code.
- `tools/strategy_farm/tests/test_qm5_38004_review_rework.py:68-161` locks input use-sites, state/admission/management ordering, framework risk wiring, exact stop/original-R behavior, sealed setfiles, and registry/hardening compliance.

## Validation evidence

Target-scoped source and registry checks:

```text
python framework/scripts/skill_build_ea_guard.py --ea-id 38004 --ea-label QM5_38004_codetrading-triple-ema-momentum-scalper
status: ok; registry and directory checks passed

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_38004_codetrading-triple-ema-momentum-scalper
PASS: 1/1

python framework/scripts/validate_build_guardrails.py <EA source and its three setfiles>
PASS: four files; zero findings

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_38004_codetrading-triple-ema-momentum-scalper
PASS: failures=0, warnings=0
```

Unit tests:

```text
python -m pytest tools/strategy_farm/tests/test_qm5_38004_review_rework.py -q
6 passed in 0.81s

python -m pytest tools/strategy_farm/tests/test_qm5_38004_review_rework.py tools/strategy_farm/tests/test_build_guardrails.py -q
26 passed in 1.49s
```

The full touched hardening-module test was also run:

```text
python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py -q
1 failed, 28 passed in 438.03s
```

Its one failure is outside this ticket's allowed scope: `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports the first unrelated finding at `framework/EAs/QM5_41134_wti-mdaily-iqrmean-mom/QM5_41134_wti-mdaily-iqrmean-mom.mq5:599` (seven 411xx findings total). EA 38004's scoped hardening check is clean.

## Compile interlock and residual risk

Two exact-label strict compile attempts were made through `framework/scripts/compile_one.ps1`. Both stopped before compilation with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` / `INCLUDE_MIRROR_REFUSED` because terminal64 processes were active. A scoped `build_check.ps1 -EALabel QM5_38004_codetrading-triple-ema-momentum-scalper -Strict -SkipCompile` reached the same guard. Evidence:

- `D:/QM/reports/compile/20260824_094916/summary.csv`
- `D:/QM/reports/compile/20260824_100005/summary.csv`

No terminal or factory state was changed, and the guard was not bypassed. The current MQ5 SHA-256 is `67a7d6723f38ac7108e7f256c830052dcb37fee89184e297c6dffc00af91ca99`. The existing EX5 remains unchanged and stale, SHA-256 `eb1da058b0b708e897bb2de639a5bd50e1cc4e2bbc46be70699d4b51fb4f2e22`. Therefore this handoff does **not** claim current-source strict compile, build-check PASS, or smoke-test PASS. A governed burn window must compile and smoke the current source before downstream approval.

## Rollback

Revert the ticket commit with `git revert <rework-38004-commit>` to restore the EA source, specification, three setfiles, unit test, and this evidence record. The EX5 needs no rollback because it was not modified. Router state must be adjusted through `agent_router.py` by the authorized workflow; do not edit the SQLite state directly.

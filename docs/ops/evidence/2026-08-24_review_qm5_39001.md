# QM5_39001 review (Claude lane) — 2026-08-24

- Review task: `2d0bdc23-749d-4443-9000-beec43d0135c` (review_ea, assigned codex, state REVIEW)
- Source build task: `60d701a7-...` (agy/gemini), verdict claimed "Remediated ... build guardrails PASS; SPEC PASS"
- EA: `framework/EAs/QM5_39001_forexfactory-trading-made-simple-tms`
- Approved card: `strategy-seeds/cards/approved/QM5_39001_forexfactory-trading-made-simple-tms.md`
- Reviewed MQ5 SHA-256: `cb6564d53eff98ec4c9bc494e4bd14b1baf4cd370c7281470695e981db056355` (== build packet)
- Prior Codex reviews: `docs/ops/evidence/2d0bdc23_qm5_39001_gemini_review_2026-08-23.md` (CHANGES_REQUIRED),
  `docs/ops/evidence/5e1b4b08_qm5_39001_gemini_build_codex_review_2026-08-18.md` (CHANGES_REQUIRED)
- Disposition: **RECYCLE** — named actionable defects persist; source unchanged since the 08-23 CHANGES_REQUIRED review.

## Verified clean

- No dead inputs: all 21 `strategy_*` inputs plus framework inputs have ≥1 use-site beyond declaration (grep, decl+use counts all ≥2).
- Setfiles use governed risk mode: `RISK_FIXED=1000`, `RISK_PERCENT=0` on all three (EURUSD/GBPUSD/AUDUSD .DWX H1).
- Capital-preservation inputs present (`strategy_daily_loss_halt_pct=2.0`, `_hard_stop_pct=2.5`, `_total_dd_halt_pct=5.0`,
  `_per_trade_risk_cap_pct=0.5`) matching card §4.2.
- Indicator reads are shift=1 closed-bar and annotated perf-allowed (lines 101–126); no ML, no raw OrderSend/CopyBuffer.

## Blocking defects (RECYCLE)

1. **Binary not bound to reviewed source (provenance).** Current MQ5 is commit `efbb11d07` dated 2026-08-23 15:04
   (sha `cb6564...`); the retained EX5 is dated 2026-08-18 18:42 and was not recompiled after remediation. The three
   setfile `build_hash` comments are mutually different (`ba2767...`, `c852d0...`, `adca3d...`) and none equals the reviewed
   MQ5 hash. Packet claims `compile_succeeded=true` while `smoke_report_path=null`. No strict compile result binds the
   current source hash. Fix: compile through the governed compiler, regenerate all setfile hashes, return strict hash-bound MQ5/EX5.
2. **GMT rollover uses raw broker time.** `StrategyInRolloverWindow(TimeCurrent())` (mq5:152) with the window compare at
   mq5:76–81 uses broker-local time; no `QM_BrokerToUTC` conversion. Card §3.1.2 specifies a 23:55–00:05 **GMT** blackout;
   Darwinex broker time is GMT+2/+3, so the blackout fires at the wrong wall-clock window.
3. **Swing stop deviates from card §3.4.** Card requires SL exactly at the recent H1 swing extreme ∓3 pips.
   mq5:205–208 (BUY) and 218–221 (SELL) impose a `0.5*ATR` minimum stop distance and recompute SL from entry, moving a
   nearer valid swing stop farther out and altering both SL and the derived 2R TP.
4. **H1 cadence not bound to H1.** Indicators read `strategy_signal_tf=PERIOD_H1` (mq5:101–126) but the new-bar gate at
   mq5:388 calls parameterless `QM_IsNewBar()`, i.e. the chart timeframe drives signal/exit refresh, not H1.

Also noted (card adjudication, not independently blocking): the 5-bar `strategy_swing_lookback` (mq5:43), the +1R
break-even rule (`strategy_be_*`), and the TDI cross-back exit are SPEC-level inventions not defined in the approved card
— these should be adjudicated into the card before implementation, per the 08-23 review.

## Scope

Read-only review. No source changed, no compile, no setfile regeneration, no Q phase, no backtest, no terminal launch,
no AutoTrading/T_Live action. DB accessed read-only.

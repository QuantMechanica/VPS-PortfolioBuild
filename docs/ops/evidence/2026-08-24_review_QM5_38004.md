# Review — QM5_38004 CodeTrading Triple EMA Momentum Scalper

- **Router task:** 02fafcc6-df34-46b4-b195-7324f635e420 (review_ea, state REVIEW, assigned codex)
- **Source task:** 952a07fb-63c7-4fc3-9582-83096a13c9e0 (build_ea, source_agent gemini/agy)
- **Source verdict:** "PASS: build verified, SPEC validated, guardrails checked (336h), D4 reachability fixed; forwarded to Codex review"
- **Artifact:** `C:/QM/repo/artifacts/qm5_38004_build_result.json`
- **EA dir:** `framework/EAs/QM5_38004_codetrading-triple-ema-momentum-scalper/`
- **Card:** `strategy-seeds/cards/approved/QM5_38004_codetrading-triple-ema-momentum-scalper.md`
- **Reviewer:** Claude (review lane), read-only DB
- **Verdict: RECYCLE** — source clean & card-faithful, but delivered `.ex5`/setfiles are stale vs. the current committed source.

## Source-code review (mq5 @ HEAD e554dfdf1b44b8da27a258ac149f1453eb1dcf5423b30dedb0f08eb289cdd334) — CLEAN

- **Card-faithful mechanism.** Long: `ema8>ema21>ema55 && low1<=ema8 && close1>ema21 && close1>open1` (mq5:198-200); Short: mirror (mq5:201-203). Matches card §3.2/§3.3 exactly. SL = `ema_slow ∓ buffer` (mq5:219), TP = 2.0R via `QM_TakeRR` (mq5:226), trail with EMA21 after +1.0R (mq5:271-292) — matches card §3.4. No-trade filters (spread>1.8×ATR, rollover, daily loss 2%, max 1 position) present (mq5:101-149,194).
- **Every declared strategy input is live** (grep of the 15 `strategy_*` inputs: all count ≥2 = declaration + ≥1 use-site). No dead inputs.
- **Framework conformity:** QM_Common include (mq5:5); `QM_FrameworkInit` (mq5:314); MAE hook `QM_FrameworkTrackOpenPositionMae()` (mq5:344); RISK_FIXED=1000 / RISK_PERCENT=0 backtest defaults (mq5:17-18); magic via `QM_FrameworkMagic()` (mq5:71,193), magic_base 380040000 = 38004×10000; news filter wired via `QM_NewsAllowsTrade2` (mq5:376-382); bounded `QM_EMA`/`QM_ATR` helpers; raw series only `iOpen/iHigh/iLow/iClose[1]` behind `QM_IsNewBar()` with perf-allowed comments (mq5:182-185); no ML; no invented commission/swap. D4 management/exit reachability confirmed (NoTradeFilter returns false when a position is open, mq5:148-149; management/exit run before the entry gate).
- All reworked helpers exist and signatures match (`QM_TakeRR` QM_StopRules.mqh:230; `QM_BrokerToUTC`, `QM_StopRulesPipsToPriceDistance`, `QM_TM_OpenPositionCount`) — source is compilable.

## Blocking defect — STALE ARTIFACTS vs current source (stale-ex5 class)

The delivered gemini artifact was internally consistent at build time (mq5 08ab42… → ex5 eb1da05…, compile_succeeded). But commit **20425871d** ("overnight rework artifact snapshot") later rewrote the mq5 (163 ins / 109 del: new SL path via QM_TakeRR, daily-loss/daily-drawdown/total-drawdown halts, inlined signal, ExitSignal now returns hard-halt state, trail rewrite) **without rebuilding the .ex5 or regenerating the setfiles.**

Evidence (hash provenance):
- current committed `.mq5` sha256 = `e554dfdf…`
- committed `.ex5` sha256 = `eb1da058…` = build_result.json ex5, built from **superseded** mq5 `08ab42f3…` (ex5 mtime Aug 18 < mq5 mtime Aug 24)
- setfiles header `build_hash: 94eb302a…` (dated 2026-08-17) — a third, older hash; **missing the 3 new inputs** `strategy_daily_loss_limit_pct`, `strategy_daily_drawdown_hard_stop_pct`, `strategy_total_drawdown_stop_pct`, and `strategy_sl_buffer_pips=2.0` is now an `int` input.

Consequence: sending to Q02 now would either run the old-behavior `.ex5` or trip the stale-ex5/build-date predicate (INVALID). There is **no COMPILE_OK / D10-clean evidence for the current source hash e554df**.

## Actionable fix (single, named)

Recompile `.ex5` from current source e554df and regenerate all three setfiles via the governed generator (`framework/scripts/gen_setfile.ps1`, `-EALabel`), in an OFF-window / COMPILE_EA queue (recompile is ROT for the review lane — not done here). Then the EA is Q02-ready; the source itself needs no code change.

## Verdict

**RECYCLE** — recompile ex5 + regenerate setfiles for source e554df (rework 20425871d); delivered ex5/setfiles/build evidence cover superseded hashes 08ab42/94eb302a. Source code is card-faithful, framework-conformant, all inputs live.

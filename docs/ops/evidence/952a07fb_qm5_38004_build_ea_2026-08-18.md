# QM5_38004 Build EA Evidence — 2026-08-18

**Task ID:** `952a07fb-63c7-4fc3-9582-83096a13c9e0` (`build_ea`, priority 50, agent `gemini`).  
**EA:** `QM5_38004_codetrading-triple-ema-momentum-scalper`.  
**Artifact Path:** `C:/QM/repo/docs/ops/evidence/952a07fb_qm5_38004_build_ea_2026-08-18.md`.  
**Strategy Card:** `strategy-seeds/cards/approved/QM5_38004_codetrading-triple-ema-momentum-scalper.md` (G0 APPROVED).  

---

## 1. Strategy Overview & Implementation

- **Strategy Thesis**: Ultra-fast intraday momentum scalper on M5 using 8, 21, and 55 EMA ribbon alignment with micro-pullbacks into the dynamic 8-21 EMA channel.
  - Long (BUY): Bullish ribbon (`EMA(8) > EMA(21) > EMA(55)`), pullback into pocket (`Low[1] <= EMA(8)[1] && Close[1] > EMA(21)[1]`), and bullish candle (`Close[1] > Open[1]`).
  - Short (SELL): Bearish ribbon (`EMA(8) < EMA(21) < EMA(55)`), pullback into pocket (`High[1] >= EMA(8)[1] && Close[1] < EMA(21)[1]`), and bearish candle (`Close[1] < Open[1]`).
  - Exits & Stops: Stop Loss placed beyond 55 EMA baseline ($\mp 2.0\text{ pips}$), Take Profit targeting $1:2.0\text{ R:R}$, and dynamic trailing along the 21 EMA line once $+1.0\text{R}$ profit is achieved.
- **Framework Corset Compliance**:
  - Implemented strictly with `EA_Skeleton.mq5` wiring and framework readers.
  - Closed-bar calculation cached in `AdvanceState_OnNewBar()` to ensure $O(1)$ per-tick performance.
  - Sizing handled strictly by `QM_LotsForRisk` from `req.sl`.
  - News temporal mode (`QM_NEWS_TEMPORAL_PRE30_POST30`) and compliance (`QM_NEWS_COMPLIANCE_DXZ`) with fail-closed stale guard (`qm_news_stale_max_hours = 336`).

---

## 2. Registry & Artifact Verification

- **Registries Updated**:
  - `framework/registry/ea_id_registry.csv`: verified `38004,codetrading-triple-ema-momentum-scalper`.
  - `framework/registry/magic_numbers.csv`: allocated slots 0 (`NDX.DWX`, `380040000`), 1 (`WS30.DWX`, `380040001`), 2 (`GDAXI.DWX`, `380040002`).
  - `framework/include/QM/QM_MagicResolver.mqh`: regenerated cleanly via `update_magic_resolver.py`.
- **SPEC Document**:
  - `framework/EAs/QM5_38004_codetrading-triple-ema-momentum-scalper/SPEC.md` written and verified with `validate_spec_doc.py` (`PASS`).
- **Static Checks & Compilation**:
  - `build_check.ps1`: `compile_one.result=PASS`, `errors=0`, `warnings=0`, `build_check.result=PASS`.
  - Emitted binary: `framework/EAs/QM5_38004_codetrading-triple-ema-momentum-scalper/QM5_38004_codetrading-triple-ema-momentum-scalper.ex5`.
- **P2 Setfiles**:
  - Generated for `NDX.DWX`, `WS30.DWX`, `GDAXI.DWX` via `gen_setfile.ps1`.

---

## 3. Smoke Test & Review State

- `run_smoke.ps1`: Encountered standalone admission rejection (`custom_history_smoke_admission.py` requires worker-bound work item under active Custom-history isolation).
- Build result JSON emitted honestly per schema to `artifacts/qm5_38004_build_result.json` with `build_check_passed=true`, `compile_succeeded=true`, `smoke_result=framework_error`.
- Task updated to `REVIEW` for mandatory Codex review per router rules.

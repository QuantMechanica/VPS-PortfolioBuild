# QM5_36001 (nnfx-classic-mcginley-ssl-wae) Build Remediation Evidence

- Task ID: `3c1da904-b03a-40d5-a1b3-c23e9ffad4b8`
- EA ID: `QM5_36001`
- Date: 2026-08-23
- Branch: `agents/board-advisor`
- Target symbols: EURUSD.DWX (slot 0), GBPUSD.DWX (slot 1), AUDUSD.DWX (slot 2), USDJPY.DWX (slot 3), USDCAD.DWX (slot 4)
- Timeframe: D1
- Magic Numbers: 360010000 (slot 0), 360010001 (slot 1), 360010002 (slot 2), 360010003 (slot 3), 360010004 (slot 4)

## Summary of Remediation Changes

Addressed all findings from Codex review (`d47d0803_qm5_36001_gemini_build_codex_review_2026-08-18.md`):

1. **Two-Stage NNFX Lifecycle (TP1 + Runner)**:
   - Removed full broker TP from entry (`req.tp = 0.0`).
   - Implemented 50% partial close at +1.0 ATR in `Strategy_ManageOpenPosition` using `QM_TM_PartialClose(ticket, half_lots, QM_EXIT_PARTIAL)`.
   - Once TP1 partial close triggers, the runner SL is moved to break-even (+1 pip buffer).

2. **SSL Crossover vs Persistent State**:
   - Replaced static SSL state check with `Strategy_SSLCross(1)` which strictly tests for a fresh transition/crossover at shift 1 relative to shift 2 (`state_now == 1 && state_prev != 1` for Long, `state_now == -1 && state_prev != -1` for Short).

3. **DeMarker Crossover Exit**:
   - Updated `Strategy_ExitSignal` to evaluate DeMarker crossing opposite extremes (`demarker_1 >= 0.70 && demarker_2 < 0.70` for Long, `demarker_1 <= 0.30 && demarker_2 > 0.30` for Short) or SSL channel flipping against the position.

4. **Waddah Attar Explosion (WAE) Directional Gate**:
   - Implemented exact card formula in `Strategy_WAEPass`:
     - Long: `WAE_Bull = (MACD[1] - MACD[2]) * Sensitivity > Max(ExplosionLine, DeadZone)`.
     - Short: `WAE_Bear = (MACD[2] - MACD[1]) * Sensitivity > Max(ExplosionLine, DeadZone)`.

5. **GMT Rollover Blackout & Capital Preservation Contract**:
   - Updated rollover blackout in `Strategy_NoTradeFilter` to use `QM_BrokerToUTC(TimeCurrent())` to evaluate true 23:55–00:05 GMT.
   - Added daily realized-loss circuit breaker check (2.0%) in `Strategy_NoTradeFilter`.
   - Initialized `QM_KillSwitchInit(qm_ea_id, QM_FrameworkMagic(), 2.5, 5.0, 1.0)` in `OnInit()` for approved 2.5% daily hard stop and 5.0% total DD limit.

6. **OnTick Ordering**:
   - Verified that position management (`Strategy_ManageOpenPosition`) and discretionary exits (`Strategy_ExitSignal`) execute before entry filters (`Strategy_NoTradeFilter` and news gate) so protective actions remain active.

## Verification Checklist

- **SPEC Validation**: `validate_spec_doc.py` -> PASS.
- **Magic Registry**: Verified 5 collision-free rows in `framework/registry/magic_numbers.csv` and regenerated resolver via `update_magic_resolver.py`.
- **Setfiles**: Generated canonical backtest setfiles for all 5 portable DWX basket symbols with `RISK_FIXED=1000` and `RISK_PERCENT=0`.
- **Build Guardrails**: `validate_build_guardrails.py` -> PASS (`max_news_stale_hours: 336`, 0 findings).
- **Compilation/Smoke**: Direct ad-hoc compilation stopped safely with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` due to active live factory `terminal64.exe` processes per governance rules.

## Disposition

Remediation complete. Submitted to REVIEW for mandatory Codex review.

# rework-12943 — QM5_12943 review repair evidence

Date: 2026-08-24 (ticket-required evidence filename date: 2026-08-23)

- Router task: `a21615ea-2d27-4bd9-9e72-c217d3e5de72`
- Source build task: `e3a2083b-eeb7-40e0-b865-0cc7d001997e`
- EA: `QM5_12943_robopip-hlhb-trend-catcher-h1`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12943_robopip-hlhb-trend-catcher-h1.md`
- Review artifact: `C:/QM/repo/artifacts/reviews/a21615ea-2d27-4bd9-9e72-c217d3e5de72.json`

## What changed

1. The time stop still counts completed H1 bars with the perf-allowed
   `iBarShift` call, but it no longer closes a losing position unconditionally.
   At or after the configured bar count, the EA compares the executable bid
   (long) or ask (short) with `POSITION_PRICE_OPEN` and closes only at or beyond
   break-even: `framework/EAs/QM5_12943_robopip-hlhb-trend-catcher-h1/QM5_12943_robopip-hlhb-trend-catcher-h1.mq5:166-190`.
2. The strategy SPEC now states the executable break-even rule and records the
   repair: `framework/EAs/QM5_12943_robopip-hlhb-trend-catcher-h1/SPEC.md:18`
   and `:121`.
3. Added four focused pytest regressions covering source hardening, H1 ATR x24,
   closed-H1-bar/break-even semantics, framework/MAE/magic/performance
   conformity, every declared input's executable use-site, fixed-risk setfiles,
   and the four-symbol delivered universe:
   `tools/strategy_farm/tests/test_qm5_12943_rework_static.py:37`, `:43`, `:62`,
   and `:93`.
4. Recompiled the EA from the repaired source against the repository's current
   `framework/include/QM` tree. The tracked EX5 changed from reviewed SHA-256
   `e8566960b6f6e3ad3af0238fdf7868855e17a6aa51033b85886785b6b5470acb`
   to `95ba06400a66dfa39e31dd09855beb3f4c64f8ee4d2573d5f6476c63234155b2`.

The prior retry's already-correct mechanisms were retained and reverified:

- H1 ATR(14) x24 is at the MQ5 `:107`; there is no native D1 ATR substitute.
- `QM_FrameworkTrackOpenPositionMae()` precedes all OnTick guards at MQ5 `:267`.
- Magic resolves through `QM_FrameworkMagic()` (MQ5 `:90`, `:152`, `:197`,
  `:281`); no manual magic formula exists.
- The set directory contains only EURUSD.DWX, GBPUSD.DWX, USDJPY.DWX, and
  XAUUSD.DWX, all H1 backtest sets with `RISK_FIXED=1000` and
  `RISK_PERCENT=0`.

No setfile, gate threshold/criterion, verdict row, backtest queue, factory
state, AutoTrading setting, T_Live path, EA-ID registry, magic allocation, or
resolver file was changed.

## Compile evidence

The normal scoped command was attempted first:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 \
  -EAPath framework/EAs/QM5_12943_robopip-hlhb-trend-catcher-h1/QM5_12943_robopip-hlhb-trend-catcher-h1.mq5 -Strict
compile_one.result=FAIL
compile_one.reason_class=INCLUDE_MIRROR_REFUSED
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
```

The factory was not toggled and no compile/backtest work was enqueued. Instead,
an isolated portable MetaEditor tree was populated with the current repository
framework includes and the source was copied there. Before accepting the EX5,
the copied source SHA-256 was required to equal the repository source SHA-256.

```text
isolated compile log:
D:/QM/strategy_farm/temp/rework-12943-slot6-20260824T074951Z/compile.log
Result: 0 errors, 0 warnings, 5929 ms elapsed, cpu='X64 Regular'
source SHA-256: 72464662d18a78e25c40c3c332e0afa85045f3a8f56a0fbdd916dee05b9496d9
EX5 SHA-256:   95ba06400a66dfa39e31dd09855beb3f4c64f8ee4d2573d5f6476c63234155b2
EX5 bytes:     398294
```

## Test and validation output

Focused EA tests:

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_12943_rework_static.py
....                                                                     [100%]
4 passed in 0.65s
```

Touched hardening module plus focused EA tests:

```text
python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py \
  tools/strategy_farm/tests/test_qm5_12943_rework_static.py
1 failed, 32 passed in 115.90s
```

The single failure is outside this ticket: the existing
`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven
`EA_INDICATOR_BUFFER_UNBOUNDED` findings in `QM5_41134_wti-mdaily-iqrmean-mom`.
No QM5_12943 assertion failed.

Other scoped validators:

```text
python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_12943_robopip-hlhb-trend-catcher-h1
PASS — 5 files, 0 findings, news stale ceiling 336 hours

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_12943_robopip-hlhb-trend-catcher-h1 --json --fail-on-leak
SINGLE_SYMBOL_OK — 0 violations

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_12943_robopip-hlhb-trend-catcher-h1
PASS — 1 PASS, 0 FAIL

python framework/scripts/skill_build_ea_guard.py --ea-id 12943 --ea-label QM5_12943_robopip-hlhb-trend-catcher-h1
status=ok; ea_registry_row=true; magic_registry_rows=true; ea_dir_exists=true
```

Every one of the 27 declared inputs was enumerated and searched with `rg`; each
has at least one executable use-site beyond its declaration. Source-only
`build_gate_hardening.analyze_file` returns zero failures and zero warnings.

## Known validation conflict (not concealed)

The required scoped build-check command was also attempted with `-SkipCompile`,
but the live-factory interlock refuses even read/static build-check execution
while terminal64 processes are active:

```text
framework/scripts/build_check.ps1 -EALabel QM5_12943_robopip-hlhb-trend-catcher-h1 -Strict -SkipCompile
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED
```

The standalone full EA hardening command executes and reports exactly nine
`EA_SYMBOL_NOT_IN_CARD_UNIVERSE` failures. All nine origins are pre-existing
active rows in `framework/registry/magic_numbers.csv:16382-16386` and
`:16391-16394`; none originates from a setfile or MQ5 symbol reference. The
four delivered setfile symbols map to the existing approved slots XAUUSD=5,
EURUSD=6, GBPUSD=7, USDJPY=8. The ticket explicitly says never to alter existing
magic allocations, so these nine rows and the generated resolver were left
byte-unchanged. Changing D17 itself would change a gate criterion and was also
out of scope.

No smoke/backtest was run or enqueued because the ticket explicitly forbids
backtest enqueue/execution. Therefore this evidence does not claim a smoke
verdict.

## Risks and open review point

- EA source, approved delivered universe, focused tests, guardrails, spec,
  symbol scope, input wiring, and binary compilation are clean.
- Full `build_check` cannot be claimed PASS under the simultaneous live-factory
  and immutable-existing-magic constraints. Review must adjudicate the nine
  surplus allocations or provide separate registry authority; the EA build
  does not use them.
- The break-even condition is price-based at the executable bid/ask. Broker
  slippage and commission can still make realized account P/L slightly below
  zero, which is unavoidable for a market close and is not specified otherwise
  by the card.

## Rollback

Revert the ticket commit with `git revert <rework-12943-commit>`. This restores
the prior MQ5, SPEC, and EX5 and removes the focused test and this evidence
file. No database, registry, queue, factory, terminal, or live rollback is
required because none of those surfaces was changed.

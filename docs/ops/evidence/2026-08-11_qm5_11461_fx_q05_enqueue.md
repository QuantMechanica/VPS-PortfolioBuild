# QM5_11461 EURUSD FX Q05 Enqueue

Date: 2026-08-11

Branch: `agents/board-advisor`

Status: one Q05 Stress MEDIUM work item pending; no dispatch performed

## Outcome

The frozen 66-pair FX cointegration frontier was reconciled before mutation.
No unbuilt relationship remains, and neither preferred anchor has a Q02 setup
blocker: `QM5_12532` retains logical-basket Q02 PASS followed by Q04 PASS and
Q05 FAIL, while `QM5_12533` retains logical-basket Q02 PASS followed by Q04
FAIL. Creating another scan-derived card or basket would duplicate existing
mechanization.

The mission fallback therefore advanced one existing low-frequency FX sleeve.
`QM5_11461_goodwin-j-outside-bar-daily-reversion-d1` on `EURUSD.DWX` had a
fresh Q04 `PASS_LOWFREQ` and no Q05 row. The supported cascade path created
exactly one Q05 successor:

| Field | Value |
|---|---|
| Q04 predecessor | `657bc4fe-d07c-474e-9f93-71389abc8ce7` |
| Q04 verdict | `PASS_LOWFREQ` |
| Q05 work item | `f50395de-a43f-4358-9468-a4e10780c5e1` |
| Q05 state at verification | `pending`, unclaimed, attempt 0 |
| Symbol / timeframe | `EURUSD.DWX` / D1 |
| Created at | `2026-08-11T08:10:36Z` |

No tester, dispatch tick, terminal reservation, or pipeline runner was
launched by this handoff.

## Why this fallback is eligible

The approved card is a structural outside-bar exhaustion fade. It uses only
completed D1 high, low, and close comparisons, skips Friday setups, applies a
fixed 200-pip stop, and closes after one held D1 bar. It declares about eight
trades per year per symbol. There are no indicators, learned models, adaptive
parameters, grids, martingale, or pyramiding.

Source lineage is Jarrod Goodwin, *Beat the Markets Strategy Guidebook*, with
the underlying outside-bar concept attributed to Larry Williams,
*Long-Term Secrets to Short-Term Trading* (1999). The existing card is
OWNER-approved with R2/R3/R4 PASS; this operation did not create, reinterpret,
or promote a new research claim.

The other obvious review-approved D1 FX rows were not used. `QM5_11561`
declares 200 trades/year and `QM5_11570` declares 120; both are explicitly in
the governed `requeue_excluded_eas.txt` high-cadence cohort. Removing or
bypassing that exclusion would violate the mission's low-frequency boundary.

## Q04 evidence and immutable identity

The Q04 aggregate is:

`D:/QM/reports/pipeline/QM5_11461/Q04/EURUSD.DWX__657bc4fe-d07c-474e-9f93-71389abc8ce7/aggregate.json`

All three real-tick OOS folds completed without setup invalidity:

| Fold | OOS year | Trades | Net PF | Result |
|---|---:|---:|---:|---|
| F1 | 2023 | 10 | 1.0808 | complete |
| F2 | 2024 | 9 | 2.2538 | complete |
| F3 | 2025 | 7 | 0.6117 | strategy PF below fold floor |

The governed low-frequency pool combined 26 trades across all three active
years at net PF 1.226, yielding `PASS_LOWFREQ`. The current repository files
match the hashes bound into every Q04 fold:

| Artifact | SHA-256 |
|---|---|
| EX5 | `1c19c94d31ce5510ccef7f0b15873f0bf5e8029772a2e83e9edc7b2b0a888403` |
| EURUSD backtest setfile | `6ec71f302db22964813b4d2849b97caf0366ff9a7ae4b685c4e187ab3da6cdb7` |
| Q04 aggregate | `ef20955a01d6971eaf858868ac30002a2917ca6fb14829b2679ec39367152935` |

The backtest preset remains `environment=backtest`, `RISK_FIXED=1000`,
`RISK_PERCENT=0`, `PORTFOLIO_WEIGHT=1`, slot 0, and D1. The EA-ID row and all
five declared magic rows are active; the EURUSD row is magic `114610000`.

## Guarded enqueue

Before mutation, the online farm DB and a new backup both returned
`PRAGMA quick_check = ok`:

`D:/QM/strategy_farm/state/backups/farm_state_pre_qm5_11461_q05_20260811T081017Z.sqlite`

The mandatory immediate pre-enqueue sample at `2026-08-11T08:10:31Z` found
three running factory terminals: `T3`, `T8`, and `T10`. This was below the
binding seven-terminal ceiling. Non-factory terminals, including `T_Live`,
were excluded from the factory count and were not controlled.

The exact supported command was:

```powershell
python tools/strategy_farm/farmctl.py --root D:/QM/strategy_farm enqueue-backtest --ea QM5_11461 --phase Q05 --from-work-item-id 657bc4fe-d07c-474e-9f93-71389abc8ce7
```

It reported one created row, zero requeues, zero skips, and no missing-setfile
or history skips. A read-only post-check found exactly one Q05 row for this EA
and `PRAGMA quick_check = ok`. Its payload is pinned to the Q04 predecessor,
`EURUSD.DWX` host, D1, and a 120-minute worker timeout. Normal workers own any
later claim and execution.

Machine-readable evidence:
`artifacts/qm5_11461_fx_q05_enqueue_20260811T081036Z_board_advisor.json`.

## Safety boundary

- No Strategy Card, EA source, EX5, registry, magic resolver, or setfile was
  changed.
- No portfolio-admission, portfolio KPI, or Q08-contribution path was touched.
- No `T_Live` manifest, deploy artifact, live setfile, or AutoTrading state was
  touched.
- No MT5 process was launched, stopped, reserved, reaped, or controlled.
- Existing unrelated dirty-worktree files were left untouched.

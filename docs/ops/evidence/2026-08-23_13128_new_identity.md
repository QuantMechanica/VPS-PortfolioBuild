# QM5_13128 source drift → QM5_41129 new identity (2026-08-23)

## Authority

OWNER 2026-08-23 (Chat), Question 2 = option (b): `QM5_13128` `.mq5` source drifted from the
compiled `.ex5` binary after commit `4112f5b07` edited the source post-build. Decision: fork the
edited source into a fresh EA identity starting clean at Q02; the old `QM5_13128` identity's source
must be reconciled back to the state that actually produced its live binary.

Task: `BUILD-13128-NEW-IDENTITY` (router task `ee266d6d-8a67-446f-9559-80332896c299`).

## 1. Drift diagnosis

- `QM5_13128` `.ex5` was last (re)built by commit `e68499506` (`2026-07-24T23:28:53+02:00`, "build:
  pump auto-commit 18 factory artifact path(s)").
- `QM5_13128` `.mq5` was last touched **before** that build by commit `027f45752`
  (`2026-07-15T23:12:59+02:00`), then edited again **after** the build by commit `4112f5b07`
  (`2026-08-22T16:15:14+02:00`, "dev: reconcile QM5_13128 event contract").
- Working tree at cycle start still carried the post-`4112f5b07` (drifted) source
  (`sha256=4e6e18c1967ae802aa31190b7ca75329eb451ddee88706f8f1dd546506172d25`), which does **not**
  match the binary (`QM5_13128_pre-fomc-drift-ndx.ex5`
  `sha256=59b9d1657fb04a9f33a030d420da76a1cae92c4223f4404842a53feed1848370`).
- The `4112f5b07` diff vs its parent added: diagnostic `QM_LogEvent` calls in the entry gate,
  a clarifying comment on the news-exemption contract, a `perf-allowed` comment, and — the one
  behavior-relevant change — removed the `QM_FrameworkTrackOpenPositionMae()` call from
  `Strategy_ManageOpenPosition()`. This is a genuine logic/diagnostics delta versus the binary
  in the field, so per OWNER decision it becomes a new identity rather than a silent rebuild
  under the old one.

## 2. New identity: QM5_41129_pre-fomc-drift-ndx-v2

| Field | Value |
|---|---|
| ea_id | 41129 |
| slug | pre-fomc-drift-ndx-v2 |
| source name | nyfed-sr512-pre-fomc-drift-v2 |
| registry row | `framework/registry/ea_id_registry.csv:4630` — `41129,pre-fomc-drift-ndx-v2,nyfed-sr512-pre-fomc-drift-v2,active,claude,2026-08-23,,,` |
| magic row | `framework/registry/magic_numbers.csv:17862` — `41129,pre-fomc-drift-ndx-v2,0,NDX.DWX,411290000,2026-08-23,claude,active` |
| `.mq5` sha256 | `00d3944a95af96aa68c4a48b43590951a306dcf424e9c6e027b2a548e033ad00` (= the post-`4112f5b07` source, unmodified content-wise, forked into the new dir) |
| SPEC.md | `framework/EAs/QM5_41129_pre-fomc-drift-ndx-v2/SPEC.md` — diff rationale in §1/§8 |
| stub setfile | `framework/EAs/QM5_41129_pre-fomc-drift-ndx-v2/sets/QM5_41129_pre-fomc-drift-ndx-v2_NDX.DWX_H1_backtest.set`, generated via `framework/scripts/gen_setfile.ps1` (`build_hash: pending`, sha256 `96ca9a2e8dfb9d69b781486f3e1138fc24a42670139498d1704b081f28621ab2`), needed only to resolve `infer_timeframe` (no approved card exists for this manual OWNER fork, so the `existing_setfiles` fast path is used, matching the `card_defaults_source=ea_input_defaults` convention used by other pre-compile stubs, e.g. `QM5_1009`). `build_check -EALabel` will regenerate/finalize it once the EX5 exists. |
| Parameters | identical to `QM5_13128` defaults — no parameter search performed for the fork (see SPEC.md §2). |

### COMPILE_EA queue

`farmctl enqueue-compile QM5_41129_pre-fomc-drift-ndx-v2` → **enqueued**, `work_item_id
7c31701a-5b08-4698-ad3c-d0146ba137c4`, `status=pending`, `activation_hold_code
COMPILE_EA_WORKER_ROLLOUT_PENDING` (governed COMPILE_EA queue; factory is running, no direct
compile performed). This satisfies the acceptance bar of "EX5 gebaut oder pending-Zeile mit ID im
Report" for this cycle.

### Q02 NDX.DWX — NOT yet enqueued (blocked on compile)

`farmctl seed-fresh-q02` binds to the **exact current binary**; `QM5_41129` has no `.ex5` yet
(compile is queued, not run). Enqueuing Q02 now would either fail the exact-binary check or bind
to nothing. This step is deliberately left for the next cycle **after** the COMPILE_EA worker
produces the `.ex5` — do not force it ahead of the binary.

## 3. Old identity: QM5_13128 reconciled to its EX5-producing source

`framework/EAs/QM5_13128_pre-fomc-drift-ndx/QM5_13128_pre-fomc-drift-ndx.mq5` restored to the
content of commit `4112f5b07^` = `027f45752` (the last version that predates the drift and matches
what was live when `e68499506` built the current `.ex5`):

| | sha256 |
|---|---|
| `.ex5` (untouched) | `59b9d1657fb04a9f33a030d420da76a1cae92c4223f4404842a53feed1848370` |
| `.mq5` before this cycle (drifted, = `4112f5b07`) | `4e6e18c1967ae802aa31190b7ca75329eb451ddee88706f8f1dd546506172d25` |
| `.mq5` after this cycle (reverted, = `027f45752`) | `e2bd93a2a66700763997af1e8a9713e40ae76e31916b86d941ae08f495e3fb19` |

No change was made to the `.ex5` binary itself (hard constraint honored).

### Obsolete compile row

`work_items` row `3c893190-0297-4efb-b810-ad7f602ff63d` (`kind=compile`, `phase=COMPILE_EA`,
`ea_id=QM5_13128`, `status=pending`, `force_rebuild=true`, snapshot `mq5_sha256
=4e6e18c1967ae802aa31190b7ca75329eb451ddee88706f8f1dd546506172d25`) was enqueued 2026-08-22 against
the **drifted** source. That hash no longer matches the working tree after this cycle's revert. Per
OWNER instruction the row is **kept, not deleted** — flagged here as OBSOLETE: its snapshot
predates the source reconciliation in §3 and should not be actioned as-is. Any future rebuild of
`QM5_13128` must re-enqueue from the current (reconciled) source rather than resuming this row.

## 4. Remaining work (next cycle)

1. Wait for the COMPILE_EA worker to build `QM5_41129_pre-fomc-drift-ndx-v2.ex5` from work item
   `7c31701a-5b08-4698-ad3c-d0146ba137c4`.
2. `build_check -EALabel QM5_41129_pre-fomc-drift-ndx-v2` to finalize the setfile (`build_hash`).
3. `farmctl seed-fresh-q02` for `QM5_41129`/`NDX.DWX` from the new exact binary.
4. Leave `3c893190-0297-4efb-b810-ad7f602ff63d` untouched (documented obsolete above).

## Hard constraints honored

- No change to the old `QM5_13128` `.ex5`.
- No T_Live touch.
- No Factory_OFF, no direct compile (used the governed COMPILE_EA queue).
- Pathspec commit (this doc + the touched EA files + registry rows only).

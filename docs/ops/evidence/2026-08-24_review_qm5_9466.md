# QM5_9466 connors-r2-d1 — Claude review-lane close (review_ea)

Date: 2026-08-24 UTC
Router task: `bf0ead8d-bde8-4b4b-838f-cace0a26e5c3` (review_ea, assigned codex, source_agent gemini/agy)
Source build task: `ffdbf22e-3ec4-4027-88d5-5a6e4ba6c1c7` (`BUILD_READY_REVIEW`)
Reviewed artifact: `framework/EAs/QM5_9466_connors-r2-d1/` (.mq5, SPEC.md, sets/, build_identity.json)
Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9466_connors-r2-d1.md`
Source MQ5 SHA-256 (unchanged since build): `3537d969bb5c629d93eb18b91640f5227b44cd2290d92a564803b21dc47cd6d4`

**Verdict: RECYCLE** — named actionable defects; do not promote to Q02.

Independent review (read-only) confirms the four findings previously raised in
`docs/ops/evidence/2026-08-23_qm5_9466_gemini_code_review.md`. Source is byte-identical
since that review (SHA match), so none were addressed.

## Findings (file:line)

1. **HIGH — card exits/management gated behind entry eligibility.**
   `OnTick()` returns on `Strategy_NoTradeFilter()`
   (`QM5_9466_connors-r2-d1.mq5:188`) *before* `Strategy_ManageOpenPosition()`
   (`:191`) and `Strategy_ExitSignal()` (`:193`). `Strategy_NoTradeFilter()`
   (`:52-64`) includes the spread gate `(ask-bid) > strategy_spread_max_atr*ATR`
   (`:63`) plus warmup/quote checks. A wide-spread day or invalid quote therefore
   suppresses the card's RSI(2)>75 exit and the 10-D1-bar time-stop on an
   already-open position. The spread gate must apply to entry construction only;
   Friday close (`:185`, correctly ahead of the filter), management, and exits
   must stay reachable.

2. **HIGH — D1 execution contract undeclared.** `OnInit()` (`:158-166`) returns
   success straight after `QM_FrameworkInit()` and never calls
   `QM_FrameworkDeclareExecutionContract(PERIOD_D1, ...)` (defined
   `framework/include/QM/QM_Common.mqh:474`, a fail-closed TF guard emitting
   `EA_INPUT_TIMEFRAME_MISMATCH`). Entry admission uses the no-arg
   `QM_IsNewBar()` (`:217`), which follows the attached chart timeframe while
   every indicator is pinned to `PERIOD_D1`. A non-D1 attachment silently
   evaluates the D1 sequence at wrong-period boundaries instead of failing
   closed. 40+ sibling EAs declare the contract; this D1 EA should too.

3. **MEDIUM/HIGH — delivered universe exceeds the approved card port.** The card
   scopes this to index proxies (SP500.DWX backtest; NDX.DWX/WS30.DWX live —
   card R3 and Symbol section). The package + Gemini SPEC ship 13 symbols
   including seven FX majors and XAUUSD.DWX with no card revision authorizing a
   strategy-universe change. Registry allocation confers identity, not scope.
   Restrict to the approved index port or obtain an OWNER-approved card revision.

4. **MEDIUM — durable SPEC corrupted.** `SPEC.md` carries control bytes
   `0x1B` @offset 2912 and `0x07` @offset 3165 (verified), corrupting the
   source_id (line 83: `f14a5d7-...`, leading `e` dropped) and card path
   (line 86: `rtifacts/...`). Risk row (line 94) reads `,000 per trade` instead
   of `$1,000`. `validate_spec_doc.py` still reports structural PASS, so its
   result is not proof of clean text. Regenerate from literal text.

## Not a defect

- **No dead inputs.** All nine `strategy_*` inputs have use-sites beyond
  declaration: `strategy_rsi_period`/`sma_period`/`day1_max`/`exit_thresh` in the
  RSI/SMA signal (`:82-104`, `:141-144`), `atr_period`+`sl_atr_mult` in the ATR
  spread gate and `QM_StopATR` (`:60-63`, `:98`), `time_stop_bars` in management
  (`:129`), `spread_max_atr` (`:63`), `warmup_bars` (`:54`, `:69`, `:138`).
- Framework chain intact: `#include <QM/QM_Common.mqh>`, MAE hook
  `QM_FrameworkTrackOpenPositionMae()` at OnTick head, kill-switch, news gate
  (`QM_NewsAllowsTrade2`), Friday close, `ZeroMemory(req)`, `OnTester` objective.
- Risk mode: setfiles `RISK_FIXED=1000` / `RISK_PERCENT=0` (backtest-correct).
- Magic identity: 13 active rows at slots 0-12, resolver entries present,
  `magic = ea_id*10000+slot` (per build evidence; no collisions reported).
- EX5/MQ5 SHA match `build_identity.json`; `build_check_passed: true`.

These establish artifact identity and baseline hardening only. Findings 1-4 are
independent, actionable defects blocking Q02 promotion.

## Disposition

Read-only review. No source, binary, registry, setfile, verdict, or trade stream
changed. T_Live/AutoTrading untouched. Task closed RECYCLE for a corrected
rebuild + fresh review.

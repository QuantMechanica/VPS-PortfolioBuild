# Public strategy archive v3.1 — REVIEW

Task: `a847f01a-74ee-4878-9b9e-30779bcbdaa1`. Delivered in the canonical checkout on `agents/board-advisor`. Publication and deployment remain pending OWNER/CEO review.

The additive producer emits **3,338 strategy families from 3,400 card revisions**. It merges repeated EA identities, explicit parent/recovery lineage, and matching source plus revision-normalized slugs. It does not merge different sources merely because they share a mechanism name. Revision hashes and recorded dates remain a sub-list; the family identity and URL no longer depend on a changing card-content hash. The private [identity audit](2026-09-05_archive_v31_dryrun/audit.json) makes every grouping reviewable.

Every entry has a deterministic English mechanism name and tagline in [the naming ledger](../../../public-data/naming/strategy_names.v1.json). Names come from SPEC section 1 and the card headline, lead/summary and concepts through a bounded editorial vocabulary. They carry `OWNER_CEO_REVIEW_REQUIRED`, including the [30-entry sample](2026-09-05_archive_v31_dryrun/sample_30.md) reviewed during implementation. All 3,338 entries matched mechanism vocabulary; none retain the generic description-gap fallback. Broad mechanism names may repeat; stable family URLs distinguish them. Editorial refinement remains review work, not an assertion that these names are approved for publication.

The ledger records full SPEC SHA-256 values and card revision hashes. **676 families have no readable SPEC**, recorded as an empty SPEC hash list with their card provenance retained. No SPEC hash was invented. The producer preserves a reviewed ledger entry while its provenance is unchanged and proposes a new pending entry when that provenance changes.

Timeframes resolve from governed set filenames/labelled metadata, explicit work-item timeframe inputs and card declarations. Multiple declared frames render as `multi`. There are **zero UNKNOWN market cells for families with set files**. Thirteen untested families have parameter-only legacy sets without any timeframe declaration; no market/timeframe assertion is synthesized for these unscoped sets. Eighty-eight market cells in families without sets still have unavailable timeframe evidence, explicitly `UNKNOWN`. This is a documented input gap rather than a guessed default. Historical cells without their own timeframe may use the family's declared set/card timeframe; the archive explicitly labels its evidence as historical, not proof of current exact-source release binding.

Phase labels follow the actual current website `C:/QM/deploy/qm-ops-refresh/Website/pipeline.html` and active v4 contract: **Validation Q00–Q08**, **Optimization and requalification Q09–Q14**, **Book build and live readiness Q15–Q17**. The task's older numeric mapping is superseded by those concrete surfaces. Each concluded gate has its own public purpose, PASS and FAIL symbol lists, and market/timeframe results. Mixed results remain mixed. Historical later evidence is visible but cannot jump missing prerequisites in terminal-status classification.

The resulting statuses are 3,307 research-frontier families, 22 in optimization, six book candidates and three explicitly retired. A historical burn-in PASS is insufficient to assert current live operation, so this producer cannot emit `live` without an independently implemented exact operational receipt. No current-live claim was inferred from a backtest or historical gate label.

The v2/v3 producers, schemas and publication paths are retained. The v3.1 CLI writes previews only and refuses the publication directory. Its schema and runtime validator reject unknown fields at every public boundary, private locators, source links/code, parameters and numeric performance. The naming ledger contains path-free provenance hashes; the internal family audit remains under evidence, outside the public payload.

Verification: **107 focused tests passed** across v3.1 and existing archive contracts. Tests cover revision merging, unrelated source separation, deterministic ledger reuse, real/multi timeframes, unscoped legacy sets, versioned phase mapping, isolated later/live results, mixed symbol outcomes and forbidden-field/prose fixtures. The complete [dry-run archive](2026-09-05_archive_v31_dryrun/strategy-archive-v31.json) passed the runtime whitelist and PowerShell `Test-Json` against the closed v3.1 schema. Canonical CLI import was also checked from the stale orchestration worktree. [Verification and hashes](2026-09-05_archive_v31_dryrun/verification.json) bind the result to the producer and schema. There were no failed-status work items carrying PASS-class verdicts in the captured DB; the final concluded-status guard therefore leaves the dry-run projection unchanged.

Reproduce a new preview with:

```powershell
python C:/QM/repo/tools/strategy_farm/website_archive_v31.py --ledger C:/QM/repo/public-data/naming/strategy_names.v1.json --out-dir C:/QM/repo/docs/ops/evidence/archive_v31_next_preview
```

The 16:15Z orchestration cycle resumed this unfinished delivery, repeated the 107 tests, validated the stored preview against both whitelist and JSON Schema, checked all four recorded artifact hashes, and read the 30-entry sample. The ledger has 187 distinct mechanism names across 3,338 families (3,151 repeated-name occurrences); brand-name differentiation remains an explicit OWNER/CEO editorial review item. [Cycle verification](2026-09-05_archive_v31_dryrun/cycle_verification_1615Z.json) records these checks.

This delivery supplies the review contract, ledger and evidence. It performs no gate execution, threshold changes, verdict overwrites, deployment or publication. Main integration remains exclusively a Claude/OWNER close-out.

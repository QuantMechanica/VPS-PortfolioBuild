# rework-1410 review repair evidence

Date executed: 2026-08-24 (Europe/Berlin)

Router task: `ee84a2b6-1d7a-4cbf-b7bd-393469e9a8be`

EA: `QM5_1410_bressert-dual-cycle-oscillator-h4`

## Work order and preflight

- Review work order: `docs/ops/evidence/2026-08-22_codex_review_qm5_1410.md`.
- Source build artifact: `D:/QM/strategy_farm/artifacts/builds/c1850502-ac54-4fc4-b397-5e54e87d1eb6.json`.
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1410_bressert-dual-cycle-oscillator-h4.md`; front matter is `g0_status: APPROVED` and the card id/slug match the EA.
- `framework/registry/ea_id_registry.csv:293` is the active EA allocation.
- `framework/registry/magic_numbers.csv:17737` through `:17750` are the 14 active symbol-slot allocations. There was no registry gap, so neither registry nor `QM_MagicResolver.mqh` was changed.
- Router claim command succeeded with state `IN_PROGRESS` and verdict `rework burn-window slot 3`.

## What changed

### Card-faithful entry and lifecycle mechanics

- The exact high-impact-news entry blackout is now 480 minutes before and after the event (`framework/EAs/QM5_1410_bressert-dual-cycle-oscillator-h4/QM5_1410_bressert-dual-cycle-oscillator-h4.mq5:266`, framework-init window at `:606`). Position management and exits execute before the framework news-entry decision (`:627-649`).
- TP1 state is reconstructed once per position identifier from durable exit-deal history (`:387-427`) before any partial-close decision (`:464`). A failed history reconstruction blocks management, so a restart cannot blindly repeat the 50% close.
- The fixed broker TP was removed (`req.tp = 0.0` at `:329` and `:367`). The card's cap is now `favorable_gain - adverse_price_excursion >= 4 ATR` (`:483-484`), with the adverse excursion reconstructed conservatively from the bounded position lifetime after restart (`:428-451`).
- The six-bar reuse latch is committed only after `QM_TM_OpenPosition` accepts the request (`:654-662`), not while merely forming a signal.

### Framework and hardening conformity

- Direct user-EA ATR/SMA handles and `CopyBuffer` calls were replaced with pooled `QM_ATR`/`QM_SMA` readers (`:137`, `:276`, `:467`).
- `QM_FrameworkTrackOpenPositionMae()` now runs before every OnTick guard (`:621`).
- The entry request is aggregate-zero-initialized (`:654`) and both entry branches explicitly set `expiration_seconds = 0` (`:332`, `:370`).
- DSS numeric buffers use resize checks and explicit `ArraySize`/configured-size guards (`:146-253`). All bespoke `CopyRates` calls carry reviewed, bounded `// perf-allowed` reasons (`:285`, `:432`, `:554`).
- Magic remains resolver-backed through framework initialization and `QM_FrameworkMagic()` (`:73`, `:96`, `:503`, `:631`); risk remains `RISK_FIXED=1000/RISK_PERCENT=0` in backtest inputs and setfiles, with the live percent input still wired through `QM_FrameworkInit` (`:604`).
- `SPEC.md:22-27` and `:137` record the repaired lifecycle contract and revision.

### Unit regression coverage

`tools/strategy_farm/tests/test_qm5_1410_review_rework.py` adds five static contract tests:

- exact entry-only news window (`:36`),
- restart-idempotent TP1 reconstruction (`:56`),
- MAE-adjusted cap with no static TP (`:72`),
- framework/input/performance wiring (`:88`),
- dynamic DSS buffer bounds (`:117`).

The declared-input audit found every input has at least one non-declaration use-site. Counts ranged from 2 to 10; no input had a count below 2.

## Validation evidence

### PASS

```text
python framework/scripts/skill_build_ea_guard.py --ea-id 1410 --ea-label QM5_1410_bressert-dual-cycle-oscillator-h4
status=ok; ea_registry_row=true; magic_registry_rows=true; ea_dir_exists=true

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_1410_bressert-dual-cycle-oscillator-h4
PASS QM5_1410_bressert-dual-cycle-oscillator-h4; Summary: 1 PASS, 0 FAIL

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_1410_bressert-dual-cycle-oscillator-h4
verdict=PASS; files_checked=15; findings=[]; max_news_stale_hours=336

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_1410_bressert-dual-cycle-oscillator-h4
failures=[]; warnings=[]; D7=0; D9=0; D10=0; D15=0

python -m pytest tools/strategy_farm/tests/test_qm5_1410_review_rework.py -q
5 passed in 0.57s

python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py -q -k "not qm5_411xx_sources_have_no_unbounded_numeric_buffers"
28 passed, 1 deselected in 114.27s

git diff --check
PASS (no whitespace errors)
```

The unfiltered combined pytest run produced `33 passed, 1 failed`; the failure is the pre-existing cohort assertion `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`, which reports seven findings in other `QM5_411xx` EAs (first: `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`). No `QM5_1410` assertion failed.

### Compile/build interlock (not bypassed)

Both required scoped commands were attempted:

```text
powershell -File framework/scripts/build_check.ps1 -EALabel QM5_1410_bressert-dual-cycle-oscillator-h4
powershell -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_1410_bressert-dual-cycle-oscillator-h4/QM5_1410_bressert-dual-cycle-oscillator-h4.mq5 -Strict
```

Both failed closed before MetaEditor launch with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` / `INCLUDE_MIRROR_REFUSED`: 12 `terminal64` processes were live. No retry was attempted by the tooling. The compile receipt is `D:/QM/reports/compile/20260824_074635/summary.csv`; log path is `framework/build/compile/20260824_074635/QM5_1410_bressert-dual-cycle-oscillator-h4.compile.log`.

A read-only URI query of `D:/QM/strategy_farm/state/farm_state.sqlite` returned no `COMPILE_EA` work item for EA 1410. This ticket did not authorize creating one. Factory state was not toggled, no process was stopped, and no backtest/compile row was enqueued.

Current identities after the refused compile:

```text
MQ5 sha256  8ea2ec147e0c0a4677a8a8d78651420f7c9e8720bc6c4ce6a392eeabbcf6b74f
EX5 sha256  4b539331592b0a65e507bf61baf5b1343aa722d7c0983f743724759eaabb764f
```

The EX5 is therefore explicitly the pre-rework binary and is not claimed as validation evidence. The open operational risk is a governed recompile when an authorized `COMPILE_EA` claim exists or the normal compile interlock permits it.

## Safety and rollback

- No changes to `C:/QM/mt5/T_Live`, AutoTrading, factory state, gate criteria, verdict rows, queues, registries, or framework includes.
- Roll back the ticket atomically with `git revert <rework-1410 commit>`. This restores the EA source/SPEC and removes the regression test and this evidence note. The unchanged pre-rework EX5 needs no separate rollback.

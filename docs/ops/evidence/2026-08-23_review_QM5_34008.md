# Review — QM5_34008 multicurrency-basket-dispersion-hedger

- Router task: `f53fcf1d-e179-47c4-8571-40430b61aff1` (review_ea, assigned claude)
- Source task: `fa7ca587-77f8-4cea-b71b-7bb1b746b33d` (gemini/agy build_ea remediation)
- Source verdict claimed: "GEMINI_BUILD_REVIEW: QM5_34008 market-neutral package and exits implemented and verified"
- EA dir: `framework/EAs/QM5_34008_multicurrency-basket-dispersion-hedger/`
- Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_34008_multicurrency-basket-dispersion-hedger.md`
- Reviewer: Claude (review lane) 2026-08-23
- **Verdict: RECYCLE** (stale build artifact — actionable, recompile required)

## Dispositive finding — committed .ex5 is stale relative to remediated source

The Aug-23 remediation (commit `10f9d94e1`, "draft QM5_34008 remediation") rewrote the
EA substantially (git: 144 insertions / 101 deletions): include swapped
`Trade/Trade.mqh` -> `QM/QM_BasketOrder.mqh`, `RISK_PERCENT` 0.5->0.0, TP/SL params
replaced (`strategy_tp_rr_mult` -> `strategy_target_profit_pct` + `strategy_hard_stop_loss_pct`),
and the two-leg package entry / portfolio-PnL exit machinery added
(`OpenPackageCount`, `OpenPackagePnL`, `CloseAllPackagePositions`).

Hash evidence proves the binary was never rebuilt from the remediated source:
- Pre-remediation mq5 sha256 (`10f9d94e1~1`): `2d9561291bd10a468c40be0f6a184d4bffda7b6f2afe08d6a88184001d68796d`
- Current committed mq5 sha256: `541f9302440fb815314d61f2e47cb1d3823388dace1fc40cffa0ff824815dcb6`
- Committed `.ex5` sha256: `85962ade...661186`, mtime **2026-08-17 21:56** (pre-remediation)
- Remediation commit `10f9d94e1` (Aug 23 12:53) touched only the `.mq5` — NOT the `.ex5`, NOT `build_result.json`.

The .ex5 (Aug 17) was compiled from the superseded source `2d956129...`. The current
`.mq5` is `541f9302...`. The binary in inventory does not contain any of the remediated
package/exit logic.

`artifacts/qm5_34008_build_result.json` is internally inconsistent / stale: it pairs the
**remediated** `mq5_sha256=541f9302...` with the **pre-remediation** `ex5_sha256=85962ade...`
and reports `build_check_passed:true` / `compile_succeeded:true`. That compile evidence
does not correspond to the current source hash.

The remediation evidence doc
(`docs/ops/evidence/2026-08-23_fa7ca587_qm5_34008_build_ea_remediation.md`) confirms this:
its verification checklist covers SPEC validation, build guardrails and setfile discipline
but records **no compile step**; disposition reads "Draft code updated and verified."
Enqueuing Q02 now would test the stale Aug-17 binary and/or trip the source-binding /
build-date predicate as INVALID.

## Code review (remediated source `541f9302`) — otherwise clean

Reviewed against the card mechanism (7-pair USD basket, buy max-negative-delta / sell
max-positive-delta, +/-1.5% portfolio-PnL package exit):

- **Card faithfulness**: mechanism implemented in `Strategy_EntrySignal` (ROC over
  `strategy_lookback_hours`, cross-sectional mean, delta, sigma, threshold =
  `strategy_dispersion_dev` * sigma; argmin -> buy USD / argmax -> sell USD via
  `IsDirectUSDPair`). Package exit in `Strategy_ExitSignal` at balance*1.5% profit or loss.
  Orphan-leg flatten in `Strategy_ManageOpenPosition`. Faithful.
- **Dead-input check**: all six `strategy_*` inputs have use sites —
  `strategy_lookback_hours` (lb), `strategy_dispersion_dev` (threshold),
  `strategy_target_profit_pct` / `strategy_hard_stop_loss_pct` (ExitSignal),
  `strategy_atr_period` + `strategy_spread_atr_mult` (NoTradeFilter/SL). No dead inputs.
- **Framework conformity**: `QM_Common.mqh` + `QM_BasketOrder.mqh` include chain present;
  `QM_FrameworkInit` wired with full arg set; `QM_SymbolGuardInit` + `QM_BasketWarmupHistory`
  bind foreign basket reads; MAE hook (`QM_FrameworkTrackOpenPositionMae`), kill-switch,
  Friday close, news (`QM_NewsAllowsTrade2`/`QM_NewsAllowsTrade`) all in OnTick; MagicResolver
  via `QM_BasketOpenPosition(qm_ea_id, ...)`.
- **Magic mapping**: `req.symbol_slot = min_idx/max_idx` (basket index 0..6) matches the
  per-symbol slots in `magic_numbers.csv` (slot 0=EURUSD ... 6=USDJPY, magic 340080000-006).
  No host/slot conflation.
- **Risk**: `RISK_FIXED=1000`, `RISK_PERCENT=0` (backtest, HR4). Setfiles per remediation doc
  configured RISK_FIXED=1000 / RISK_PERCENT=0. No ML libs, no hardcoded commission/swap/DST.

Note (non-blocking): `Strategy_EntrySignal` opens both legs as a side effect and always
returns `false` (return value ignored at the OnTick call site) — the intended basket-EA
pattern, not a defect.

## Required actions before re-review / Q02 (RECYCLE)

1. Recompile the remediated source (`mq5` sha `541f9302...`) via the governed COMPILE_EA
   queue in an OFF window (recompile is ROT / not performed in review). Confirm the new
   `.ex5` corresponds to source `541f9302...`.
2. Regenerate `build_check` evidence and `artifacts/qm5_34008_build_result.json` so
   `compile_succeeded`/`build_check_passed` reflect COMPILE_OK for the CURRENT source hash
   (ex5_sha256 recorded from the rebuilt binary).
3. Re-submit for review, then Q02 enqueue.

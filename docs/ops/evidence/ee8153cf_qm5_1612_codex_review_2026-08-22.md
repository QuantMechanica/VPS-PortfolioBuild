# QM5_1612 Mandatory Codex Review — FAIL

- Review task: `ee8153cf-df11-47af-8157-aaf473ce8dbb`
- Gemini build task: `690cd9ab-da44-4dd5-8cb2-0212384dc3db`
- EA: `QM5_1612_aa-dsp-hplwma10`
- Source commit under review: `9fb5ad806`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1612_aa-dsp-hplwma10.md`
- Review artifact: `D:/QM/strategy_farm/artifacts/reviews/ee8153cf-df11-47af-8157-aaf473ce8dbb.json`
- Verdict: `FAIL`; the Gemini build must remain in REVIEW and must not advance to the pipeline.

## Identity and reproducibility

- Current MQ5 SHA-256: `b33a94addfe2c85ed3a8607aaeedd6a2c2305f1f045059857f5095eabefc824e`.
- Current EX5 SHA-256: `abeb9a5a07acb2d5250dcd65864bddfbc8f3c6754013fc7901334aab157e0d45` (392,028 bytes).
- These hashes match Gemini's `build_identity.json`, whose SHA-256 is `a5facd3c3f899dd5e5a07275ffc3f02fc37c2b1df25504138102e2dff253cd11`.
- The source, EX5, spec, and 13 set files are present in commit `9fb5ad806`; `build_identity.json` itself is untracked.
- The supplied identity JSON is not the governed build-result schema: it has `build_check_passed`, MQ5/EX5 identities, and set paths, but lacks `compile_succeeded`, smoke fields, EA directory, magic base, registered symbols, and blocked-reason semantics. No smoke report was supplied. Therefore review sections `build_result` and `smoke_sanity` cannot pass.

## Mechanical failures

### Card spread rule replaced

The approved card requires new entries to be skipped when the D1 spread exceeds 2.5 times the 20-day median spread (card line 58). The source instead defines `strategy_max_spread_points=0` (line 59) and only applies a fixed absolute-point cap when that input is positive (lines 130-134). At the shipped default the card's spread filter is disabled; even when enabled it is a different measure.

### Daily reevaluation is not new-bar gated

The card requires re-evaluation once per completed D1 bar. Entry is behind `QM_IsNewBar`, but `Strategy_ExitSignal` is called at line 306 and the new-bar gate is not reached until line 320. The 10-close filter is consequently recomputed on every tick and the package does not implement the stated daily cadence consistently.

### Approved symbol omitted from governed identity

The card's baseline explicitly includes `USOIL.DWX` (line 40). The active QM5_1612 registry has 13 rows but contains neither `USOIL.DWX` nor broker alias `XTIUSD.DWX`; the generated package has no oil set file. The remaining 13 magic rows have no collisions and are canonical, but that does not cure the missing approved-card symbol. The identity mismatch requires registry/card adjudication before a compliant build.

### Spec corruption

`SPEC.md` contains byte-level template corruption: line 4 starts the slug with BEL (`0x07`), line 5 starts the source ID with ESC (`0x1B`), and line 79 renders the required fixed-risk amount as `,000` rather than `$1,000`. The lightweight spec validator does not detect those control characters.

## Passing checks and advisory

- `validate_spec_doc.py`: structural PASS, despite the corruption above.
- `validate_build_guardrails.py`: PASS; 14 files, zero findings, 336-hour maximum news staleness.
- `validate_symbol_scope.py --fail-on-leak`: `SINGLE_SYMBOL_OK`.
- The current 13 registry magics are collision-free and their symbols exist in the DWX matrix.
- Static forbidden-code grep found no raw indicator handles, `CopyBuffer`, `OrderSend`, long sleep, ML, grid, or martingale pattern.
- News rejection occurs before management/exit. Because entries carry broker-side ATR stops, the review contract classifies this as an advisory rather than a hard forbidden-code failure; it can nevertheless delay the card's zero-cross exit.

## Required rework

1. Resolve the approved `USOIL.DWX` baseline against a governed broker symbol and magic row, or obtain an OWNER-approved card amendment before rebuilding.
2. Implement the specified 2.5 x 20-day median-spread entry guard; do not replace it with a disabled fixed-point input.
3. Put the closed-bar strategy exit evaluation behind the D1 new-bar gate while preserving protective framework management on every tick.
4. Regenerate a clean spec and strategy-bearing set files without control characters.
5. Produce the canonical build-result JSON and smoke summary, then submit a fresh Codex review. Do not advance the current Gemini build.

# rework-38002 review repair evidence

## Scope and disposition

- Router task: `0a687cb4-31fa-418f-8866-5d12c3c45b75` (`RECYCLE` claimed as `IN_PROGRESS`).
- EA: `QM5_38002_codetrading-macd-ema-trend-pullback` only.
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_38002_codetrading-macd-ema-trend-pullback.md` (`g0_status: APPROVED`).
- Registry preflight: `framework/registry/ea_id_registry.csv:4473`; active magic slots `0..2` at `framework/registry/magic_numbers.csv:17488-17490`. No registry files changed.
- No backtest was enqueued or deleted; factory state, gate criteria, verdict rows, AutoTrading, and `T_Live` were not changed.

## Changes

- Restored the card's five-closed-bar swing stop with a 2-pip buffer and bounded `CopyRates`/`ArraySize` handling: `framework/EAs/QM5_38002_codetrading-macd-ema-trend-pullback/QM5_38002_codetrading-macd-ema-trend-pullback.mq5:43`, `:131-149`, `:248-264`.
- Wired the 2.0% realized daily entry halt, 2.5% daily hard stop, 5.0% total drawdown stop, and 0.5% live percentage-risk cap: same MQ5 `:51-54`, `:186-212`, `:338-347`. Fixed-risk backtests remain `RISK_FIXED=1000`; the percentage cap override is applied only when `RISK_FIXED <= 0`.
- Converted the card's rollover window from broker time to UTC and wired the three-tick deviation through `QM_EntryConfigure`: same MQ5 `:199`, `:113-122`, `:329-336`.
- Seeded closed-bar state in `OnInit`, refreshes it before management on a new bar, and moved active management/exit ahead of all entry-only rollover/spread/news/open-position filters: same MQ5 `:349`, `:370-408`.
- Updated the card-faithful spec at `framework/EAs/QM5_38002_codetrading-macd-ema-trend-pullback/SPEC.md:17`, `:32-44`, `:110` and all three backtest setfiles (strategy bindings begin at line 31; loss controls at line 40).
- Added static regression coverage at `tools/strategy_farm/tests/test_qm5_38002_review_rework.py:27`, `:48`, `:65`. The third test asserts every declared `strategy_*` input has a source use-site and an exact binding in every setfile.

## Validation

PASS:

- `python -m pytest tools/strategy_farm/tests/test_qm5_38002_review_rework.py -q` -> `3 passed in 0.68s`.
- `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_38002_codetrading-macd-ema-trend-pullback` -> `failures: []`, `warnings: []`, D2/D4/D5/D7/D10/D11 all clean.
- `python framework/scripts/skill_build_ea_guard.py --ea-id 38002 --ea-label QM5_38002_codetrading-macd-ema-trend-pullback` -> `status: ok` (EA row, magic rows, directory true).
- `python framework/scripts/validate_spec_doc.py framework/EAs/QM5_38002_codetrading-macd-ema-trend-pullback` -> `1 PASS, 0 FAIL`.
- `python tools/strategy_farm/validate_build_guardrails.py <MQ5 + exact three setfiles>` -> all four files `PASS`, no findings.
- Input census -> all 20 declared `strategy_*` inputs have at least three source occurrences; the pytest setfile parity test passed.
- `git diff --check` -> no whitespace errors (only normal LF-to-CRLF worktree notices).

Related-suite baseline:

- `python -m pytest tools/strategy_farm/tests/test_qm5_38002_review_rework.py tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_build_guardrails.py -q` -> `1 failed, 51 passed in 115.68s`.
- The sole failure is outside this ticket: `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven existing findings beginning at `framework/EAs/QM5_41134_wti-mdaily-iqrmean-mom/QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`. EA 38002 and every newly added test passed.

## Governed compile blocker

The required scoped command was attempted exactly:

`powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_38002_codetrading-macd-ema-trend-pullback/QM5_38002_codetrading-macd-ema-trend-pullback.mq5 -EALabel QM5_38002_codetrading-macd-ema-trend-pullback -Strict`

It failed closed before compilation with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because factory terminals were active. Evidence: `framework/build/compile/20260824_100347/QM5_38002_codetrading-macd-ema-trend-pullback.compile.log` and `D:/QM/reports/compile/20260824_100347/summary.csv`; reported compile counts are `errors=-1`, `warnings=-1`, reason class `INCLUDE_MIRROR_REFUSED`. A five-minute read-only monitor observed factory slots continuously active and ended with `safe_compile_window=NOT_OBSERVED`. The existing `.ex5` was not changed or represented as rebuilt.

The noncanonical governed compile enqueue was also correctly refused by `farmctl` before mutation; no compile or backtest work item was created. Completing the binary/build-hash binding requires a quiescent governed compile window or an authorized compile work item. The router task must not be represented as compile-complete until that occurs.

## Rollback

Revert the resulting narrow commit with `git revert <commit>`; this restores the prior MQ5, spec, and three setfiles and removes the regression test/evidence file. No database, registry, factory, backtest queue, or live-terminal rollback is required because none was changed by the repair.

# QM5_13003 Q02 Priority - 2026-07-03

Branch: `agents/board-advisor`

## Scope

Mission: grow the certified V5 book with new forex sleeves, prioritizing market-neutral FX
cointegration baskets. I did not touch `T_Live`, AutoTrading, portfolio admission/KPI/Q08
contribution code, or deploy manifests.

## Triage

- `QM5_12532` is not Q02-blocked: Q02 `PASS`, latest observed state Q05 `FAIL`.
- `QM5_12533` is not Q02-blocked: Q02 `PASS`, latest observed state Q04 `FAIL`.
- The original 66-pair scan note names only `QM5_12533` and `QM5_12532` as strict positive-hedge
  survivors. The later approved negative-hedge extension baskets are already built through
  `QM5_13003`.
- `QM5_13003` (`USDCAD~NZDUSD`) had exactly one pending logical basket Q02 row and no
  `priority_track` payload before this action.

## Queue Action

The farm was CPU-saturated (`active=5`, `pending=6143` precheck), so I did not launch MT5 and did
not insert a new Q02 row. I priority-marked the existing pending `QM5_13003` row in place:

| Field | Value |
|---|---|
| Work item | `48141c69-b04f-4ebc-a6fe-fd15bc30317e` |
| Phase | `Q02` |
| Logical symbol | `QM5_13003_USDCAD_NZDUSD_COINTEGRATION_D1` |
| Status after | `pending` |
| `priority_track` | `true` |
| DB backup | `D:/QM/strategy_farm/state/backups/farm_state_before_qm5_13003_q02_priority_20260703T211729Z.sqlite` |
| Event | `priority_track_set`, id `210283` |

Duplicate guard after mutation: exactly one total Q02 row and one open Q02 row for this
EA/logical-symbol pair.

## Verification

- `build_check.ps1 -EALabel QM5_13003_edgelab-usdcad-nzdusd-cointegration -SkipCompile` -> PASS,
  0 failures, 16 existing shared-framework advisory warnings.
- `python -m pytest tools/strategy_farm/tests/test_fx_basket_manifests.py -q` -> PASS, 9 tests.
- `python -m unittest framework.scripts.tests.test_mt5_queue_status` -> PASS, 2 tests.
- `python framework/scripts/mt5_queue_status.py --sqlite D:/QM/strategy_farm/state/farm_state.sqlite --limit 10`
  reports `QM5_13003` as queued rank 2 with `priority_track=true`, behind the already-prioritized
  `QM5_12969` Q02 row.

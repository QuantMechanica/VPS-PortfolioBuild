# QM5_9467 connors-crsi-pullback-d1 — Claude review-lane close

- Router task: `f5be58aa-c65f-4652-8fdb-6816c30957b0` (review_ea, source_agent `gemini`, backend `agy`)
- Source build task: `5de38382-e3f2-4179-b63b-6f60222bccc3` (BUILD_READY_REVIEW)
- Reviewed: `framework/EAs/QM5_9467_connors-crsi-pullback-d1/` (.mq5, SPEC.md, sets/, build_identity.json)
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9467_connors-crsi-pullback-d1.md` (g0_status APPROVED; **not mirrored into repo `artifacts/cards_approved/`**)
- Prior Codex review: `docs/ops/evidence/2026-08-23_qm5_9467_gemini_code_review.md` (REQUEST_CHANGES, 7 findings)
- Date: 2026-08-24 UTC

## Verdict: RECYCLE

Named, actionable, fixable defects remain — not clean for Q02. Independent verification
confirms the material findings of the prior Codex review; two are hard card-faithfulness /
correctness defects.

## Confirmed defects (file:line)

1. **Symbol universe exceeds approved card scope (card-faithfulness).**
   Card ports only `SP500.DWX` (backtest baseline) + `NDX.DWX`/`WS30.DWX` (live)
   — card lines 19-20, 75-79; `r3_reasoning` explicitly "stock-liquidity rules
   replaced with CFD spread filters." The delivered package (SPEC.md §3;
   build evidence magic rows 0-12) expands to **13 symbols** including 7 FX majors,
   `XAUUSD.DWX`, `GDAXI.DWX`, `UK100.DWX` — a mean-reversion index/stock strategy ported
   to FX/gold with **no card revision**. Registry allocation proves identity, not
   strategy-universe authorization.

2. **Exit and time-stop are gated behind the entry spread filter (correctness).**
   `OnTick()` returns on `Strategy_NoTradeFilter()`
   (`QM5_9467_connors-crsi-pullback-d1.mq5:337`) **before**
   `Strategy_ManageOpenPosition()` (:340) and `Strategy_ExitSignal()` (:342).
   `Strategy_NoTradeFilter()` (:182-196) rejects on wide spread
   (`(ask-bid) > spread_max_atr*ATR`). A widening spread therefore blocks the
   CRSI>80 exit and the 8-D1-bar time-stop of an already-open position. Risk-reducing
   management must be reachable independently of every new-entry filter.

3. **Pending-order protective stop bound to limit price, not fill (risk).**
   `Strategy_EntrySignal()` sets `req.sl = QM_StopATR(..., limit_price, ...)`
   for the buy-limit path (:250; market path :244) and there is no `OnTradeTransaction` re-bind
   (:389 only forwards to framework). A gap fill below the limit leaves the
   ATR stop distance and fixed-risk sizing off the actual fill; a deep gap can place
   the stop above the fill.

4. **D1 execution contract undeclared / new-bar clock mismatch.**
   `OnInit()` (:307-316, QM_FrameworkInit at :309) returns success after `QM_FrameworkInit` without calling
   `QM_FrameworkDeclareExecutionContract(PERIOD_D1, ...)` — the framework's fail-closed
   Card-v2 binding, documented "Call this in OnInit immediately after QM_FrameworkInit"
   (`framework/include/QM/QM_Common.mqh:469-474`). Entry admission uses no-arg
   `QM_IsNewBar()` (:366) which tracks `PERIOD_CURRENT` while strategy data is fixed to
   `PERIOD_D1` (latent if ever run off a D1 chart).

## Lower-severity (fix in same pass)

5. Per-tick `Strategy_ComputeCRSI(1)` inside `Strategy_ExitSignal()` runs before the
   new-bar gate on every tick even with no open position (:285-296, ComputeCRSI :113-175) —
   a 120-bar reconstruction each tick; tester-timeout risk on tick modelling.
6. `expiration_seconds = 86400` (:256) is wall-clock, not the next D1 calendar boundary
   (Fri/weekend/holiday/DST drift).
7. SPEC.md control-byte corruption (0x1B @3577, 0x07 @3846) and `$1,000` rendered `,000`
   in §7; `validate_spec_doc.py` still PASS.

## Checks that passed

- All 14 `strategy_*` inputs wired to ≥1 use-site — **no dead inputs**.
- Framework chain present: `QM_Common.mqh` include; `QM_FrameworkTrackOpenPositionMae()`
  MAE hook; `QM_KillSwitchCheck`; Friday-close; framework news gate
  (`QM_NewsAllowsTrade2`) wired in OnTick; `QM_FrameworkMagic()` (MagicResolver);
  `RISK_FIXED=1000 / RISK_PERCENT=0`; one-position-per-magic; no ML; no raw indicator
  handles / CopyBuffer / raw OrderSend; CRSI buffers bounded (rank_period+20).
- `build_identity.json` `build_check_passed:true`; mq5/ex5 SHA-256 self-consistent.

## Disposition

No source, binary, registry, setfile, work item, verdict, or trade stream changed.
T_Live/AutoTrading untouched. Task closed **RECYCLE**; corrected build requires a fresh
review before enqueue.

# rework-35005 — review repair and compile handoff evidence

**Router task:** `3281881e-4597-4243-9a2b-e8d7c4fa6360`

**EA:** `QM5_35005_sma-crossover-pullback-system`

**Status:** SOURCE REPAIR COMPLETE; COMPILE/RESUBMIT BLOCKED BY GOVERNED WORKER ROLLOUT
**Scope:** no backtest enqueue, no factory toggle, no T_Live write, no verdict-row overwrite, no registry allocation change.

## Work order

The router artifact `docs/ops/evidence/3281881e_qm5_35005_gemini_build_codex_review_2026-08-18.md` required committed build identity, UTC rollover handling, the card's 2.0% / 2.5% / 5.0% loss limits, and entry-only filters that do not suspend trailing management. The follow-up review `docs/ops/evidence/954f01d5_qm5_35005_claude_review_stale_ex5_2026-08-23.md` verified the source-level remediation and identified the remaining blocker: EX5 SHA-256 `28ef9a97341ab09666f4b8ac6a817bbdabe806c968fbc96279a0e1be0b2fbd59` predates the repaired source.

The approved card was read from `D:/QM/strategy_farm/artifacts/cards_approved/QM5_35005_sma-crossover-pullback-system.md`; it has `status: APPROVED` and `g0_status: APPROVED`. Existing registry rows were verified and not modified:

- `framework/registry/ea_id_registry.csv:4452` — ea_id 35005 / matching slug / active.
- `framework/registry/magic_numbers.csv:17414-17416` — slots 0/1/2 for EURUSD.DWX, GBPUSD.DWX, EURJPY.DWX with magics 350050000/1/2.

## Changes

- `framework/EAs/QM5_35005_sma-crossover-pullback-system/QM5_35005_sma-crossover-pullback-system.mq5:66` makes the approved GMT rollover interval explicitly inclusive through 00:05 after `QM_BrokerToUTC` conversion.
- `.mq5:72-82` implements the card's **realized** daily loss entry halt via the framework `QM_ChartUITodayPnL` closed-deal helper and broker-day starting balance; it no longer substitutes floating equity drawdown for realized P&L.
- `.mq5:157-184` removes both raw `iClose` fallbacks. Invalid market quotes now fail closed; indicator reads remain on pooled `QM_SMA`, `QM_Stoch_*`, and `QM_ATR` helpers.
- Existing framework wiring remains intact: card hard stops via `QM_KillSwitchInit` (`.mq5:238`), explicit MAE hook (`.mq5:255`), management before news/entry filters, backtest `RISK_FIXED=1000` / `RISK_PERCENT=0`, and magic resolution through `QM_FrameworkInit` / `QM_FrameworkMagic`.
- `tools/strategy_farm/compile_work_items.py:86-91,212-225` adds a fail-closed source-repair authority bound to exactly this router task and this EA label. It grants no rebuild permission to any other label or task.
- `tools/strategy_farm/tests/test_qm5_35005_review_rework.py:44-127` locks card mechanism/order, UTC and realized-loss behavior, all 17 strategy-input use sites, pooled-data/no-ML rules, framework/MAE/magic wiring, registry/setfile scope, and rejection of the known stale EX5 hash.
- `tools/strategy_farm/tests/test_compile_work_items.py:213-228` proves the new compile authority accepts only the exact task/EA pair and rejects wrong labels and task IDs.

Repaired MQ5 SHA-256: `4b9750309f6979507e6a2a1510031f706f8fb5bc47977eaf6f00164155630e30`.

## Validation evidence

### PASS

```text
python -m pytest -q tools/strategy_farm/tests/test_compile_work_items.py
..................                                                       [100%]
18 passed in 3.92s

python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py -k "not qm5_411xx_sources_have_no_unbounded_numeric_buffers"
............................                                             [100%]
28 passed, 1 deselected in 132.90s

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_35005_sma-crossover-pullback-system
files_scanned=1; failures=[]; warnings=[]
```

The deselected hardening test is an unrelated fleet-wide QM5_411xx corpus scan. The touched checker itself and all applicable per-EA predicates ran and passed.

### Expected fail-closed result until compile completes

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_35005_review_rework.py
2 passed, 1 failed
FAILED test_registry_setfiles_and_fresh_binary_identity
actual EX5 sha256 = 28ef9a97341ab09666f4b8ac6a817bbdabe806c968fbc96279a0e1be0b2fbd59
```

This is the review finding, intentionally made executable: the stale binary cannot pass the repair suite.

### Compile/build-check blocker

```text
python tools/strategy_farm/compile_ea.py --ea-label QM5_35005_sma-crossover-pullback-system --force --json --fail-on-error
verdict=COMPILE_FAILED
reason_class=INCLUDE_MIRROR_REFUSED
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
```

Durable receipt: `D:/QM/reports/compile/QM5_35005_sma-crossover-pullback-system/result.json` (timestamp `2026-08-24T11:35:55+00:00`). A scoped `build_check.ps1 -EALabel QM5_35005_sma-crossover-pullback-system -SkipCompile` was refused by the same live-factory guard before any unscoped scan or mutation.

The mandated governed successor was enqueued (not a backtest):

```text
COMPILE_EA work_item_id = 73d3e2a5-1743-40b5-b744-878543577bf2
status = pending
mq5_sha256 = 4b9750309f6979507e6a2a1510031f706f8fb5bc47977eaf6f00164155630e30
hold = COMPILE_EA_WORKER_ROLLOUT_PENDING (active=1, release_on_restart=1)
```

The hold is correct: resident workers execute from `C:/QM/repo`, where the source is still SHA `758b0021667e9354415331c20dbee6389934813db590e3f837c242f4bac5be90`. Releasing before this commit is deployed would compile/recheck the wrong source. Completing the release requires the governed reviewed-worker rollout/restart ceremony. This ticket explicitly forbids toggling the factory, so the hold was not released and the router task was not falsely resubmitted to REVIEW.

## Rollback

1. Revert the ticket commit to restore the prior MQ5 and remove the exact compile-authority/tests/evidence changes.
2. Keep the held `COMPILE_EA` row append-only. Do not delete it; after revert it remains safely blocked by its source-hash binding and active rollout hold.
3. No registry rollback is needed because `ea_id_registry.csv`, `magic_numbers.csv`, and `QM_MagicResolver.mqh` were read-only for this ticket.

## Required continuation

Deploy this commit to the canonical worker checkout through the governed release process, perform the authorized worker restart/hold release for work item `73d3e2a5-1743-40b5-b744-878543577bf2`, verify a new EX5 hash and scoped strict build-check PASS, rerun `test_qm5_35005_review_rework.py`, then resubmit router task `3281881e-4597-4243-9a2b-e8d7c4fa6360` to REVIEW with this evidence path. No Q02/backtest fanout is authorized by this handoff.

# QM5_38006 Codex review evidence — 2026-08-18

- Review task: `67e670f0-e79d-4b6b-a7b2-effd03a626f4`
- Source build task: `9b992eb5-9773-40ff-b4f3-ef03719e373e` (Gemini)
- EA: `QM5_38006_codetrading-doji-hammer-pivot-rejection`
- Disposition: `FAIL_CODE_REVIEW`; remain in `REVIEW`

## Verification

- Mechanical pre-screen: PASS (artifacts, freshness, three active magic rows, setfiles, fixed-risk mode, input wiring)
- Build guardrails: PASS, four files checked, no findings, news staleness ceiling 336 hours
- Fresh compile: PASS, 0 errors, 0 warnings
- Backtest sets: fixed risk and registry-aligned symbol slots
- Card-to-code review: FAIL

## Blocking findings

1. `Strategy_NoTradeFilter()` rejects when the magic has an open position (MQ5 line 127). `OnTick()` returns on that filter at line 305 before calling `Strategy_ManageOpenPosition()` at line 308. The approved +1R break-even management cannot execute while the position exists; later strategy exits would be unreachable for the same reason.
2. `OnTick()` applies the ATR spread filter before `AdvanceState_OnNewBar()` at line 335. On startup `g_last_atr` is zero, so the first bar can pass admission, populate ATR/signal state, and enter without rechecking the required current-spread ceiling.

## Required repair

- Separate entry admission from open-position management so management/exits remain reachable.
- Refresh closed-bar state before ATR-dependent admission and recheck current spread immediately before opening.
- Recompile and regenerate bound evidence; later Codex review remains mandatory.

No Q phase or pipeline verdict is inferred. No terminal, T_Live, or AutoTrading action occurred.

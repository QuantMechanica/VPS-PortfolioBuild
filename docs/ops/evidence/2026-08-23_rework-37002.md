# rework-37002 — QM5_37002 review remediation

- Router task: `e8869500-9153-47dc-bd5d-4ebc884a0ef9`
- EA: `QM5_37002_dual-thrust-asymmetric-range-breakout`
- Approved card: `strategy-seeds/cards/approved/QM5_37002_dual-thrust-asymmetric-range-breakout.md`
- Reworked MQ5 SHA-256: `60c0a165d26b08889a66e8cd804d72f8e7458b7608f82f6c427f6f83459de9f3`
- Existing/stale EX5 SHA-256: `2948aaf1c16e41fbb055cc4bc2f75951c7b4a339613d1d151ae2293403297647`
- Status: `SOURCE_READY_COMPILE_HELD`; source/static remediation complete, fresh governed compile not claimed.

## Work order

The review findings were read from:

- `docs/ops/evidence/e8869500_qm5_37002_gemini_build_codex_review_2026-08-18.md`
- `docs/ops/evidence/2026-08-23_review_QM5_37002.md`
- `docs/ops/evidence/2026-08-23_review_ea_37002_gemini_first_draft.md`
- `docs/ops/evidence/2026-08-23_qm5_37002_claude_gemini_build_review.md`

The actionable defects were current-open/lookahead drift, market-order substitution for the
card's pending stops, ATR/time-stop substitution, missing settlement close and drawdown
limits, entry filters preceding protective management, and the local GMT conversion.

## What changed

### Card-faithful signal and order geometry

- `framework/EAs/QM5_37002_dual-thrust-asymmetric-range-breakout/QM5_37002_dual-thrust-asymmetric-range-breakout.mq5:98-132`
  copies one bounded vector from D1 shift 0, guards both the returned count and `ArraySize`,
  uses only `rates[1..lookback]` for HH/HC/LL/LC, and anchors both triggers to
  `rates[0].open`. This removes same-bar range lookahead and the shift-1 open anchor.
- `...mq5:344-367` creates both `QM_BUY_STOP` and `QM_SELL_STOP` requests. The opposite
  trigger is the broker-held SL and the stated 1:1.5 card profile becomes a 1.5R TP. The
  first leg is rolled back if the second submission fails.
- `...mq5:370-384` cancels the OCO peer after a fill, cancels stale pending orders at the
  settlement window, and exposes the mandatory settlement exit. The unapproved ATR stop and
  five-bar time stop were removed.

### Loss limits, time base, and management reachability

- `...mq5:139-164,231-268` converts broker time through `QM_BrokerToUTC`, maps UTC midnight
  back through `QM_UTCToBroker` for account deal history, and enforces the card's 2.0%
  account realized-loss entry halt.
- `...mq5:395-441` fail-closes weaker overrides, configures the card's three-tick maximum
  deviation, and replaces the framework default kill switch with explicit 2.5% daily and
  5.0% total-drawdown limits.
- `...mq5:453-480` retains the MAE hook and calls management/exit before Friday, custom,
  spread, or news entry filters. Protective exits can no longer be suspended by an entry
  blackout.

### Inputs, setfiles, registries, and docs

- Every declared input has at least one executable use site; the automated count check is
  `tools/strategy_farm/tests/test_qm5_37002_rework_contract.py:58-68`.
- All three setfiles retain their governed slots (SP500=0, NDX=1, XTIUSD=2),
  `RISK_FIXED=1000`, and `RISK_PERCENT=0`; they seal the card loss/slippage defaults at
  lines 24-30. Their old binary-bound hashes were replaced with `build_hash: pending` at
  line 13 because no fresh EX5 was produced.
- `framework/EAs/QM5_37002_dual-thrust-asymmetric-range-breakout/SPEC.md:28-40` now documents
  only the implemented/card-traceable parameter surface.
- No registry file changed. Existing active rows remain
  `magic_numbers.csv:17455-17457` (370020000-370020002), resolved through the framework's
  `QM_FrameworkInit` / `QM_FrameworkMagic` chain.

## Tests and validation

Focused regression:

```text
python -m pytest tools/strategy_farm/tests/test_qm5_37002_rework_contract.py -q
.......                                                                  [100%]
7 passed in 2.34s
```

The regression cases are at
`tools/strategy_farm/tests/test_qm5_37002_rework_contract.py:58-208` and cover every input,
closed-bar/current-open indexing, pending order types, SL/TP geometry, OCO/settlement
behavior, filter ordering, loss limits, fixed-risk setfiles, magic rows, and EA-scoped
hardening.

Scoped validators:

```text
skill_build_ea_guard.py --ea-id 37002 --ea-label QM5_37002_dual-thrust-asymmetric-range-breakout
status=ok; ea_registry_row=true; magic_registry_rows=true; ea_dir_exists=true

validate_spec_doc.py framework/EAs/QM5_37002_dual-thrust-asymmetric-range-breakout
Summary: 1 PASS, 0 FAIL

validate_build_guardrails.py framework/EAs/QM5_37002_dual-thrust-asymmetric-range-breakout
verdict=PASS; files_checked=4; findings=[]

validate_symbol_scope.py --ea-label QM5_37002_dual-thrust-asymmetric-range-breakout --fail-on-leak --json
verdict=SINGLE_SYMBOL_OK; n_violations=0

build_gate_hardening.py --repo-root . --ea-label QM5_37002_dual-thrust-asymmetric-range-breakout
failures=[]; files_scanned=1; D2/D4/D5/D7/D9/D10/D11 all clean

git diff --check
PASS (no whitespace errors; only Git's LF-to-CRLF worktree notices)
```

Full requested suite:

```text
python -m pytest tools/strategy_farm/tests -q
4 failed, 4672 passed, 4 skipped, 2 warnings, 42 subtests passed in 2078.60s
```

All four failures are outside this ticket and reference no QM5_37002 path:

1. `test_build_gate_hardening.py::test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`
   — seven pre-existing `QM5_411xx` findings, first at `QM5_41134...mq5:599`.
2. `test_execution_contract_lint.py::test_20030_20032_bindings_follow_deployed_calendar_but_remain_blocked`
   — expected news-calendar hash/coverage ended 2026-08-21, runtime binding is 2026-08-29.
3. `test_execution_contract_lint.py::test_20009_ftmo_news_calendar_is_exact_and_evidence_bound`
   — the same refreshed calendar hashes differ from pinned expectations.
4. `test_opt_census_dispatch.py::test_opt_census_ranks_tier6_not_priority`
   — expected v4 rank 7, stored query rank 6.

## Compile boundary

The exact scoped command was attempted:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 \
  -EAPath framework/EAs/QM5_37002_dual-thrust-asymmetric-range-breakout/QM5_37002_dual-thrust-asymmetric-range-breakout.mq5 -Strict
```

It stopped before MetaEditor with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` /
`INCLUDE_MIRROR_REFUSED` because factory `terminal64` processes were alive. The durable
receipt is `D:/QM/reports/compile/20260824_103550/summary.csv`. A second EA-scoped
`build_check.ps1 -EALabel QM5_37002_dual-thrust-asymmetric-range-breakout -Strict
-SkipCompile` was refused by the same compile guard before any gates ran.

No guard bypass, process stop, factory toggle, compile enqueue, backtest enqueue, terminal
launch, T_Live access, or verdict-row mutation occurred. A current read-only check still
showed nine `terminal64` processes. The pre-existing EX5 therefore remains stale and is not
included in this change; a fresh governed canonical compile must replace it and seal the
three pending setfile hashes before any pipeline handoff.

## Rollback

Revert the ticket commit with `git revert <rework-37002-commit>`. This restores the previous
MQ5, SPEC, and three setfiles and removes the regression/evidence files. No registry, router
history, verdict row, backtest queue, factory state, or live terminal requires rollback.

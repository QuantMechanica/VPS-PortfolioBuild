# QM5_9910 bandy-tema-adx-crossover-trend — Claude review-lane close

Date: 2026-08-24 UTC
Router task: `b0616421-88f4-4370-b053-b96031a43386` (state REVIEW, assigned codex)
Source task: `499eaa2a-1f7a-47d2-b6df-a52d4d2999dc` (`gemini`/agy, build_ea)
Source verdict: "QM5_9910 mq5 built, SPEC added, setfiles aligned, guardrails PASS; ready for Codex review"
Reviewed artifact: `D:/QM/strategy_farm/artifacts/builds/499eaa2a-1f7a-47d2-b6df-a52d4d2999dc.json`
Approved card: `framework/EAs/QM5_9910_bandy-tema-adx-crossover-trend/docs/strategy_card.md` (`g0_status: APPROVED`)
Prior Codex note (same task): `docs/ops/evidence/2026-08-23_qm5_9910_gemini_code_review.md` (REQUEST_CHANGES)

## Verdict: RECYCLE

One decisive, card-faithfulness defect blocks Q02 acceptance. Independent verification
also overturns/downgrades several of the prior Codex findings; those are recorded so the
rebuild does not chase non-defects.

## Blocking defect (actionable)

### D1 — `strategy_catastrophic_atr_mult` is a dead input; the card-required 5.0×ATR catastrophic backstop is not implemented

The approved card (`docs/strategy_card.md`, "Stop Loss": *"Catastrophic backstop: `5.0 * ATR(14, D1)` from entry"*)
requires a two-layer stop: a 2.0×ATR Chandelier trail **and** a separate 5.0×ATR
catastrophic backstop.

`strategy_catastrophic_atr_mult` (declared line 44) is grepped to exactly two use-sites:
- line 44 — declaration
- line 87 — `Strategy_NoTradeFilter()` validity guard (`... <= 0.0`)

It is never passed to any stop calculation. Entry sets the sole stop with the trail
multiplier (`QM_StopATRFromValue(..., strategy_trail_atr_mult)`, line 135) and management
only maintains that trail (`QM_TM_TrailATR(..., strategy_trail_atr_mult)`, line 164). No
5.0×ATR backstop exists anywhere in the EA. Per the review dead-input rule this is a
RECYCLE, and it also fails card fidelity.

Required correction: implement the 5.0×ATR-from-entry catastrophic backstop the card
mandates, then recompile, regenerate the setfiles via the governed generator, and emit a
fresh task-bound build artifact for re-review.

## Prior Codex findings — independent re-adjudication

- **Codex #1 (D1 lifecycle / exits suppressed on wrong-TF attach): DOWNGRADED — not a
  9910-specific defect.** `Strategy_NoTradeFilter()` is called before
  `Strategy_ManageOpenPosition()`/`Strategy_ExitSignal()` in 37/38 sampled fleet EAs — this
  is the framework-standard OnTick ordering, not a defect introduced by this build. The
  `_Period != PERIOD_D1` guard 9910 adds is an extra safeguard; factory attachment is D1 via
  the setfiles. Non-blocking framework observation, not a build RECYCLE reason.
- **Codex #3 (setfile `build_hash` not bound to MQ5 sha): FALSE POSITIVE.** The setfile
  `build_hash` is a per-setfile content self-hash (`framework/scripts/build_check.ps1:414-419`
  computes SHA256 over the setfile's own normalized lines), not a binding to the MQ5 sha. 13
  distinct values across 13 symbols are expected and correct; none is supposed to equal the
  MQ5 sha. The router dispatch gate (`tools/strategy_farm/agent_router.py:1854`) only requires
  a well-formed `[0-9a-f]{64}` value, which every setfile carries. Not a defect.
- **Codex #4 (cohort adds GDAXI/UK100, omits oil): DOWNGRADED.** The 13-symbol cohort matches
  the governed magic-allocator rows in `framework/registry/magic_numbers.csv` (ea 9910, base
  `99100000`, slots 0-12, author "Codex governed allocator" 2026-08-17), not an arbitrary
  gemini choice. Any card-vs-registry scope drift is upstream of this build; the card R3 prose
  authorizes "index CFDs" generically. Note for the rebuild, not independently blocking.
- **Codex #5 (producer artifact schema-incomplete / no smoke proof): NOTED, non-blocking for
  the strategy.** The artifact carries `build_check_passed:true` plus matching mq5/ex5 hashes
  and the ex5 (04:52) is newer than the mq5 (04:49); compile evidence
  `framework/build/compile/20260822_120847/...compile.log` reports "0 errors, 0 warnings".
  The fresh post-fix build should carry compile identity, but this is producer-evidence
  quality, not a strategy defect.

Additional minor: SPEC.md §6 points the card at `artifacts/cards_approved/QM5_9910_...md`,
which does not exist; the real approved card is the EA-local `docs/strategy_card.md`. Fix the
pointer in the rebuild.

## Checks that passed

- Card-faithful mechanism otherwise present: closed-D1 TEMA(8)/TEMA(21) crossover (lines
  105-125), ADX(14)≥20 gate (109/114), 2.0×ATR initial stop + trail (135/164), one-position
  enforcement (100), opposite-cross signal exit (196-207), 60-D1-bar time stop (178).
- Framework conformity: `#include <QM/QM_Common.mqh>`; MAE hook `QM_FrameworkTrackOpenPositionMae`
  first in OnTick; `RISK_FIXED=1000`, `RISK_PERCENT=0`; magic via `QM_FrameworkMagic()`
  (MagicResolver); news filter wired (`QM_NewsAllowsTrade2`); pooled/bounded indicator buffers
  (shift 1/2, framework readers); no raw series, no ML, no invented commission/swap. All called
  framework functions resolve in `framework/include/QM/`.
- Magic registry: 13 active rows ea 9910 base `99100000` = ea_id*10000+slot; ea_id_registry row
  active.
- All 13 setfiles: `RISK_FIXED>0`, `RISK_PERCENT=0`, well-formed 64-hex `build_hash`.

## Disposition

Read-only review. No source, binary, registry, resolver, setfile, work item, verdict, or trade
stream changed. T_Live/AutoTrading untouched. Task closed RECYCLE with one actionable defect
(D1). Corrected gemini code + fresh artifact require a new mandatory review.

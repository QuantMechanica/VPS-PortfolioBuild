# rework-36003 — QM5_36003 review repair and card-defect block

- Router task: `b4c223a0-818d-444d-bb7d-1336da8abdd2`
- EA: `QM5_36003_nnfx-hull-ma-zerolag-macd-stc`
- Worktree: `C:\QM\worktrees\rework-slot-1`
- Date executed: 2026-08-24
- Disposition: **BLOCKED — OWNER-approved card does not define `BetterVol == HIGH` numerically**

## Authority and preflight

The runtime approved card
`D:\QM\strategy_farm\artifacts\cards_approved\QM5_36003_nnfx-hull-ma-zerolag-macd-stc.md`
has `g0_status: APPROVED`, `ea_id: QM5_36003`, and the matching slug. Existing
registries were verified and not modified:

- `framework/registry/ea_id_registry.csv:4458` — one active EA identity.
- `framework/registry/magic_numbers.csv:17434-17436` — existing active slots
  0/1/2 for EURUSD.DWX, GBPUSD.DWX, and XAUUSD.DWX with magics
  360030000/360030001/360030002.

The originally referenced review note was read in full:
`docs/ops/evidence/b4c223a0_qm5_36003_gemini_build_codex_review_2026-08-18.md`.
The later source review and stale-binary findings were also read:
`docs/ops/evidence/2026-08-23_review_QM5_36003.md` and
`docs/ops/evidence/c8791ede_qm5_36003_claude_review_stale_ex5_2026-08-23.md`.

While this repair was in progress, another reviewer changed the router task
from the claimed `IN_PROGRESS` state to `REVIEW` at
`2026-08-24T11:16:45Z` and attached a newer artifact:
`C:\QM\repo\docs\ops\evidence\b4c223a0_qm5_36003_recycle1_codex_review_2026-08-24.json`.
That artifact independently confirms the headline ZeroLag/STC/TP1/UTC repairs
but finds that the approved card never defines `BetterVol == HIGH`
numerically. The prior-20-bar tick-volume mean is therefore still an
unapproved implementation choice. The ticket explicitly requires `BLOCKED`
for a card defect beyond repair; changing the approved card is outside this
task and would require OWNER authority.

## Objective changes completed before the card defect was surfaced

1. Added explicit fail-fast `ArraySize` proofs for the bounded entry buffers,
   STC rolling queues, and entry latest index at
   `framework/EAs/QM5_36003_nnfx-hull-ma-zerolag-macd-stc/QM5_36003_nnfx-hull-ma-zerolag-macd-stc.mq5:228-382`.
2. Added the corresponding runner-buffer and previous/latest index proofs at
   the same source file `:562-611`. Both raw `CopyRates` reads remain bounded,
   closed-bar-gated, and `perf-allowed` annotated (`:210`, `:554`).
3. Regenerated only the three authorized D1 backtest setfiles with
   `framework/scripts/gen_setfile.ps1`. Each now contains the exact 20 declared
   `strategy_*` names and defaults, rather than stale aliases such as
   `strategy_stc_length`, `strategy_sl_atr_mult`, and
   `strategy_total_dd_halt_pct`. Example:
   `framework/EAs/QM5_36003_nnfx-hull-ma-zerolag-macd-stc/sets/QM5_36003_nnfx-hull-ma-zerolag-macd-stc_EURUSD.DWX_D1_backtest.set:18-43`.
   All three keep `RISK_FIXED=1000` and `RISK_PERCENT=0`.
4. Added focused static regression coverage at
   `tools/strategy_farm/tests/test_qm5_36003_review_rework.py:39`, `:97`,
   `:122`, and `:156`. It locks the reviewed ZeroLag formula, BetterVol code
   shape without the rejected 1.02 multiplier, 50% TP1/runner lifecycle,
   UTC/loss controls, MAE/news ordering, buffer proofs, every declared input
   use-site, exact setfile strategy keys, fixed-risk mode, and existing magic
   rows.

No registry, gate criterion, backtest queue, verdict row, factory setting, or
`T_Live` artifact was changed.

## Validation output

### Focused EA tests

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_36003_review_rework.py
....                                                                     [100%]
4 passed in 0.89s
```

### Touched validation modules

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_36003_review_rework.py \
  tools/strategy_farm/tests/test_gen_setfile.py \
  tools/strategy_farm/tests/test_build_guardrails.py \
  tools/strategy_farm/tests/test_build_gate_hardening.py \
  -k 'not qm5_411xx_sources_have_no_unbounded_numeric_buffers'
53 passed, 1 deselected in 452.73s (0:07:32)
```

The unfiltered touched-module run was also executed. It produced `53 passed,
1 failed`; the sole failure is the unrelated whole-cohort assertion
`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`, which reports
pre-existing unbounded buffers in `QM5_41134_wti-mdaily-iqrmean-mom.mq5`.
That EA is outside the one-EA scope and was not modified.

### Mechanical gates

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . \
  --ea-label QM5_36003_nnfx-hull-ma-zerolag-macd-stc
files_scanned=1 failures=0 warnings=0 symbols_observed=3

python tools/strategy_farm/validate_build_guardrails.py \
  framework/EAs/QM5_36003_nnfx-hull-ma-zerolag-macd-stc
verdict=PASS files_checked=4 findings=[] max_news_stale_hours=336

python framework/scripts/validate_spec_doc.py \
  framework/EAs/QM5_36003_nnfx-hull-ma-zerolag-macd-stc
PASS QM5_36003_nnfx-hull-ma-zerolag-macd-stc
Summary: 1 PASS, 0 FAIL (of 1)
```

`git diff --check` found no whitespace errors (only the repository's normal
LF-to-CRLF checkout warnings).

## Compile/build identity — deliberately not claimed

The scoped authoritative command was attempted exactly as required:

```text
framework/scripts/build_check.ps1 \
  -EALabel QM5_36003_nnfx-hull-ma-zerolag-macd-stc -SkipCompile
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED / LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
```

The sanctioned compile-only queue was then attempted with the exact router
task as source-repair authority. It created no row and refused deterministically:

```text
python tools/strategy_farm/farmctl.py enqueue-compile \
  QM5_36003_nnfx-hull-ma-zerolag-macd-stc \
  --source-repair-authority b4c223a0-818d-444d-bb7d-1336da8abdd2
SOURCE_REPAIR_AUTHORITY_INVALID; EX5_ALREADY_PRESENT; BUILD_TASK_EXISTS
enqueued_count=0
```

A read-only DB query confirmed zero `COMPILE_EA` rows for `QM5_36003`. The
compile gate was not weakened and the factory was not toggled.

Current identities after the source-only repair are:

- MQ5 SHA-256: `9e19309126d6707b9805e4d5ed45377517b775c1c21624b3aad605b751f42206`
- Existing EX5 SHA-256: `baffb41f8b8af18990919b622acf8c27d5c98a4543dc2c9160f7b020a1a3be47`

The EX5 is still the rejected pre-remediation binary and is not represented as
current evidence. No backtest or smoke was enqueued or run. Because the newer
review establishes a card defect before compilation, generating a fresh binary
would not make the EA approvable and was not pursued through an unauthorized
compile bypass.

## Remaining OWNER decision

The approved card must define `BetterVol == HIGH` mechanically (data field,
closed-bar window, whether the current bar is excluded, aggregation, comparison
operator, and any multiplier). A revised card must receive the required OWNER
approval before Development can implement or compile this entry gate. The
newer review also requests a fail-closed D1 execution contract and durable
restart reconstruction for daily-loss and TP1 state; those should be resolved
against the revised card before a fresh EX5/build-check/smoke chain is created.

## Rollback

Revert the commit containing this evidence file with `git revert <commit>`.
That restores the prior MQ5 and three setfiles and removes the focused test.
There is no runtime rollback: no compile row, backtest row, registry row,
factory flag, verdict row, deploy artifact, or live terminal was changed.

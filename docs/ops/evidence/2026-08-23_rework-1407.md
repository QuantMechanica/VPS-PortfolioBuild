# rework-1407 review-fix evidence

**Router task:** `c789e9ec-4aeb-41c4-acdd-ad8aff760737`
**EA:** `QM5_1407_classical-symmetric-triangle-breakout-h4`
**Worktree:** `C:/QM/worktrees/rework-slot-5`
**Prepared:** 2026-08-24 (ticket evidence namespace requested as 2026-08-23)
**Scope:** build/review fix only; no backtest enqueue, verdict overwrite, factory toggle, T_Live write, or gate-criteria change.

## Work order

The independent review at `docs/ops/evidence/2026-08-22_codex_review_qm5_1407.md` required eight repairs: pending-order OCO entry/expiry, ATR spread, exact card universe, past-apex rejection, pivot-overlap reuse, restart-safe management, checked TP1 operations, and a time-advancing pattern-failure line.

The runtime-approved card was read from `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1407_classical-symmetric-triangle-breakout-h4.md`; its front matter is `g0_status: APPROVED` and its slug matches the active `ea_id_registry.csv` row.

## What changed

### Card-faithful EA

- Strategy inputs now encode the 12-H4 pending lifetime and 0.25-ATR spread limit (`framework/EAs/QM5_1407_classical-symmetric-triangle-breakout-h4/QM5_1407_classical-symmetric-triangle-breakout-h4.mq5:53`, `:56`). Every declared Strategy input has an executable use-site, asserted by pytest.
- Pattern detection uses bounded `CopyRates` plus `ArraySize` guards and D1 ATR for the slope contract (`mq5:358`). Apex shift zero or positive is rejected explicitly (`mq5:420`).
- Entry submits both `QM_BUY_STOP` and `QM_SELL_STOP` through the framework, with the 12-bar expiry carried in each request (`mq5:566-575`, `:608-613`). The first live fill cancels the peer (`mq5:653`). A rejected second leg cancels the incomplete bracket (`mq5:802`).
- Pending state is structurally revalidated on each new H4 bar, with explicit incomplete-bracket, expiry, and at/beyond-apex invalidations (`mq5:691-709`).
- Entry spread is `ask-bid <= 0.25 * ATR(14,H4)` (`mq5:530-531`); the card's entry-only high-impact-news window is a literal 480/480 minutes and runs after management.
- Reuse is persisted by pivot timestamps and blocks only when overlap is greater than the configured 50% during the 20-bar window (`mq5:474-486`, `:499-505`). Entry and invalidation record the event.
- Pending geometry, projected lines, active/reuse pivots, lifecycle, and separate TP1 flags persist in terminal global variables. Missing state with a live position arms a fail-closed strategy exit (`mq5:726`, `:761`).
- TP1 partial close and break-even move are checked independently; only successful actions set their durable completion flag, so failures retry (`mq5:675-680`). Pattern failure evaluates the line at the current closed bar time (`mq5:712-742`).
- The direct MAE hook remains first in strategy tick processing (`mq5:781`). `QM_Common.mqh`, registry-backed framework magic, `RISK_FIXED` backtest defaults, and framework trade helpers remain intact. No ML was added.

### Spec, universe, registry, and sets

- `SPEC.md` was rewritten to describe the approved OCO mechanics, restart contract, and exact eight-symbol universe.
- Setfiles now exist only for EURUSD, GBPUSD, XAUUSD, NDX, WS30, GDAXI, UK100, and XTIUSD. All eight remain backtest fixed-risk (`RISK_FIXED=1000`, `RISK_PERCENT=0`) and carry the card-aligned strategy defaults.
- Six unauthorized setfiles were removed: AUDUSD, NZDUSD, SP500, USDCAD, USDCHF, USDJPY.
- The review identified the only missing required allocation: XTIUSD. Slot 13 / magic 14070013 was appended at `framework/registry/magic_numbers.csv:17707`. Existing slots 0-12 were not edited, removed, renumbered, or retired.
- `python framework/scripts/update_magic_resolver.py --keep-obsolete` wrote `framework/include/QM/QM_MagicResolver.mqh` with 17,993 rows, zero dropped, registry SHA `4E8253D46684482ECCA4CD725B033918F1805EBBB9D9E563741167B4C2832474` (`QM_MagicResolver.mqh:16-18`). A subsequent dry run was byte-idempotent.

### Unit coverage

`tools/strategy_farm/tests/test_qm5_1407_rework_static.py` asserts source hardening, both pending sides, OCO/expiry/apex behavior, ATR spread, 480-minute news window, pivot-aware durable reuse, restart fail-close, checked TP1 actions, advancing-line exits, bounded/perf-allowed series access, input use-sites, exact setfile universe/fixed risk, and preservation of magic slots 0-12 plus XTIUSD slot 13.

## Validation output

### PASS

```text
python -m pytest tools/strategy_farm/tests/test_qm5_1407_rework_static.py \
  tools/strategy_farm/tests/test_build_guardrails.py \
  tools/strategy_farm/tests/test_magic_resolver_reconcile_newlines.py -q
28 passed in 1.39s

python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py -q \
  -k "not test_qm5_411xx_sources_have_no_unbounded_numeric_buffers"
28 passed, 1 deselected in 117.24s

python tools/strategy_farm/validate_build_guardrails.py \
  framework/EAs/QM5_1407_classical-symmetric-triangle-breakout-h4
verdict: PASS; files_checked: 9; findings: []

build_gate_hardening.analyze_file(<1407 mq5>, <approved card>)
failures: []; warnings: []
```

The unfiltered touched hardening module run completed `56 passed, 1 failed`; the single failure is pre-existing and outside this ticket:

```text
test_build_gate_hardening.py::test_qm5_411xx_sources_have_no_unbounded_numeric_buffers
EA_INDICATOR_BUFFER_UNBOUNDED: QM5_41134_wti-mdaily-iqrmean-mom.mq5:599 ...
```

No QM5_1407 assertion failed.

### Compile/build-check interlock (not bypassed)

The required scoped compile was attempted:

```text
powershell -File framework/scripts/compile_one.ps1 \
  -EAPath framework/EAs/QM5_1407_classical-symmetric-triangle-breakout-h4/QM5_1407_classical-symmetric-triangle-breakout-h4.mq5 -Strict
compile_one.result=FAIL
compile_one.reason_class=INCLUDE_MIRROR_REFUSED
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
```

Evidence log: `framework/build/compile/20260824_075242/QM5_1407_classical-symmetric-triangle-breakout-h4.compile.log`; summary: `D:/QM/reports/compile/20260824_075242/summary.csv`.

`build_check.ps1 -EALabel QM5_1407_classical-symmetric-triangle-breakout-h4 -SkipCompile` was likewise refused before checks by the same global live-terminal include-mirror interlock. No terminal—including `C:/QM/mt5/T_Live`—was stopped or modified.

The governed non-backtest `enqueue-compile` alternative was attempted narrowly with the worktree override but enqueued nothing: `eligible_count=0`, `enqueued_count=0`, reason `EX5_ALREADY_PRESENT` (and initially old bound setfile hashes). No compile or backtest work item was created. The checked-in EX5 is therefore stale; all revised setfiles truthfully carry `build_hash: PENDING_COMPILE`.

### Scoped hardening registry conflict

`python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_1407_classical-symmetric-triangle-breakout-h4` proves the source row clean, but its aggregate D17 result reports six failures solely from the untouched Gemini-era active magic allocations for AUDUSD, NZDUSD, SP500, USDCAD, USDCHF, and USDJPY. The setfile universe itself is exactly the eight card symbols.

Retiring or editing those six existing rows would make D17 green, but the ticket explicitly says never to touch allocations that already exist. No gate criterion or registry status was changed to manufacture a PASS. This conflict requires registry-governance adjudication during review.

## Risks and open validation

1. Fresh MetaEditor syntax/EX5 proof is absent because the live-terminal interlock refused every ad-hoc path and the governed compile candidate refused the stale existing binary without an authorized source-repair successor.
2. The old EX5 does not represent this source and must not be used. Setfile hashes remain `PENDING_COMPILE` until a governed fresh compile.
3. Aggregate hardening remains D17-failed until an authorized reviewer decides whether the six erroneous existing magic rows may be retired despite the ticket's preservation rule.
4. Full `test_build_gate_hardening.py` remains red on unrelated QM5_41134; all other cases and all 1407-specific tests pass.

## Rollback

Revert the ticket commit with `git revert <rework-1407-commit>`. This restores the prior EA/spec/setfiles, removes the appended XTIUSD row, and restores the previous generated resolver bytes. Router state is external and must be updated separately if the review task is withdrawn. No backtest/verdict/factory/live rollback is required because none was mutated.

# QM5_1539 build artifact — compile blocked by live-factory guard

- Router task: `576c773a-9bba-40eb-a32d-7d0a52d8b386`
- EA: `QM5_1539_aa-canary-13612w`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1539_aa-canary-13612w.md`
- Canonical checkout / branch: `C:/QM/repo` / `agents/board-advisor`
- Pre-build HEAD: `357f1d4e8`
- MQ5 SHA-256: `7cbbb46b1b94a17bef08e0e0173239c66f68d7c5c6877755d7f5314836ec6d71`
- Verdict: **COMPILE_BLOCKED_INCLUDE_MIRROR — remain in REVIEW; no pipeline handoff**

## Implemented artifact

The prior tracked MQ5 was an `Unknown Strategy` skeleton whose entry hook
always returned false. This task implemented the OWNER-approved deterministic
D1 mechanic:

- the first D1 bar of each calendar month is the only strategy decision point;
- closed-price returns over 21, 63, 126, and 252 D1 bars feed
  `12*r_1m + 4*r_3m + 2*r_6m + r_12m`;
- all four proxy observations named by the card (`NDX.DWX`, `GDAXI.DWX`,
  `USDJPY.DWX`, and `XAUUSD.DWX`) are evaluated as canaries;
- with `B=1`, any canary score `<= 0` selects the defensive sleeve; otherwise
  the risk sleeve is active;
- the highest positive 13612W score selects one risk or defensive symbol, and
  all non-positive candidates select cash;
- strict `>` comparison makes equal-score tie order deterministic and matches
  the documented candidate order;
- each symbol-hosted instance enters long only when `_Symbol` is the selected
  winner, then revalidates/rotates monthly;
- the initial stop is `3.0 * ATR(20,D1)`, with no unapproved take-profit,
  short, intramonth strategy exit, leverage, ML, grid, or martingale logic;
- missing data for any required canary or active-sleeve candidate fails the
  monthly decision closed.

The card metadata says 100 expected trades/year/symbol, but the approved
mechanic is monthly. The EA implements the mechanical monthly rule rather than
inventing intramonth entries to satisfy inconsistent metadata.

`SPEC.md` records the four-proxy interpretation, daily-bar month mapping,
cash/tie behavior, symbol universe, and framework alignment. Thirteen D1
backtest setfiles match the active magic registry and declare every strategy
input. All use `RISK_FIXED=1000`, `RISK_PERCENT=0`, and the MQ5 source hash.

## Governed gates

| Gate | Result |
|---|---|
| Card `g0_status`, EA ID, and slug | PASS |
| Active EA registry row | PASS — `1539,aa-canary-13612w,...,active` |
| Active magic rows | PASS — 13 slots/symbols |
| Build guardrails, 336-hour stale-news ceiling | PASS — 14 files, zero findings |
| D2–D11 build hardening and exact symbol matrix | PASS — zero failures/warnings, 13 symbols |
| SPEC validator | PASS |
| Setfile source-hash contract | PASS — 13/13 match MQ5 SHA-256 |
| Backtest risk contract | PASS — 13/13 fixed 1000, percent 0 |
| Fresh governed compile | BLOCKED — include mirror refused while live terminal workers were active |

## Compile evidence and stale-binary boundary

`python tools/strategy_farm/compile_ea.py --ea-id 1539 --force --json
--fail-on-error` returned `COMPILE_FAILED` with
`reason_class=INCLUDE_MIRROR_REFUSED` before MetaEditor compilation. Durable
summary: `D:/QM/reports/compile/20260822_172834/summary.csv`.

A subsequent static `build_check.ps1 -SkipCompile` invocation was also stopped
at its live-factory guard with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`. It
instructed callers to enqueue compile work; this orchestration cycle cannot
invent or route work, so no enqueue, retry, bypass, or terminal interruption
was attempted.

An untracked EX5 already existed before this task. Its SHA-256 is
`7bb336c2c4e247fb01311bd397139671807d3de9037d091b6ee5cc065cff7a93` and its
mtime is `2026-08-22T09:02:29Z`, while the implemented MQ5 mtime is
`2026-08-22T17:27:49Z`. The compile attempt left that binary byte-for-byte
unchanged. It therefore predates the implemented source, is excluded from the
commit, and is not accepted as compile evidence.

No pipeline phase or backtest was run. No terminal was launched or
interrupted. AutoTrading and T_Live were not touched.

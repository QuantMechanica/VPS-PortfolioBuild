# rework-38006 review-fix evidence

- Ticket: `rework-38006`
- Router task: `67e670f0-e79d-4b6b-a7b2-effd03a626f4`
- EA: `QM5_38006_codetrading-doji-hammer-pivot-rejection`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_38006_codetrading-doji-hammer-pivot-rejection.md`
- Worktree branch: `rework-slot-5`
- Scope: one-EA review repair only; no backtest enqueue/delete, verdict overwrite,
  factory toggle, gate change, magic allocation change, or `T_Live` access

## Review findings closed

1. **Open-position management is reachable.** `OnTick` refreshes closed-bar state,
   then calls `Strategy_ManageOpenPosition` and evaluates `Strategy_ExitSignal`
   before the entry-only no-trade gate (`.mq5:386`, `:390`, `:392`, `:412`). The
   +1R transition uses the original broker-side stop distance and moves to exact
   entry (`.mq5:275-303`); it is no longer bypassed by the one-position admission
   rule.
2. **Closed-bar state precedes ATR admission.** A new H1 bar calls
   `AdvanceState_OnNewBar` before any entry-only filter (`.mq5:382-412`). Its four
   O(1) OHLC reads are shift-1 and explicitly `perf-allowed` (`.mq5:124-127`).
3. **Spread is checked at the execution boundary.** The fail-closed live-spread
   predicate is implemented at `.mq5:178-187`, reused by the no-trade gate at
   `.mq5:200`, and re-read after signal construction immediately before
   `QM_TM_OpenPosition` (`.mq5:425-433`). This prevents both zero-ATR startup
   admission and spread expansion during intervening news checks.

The inherited source already contained the broader card remediation from commit
`8d64f9c0d` (reachable management, exact structural stops, UTC rollover, loss
rails, and three-tick deviation). This ticket closes the remaining immediate
spread-recheck gap and makes both rejected paths durable in focused tests.

## Card and framework conformity

- Structural stops remain exactly beyond the rejection candle at `.mq5:232` and
  `.mq5:240`; TP remains exact 1.8R. There is no ATR stop fallback.
- `QM_Common` is the only framework umbrella include (`.mq5:5`); MAE tracking is
  called before tick guards (`.mq5:375`). Entry configuration and kill-switch
  risk rails are wired at `.mq5:346-359`.
- Magic is obtained via `QM_FrameworkMagic` (`.mq5:193`, `:260`, `:352`, `:355`,
  `:394`), which is initialized through the generated resolver chain. No raw
  magic arithmetic occurs in the EA.
- Registry identity is unchanged: `ea_id_registry.csv:4477`; the existing active
  magic rows are `magic_numbers.csv:17499-17501`. No registry or resolver edit
  was needed.
- All 31 declared inputs have an executable use-site. The explicit token audit
  reported a minimum count of 2; the invariant is enforced by
  `test_qm5_38006_rework_static.py:105-120`.
- There are no direct indicator handles, `CopyBuffer` calls, dynamic indicator
  buffers, or ML dependencies. The raw-series/performance/framework assertions
  are at `test_qm5_38006_rework_static.py:122-163`.
- All three H1 backtest sets use slots 0/1/2 and retain `RISK_FIXED=1000`,
  `RISK_PERCENT=0` (`sets/*.set:18-20`). Their `build_hash` at line 13 equals the
  repaired MQ5 SHA-256
  `9c72cbc80c712783cc4f82aa313800837ae8bff1ecc4cd9bcbc6b770f02395df`.
- `SPEC.md:21` documents management/admission ordering and the immediate spread
  recheck. `SPEC.md:103-104` names the real framework diagnostic
  `EA_RISK_SIZER_UNCONFIGURED`, not the obsolete nonexistent risk-mode code.

## Validation evidence

Commands ran from `C:/QM/worktrees/rework-slot-5` on 2026-08-24.

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_38006_rework_static.py
...... [100%]
6 passed in 0.62s

python -m pytest -q tools/strategy_farm/tests/test_build_guardrails.py
.................... [100%]
20 passed in 0.89s

python tools/strategy_farm/build_gate_hardening.py --repo-root C:/QM/worktrees/rework-slot-5 --ea-label QM5_38006_codetrading-doji-hammer-pivot-rejection
files_scanned=1; failures=[]; warnings=[]

python tools/strategy_farm/validate_build_guardrails.py <scoped-mq5> <scoped-sets-dir>
MQ5 files_checked=1; sets files_checked=3; findings=[]; verdict=PASS

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_38006_codetrading-doji-hammer-pivot-rejection
Summary: 1 PASS, 0 FAIL (of 1)

python framework/scripts/skill_build_ea_guard.py --ea-id 38006 --ea-label QM5_38006_codetrading-doji-hammer-pivot-rejection
status=ok; ea_registry_row=true; magic_registry_rows=true; ea_dir_exists=true

git diff --check -- <EA-directory> tools/strategy_farm/tests/test_qm5_38006_rework_static.py
PASS (exit 0; line-ending notices only)
```

The touched hardening module's complete test suite was also run together with the
focused tests:

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_38006_rework_static.py tools/strategy_farm/tests/test_build_gate_hardening.py
1 failed, 34 passed in 416.97s
```

The sole failure is the pre-existing repository-wide assertion
`test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`, which reports seven
findings in out-of-scope `QM5_41134_wti-mdaily-iqrmean-mom.mq5` (first at its
line 599). The EA-scoped hardening result above is clean; this ticket does not
change another EA.

### Authoritative build-check interlock

The required exact-label command was attempted:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -Strict -EALabel QM5_38006_codetrading-doji-hammer-pivot-rejection -RepoRoot C:/QM/worktrees/rework-slot-5
exit=1
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
detail=terminal64 processes are alive; ad-hoc compile/build_check is refused
```

The command failed closed before compilation. The ticket forbids toggling the
factory or disturbing terminals and does not authorize a state-DB `COMPILE_EA`
enqueue, so no bypass, process stop, or compile enqueue was performed. The stale
binary had already been removed by the inherited remediation; the repaired source
therefore has no current `.ex5`. Review must retain this explicit binary-binding
risk until a governed quiescent compile lane builds the exact source hash above.
No pipeline phase or live authorization is inferred.

## Rollback

Revert this ticket atomically with:

```text
git revert <rework-38006-commit>
```

That restores the prior MQ5, SPEC, three setfile bindings, focused tests, and this
evidence note. It does not alter registries, router history, work items, verdict
rows, factory state, backtests, or live terminals. The inherited remediation
commit `8d64f9c0d` remains outside this rollback.

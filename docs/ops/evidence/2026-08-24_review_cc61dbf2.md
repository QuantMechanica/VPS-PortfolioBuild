# Review — cc61dbf2 Public EA showcase renderer (ops_issue)

- Router task: `cc61dbf2-501a-4b31-bf9a-c1f5404f0004` (task_type `ops_issue`, state REVIEW → APPROVED)
- ToDo: `QM-TODO-20260823-514`
- Worker verdict: `PASS_RENDERER_READY_PUBLICATION_GATED`
- Worker artifact: `docs/ops/evidence/cc61dbf2_public_ea_showcase_renderer_2026-08-23.md`
- Delivered code: `tools/strategy_farm/public_ea_showcase.py` (550 lines, commit `8d5be98ca`),
  tests `tools/strategy_farm/tests/test_public_ea_showcase.py`,
  input `docs/ops/evidence/cc61dbf2_public_ea_showcase_empty_projection_2026-08-23.json`

## Verdict: APPROVED

Standalone, staging-only, fail-closed public renderer for the reduced showcase
projection `qm.public-ea-showcase-projection.v1`. Reproduced read-only against repo/DB;
publication remains OWNER-gated by design (no page published, no MQL5 listing, no
public-data write).

## Verification (reproduced this review)

- `py_compile` renderer + test: OK.
- `pytest test_public_ea_showcase.py test_website_archive_contract.py -q` → **85 passed**
  (matches worker's claim).
- Deterministic render of the empty projection reproduced the worker's exact hashes:
  - `pages=0`, `render_id=8e83fd74d2324692`
  - `index.html` SHA-256 `63464c82d822f1347f7449f75f96ad56a00bbbb13ab6bd4cece97b770a10412a` ✓
  - `manifest.json` SHA-256 `f3375bed72b416f4906478a1a8412a20ceed08a72c1699b9a0394726fb0a576f` ✓
- Negative spot-checks (bite confirmed):
  - output to `public-data/` refused (`public_ea_showcase.py:483-487`);
  - item with unknown internal-rule field (`entry_rules`) rejected via whitelist, not
    silently dropped (`_expect_keys`, `public_ea_showcase.py:107-122`, `_ITEM_ALLOWED:70`);
  - unevidenced numeric copy in number-free field blocked (`_safe_text` number_free,
    `public_ea_showcase.py:147-150`).

## Contract conformity (source-verified)

- Separate renderer with its own field whitelist; never a filter over the internal card;
  reads no Strategy Card / EA source / farm DB / manifest / terminal (module docstring +
  no such imports). Only input is the reduced projection.
- Redaction reuses `website_archive_contract.scrub_text` / `REDACTED`
  (`public_ea_showcase.py:31,139-146`); UUID / internal-EA-ID / legacy-P-phase /
  build-manual regexes reject private locators (`:41-55,141-146`).
- Eligibility fail-closed: all of `in_live_book`, `traded_live`, `marketplace_candidate`,
  `product_ea_ready` must be `true` (bool only) and `rights_status == CLEARED`
  (`:186-206`).
- Mandatory evidence classes GATE / OUT_OF_SAMPLE / COST_MODEL / DRAWDOWN (`:80-85,246-250`);
  backtest/live records rendered under separate headings with explicit
  `BACKTEST — NOT LIVE` / `LIVE — REAL ACCOUNT RECORD` badges (`:406-417`); missing live
  record disclosed, never synthesized (`:434-438`); track record must reference a matching
  `rpt_*` evidence kind (`:276-286`).
- MQL5 URL restricted to clean `https://(www.)mql5.com/en/market/product/<id>` with no
  auth/query/fragment (`:166-183`).
- Staging-only: refuses `public-data`; no publish/git/MQL5/MT5/network action; atomic
  writes into an immutable `render_<id>/` dir (`:483-531`).
- Empty projection is correct current state: no product EA and no per-EA rights clearance
  exist yet (payload `blocked_by`), so `items=[]` and the index states no EA satisfies the
  gates — no candidate guessed, no coverage card leaked.

## Scope / ROT boundary

Deliverable is tooling only; actual publication of any non-empty projection is OWNER
territory (MQL5 product EA + per-EA rights, public-exposure degree). Nothing here touches
gate thresholds, verdicts, the live account, or candidate-pool definition. APPROVED = the
renderer is formally clean; it does not itself authorize any publication.

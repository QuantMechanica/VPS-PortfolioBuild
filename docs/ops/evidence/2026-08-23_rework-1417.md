# rework-1417 review-fix evidence

- Router task: `98a8ca09-c712-4064-866e-cf7112c465d5`
- EA: `QM5_1417_classical-pennant-continuation-h1`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1417_classical-pennant-continuation-h1.md` (`g0_status: APPROVED`, card line 16)
- Review work order: `docs/ops/evidence/98a8ca09_qm5_1417_gemini_build_codex_review_2026-08-22.md`
- Reworked MQ5 SHA-256: `730efcecfa5ec6a043a423cd64fabcd1285c5881e6653f462d67a8a0d2732a20`

## What changed

1. Removed the fabricated `±0.05 * ATR` trendline fallback. A setup now requires at least two detected pivot highs and two detected pivot lows before both regressions can run (`framework/EAs/QM5_1417_classical-pennant-continuation-h1/QM5_1417_classical-pennant-continuation-h1.mq5:577`).
2. Replaced late market entries with the approved `QM_BUY_STOP` / `QM_SELL_STOP` requests and six-H1-bar expiry (`...mq5:623-629`, `...mq5:651-657`). Trigger, SL cap, TP and partial-TP prices are based on the pending trigger price.
3. Corrected CopyBuffer chronology: index 1 is the newer shift-1 SMA and index 0 is the older shift-2 SMA; rising/falling tests now use the card direction (`...mq5:314-319`).
4. Implemented the card's entry-only ±180-minute high-impact-news blackout (`...mq5:775-794`) and placed it after MAE tracking, management, and exits (`...mq5:828-856`). The standard two-axis compliance check remains in the entry path.
5. Split candidate staging from active setup state. Active geometry is committed only after `QM_TM_OpenPosition` returns true (`...mq5:202`, `...mq5:868-871`). The ten-bar reuse lock starts on actual position entry or pending-order invalidation, not request construction.
6. Advanced the fitted upper/lower line projection by bars elapsed since setup and require the close to be inside both boundaries for pattern-failure exit (`...mq5:745-759`).
7. Added the fail-closed H1 / Friday-close runtime execution declaration immediately after framework initialization (`...mq5:809-812`).
8. Updated all 14 scoped backtest setfile `build_hash` headers to the reworked MQ5 hash. Their risk contract remains `RISK_FIXED=1000`, `RISK_PERCENT=0`, `PORTFOLIO_WEIGHT=1` (14/14 grouped result).
9. Added focused semantic regression tests (`tools/strategy_farm/tests/test_qm5_1417_review_rework.py:36-98`) and changed the hardening regression fixture to require fixed QM5_1417 to be clean while QM5_1425 remains detected (`tools/strategy_farm/tests/test_build_gate_hardening.py:696`).

Every declared input has at least one executable use-site; the focused test extracts all `input` declarations and fails on any occurrence count below two (`tools/strategy_farm/tests/test_qm5_1417_review_rework.py:76`). Registry verification found one active EA row and 14 existing active magic rows; no registry or resolver file changed.

## Validation evidence

### Unit tests

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_1417_review_rework.py
....                                                                     [100%]
4 passed in 0.67s

python -m pytest -q \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_d13_pending_order_type_pass_and_market_substitution_fail \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_d14_copybuffer_direction_pass_and_inversion_fail \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_d15_news_duration_and_entry_only_placement_fixtures \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_semantic_predicates_have_real_satisfying_sources \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_semantic_regression_accepts_fixed_1417_and_still_catches_1425
.....                                                                    [100%]
5 passed in 1.10s
```

The full touched hardening module was also run:

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_1417_review_rework.py tools/strategy_farm/tests/test_build_gate_hardening.py
1 failed, 32 passed in 431.77s
```

The sole failure is outside this ticket: `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` at `tools/strategy_farm/tests/test_build_gate_hardening.py:404` reports seven pre-existing findings beginning at `framework/EAs/QM5_41134_wti-mdaily-iqrmean-mom/QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`. No QM5_1417 test failed, and this ticket did not modify QM5_41134.

### Scoped gates

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_1417_classical-pennant-continuation-h1
schema=qm.build-gate-hardening/v1 files_scanned=1 failures=0 warnings=0 symbols_observed=14

python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 framework/EAs/QM5_1417_classical-pennant-continuation-h1
verdict=PASS files_checked=15 findings=[]

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_1417_classical-pennant-continuation-h1
Summary: 1 PASS, 0 FAIL

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_1417_classical-pennant-continuation-h1 --json --fail-on-leak
verdict=SINGLE_SYMBOL_OK n_violations=0
```

`git diff --check` returned exit 0. A direct setfile audit found 14 setfiles, all bound to MQ5 SHA-256 `730efcec...2a20`, with one risk tuple only: `1000, 0, 1` (count 14). The magic registry query was `rg -c '^1417,.*?,active$' framework/registry/magic_numbers.csv` → `14`.

### Compile / build_check infrastructure refusal

The required scoped compile was attempted:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 \
  -EAPath framework/EAs/QM5_1417_classical-pennant-continuation-h1/QM5_1417_classical-pennant-continuation-h1.mq5 \
  -EALabel QM5_1417_classical-pennant-continuation-h1 -Strict
compile_one.result=FAIL
compile_one.reason_class=INCLUDE_MIRROR_REFUSED
```

Durable receipt: `D:/QM/reports/compile/20260824_072102/summary.csv`. It records `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`: terminal64 workers were alive, so the include mirror refused without retry. A separately scoped `build_check.ps1 -EALabel QM5_1417_classical-pennant-continuation-h1 -RepoRoot . -SkipCompile` attempt was refused by the same guard before checks ran. The suggested recovery was a governed compile enqueue; it was not used because this ticket does not authorize additional farm-state writes. Factory state, backtest queues, verdict rows, T_Live, and AutoTrading were not touched.

The existing EX5 is therefore unchanged at SHA-256 `a22a278f4526793299851a1f4557bcaa9ee3fceb411c14d26726e68051830ee9` and does not represent the reworked MQ5. Review must treat fresh compilation as pending infrastructure work, not as a claimed PASS.

## Rollback

Revert the single ticket commit containing this evidence file (`git revert <rework-1417-commit>`). That restores the rejected MQ5 logic, the prior 14 setfile hash headers, and the prior tests. No database, registry, resolver, queue, factory, terminal, or live rollback is required because none was changed.

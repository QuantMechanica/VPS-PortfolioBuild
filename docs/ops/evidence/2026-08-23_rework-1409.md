# rework-1409 review-fix evidence

Date executed: 2026-08-24 (Europe/Berlin)
Router task: `9b0b37f8-087b-436e-a044-8b004c20905f`
EA: `QM5_1409_wyckoff-sign-of-strength-phase-d-h4`

## Work order and disposition

The work order is the blocking-finding list in
`docs/ops/evidence/2026-08-22_codex_review_qm5_1409.md`. The approved card is
`D:/QM/strategy_farm/artifacts/cards_approved/QM5_1409_wyckoff-sign-of-strength-phase-d-h4.md`
with `g0_status: APPROVED`; this is a repairable implementation defect, not
`GENUINE_NO_SIGNAL`.

The router task was claimed with:

```text
python tools/strategy_farm/agent_router.py update-task 9b0b37f8-087b-436e-a044-8b004c20905f --state IN_PROGRESS --verdict "rework burn-window slot 4"
state=IN_PROGRESS updated=true
```

No backtest was enqueued or deleted. Factory/AutoTrading/T_Live and all gate
criteria were untouched. Existing `magic_numbers.csv` allocations were not
modified.

## What changed

- Card-faithful range construction now removes the outer 5% before taking the
  retained-sample 20th/80th percentiles, with explicit bounds, and the declared
  Williams-fractal wing input now gates spring pivots
  (`framework/EAs/QM5_1409_wyckoff-sign-of-strength-phase-d-h4/QM5_1409_wyckoff-sign-of-strength-phase-d-h4.mq5:376`,
  `:428-444`, `:485`).
- The card's high-impact news window is a literal 480/480-minute entry check;
  management and exits run first. MAE is sampled before every early return
  (`...mq5:656`, `:876-910`).
- `QM_EntryRequest` is zeroed and all seven fields are initialized. Partial
  close and break-even now use `QM_TM_PartialClose` and `QM_TM_MoveSL`, with no
  raw `CTrade` management (`...mq5:636-644`, `:778-784`, `:913-928`).
- Pending setup state is published before order send and copied to
  ticket-scoped terminal Global Variables after fill. Range high, measured
  move, TP1 trigger/completion, signal time, and reuse deadline survive
  restart; missing active state closes fail-closed. Entry and LPS invalidation
  both create the durable 80-H4-bar reuse guard (`...mq5:228-294`, `:601-628`,
  `:731`, `:803-804`).
- The EA enforces the approved eight-symbol host/slot scope through the
  framework's existing MagicResolver allocations. `GER40.DWX` is explicitly
  reconciled to canonical `GDAXI.DWX`; per-symbol volume reliability is
  deterministic (`...mq5:105-160`, `SPEC.md:113-134`).
- Six unauthorized setfiles were removed: AUDUSD, NZDUSD, SP500, UK100,
  USDCAD, and USDCHF. The eight retained setfiles bind fixed backtest risk,
  volume reliability/bias, and source hash
  `42c264eb5849a2d7442ac2df2d8e913ff1b281a2b7601c9a7554c78ac7ce3d5d`
  (representative setfile `...EURUSD.DWX_H4_backtest.set:13-22`).
- Focused regression coverage is in
  `tools/strategy_farm/tests/test_qm5_1409_review_rework.py:46-126`.

## Build identity

```text
MQ5 SHA-256  42c264eb5849a2d7442ac2df2d8e913ff1b281a2b7601c9a7554c78ac7ce3d5d
EX5 SHA-256  6fdbf8b1f6c28e3e5f8c9d517106b84d37d8dee0e1f01c27d27b1bb271dcbd3b
EX5 bytes     427490
```

The normal `compile_one.ps1 -Strict` attempt refused before include mirroring
because factory terminal processes were live. Refusal evidence:
`framework/build/compile/20260824_071751/QM5_1409_wyckoff-sign-of-strength-phase-d-h4.compile.log`
and reason class `INCLUDE_MIRROR_REFUSED / LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`.
No terminal was stopped and no compile work item was enqueued.

Syntax/binary validation was then performed in an isolated portable MetaEditor
profile populated from the current worktree framework includes, so no running
terminal include tree was changed. Log:
`C:/Users/Administrator/AppData/Local/Temp/3/qm5_1409_compile_20260824_091932/compile.log`.

```text
Result: 0 errors, 0 warnings, 6893 ms elapsed, cpu='X64 Regular'
```

## Validation output

```text
python framework/scripts/skill_build_ea_guard.py --ea-id 1409 --ea-label QM5_1409_wyckoff-sign-of-strength-phase-d-h4
status=ok; ea_registry_row=true; magic_registry_rows=true; ea_dir_exists=true

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_1409_wyckoff-sign-of-strength-phase-d-h4
Summary: 1 PASS, 0 FAIL

python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_1409_wyckoff-sign-of-strength-phase-d-h4
verdict=PASS; files_checked=9; findings=[]

python -m pytest -q tools/strategy_farm/tests/test_qm5_1409_review_rework.py
6 passed in 0.90s

python -m pytest -q tools/strategy_farm/tests/test_build_guardrails.py
20 passed in 0.96s

python -m pytest -q tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_build_gate_hardening.py -k "d7_ or d9_ or d10_ or d15_ or d17_"
6 passed, 43 deselected in 1.06s
```

The scoped strict hardening command was run exactly for this EA. All source
findings are closed (`source_failure_count=0`): D7 MAE, D9 request, D10 buffer,
and D15 news each report zero failures. Its aggregate result still reports
seven `EA_SYMBOL_NOT_IN_CARD_UNIVERSE` D17 rows solely because the checker
treats every pre-existing active magic allocation as current EA use, including
the six allocations this ticket explicitly forbids changing and the
card-to-house-symbol `GER40.DWX -> GDAXI.DWX` reconciliation. The emitted
setfile universe itself is exactly the approved canonical eight. Changing the
registry or D17 criterion would violate this ticket, so neither was done.

The scoped `build_check.ps1 -EALabel ... -SkipCompile` was also attempted and
fail-closed at its live-factory compile guard before running its checks. The
isolated compile and direct validators above provide the non-mutating build
evidence; the D17 registry/card contradiction remains for reviewer adjudication.

## Rollback

Revert the single `rework(1409)` commit produced by this ticket. That restores
the prior MQ5/EX5/SPEC, restores the six removed setfiles, removes the focused
test and this evidence file, and leaves registries/runtime state untouched.
For a path-scoped manual rollback, restore the EA directory, the focused test,
and this evidence file from the commit's first parent; do not edit the runtime
database or magic registry.

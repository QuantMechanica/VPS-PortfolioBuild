# QM5_9716 bandy-trend-stretch-ratio-mr-index — Claude review closure

Date: 2026-08-24 UTC
Router task: `a06e3ace-6c6a-437e-9a6e-da3e37700a59` (review_ea, gemini/agy build → mandatory Codex/Claude review)
Source build task: `d573fb90-49fa-4f73-87a5-4f01d0254002`
Source verdict (producer): PASS (self-reported)
Prior Codex artifact: `docs/ops/evidence/2026-08-23_qm5_9716_gemini_code_review.md` (REQUEST_CHANGES)

Reviewed against approved card
`framework/EAs/QM5_9716_bandy-trend-stretch-ratio-mr-index/docs/strategy_card.md`
(`g0_status: APPROVED`).

## Verdict: RECYCLE

Card mechanism is faithful, no dead inputs, static forbidden scan clean — but the
delivery breaches the card symbol universe (ROT: universe expansion needs OWNER),
ships a contradictory durable SPEC, gates mandatory exits behind an entry-only
filter, omits the mandatory framework execution contract, and the producer evidence
is not schema-complete. Do not promote to PIPELINE; repair + fresh review required.

## Confirmed defects (file:line)

1. **Symbol universe breach (decisive).** Card `## Target Symbols` = `SP500.DWX`
   (backtest), `NDX.DWX`, `WS30.DWX`, D1 only (card lines 81-82). Delivery ships
   13 symbols across setfiles (`sets/`, 13 files), 13 active magic rows
   (`framework/registry/magic_numbers.csv`, slots 0-12: adds GDAXI, UK100, XAUUSD +
   7 FX pairs), and the SPEC universe. Candidate/universe definition is ROT —
   cannot be expanded without an OWNER card amendment. Restrict to the 3 approved
   targets or obtain amendment.

2. **Mandatory exits gated behind entry filter.** `OnTick` calls
   `if(Strategy_NoTradeFilter()) return;` (mq5:211) *before*
   `Strategy_ManageOpenPosition()` (mq5:214) and `Strategy_ExitSignal()` (mq5:216).
   `Strategy_NoTradeFilter()` (mq5:71) rejects on warmup / invalid quote / wide
   spread, so an open position's 7-day time stop and TSR zero-cross exit are
   suppressed whenever an entry-only condition trips. Friday close (checked earlier)
   is unaffected, but the two card exits are not. Make time-stop management and
   strategy-exit evaluation reachable independently of every new-entry filter.

3. **Execution contract undeclared.** `OnInit` (mq5:182) does a raw
   `_Period != PERIOD_D1` check (mq5:184) then returns `INIT_SUCCEEDED` (mq5:192)
   without calling `QM_FrameworkDeclareExecutionContract(...)`. The framework
   requires this call immediately after `QM_FrameworkInit` for the Card-v2 contract
   (`framework/include/QM/QM_Common.mqh:470-474`); it validates the D1/Friday-close
   contract and emits the machine-searchable `EXECUTION_CONTRACT` record
   (QM_Common.mqh:526-534). Sibling `QM5_9717` shares this gap (class defect in the
   gemini bandy batch). Declare the contract and fail init if it fails.

4. **Durable SPEC contradicts card + source.** `SPEC.md` declares base timeframe
   `H1` (card + mq5 are D1-only, card line 63), states "no strategy-specific inputs"
   while the mq5 exposes 9 (`strategy_*`, mq5:38-46), and advertises the
   unauthorized 13-symbol universe. Regenerate SPEC from the approved card and the
   actual mq5.

5. **Producer build result not schema-complete.**
   `docs/ops/evidence/2026-08-23_QM5_9716_..._build_result.json` claims
   `compile_succeeded`/`build_check_passed` but omits canonical fields
   (`task_id`, `magic_base`, `symbols_registered`, `smoke_result`,
   `smoke_report_path`); no smoke summary / `build_identity.json`. Runtime smoke
   sanity is UNKNOWN. Emit a schema-complete, task-bound result with smoke evidence.

## Checks that passed
- Card mechanism faithful: TSR = (close-SMA(50))/ATR(14), entry `tsr<=-2.5 &&
  close>SMA(200)`, exit `tsr>=0.0`, 7-day time stop, 3.0×ATR catastrophic stop —
  all match card mechanik (mq5 CalculateTSR / Strategy_EntrySignal / Strategy_ExitSignal / Strategy_ManageOpenPosition).
- No dead inputs: all 9 `strategy_*` inputs (mq5:38-46) have use-sites beyond
  declaration.
- Framework wiring present: QM_Common include, `QM_FrameworkTrackOpenPositionMae`
  (MAE hook), `QM_KillSwitchCheck`, `QM_FrameworkHandleFridayClose`, news hook,
  `QM_FrameworkMagic` (ea_id*10000+slot via resolver), RISK_FIXED=1000 / RISK_PERCENT=0.
- Setfiles: 13 files, `RISK_FIXED=1000`, `RISK_PERCENT=0` (per prior Codex scan);
  governed generator lineage. No ML, no raw handles/CopyBuffer/OrderSend/Sleep
  (prior forbidden scan).
- MQ5/EX5 SHA-256 match producer JSON.

## Disposition
Read-only review. No source, binary, registry, setfile, work item, verdict, or
trade stream changed. T_Live/AutoTrading untouched. Verdict RECYCLE with the five
actionable defects above; corrected build + evidence require a fresh review.

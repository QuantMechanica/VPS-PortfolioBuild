# Review: QM5_34003 triple-timeframe-williams-r-champion (Q02 admission)

- Router task: 1ff032e2-8ae5-44ed-9494-9832debee9bb (review_ea, assigned codex, source_agent gemini/agy)
- Source build task: d5a96505-0d9a-4dfa-8b59-119e6b114d61 (build_ea)
- Reviewer: Claude review lane, 2026-08-24 (read-only DB)
- EA dir: framework/EAs/QM5_34003_triple-timeframe-williams-r-champion/
- Card: strategy-seeds/cards/approved/QM5_34003_triple-timeframe-williams-r-champion.md

## Verdict: RECYCLE

Source is clean and card-faithful, but the delivered compiled binary (.ex5) is STALE
relative to the current source. It must be recompiled and setfile build_hash refreshed
before Q02. Recompile is ROT / not the review lane's action.

## What is GOOD (source)
- Card-faithful mechanism: triple-TF Williams %R. Long H4>=-35 AND H1>=-50 AND M15<=-80;
  Short H4<=-65 AND H1<=-50 AND M15>=-20; SL=1.5*ATR(14,M15)[1]; TP=2.5*SL; spread filter
  >1.8*ATR; rollover blackout 23:55-00:05; max 1 open position. Matches card sections 3.1-3.4.
- Dead-input check: all 11 strategy_* inputs have use-sites beyond declaration
  (grep counts >=2 each in the .mq5). No dead inputs.
- Framework conformity in SOURCE: #include <QM/QM_Common.mqh>; magic via QM_FrameworkMagic()
  (ea_id*10000+slot; registry rows 340030000/1/2 correct for EURUSD/GBPUSD/USDCHF .DWX);
  news filter wired (QM_NewsAllowsTrade2 in OnTick); bounded QM_WPR/QM_ATR helpers, no raw
  series; no ML; no invented commission/swap; RISK_FIXED=1000 / RISK_PERCENT=0 in backtest
  setfile (ENV=backtest, risk_mode FIXED), governed-generator header present.
- MAE hook QM_FrameworkTrackOpenPositionMae() present in OnTick (added Aug 23).

## DEFECT (RECYCLE) — stale .ex5, no COMPILE_OK for current source hash
- Current .mq5 sha256 = d276dac1a17a0bb646d2e6cf20b2a0c171cf1053b2b6cd60f644aac5c2ee566b
  (matches build artifact). .ex5 sha256 = 5b4d5c19...d2a6a, compiled 2026-08-17 21:22.
- Commit a040de2b5 (2026-08-23 17:40 "hardening and setfile alignment") edited the .mq5
  AFTER that compile with two functional changes but did NOT recompile:
    1. Added QM_FrameworkTrackOpenPositionMae() to OnTick (MAE hook was absent before).
    2. Renamed OnTradKTransaction -> OnTradeTransaction. The old (compiled) binary carried
       the typo, so MT5 never invoked the trade-transaction handler -> framework
       QM_FrameworkOnTradeTransaction accounting was unwired in the Aug-17 .ex5.
- Only compile logs are framework/build/compile/20260817_1921*/ (both Aug 17, OLD source,
  0 errors 0 warnings). No compile artifact newer than 2026-08-23 for this EA
  (find framework/build -newermt 2026-08-23 -iname *34003* -> empty).
- Setfiles pin build_hash: 5b4d5c19... = the stale Aug-17 binary.
- Consequence: the .ex5 that would enter Q02 is the pre-hardening binary (no MAE hook,
  trade-transaction handler unwired). build_check_passed/compile_succeeded=true in the
  artifact refer to the superseded Aug-17 source, not to current hash d276dac1.

## Required actions before re-review
1. Recompile QM5_34003 from current source (sha d276dac1) in an OFF-window / COMPILE_EA
   queue; capture a COMPILE_OK log dated after 2026-08-23.
2. Regenerate the three backtest setfiles via gen_setfile.ps1 so build_hash matches the
   fresh .ex5.
3. Resubmit for review with the refreshed .ex5 + compile log.

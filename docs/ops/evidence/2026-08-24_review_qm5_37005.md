# Claude review: QM5_37005 chan-bollinger-adx-mean-reversion (re-review)

- Review task: `cf69bca5-0d11-498c-acfa-19c602f1b5f6` (`review_ea`, assigned codex, closed by Claude review lane)
- Source build task: `98fb1997-3c5d-4bfa-af26-6f46dd7c8a1b` (`build_ea`, producer `gemini`/agy)
- Source artifact: `C:/QM/repo/artifacts/builds/98fb1997-3c5d-4bfa-af26-6f46dd7c8a1b.json`
- Approved card: `strategy-seeds/cards/approved/QM5_37005_chan-bollinger-adx-mean-reversion.md`
- MQ5 SHA-256 (current, tracked): `12dd519d1493d732d093e7d613dde5c6c63c5ff7fe0102037e4a61f1aa754e1f`
- EX5 SHA-256: `24552679e230ad968363a8afd13972d880a422ea74eb279869f8fdbc31ab7709`
- **Verdict: RECYCLE** — named actionable card-fidelity defects; remain out of pipeline.

## Context
The reviewed source is byte-identical to the prior independent Codex review
`8d44760b` (2026-08-18, CHANGES_REQUIRED). Since then only the identity was
cured: the MQ5/EX5/SPEC are now git-tracked (pump auto-commit `bfd467bc6` et al.),
resolving prior finding #3. The two mechanism findings below were never fixed
because the source was not changed. Independently confirmed by re-reading the file.

## Findings (confirmed)

### 1. HIGH — silent 1R TP substitution deviates from card midline exit
`QM5_37005_...mq5:149` and `:158`:
`req.tp = (g_cached_bb_middle > ask) ? g_cached_bb_middle : (ask + sl_dist);`
`req.tp = (g_cached_bb_middle < bid) ? g_cached_bb_middle : (bid - sl_dist);`
Card §3.4 authorizes exactly one TP: the 20-period SMA midline. When the midline
is not beyond the live quote (price already crossed the midline in the current
bar), the code silently converts the exit to a fixed 1R payoff (entry ± SL
distance) — a mechanism the card never authorizes. Trades taken in this branch
measure a different strategy than the card at Q02. Fix: reject the entry when the
midline target is not beyond the entry quote (mean reversion already realized),
or obtain an OWNER-approved fallback rule. Card fidelity must hold before Q02.

### 2. MEDIUM — card loss-limit / no-trade contract not implemented
Card §3.1.3 declares a daily realized-loss ≥2.0% entry halt as a no-trade
condition; §4.2 declares a 2.5% daily hard stop and 5.0% total-DD stop. The EA
implements the spread filter (`:101-104`), rollover blackout (`:93`) and
max-1-position (`:119`), but no daily-loss no-trade gate and no card-specific DD
stops (it relies on the generic framework kill-switch). Either wire the declared
daily-loss no-trade condition, or record an OWNER decision that these prop
overlays are framework-delegated and strike them from the card (consistent with
the g0 note that unevidenced source claims are discounted).

## Conformity checks that PASS (not blocking)
- No dead inputs: all `strategy_*` inputs have use-sites beyond declaration
  (bb_period/dev :69-71; adx_period :68; max_adx :126; atr_period :72;
  sl_atr_mult :139; spread_atr_mult :101; max_spread_points :103-104).
- Framework chain: QM_Common/QM_Signals included; MAE hook `QM_FrameworkTrackOpenPositionMae` :217;
  news filter wired :247-253; O(1) per-tick behind `QM_IsNewBar` :255-258;
  bounded QM_* indicator buffers, no raw series abuse, no ML, no invented cost values.
- Risk mode: all three backtest setfiles `RISK_FIXED=1000`, `RISK_PERCENT=0`; EA
  defaults match (`:27-28`).
- Magic: registry rows `370050000/1/2` = ea_id*10000+slot, collision-free, active;
  MagicResolver regenerated.
- Build evidence for current hash: compile PASS, build_gate_hardening PASS (D1-D18),
  validate_spec PASS, dispatch gate PASS (per `98fb1997` artifact + build evidence).

## Actions
Read-only review (DB mode=ro). No source, registry, binary, terminal, AutoTrading,
or pipeline state changed. Router close-review is the only sanctioned write.

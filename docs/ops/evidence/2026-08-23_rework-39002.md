# rework-39002 review-fix evidence

- Ticket: `rework-39002`
- Router task: `70395b6d-ee55-4906-bb55-91d75157213b`
- EA: `QM5_39002_forexfactory-sonic-r-system`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_39002_forexfactory-sonic-r-system.md`
- Worktree branch: `rework-slot-5`
- Scope: review repair only; no backtest enqueue/delete, verdict overwrite, factory toggle, gate change, magic allocation change, or `T_Live` access

## Review findings closed

1. **Break-even management reachability.** Open-position admission is confined to
   `Strategy_EntrySignal` (`.mq5:183-185`), while `OnTick` calls
   `Strategy_ManageOpenPosition` before the entry-only no-trade filter
   (`.mq5:364`, `.mq5:386`). The no-trade hook no longer blocks management.
2. **Loss-limit contract.** The 2.0% realized daily entry halt is evaluated at
   `.mq5:139-147`; the 2.5% daily hard stop, 5.0% total-DD threshold, and 0.5%
   per-trade cap are passed to the framework kill switch at `.mq5:328-332`.
3. **Dragon stop boundary.** Long and short SLs remain outside the respective
   outer Dragon edge plus the exact buffer at `.mq5:196-218`; the rejected
   0.5/3.5 ATR distance clamp is absent.
4. **Raw-series strict gate.** The only three direct closed-bar OHLC reads are
   bounded O(1), cached per M15 bar (plus initialization), and now carry the
   required `perf-allowed` review annotation at `.mq5:111-113`. EMA/ATR access
   remains through `QM_EMA`/`QM_ATR` at `.mq5:117-120`; there are no direct
   indicator handles or `CopyBuffer` calls.
5. **Durable identity/regression coverage.** This commit binds the reviewed
   source closure to six static tests in
   `tools/strategy_farm/tests/test_qm5_39002_rework_static.py:38-138`.

The prior semantic remediation already present on this branch is commit
`e56c2c8b9` (`build(ea): remediate QM5_39002 card contract`). This ticket
commit adds the missing strict-gate closure and regression coverage without
changing the approved entry/exit mechanics.

## Framework and input audit

- `QM_Common` chain: `.mq5:5`, framework initialization `.mq5:302-317`.
- MAE hook: `.mq5:349`.
- Magic: resolved through `QM_FrameworkMagic`; no raw magic arithmetic in the EA.
- Risk modes: all three backtest setfiles bind `RISK_FIXED=1000` and
  `RISK_PERCENT=0`; the source declares and forwards both risk modes.
- Registry: `framework/registry/ea_id_registry.csv:4481`; active magic rows
  `framework/registry/magic_numbers.csv:17508-17510`. No registry/resolver edit
  was needed.
- All 30 declared inputs have at least one non-declaration use-site. The `rg`
  audit returned minimum count 2 (`RISK_PERCENT`, `RISK_FIXED`,
  `PORTFOLIO_WEIGHT`, news stale/impact, Friday-close, and RNG inputs); the
  regression assertion is at `test_qm5_39002_rework_static.py:102-116`.
- No ML tokens or APIs are present; the static assertion is at
  `test_qm5_39002_rework_static.py:130-131`.
- SPEC terminology now names the two current news axes and the real risk gate
  `EA_RISK_SIZER_UNCONFIGURED` (`SPEC.md:52-56`, `SPEC.md:115-119`); M15 bar
  gating is documented at `SPEC.md:79`.

## Validation evidence

Commands ran from `C:/QM/worktrees/rework-slot-5` on 2026-08-24.

```text
python -m pytest tools/strategy_farm/tests/test_qm5_39002_rework_static.py -q
...... [100%]
6 passed in 0.70s

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_39002_forexfactory-sonic-r-system
files_scanned=1 failures=0 warnings=0

python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 framework/EAs/QM5_39002_forexfactory-sonic-r-system
files_checked=4; findings=[]; verdict=PASS

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_39002_forexfactory-sonic-r-system
Summary: 1 PASS, 0 FAIL (of 1)

python framework/scripts/skill_build_ea_guard.py --ea-id 39002 --ea-label QM5_39002_forexfactory-sonic-r-system
status=ok; ea_registry_row=true; magic_registry_rows=true; ea_dir_exists=true
```

The touched analyzer's complete pytest module was also run:

```text
python -m pytest tools/strategy_farm/tests/test_qm5_39002_rework_static.py tools/strategy_farm/tests/test_build_gate_hardening.py -q
1 failed, 34 passed in 333.12s
```

The sole failure is an unrelated pre-existing global census assertion for
seven unbounded buffers in `QM5_41134_wti-mdaily-iqrmean-mom.mq5`, first
reported at its source line 599. Ticket scope forbids changing another EA;
the EA-scoped hardening run above is clean.

### Compile / authoritative build-check interlock

Both scoped commands were attempted and refused before compilation or static
scanning because live factory `terminal64` processes were present:

```text
pwsh -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_39002_forexfactory-sonic-r-system/QM5_39002_forexfactory-sonic-r-system.mq5 -Strict
result=FAIL; reason_class=INCLUDE_MIRROR_REFUSED;
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED

pwsh -File framework/scripts/build_check.ps1 -EALabel QM5_39002_forexfactory-sonic-r-system -SkipCompile
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED
```

The durable refusal row is
`D:/QM/reports/compile/20260824_092110/summary.csv`. The interlock explicitly
requires a governed compile work item while the factory is active. This ticket
does not authorize a state-DB compile enqueue, and stopping factory terminals
is forbidden, so no bypass/retry was performed. The current source delta is
comments-only in the `.mq5`; executable strategy semantics remain those in the
previous committed remediation.

## Rollback

After the commit hash is known, revert this ticket atomically with:

```text
git revert <rework-39002-commit>
```

That removes the three strict-gate annotations, SPEC corrections, focused
tests, and this evidence file. It does not alter registries, router history,
work items, verdict rows, factory state, backtests, or live terminals. The
earlier semantic remediation `e56c2c8b9` is deliberately outside this rollback
and must not be reverted without a separate OWNER-scoped decision.

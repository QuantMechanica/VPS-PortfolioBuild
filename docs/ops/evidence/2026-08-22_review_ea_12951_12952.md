# review_ea — QM5_12951 and QM5_12952 — 2026-08-22

Router tasks `6f392d1e-f1d8-402c-8d33-37d6661ab14e` (12951) and
`2c43671f-aabb-4ca5-8cf8-857505375ce2` (12952), both `review_ea`/claude,
`reason=codex_review_required_for_gemini_code` (gemini/agy-authored build_ea, hard
rule: Codex review still mandatory before acceptance — this task closes claude's
static review only, does not itself move either task past REVIEW).

Static/evidence-based review only — factory has live terminal64 processes (T1-T10
active), so `build_check.ps1` is fail-closed refused
(`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`, correct per hard rule not to interrupt active
backtests / never start terminal64 manually). Relied on the build task's captured
`compile.log` (0 errors/0 warnings, both) plus the three validators that do not touch
the live terminal.

## Overall verdict

Both EAs are card-faithful, fully wired, and controls-clean. **REVIEW (hold for Codex)**
for both — per hard rule, gemini-authored code cannot be self-approved/moved to
PIPELINE by claude regardless of review outcome. No blocking defects found.

---

## QM5_12951_mql5-chaikin-zero-card

### Reviewed identity

- SPEC: `framework/EAs/QM5_12951_mql5-chaikin-zero-card/SPEC.md`
- MQ5 SHA-256: `c55ab571f66cf5eee351917cee26982dd06bb413852e9fc1f661262198e0d4fe`
- EX5 SHA-256: `aa7985bfb3154635a3254db7165d449c4bc0d7d6fad4fc846529c2baa80865e1`
- EX5 mtime 10:47 > MQ5 mtime 10:40 — binary not stale against source.
- Source citation: MQL5 article 11242, "Learn how to design a trading system by
  Chaikin Oscillator" (Mohamed Abdelmaaboud, 2022-07-28).

### Card fidelity

Chaikin Oscillator (ADL smoothed by EMA3/EMA10 on tick volume) zero-cross, filtered by
EMA(100) trend direction and an ATR(14)>=50%*ATR(100) volatility gate, on closed H1
bars. Long: `close>EMA100 && osc_prev<=0 && osc_now>0`; short is the mirror. Exit on
opposite Chaikin cross or EMA-side flip, plus a 36-bar failsafe time stop. SL =
entry -/+ 1.7*ATR(14), TP = 2.0R. `Strategy_ManageOpenPosition` is empty — no
trailing/break-even/partial/grid/martingale/ML was added; matches the SPEC's stated
mechanics exactly. `Strategy_ReadChaikinOsc` walks the copied-rates window
oldest-to-newest, recursive EMA of the accumulation/distribution line — algebra
verified by hand, no off-by-one against "previous vs current" osc values.

### Input-use audit

All 10 strategy inputs grep to >=2 occurrences (declaration + real use), no dead
parameters: `strategy_chaikin_fast_ema`(3), `strategy_chaikin_slow_ema`(6),
`strategy_chaikin_warmup_bars`(2), `strategy_ema_period`(3), `strategy_atr_period`(3),
`strategy_atr_filter_period`(2), `strategy_atr_filter_ratio`(2),
`strategy_atr_sl_mult`(2), `strategy_rr_target`(2), `strategy_max_hold_bars`(2).

### Host / magic / risk / time controls

- `ea_id_registry.csv`: `12951,mql5-chaikin-zero-card,...,active` (single row).
- `magic_numbers.csv`: 3 active slots (0=EURUSD.DWX, 1=GBPUSD.DWX, 2=XAUUSD.DWX),
  magic `129510000+slot` — matches `ea_id*10000+slot` formula.
- SPEC "Designed for" declares 3 symbols; 3 `.set` files shipped; 3 magic slots — no
  count mismatch. Each setfile's `qm_magic_slot_offset` matches its symbol's registry
  slot (0/1/2).
- All 3 backtest setfiles: `RISK_FIXED=1000`, `RISK_PERCENT=0`, ENV=backtest.
- No custom DST/session math; broker time only via framework helpers.
- `QM_KillSwitchCheck()` gate at top of OnTick; framework wiring (OnInit/OnTick/
  OnTimer/OnTradeTransaction/OnTester) is the unmodified template — byte-identical
  structure to other passing EAs in this cohort.

### Focused verification

- `validate_spec_doc.py framework/EAs/QM5_12951_mql5-chaikin-zero-card` → **PASS**.
- `validate_symbol_scope.py --ea-label QM5_12951_mql5-chaikin-zero-card --fail-on-leak`
  → `SINGLE_SYMBOL_OK`, 0 violations.
- `validate_build_guardrails.py <mq5>` → **PASS**, `max_news_stale_hours=336` (at the
  336-hour ceiling, not above it), 0 findings.
- `skill_build_ea_guard.py --ea-id 12951 --ea-label mql5-chaikin-zero-card` → status ok
  (`ea_registry_row` and `magic_registry_rows` true; `ea_dir_exists=false` is a known
  false negative — the guard checks `EA_ROOT/<label>` without the `QM5_<id>_` prefix,
  the directory does exist under its full canonical name).
- Build task's `compile.log`: 0 errors, 0 warnings, 8561ms.
- `build_check.ps1 -Strict -SkipCompile` refused ad-hoc
  (`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` — factory terminal64 processes are live);
  not run, correctly fail-closed rather than interrupting T1-T10.

**Verdict: card-faithful, controls-clean, no blocking defects — held in REVIEW for
mandatory Codex review per hard rule (gemini-authored code).**

---

## QM5_12952_mql5-force-ema-card

### Reviewed identity

- SPEC: `framework/EAs/QM5_12952_mql5-force-ema-card/SPEC.md`
- MQ5 SHA-256: `f891223db2dc839f5f64a1645735afb884d9e757f05116c28e3ce3c69b40cce0`
- EX5 SHA-256: `516597a261863fa8db4879431c09306ab6b1fd62010493c6ce41d79b98178f56`
- EX5 mtime 10:45 > MQ5 mtime 10:38 — binary not stale against source.
- Source citation: MQL5 article 11269, "Learn how to design a trading system by Force
  Index" (Mohamed Abdelmaaboud, 2022-08-03).

### Card fidelity

Force Index(13, EMA-smoothed, tick volume) zero-cross filtered by EMA(100) trend and
the same ATR(14)>=50%*ATR(100) volatility gate, closed H1 bars. Long:
`close>EMA100 && force_prev<0 && force_now>0`; short mirrors. Exit on opposite Force
cross or EMA-side flip, plus 36-bar time stop. SL = entry -/+ 1.6*ATR(14), TP = 2.0R.
`Strategy_ManageOpenPosition` empty, matching the SPEC. `Strategy_ReadForceEma` uses
`ArraySetAsSeries` + a `target_shift=1` walk from `oldest_shift=copied-2` down to
`target_shift`; `force_previous` is captured one bar older than `force_current`
(shift = target_shift+1, which is chronologically *before* shift=1 under
series-indexing) — the "previous crosses to current" semantics are correct, and the
raw-force formula `tick_volume[shift]*(close[shift]-close[shift+1])` computes
close-now-minus-close-before in chronological terms, i.e. the standard Force Index.
Hand-verified, no off-by-one.

### Input-use audit

All 8 strategy inputs grep to >=2 occurrences: `strategy_force_period`(5),
`strategy_ema_period`(3), `strategy_atr_period`(3), `strategy_atr_filter_period`(2),
`strategy_atr_filter_ratio`(2), `strategy_atr_sl_mult`(2), `strategy_rr_target`(2),
`strategy_max_hold_bars`(2). No dead parameters.

### Host / magic / risk / time controls

- `ea_id_registry.csv`: `12952,mql5-force-ema-card,...,active` (single row).
- `magic_numbers.csv`: 3 active slots (0=EURUSD.DWX, 1=USDJPY.DWX, 2=GER40.DWX),
  magic `129520000+slot`.
- SPEC declares 3 symbols; 3 `.set` files shipped; 3 magic slots — no mismatch. Each
  setfile's `qm_magic_slot_offset` matches its symbol's registry slot (0/1/2).
- All 3 backtest setfiles: `RISK_FIXED=1000`, `RISK_PERCENT=0`, ENV=backtest.
- No custom DST/session math; broker time only via framework helpers. Framework
  wiring unmodified template, identical structure to 12951 and other passing EAs.

### Focused verification

- `validate_spec_doc.py framework/EAs/QM5_12952_mql5-force-ema-card` → **PASS**.
- `validate_symbol_scope.py --ea-label QM5_12952_mql5-force-ema-card --fail-on-leak`
  → `SINGLE_SYMBOL_OK`, 0 violations.
- `validate_build_guardrails.py <mq5>` → **PASS**, `max_news_stale_hours=336`, 0
  findings.
- `skill_build_ea_guard.py --ea-id 12952 --ea-label mql5-force-ema-card` → status ok
  (same `ea_dir_exists` false-negative as above, non-blocking).
- Build task's `compile.log`: 0 errors, 0 warnings, 11470ms.
- `build_check.ps1 -Strict -SkipCompile` refused ad-hoc
  (`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED`); not run, correctly fail-closed.

**Verdict: card-faithful, controls-clean, no blocking defects — held in REVIEW for
mandatory Codex review per hard rule (gemini-authored code).**

---

## Summary line for the router

Both 12951 (Chaikin zero-cross) and 12952 (Force Index/EMA) are card-faithful,
fully-wired, magic/slot/risk-mode clean, compile clean (0/0 from build task log),
`validate_spec_doc`/`validate_symbol_scope`/`validate_build_guardrails` all PASS.
`build_check.ps1` itself could not be re-run live (factory terminal64 processes
active, correctly fail-closed) — relied on captured build-task compile evidence
instead. No defects found in either. Per hard rule, gemini-authored code requires
Codex review before acceptance — both left in REVIEW, not self-approved or advanced
to PIPELINE.

# Review — ops_issue b24d7875 (report.htm retention policy)

**Reviewer:** Claude (review lane), 2026-08-23
**Task:** `b24d7875-1417-4e75-8452-76e8e9df51ea` (`QM-TODO-20260823-506`)
**Type:** ops_issue — "Report retention policy: MT5 report.htm has an accidental ~6 week half-life"
**Verdict:** APPROVED

## What was reviewed

The task's "actual problem" was: no retention DECISION existed for `report.htm`; the
artifacts survived by accident (only `*.log` was purged) and vanished by accident
(one-time manual disk reclaims during the D: crises removed whole trees). The delivered
resolution is a policy decision plus an implementing job.

## Claims verified (read-only)

- **Automated purges delete only `*.log`.** Confirmed in source:
  `tools/strategy_farm/reports_log_purge.ps1:19` ("KEEPS: every .htm / .json / .set / .ini")
  and `tools/strategy_farm/prune_workitem_logs.py:8-11` (deletes `*.log`, keeps
  summary.json/report.htm/etc). Matches the payload's `automated_purges_delete_only_logs`.
- **Retention decision now exists.** `decisions/DL-090_backtest_report_retention_policy.md`
  (Status ADOPTED, OWNER-authorized 2026-08-23, "B' + C"). Rule §2: keep PASS-family
  runs + newest standing rejection per (ea_id, symbol, gate) indefinitely; age out
  superseded non-PASS + infra/invalid at >=30d; compress the kept set at >=30d. This
  matches the payload's own recommendation "(b) plus (c)".
- **Implementing job committed.** `tools/strategy_farm/report_retention_purge.py`
  (commit `6fc367326`). Fail-closed per DL-090 §4, verified in source:
  - `FORBIDDEN_PREFIXES` (report_retention_purge.py:76-79) rejects `C:/QM/mt5/T_Live`,
    `D:/QM/reports/state`, `decisions/`; candidates root-scoped to
    `D:/QM/reports/work_items` (`_run_dir_for_evidence`, line 111-114).
  - Dry-run by default; `quarantine()`/`compress_kept()` mutate only when `execute=True`
    (lines 253, 388-390); `--classify-only` never touches the filesystem.
  - Quarantine-before-delete: `quarantine()` moves dirs; a separate `reap_quarantine()`
    is the only unlink path. `MIN_AGE_DAYS_DEFAULT=30`, `QUARANTINE_REAP_DAYS_DEFAULT=30`.
  - Standing rejection recomputed from a live query each run (never cached).
- **Tests green.** `python -m pytest tools/strategy_farm/tests/test_report_retention_purge.py -q`
  -> 12 passed.
- **Evidence doc present and honest.**
  `docs/ops/evidence/2026-08-23_report_retention_purge_implementation.md` records a
  dry-run classification against the live DB (kept 8052/10936 rows, age-out 2884, no
  filesystem mutation), the one bug caught (report lives at `raw/run_NN/report.htm`,
  fixed with rglob), and explicitly did NOT run `--execute`.

## Scope / hard-rule check

No verdict mutated, no threshold changed, no enqueue, no Factory_OFF, no recompile, no
T_Live touched. The `.log` disk-pressure purges are unchanged. All within GRUEN/scope.

## Follow-up (must be commissioned as a router task, not just noted)

The first `--execute` run and installation of a scheduled task
(e.g. `QM_StrategyFarm_ReportRetention`, mirroring `reports_log_purge.ps1`'s installer)
were deliberately deferred for second-eyes on the live-run numbers. This is correct
handling, not a defect — but per the binding rule it must exist as its own `agent_tasks`
row (Codex lane) before the policy actually protects any evidence. Recommend the
orchestrator enqueue it.

## Verdict

APPROVED. The ops issue is resolved: a written, OWNER-ratified retention decision
(DL-090) now governs `report.htm`, and a fail-closed, dry-run-default, tested implementing
job is committed. The archive detail page can now promise an evidence trail the policy
controls, once the deferred first `--execute` + scheduling follow-up is commissioned.

# QM5_10269 diverse-FX Q02 recovery enqueue

Date: 2026-09-10 UTC

Branch: `agents/board-advisor`

Outcome: `AUDUSD.DWX D1 Q02 SUCCESSOR ENQUEUED AND CLAIMED`

## Selection and coordination

The live approved-card and registry preflight had no collision-free,
low-frequency diversity build ready for a new Q01 implementation. The remaining
registry-ready source gaps were intraday/high-turnover cards, while the newer FX
cards still lacked their governed magic precondition.

This paced unit therefore continued the existing router-owned priority-2 repair
for `QM5_10269_gawd-wma30-trend`, task
`2cd23795-b866-4ba8-b1c0-8c7d13ef5fcf`. The task was moved from `REVIEW` to
`IN_PROGRESS` before mutation. Its D1 universe includes seven FX instruments;
the current unit advances the exact AUDUSD cell that previously ended
`INFRA_FAIL` during `OnInit` because the old binary lacked the registered magic
slot.

The governed rebuild was already complete:

- compile work item: `3a92321c-deb2-45a0-872e-c39d8602d5cb`;
- compile verdict: `COMPILE_OK` with strict build check PASS;
- MQ5 SHA-256:
  `ba1bdd99f02957c30d854a243aa870fdbda719cdee8fc0d76b8d788a1c0b1801`;
- EX5 SHA-256:
  `284d46702d34032f95322197fed9295d6e4296ffd387da49c5aa76b7961aecf6`.

## Mandatory PACER guard

Before the Q02 mutation, the exact current source was audited with:

```text
python C:/QM/repo/tools/strategy_farm/audit_framework_input_pins.py --check-source C:/QM/repo/framework/EAs/QM5_10269_gawd-wma30-trend/QM5_10269_gawd-wma30-trend.mq5
```

Result: `ok=true`, `predicate=EA_FRAMEWORK_INPUT_PINNED`, `hit_count=0`, exit
code 0. The AUDUSD backtest setfile retains `RISK_FIXED=1000`,
`RISK_PERCENT=0`, and `qm_magic_slot_offset=4`.

## Dry-run and CPU admission

The exact append-only `farmctl requalify-q02` dry-run against predecessor
`9a0ced77-9871-4368-b153-e2a877b4a750` returned:

- `eligible=true`;
- `would_enqueue=true`;
- `parameter_change_count=0` and an empty parameter diff;
- current setfile SHA-256
  `e5bfc739941e8cb81de7e023ab768471ddbf2c91e12f5e36472da341a16e921d`;
- current EX5 SHA-256 equal to the sealed `COMPILE_OK` hash above.

The fresh five-sample whole-host CPU window was `83.0267`, `83.1157`,
`76.3698`, `73.3427`, and `77.7479` percent. Average CPU was `78.7206%` and
maximum CPU was `83.1157%`, both below the binding `97%` ceiling.

## Append-only result

The same command with `--apply` appended successor work item
`e8b6ace9-17de-4975-863a-1813bae9b783` for `AUDUSD.DWX`, `D1`, `Q02`.
The historical `INFRA_FAIL` predecessor is preserved. The receipt is:

```text
D:\QM\strategy_farm\artifacts\receipts\q02_post_binding_requalification\9a0ced77-9871-4368-b153-e2a877b4a750_e8b6ace9-17de-4975-863a-1813bae9b783.json
SHA-256 be6423a02582aa869e133b12602ac64115cd65d249af5d107ee041c0c64360ba
```

At verification, the successor was `active` and claimed by factory terminal
`T4`. No strategy parameter, EA source, registry, portfolio gate, deploy
manifest, `T_Live` file, or AutoTrading state was changed by this unit.

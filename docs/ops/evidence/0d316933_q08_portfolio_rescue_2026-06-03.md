# Q08 Portfolio Rescue Evidence - 0d316933

## Scope
- Implemented in `C:/QM/repo` because `C:/QM/worktrees/codex-orchestration-1` is still at `53bbadf59` and does not contain `framework/scripts/q08_davey/aggregate.py` or `docs/ops/Q08_PORTFOLIO_RESCUE_PLAN_2026-06-03.md`.
- Preserved Q-only phase naming in code paths. No T_Live, AutoTrading, terminal launch, or pipeline verdict fabrication.

## Changes
- `framework/scripts/q08_davey/aggregate.py`
  - Added Q08 failure taxonomy: `LOW_SAMPLE`, `EDGE_SOFT`, `EDGE_HARD`.
  - Emits `PASS`, `FAIL_SOFT`, `FAIL_HARD`, or `INVALID` plus `verdict_classification`.
  - Locked calibration constants: `N_SEASON=3`, `CHOP_SOFT=0.90`, `PBO_HARD=55.0`.
- `tools/strategy_farm/farmctl.py`
  - Preserves `FAIL_SOFT`, `FAIL_HARD`, `PASS_PORTFOLIO`, `FAIL_PORTFOLIO`, and `NEED_MORE_DATA`.
  - Fixes Q08 `INVALID` so it no longer collapses to strategy `FAIL`.
  - Adds `Q09_PORTFOLIO` runner wiring and aggregate artifact discovery.
  - Routes Q08 `FAIL_SOFT` with at least 30 trades to pending `Q09_PORTFOLIO`.
  - Parks low-sample Q08 `FAIL_SOFT` rows as `Q09_PORTFOLIO` / `NEED_MORE_DATA`.
  - Inserts `PASS_PORTFOLIO` rows into `portfolio_candidates` as `Q12_REVIEW_READY` with `portfolio_only` payload metadata.
  - Manual cascade enqueue now routes `Q09_PORTFOLIO` from Q08 `FAIL_SOFT` and Q10 from Q08 `PASS`.
- `tools/strategy_farm/portfolio/portfolio_q08_contribution.py`
  - Adds Q09 portfolio contribution evaluation wrapper around existing portfolio admission logic.
  - Emits monthly returns, equity curve, and `PASS_PORTFOLIO` / `FAIL_PORTFOLIO` / `NEED_MORE_DATA`.
- `framework/scripts/q09_portfolio.py`
  - Direct script entrypoint for the Q09 portfolio runner.
- `tools/strategy_farm/tests/test_verdict_taxonomy_ws2.py`
  - Adds focused coverage for Q08 invalid handling, split verdict preservation, taxonomy classification, Q08 soft-fail promotion, and Q09 portfolio admission.

## Verification
- `python -m unittest tools.strategy_farm.tests.test_verdict_taxonomy_ws2`
  - PASS: 10 tests.
- `python -m py_compile framework/scripts/q08_davey/aggregate.py framework/scripts/q09_portfolio.py tools/strategy_farm/farmctl.py tools/strategy_farm/portfolio/portfolio_q08_contribution.py tools/strategy_farm/tests/test_verdict_taxonomy_ws2.py`
  - PASS: no syntax errors.

## Notes
- This task is left for REVIEW only. Claude review and landing to main remain required.
- Existing unrelated dirty files in both worktrees were not reverted.

# rework-35003 — QM5_35003 review repair evidence

**Router task:** `ae389d95-9df7-403d-8a0d-6da62c6e52e7`  
**EA:** `QM5_35003_three-ducks-multi-timeframe-system`  
**Card:** `D:/QM/strategy_farm/artifacts/cards_approved/QM5_35003_three-ducks-multi-timeframe-system.md` (`g0_status: APPROVED`)  
**Review work order:** `docs/ops/evidence/ae389d95_qm5_35003_gemini_build_codex_review_2026-08-18.md`  
**Scope:** source, SPEC, four existing backtest sets, regression tests, and this evidence file only. No registry, factory, backtest queue, verdict, or live-terminal mutation.

## What changed

1. The 23:55-00:05 rollover admission window now converts Darwinex broker time with `QM_BrokerToUTC` before evaluating GMT (`framework/EAs/QM5_35003_three-ducks-multi-timeframe-system/QM5_35003_three-ducks-multi-timeframe-system.mq5:199`).
2. Initial stops are exactly completed M5 SMA(60) minus/plus the card's three-pip buffer (`...mq5:294`, `...mq5:313`). The invented 0.5-3.5 ATR clamp and 20-pip trailing fallback were removed; invalid stop direction fails closed.
3. The exact card loss contracts are declared and wired: 2.0% account-wide realised-loss entry halt, 2.5% daily hard drawdown stop, and 5.0% total drawdown stop (`...mq5:44-46`, daily history/account-balance reconstruction at `...mq5:100-143`, hard-stop enforcement at `...mq5:165-186`). The framework kill switch is reinitialized with the exact 2.5/5.0 values (`...mq5:446-452`).
4. Open-position management and explicit exits run before entry-only rollover/spread/loss gates (`...mq5:484-510`). The MAE hook remains first and before the kill switch (`...mq5:484-485`).
5. Backtest risk defaults now follow the V5 contract (`RISK_PERCENT=0`, `RISK_FIXED=1000`), and the card's three-tick market-order slippage cap is passed through `QM_EntryConfigure` (`...mq5:461-467`). Magic remains resolver-derived through `QM_FrameworkMagic`; the four existing active registry rows were not changed.
6. All four existing M5 backtest sets now carry the three loss-limit inputs and a source-bound `build_hash` of `76c984656713c7f6dbc70f42d3cde8bf2bec64e73e5f9b508cea816627b3eef2` (each set: header line 13 and assignments lines 28-30).
7. Static regression coverage is in `tools/strategy_farm/tests/test_qm5_35003_review_rework_static.py:68-181`. It checks approved-card identity, all five review repairs, every strategy input's non-declaration use site, bounded/perf-allowed series access, framework/risk/magic/MAE conformance, registry identity, and all four set contracts.

## Input wiring census

The source census asserted every declared input has at least one non-declaration use. Counts ranged from 2 to 7; the strategy inputs were: SMA 7, swing lookback 4, SL buffer 4, TP/RR 6, ATR period 3, spread multiplier 3, daily realised loss 3, daily hard stop 5, and total drawdown stop 4.

Registry preflight (read-only):

- `framework/registry/ea_id_registry.csv:4450` — active `35003,three-ducks-multi-timeframe-system`.
- `framework/registry/magic_numbers.csv:17408-17411` — active slots 0-3 for EURUSD/GBPUSD/AUDUSD/USDJPY with deterministic magics 350030000-350030003.

## Validation evidence

### PASS — focused unit and hardening tests

```text
python -m pytest tools/strategy_farm/tests/test_qm5_35003_review_rework_static.py \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_d2_loss_limit_matching_and_mismatching_fixtures \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_d4_management_before_guard_passes_and_early_open_guard_fails \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_d5_raw_broker_hour_fails_and_utc_conversion_passes \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_d7_framework_mae_hook_pass_and_fail_fixtures \
  tools/strategy_farm/tests/test_build_gate_hardening.py::test_d10_indicator_buffer_bound_pass_and_fail_fixtures -q

..........                                                               [100%]
10 passed in 1.15s
```

EA-only rerun: `5 passed in 0.65s`.

### PASS — EA-scoped mechanical validators

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . \
  --ea-label QM5_35003_three-ducks-multi-timeframe-system
```

Result: exit 0, top-level `failures=[]`, `warnings=[]`; D2/D3/D4/D5/D6/D7/D8/D9/D10/D11/D13/D14/D15/D17/D18 all reported zero failures for this EA.

```text
python tools/strategy_farm/validate_build_guardrails.py \
  framework/EAs/QM5_35003_three-ducks-multi-timeframe-system/QM5_35003_three-ducks-multi-timeframe-system.mq5
```

Result: `verdict: PASS`, zero findings, news-stale ceiling 336.

```text
python framework/scripts/validate_spec_doc.py \
  framework/EAs/QM5_35003_three-ducks-multi-timeframe-system
```

Result: `1 PASS, 0 FAIL`.

### Compile/build_check preflight — correctly refused before compilation

The required EA-scoped command was attempted exactly as scoped:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 \
  -EALabel QM5_35003_three-ducks-multi-timeframe-system -SkipCompile
```

It stopped before validation with `LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` because `terminal64` processes were alive. A second scoped compile attempt:

```text
python tools/strategy_farm/compile_ea.py \
  --ea-label QM5_35003_three-ducks-multi-timeframe-system --force --json --fail-on-error
```

returned `COMPILE_FAILED`, `reason_class=INCLUDE_MIRROR_REFUSED`, exit 1 at `2026-08-24T11:39:55Z`. The reported preflight-only path was `framework/build/compile/20260824_113952/QM5_35003_three-ducks-multi-timeframe-system.compile.log`; no log or new EX5 was created because refusal occurred before MetaEditor launch. Read-only process evidence included `C:/QM/mt5/T_Live/MT5_Base/terminal64.exe`; per ticket constraints it was not touched, and factory was not toggled. No compile or backtest work item was enqueued.

### Unrelated repository-wide observation

The full `test_build_gate_hardening.py` run reached `34 passed` and one failure after 414.27s. The failure was the fleet-global `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` against unrelated `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`; no 35003 assertion failed. The focused D2/D4/D5/D7/D10 tests above were rerun separately and all passed.

## Risks / open verification

- MQL compilation and authoritative `build_check` completion remain unproven in this worktree because the mandatory include-mirror guard refused while live/test terminals were active. This evidence does not claim a compile PASS.
- The existing tracked EX5 was intentionally not staged or modified; review must compile through an authorized governed compile window before pipeline handoff.
- The strategy's total-equity anchor is initialized at EA startup. The framework's persistent kill-switch state supplies the governed live halt channel; restart semantics beyond the approved card's stated formulas remain a reviewer-visible operational risk, not an invented card rule.

## Rollback

Revert the single `rework(35003)` commit containing this file. That restores the prior `.mq5`, SPEC, four setfiles, and removes the regression test/evidence without touching registries or runtime state. The router state change is append-only operational coordination and can be returned to `RECYCLE` by the reviewer if the rework is rejected.


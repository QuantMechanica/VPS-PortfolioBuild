# Review — QM5_12944 Sperandeo Trend Fault-Line Break (H4)

- Router task: `c74163e1-fd20-413d-a42f-e2f3eb432f4f` (review_ea, assigned codex, state REVIEW)
- Build task: `01bd8a9d-bd3f-4da5-a275-be9192a763ed`
- Worker verdict (codex): FAIL — `docs/ops/evidence/c74163e1_qm5_12944_recycled_gemini_codex_review_2026-08-24.json`
- Claude-lane verdict: **RECYCLE** (worker findings confirmed; defects are named + actionable, card stays approved)

## Card vs source (card `framework/EAs/QM5_12944_.../docs/strategy_card.md`)

Mechanism is card-faithful in outline: ZigZag/local-extrema pivot detection, 3-pivot
linear-regression fault line, ATR(1)>1.5×ATR(20) volatility-expansion gate, 0.5×ATR break
buffer, D1 close vs SMA(200) regime gate, 2.0×ATR TP / 1.5×ATR SL, reverse-close, spread
filter, 1-pos-per-magic. Framework corset present: `QM_Common` include, MAE hook
`QM_FrameworkTrackOpenPositionMae` (mq5:429), news wired (mq5:452-457), Friday close,
kill-switch, bounded buffers (`PivotPoint[20]`, `CopyRates(...,160)` with perf-allowed
comment), no ML, no invented commission/swap.

## Confirmed defects (file:line)

1. **Stale EX5 — build FAIL (blocking).** Source rewritten `2026-08-23 19:21`
   (`QM5_12944_sperandeo-trend-fault-line-h4.mq5`) but the committed
   `QM5_12944_sperandeo-trend-fault-line-h4.ex5` is dated `2026-08-21 18:52` — the binary
   predates the rewrite and does not contain the current source. Codex hash check: live
   MQ5 `a83623b3…` != producer `18ece113…`; EX5 is the pre-rework binary. Q02 must not run
   a stale binary. Recompile required (ROT: recompile is OWNER/OFF-window governed — hand
   to the COMPILE_EA queue, not done here).
2. **News window 30/30 vs card 15/15 — card divergence.** `qm_news_temporal` default is
   `QM_NEWS_TEMPORAL_PRE30_POST30` (mq5:22) and `QM_FrameworkInit` is called with literal
   `30, 30` for `news_pause_before/after_minutes` (mq5:407-408; signature
   `QM_Common.mqh:336-337`). Card §Zusätzliche Filter mandates "±15 min around
   high-impact news". Set to 15/15 (and `QM_NEWS_TEMPORAL_PRE15_POST15`).
3. **Dead input `strategy_min_pivots` (mq5:38).** Declared but grep finds no use-site
   beyond the declaration; both fault-line searches hard-code 3 pivots
   (`FindDownFaultLine`/`FindUpFaultLine` loop `i <= *_count-3`, `[i+2]`). Runtime contract
   does not match the advertised parameter surface. Wire it or remove it.
4. **Both risk defaults non-zero (source hygiene).** `RISK_PERCENT=0.5` (mq5:17) and
   `RISK_FIXED=1000` (mq5:18) both set as source defaults — a default framework attach
   fails risk-input validation. Non-blocking for Q02: all 13 backtest setfiles correctly
   set `RISK_FIXED=1000` / `RISK_PERCENT=0` / `environment: backtest`. Fix defaults to a
   single active mode for cleanliness.

## PASS

Magic registry, forbidden-code grep, SPEC validation, and all 13 setfile risk checks pass
(codex evidence; setfiles governed-generator form verified). None of these establishes a
valid default attach or a source-bound compiled artifact.

## Action

RECYCLE to Codex: fix news window (15/15), resolve `strategy_min_pivots`, single-mode risk
defaults; then rebuild via the COMPILE_EA/OFF-window path so the EX5 matches the source hash
before Q02.

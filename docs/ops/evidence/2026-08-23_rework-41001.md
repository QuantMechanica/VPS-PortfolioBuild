# rework-41001 — QM5_41001 review-fix evidence

- Router task: `fc3ed902-4615-4349-ad96-634e1a20710a`
- EA: `QM5_41001_keith-fitschen-aberration-trading-system`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_41001_keith-fitschen-aberration-trading-system.md` (`g0_status: APPROVED`)
- Starting HEAD: `cb3aa135cb14`
- Scope: EA source, its SPEC, producer build-result smoke evidence, one focused pytest module, and this note only.

## What changed

1. Wired the card's maximum three-trade-tick market-entry slippage contract. `StrategyEntryDeviationPoints()` converts `3 * SYMBOL_TRADE_TICK_SIZE` to the point unit expected by `QM_EntryConfigure`, rounds down so the tolerance cannot widen, and fails closed on missing symbol metadata (`framework/EAs/QM5_41001_keith-fitschen-aberration-trading-system/QM5_41001_keith-fitschen-aberration-trading-system.mq5:66`, `:307`). The framework configuration retains the registered host magic through `QM_FrameworkMagic()` (`:313-319`).
2. Corrected SPEC drift: the removed `strategy_tp_rr_mult`/8R target is no longer documented; the source and SPEC now both state an open-ended trend exit and the three-tick execution contract (`framework/EAs/QM5_41001_keith-fitschen-aberration-trading-system/SPEC.md:19-21`, `:103`). Source entries remain `req.tp = 0.0` (`QM5_41001_keith-fitschen-aberration-trading-system.mq5:191`, `:225`).
3. Added durable deferred-smoke capacity evidence. A read-only `mode=ro` database query at `2026-08-24T08:34:56Z` found eight active work items claimed by `T1,T2,T3,T4,T5,T6,T8,T9`; the contemporaneous process census found eight corresponding farm `terminal64` processes and six `metatester64` processes. No smoke/backtest was launched or enqueued (`artifacts/qm5_41001_build_result.json:26-45`).
4. Added focused static regression tests for the three-tick configuration, open-ended TP, management reachability, all declared `strategy_*` input use-sites, SPEC parity, and durable smoke-deferral evidence (`tools/strategy_farm/tests/test_qm5_41001_rework_static.py:19-70`).

## Review-finding closure

The five findings in `docs/ops/evidence/fc3ed902_qm5_41001_gemini_build_codex_review_2026-08-18.md` were already present in the source at starting HEAD and were reverified, not rewritten:

- position management runs before entry-only no-trade/news/new-bar gates (`mq5:343-362`);
- entries have no fixed TP (`mq5:191`, `:225`);
- the D1 execution contract is declared (`mq5:305-308`);
- cached signal/indicator state is cleared before fallible close reads (`mq5:122-134`);
- 2.0% realized-loss halt and 2.5%/5.0% kill-switch limits are wired (`mq5:84-91`, `:99-116`, `:321`).

The three findings in `docs/ops/evidence/0860930e_qm5_41001_gemini_codex_review_2026-08-23.json` are addressed by changes 1-3 above.

## Registry and framework evidence

- `framework/registry/ea_id_registry.csv:4496` contains active EA id `41001` with the exact slug.
- `framework/registry/magic_numbers.csv:17542-17544` contains the existing active XTIUSD/XAUUSD/SP500 slots. No registry or resolver file was changed.
- The generated resolver contains `410010000`, `410010001`, and `410010002` in `framework/include/QM/QM_MagicResolver.mqh:22`.
- All three backtest setfiles retain `RISK_FIXED=1000`, `RISK_PERCENT=0`, and slots 0/1/2 (`sets/*.set:18-20`).
- The EA includes the `QM_Common.mqh` umbrella (`mq5:5`), calls the MAE hook (`mq5:335`), resolves magic through framework APIs (`mq5:196`, `:233`, `:319`), uses no ML, and has only two raw series reads, both explicitly perf-allowed closed-bar reads (`mq5:133-134`). It declares no dynamic indicator buffers requiring an `ArraySize` guard.
- Every declared `strategy_*` input has at least one executable use-site; observed token counts ranged from 2 to 10. The focused pytest enforces this invariant.

## Validation output

### PASS

```text
python -m pytest tools/strategy_farm/tests/test_qm5_41001_rework_static.py -q
.....                                                                    [100%]
5 passed in 0.69s

python -m pytest tools/strategy_farm/tests/test_build_guardrails.py -q
....................                                                     [100%]
20 passed in 1.18s

python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_41001_keith-fitschen-aberration-trading-system
files_scanned=1; failures=[]; warnings=[]

python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 framework/EAs/QM5_41001_keith-fitschen-aberration-trading-system
verdict=PASS; findings=[]; files_checked=4

python framework/scripts/validate_spec_doc.py framework/EAs/QM5_41001_keith-fitschen-aberration-trading-system
Summary: 1 PASS, 0 FAIL (of 1)
```

### Scoped build-check refusal (fail-closed infrastructure guard)

```text
pwsh -NoProfile -File framework/scripts/build_check.ps1 -EALabel QM5_41001_keith-fitschen-aberration-trading-system -RepoRoot . -SkipCompile
BUILD_CHECK_LIVE_FACTORY_COMPILE_REFUSED
terminal64 processes are alive; ad-hoc compile/build_check is refused.
```

The command was correctly EA-scoped. It stopped before compile/static aggregation because the factory terminals were live. The task forbids toggling the factory or disturbing terminal work, and no authorized `COMPILE_EA` claim exists for this EA. Therefore the tracked EX5 was not overwritten and remains the pre-rework binary (`SHA-256 910258ee5c88d0428c08109e66301d2849ce9aed4f01848429a8f0aeb846f44b`). The reworked MQ5 SHA-256 before commit is `4dd850d3f913fa1e02ff017cf0f582467efbc9a0277b7643b77611c0d23de69f`.

### Existing unrelated corpus failure

```text
python -m pytest tools/strategy_farm/tests/test_build_gate_hardening.py -q
1 failed, 28 passed in 162.40s
```

The sole failure is the pre-existing cross-corpus test `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`, which reports seven findings in out-of-scope `QM5_411xx` EAs (first: `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`). The EA-scoped hardening command above reports zero findings for QM5_41001. Per ticket scope, no other EA was changed.

## Risk / open review item

The source-level findings are repaired and static checks pass, but the EX5 cannot be claimed as bound to the repaired MQ5 until a governed, quiescent compile lane runs the exact EA-scoped build. Review must keep that binary-binding limitation explicit; no pipeline or live authorization is inferred.

## Rollback

After this ticket commit, use `git revert <rework-41001-commit>` to restore the prior MQ5, SPEC, build-result smoke fields, tests, and this evidence note as one auditable inverse commit. No database verdict, backtest queue, magic allocation, factory flag, or live terminal state requires rollback because none was changed (apart from the explicitly requested router-task state transitions).

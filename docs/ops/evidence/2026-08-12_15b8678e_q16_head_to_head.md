# OPT-3 Q16 sealed head-to-head evaluator

Router task: `15b8678e-33bc-4103-839b-46d73c6b6115`  
Decision contract: DL-084  
Execution date: 2026-08-12  
Scope: analytic Q16 evaluation and dry-run enqueue consumption only. No terminal, live, deployment, T_Live, AutoTrading, or production work-item mutation occurred.

## Delivered behavior

`framework/scripts/q16_head_to_head.py` now provides:

- exact opt-card window enforcement: at least three non-overlapping `Q04_ANCHORED_OOS` folds plus one `POST_DEV_HOLDOUT`, with all other trades excluded (line 122);
- raw-trade repricing with the repository `venue_cost_model.json`, using the worse declared DXZ/FTMO round-trip commission (line 154);
- immutable binary, setfile, Q10-evidence, and stream bindings for parent and challenger, including hard enforcement of `RISK_FIXED=1000` and `RISK_PERCENT=0`;
- a hard trial-ledger check requiring unique ledger rows and matching Q07/Q08 declared and observed trial counts; any missing/undercounted evidence returns `FAIL` (line 251);
- the mandatory no-change control: the parent is re-evaluated beside the challenger on identical windows, raw bytes, risk, and costs;
- the pre-registered primary-metric comparison plus MaxDD/worst-day guards (line 378);
- DL-082/DL-083 marginal book math: capped inverse-vol cap 1 / total 9.75, regime-split correlation, ΔSharpe 0.020 never as a sole driver, ΔMaxDD, Δworst-day, and minimum annual contribution 0.06% (line 446);
- `ADMIT_BOTH` only when both sleeves contribute and the pair's maximum absolute regime correlation is below 0.15; otherwise a beating, contributing challenger is `PROMOTE_CHALLENGER` (lines 652-655). Contract/evidence failures are `FAIL`; a valid non-beating challenger is `KEEP_INCUMBENT`.

`tools/strategy_farm/farmctl.py` adds `enqueue-head-to-head` (line 21076). It is dry-run by default and only `--apply` is state-mutating. The explicit apply path creates an `analytic` Q16 row, validates matching done Q10-PASS parent/challenger dependencies, and is deterministic/idempotent. Its payload declares `ANALYTIC_DISPATCH_NOT_TERMINAL_WORKER`; terminal-worker code was not changed.

## Exit-surgery precedent fixture

The fixture in `tools/strategy_farm/tests/test_q16_head_to_head.py` uses the real Tier-A precedent identity QM5_10939/GBPUSD -> QM5_12990/GBPUSD from `D2C_13SLEEVE_EXIT_SURGERY_AUDIT_2026-07-03.md`. Its small trade streams are deterministic modeled test data, not historical pipeline evidence.

Durable fixture package:

- inputs: `D:/QM/reports/q16_exit_surgery_fixture_20260812/inputs/`;
- result: `D:/QM/reports/q16_exit_surgery_fixture_20260812/q16_result.json`;
- evidence: `D:/QM/reports/q16_exit_surgery_fixture_20260812/evidence.md`;
- result file SHA-256: `6473808de8e1d4bbb2c1f8f853afc7a072e8c3c019e768d53097bf6ff57f9a93`;
- result body SHA-256: `63d52774c254012763aec9868084488eec21d4fc31446f3e59654f1332904c77`.

The modeled fixture emits `PROMOTE_CHALLENGER`: the challenger beats the identical-data no-change control and contributes to the book, while `ADMIT_BOTH` remains closed because the pair correlation is not proven below 0.15. A second CLI run produced byte-identical JSON. This is a test oracle only and confers no verdict on either real EA.

The farmctl dry run emitted work-item preview `80a612d8-5239-5f1c-aa6e-c7d546ebaf18` with `enqueued: false`; no production Q16 row was created.

## Verification

```text
python -m pytest tools/strategy_farm/tests/test_q16_head_to_head.py -q
...... [100%]
6 passed

python -m pytest tools/strategy_farm/tests/test_work_item_lifecycle_v2.py \
  tools/strategy_farm/tests/test_q09_news_farmctl_integration.py -q
..................... [100%]
21 passed
```

Coverage includes deterministic evaluation, sealed-window exclusion, trial-ledger undercount, challenger-Q10 failure, frozen parent hash mismatch, minimum window geometry, dry-run-default enqueue, and idempotent apply against a temporary SQLite fixture. `python -m py_compile` and `git diff --check` passed.

## Review disposition

`PASS_TO_REVIEW`. Q16 now has a deterministic, fail-closed analytic implementation and a dry-run farmctl consumer. Production enqueue still requires an explicit `--apply` with exact Q10 dependency ids; none was run here.

# Review — QM5_41001 Keith Fitschen Aberration Commodity Trend System

- **Router task:** 0860930e-72cb-44a3-bb8b-94060ca4d8e2 (review_ea, assigned codex, Claude review lane closes)
- **Source build task:** 9fbca489-f822-4412-8066-a819bc100eb7 (build_ea, source_agent gemini/agy)
- **Source verdict:** "PASS: Aberration D1 code remediated, execution contract declared, card loss limits wired, gates pass"
- **EA dir:** framework/EAs/QM5_41001_keith-fitschen-aberration-trading-system
- **mq5 sha256:** 747716971ecca1805addc8646d8da24cd30e9af35a429fc66eee1208fb736c92 (matches build_result.json; current-hash review)
- **Reviewer:** Claude (independent), reconciled against prior codex review docs/ops/evidence/0860930e_qm5_41001_gemini_codex_review_2026-08-23.json (codex verdict FAIL)
- **Verdict:** **RECYCLE**

## What passes

- **Card-faithful mechanism.** 30-period 3.0σ Bollinger breakout on D1, shift=1: long `close1>upper1 && close2<=upper2`, short `close1<lower1 && close2>=lower2` (mq5 AdvanceState_OnNewBar). SL = 2.5×ATR(14)[1]; midline exit closes long when Close[1]<SMA30[1] (Strategy_ManageOpenPosition). Matches card §3.2–§3.4.
- **No dead inputs.** All 12 strategy/loss inputs grepped to live use-sites: signal_tf, sma_period, dev_multiplier, atr_period, atr_sl_mult, use_mid_exit, rollover_start/end_hhmm, spread_filter_mult (indicator + filter code); daily_loss_halt_pct (Strategy_DailyRealizedLossHalt), daily_hard_stop_pct + total_dd_stop_pct (QM_KillSwitchInit). No orphaned declarations.
- **Framework conformity.** QM_Common include; MAE hook (QM_FrameworkTrackOpenPositionMae in OnTick); RISK_FIXED=1000 / RISK_PERCENT=0 (backtest); magic via QM_FrameworkMagic() with registry rows 41001 slot0/1/2 = 410010000/1/2 (ea_id*10000+slot verified, magic_numbers.csv:17543-17545, resolver embeds 41001); news wired via QM_NewsAllowsTrade2; bounded indicator wrappers (QM_BB_*/QM_ATR), only shift-1/2 iClose marked perf-allowed; kill-switch, Friday-close, execution contract declared. No ML, no invented commission/swap.
- **Build gate.** build_check_passed=true, compile_succeeded=true for current source hash (artifacts/qm5_41001_build_result.json).

## Actionable defects (RECYCLE)

1. **Card §4.2 slippage tolerance (3.0 ticks) not wired.** QM_FrameworkInit hardcodes a 20-point market-order deviation (QM_Common.mqh:296 `QM_EntryConfigure(ea_id, news_mode, 20, ...)`; consumed at QM_Entry.mqh:462 `trade_req.deviation = g_qm_entry_deviation_points`). The EA never overrides it, so the approved card's "Max 3.0 ticks on market orders" limit is absent. Inert in Q02 backtest but a declared capital-preservation limit that must exist before the live path. Remediation: re-invoke QM_EntryConfigure with deviation=3 after QM_FrameworkInit, or expose deviation on the framework init call. (Concurs with codex framework_corset FAIL.)
2. **SPEC.md ↔ source drift.** SPEC.md line 32 declares `strategy_tp_rr_mult = 8.0`, but no such input exists in the mq5 and entries are open-ended (`req.tp = 0.0`, EntrySignal). Card §3.4 mandates open-ended trend-riding TP, so the *code* is card-faithful — the *spec* is wrong. Remediation: remove strategy_tp_rr_mult from SPEC.md §2 (and any mention) so the reviewed spec describes the compiled source.

## Soft note (not a standalone blocker)

3. **Smoke evidence.** build_result marks `smoke_result=deferred_p2_smoke` with a generic `smoke_skipped_reason` (backtest_cpu_ceiling) and `blocked_reason=null`; no QM5_41001 summary under D:/QM/reports/smoke. Deferring P2 smoke to Q02 is an accepted shop pattern and compile+build_check are green, so this does not itself block, but the deferral reason should carry durable resolver/process-occupancy evidence per SCHEMAS.md. Q02 will supply runtime evidence.

## Verdict

**RECYCLE** — mechanism and wiring are sound and no input is dead, but two named actionable defects (card slippage limit unwired; SPEC tp_rr_mult drift) must be fixed before the EA is clean for Q02. Both are small, targeted edits; re-review on the corrected source hash.

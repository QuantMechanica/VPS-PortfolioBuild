# M05 evidence recovery and deletion attribution

Task `618d640e-4054-418a-b8a0-a0e24200265e`: REVIEW. **Twenty-seven evidence files were recovered into a new canonical directory. Exact receipts explain the other 129 losses.** No original report path, database verdict, live file or terminal was changed.

| Classification | Files | Finding |
|---|---:|---|
| RECOVERED_COPY | 27 | Survived in `C:/QM/backups_relocated/retention_quarantine/`; copied with source/output hashes and receipt-bound file identity. |
| REGENERATED | 0 | No missing aggregate had a complete surviving primary input set from which its original evidence could be deterministically reconstructed. |
| IRRECOVERABLE | 129 | Exact target or gzip representation has a DELETED receipt; primary report inputs are absent and no matching copy was located in the searched local sources. |
| STILL_UNEXPLAINED | 0 | Every target is now tied to a surviving relocated copy or an exact deletion receipt. |
| **Total** | **156** | One classification per original watched path. |

“IRRECOVERABLE” is scoped to the enumerated local sources. It does not assert that an unavailable off-host backup or specialist disk recovery could never recover bytes. The search indexed **304,520 files** across the report archives/quarantines/recovery roots, exports, farm artifacts, relocated backups and repository artifacts. ZIP member identity searches found no additional candidates. The 129 deleted targets have no exact-named alternative among the matching work-item paths. Original primary report roots are empty in all 156 cases. Partial database metrics or a later run do not recreate the missing evidence.

## What removed the files

The strongest evidence is the **August 31 OWNER backup-retention execution**, task `4c0a5ae7`, committed as `99143f421feccc325778dce4ef685b3c9616e6db`. This is a separate code path from the tester-cache purge and ordinary report-retention jobs.

The executor's compressed exact-path receipts name **129 original targets or their gzip representations**, each with `disposition=DELETE_NONRETAINED`, `status=DELETED` and `detail=verified_quarantine_then_unlink`. Of these, **80 were raw JSON and 49 were gzip copies**. Receipt completion times fall between **2026-08-31 15:20:25 and 15:21:22 UTC**. These are batch completion bounds, not invented per-file deletion timestamps. Parent-directory changes cluster in the same interval. Some literal paths disappeared earlier through compression; the August 31 event then removed their surviving gzip bytes.

Both the gzip hash and the canonical decoded receipt-stream hash were independently verified for **155 receipt streams** (153 execution batches plus recovery streams). The exact-path receipt is content-hashed, but ordinary deleted source files did **not** receive individual content hashes; do not confuse receipt integrity with a pre-deletion file-content attestation. The original cohort baseline also stored no content hashes.

The executed classifier is frozen in [executed_build_backup_retention_manifest.py](2026-09-05_m05_recovery/executed_build_backup_retention_manifest.py). Its line 128 admits PASS-family history as a path-to-25 condition only for the v4 contract. Lines 238–243 delete non-path artifacts outside the Q02/Q04 retained set. The actual unlink is at line 565 of [executed_execute_backup_retention_phase2.py](2026-09-05_m05_recovery/executed_execute_backup_retention_phase2.py). This narrower backup doctrine explains the apparent conflict with the current DL-090 classifier: **all 126 current KEEP-class losses are among the 129 deletions**; the other three deleted paths are AGE_OUT. All 27 recovered paths are currently AGE_OUT.

This finding attributes an executed, receipt-backed policy. It does not infer a malicious or unlogged operator. The earlier M05 adjudication examined ordinary retention/purge logs and did not include the relocated C-drive quarantine or the compressed backup-executor receipts; this follow-up closes that search gap without overwriting the prior evidence.

The daily report-retention log provides coarse compression/quarantine events for the original moves. The log-only purge targets `*.log`, and the tester-cache purge targets terminal caches. Neither supplies a receipt for these JSON deletions. Scheduled-task and PowerShell event logs no longer retain August 31: their oldest available records are September 5. Their absence is not exculpatory evidence. Current task definitions and the available log coverage are captured in [scheduled_history.json](2026-09-05_m05_recovery/scheduled_history.json).

## Recovery and the two named cases

The 27 recovered files retain the same logical size and NTFS file identity as the backup-retention compression receipts. Each copy was parsed as JSON and hashed before and after copying; sources remained unchanged. Source path, timestamp, receipt reference, observed content hash, output hash and replay command are recorded for every file in [per_path_forensics.json](2026-09-05_m05_recovery/per_path_forensics.json). The copies are in [restored](2026-09-05_m05_recovery/restored/). Without an original baseline hash, this is a receipt-bound recovered copy, not a claim to have compared against previously sealed content bytes.

**QM5_13128 / NDX:** the `6f4fff24-3a6d-40e3-aaca-773b5195f6d2` aggregate survived under the relocated August 23 quarantine. Its recovered SHA-256 is `17ab2512bde3d95c661e560d435c3e883a4fac607de383733e9285ee2ba396cb`. [Recovered aggregate](2026-09-05_m05_recovery/restored/6f4fff24-3a6d-40e3-aaca-773b5195f6d2/aggregate.json). This resolves the missing research artifact; it does not by itself prove binding to the currently deployed live sleeve.

**QM5_1328 / EURJPY:** the separate Q11-lineage case is **outside the 156** and is reported separately. Its historical storage path contains `Q10`, which maps to the active Q11 confirmation gate. The gzip aggregate was deleted in resume batch 0061, exact-path line 400, completed **2026-08-31 15:21:09 UTC**. The primary run inputs are also absent. A database PASS and two summary metrics cannot recreate its aggregate or trade evidence. No replacement verdict or aggregate was invented.

## Would the 91-pair tester-cache guard have prevented this?

**No, not on this executor path.** `execute_backup_retention_phase2.py` does not invoke `tester_cache_purge_guard`. Its own snapshot/classifier decides retention, and its source roots include report and relocated-quarantine paths outside the tester-cache targets. A pair listed in the separate 91-pair guard is not automatically protected here. A future change needs to reconcile these classifications across the actual writers; this read-only recovery task did not change either policy or executor.

Verification: 156 unique cohort entries, counts sum to 156, 27 source/output hash matches and valid JSON copies, 155 receipt-stream hash pairs verified, zero original-path writes, unchanged cohort baseline hash, and the named extra case kept outside the denominator. [Verification](2026-09-05_m05_recovery/verification.json), [summary](2026-09-05_m05_recovery/summary.json), [alternative search](2026-09-05_m05_recovery/alternative_search.json), [git history](2026-09-05_m05_recovery/git_history.json). All evidence is committed only on canonical `agents/board-advisor`.

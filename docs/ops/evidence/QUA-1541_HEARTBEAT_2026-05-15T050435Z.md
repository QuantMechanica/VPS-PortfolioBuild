# QUA-1541 Heartbeat Evidence - 2026-05-15T05:04:35Z

## Scope
- Issue: `QUA-1541` (MT5 multi-EA saturation scheduler)
- Action: reconcile lingering `dispatched` queue row against live terminal/process reality

## Queue Row Reconciled
`mt5_job_queue` (`status='dispatched'`) snapshot:
- `id=1`
- `ea_id=QM5_1003`
- `phase=P2`
- `symbol=EURUSD.DWX`
- `assigned_terminal=T2`
- `dispatched_at=2026-05-15T04:44:11.173029+00:00`

## Adapter Hardening Added
File updated:
- `C:\QM\paperclip\tools\ops\multi_ea_scheduler.py`

New metrics emitted each tick:
- `live_terminals_from_process`
- `dispatched_rows_count`
- `dispatched_rows_by_terminal`
- `orphan_dispatched_rows`

## Verification
Command:
```powershell
python C:\QM\paperclip\tools\ops\multi_ea_scheduler.py --once --dry-run --orphan-dispatched-minutes 10
```

Result:
- `status=ok`
- `queue_counts_after={"dispatched":1}`
- `live_terminals_from_process=[]`
- `dispatched_rows_by_terminal={"T2":1}`
- `orphan_dispatched_rows=1`

Interpretation:
- The only dispatched row is now deterministically classified as orphaned under 10m threshold when no matching live terminal exists.

## Next Action
- Implement deterministic stale-dispatch recovery policy:
  - promote orphaned `dispatched` rows back to `queued` after timeout, or
  - mark terminal error state and handoff to dispatcher recovery routine,
  with an explicit audit trail in evidence JSON and queue row metadata.

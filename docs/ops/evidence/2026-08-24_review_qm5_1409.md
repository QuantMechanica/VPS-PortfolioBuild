# Claude review — QM5_1409 (router task 9b0b37f8) — 2026-08-24

- Lane: Claude review, `agents/board-advisor`, repo `C:/QM/repo` (read-only DB).
- Task: `9b0b37f8-087b-436e-a044-8b004c20905f` `review_ea`, state REVIEW, assigned `codex`.
- EA: `QM5_1409_wyckoff-sign-of-strength-phase-d-h4`.
- Card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_1409_wyckoff-sign-of-strength-phase-d-h4.md` (`g0_status: APPROVED`).
- Verdict: **RECYCLE** — mechanism-level rework is genuine and clean, but the delivered
  binary and setfiles do not bind the committed source, and 6 out-of-card magic rows
  keep the strict hardening gate red.

## Deliverable under review

The current REVIEW deliverable is the rework commit `15b2a36c6` (2026-08-24 09:38 +0200),
documented in `docs/ops/evidence/2026-08-23_rework-1409.md`. Task `updated_at` is
`2026-08-24T07:39:31Z` — immediately after that commit. The earlier codex FAIL artifact
`docs/ops/evidence/9b0b37f8_qm5_1409_recycled_gemini_codex_review_2026-08-24.json`
(07:26:14Z) predates the rework by ~12 minutes and reviewed the pre-rework hashes
(`deffd643`/`71640fdd`); it is stale and is not the state I adjudicate.

## What the rework fixed (independently re-verified on current source)

- `validate_build_guardrails.py`: verdict PASS, 0 findings (9 files).
- `validate_spec_doc.py`: 1 PASS, 0 FAIL (the 08-22 missing Source/Risk sections are fixed).
- `build_gate_hardening.py --ea-label`: all source checks clean — D7 MAE hook present
  (`QM_FrameworkTrackOpenPositionMae()` mq5:876), D9 request init 0 failures, D10 buffers
  0 failures, D15 news 0 failures.
- Forbidden-code grep on current mq5: `CopyBuffer` count = **0** (the 4 raw calls from the
  stale review are gone); no ML tokens; no raw `CTrade` — management routes through
  `QM_TM_PartialClose`/`QM_TM_MoveSL`/`QM_TM_ClosePosition`/`QM_TM_OpenPosition`.
- News is entry-only and card-literal 480/480 min (`QM_NewsInWindow(...,480,480,...)` mq5:656);
  management/exits and the MAE sample run before the entry gate.
- Framework chain: `#include <QM/QM_Common.mqh>`; one bounded bespoke `CopyRates` (mq5:673)
  correctly annotated `perf-allowed` (once per closed H4 bar after `QM_IsNewBar`).
- Setfiles pruned to the approved 8 (EURUSD, GBPUSD, GDAXI, NDX, USDJPY, WS30, XAUUSD,
  XTIUSD; GDAXI = card's canonical GER40); `RISK_FIXED=1000`, `RISK_PERCENT=0` for backtest.
- Dead-input scan: every declared `strategy_*`/`qm_*` input has ≥1 use-site beyond its
  declaration (min occurrence count 2). No dead inputs.

## Blocking findings (RECYCLE scope)

1. **Stale source-hash binding — binary and setfiles do not match the committed source.**
   Committed MQ5 hashes to `dc85e92d…c097` (git blob, LF) / `b2e08065…9776` (worktree, CRLF).
   All 8 setfiles record `build_hash: 42c264eb5849…3d5d` (e.g.
   `sets/..._EURUSD.DWX_H4_backtest.set:3`) and the shipped EX5 is `6fdbf8b1…bd3b`, which the
   rework doc states was compiled from `42c264eb`. Neither the raw, CRLF→LF, nor LF→CRLF hash
   of the committed MQ5 equals `42c264eb`, so this is genuine source drift, not an EOL artifact:
   the MQ5 was edited after setfile generation and after the isolated compile, but setfiles were
   not regenerated and the EX5 was not recompiled. Q02 would run a binary/setfile pair that does
   not correspond to the reviewed source. No governed `COMPILE_OK` exists for the current source
   (the rework's `compile_one.ps1 -Strict`/`build_check.ps1` both fail-closed at the live-factory
   include-mirror guard; only an isolated portable-MetaEditor compile of the `42c264eb` version was
   produced). This is the same defect class as the adjacent `baeb73aba review(QM5_1612): RECYCLE —
   stale setfile/identity source-hash binding`.

2. **Strict hardening gate is not green — 6 out-of-card active magic rows (D17).**
   `build_gate_hardening.py --ea-label` returns 7 `EA_SYMBOL_NOT_IN_CARD_UNIVERSE` failures.
   One (GDAXI, magic_registry line 17731 slot 9) is the benign card `GER40.DWX` → canonical
   `GDAXI.DWX` reconciliation and also has a legitimate setfile. The other six are genuine:
   AUDUSD (line 17725 slot 3), USDCAD (17726/4), USDCHF (17727/5), NZDUSD (17728/6),
   UK100 (17732/10), SP500 (17733/11) — all `status=active` in `framework/registry/magic_numbers.csv`
   with no matching setfile. The rework correctly pruned the setfiles but left these registry
   rows active, so the gate stays red. For a clean Q02 handoff these six allocations must be
   retired via the governed magic-registry path (dirs → CSV → resolver regen → verify).

## Required recycle scope

Recompile the current committed MQ5 through the governed build path and regenerate the 8
setfiles via `gen_setfile.ps1` so `build_hash`/EX5 bind the committed source hash; retire the
six out-of-card active magic rows via the governed registry order-of-operations; then return
the new binary/hash pair and a green `build_gate_hardening` result for re-review. Do not move
this candidate to PIPELINE.

## Safety boundary

Read-only DB (mode=ro) except the single sanctioned `close-review`. No backtest enqueued,
no factory/AutoTrading/T_Live toggled, no recompile, no EA/card/setfile/registry/resolver/EX5
mutated. Findings are file:line-anchored and independently reproduced with the cited validators.

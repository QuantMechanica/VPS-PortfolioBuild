# Review — QM5_40008 aqr-value-and-momentum-everywhere (review_ea 4f626c91)

- Router task: `4f626c91-ebe2-4df1-b689-d2d30bbfb24d` (review_ea, state REVIEW, assigned codex)
- Source build task: `3e8438fe-884f-4337-974d-7c8c2a1dd459` (build_ea, agent gemini/agy)
- Build verdict claimed: PASS (cross-asset ranking + card loss limits, gates pass)
- Reviewer: Claude review lane (read-only DB, no factory/T_Live/recompile)
- EA dir: `framework/EAs/QM5_40008_aqr-value-and-momentum-everywhere/`
- Card: `strategy-seeds/cards/approved/QM5_40008_aqr-value-and-momentum-everywhere.md` (APPROVED, D1, 4-asset cross-sectional)

## Verdict: RECYCLE

Card-faithful in mechanism and cleanly compiling, but the card's declared slippage
tolerance is silently dropped and the quarterly cadence is hand-rolled instead of using
the framework calendar helpers. Named, actionable — back to the build lane. Independent
review reaches the same conclusion as the Codex artifact
`docs/ops/evidence/4f626c91_qm5_40008_gemini_codex_review_2026-08-23.json` (FAIL).

## Clean items (verified)

- **Compile/hash**: current `.mq5` sha256 `0d91c717...f131d7` matches the build artifact
  (`artifacts/qm5_40008_build_result.json`); `build_check_passed:true`,
  `compile_succeeded:true`; build doc reports `build_gate_hardening` D2–D18 0 failures.
- **Card-faithful mechanism**: cross-sectional CombinedScore = 0.50·Rank(M)+0.50·Rank(V)
  over the 4-asset universe (mq5:154-206), M = 252d return, V = -(P[1]-SMA(1260))/std(1260)
  with std via BB(1260,dev=1.0) upper-mid (mq5:190-196); SMA(200) macro gate; SL = 2.5·ATR(14);
  quarterly rebalance exit. Rank normalization maps to [0,1] correctly.
- **No dead inputs**: all 14 strategy inputs grepped to use-sites beyond declaration
  (`InpMomDays..InpSpreadATRMult`, rollover, loss-limit trio all ≥2 refs; kill-switch trio
  wired at OnInit).
- **Framework conformity**: `#include <QM/QM_Common.mqh>`; MAE hook
  `QM_FrameworkTrackOpenPositionMae()` (OnTick); kill switch `QM_KillSwitchInit/Check`;
  news filter wired (`QM_NewsAllowsTrade2`); magic via `QM_FrameworkMagic()`
  = ea_id·10000+slot (magic_base 400080000); bounded indicator wrappers only
  (QM_SMA/QM_BB/QM_ATR + perf-annotated iClose/iBars); no ML; no invented commission/swap.
- **Risk model**: setfiles governed (build_hash present, card_defaults_source), env=backtest,
  risk_mode=FIXED, RISK_FIXED=1000, RISK_PERCENT=0.

## Actionable defects

1. **Card §4.2 slippage tolerance not wired (primary).** Card declares "Slippage Tolerance:
   Max 3.0 ticks on market orders." The EA never calls `QM_EntryConfigure` with a custom
   deviation; `QM_FrameworkInit` fixes it at the framework 20-point default
   (`framework/include/QM/QM_Common.mqh:296`, applied `QM_Entry.mqh:63,462`). The declared
   3-tick cap is silently dropped. Fix: expose a deviation input mapped to 3 ticks and pass
   it through, or document why 20-point default is retained.

2. **Quarterly cadence hand-rolled (secondary, conformity).** `IsNewQuarter`
   (mq5:129-160) latches file-scope `g_last_quarter`/`g_last_year` from raw broker
   `TimeCurrent()` (not UTC, unlike the rollover path that uses `QM_BrokerToUTC`). Framework
   provides `QM_CalendarPeriodKey`/`QM_IsNewCalendarPeriod`
   (`framework/include/QM/QM_Indicators.mqh:217,251`) for tester-robust calendar cadence —
   no quarterly variant exists, so derive quarter from the MN1 calendar key rather than an
   ad-hoc latch.

## Noted, non-blocking

3. **Smoke deferred to Q02.** `smoke_result=deferred_p2_smoke`, `blocked_reason:null`,
   reason `backtest_cpu_ceiling`. Acceptable per the CPU-ceiling defer convention (Q02
   supplies runtime evidence); not a review blocker. Codex flagged the skip reason as not the
   durable resolver/occupancy evidence SCHEMAS.md prefers — a process nit for the build lane.

## Q02 risk to watch (not a code defect)

Cross-sectional ranking needs all 4 universe symbols' D1 history present in the tester
(`valid_count<2` guard → no trade). Single-symbol Q02 runs without sibling data will
produce zero trades; measure and confirm data availability at Q02.

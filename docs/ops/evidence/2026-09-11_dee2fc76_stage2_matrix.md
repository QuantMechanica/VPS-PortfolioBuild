# Balke stage-2 matrix hand-back — 2026-09-11

Task: dee2fc76-67c5-408e-9b05-e6a0271ea81c (priority 89).

RESULT: Q-RESEARCH REVIEW — 350-cell matrix materialized; enqueue and selection await the separately assigned QM5_41405 build and seven default-cell deal-list identity proofs.

The adjacent `2026-09-11_dee2fc76_stage2_matrix.csv` enumerates the authorized 48 factorial configurations and two broker-clock Balke configurations across 2019–2025. Config 0 is the fixed UTC+3, AS_IS, zero-buffer, ATR-band-on s0_l8 control. The Balke rows use broker time, AS_IS, M30 bars and 07:30 range end. Focused verification confirms 50 configurations, seven years each, 350 unique config/year pairs, and the exact control defaults. This CSV is a reviewable parameter inventory, not an executable program declaration.

Authority: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_41405_balke-clock-audit-opt.md`. The scoring contract is `docs/research/BALKE_WINDOW_SWEEP_PLAN_2026-09-09.md` sections 4–5: costed net subtracts USD 5 per lot round trip; score is costed net/max(drawdown,1); DEV median 2019–2022, OOS 2023–2025; >=10 entry days and >=5 trades in every year, average >=40 trades/year, all seven cells MEASURED. The card fixes buffer/band neighbourhoods and the five 1.10 hypothesis margins. No thresholds have changed.

At handling time the sibling has no source or EX5. Its implementation is assigned separately to build task a10e4704-b348-4e3c-b8ea-49c3a8309d9d (priority 92), which follows this task under the requested ascending ordering. The approved card explicitly requires all seven control trade lists to match before the 350-cell enqueue. There is therefore no lawful enqueue or final economics report yet. Runtime projection remains the card's unmeasured ~17 hours at two lanes (GELB); replace it with observed control runtime before enqueue.

The report/declaration must also state how categorical ties are broken and how the published no-trailing Balke setting is represented: the approved six-input sibling explicitly preserves parent trailing. This inventory does not silently add an extra strategy switch or label a trailing run as an exact published-settings replication.

Hand-back: finish the tracked build, collect governed control evidence, commit the complete scoring/report pre-registration and bind its commit and input hashes into the program declaration, then enqueue idempotently. Selection must refuse until all 350 cells are MEASURED. Hypotheses, fade-only-day economics and a recommended configuration remain unmeasured.

No work items, parent source/binary/sets, terminal activation, or pipeline verdict were changed by this task.

## Router continuation — compiled sibling and controlled enqueue refusal

The separate build is now complete: `QM5_41405_balke-clock-audit-opt` has a
source-bound EX5 and governed `COMPILE_EA` evidence at
`docs/ops/evidence/2026-09-11_a10e4704_compile_evidence.json` (strict compile
PASS, 0 errors, 0 warnings). Its canonical backtest set has
`RISK_FIXED=1000` and `RISK_PERCENT=0`; the parent 41398 artifact remains
untouched.

The required first-seven-control-cell enqueue cannot be performed by
`tools/strategy_farm/window_sweep.py`: its sealed validator fixes
`program_id=WINSWEEP_QM5_41398_USDJPY_DWX_2019_2025`, `ea_id=QM5_41398`, the
parent binary/setfile hashes, a 90-trial declaration, and a 420-cell ledger.
Applying it to QM5_41405 would therefore fail its own declaration/ledger
contracts or, worse, target the parent experiment. No factory row was written.

**RESULT (Q-only):** Q-STAGE2 is REVIEW, not selected. The build prerequisite
is PASS, but the 350-cell matrix needs its own governed, idempotent
`qm.window-sweep.v1` adapter plus a pre-registered declaration that binds the
QM5_41405 MQ5/EX5/set hashes and commit before even the seven control cells are
enqueued. Until that adapter has a real first claim and all 350 cells are
MEASURED, deal-list identity, hypothesis results, fade-only economics, and any
recommended configuration remain unproven.

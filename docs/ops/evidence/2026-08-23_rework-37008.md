# rework-37008 — QM5_37008 review repair evidence

## Scope and authority

- Router task: `fe559a03-a33b-4003-a976-8bd9244cfe55`; claimed with state
  `IN_PROGRESS` and verdict `rework burn-window slot 2`.
- Work order: `docs/ops/evidence/fe559a03_qm5_37008_codex_review_2026-08-18.md`.
- Approved card: `strategy-seeds/cards/approved/QM5_37008_garch-volatility-forecast-breakout.md`:
  `ea_id`/slug/G0 approval at lines 3-10, card spread rule at line 84, and mandatory
  one-sigma GARCH-cone trail at line 98.
- Registry preflight: `framework/registry/ea_id_registry.csv:4471`; active magic rows
  `framework/registry/magic_numbers.csv:17481-17483`. No registry change was needed.
- Scope remained build-only. No backtest was enqueued or deleted, no factory state was
  toggled, and `C:/QM/mt5/T_Live` was not touched.

## What changed

1. Preserved the card-faithful one-sigma trailing mechanic and made its tightening-only
   proof reviewable: the manager filters by chart symbol and framework-resolved magic,
   derives `Open[1] +/- sigma`, checks that the candidate improves the existing stop and
   remains on the valid quote side, then calls `QM_TM_MoveSL`
   (`framework/EAs/QM5_37008_garch-volatility-forecast-breakout/QM5_37008_garch-volatility-forecast-breakout.mq5:295-333`).
2. Centralized the sole card spread rule in `Strategy_SpreadAllowsEntry` and invoked it
   from both the no-trade hook and immediately inside `Strategy_EntrySignal`
   (`...mq5:213-218`, `:238`, `:263`). The undocumented absolute-points spread cap is
   absent. The D1 state refresh precedes no-trade admission and entry
   (`...mq5:411-453`).
3. Added explicit bounds for the maximum 100-close GARCH window and `ArraySize` guards
   (`...mq5:117-118`, `:169-182`). Both raw series calls carry truthful `perf-allowed`
   annotations and run only at initialization/new-D1-bar refresh (`...mq5:169`, `:181`).
4. Retained framework conformity: fixed-risk backtest/live-risk inputs (`...mq5:28-29`),
   `QM_Common` initialization (`...mq5:355-380`), MagicResolver-backed
   `QM_FrameworkMagic`, and the MAE hook before the kill-switch (`...mq5:402`). Every
   declared input has a non-declaration use site, enforced by the new regression test.
5. Rebound all three D1 backtest sets to source SHA-256
   `af38c6703b135fc4a92797df00c5ed6a3cd1aae43ccd6ba43e6bc41100382dd1` with slots
   0/1/2 and `RISK_FIXED=1000`, `RISK_PERCENT=0` (each set file lines 13 and 18-20).
6. Documented the immediate spread recheck and bounded state in
   `framework/EAs/QM5_37008_garch-volatility-forecast-breakout/SPEC.md:20-22` and its
   revision record at line 121.
7. Added five focused pytest regressions covering the approved-card identity, all three
   review findings, raw-series/buffer discipline, every declared input, and framework/
   magic/setfile bindings
   (`tools/strategy_farm/tests/test_qm5_37008_review_rework_static.py:54-173`).

## Validation evidence

- `python -m pytest tools/strategy_farm/tests/test_qm5_37008_review_rework_static.py -q`
  -> `5 passed in 0.73s`.
- `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_37008_garch-volatility-forecast-breakout`
  -> exit 0; one EA scanned; `failures=[]`, `warnings=[]`; D10 buffer, D7 MAE,
  D4 management-reachability, D8 new-bar, D9 request-init, and all three exact symbol
  checks passed.
- `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_37008_garch-volatility-forecast-breakout`
  -> exit 0; `PASS`, four files checked, no findings.
- `python framework/scripts/skill_build_ea_guard.py --ea-id 37008 --ea-label QM5_37008_garch-volatility-forecast-breakout`
  -> exit 0; registry row, magic rows, and EA directory all true.
- `python framework/scripts/validate_spec_doc.py framework/EAs/QM5_37008_garch-volatility-forecast-breakout`
  -> `1 PASS, 0 FAIL`.
- `python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_37008_garch-volatility-forecast-breakout --json --fail-on-leak`
  -> exit 0; `SINGLE_SYMBOL_OK`, zero violations.
- Relevant suite:
  `python -m pytest tools/strategy_farm/tests/test_qm5_37008_review_rework_static.py tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_validate_symbol_scope.py -q`
  -> `57 passed, 1 failed in 477.87s`. The sole failure is outside this EA and change:
  repository-wide fixture `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`
  reports seven existing QM5_411xx findings, first at
  `QM5_41134_wti-mdaily-iqrmean-mom.mq5:599`; the scoped QM5_37008 hardening check above
  is clean.
- `git diff --check -- framework/EAs/QM5_37008_garch-volatility-forecast-breakout tools/strategy_farm/tests/test_qm5_37008_review_rework_static.py`
  -> exit 0 (only Git line-ending notices).

## Compile interlock evidence

The final strict compile command was scoped to this exact EA:

```powershell
framework/scripts/compile_one.ps1 `
  -EAPath framework/EAs/QM5_37008_garch-volatility-forecast-breakout/QM5_37008_garch-volatility-forecast-breakout.mq5 `
  -EALabel QM5_37008_garch-volatility-forecast-breakout -Strict
```

It failed closed before MetaEditor launch with
`LIVE_FACTORY_AD_HOC_COMPILE_REFUSED` / `INCLUDE_MIRROR_REFUSED` because terminal64
processes are alive. Receipt paths:

- `C:/QM/worktrees/rework-slot-2/framework/build/compile/20260824_095848/QM5_37008_garch-volatility-forecast-breakout.compile.log`
- `D:/QM/reports/compile/20260824_095848/summary.csv`

The instructed governed compile-only fallback was attempted from this deliberate
worktree context; it also failed closed with `BOUND_SETFILE_HASH_EXISTS`,
`enqueued_count=0`. Thus no compile work item or backtest was created, no existing binary
was overwritten, and no unsafe bypass was used. A fresh `.ex5` remains an explicit review
risk requiring an authorized compile lane after this source commit is visible there.

## Rollback

Revert the commit containing this evidence (`git revert <rework-37008-commit>`). That
atomically restores the EA source, SPEC, and all three setfile hashes and removes the
QM5_37008 regression test/evidence. No work-item, registry, factory, backtest, or live
rollback is required; the only database writes were the explicitly required router-state
transitions for this review task.

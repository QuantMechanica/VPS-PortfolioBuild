# greview-12944 — Codex cross-review of Gemini build QM5_12944

**Date:** 2026-08-24

**Router task:** `01bd8a9d-bd3f-4da5-a275-be9192a763ed` (`build_ea`, assigned `gemini`, state observed as `REVIEW`)

**EA:** `QM5_12944_sperandeo-trend-fault-line-h4`

**Approved card:** `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12944_sperandeo-trend-fault-line-h4.md` (`g0_status: APPROVED`)
**Verdict:** **RECYCLE** — the current build is not formally clean for Q02 because the delivered EX5 is stale relative to the current MQ5, one declared strategy input is dead, the news window differs from the approved card, and the source-default risk configuration fails framework initialization.

## Scope and artifact resolution

The required rebaseline proposal, automation inventory, gate-name census, and OWNER vault directive were read before review. No gate criterion or threshold was changed.

`agent_router.py list-tasks --agent gemini` returned the requested task but its current payload contains only `ea_id: "12944"`, the slug, recycle metadata, and `target_agent_profile`; it does not contain the promised `ea_dir`, `card_path`, or `artifact` fields. The missing paths were resolved deterministically:

- EA directory: `framework/EAs/QM5_12944_sperandeo-trend-fault-line-h4/`.
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12944_sperandeo-trend-fault-line-h4.md`.
- Governed repository build artifact: `artifacts/builds/01bd8a9d-bd3f-4da5-a275-be9192a763ed.json`.
- Runtime build artifact: `D:/QM/strategy_farm/artifacts/builds/01bd8a9d-bd3f-4da5-a275-be9192a763ed.json`.

This was a read-only EA review. No compilation, backtest enqueue/delete, verdict-row overwrite, factory toggle, or `T_Live` access occurred. Per the ticket's explicit commit-only rule, no EA code or test file was changed; this evidence note is the only deliverable committed.

## Blocking findings

### 1. Stale EX5 / invalid source-binary lineage

- Current file hashes from `Get-FileHash -Algorithm SHA256`:
  - MQ5: `a83623b3b5946bb593fd7a9a7a2e819b6ccbdbf60d5020809f20b8952d050d31`.
  - EX5: `e19a9d287c247aaec8f074ff0469aeb977f0fc03b349a95617aa02a347c2d711`.
- The initial runtime build artifact binds producer MQ5 `18ece1138bf8394700421bedd273ff2fc5315640c613ead72f110249fe786a79` and the same EX5 `e19a9d28...` (`D:/QM/strategy_farm/artifacts/builds/01bd8a9d-bd3f-4da5-a275-be9192a763ed.json:6-8`).
- The repository artifact was later edited to claim current MQ5 `a83623b3...` with `compile_succeeded: true`, but still names the identical old EX5 `e19a9d28...` (`artifacts/builds/01bd8a9d-bd3f-4da5-a275-be9192a763ed.json:6-8,41-42`). It supplies neither a compile-log path nor a source-bound build identity.
- Git history proves the mismatch: `git diff --name-status 941b08d8 02f10d02 -- framework/EAs/QM5_12944_sperandeo-trend-fault-line-h4` reports only the MQ5 and SPEC as modified; the EX5 blob is unchanged across the source rewrite. The current source therefore cannot be the source that produced the delivered binary.

**Named fix:** after all source defects below are corrected, recompile via governed `COMPILE_EA` and emit durable compile/build-identity evidence binding the exact current MQ5 SHA-256 to the replacement EX5 SHA-256. Do not admit the existing binary to Q02.

### 2. Dead declared strategy input

`strategy_min_pivots` is declared at `framework/EAs/QM5_12944_sperandeo-trend-fault-line-h4/QM5_12944_sperandeo-trend-fault-line-h4.mq5:38` and has no use-site. A complete declared-input occurrence census found every other input at least twice but `strategy_min_pivots` exactly once. Both fault-line paths hard-code three pivots instead (`mq5:119-130` and `mq5:165-176`).

**Named fix:** either remove the input because the approved card fixes the value at three, or wire and validate it through both pivot searches and regression fitting. Regenerate governed setfiles/SPEC if the public input surface changes.

### 3. Card-divergent news window

The approved card requires skipping entries during **±15 minutes** around high-impact news (`D:/QM/strategy_farm/artifacts/cards_approved/QM5_12944_sperandeo-trend-fault-line-h4.md:103-105`). The EA defaults to `QM_NEWS_TEMPORAL_PRE30_POST30` (`mq5:21-26`) and also passes literal `30, 30` to `QM_FrameworkInit` (`mq5:399-414`). This is a fixed-strategy-parameter divergence, not a gate-threshold decision.

**Named fix:** implement the exact entry-only 15/15-minute high-impact blackout through the supported framework news path while retaining stale-calendar and compliance fail-closed behavior; remove the active 30/30 configuration.

### 4. Invalid source-default risk mode

The MQ5 declares both `RISK_PERCENT=0.5` and `RISK_FIXED=1000.0` (`mq5:16-19`). `QM_FrameworkValidateRiskInputs` explicitly rejects configurations where both are positive with `EA_INPUT_RISK_BOTH_SET` (`framework/include/QM/QM_Common.mqh:166-176`), and `QM_FrameworkInit` invokes that validation (`QM_Common.mqh:220-221`). A default attach therefore fails `OnInit`.

All 13 backtest setfiles correctly override `RISK_FIXED=1000` and `RISK_PERCENT=0`, so the governed Q02 risk mode is correct; that does not cure the invalid source default or provide the required live `RISK_PERCENT` preset.

**Named fix:** make the source default single-mode (for example, fixed-risk backtest default with `RISK_PERCENT=0`) and keep governed live setfiles single-mode in the opposite direction (`RISK_FIXED=0`, positive `RISK_PERCENT`).

## Passing review surfaces

- **Mechanism outline:** the current source implements bounded local-pivot scans, three-pivot linear regression, ATR(1) > 1.5 × ATR(20), 0.5 × ATR crossing confirmation, D1 close versus SMA(200), 1.5 × ATR SL, 2.0 × ATR TP, failed-break exit, reverse close/open handling, and the rolling-spread filter (`mq5:51-389`). These do not offset the blocking defects above.
- **Framework chain:** `QM_Common.mqh` include (`mq5:5`), `QM_FrameworkInit` (`mq5:397-415`), MAE hook first in `OnTick` (`mq5:427-430`), kill switch, Friday close, news calls, equity stream, framework trade management, timer, transaction, and tester hooks are present (`mq5:427-481`).
- **Magic:** registry rows are active for slots 0-12 with magics `129440000`-`129440012` (`framework/registry/magic_numbers.csv:16395-16407`). Framework initialization resolves through `QM_MagicChecked` (`framework/include/QM/QM_Common.mqh:223-226`); the resolver computes `ea_id * 10000 + slot` (`framework/include/QM/QM_MagicResolver.mqh:55`).
- **Buffers and raw series:** no raw `CopyBuffer` use exists. `CopyRates(...,160,...)` is bounded and checked by its returned count before indexing (`mq5:245-249,345-349`); static pivot arrays cap at 20 with count-bounded writes (`mq5:92-135,138-181`). Raw `CopyRates`/`iClose` uses carry `perf-allowed` annotations (`mq5:247,251,347`). Automated D10 reported zero failures.
- **Forbidden mechanics:** grep found no ML, invented commission, or invented swap logic.
- **Setfiles:** 13 governed-generator-form backtest setfiles are present. Each declares `environment: backtest`, card provenance, all ten strategy keys, `RISK_FIXED=1000`, `RISK_PERCENT=0`, and the registered symbol slot (example `sets/QM5_12944_sperandeo-trend-fault-line-h4_GDAXI.DWX_H4_backtest.set:1-32`).
- **SPEC:** `python framework/scripts/validate_spec_doc.py framework/EAs/QM5_12944_sperandeo-trend-fault-line-h4` returned `1 PASS, 0 FAIL`.

## Validation evidence

Read-only commands from `C:/QM/worktrees/rework-slot-9`:

1. `python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_12944_sperandeo-trend-fault-line-h4`
   - Result: no failures or warnings; D10 buffer bounds zero failures; registry/matrix valid for 13 symbols.
2. `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_12944_sperandeo-trend-fault-line-h4`
   - Result: `PASS`, 14 files checked, zero findings.
3. `python -m pytest -q tools/strategy_farm/tests/test_build_gate_hardening.py tools/strategy_farm/tests/test_build_guardrails.py tools/strategy_farm/tests/test_gen_setfile.py tools/strategy_farm/tests/test_setfile_canonicalization.py`
   - Result: `53 passed, 1 failed in 448.02s`.
   - The sole failure is the unrelated fleet-wide `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers`, reporting seven findings in `QM5_41134`-class sources. It does not reference QM5_12944 or a touched file.
4. Same pytest selection with `-k "not test_qm5_411xx_sources_have_no_unbounded_numeric_buffers"`
   - Result: `53 passed, 1 deselected in 121.55s`.

The validators' PASS results are narrower than this review: their automation did not classify the card's free-text ±15-minute rule, dead-input semantics, mutually active default risk modes, or EX5 producer lineage. Manual evidence therefore controls the verdict.

## Rollback

- Repository rollback: revert the review commit containing this note with `git revert <review-commit-sha>`; no EA/runtime file is part of that commit.
- Router rollback is separate append-only operational state: if the closure itself is adjudicated incorrect, use the governed router reopen/review workflow. Do not overwrite historical review/verdict rows.

## Final disposition

**RECYCLE.** Correct the dead input, exact news window, and source-default risk mode; then **recompile via governed `COMPILE_EA`** and provide source-bound build identity/compile evidence for the new EX5. The approved Strategy Card remains approved; no Q02 execution is authorized by this review.

# rework-35001 — QM5_35001 review repair evidence

- Router task: `9a869adc-1d15-4e54-bd1f-6605d2a36291`
- EA: `QM5_35001_cowabunga-multi-timeframe-trend-system`
- Worktree: `C:\QM\worktrees\rework-slot-3`
- Approved card: `strategy-seeds/cards/approved/QM5_35001_cowabunga-multi-timeframe-trend-system.md` (`g0_status: APPROVED`)
- Review work order: `docs/ops/evidence/9a869adc_qm5_35001_gemini_build_codex_review_2026-08-17.md`
- Source SHA-256 after repair: `22efb336295f79f7d95779b2f9dcaaad155f786aa199be20e02a2ee47603bdf8`

## What changed

1. Restored the card's exact MACD zero-line transition. Long now requires
   `hist[1] > 0 && hist[2] <= 0`; short uses the exact symmetric transition
   (`framework/EAs/QM5_35001_cowabunga-multi-timeframe-trend-system/QM5_35001_cowabunga-multi-timeframe-trend-system.mq5:226`, `:246`). The broadened positive/negative slope alternatives were removed.
2. Restored the exact swing stop. Long uses `swing_low - buffer` and short uses
   `swing_high + buffer`; an invalid-side stop rejects the entry instead of
   substituting an ATR corridor (`...mq5:230-234`, `:250-254`).
3. Moved open-position management and strategy exits ahead of all entry-only
   news/no-trade filters (`...mq5:387-411`). The MAE hook remains first in
   `OnTick` (`...mq5:383`).
4. Closed the same-card hardening gaps found during rework: the 23:55-00:05
   blackout now converts broker time to UTC (`...mq5:121`); the 2.0% realized
   daily entry halt, 2.5% daily hard stop, 5.0% total drawdown stop, and
   three-tick slippage ceiling are declared, bounded, and wired
   (`...mq5:52-55`, `:94-112`, `:353-370`).
5. Restored the framework risk default: source and both backtest sets use
   `RISK_PERCENT=0` with `RISK_FIXED=1000`; live continues to use the same
   framework `RISK_PERCENT` input through `QM_FrameworkInit` (`...mq5:19-21`,
   `:347`). No live set or deployment artifact was created.
6. Bounded the swing-series loop and fail-closed invalid parameter ranges
   (`...mq5:70-102`, `:204`). Indicator access otherwise remains through the
   `QM_Common.mqh` wrapper chain; the only raw closed-bar series calls retain
   their `perf-allowed:` annotations.
7. Updated `SPEC.md` to describe the implemented zero transition, exact swing
   stop, GMT/loss controls, ordering, and actual risk failure code. Updated both
   setfiles with every strategy input and the repaired source hash.
8. Added static regression coverage in
   `tools/strategy_farm/tests/test_qm5_35001_review_rework.py:53-163` for every
   review finding, all input use-sites, set/hash binding, framework magic/MAE,
   bounded/raw-series rules, forbidden ML/direct-order surfaces, and the two
   active registry slots.

## Registry and input evidence

- EA registry identity exists and is active:
  `framework/registry/ea_id_registry.csv:4448`.
- Existing magic rows are active and unchanged:
  `framework/registry/magic_numbers.csv:17400-17401` (slots 0/1,
  GBPUSD.DWX/EURUSD.DWX, magics 350010000/350010001).
- `QM_FrameworkInit` resolves the host magic through the
  `QM_Common.mqh` -> `QM_MagicChecked` -> `QM_MagicResolver.mqh` chain; the EA
  consumes it through `QM_FrameworkMagic()` (`...mq5:153`, `:268`, `:354-371`).
- Per-input `rg` census found every declared input at its declaration and at
  least one executable/configuration use-site. The regression test enforces
  this invariant (`test_qm5_35001_review_rework.py:107`).
- No registry gap was found, so `magic_numbers.csv`, `ea_id_registry.csv`, and
  `QM_MagicResolver.mqh` were not changed or regenerated.

## Validation output

### EA regression tests

Command:

```text
python -m pytest tools/strategy_farm/tests/test_qm5_35001_review_rework.py -q
```

Result:

```text
......                                                                   [100%]
6 passed in 3.82s
```

### Scoped build-gate hardening

Command:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . --ea-label QM5_35001_cowabunga-multi-timeframe-trend-system
```

Result: exit 0, `files_scanned: 1`, `failures: []`, no warnings. D2 loss
limits, D4 management reachability, D5 GMT conversion, D7 MAE, D9 request
initialization, D10 buffer bounds, D11 registries, and D17 symbol universe all
reported zero failures.

### Focused validators

```text
python framework/scripts/validate_spec_doc.py framework/EAs/QM5_35001_cowabunga-multi-timeframe-trend-system
PASS  QM5_35001_cowabunga-multi-timeframe-trend-system
Summary: 1 PASS, 0 FAIL (of 1)

python tools/strategy_farm/validate_build_guardrails.py --max-news-stale-hours 336 framework/EAs/QM5_35001_cowabunga-multi-timeframe-trend-system
verdict: PASS; files_checked: 3; findings: []

python tools/strategy_farm/validate_symbol_scope.py --ea-label QM5_35001_cowabunga-multi-timeframe-trend-system --json --fail-on-leak
verdict: SINGLE_SYMBOL_OK; n_violations: 0
```

### Touched/relevant hardening tests

Command:

```text
python -m pytest tools/strategy_farm/tests/test_qm5_35001_review_rework.py tools/strategy_farm/tests/test_build_gate_hardening.py -q
```

Result: `34 passed, 1 failed in 531.56s`. The sole failure is unrelated to this
ticket: `test_qm5_411xx_sources_have_no_unbounded_numeric_buffers` reports seven
pre-existing `QM5_41134_*` and later 411xx findings. The six QM5_35001 tests all
passed, and the direct EA-scoped hardening command above returned no failures.
No 411xx file was touched.

### Compile/build-check refusal receipt

Strict command attempted:

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/compile_one.ps1 -EAPath framework/EAs/QM5_35001_cowabunga-multi-timeframe-trend-system/QM5_35001_cowabunga-multi-timeframe-trend-system.mq5 -Strict
```

The governed preflight refused before MetaEditor ran:

```text
failure_class: LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
reason_class: INCLUDE_MIRROR_REFUSED
errors: -1
warnings: -1
```

Receipt: `D:/QM/reports/compile/20260824_114235/summary.csv`. A separately
scoped `build_check.ps1 -EALabel QM5_35001_cowabunga-multi-timeframe-trend-system -SkipCompile`
attempt was refused by the same live-factory preflight. At the final probe,
terminal64 was active on T1-T6 and T8-T10. Per task controls, the factory and
terminals were not stopped or toggled. The governed `compile-status` query
returned `NOT_ENQUEUED`; the attempted noncanonical enqueue was rejected before
mutation, so no compile or backtest work item was created.

The existing EX5 was therefore not rebuilt and is deliberately not claimed as
evidence for the repaired source. Its unchanged SHA-256 is
`952d324cbe31a56029a70b94242101655ec82f9712f67adb416af2fd90908d7b`;
review must treat fresh compilation as the remaining infrastructure-bound risk.

## Scope and safety

- No backtest was enqueued, deleted, or run.
- No verdict row was overwritten.
- No gate threshold/criterion or pipeline contract was changed.
- Factory state, AutoTrading, `C:/QM/mt5/T_Live`, and all live/deploy artifacts
  were untouched.
- No other EA was edited by this ticket.

## Rollback

After commit, revert the single ticket commit with `git revert <ticket-commit>`.
This restores the prior EA source/SPEC/setfiles and removes the ticket test and
evidence note. The unchanged EX5 and registries require no rollback.

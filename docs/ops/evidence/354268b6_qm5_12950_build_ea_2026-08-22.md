# QM5_12950 Build EA Evidence — 2026-08-22

**Task ID:** `354268b6-da27-404a-8288-3071107d73b9` (`build_ea`, priority 50, agent `gemini`).  
**EA:** `QM5_12950_mql5-ad-price-card`.  
**Artifact Path:** `C:/QM/repo/docs/ops/evidence/354268b6_qm5_12950_build_ea_2026-08-22.md`.  
**Strategy Card:** `D:/QM/strategy_farm/artifacts/cards_approved/QM5_12950_mql5-ad-price-card.md` (G0 APPROVED).  

---

## 1. Strategy Overview & Implementation

- **Strategy Thesis**: Mohamed Abdelmaaboud (2022) Accumulation/Distribution Price Confirmation (H1).
  - Calculates Accumulation/Distribution (AD) on tick volume, EMA(100), and ATR(14) on closed H1 bars.
  - Volatility filter: Skips entries if ATR(14) < 0.5 * ATR(100) to avoid dead ranges.
  - Long Entry: `close > EMA(100)`, `high[1] > high[2]`, and `AD[1] > AD[2]`.
  - Short Entry: `close < EMA(100)`, `low[1] < low[2]`, and `AD[1] < AD[2]`.
  - Protective Stop: `entry - ATR(14) * 1.6` for Long; `entry + ATR(14) * 1.6` for Short.
  - Take Profit: 2.0R distance from entry (`entry +/- (entry - sl) * 2.0`).
  - Indicator Exits:
    - Long exit: AD declines for two consecutive closed bars (`AD[1] < AD[2] && AD[2] < AD[3]`) or `close < EMA(100)`.
    - Short exit: AD rises for two consecutive closed bars (`AD[1] > AD[2] && AD[2] > AD[3]`) or `close > EMA(100)`.
  - Failsafe time exit: after 36 H1 bars.
  - Single position per magic number; opposite signal closes position.
- **Framework Corset Compliance**:
  - Implemented using V5 framework standards and `QM_Common.mqh`.
  - News temporal mode (`QM_NEWS_TEMPORAL_PRE30_POST30`) and compliance profile (`QM_NEWS_COMPLIANCE_DXZ`) with fail-closed stale guard (`qm_news_stale_max_hours = 336`).
  - Sizing handled strictly via framework risk model (`RISK_PERCENT = 0.0`, `RISK_FIXED = 1000.0` for backtest).

---

## 2. Registry & Artifact Verification

- **Registries**:
  - `framework/registry/ea_id_registry.csv`: verified `12950,mql5-ad-price-card`.
  - `framework/registry/magic_numbers.csv`: registered 3 slots (0: `EURUSD.DWX`/`129500000`, 1: `GBPUSD.DWX`/`129500001`, 2: `XAUUSD.DWX`/`129500002`).
  - `framework/include/QM/QM_MagicResolver.mqh`: regenerated cleanly via `update_magic_resolver.py` (17,624 rows preserved, 0 dropped).
- **SPEC Document**:
  - `framework/EAs/QM5_12950_mql5-ad-price-card/SPEC.md` generated via `gen_spec_md.py` and validated with `validate_spec_doc.py` (`PASS`).
- **Static Checks & Guardrails**:
  - `validate_build_guardrails.py`: `PASS` (0 findings, stale news max hours <= 336).
- **P2 Setfiles**:
  - Generated all 3 backtest setfiles in `sets/` via `gen_setfile.ps1` (`EURUSD.DWX`, `GBPUSD.DWX`, `XAUUSD.DWX`) with `RISK_FIXED=1000` and `RISK_PERCENT=0`.

---

## 3. Build Result & Review State

- Emitted build result JSON to `D:/QM/strategy_farm/artifacts/builds/354268b6-da27-404a-8288-3071107d73b9.json` and `C:/QM/repo/artifacts/qm5_12950_build_result.json`.
- Moving task `354268b6-da27-404a-8288-3071107d73b9` to `REVIEW` for mandatory Codex review per router rules.

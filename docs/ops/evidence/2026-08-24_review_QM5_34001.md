# Review — QM5_34001 kalman-filter-state-estimation-scalper (review_ea 93e11b54)

- Router task: `93e11b54-6405-4f6a-acc4-92f356a176ee` (review_ea, assigned codex; source build_ea `f9a8b8f7`, source_agent gemini/agy)
- EA dir: `framework/EAs/QM5_34001_kalman-filter-state-estimation-scalper/`
- Card: `strategy-seeds/cards/approved/QM5_34001_kalman-filter-state-estimation-scalper.md` (G0 APPROVED)
- Reviewer: Claude (review lane), 2026-08-24. DB read-only; no recompile, no dispatch, no T_Live.

## Verdict: RECYCLE — stale ex5 (no COMPILE_OK for the current source hash)

### Blocking defect
The MAE hook was added to the source on 2026-08-23 (commit `5ce4af45c`):
`QM5_34001_...mq5:218` inserts `QM_FrameworkTrackOpenPositionMae();` into `OnTick`.
- Current `.mq5` sha256 = `2095ff43…28c0`, mtime **2026-08-23 17:22**.
- Committed `.ex5` sha256 = `7287c011…d608`, mtime **2026-08-17 21:24** — predates the hook.
The build artifact (`artifacts/builds/f9a8b8f7…json`) and the build note
(`docs/ops/evidence/f9a8b8f7_qm5_34001_build_ea_result_2026-08-23.md`) pair the
post-hardening mq5 hash with the pre-hardening ex5 hash and still assert
`compile_succeeded: true` / "Hardening D7 MAE hook: Added". The delivered binary does
NOT contain the MAE hook it claims to add. There is no COMPILE_OK evidence for the
current source hash, and by the build-date predicate the ex5 is older than the mq5, so a
Q02 dispatch would run stale/void (stale-ex5 class).
Setfile `build_hash` also pins the stale ex5 (`7287c011…d608`).

**Actionable fix (build lane, ROT for review lane):** governed recompile in the OFF
window / COMPILE_EA queue to produce an ex5 that includes the MAE hook, then regenerate
the three setfiles (build_hash) against the new ex5, then re-submit for review.

### Clean on all other axes (would pass once ex5 is rebuilt)
- **Card-faithful mechanism:** entry Z<=-thr & Close>Open -> BUY, Z>=+thr & Close<Open ->
  SELL; SL=1.5*ATR(14); TP at Kalman state estimate; spread filter 1.8*ATR; rollover
  blackout 23:55-00:05 — all match card §3. Kalman recursion evaluated at shift=1 (no
  lookahead), bounded 100-bar buffer.
- **No dead inputs:** all 7 `strategy_*` inputs have use-sites beyond declaration
  (q/r -> ComputeKalmanState; z_threshold, atr_period, sl_atr_mult -> Strategy_EntrySignal;
  spread_atr_period, spread_atr_mult -> Strategy_NoTradeFilter).
- **Framework conformity:** QM_Common chain; QM_FrameworkInit/Shutdown; magic via
  QM_FrameworkMagic() (MagicResolver), registry rows 340010000/1/2 = 34001*10000+slot,
  3 active rows; news wired (QM_NewsAllowsTrade2 in OnTick); Friday close; RISK_FIXED
  input + setfile RISK_FIXED=1000/RISK_PERCENT=0 for backtest ENV via governed generator;
  MAE hook present in SOURCE; no ML (closed-form Kalman recursion); no invented
  commission/swap; no raw unbounded series.
- **Minor (non-blocking):** card §3.1.3 daily-loss-limit not in Strategy_NoTradeFilter
  (relies on framework kill switch); card §5 BE/trailing states are aspirational, not in
  deterministic exit rules §3.4 — Manage/Exit no-ops match §3.4 (SL/TP set at entry).

Once the ex5 is rebuilt to match `2095ff43…28c0`, this EA is clean for Q02.

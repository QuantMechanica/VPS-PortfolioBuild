# rework-9932 — QM5_9932 review repair evidence

Date: 2026-08-24 (ticket-required evidence filename date: 2026-08-23)

- Router task: `1db5da1a-e87a-486f-a6f6-2224724a09fe`
- Source build task: `c71f308a-76be-456f-8ae7-a5d7c3abc4e0`
- EA: `QM5_9932_bandy-roc-zscore-normalised-mr-index`
- Approved card: `D:/QM/strategy_farm/artifacts/cards_approved/QM5_9932_bandy-roc-zscore-normalised-mr-index.md`
- Review work orders: `docs/ops/evidence/2026-08-24_review_QM5_9932.md` and
  `docs/ops/evidence/2026-08-23_codex_review_1db5da1a_QM5_9932.json`

## What changed

1. The delivered universe is now exactly the card-authorized D1 index set:
   NDX.DWX, SP500.DWX, and WS30.DWX. The SPEC records that contract at
   `framework/EAs/QM5_9932_bandy-roc-zscore-normalised-mr-index/SPEC.md:44`
   and rejects registry-only universe expansion at `:51`. The ten unauthorized
   backtest setfiles were removed; the three retained files use existing magic
   slots 1, 2, and 4 and keep `RISK_FIXED=1000` / `RISK_PERCENT=0`.
2. Every pre-existing registry allocation remains unchanged, as required by
   the ticket. The three consumed rows are
   `framework/registry/magic_numbers.csv:17138`, `:17139`, and `:17141`.
   Rows `:17137`, `:17140`, and `:17142-17149` remain allocated but have no
   delivered setfile and therefore do not expand this build's universe.
3. The reviewed trading mechanism is unchanged. All ten declared strategy
   inputs remain at MQ5 lines 38-47 and have executable use-sites through
   lines 79-226. The backtest risk defaults are at MQ5 lines 19-20, magic is
   resolved through `QM_FrameworkMagic()` at lines 171/210/289, and the MAE
   hook is first in `OnTick` at line 270.
4. The bounded ROC buffers now have explicit allocation proofs at MQ5 lines
   85 and 93. The card-required `CopyRates` window and the two D1 readiness
   probes carry same-line `perf-allowed` reviewer markers at lines 88, 140,
   and 168. No raw indicator handle, unbounded buffer, or ML path was added.
5. A durable build-time copy of the approved card is at
   `framework/EAs/QM5_9932_bandy-roc-zscore-normalised-mr-index/docs/strategy_card.md:1`.
   Truthful producer artifacts bind the exact MQ5/EX5 and compile log hashes at
   EA `build_result.json:8-10`, `:38-47`, and `build_identity.json:6-12`.
6. Five focused regressions cover the approved card/SPEC universe, all ten
   strategy input use-sites and mechanism, framework/risk/magic/MAE/buffer/
   performance conformity, exact setfile delivery, and MQ5/EX5/setfile/spec/
   card hash binding at
   `tools/strategy_farm/tests/test_qm5_9932_review_rework.py:55`, `:71`, `:90`,
   `:108`, and `:135`.

No gate threshold/criterion, verdict row, backtest queue, factory state,
AutoTrading setting, T_Live path, EA-ID registry row, magic allocation, or
resolver file was changed. No backtest or smoke test was run or enqueued.

## Compile and build-gate evidence

The required scoped command was run twice (normal and `-SkipCompile`; the
interlock executes before either path can scan):

```text
powershell -NoProfile -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 \
  -EALabel QM5_9932_bandy-roc-zscore-normalised-mr-index
exit=1
failure_class=LIVE_FACTORY_AD_HOC_COMPILE_REFUSED
retry_attempted=false
detail=terminal64 processes are alive; ad-hoc compile/build_check is refused
```

The factory was not toggled and no compile/backtest work item was enqueued.
The exact repaired source was compiled in a disposable portable MetaEditor
copy containing the current repository framework includes. Source bytes were
verified before and after compile before the EX5 was copied back.

```text
compile log: D:/QM/strategy_farm/temp/rework-9932-slot12-20260824T143000Z/compile.log
Result: 0 errors, 0 warnings, 7847 ms elapsed, cpu='X64 Regular'
source SHA-256: 20553e4da0339eb9a8d5a949a230b67c5980cd7957bcdf607f6700613e970a34
EX5 SHA-256:   feea799e3e46a96c0f6094e76d6d4dfc1f4069c6c800195430be00b99f2d83dc
EX5 bytes:     401436
log SHA-256:   3eae69375c3ef0eea691b9c735a7e3acd005f1b1ffa4c269a1fd8f413aeba747
```

`build_result.json:38` and `build_identity.json:6` therefore retain
`build_check_passed=false`; neither artifact misrepresents the interlocked
full-script result. Compile success is separately bound to the exact hashes.

## Test and validation output

Focused EA regressions after final hash binding:

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_9932_review_rework.py
.....                                                                    [100%]
5 passed in 1.27s
```

Touched hardening and symbol-scope module tests:

```text
python -m pytest -q tools/strategy_farm/tests/test_qm5_9932_review_rework.py \
  tools/strategy_farm/tests/test_build_gate_hardening.py \
  tools/strategy_farm/tests/test_validate_symbol_scope.py
.....................................                                    [100%]
37 passed in 638.73s (0:10:38)
```

Scoped validators:

```text
python tools/strategy_farm/build_gate_hardening.py --repo-root . \
  --ea-label QM5_9932_bandy-roc-zscore-normalised-mr-index
PASS — 1 file, 0 failures, 0 warnings

python tools/strategy_farm/validate_build_guardrails.py \
  framework/EAs/QM5_9932_bandy-roc-zscore-normalised-mr-index
PASS — 4 files, 0 findings

python tools/strategy_farm/validate_symbol_scope.py \
  --ea-label QM5_9932_bandy-roc-zscore-normalised-mr-index --json --fail-on-leak
SINGLE_SYMBOL_OK — 0 violations

python framework/scripts/validate_spec_doc.py \
  framework/EAs/QM5_9932_bandy-roc-zscore-normalised-mr-index
PASS — 1 PASS, 0 FAIL

python framework/scripts/skill_build_ea_guard.py --ea-id 9932 \
  --ea-label QM5_9932_bandy-roc-zscore-normalised-mr-index
status=ok; ea_registry_row=true; magic_registry_rows=true; ea_dir_exists=true

git diff --check
PASS — no whitespace errors
```

## Risks and open review points

- Ten pre-existing active magic allocations remain outside the approved card
  universe. The ticket explicitly forbids changing existing allocations; no
  delivered setfile or EA code consumes them. The focused regression enforces
  the three-symbol artifact universe because the current hardening card parser
  reports an empty `card_target_symbols` list for this prose-form card.
- Full `build_check` cannot be claimed PASS while the live-factory interlock is
  active. Review can accept the isolated exact-source 0/0 compile plus scoped
  gates, or request a later governed COMPILE_EA/OFF-window run.
- Smoke remains `deferred_p2_smoke`; running or enqueueing it was outside this
  build-only rework ticket.

## Rollback

Revert the ticket commit with `git revert <rework-9932-commit>`. This restores
the prior MQ5, EX5, SPEC, and thirteen-setfile delivery and removes the focused
test plus EA-local card/build identity/result and this evidence file. No
database, registry, queue, factory, terminal, or live rollback is required
because none of those surfaces was changed beyond the authorized router task
transitions.

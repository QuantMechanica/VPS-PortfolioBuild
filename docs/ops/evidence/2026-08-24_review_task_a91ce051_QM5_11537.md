# Codex re-review — QM5_11537 Carter H1 EMA/BB/RSI

- Router task: `a91ce051-fe84-4156-a3fb-0f566aca2365`
- Review date: 2026-08-24
- Branch: `agents/board-advisor`
- Verdict: **FAIL / RECYCLE — keep in REVIEW**

## Blocking finding

The H1 execution contract remains undeclared. The strategy reads closed `PERIOD_H1` price/EMA75/Bollinger-middle/RSI state (`mq5:100-103`), but `OnInit()` only calls the legacy `QM_FrameworkInit` (`mq5:174-195`) and the entry cadence still uses the no-argument, chart-period `QM_IsNewBar()` (`mq5:244`). There is no `QM_FrameworkDeclareExecutionContract(PERIOD_H1, ...)` call or equivalent fail-closed H1 initialization guard.

A wrong-period chart can therefore evaluate H1 state on the chart's cadence instead of failing initialization. This is the same uncorrected blocker recorded in the 2026-08-21 independent review; the current MQ5 and EX5 hashes are identical to that review.

Required repair: declare the H1 execution contract and the card's Friday-close mode, rebuild through the governed compile path, refresh the set/build identity hashes, and return for a fresh review.

## Current hash identity

- MQ5 SHA-256: `9797890d9f32e5cbd28f994565d0c2c2a031427add1f75e147618d68b987b39a`
- EX5 SHA-256: `5be000517de4d42f4f124447d8275dc0c1b509cea2b91b1fa5bc69f9856ad3c3`
- Last source commit remains `52065efc2ab838aadb71469e7f49764e05613847` (2026-08-16); no source rework has followed the rejection.
- The prior strict compile/build evidence remains hash-bound to these exact files: compile 0 errors/0 warnings and strict build-check PASS. Its mechanical PASS does not prove the missing semantic execution contract.

## Reverified passing scope

- The approved card's otherwise mechanical EMA75/Bollinger-middle/RSI direction rule, five-H1-bar swing stop capped at 40 pips, 2R target, 15-pip entry spread cap, and no-Friday entry remain implemented.
- Active identity and magic rows are exact: EURUSD.DWX slot 0 / `115370000`; GBPUSD.DWX slot 1 / `115370001` (`ea_id * 10000 + slot`).
- Both tracked H1 backtest sets use `RISK_FIXED=1000` and `RISK_PERCENT=0` with slots 0 and 1.
- The card states no account daily/total loss percentages, so no card-specific percentage override is missing.
- Fresh `validate_build_guardrails.py` at 2026-08-24T14:11Z returned PASS with `max_news_stale_hours=336` and no findings.
- Fresh `build_gate_hardening.py` returned exit 0 with no failures or warnings and verified the exact two-symbol card/set/registry universe.

## Scope boundary

No source, set, registry, queue, compiler, terminal, backtest, pipeline, or live-trading state was changed. This review does not infer a pipeline verdict and does not authorize promotion.

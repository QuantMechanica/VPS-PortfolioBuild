# QM5_10025 FX pairs Q02 timeout repair — 2026-08-05

## Outcome

`QM5_10025_rw-fx-broad-pairs` was repaired as the paced fleet's diverse
Q02-infrastructure unit. The strategy remains a structural, low-frequency H4
market-neutral FX spread sleeve over its seven approved DWX majors. Its
recurrent real-tick timeout path is now H4-gated, the rebuilt EX5 passes the
strict framework gate, and the governed build transition created three
priority-track Q02 probes.

No manual backtest, T_Live action, AutoTrading action, portfolio-gate mutation,
or live-manifest change was performed.

## Farm claim and diagnosis

Build-rework task `71d862ed-21b8-4337-8986-c1366dd692dc` was atomically
claimed as
`paced_fleet:infra_repair:QM5_10025:fcce03cae4e8`. Farm event `341386`
records the claim by `codex:agents/board-advisor` at
`2026-08-05T02:57:33+00:00`.

The task's durable trigger was `STRATEGY_HANG_RECURRENT`: 18 of 35 sampled
runs ended as `ACTIVE_TIMEOUT` (51.4%). Recent examples exceeded the 45-minute
worker limit while using the same old binary:

| Symbol | Work item | Runtime at classification | Terminal |
|---|---|---:|---|
| USDJPY.DWX | `d05b4de5-dfd8-40c0-80f6-dde3b7a060ae` | 48.58 min | T7 |
| USDCAD.DWX | `f83e63c3-60f8-4807-9dac-c4bdb5e1a0aa` | 51.40 min | T5 |

Those executions were bound to MQ5
`054311c6eb44c1f00b5be879f45f592d1299dfb4897c5bd1fea5dd869997837e`
and EX5
`bc76d9cc436fc1012631eda839068012e25eaa528b02b601714298ae87b6df0a`.

Static inspection found four interacting defects:

- Every real tick ran seven universe-wide calendar checks and then repeated a
  host-symbol calendar check.
- Every real tick read spreads for the full seven-symbol formation universe,
  although only the selected two-leg package can trade.
- A month with no qualifying pair repeated all six 252-bar pair scans on every
  H4 bar instead of caching the negative monthly selection.
- Historical broad fanout had created setfiles and pending Q02 work outside the
  approved card and magic registry.

The entry package could also leave a directional orphan when only one leg
opened; this was corrected while the pair execution path was in scope.

## Repair

- `OnTick` now rejects non-H4 and non-card hosts before cross-symbol work and
  gates all history, spread, and entry-news work behind `QM_IsNewBar`.
- Exit and Friday-close handling run independently of the entry-news blackout.
- Closed-bar state is cached so exit and entry cannot repeat the same
  multi-symbol history reads; failed monthly selection is cached until the
  month changes.
- Spread and news checks cover only the selected host/partner package.
- Two-leg entry is atomic: a partial package is immediately rolled back.
- Q08 MAE sampling remains the first lifecycle action in `OnTick`.
- The repository now retains only the seven authorized H4 fixed-risk setfiles;
  30 non-card setfiles were removed.
- The exact approved card was copied to `docs/strategy_card.md`, and the SPEC
  was brought onto the mandatory seven-section contract with the approved
  low-frequency estimate of six round trips/year/host.

The authorized magic rows remain slots 0-6, bases `100250000` through
`100250006`, for EURUSD, GBPUSD, AUDUSD, NZDUSD, USDCHF, USDCAD, and USDJPY.
Every retained backtest setfile has `RISK_FIXED=1000`, `RISK_PERCENT=0`, and
`PORTFOLIO_WEIGHT=1`.

## Build evidence

| Check | Result | Evidence |
|---|---|---|
| SPEC validator | PASS | `python framework/scripts/validate_spec_doc.py framework/EAs/QM5_10025_rw-fx-broad-pairs` |
| Build guardrails | PASS; no findings across 8 files | `python tools/strategy_farm/validate_build_guardrails.py framework/EAs/QM5_10025_rw-fx-broad-pairs` |
| Strict build gate | PASS; 0 failures, 0 warnings | `D:/QM/reports/framework/21/build_check_20260805_030459.json` |
| Strict compile | PASS; 0 errors, 0 warnings | `C:/QM/repo/framework/build/compile/20260805_030459/QM5_10025_rw-fx-broad-pairs.compile.log` |
| Compile summary | PASS | `D:/QM/reports/compile/20260805_030459/summary.csv` |
| Build task | `done` | `71d862ed-21b8-4337-8986-c1366dd692dc` |
| Build result | recorded | SHA-256 `f73965261de56759ef827ee945018ed51c71f690cf383e8fc1b6c84450e4e2b9` |

Current artifact bindings:

| Artifact | SHA-256 |
|---|---|
| MQ5 | `bb299cec5c9af45054922214697fe08965888da0a0965df1dfbdad1a2216c8d5` |
| EX5 | `7ea6c83f7dae633c2602c6e0d00fecb536a6e9457146a90614c1de506a3baae0` |
| Approved/local strategy card | `6424aa2ac88f13c44bb988dd3890684124dc3aa6935bc5615624c9519f6d3de9` |

The governed build skill is build-only, so the result records
`deferred_p2_smoke`. A read-only slot scan at
`2026-08-05T03:12:30+00:00` found three paced factory terminals running; no
extra tester was launched by this repair.

## Q02 handoff and scope cleanup

At `2026-08-05T03:10:24+00:00`, farm event `341387` recorded a
compare-and-set cleanup with preimage SHA-256
`668fd32a564f0d3e569be846481ced73294d80aa9489c17ece4b99eab3b439bc`:

- 22 unclaimed pending Q02 rows outside the approved card/magic scope were
  terminally preserved as `SUPERSEDED`;
- the old unbound USDJPY pending row
  `24fde4cb-4c5d-4257-ac15-c310506e0fe0` was superseded for current-binary
  replacement;
- all historical terminal evidence was left unchanged.

`record_build_result.auto_q02` then created the governed three-symbol stage-1
wave, all at attempt 0 with `priority_track=true`:

| Symbol | Work item | Initial state |
|---|---|---|
| USDJPY.DWX | `7337e624-4253-44fe-a941-d0ffdbac705b` | pending |
| NZDUSD.DWX | `fcb19233-33a5-4ec6-8ce4-940940ba0c49` | pending |
| USDCHF.DWX | `e10922e9-9ec4-460d-a338-7992e91187be` | pending |

The other four approved hosts already had one open Q02 row each and were not
duplicated. After the transition, the EA had exactly one open Q02 row for each
of its seven authorized hosts and none outside the card. Farm event `341388`
records the claimed build task's `pending -> done` transition.

# Review — QM5_12932 wyckoff-phase-e-markdown-continuation-h4 (H4)

- **Router task:** c70f99bf-2ad7-46a6-a399-3fa8b0213fa9 (`review_ea`, state REVIEW, assigned codex)
- **Source build task:** d9a93eb1-ac7a-489c-bf8d-5e45162142dd (`build_ea`, agy/gemini)
- **Source verdict:** "PASS: Rebuilt end-to-end … Awaiting Codex review."
- **Reviewer (this note):** Claude review lane, read-only (`mode=ro`), independent re-verification
- **Verdict: RECYCLE** — named, actionable card-fidelity and framework-conformity defects; source needs rework + recompile before Q02.

## Artifacts reviewed
- `framework/EAs/QM5_12932_wyckoff-phase-e-markdown-continuation-h4/QM5_12932_wyckoff-phase-e-markdown-continuation-h4.mq5` (sha256 `5c7d4644…30ab`, matches `build_result.json`)
- `.../SPEC.md`, `.../sets/` (13 backtest setfiles), `.../build_result.json`
- Prior codex review artifact `docs/ops/evidence/c70f99bf_qm5_12932_gemini_codex_review_2026-08-23.json` (verdict FAIL)

## Independently confirmed defects (file:line)

1. **Stale EX5 — binary does not bind reviewed source.** `.mq5` mtime `2026-08-23 20:13:57`; `.ex5` mtime `2026-08-23 11:04:33` (ex5 predates source by ~9h). `build_result.json compile_succeeded=true` reflects the earlier compile, not the current hash. Not COMPILE_OK / D10-clean for the reviewed source. Recompile required in an OFF window / COMPILE_EA queue (ROT: no recompile in active inventory by review lane).

2. **Kill-switch precedes MAE sampling (skeleton-order violation).** `OnTick` lines 447–448: `QM_KillSwitchCheck()` returns before `QM_FrameworkTrackOpenPositionMae()`. When the kill switch trips, Q08 open-position MAE sampling is skipped. Fleet census: 653/701 EAs sample MAE *before* the kill switch; 12932 is in the deviating minority. Reorder to MAE-first.

3. **Time-stop measures wall-clock seconds, not completed H4 bars.** `Strategy_ExitSignal` lines 402–407: `TimeCurrent() - open_time >= strategy_time_stop_bars * tf_seconds`. Weekends/market closures advance elapsed seconds without producing H4 bars, so the "50 completed H4 bars" stop fires early. Count completed bars instead.

4. **One-range-per-break state consumed before the order is placed.** `Strategy_EntrySignal` lines 350–353 set `g_last_entry_time / g_last_range_start_time / g_last_range_L / g_last_range_U` on signal, but `QM_TM_OpenPosition` runs later at lines 479–483. A rejected/failed order still burns the range's reuse state → missed valid re-entries. Commit state only on confirmed fill.

5. **Gate 6 "at-resistance" test is one-sided.** Lines 285–288: rejects only when `bar1.high` is both >0.5·ATR from `L` AND below `L−0.5·ATR`. Highs arbitrarily *above* `L` pass. The card's alternative "within 0.5 ATR of most recent significant pullback pivot high" branch is not implemented. Tighten to a symmetric band around L and add the pivot-high branch.

6. **TP/scale-out structure does not match card.** Card (per codex review + SPEC reference) requires 60% partial close at TP1, breakeven for the remainder, TP2 at 1.5 range amplitudes. Source line 338 sets the *full* position TP at TP1 (`entry − range_amp`, 1.0×); lines 378–389 only move SL to breakeven at the same threshold. No partial close, no TP2.

7. **News blackout absent.** Inputs default all news modes off — `qm_news_temporal = QM_NEWS_TEMPORAL_OFF`, `qm_news_compliance = QM_NEWS_COMPLIANCE_NONE`, `qm_news_mode_legacy = QM_NEWS_OFF` (lines 22–26). Card requires a ±2 H4-bar (480/480-min) high-impact blackout; codex build-gate hardening flags `EA_CARD_NEWS_WINDOW_MISMATCH`. Setfiles must carry the required window via the governed generator.

## Additional finding (this review)
- **Approved card missing from repo.** SPEC.md and `build_result` cite `artifacts/cards_approved/QM5_12932_wyckoff-phase-e-markdown-continuation-h4.md`, but it is absent from the working tree and from `HEAD` (`git show HEAD:… → does not exist`). The card body must be restored/committed so card-fidelity is auditable at Q00→Q02.

## Conformity that PASSES
- `QM_Common.mqh` chain, `QM_FrameworkInit/Shutdown`, `OnTradeTransaction`/`OnTimer` wiring present.
- RISK_FIXED=1000 / RISK_PERCENT=0 (backtest-correct); magic via `QM_FrameworkMagic()` / MagicResolver; 13 active magic rows (`magic_numbers.csv`), ea_id row active.
- Bounded indicator access (`QM_ReadBar`/`QM_ATR`/`QM_SMA` in bounded loops), no raw series, no ML, no invented commission/swap. Forbidden-pattern grep clean (concurs with codex `forbidden_grep: PASS`, `magic_registry: PASS`).

## Recommended next step
RECYCLE to a Codex build task: fix defects 2–7, restore the approved card (finding), regenerate setfiles via `gen_setfile.ps1` with the news window, then recompile in an OFF/COMPILE_EA window so the EX5 binds the fixed source before Q02.

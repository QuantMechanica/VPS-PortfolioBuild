# OPS_LOG — 2026-08-18

Watchdog nach Work Order §2. Alle acht Checks je Zyklus, **Grün-Einträge inklusive**.

---

## Zyklus 1 — 2026-08-18T13:0x–13:1xZ (vor jeder Analyse, wie §2.1 verlangt)

| # | Prüfung | Ergebnis | Ampel |
|---|---|---|---|
| 1 | Queue depth je Stage | Q02 690 pending/0 aktiv/**322 min** ohne Fortschritt · Q03 81/0/**309 min** · Q04 **1.497**/0/14 min · Q05 13/0/**207 min** · Q06 2/0/**207 min** · Q07 34/0/**243 min** · Q08 10/2/8 min · Q09_NEWS 19/0/58 min · Q09_PORTFOLIO 2/0/**337 min** | **ROT** (6 Stufen) |
| 2 | Active slots vs. laufende Prozesse | 2 aktive work_items, beide PIDs lebend (15836, 4576) → **bleeding = 0** | grün |
| 3 | RUNNING ohne Heartbeat, > 2× Median | QM5_12958/XAUUSD **169 min** gegen Q08-Median **40 min** → 4,2× | **ROT** |
| 4 | Fehlerklassen-Verteilung, 60 min | 5 Läufe: FAIL_SOFT 4 (80 %), PENDING_RUNNER 1 (20 %) | **ROT nach Kriterium** |
| 5 | Schreibfehler / Locks auf der Ergebnis-DB | **0 echte Lock-Events** | grün |
| 6 | Disk / Terminals | **C: 46,8 GB von 476,1 = 10 %** · D: 160,7 von 953,9 = 17 % · terminal64 = 4 | **ROT (C:)** |
| 7 | `git status` + ungepushte Commits | 212 dirty paths · `agents/board-advisor` **0 ungepusht** · **1.014 Commits** über 34 Branches von keinem Remote erreichbar | siehe Triage |
| 8 | Neue Verdikte vs. eingefrorener Binary-Hash | 5 Verdikte im Fenster: 1 hash-verifiziert, **4 ohne Bindung**, 0 Mismatch | grün mit Vorbehalt |

### Triage der roten Punkte

**Check 1 (ROT) — Ursache bekannt und gewollt, Kosten jetzt beziffert.** Die sechs stillstehenden
Stufen sind die Folge des Vorziehens der 28 Batch-Zeilen (Entscheidung v9 §0). Die Flotte fährt
ausschließlich Q08-Batch. **Kosten: 690 Q02 + 1.497 Q04 + 81 Q03 + 34 Q07 pending bei null aktiven
Zeilen über 3–5,5 Stunden.** Kein Eingriff — die Entscheidung steht, und der Rückbau (7.R) ist für
das Ende von (b) geplant. **P2, ins Log, kein Wechsel von Track A.**

**Check 3 (ROT) — geprüft, kein Defekt.** Der Detektor der Fabrik selbst wurde read-only gefahren:
`_phase_runner_external_report_progress` liefert `determined: true`,
`reason: phase_runner_external_artifact_growth`, 12 Artefakte, jüngstes 12:24:06Z, `stalled_min`
29,8. Der Lauf arbeitet. **Mein früherer Verdacht „Lauf ist tot" war falsch** — ich hatte
`report_root` unter `D:\QM\reports\work_items\…` geprüft, während die kanonische Q08-Tester-Session
unter `D:\QM\reports\pipeline\<EA>\` schreibt. Falscher Ort, nicht fehlender Fortschritt. **P3.**

**Check 4 (ROT nach Kriterium) — nicht aussagekräftig.** n = 5. Vier FAIL_SOFT bei fünf Läufen sind
von der Batch-Grundrate nicht unterscheidbar. Nach Work Order §4.4 keine Auffälligkeit ohne
Basisrate — hier fehlt sie mangels Stichprobe. **P3, nur notiert.**

**Check 5 — Fehlalarm meiner eigenen Abfrage, korrigiert.** Der erste Lauf meldete 990 Lock-Events.
Die Triage zeigt: **alle 990 sind `quota_gate_blocked`**, mein Filter traf auf „b**lock**ed" im
`detail_json`. Echte DB-Locks: **0**. Der Rest ist die bekannte Event-Flut (~1.500/h, bereits als
Dauerlast geführt). **Grün.**

**Check 6 (ROT) — C: bei 10 % frei.** Kein Datenverlustrisiko, kein Durchsatzstopp → **P2**. Eine
rekursive Verbrauchsanalyse lief in den 2-min-Timeout und danach im Hintergrund; Ergebnis wird
nachgetragen. Kein Eingriff ohne Kenntnis des Verbrauchers.

**Check 7 — die Behauptung aus §3.1.5 ist untertrieben, nicht falsch.** Erwartet „~730+", gemessen
**1.014** Commits über 34 Branches, die von keinem Remote-Ref erreichbar sind. Größte Posten:
`agents/gmail-monitor` 478 · `agents/pipeline-operations-project` 452 · **`main` 14**. Die
Arbeitsbranche `agents/board-advisor` ist bei **0**. **Die Zahl steigt nicht** (P1-Kriterium nicht
erfüllt) — sie ist ein Altbestand. **Gesichert per Bundle, siehe DECISIONS.**

**Check 8 — grün mit Vorbehalt, und der Vorbehalt ist ein Audit-Befund.** Kein Mismatch, aber
**4 von 5** Verdikten tragen überhaupt keine `expected_ex5_sha256`-Bindung. Ein Hash-Check kann nur
prüfen, was gebunden ist. Fließt als Beobachtung in Audit-Grund 1 ein.

---

## Verifikation der Liste aus §3.1 — „Ausgangspunkt, keine Wahrheit"

| # | Behauptung | Befund |
|---|---|---|
| 1 | Slot-bleeding QM5_410xx belegt aktive Slots | **NICHT bestätigt zum Prüfzeitpunkt.** 2 aktive work_items, beide PIDs lebend, bleeding = 0. Kein QM5_410xx unter den aktiven. |
| 2 | Clone artifacts / stale `qm_ea_id` in 410xx | **offen** — nicht in diesem Zyklus geprüft, gehört in Zyklus 2 |
| 3 | Parser liest derived JSON statt `ea_metrics` | **indirekt gestützt und schwerwiegender als beschrieben**: `ea_metrics` ist zu 69 % `source='missing'`, Q04/Q08/Q14/Q15 haben **null** `drawdown_pct`. Siehe OQ-1/OQ-2. |
| 4 | DD-/Trades-Extraktion fehlerhaft | **Frage entschieden, aber anders**: die genannten DD-Werte können nicht aus `ea_metrics` stammen — dort existieren sie für die betroffenen Phasen nicht. **Stop-Bedingung §9.** |
| 5 | ~730+ ungepushte Commits | **bestätigt und untertrieben** — 1.014, s. o. |
| 6 | Survivor-Pool eingefroren, Q14–Q16 steht still | **teilweise bestätigt**: Q14 letzter Fortschritt vor **7.694 min** (≈ 5,3 Tagen), Q15 vor **6.058 min**, Q16 existiert nicht in `work_items`. Welches Gate friert und was das Auftauen kostet: **noch nicht bestimmt**, gehört in Zyklus 2. |

---

## Zyklus 2 — offen

Fällig nach dem nächsten abgeschlossenen Audit-Abschnitt bzw. spätestens nach zwei weiteren
beantworteten Fragen (§2.2). Offene Punkte für Zyklus 2: §3.1.2 (Clone artifacts, Zahl der
betroffenen Kandidatenpaare), §3.1.6 (welches Gate friert Q14–Q16), C:-Verbrauchsanalyse.

---

# Runde 5 — Ausführungsprotokoll

**Baseline `3472a5d2e1b5`**, genommen 15:33 UTC, off-host auf G:.

## Teil A — Analysespur

| Zeit UTC | Vorgang | Ergebnis |
|---|---|---|
| ~15:10 | Harness gebaut, der die rev4-Anker selbst prüft | **10 von 11 Ankern exakt reproduziert**; MAE-Boden bei 1,00× ergibt 28 % statt 26 % (14 statt 13 Fenster). Dokumentiert statt versteckt. |
| ~15:15 | §1 Sweep über 17 Multiplikatoren, beide Kurven | **Grenz-Multiplikator 0,60×**, erste Divergenz 0,65×. Robust über Schwellen 2–15 pp (0,55×–0,65×). |
| ~15:20 | §2 Kreuztabelle Kalenderhälfte × Buchvollständigkeit | rev4s 67 %/94 % teilte die **Liste**, nicht die Zeit. Bei konstanter Vollständigkeit **79 % gegen 82 %**. |

**Rechenstopp nach §3 eingehalten.** Zwei Messungen, die etwas ändern würden, sind als OQ-5 und OQ-6
notiert und **nicht** ausgeführt.

## Teil B — Regenerierung

| Zeit UTC | Vorgang | Ergebnis |
|---|---|---|
| ~15:25 | E-2 Fehlerbild | `source='missing'` ist **Alter, nicht Extraktion**: 100 % Mai/Juni, 0 % August; 389 von 393 Work-Item-Verzeichnissen fehlen ganz; Pfad-Drift falsifiziert (0 von 393). |
| ~15:30 | Feldweise Diagnose der 19.275 lesbaren Zeilen | Fünf Klassen benannt, jede mit Codezeile. Q08-DD steht als `mc_maxdd_p95_pct` im Aggregat, das der Extraktor nie öffnet. |
| 15:33 | **§5 Baseline eingefroren** | 6 Komponenten, Snapshot `3472a5d2e1b5`, Kopie auf G: |
| ~15:40 | E-2 an Codex dispatcht | Ticket `59c2e32c`, Priorität 90, **ohne DB-Schreibrecht** |
| ~15:42 | §6 Auszählung | **83 von 91 Paaren** brauchen keinen Backtest. Alle 8 echten Rechenfälle liegen in C3, also im ohnehin geplanten Batch. Zusatzkosten für E-1: **null**. |

## P1-Unterbrechung — der Progress-Detektor tötete einen gesunden Basket

| Zeit UTC | Ereignis |
|---|---|
| 14:39:44 | QM5_12712 auf T10 beansprucht, Copy-on-Claim kopiert 216 Historien-Dateien |
| 14:44–15:00 | Baseline-Backtest läuft und schließt ab (report.htm, summary.json) |
| 15:00:13 | Neighborhood-Setfile geschrieben — **letztes vom Detektor gesehenes Artefakt** |
| 15:01:31 | tester.ini des nächsten Laufs geschrieben — **der Lauf hatte begonnen** |
| 15:34:24 | Reaper tötet die Zeile: `NO_FORWARD_PROGRESS`, 34,2 min „stalled", bei einem absoluten Deckel von 368 min |

**Ursache, exakt:** `_q08_tester_ini_matches_contract` verlangt
`tester.ini Symbol == work_item.symbol`. Das Symbol einer Basket-Zeile ist das synthetische Label
`QM5_12712_EURGBP_EURAUD_COINTEGRATION_D1`; MT5 chartet `EURGBP.DWX`. **Sie können nie
übereinstimmen** — und `report.htm` und `summary.json` werden nur *innerhalb* desselben verworfenen
Zweigs beobachtet. Das gesamte Fortschrittssignal fiel auf ein einziges Setfile zusammen.

**Behoben** (`e1a98f77f`): die Zeile darf ihre eigenen deklarierten Konstituenten als Chart-Symbole
gelten lassen, aus denselben drei Payload-Schlüsseln, die auch der Claim-Pfad liest. Expert und
ExpertParameters binden weiterhin EA und Set-Variante. 11 bestehende Tests grün; Positivkontrolle
(beide tester.ini matchen jetzt) und Negativkontrolle (fremdes Symbol wird weiter abgelehnt) belegt.

**Zweiter Befund, dieselbe Klasse wie schon zweimal:** der Reaper begründet sein INFRA_FAIL damit,
dass die stranded-INFRA-Sweep die Zeile requeuen könne. **Diese Sweep hat keinen Aufrufer**, und
`QM_StrategyFarm_Repair_Hourly` ist Disabled. Dritter Fall eines Mechanismus ohne Operator in diesem
Monat. Operator angelegt (`requeue_false_progress_reap.py`), 214 Zeilen geprüft, **1 requeued** —
und die Eignungsprüfung musste vorher verschärft werden, weil ihre erste Fassung zwei Zeilen auf
Artefakte *anderer* Läufe hin akzeptiert hätte.

## Teil C

Vier Vorlagen geschrieben, **nichts gestartet**: `BATCH_SPEC_MERGED.md`, `E3_DECISION_BRIEF.md`,
`LIVETEST_OPTION.md`, `OPEN_QUESTIONS.md` (Endfassung).

Wichtigster Befund für die Bepreisung: **drei der vier Zusatzgrößen aus §7 liegen bereits vor** —
Zeitstempel je Trade minutengenau in den Streams, `.ex5`-Bindung in 51 von 58 lesbaren
Q08-Aggregaten. Nur die Equity-Snapshots fehlen wirklich, und das Primitiv dafür existiert als
geprüftes Modul (`QM_Mod_FtmoJointEquitySampler_20180`) — es muss in den Standard-Bau, was einen
Recompile erzwingt und damit **D-4** auslöst.

---

# Runde 6 — Ausführungsprotokoll

## Betrieb zuerst

| Zeit UTC | Vorgang | Ergebnis |
|---|---|---|
| 18:24 | **D-5** über `governed_work_item_hold.py apply`, fünf Basket-Zeilen | Hold-Code `OWNER_D5_BASKET_LEASE_HOLD`, je Zeile SQLite-Backup + `BEGIN IMMEDIATE` + Rücklesen. **Verifiziert: 0 von 5 beanspruchbar, 2.274 gewöhnliche Zeilen frei.** `status` unangetastet. |
| 18:37 | **§2** Evidenz-Tresor | 299 Dateien, 2,3 MB, off-host G:, Hash `831a0b9c…bffbe0` |
| 20:43 | **§5.1** Bundle statt Push | 41 Commits ungepusht; `QM_Repo_Push` ist ein **Tagesjob**. Inkrementelles Bundle 978 KB, `git bundle verify` OK, lokal = off-host `663FA579…9961` |

## Analyse

| Auftrag | Ergebnis |
|---|---|
| **§1 Obergrenze** | **Nein** — die Equity-Messung kann die Entscheidung nicht drehen. Ceiling 81 %, −8 Flip, +2…5 Population ⇒ **75–78 %** gegen ein Kriterium, das eine **Untergrenze ≥ 0,80** verlangt, während sie bei n = 36 **0,65** beträgt. Ihre Überschlagsrechnung (≈72 %) war richtig; zwei kleine Abweichungen hoben sich auf. |
| **§1 Population** | **Richtung umgekehrt zur bisherigen Annahme**: +2,27 pp je Sleeve bei konstantem Gesamtrisiko. Aber realistisch wächst das Buch nur von 21 auf ~23 — von 91 Pool-Paaren scheitern **24 an <250 Handelstagen** und **33 an den Gates**. |
| **OQ-5** | **Gegen meine Erwartung.** Der überlappungsbeschränkte Boden ist strenger: schlechtester Tag −11,06 %, 501 Tage ≤ −5 %, Übereinstimmungsgrenze **0,60× → 0,50×**. |
| **OQ-6** | Flip ist **nicht sizing-invariant**: −11/−3/−8/−3/−6/−6 pp bei 0,50/0,60/0,85/0,90/1,00/1,10×. |
| **§2 OQ-8** | **Kein Retention-Job, ein Ereignis.** Nichts unter `reports\work_items` ist älter als **2026-07-07 10:30**; jeder andere D:-Teilbaum trägt noch April-Einträge. Storage Sense läuft gegen **C:**. Für den 29.07. ist der Urheber protokolliert (manuelle Purge, 198 GB), **für die Wand am 07.07. nicht**. |
| **§3 Lease** | **Der Lease ist kein Normalbetrieb.** `acquire_lease` liefert `required=False`, solange Containment aus ist. Containment schaltete sich heute 14:39:42 automatisch ein — echter Copy-on-Claim-Fehler einer Zeile, die meine erste Reparaturrunde unter `claimed_by IS NULL` übersprungen hatte und die 28 s später repariert wurde. |
| **§5.2** | 8 deaktivierte QM-Aufgaben, darunter **`TerminalWorkers_AT_STARTUP`** (auf die CLAUDE.md ausdrücklich verweist) und `Repair_Hourly` (seit 01.06.). `requeue_stranded_infra.py` hat belastbar **keinen Aufrufer**. |

## Selbstkorrekturen dieser Runde

1. **OQ-5 ging gegen mich.** Ich hatte die Ausnahme mit der Begründung empfohlen, sie hebe die
   Untergrenze. Sie senkt sie. Steht als erster Abschnitt in rev6, nicht in einer Fußnote.
2. **Meine Runde-5-Darstellung des Lease war falsch.** Ich beschrieb einen fabrikweiten Mutex als
   Systemeigenschaft. Er ist die Eigenschaft eines Ausnahmezustands, der seit vier Stunden anliegt.
3. **Mein eigener Orphan-Scan war zu großzügig** — er zählte Docstring-Nennungen als Aufrufe.
   Strenger Durchlauf stuft 10 von 13 zurück; die Liste geht als **Kandidaten**liste heraus.

## Stand der Fabrik am Rundenende (19:08 UTC)

Ein aktiver Claim (QM5_12831, 210 min), **2.345 wartende Zeilen**, neun Terminals still.
**Ursache ist nicht D-5, sondern der noch laufende Basket, der den Lease hält.** Die Flotte bleibt
blockiert, bis er endet (Deckel 368 min ⇒ spätestens ~20:47 UTC) — **oder bis Containment
freigegeben wird**, was die neun Terminals sofort freigäbe, ohne den Basket anzufassen (D-8).

## 19:26 UTC — der Basket ist durch, und der Lease-Preis steht jetzt isoliert da

**QM5_12831 hat vollständig abgeschlossen**, 15:37 → 19:25 UTC (**228 min**), aus eigener Kraft
innerhalb des 368-Minuten-Deckels.

| | |
|---|---|
| Verdikt | **FAIL_SOFT** — ein echtes Strategie-Verdikt, kein INFRA_FAIL |
| Sub-Gates | **11 ausgewertet: 7 PASS, 4 FAIL** (8.4 seasonal · 8.6 chopping_block · 8.7 pbo · 8.10 regime_crisis) |
| Trades / Equity-Snapshots | 176 / 2.023 |
| Portfolio-Stream | **persistiert**, 176 Zeilen |

**Das ist genau der End-to-End-Nachweis, für den D-5 einen Basket laufen ließ.** Der Pfad
Copy-on-Claim → privatisierte Historie → Baseline → Neighborhood-Sweep → Sub-Gates → Aggregat →
Stream ist damit als Ganzes belegt — derselbe Pfad, der heute zweimal gebrochen war
(Copy-on-Claim-Fail-Closed-Schleife, dann der Progress-Detektor-Reap).

**Die verbleibenden vier Baskets liefern jetzt nur noch Verdikte, keinen Nachweis mehr.** Das war
die Begründung, sie zu parken, und sie hält.

Batch (b): **73 done / 5 gehalten** — 28 PASS · 31 FAIL_SOFT · 5 FAIL_HARD · 8 INFRA_FAIL ·
1 PENDING_RUNNER.

### Der D-8-Preis, jetzt ohne Basket-Konfundierung

**[MESSUNG] 19:30–19:37 UTC:** T1 hält den Lease mit einem Q07-Lauf. Die **anderen neun** Worker
protokollieren in diesen sieben Minuten je 4 bis 7 `custom_history_lease_busy` — sie fragen im
Minutentakt an und werden jedes Mal abgewiesen.

**Damit ist die Zahl sauber:** bei eingeschaltetem Containment läuft die Fabrik mit **einem** Lauf
gleichzeitig statt mit bis zu zehn — bei **2.344 wartenden Zeilen**. Bei einer gemessenen
Median-Wanduhr von 0,35 h je gewöhnlicher Q08-Zeile heißt das grob **~3 statt ~29 Zeilen je
Stunde**; auf den Rückstau gerechnet Größenordnung **Wochen statt Tage**. Die Schätzung mischt
Phasen mit unterschiedlicher Laufzeit und ist als Größenordnung zu lesen, nicht als Fahrplan.

## 20:07 UTC — der realisierte Durchsatzverlust, gemessen statt geschätzt

| Fenster | Dauer | terminale Rows | Rate |
|---|---:|---:|---:|
| vor Containment (Batch b) | 16,8 h | **132** | **7,84 /h** |
| seit Containment (14:39:42) | 5,5 h | **1** | **0,18 /h** |

**Fehlbetrag gegenüber der vorherigen Rate: ~42 Zeilen.**

**Attribution, damit die Zahl nicht überzeichnet:** 3,8 der 5,5 Stunden hat der Basket den einen
Slot gehalten — das war die OWNER-Entscheidung D-5 und hat den End-to-End-Nachweis gekauft. Nur die
restlichen ~1,7 h sind reine Containment-Kosten. **Ab jetzt ist es die ganzen Kosten**, weil kein
Basket mehr im Weg steht und der Deckel von einem gleichzeitigen Lauf allein von `enabled:true`
kommt. Die strukturelle Schätzung lautete ~3 statt ~29 Zeilen/h; die gemessene Vorher-Rate von
7,84/h ist die realistischere Bezugsgröße.

# STR-066 — Reconciliation (2026-07-24/25)

Convergent: baseline = the EDITED post-#1 ruleset (RSI period 2 — both
blind specs picked 2 of "2 or 3"; 3 = variant); threshold 50 strict;
closed-bar MTF reads (each TF at its own shift 1); TP 30 / SL 20; edge
trigger on alignment completion; one position; the pre-edit original (RSI
55, progressive martingale MM) excluded — martingale by hard rule, and
superseded by the edit. Prior build QM5_9702 deltas (old version + M5
execution + invented gates + M15-cross exit) per G0_REVIEW_T6.
Conflict: D1 stack membership — claude excluded ("D1 (optional)" verbatim
→ optional ≠ baseline), codex included. RESOLVED → claude: stack = {H4,
H1, M30, M15, M5, M1}; D1-inclusive = documented variant (tie-break 1:
the optional marker is authorial). Execution TF M15 (ledger), cohort
EURUSD/GBPUSD test-design (spread/ADR criteria = instrument guidance).

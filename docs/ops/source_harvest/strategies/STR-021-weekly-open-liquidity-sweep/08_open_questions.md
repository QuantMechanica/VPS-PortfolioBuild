# STR-021 / QM5_20098 — Open questions (build-run close)

1. **Fill frequency 516-542/yr vs 8-25/yr spec estimate.** Per-side re-arming
   within the week is mechanically spec-conformant but 20x the source's
   apparent cadence; if Q02+ economics fail on churn, a
   one-setup-per-week-per-side variant is the first reconciliation candidate.
2. **Codex build-closure ACK pending** for the 2026-07-24 TP-attach fix
   (rr_target_attained_pre_tp close + per-bar retry pacing) — review task
   queued; fix is committed and smoke-validated (542 trades deterministic).
3. Volume confirmation (strategy_vol_confirm_enabled) default OFF per source
   p.25; tick-volume proxy quality on .DWX untested.
4. Falsified-family caveat (ICT/SMC) stands: the ledger admits this build as
   the one distinct un-built mechanic of the family; Q-gates judge.

# STR-067 — Reconciliation (2026-07-24/25)

The ledger's variant-split suspect is RESOLVED identically by both blind
specs: the author's entry table defines BOTH families per band (upper
cross-out = breakout BUY; upper cross-back = fade SELL; lower mirror),
confirmed by his p.3 support/resistance explanation; the fade-only
reading was a third-party partial implementation (Magix), never a
retraction. Four-case baseline stands. Convergent: BB(20,2,close),
Stoch(14,3,3) main-vs-signal + zone confirms (main<80 buy / >20 sell on
the confirm candle), confirm candle = shift 1 with the crossing candle at
shift 2 (crossing established vs shift 3); entry next bar; TP 50 / SL 50;
MT4-style 15-pip trailing ratchet (per-tick, min-step, never widen —
flagged mechanization); one position; cohort EURUSD/GBPUSD test-design.
Prior build QM5_10015 (fade-only) not transferable. Ledger suspect flag
CLEARED by this two-agent resolution.

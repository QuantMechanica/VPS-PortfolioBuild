# STR-051 / QM5_20119 — Open questions

1. UK-grid custom bars: verify in Q02 evidence that boundary skips
   (weekends/holidays) behave as specced.
2. Heaviest EA of the run — watch tester CPU in Q02 (per-M15 recompute).
3. UK-DST calendar helper is in-EA; if more UK-clock EAs appear, promote
   to QM_DSTAware (framework change, wave-scoped).

# STR-049 — Reconciliation (2026-07-24)

Fully convergent (both blind specs): CCI(20) H1 closed bars; excursion/
prior-peak tracking (peak recorded when CCI falls back inside the zone);
signal = new excursion exceeding the prior peak, strict zone semantics
(equality outside); entry at candle close (next bar); SL = signal-bar
extreme; half at +1R + breakeven move; TP remainder +2R; hard stop ("I am
right or I am out"); one campaign; replay-derived restart. Netted
half-close via the 20101 machinery. Prior-build deltas vs QM5_10000
(codex G0_REVIEW_T6): ATR buffer, max-range veto, CCI-zero exit, 36-bar
time exit added and the +1R half realization omitted — all absent here.
Cohort EURUSD/GBPUSD test-design (both).

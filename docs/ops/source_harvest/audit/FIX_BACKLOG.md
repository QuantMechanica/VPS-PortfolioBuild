# Fix Backlog — Factory Audit 2026-07-24

**Implementation run 2026-07-24 (OWNER: "alles umsetzen") — status:**
FB-01 DONE `550603304` · FB-02 DONE `bd9c3e049` (root cause was the 07-23
evidence-binding regression, not the suspected spawn args) · FB-03 DONE `5562e8463` ·
FB-04/ESC-02 DONE `bf3f03a1a` (manifest pending OWNER countersign) · FB-05 DONE (see
below) · FB-06 DONE `550603304` · FB-07 DONE `6e79b9ea9` · FB-08 DONE `6e79b9ea9` ·
FB-09 DONE (9/9 tasks deleted, XML backups in `evidence/purge__task_xml/`) · FB-10 DONE
(REVIEW queue 0; two rows honestly re-dispositioned, see commit msg) · FB-11 RESOLVED
as phantom `7e11856d4` · FB-12 DONE-with-residue (5 quarantine moves, 12 branches;
7 dirty worktrees + stray codex-13140 dir documented for OWNER review,
`evidence/purge__actions.log`) · FB-13 DONE for 10940 (`6e79b9ea9`; 10476/10692/10715
deliberately kept active — live Q12-pool candidates) · FB-14 DOCUMENTED
(`evidence/fb14__strategy_id_semantics.md`). ESC-01 implemented (DD guard live);
ESC-04 amended; ESC-05 draft awaiting sign-off; ESC-06 10145 re-gated (Q10 requeued),
20048 to Q12. Open OWNER items: `NEEDS_FABIAN.md`.

Prioritized. Effort: S ≤ 1h, M ≤ half day, L ≥ day. Fixes applied during the audit
itself (event_vocabulary commit `8154d302f`, 13213 registry backfill `51778300b`) are
listed in `AUDIT_REPORT.md` §7, not here. Compliance-matrix-driven items (killswitch
recompile debt, max-DD feeder) also appear in `ESCALATIONS.md` where OWNER decisions
are required.

## P1 — live-money / factory-throughput relevant

- **FB-01 (S): Stop event_vocabulary.json self-blocking builds.** Add
  `framework/registry/event_vocabulary.json` to the pump auto-commit set
  (`farmctl.py:9307+` `_auto_commit_build_artifacts`) — it is regenerated by
  `generate_event_vocabulary.py` on every build that introduces new event names and
  then blocks all subsequent generic build spawns via `_repo_dirty_status`
  (`farmctl.py:309-353`). Evidence: `evidence/pipeline__git_dirty_status.txt`. ~3-line
  diff, but it changes pump commit behavior → done as a reviewed change, not in this
  audit run.
- **FB-02 (M): Q03 INFRA_FAIL class.** 35 INFRA_FAIL / 0 PASS in 6h; runner dies
  before `summary.json` (empty evidence_path). Read a recent `work_item_<id>.log` and
  check `_phase_runner_cmd_for_work_item` Q03 spawn args. Codex triage tasks are
  already routed (14 triage_failure + 3 q02_infra_repair) — track to closure.
  Evidence: `evidence/pipeline__q03_infra_fail_sample.txt`.
- **FB-03 (S): live_book_pulse preset parser.** `PRESET_FILE_RE` still expects
  `slot<N>_..._magic<magic>.set`; deployed presets are `NN_SYMBOL_TF_QM5_<id>_<slug>.set`
  → 0/24 matched, 23 phantom `manifest_missing_live_preset` alarms, pulse verdict
  permanently ALARM (alarm-noise normalization on the LIVE monitoring surface).
  Evidence: `evidence/inventory__setfiles_deployed.txt`, `tools/strategy_farm/live_book_pulse.py`.
- **FB-04 (S): Live-book manifest.** Regenerate + OWNER-approve a 24-sleeve manifest
  (current file is a 23-sleeve DRAFT from 07-11: 3 ghosts, 4 unlisted live sleeves).
  Requires OWNER sign-off → ESC-02. Evidence: `evidence/inventory__manifest_reconcile.txt`.

## P2 — evidence/state integrity

- **FB-05 (M): pipeline_state.json per-EA surface.** Either rewire
  `scripts/build_pipeline_state.py` to derive per_ea from `work_items` or stamp the
  file `non_authoritative: true` and strip per_ea; today it claims 191/191 NOT_RUN vs
  23,256 DB PASS rows (5/5 spot checks disagree). Evidence:
  `evidence/state__pipeline_json_vs_db_spotcheck.txt`.
- **FB-06 (S): Pump lock blind spot.** Health check should flag a persisted
  `logs/pump_task.lock` whose PID is dead (06:57 incident: 3 silent no-op cycles;
  `pump_task_lastresult=0x0` masked it). Evidence: salvaged pipeline recon notes.
- **FB-07 (S): work_items timestamp hygiene.** 189 rows `created_at=2027-01-01`
  (grew from 3 since the 07-09 audit; its recommended health check was never built) +
  2 integer-epoch `updated_at` rows. Add the health check; repair rows in one
  reviewed UPDATE. Evidence: `evidence/state__work_items_bad_timestamps.txt`,
  `docs/ops/evidence/FULL_SYSTEM_FTMO_READINESS_AUDIT_2026-07-09.md` L151-157.
- **FB-08 (S): Malformed agent label.** Stop the board-advisor session writing
  `assigned_agent='codex:agents/board-advisor'` (30 rows, invisible to cockpit codex
  attribution `render_cockpit.py:1650`); normalize existing rows to `codex` in one
  reviewed UPDATE; document the direct-DB-write path or route it through the router.
  Evidence: `evidence/state__board_advisor_tasks.txt`.

## P3 — hygiene

- **FB-09 (S): Delete 9 scheduled tasks pointing at deleted one-off launchers**
  (QM_Agy_BalkeWindows, QM_Balke_RangeBreakout_Sonnet, QM_Codex_Multicurrency,
  QM_NDX_Dukas_Download, QM_NDX_Pack, QM_NDX_Swarm_Download, QM_Sonnet_BalkeMinute,
  QM_Sonnet_BalkeSymbols, QM_Sonnet_Secret06). Evidence:
  salvaged pipeline recon `broken_scripts` + `evidence/orphans__scheduled_tasks.txt`.
- **FB-10 (S): Approval-queue closure.** Close the 5 finished-but-unclosed REVIEW
  tasks (3a32ab4b, 31bc37a1, 62b407a5, 19983f60, 589b946f) and the 2 obsolete
  52.7d BLOCKED Q11-spec tasks (4b3026f0, 75a1670d — spec delivered). Evidence:
  `evidence/state__agent_tasks_review_blocked_inprogress.txt`.
- **FB-11 (S): QM_Branding.mqh — RESOLVED as phantom (2026-07-24).** Deep verification
  showed both copies byte-identical (same git blob 68186a2e; the orphan scan's
  "divergent" SHAs were an autocrlf hash artifact) and `sync_brand_tokens.ps1` wrote
  both paths. Cleanup applied instead: orphan `framework/include/QM/QM_Branding.mqh`
  deleted + generator now emits only the compiled root copy. Evidence:
  `evidence/fb11__branding_analysis.md`.
- **FB-12 (M): Relic purge (reviewed delete list).** P*-era report dirs with zero
  readers (`reports/pipeline/QM5_*/P2|P3`, `master_ea_p2_gate`, `master_ea_p25_gate`,
  `pipeline_operator`, `tmp_codex_p8_test`), 7 merged-branch worktrees + 12 merged
  branches + 1 stray dir, stale factory `.ex5` for genuinely retired ids
  (9184, 9221, 9265, 10386, 13204, 13299, 13302 after per-id confirmation), 20 MB
  cards_approved backup. Evidence: `evidence/orphans__*.txt`.
- **FB-13 (S): Registry status lag.** Flip `status` for swapped-out/retired live EAs
  (10476, 10692, 10715, 10940 + magics) and refresh `portfolio_candidates` states
  (~17 already-live rows still Q12_REVIEW_READY; add a terminal LIVE state).
  Evidence: `evidence/inventory__registry_reconcile.txt`.
- **FB-14 (S): ea_id_registry strategy_id quality.** `strategy_id` is duplicated
  across unrelated EAs (e.g. 1490 raschke and 9936 ff-range-breakout share
  `6e967762-…`); dedup checks key on it (`research_dedup_check.py:69`). Audit the
  column once, document intended semantics. Evidence: `framework/registry/ea_id_registry.csv`
  lines 2/805.

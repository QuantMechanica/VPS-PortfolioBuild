# CODEX INDEPENDENT COMPLIANCE AUDIT — 2026-07-24

You are Codex, acting as an INDEPENDENT compliance auditor for the QuantMechanica V5
live book. This brief is your only instruction source. Work alone: do NOT read any
other file under `C:\QM\repo\docs\ops\source_harvest\audit\` (they may contain a
parallel auditor's intermediate results and must not bias you). Do not coordinate
with other agents.

## Mission

For EVERY live-deployed EA on T_Live and every live-candidate EA, produce a
compliance matrix **EA × Check** with verdict `PASS` / `FAIL` / `MISSING` per cell and
a concrete evidence path (CSV / report / log / set-file path, plus line or key) per
cell. "Looks fine" without a path is an invalid cell.

Live truth comes EXCLUSIVELY from the T_Live terminal state:
- Terminal + expert logs under `C:\QM\mt5\T_Live\MT5_Base\Logs\` and
  `C:\QM\mt5\T_Live\MT5_Base\MQL5\Logs\` (logs may be UTF-16 / German locale).
- Deployed `.set` files under `C:\QM\mt5\T_Live\` (locate them yourself).
- Cross-reference `D:\QM\reports\state\live_book_pulse.json` (generated by
  `tools/strategy_farm/live_book_pulse.py`, refreshed 30-min cadence).
Never use screenshots or dashboards as evidence.

Live-candidate EAs = work_items in `D:\QM\strategy_farm\state\farm_state.sqlite`
whose Q10 verdict is PASS but which are not in the live book yet (Q11–Q13 queue).
For candidates, audit the registry/source/factory-set-file side (they have no
T_Live deployment yet) and mark T_Live-only cells `N/A (not deployed)`.

All repo reads use the canonical checkout `C:\QM\repo` (absolute paths), not your
worktree copy.

## Checks (the columns)

1. **Magic registered** — `C:\QM\repo\framework\registry\magic_numbers.csv`;
   formula `ea_id*10000+slot`; run a registry-wide duplicate/collision scan.
2. **Risk mode per ENV** — the DEPLOYED set-file header `risk_mode` must be
   `PERCENT` for live (backtest convention is `FIXED`), AND inputs must satisfy
   `RISK_PERCENT > 0`, `RISK_FIXED = 0`. NOTE: ENV→mode is only a generator
   convention (`framework/scripts/gen_setfile.ps1`), NOT a runtime hard-fail —
   check the actual header and both inputs, do not infer from ENV.
3. **Per-trade risk cap** — deployed per-trade risk ≤ 1% (default ceiling);
   hard ceiling 5%. Value from the deployed set file.
4. **News filter active** — live uses the NATIVE MT5 calendar (CSV path is
   tester-only). Verify the set-file news inputs and INIT-time log evidence in the
   T_Live logs; note fail-closed behaviour if verifiable.
5. **Daily-loss kill** — `KS_DAILY_LOSS` is framework-hardcoded at 3% and has no
   set-file input; verify per EA via the INIT log line containing
   `daily_loss_halt_pct` in the T_Live expert logs.
6. **Max-DD kill** — `KS_PORTFOLIO_DD` defaults to OFF (0.0) and is purely
   external-signal-driven; there is NO EA-internal equity-curve DD guard. Score
   FAIL/MISSING unless you can name a live external monitor process/scheduled task
   that demonstrably feeds the signal file (evidence: task + signal file path +
   recent mtime).
7. **Friday-Close** — default-on via `EA_Skeleton`; verify it is not disabled via
   set file. A separate weekend filter does NOT exist — do not score that as
   MISSING, but document it once.
8. **KillSwitch halt channel in the deployed binary** — prior audit finding P0.3
   found 12/24 dead channels. Determine per EA whether the DEPLOYED `.ex5`
   contains the fixed channel: use deployed-binary build timestamp vs the fix
   commit(s) in `C:\QM\repo` git history, plus runtime log evidence
   (KILL_SWITCH_INIT / KS_* event lines for that magic in T_Live logs). State your
   method per cell.

## Hard constraints

- READ-ONLY everywhere outside your output directory. Absolutely no writes under
  `C:\QM\mt5\T_Live` (no file create/move/delete), no AutoTrading changes, no
  terminal starts, no MT5 interaction beyond reading files.
- No `Factory_OFF`/`Factory_ON`, no scheduled-task changes, no config changes, no
  git commits/checkouts anywhere, no builds, no deployments, no set-file edits.
- Do not modify the farm DB except the single `update-task` call below.
- Backtests are out of scope; do not launch anything.

## Output

Write to `D:\QM\reports\audit\codex_compliance_2026-07-24\` (create the dir):
- `COMPLIANCE_MATRIX_CODEX.md` — the EA × Check matrix, one row per EA
  (ea_id, name, symbol, magic), evidence path in every cell.
- `FINDINGS_CODEX.md` — every FAIL/MISSING as a numbered finding with severity
  (P1/P2/P3), evidence, and a one-line suggested remediation.
- Optional: small evidence extracts (log excerpts) in the same dir.

When done, report back through the router (this is the task that carried this
brief — find its id via
`python C:\QM\repo\tools\strategy_farm\agent_router.py list-tasks --agent codex --state IN_PROGRESS`):

```
python C:\QM\repo\tools\strategy_farm\agent_router.py update-task <task_id> --state REVIEW --artifact-path "D:\QM\reports\audit\codex_compliance_2026-07-24\COMPLIANCE_MATRIX_CODEX.md" --verdict "independent compliance audit complete: <n_pass>/<n_cells> PASS, <n_fail> FAIL, <n_missing> MISSING"
```

Then exit. Do not pick up other work in the same session before this task is in
REVIEW.

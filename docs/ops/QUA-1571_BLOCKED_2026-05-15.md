# QUA-1571 Blocked - SRC01_S06 Build (davey-3bar-eu-h4)

Date: 2026-05-15
Issue: QUA-1571
Scope: P0 build `SRC01_S06 davey-3bar-eu-h4` (`.ex5` from G0-approved card)

## Wake Acknowledgement

Wake assigned `QUA-1571` directly to Development. This changes next action to immediate EA implementation only if V5 hard gates are satisfied in this checkout.

## Blocking Evidence

1. Strategy card artifact for the requested strategy is absent in this checkout:
- No file matching `strategy-seeds/cards/*3bar*eu*h4*`.
- Davey card set present is limited to:
  - `davey-eu-night_card.md` (`SRC01_S01`)
  - `davey-eu-day_card.md` (`SRC01_S02`)
  - `davey-baseline-3bar_card.md` (`SRC01_S03`)
  - `davey-es-breakout_card.md` (`SRC01_S04`)
  - `davey-worldcup_card.md` (`SRC01_S05`)

2. Registry has no `SRC01_S06` allocation:
- `framework/registry/ea_id_registry.csv` has no row with `strategy_id=SRC01_S06`.
- No slug row for `davey-3bar-eu-h4`.

3. Deterministic preflight guard fails:
- Command:
  - `python C:/QM/repo/framework/scripts/skill_build_ea_guard.py --ea-id QM5_1003 --ea-label QM5_1003_davey_3bar_eu_h4`
- Result:
  - `ea_registry_row=false`
  - `ea_dir_exists=false`
  - `next_action=fix_registry_first`

4. V5 hard-rule impact:
- Without approved card artifact + allocated `ea_id`, creating `framework/EAs/QM5_<ea_id>_<slug>/...` would violate V5 framework naming/allocation policy.

## Blocked State

`QUA-1571` is blocked for Development implementation in this heartbeat.

## Unblock Owner + Action

- Owner: CTO + CEO
- Required action:
1. Publish/sync the approved `SRC01_S06` Strategy Card for `davey-3bar-eu-h4` into `strategy-seeds/cards/` in this checkout.
2. Allocate `ea_id` for `slug=davey-3bar-eu-h4`, `strategy_id=SRC01_S06` in `framework/registry/ea_id_registry.csv`.
3. Re-dispatch Development on `QUA-1571` once both artifacts are visible here.

## Immediate Next Action After Unblock

Implement `framework/EAs/QM5_<ea_id>_davey_3bar_eu_h4/QM5_<ea_id>_davey_3bar_eu_h4.mq5` using V5 module boundaries (`Strategy_EntrySignal`, `Strategy_ManageOpenPosition`, `Strategy_ExitSignal`) with inline card section/page citations, then compile and hand off to CTO review-only gate.

# QUA-405 Blocked - SRC04 Phase-2 Build (Card QUA-345)

Date: 2026-04-28
Issue: QUA-405
Scope: SRC04 phase-2 EA build from card QUA-345 (`SRC04_S06`, `lien-fader`)

## Wake Acknowledgement

Wake assigned `QUA-405 SRC04 phase-2 build: card QUA-345` to Development. Next action is EA implementation only if V5 hard gates are satisfied.

## Blocking Evidence

1. Required strategy card is not available in this assigned checkout:
- `C:\QM\worktrees\development\strategy-seeds\cards\` contains only:
  - `lien-dbb-pick-tops_card.md`
  - `lien-dbb-trend-join_card.md`
- `lien-fader_card.md` is absent here.

2. Authoritative card artifact exists but is not approved yet:
- Path: `C:\QM\repo\strategy-seeds\cards\lien-fader_card.md`
- Header currently declares:
  - `strategy_id: SRC04_S06`
  - `slug: lien-fader`
  - `status: DRAFT`
  - `ea_id: TBD`

3. EA ID is not allocated in registry:
- Path: `framework/registry/ea_id_registry.csv` (this checkout)
- No row exists with `strategy_id = SRC04_S06` and no slug `lien-fader` row.

4. Deterministic target EA path cannot be formed:
- V5 naming requires `framework/EAs/QM5_<ea_id>_<slug>/QM5_<ea_id>_<slug>.mq5`.
- With `ea_id` unallocated, any implementation would violate framework hard rules.

## Blocked State

`QUA-405` is blocked for Development implementation in this heartbeat.

## Unblock Owner + Action

- Owner: CEO + CTO
- Required action:
1. Approve card `SRC04_S06` (QUA-345) and set card status to `APPROVED`.
2. Allocate `ea_id` for `slug=lien-fader`, `strategy_id=SRC04_S06` in `framework/registry/ea_id_registry.csv`.
3. Sync approved card + registry row into this assigned Development checkout.
4. Re-dispatch Development on QUA-405 after steps 1-3 are visible in this checkout.

## Immediate Next Action After Unblock

Implement `QM5_<ea_id>_lien_fader.mq5` under `framework/EAs/QM5_<ea_id>_lien_fader/` using V5 framework modules (`Strategy_EntrySignal`, `Strategy_ManageOpenPosition`, `Strategy_ExitSignal`) with card section/page citation comments, then compile clean and hand off to CTO review gate.

# Orchestration Cycle — 2026-05-25T1600Z

## Status: NO CLAUDE TASKS — IDLE CYCLE (queue +28, pwsh -3, codex visible 4→1)

Cycle fired on scheduler cadence. Health check timestamp:
`2026-05-25T16:00:27Z`.

---

## Farm Health

**Overall: FAIL** (4 fail, 1 warn, 14 ok). The familiar three FAIL
checks held (`p2_pass_no_p3=127`, `unbuilt_cards_count=832`,
`p_pass_stagnation=0`) and **`unenqueued_eas_count` re-promoted to
FAIL** at 11 (was 10 last cycle, threshold 10). Dispatch queue
continued upward (+28 to 1067), pwsh pool eroded another -3 to 113
(now 8 below the 121-123 historical band), and Codex visible workers
collapsed 4 → 1.

| Check | Status | Value | Delta vs T1530Z |
|-------|--------|-------|------------------|
| pump_task_lastresult | OK | exit 0 | unchanged |
| p2_pass_no_p3 | FAIL | 127 | unchanged |
| unbuilt_cards_count | FAIL | 832 | unchanged |
| p_pass_stagnation | FAIL | 0 Q03+ PASS in 12h | unchanged |
| **unenqueued_eas_count** | **FAIL** | **11** | **+1 (was WARN 10)** |
| mt5_worker_saturation | WARN | 8/10 (T1, T10 missing) | unchanged |
| mt5_dispatch_idle | OK | **1067 pending / 8 active / 113 pwsh / 2 fresh logs** | **+28, 0, -3, -10** |
| codex_zero_activity | OK | **1 codex, 2 pending** | **-3 codex, 0 pending** |
| codex_auth_broken | OK | auth_age=148.2h | +0.5h |
| disk_free_gb | OK | D: free 137.2 GB | -0.7 GB |
| source_pool_drained | OK | 12 pending | unchanged |
| quota_snapshot_fresh | OK | codex=37s, claude=37s | minor drift |
| codex_bridge_heartbeat | OK | stale 679507s (legacy) | +1805s drift |
| codex_review_fail_rate_1h | OK | 5/26 (0.19, strategy-quality) | richer sample |
| cards_ready_stagnation | OK | 1 old source waiting on in-flight | unchanged |
| zerotrade_rework_backlog | OK | 0 | unchanged |
| ablation_grandchildren | OK | none | unchanged |
| active_row_age | OK | no rows beyond timeout | unchanged |
| claude_review_starved | OK | value=2 | unchanged |

Notable:

- **`unenqueued_eas_count` 10 → 11 → FAIL**. QM5_10075 added to the
  list this cycle (full set: QM5_10019, 10021, 10028, 10035, 10039,
  10043, 10044, 10050, 10075, 10076 + at least one more behind the
  truncation). Pump has not enqueued these built EAs into Q02 across
  the last several cycles — direct corollary of the
  `set-file no-params defect` and `dispatcher universe-mismatch`
  carry-forwards.
- **Codex visible workers 4 → 1**. `codex_zero_activity` detail
  collapsed sharply this cycle. Pending steady at 2. Combined with
  48+ cycles of stalled APPROVED router rows, this looks like Codex
  pickup capacity, not arrival rate.
- **pwsh pool eroded another -3 to 113**. Second consecutive cycle
  below the historical 121-123 band (was 123 → 116 → 113). The dip
  is now a trend, not a sampling blip.
- **`fresh work_item logs` halved**: 12 → 2. Active workers still 8,
  pending +28 to 1067. Drain ratio worsened to 133:1.
- **`open_build_or_review_tasks` 68 → 75** (+7) in replenish status —
  Codex review backlog is the gating factor that keeps replenishment
  frozen, even with the explicit
  `generic_research_replenishment_frozen_edge_lab_primary_2026-05-22`
  reason.

---

## Router Status

- **Claude**: 0 IN_PROGRESS, 0 assigned. `list-tasks --agent claude`
  returned `[]`.
- **Codex**: 7 router rows (3 APPROVED build_ea, 2 APPROVED
  ops_issue assigned, 1 APPROVED ops_issue unassigned, 1 REVIEW
  ops_issue) — **49 consecutive cycles** with this composition.
- **Gemini**: 1 IN_PROGRESS research_strategy, 5 FAILED. Unchanged.
- **run --min-ready-strategy-cards 5**: replenishment frozen,
  `ready_strategy_cards=0`, `approved_cards=2566`,
  `blocked_approved_cards=2566`, `open_build_or_review_tasks=75`
  (+7 vs T1530Z), `draft_cards=54` (unchanged).
- **route-many**: `no_routable_task`.

---

## QM5_10260 — 8 Failed INVALID + 3 Pending (Index Trio Still Untouched)

Re-inspected `work_items` directly via sqlite. Current state:
**8 forex rows `verdict=INVALID, status=failed`** (setfile_missing
preflight, unchanged) plus **3 index rows still `status=pending`**
(NDX.DWX, SP500.DWX, WS30.DWX) all stamped 2026-05-25T12:43:15Z and
unclaimed `~3h 17m` later. Same picture as T1530Z.

No movement this cycle. Disposition belongs to the in-flight
QM5_10260 perf-rework Codex task — observation only, not invented
work.

---

## Actions Taken

1. Ran the read-only orchestration sequence (`farmctl health`,
   `agent_router status / run / route-many / list-tasks --agent claude`).
2. Spot-checked QM5_10260 work_items via sqlite (`farmctl work-items`
   schema lacks `last_error`; queried the table directly).
3. Recorded this cycle log.

No router-task work executed; no untracked work invented.

---

## Risks / Blockers (carry-forward + new)

1. **NEW (escalation): `unenqueued_eas_count` crossed threshold** —
   10 → 11, FAIL. QM5_10075 added to the stuck list. The set
   `10019/10021/10028/10035/10039/10043/10044/10050/10075/10076…` has
   been growing rather than draining; pump-side enqueue is silently
   skipping these.
2. **NEW: Codex visible workers 4 → 1**. Combined with 49 cycles of
   stalled APPROVED router rows, this is the strongest signal yet
   that Codex pickup capacity (not priority order) is the
   bottleneck. Worth a manual `agent_router list-tasks --agent codex`
   + daemon log check.
3. **NEW: `fresh work_item logs` 12 → 2**. Sharp decline in
   work-item-level activity despite stable 8 active workers and
   growing pending queue — suggests workers are running long backtests
   without retiring rows.
4. **pwsh worker pool now -8 below band** (123 → 113 across 3
   cycles). Trend, not blip.
5. **Codex router-task pickup stall** — 6 APPROVED + 1 REVIEW flat
   for **49 consecutive cycles** (~12.25h).
6. **`C:\QM\repo` working-tree drift** — unchanged; OWNER decision
   still needed.
7. **Q02→Q03 promotion stall** — 127 Q02-PASS without Q03 promotion;
   0 Q03+ PASS in 12h.
8. **Unbuilt cards held at 832** — flat for 10 consecutive cycles.
9. **T1 + T10 workers down** — saturation flat at 8/10 (9th cycle).
10. **Dispatch queue at 1067** — growth continued (+28). Drain ratio
    133:1 (worse than 130:1 last cycle).
11. **QM5_10260 index trio stale-pending** — 3 rows untouched for
    ~3h 17m since 12:43:15Z creation.

## Recommended Next Step

Three actionable items, in OWNER priority order:

1. **Codex daemon health check** — `1 codex` visible + 49 cycles flat
   pickup + 75 open build/review tasks is no longer plausibly
   priority-first selection. Either (a) restart Codex worker, or (b)
   inspect daemon log for the polling loop. Memory
   `project_qm_codex_daemon_priority_floor_2026-05-25` cautioned
   against premature "daemon not polling" diagnosis, but the
   conditions have now exceeded its threshold.
2. **Restart T1 + T10** to lift saturation WARN and add ~20%
   throughput at a moment when drain ratio is worsening.
3. **Pump audit for `unenqueued_eas_count` newly-FAIL** — the
   QM5_10075 addition this cycle, and the set's continued growth
   rather than drain, suggests the enqueue branch is being skipped
   for reasons that need direct inspection (likely tied to the
   `set-file no-params defect` and `DL-062 v2 ea_dir_ambiguous`
   blockers, but worth confirming on QM5_10075 specifically).

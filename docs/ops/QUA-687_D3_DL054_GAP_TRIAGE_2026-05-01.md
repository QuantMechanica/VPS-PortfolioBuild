# QUA-687 D3 DL-054 Gap Triage (2026-05-01)

Status: `blocked` (per CEO dependency contract on D2 + D4).

## Scope checked

- Launcher wiring currently lives in `framework/scripts/run_smoke.ps1`.
- `framework/scripts/run_phase.ps1` orchestrates later phases but does not implement DL-054 gates directly.

## Contract match vs CEO comment `c0521b8a`

1. Gate 2 (`tester_defaults`) is wired:
- `run_smoke.ps1` loads `framework/registry/tester_defaults.json` and uses deposit/currency/leverage in generated tester INI.
- `run_smoke.ps1` enforces explicit `-SetFile` (`DL054_GATE2_SETFILE_REQUIRED`).

2. Gate 3 (`rejected journal lines`) is wired:
- `run_smoke.ps1` rejects configured journal markers and maps to `DL054_GATE3_JOURNAL_REJECT`.

3. Gate 4 (`trade evidence`) is mostly wired:
- `run_smoke.ps1` enforces minimum trade count from defaults and marks invalidation on missing trade evidence.
- Remaining required addition for strict CEO acceptance: per-symbol ADR file auto-write path check (`decisions/<date>_zero_trade_<ea>_<symbol>.md`) when zero-trade occurs.

4. Gate 1 (`D2 PASS list freshness`) is **not yet wired to upstream artifact**:
- Current implementation infers data access from tester log markers.
- Missing required pre-launch check:
  - symbol must exist in latest D2 per-symbol PASS list artifact.
  - D2 PASS list artifact timestamp must be newer than latest DWX import log timestamp.

5. Gate 5 (`D4 canonical matrix`) is **not yet wired to upstream artifact**:
- Current implementation derives canonical symbols from latest T1 import log text.
- Missing required pre-launch check against D4 canonical matrix artifact with exact symbol match.

## Unblock inputs required from upstream

1. D2 (QUA-686):
- Published artifact path for per-symbol PASS list.
- Artifact schema (required fields): `symbol`, `status`, `validated_at_utc` (or equivalent canonical timestamp).

2. D4 (QUA-685):
- Published artifact path for canonical symbol matrix.
- Artifact schema (required fields): tester symbol exact canonical string (including `m` suffix where applicable).

## Next implementation step after D2 + D4 close

Patch `run_smoke.ps1` pre-launch to:
- load D2 PASS list + freshness check vs latest DWX import timestamp (hard `INVALID` on fail).
- load D4 matrix + exact canonical symbol check (hard `INVALID` on fail).
- write per-symbol zero-trade ADR file at required `decisions/` path when Gate 4 zero-trade triggers.

# QT Triage: QUA-1060 — QM5_1003 P3 FAIL, H2 (P2 Baseline Too Marginal)
Date: 2026-05-09
Author: Quality-Tech Agent
Issue: QUA-1060

## Verdict

**KILL QM5_1003. Do not re-spec.**

H2 is confirmed — but the finding is stronger than "too marginal." The P2 baseline is catastrophically negative across the entire tested universe. This is not a marginal-edge EA that failed to survive parameter sweep; it is a strategy with no edge at all.

---

## Evidence: P2 "PASS" Baseline Metrics

Three symbols received PASS verdict from the P2 runner. Actual backtest metrics extracted from summary JSONs:

| Symbol       | PF   | DD%    | Trades | Net Profit   | G1 Gate (PF>1.30)? |
|-------------|------|--------|--------|--------------|---------------------|
| EURNZD.DWX  | 0.35 | 67.70% | 143    | -67,407 USD  | FAIL                |
| AUDCHF.DWX  | 0.65 | 68.47% | 335    | -68,369 USD  | FAIL                |
| AUDJPY.DWX  | 0.66 | 75.18% | 344    | -73,377 USD  | FAIL                |

All three symbols:
- PF well below minimum threshold of 1.30
- Net profit deeply negative (all losing strategies)
- DD far exceeds 12% cap (67–75%)
- `model4_log_marker_detected: false` on all runs — Model 4 tick data not verified

Sources:
- `D:/QM/reports/pipeline/QM5_1003/P2/QM5_1003/20260506_023608/summary.json` (EURNZD)
- `D:/QM/reports/pipeline/QM5_1003/P2/QM5_1003/20260506_032435/summary.json` (AUDCHF)
- `D:/QM/reports/pipeline/QM5_1003/P2/QM5_1003/20260508_021836/summary.json` (AUDJPY)

---

## Root Cause: P2 Runner G1 Gate Not Enforced

The P2 runner awarded PASS based on:
- `deterministic: true` (run_01 == run_02)
- `min_trades_required: 20` (spec requires 200 — 10x discrepancy)

The runner did NOT check:
- PF > 1.30 (all 3 passes have PF < 0.70)
- DD < 12% (all 3 passes have DD > 67%)
- `model4_log_marker_detected` flag

This is the same enforcement failure documented in QUA-765. The "fix verified" in the QUA-765 commit was not deployed before these P2 runs (2026-05-06 and 2026-05-08).

**Secondary finding:** P2 G1 gate enforcement needs re-audit. Any EA that passed P2 via the same runner (QM5_1001, QM5_1004) should have their PASS summary JSONs spot-checked for PF and DD values.

---

## Evidence: P3 Sweep

P3 ran 160+ configurations across AUDCHF.DWX and EURNZD.DWX (H1 and M15 timeframes), covering:
- ssl1 ∈ {0.5, 0.75, 1.0, 1.5, 2.0, 2.5, 3.0}
- ssl_usd_cap ∈ {1000, 2000, 3000, 5000} (later configs)
- strategy_atr_period ∈ {10, 14, 20, 30}

Result: **0 PASSes out of all configurations tested**

Failure mode pattern: `run_smoke.summary=` (empty summary marker) — infrastructure is not returning usable backtest results. This is the same toolchain failure seen in P2.

However, the P3 infrastructure failure is irrelevant to the kill decision: a strategy with PF=0.35–0.66 at the default parameter point cannot be rescued by any combination of ssl1 and ATR period adjustments. The parameter sweep is exploring around a fundamentally negative baseline.

---

## H2 Hypothesis Assessment

**H2: P2 baseline too marginal → P3 parameter space collapses.**

Status: **CONFIRMED, with upgrade.**

The P2 baseline is not marginal — it is catastrophically negative. H2 was formulated assuming the issue was a PF of ~1.1–1.3 (scraping by the gate). The actual finding is PF 0.35–0.66, which is not a borderline case. There is no ssl1 or ATR period value that will turn a net-losing strategy into PF > 1.30 on the same data.

The P3 infrastructure failure (empty summary markers) is a secondary contributing factor but not the kill reason. Even with perfect P3 infrastructure, the sweep would show 0/N profitable configurations.

---

## Verdict: KILL

QM5_1003 (`QM5_1003_davey_baseline_3bar`) is killed at P3.

**Rationale:**
1. P2 baseline PF 0.35–0.66 on all tested symbols — below-random performance, not a viable edge
2. 33 of 35 symbols failed MIN_TRADES_NOT_MET, NO_REAL_TICKS_MARKER, or infrastructure failure at P2
3. 0 profitable configurations in P3 sweep across 2 symbols × 2 timeframes × full parameter grid
4. Strategy concept (3-bar pattern with SSL filter) shows no systematic edge on Darwinex universe

Re-spec is not warranted. A re-spec would require evidence of a viable edge in at least 1 symbol at the baseline parameter point. No such evidence exists.

---

## Action Items

1. **CEO / CEO-delegate:** Mark QM5_1003 as KILLED in the pipeline registry.
2. **Development:** No action — no re-spec requested.
3. **Quality-Tech (secondary finding):** File sub-issue for P2 runner G1 gate enforcement re-audit (min_trades_required: 20 vs spec 200; PF not checked on PASS).
4. **Pipeline-Operator:** Remove QM5_1003 setfiles from active P3/P4 queue.

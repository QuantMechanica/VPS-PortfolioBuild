# RAM-479 parked-cohort analysis — class decomposition and scheduling recommendation for Fable/OWNER

Date: 2026-09-16
Agent: Kimi interim OWNER delegation (task `ram479-analysis`)
Preflight: `tools/strategy_farm/agent_worktree_preflight.py --task-id ram479-analysis --paths docs/ops docs/research` → **PASS** (branch `agents/board-advisor`, base `0ef15a46`, no collisions)
Authority: OWNER 2026-09-15 (no VPS upgrade — park/schedule differently); this document **analyzes and recommends only**. No hold was released, no reservation changed, no code touched, no hardware proposed. New file only; no commit (central pass).

Evidence base:

- Calibration result + receipt: `docs/ops/evidence/2026-09-16_deterministic_repairs/2026-09-16_ram44_calibration.md` / `..._ram44_calibration_receipt.json` (ticket 6cdc6811, commit `c29b4196d1`; tester-memory ledger trailing 14 d, 19,210 records).
- Backlog entry: `D:/QM/reports/state/factory_population.json` → `recoverable_backlog[0]` `ram_44gb_reservation_class`, status AUTHORIZED_PENDING_EXECUTION, currently **391 rows / ~42.35 runnable h / economic_priority_rank 1** (post-release recount; the calibration-time estimate was 443 rows / ~46.43 h).
- Live holds: direct census of `work_item_holds` in `D:/QM/strategy_farm/state/farm_state.sqlite` (read-only), each row re-classified with `terminal_worker._ram_reservation_detail_for_candidate` under the corrected table — the same code path as the park/release tools.

## 1. Live census — the 479 parked rows, decomposed

Active hold `RAM_RESERVATION_44GB_NOT_WINNABLE_20260914`: **479 rows, 0 classification failures.**

| # | Class (reservation) | Rows | Composition | Evidentiary status |
|---|---|---:|---|---|
| A | `single_index_tick @44 GB` | **52** | 48 SP500.DWX (Q02×9, Q03×3, Q04×36), 2 NDX.DWX (Q04, Q08), 2 GDAXI.DWX (Q04, Q08); 51 distinct EAs | **Measured necessity** — class peaks 35.4–46.8 GB (§2) |
| B | `heavy_or_unknown_multisymbol @44 GB` | **427** | 145 distinct EAs, 65 symbols; top row-counts QM5_1132×33, QM5_1081×14, QM5_1221×9, QM5_1218×9; heaviest symbol mixes NDX×55, GBPUSD×42, XAUUSD×42, EURUSD×37, WS30×31 | **Fail-safe, n=0 measured** — zero ledger records in 14 d; lowering forbidden |
| C | `multi_leg_fx_basket` | **0 parked** | flat-32 GB class, not part of the 44 GB park | Measured max **33.5 GB > flat 32** → under-reserved (§5) |

Adjacent holds in the same backlog class (for completeness): 5 `RAM_WINDOW_44GB` rows (all re-classify to `single_index_tick @44` — NDX Q12×3, SP500 Q07×1, GDAXI Q02×1) and 3 `RAM_OUTLIER_4X_RESERVATION` rows (all re-classify to `ordinary @24` phase-floor Q05 — EURJPY×2, GBPJPY×1).

The 52 `two_leg_metal_pair @24 GB` rows identified by the same census were already released on 2026-09-16 (52/52 journal-ok, `ram44_release_journal.jsonl`); they are not part of this analysis.

## 2. Measured RAM distribution per class (calibration ledger, trailing 14 d)

| class | n | p50 | p95 | max |
|---|---:|---:|---:|---:|
| single_index_tick | 489 | 2.07 GB | 10.6 GB | **39.5 GB** |
| multi_leg_fx_basket | 26 | 21.2 GB | 33.4 GB | **33.5 GB** |
| heavy_or_unknown_multisymbol | 0 | — | — | — |

Named full-window D1 monsters (the 39.5 GB ledger evidence):

- NDX.DWX QM5_10280 Q05 D1 — finished **39.5 GB WS / 49.3 GB private** (reservation at run: 12)
- NDX.DWX QM5_1077 Q05 D1 — finished **39.2 GB WS**; sibling run ram_emergency_reap at 25.7 GB (reservation at run: 12)
- GDAXI.DWX QM5_1642 Q02 D1 smoke — finished **35.4 GB** (reservation at run: 24)
- SP500 pre-window 2026-08-15 Q02 — **45.7 GB private / 46.8 GB WS** (the original 44 GB basis)

Host arithmetic: 63.1 GB host, `RAM_MIN_FREE_GB=14.0` post-reservation floor → a 44 GB row normally needs 58 GB free vs **54.1 GB max free observed on the idle ten-worker fleet (2026-09-14 04:xxZ)** — structurally unwinnable beside other testers; winnable only alone on the fleet (44+4=48 ≤ 54.1). Guards unchanged: `max(flat, measured, floor)`, 14 GB floor, drain-window floor, cold-restart reserve, RAM emergency reaper; reservation values are admission-lane labels, not kill limits.

## 3. Genuinely ≥44 GB vs over-classified — per class

**Class A (52 index rows) — measured necessity, with one honest caveat.** The class label is justified by measurement on every base it covers: SP500 measured **above** 44 (45.7/46.8) → the 48 SP500 rows are genuinely ≥44 GB on the evidence; NDX measured 39.2–39.5 GB WS (49.3 GB private) and GDAXI 35.4 GB → the 4 NDX/GDAXI rows carry WS peaks ~4.5–8.6 GB **below** the 44 label; the label is the class flat applied under "never below a measured peak", not a per-row measurement. Caveat: the parked phase mix is mostly Q04 (36 of 48 SP500), while the monster evidence is full-window D1 (Q02/Q05) — a short-phase row inherits the class label; it is not a lowering candidate (reservation lowering is out of scope/forbidden here), but its *scheduling* need is met by the exclusive lane either way because the label is what the admission gate checks.

**Class B (427 heavy rows) — over-classified by construction, and correctly so.** n=0 measurements: not one row can be shown to need 44 GB, nor can any be shown not to. This is the documented fail-safe ("defaulting unknowns to heavy"); it **must stay 44 GB** — lowering without measurements is forbidden and no reservation change is proposed.

**Class C (fx basket, 0 parked) — the opposite problem.** The live flat 32 GB is **below** the measured class max (33.5 GB, p95 33.4, n=26). The measured-path guard raises at admission, so nothing is under-admitted today, but the table itself is stale-low. 8 pending rows carry this class. Fix is a raise + measurement wave (§5, menu item 3), not a park release.

## 4. Safe scheduling strategies on the EXISTING VPS (existing mechanisms only)

All three mechanisms are claim-selection-only reorderings already deployed in `terminal_worker`; none changes reservations, gates, the 14 GB floor, or the reaper.

**S1 — Serial heavy lane (exclusive drain lane), the OWNER-approved configuration for ≥40 GB rows.**
Mechanism: `QM_DRAIN_EXCLUSIVE` lane (commit `e246cc0dab`; kill switch `QM_DRAIN_EXCLUSIVE=0`; constants `DRAIN_EXCLUSIVE_MIN_RESERVATION_GB=40`, `DRAIN_ARMED_ROW_FLOOR_GB=4`, `DRAIN_HOST_BASELINE_EMPTY_FLEET_GB=9`, pre-drain ≤240 min, cooldown 180 min, **≤4 exclusive claims per UTC day fleet-wide**). A 44 GB row runs alone on the fleet: need = 44+4 = 48 GB against 63.1−9 = 54.1 GB empty-fleet free → winnable with ~6 GB slack. While long runs (Q07+, news) are active a pre-drain refuses only NEW long-run claims (short rows keep flowing); the normal RAM gate enforces the exclusivity for the run's duration. Use in off-peak windows (the 54.1 GB plateau was measured 04:xxZ on the idle fleet) so the day fleet's census/ordinary throughput is untouched; the duty cycle bounds the long-run lanes' lost time.
Throughput: duty-capped at 4 rows/UTC day → a 13-day floor for 52 rows; full-window D1 runs are multi-hour, so realistically 1–2 exclusive runs per nightly off-peak window → **plan on ~4–7 weeks of nightly exclusive runs for the 52-row index cohort** (~5–6 h total tester time at the backlog's per-row rate, spread by the duty cycle). The 427-heavy cohort at this rate is ~4 months — that is the parking argument, not the scheduling argument.

**S2 — Bounded drain window (`QM_DRAIN_WINDOW`, 2026-09-03; kill switch `=0`).**
Mechanism: a ≥24 GB row headroom-skipped >20 min arms a drain; the fleet stops taking NEW short rows for ≤30 min; the armed row is admitted at reservation+4 GB floor (+3 GB margin, 14 GB host baseline, plateau memory 360 min). Arithmetic for 44 GB: 44+4+3 = 51 > 49.1 ceiling → **never arms** for class A/B (that is exactly why S1 exists). It does arm for a 36 GB fx-basket row: 36+4+3 = 43 ≤ 49.1 → the fx-basket follow-up needs no exclusive lane.

**S3 — Cell/census lane (evidence generation, 4 GB `OPT_CENSUS` cells).**
Mechanism: the census-cell machinery (`opt_census_cell @4 GB`, observed 13 cells/10 min outside a RAM pause, 2026-09-02 admission doc) is the fleet's high-throughput lane. For class B it converts n=0 into measurements without touching any reservation: run symbol-independent heavy legs as per-symbol cells (each cell classifies on its own measured peak), accumulate ledger evidence, then re-classify. Caveat: faithful only for EAs whose legs are symbol-independent; cross-symbol logic must still run as one basket (S1). Throughput: a 60-cell wave (e.g. top-20 heavy EAs × 3 symbols) ≈ well under an hour of interleaved fleet time, repeated weekly — after 2–3 waves the 427-cohort stops being n=0 and re-classification becomes an evidence question instead of a fail-safe guess.

## 5. Recommendation menu for Fable

Expected tester-time yield is quoted from the backlog rate (~42.35 h over the remaining class; ~41 h sat in the two 44 GB classes per the calibration receipt).

1. **Keep the 427 heavy_or_unknown rows parked.** No measurement exists to schedule them safely at any lower reservation, and the exclusive-lane calendar (~4 months) crowds out higher-value nightly use. Revisit only through S3 measurement waves; each wave is cheap (≈1 h fleet time) and compounds.
2. **Run the 52 measured-necessity index rows only under S1 preconditions: ≥2 terminals idle AND fleet free-RAM headroom >50 GB** (i.e. the empty-fleet plateau region), ≤4 exclusive claims/UTC day, off-peak. Priority order: 48 SP500 Q04 block first (direct 45.7/46.8 GB measurement, largest contiguous yield), then NDX/GDAXI. Expected: ~5–6 h tester time over ~4–7 weeks of nights; unlocks ~half the class's backlog hours without touching a single guard.
3. **Raise `multi_leg_fx_basket` flat 32→36 GB** (never below the measured 33.5 GB max) **with a follow-on measurement wave** (target n≥60 across phases; n=26 today), scheduled via the ordinary drain window (S2 arithmetic passes at 36). Separately authorize; independent of the parked 479.
4. **Explicitly NOT recommended / not permitted:** lowering any 44 GB reservation (class A is measured necessity; class B is fail-safe with n=0 — lowering forbidden); relaxing `RAM_MIN_FREE_GB=14.0`, the drain floors, or the RAM emergency reaper; any VPS/hardware change (OWNER 2026-09-15 is binding: park/schedule differently).

**What Fable must decide:** (a) authorize governed release of the 52 index holds under menu item 2 (hold release via `farmctl.release_work_item_hold` with the S1 preconditions as release note), (b) approve the S3 measurement-wave cadence for class B, (c) approve the fx-basket 32→36 raise as a separate ticket.
**What happens automatically after approval:** released rows re-enter the pending queue and the exclusive lane claims them when the preconditions and duty cycle allow; S3 cells flow through the normal census lanes; every release is CAS-guarded with a transition-ledger record and journal line, exactly as the 52-row 2026-09-16 release was executed.

---

# Part 2 — Q09 sealed-plan recommendation (QM5_10148 / QM5_11476)

Source: `docs/ops/evidence/2026-09-16_deterministic_repairs/` — Q09 STOP + OWNER package, **committed as `30c00b0b95`** ("ops(evidence): Q09 sealed-plan derivation item STOP - OWNER package for both rows"), machine-readable `2026-09-16_q09_sealed_plan_owner_package.json`, binder census `2026-09-16_q09_binder_census.json`. Both rows sit under active `Q09_AWAITING_SEALED_PLAN` holds on their pending `Q10_NEWS` items; both fail-closed refusals were verified correct (evidence-semantic, no derivation defect). Expected unlock if resolved: ~4.08 runnable h. This section recommends; it authorizes nothing.

## QM5_10148 (EURNZD.DWX, tii-signal) — **RECOMMENDED: REMEASURE**

Facts: the Q07 seed-stability PASS (`bad1b2f7`) evidence file was destroyed in the 2026-08-31 evidence-loss incident and a 2026-09-15 backup scan proved **restore impossible**; the Q09 PASS source `6ee9adbe` contradicts itself — aggregate says PASS (pf 1.57) while its own immutable run summary says FAIL `MIN_TRADES_NOT_MET` (29<30), `recency_gate=STALE_WINDOW`, `deployment_blocker=true`. Crucially, the card's economics are intact: it holds a genuine **Q08 PASS on the exact symbol** (`1da1645c`) and is blocked only at the final news gate. Justification: remeasure costs one Q07 seed-stability slot plus one Q09 news-runner run plus readjudication-lane resolution of the `6ee9adbe` contradiction — bounded spend of ~2 terminal slots — while retire writes off, at its last gate, one of the few cards that already cleared the economic gate; with the 2026-09-16 D1 cohort having just burned 13 executions for zero yield (11× FAIL_HARD; every trend row dead on §8.2 DSR), completing an economically proven card is exactly where scarce measurement slots earn their keep. **Fable must decide:** authorize the fresh Q07 + Q09 re-measurement (OWNER decision id) or retire/disposition the row. **After remeasure:** a fresh Q07 enqueues as a new work item (identity-bound, superseding — no verdict-lineage edit), the contradiction routes to the readjudication lane automatically, and on fresh Q07 PASS + Q09 PASS the autoseal/bind lane derives and binds the sealed plan and the hold self-releases; Q10_NEWS becomes claimable. **After retire:** governed disposition closes Q10_NEWS with a retirement note; the card and its verdicts remain in the archive, immutable.

## QM5_11476 (USDJPY.DWX, lien-k-double-bb-trend-h1) — **RECOMMENDED: RETIRE**

Facts: no Q07 work item exists for this EA at all — there is nothing to authenticate and nothing lost to restore; the only Q09 rows are done FAIL (`fae2b8eb`, `2a7e17b7`), the Q08_INPUT dependency is FAIL_SOFT with an empty lineage payload, and the EA never produced a Q08 PASS on the target symbol. Justification: "remeasure" here does not mean re-running a lost measurement — it means building an economic verdict case from nothing (fresh Q08-class economic work, then Q09) on a trend-family card, and the 2026-09-16 failure analysis shows trend systems are the book's failure concentration (every executed trend row FAIL_HARD on §8.2; D1/H1 index trend-following 2017–2025 has no measurable risk-adjusted edge, and the surviving economics are mean-reversion); spending terminal slots plus news-runner time on the lowest base-rate family, for an EA that has already failed Q08 twice and Q09 twice, buys an option with poor expected value, while retire costs essentially nothing — the row is disposed, evidence and verdicts stay immutable, and the USDJPY sleeve loses no capability it currently has. **Fable must decide:** retire/disposition the row (OWNER decision id) or order the full economic rebuild (Q08-class work first — a verdict programme, not an infra repair). **After retire:** governed disposition releases the hold with a retirement note and closes Q10_NEWS; the EA is archived, not deleted; the sibling row `d712832c` (QM5_11422, source-vintage mismatch) stays in the same lane pending its own re-derivation basis and is unaffected by either choice here.

*End of document. Analysis only — all holds, reservations, tables and code unchanged.*

# QUA-1109 — CTO P0 Build Schedule (Dual-APPROVED Cohort)

Date: 2026-05-09  
Source manifest: `processes/strategy_cards/g1_approved_2026-05-09.md` (QB G1)  
Scope: Dual-APPROVED cards only (CEO G0 + QB G1), with no duplicate build dispatch.

## Reconciliation: 13 vs 11

- The manifest summary reports 13 Dual-APPROVED total.
- The actionable post-SRC02/SRC03/SRC04 subset listed in the table is 11 cards.
- The 2-card delta is legacy SRC01 approvals already in pipeline history and not part of this new scheduling batch.

## Card State Snapshot (2026-05-09)

Already in pipeline (do not re-dispatch P0 build):
- `SRC02_S01` `chan-pairs-stat-arb` (`ea_id=1017`) — pipeline state present.
- `SRC04_S03` `lien-fade-double-zeros` (`ea_id=1009`) — pipeline state present.

Already in-flight build (explicit de-dup):
- `SRC04_S08` `lien-channels` (`ea_id=1014`) — in-flight under `QUA-1090`; do not open duplicate build issue.

Ready for new P0 build dispatch now:
- `SRC04_S04` `lien-waiting-deal` (`ea_id=1010`)
- `SRC04_S05` `lien-inside-day-breakout` (`ea_id=1011`)
- `SRC04_S06` `lien-fader` (`ea_id=1012`)
- `SRC04_S07` `lien-20day-breakout` (`ea_id=1013`)
- `SRC04_S09` `lien-perfect-order` (`ea_id=1015`)
- `SRC04_S11` `lien-carry-trade` (`ea_id=1016`)

Dual-APPROVED but blocked on missing EA ID allocation:
- `SRC03_S16` `williams-pro-go` — assign `ea_id=1018` (next available policy from prior queue notes).
- `SRC03_S17` `williams-pinch-paunch` — assign `ea_id=1019`.

## P0 Dispatch Order (capacity-safe)

Lane A:
1. `SRC04_S04` (`1010`)
2. `SRC04_S06` (`1012`)
3. `SRC04_S09` (`1015`)
4. `SRC03_S16` (`1018`) after registry allocation

Lane B:
1. `SRC04_S05` (`1011`)
2. `SRC04_S07` (`1013`)
3. `SRC04_S11` (`1016`)
4. `SRC03_S17` (`1019`) after registry allocation

## Hard-Rule Constraints for Development Tickets

- Enforce `Model 4 Every Real Tick` on baseline runs.
- Include both `RISK_FIXED` and `RISK_PERCENT` inputs in all EAs.
- Use `.DWX` symbols in research/backtest artifacts.
- Apply magic schema `ea_id * 10000 + symbol_slot`; collision is hard-abort.
- Keep strict 4-module boundary (`No-Trade`, `Entry`, `Management`, `Close`).
- Friday close default must be enabled unless card-documented waiver exists.
- `SRC04_S11` carry-trade note: risk-aversion bond-yield threshold calibration is a P3 axis; not a P0 scaffold blocker.

## Next Concrete Actions

1. Register `SRC03_S16/S17` EA IDs (`1018/1019`) in `framework/registry/ea_id_registry.csv`.
2. Open/assign Development build tickets for the 8-card dispatch queue above, excluding `SRC04_S08`.
3. Gate each build through CTO DL-036 EA-vs-Card review before Pipeline-Operator smoke.

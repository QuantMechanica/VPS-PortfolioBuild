# QM5_10718 Build + QM5_10717 Review - 2026-05-22

Task: `606953dd-c473-4b88-bfac-df5b65f3cfa4`

## Scope

- Built `QM5_10718_edgelab-regime-filtered-carry` as a single host-chart FX8 basket EA.
- Reviewed `QM5_10717_edgelab-xsec-fx-momentum` as the reference basket EA.
- Verified basket queue wiring creates one logical `FX8_BASKET_D1` Q02 work item.

## QM5_10718 Build Evidence

Files present under `framework/EAs/QM5_10718_edgelab-regime-filtered-carry/`:

- `QM5_10718_edgelab-regime-filtered-carry.mq5`
- `QM5_10718_edgelab-regime-filtered-carry.ex5`
- `basket_manifest.json`
- `docs/strategy_card.md`
- `sets/QM5_10718_edgelab-regime-filtered-carry_FX8_BASKET_D1_D1_backtest.set`

Implementation summary:

- Uses `<QM/QM_BasketOrder.mqh>` and `QM_BasketOpenPosition`.
- Host chart is `EURUSD.DWX` / `D1`; logical test symbol is `FX8_BASKET_D1`.
- Ranks the 8 major currencies by broker swap proxy using `SYMBOL_SWAP_LONG` and `SYMBOL_SWAP_SHORT`.
- Trades top-2 carry currencies against bottom-2 currencies when the regime filter is green.
- Regime filter is basket 20-day realized volatility versus the 252-day median; red regime closes and stays flat.
- Per-leg hard stop is `2.0 * ATR(20)`.
- News mode is `QM_NEWS_FTMO_PAUSE`; Friday close is enabled.
- No ML, no grid, no martingale, no HFT.

Build verification:

```text
powershell -ExecutionPolicy Bypass -File framework/scripts/build_check.ps1 -EALabel QM5_10718_edgelab-regime-filtered-carry -RepoRoot C:/QM/worktrees/codex-orchestration-1
build_check.result=PASS
compile_one.errors=0
compile_one.warnings=0
build_check.report=D:\QM\reports\framework\21\build_check_20260522_143236.json
```

Note: the task text named `C:/QM/repo` for build_check, but the active isolated worktree for this codex session is `C:/QM/worktrees/codex-orchestration-1`. Running against `C:/QM/repo` failed label discovery because that checkout had not received `QM5_10718`.

## QM5_10717 Review

Verdict: `REFERENCE_PATTERN_ACCEPTED_WITH_QUEUE_PREFLIGHT_CAVEAT`

Reviewed file: `framework/EAs/QM5_10717_edgelab-xsec-fx-momentum/QM5_10717_edgelab-xsec-fx-momentum.mq5`

Findings:

- Cross-sectional ranking is mechanical: each currency score is the average signed D1 return across available basket pairs over `strategy_lookback_days`.
- Basket-order helper usage is consistent with the design: every leg calls `QM_BasketOpenPosition`, and `symbol_slot` maps to the selected pair index.
- Magic model is deterministic: `qm_ea_id * 10000 + symbol_slot`; registry contains active rows for slots 0-27.
- FTMO design box is present: mandatory news pause, Friday close, D1 weekly rebalance, ATR stops, no grid/martingale/ML.
- Basket manifest and logical-symbol setfile exist in the isolated worktree.

Caveat:

- Existing Q02 work item `f587cbe6-478c-42cb-b4ff-94ba86130d77` is the required single `FX8_BASKET_D1` item for `QM5_10717`, but it previously failed `INVALID` with `ea_dir_missing` because the worker looked in `C:/QM/repo` while the basket work was in the isolated codex worktree. This is an environment propagation issue, not an EA source defect.

## Queue Evidence

```text
python tools/strategy_farm/farmctl.py work-items --ea QM5_10717
count=1
item=f587cbe6-478c-42cb-b4ff-94ba86130d77
phase=Q02
symbol=FX8_BASKET_D1
parent_task_id=31c9c81e-7711-45b5-958c-c15f6f49c822
status=failed
verdict=INVALID
preflight_reason=ea_dir_missing
```

The required one-item basket shape is present. The failed item should be requeued only after `C:/QM/repo` or the worker repo root includes the same basket EA files as this worktree.

## Focused Verification

```text
python -m pytest tools/strategy_farm/tests/test_basket_work_items.py
1 passed
```

## Verdict

`QM5_10718_BUILD_PASS_QM5_10717_REVIEW_DONE_QUEUE_SINGLE_ITEM_PRESENT`

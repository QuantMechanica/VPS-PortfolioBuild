# QUA-837 Closeout — Strategy Wiki Tooling

Date: 2026-05-08
Issue: QUA-837
DevOps commit: `f5b6137b`

## Scope delivered

- Added deterministic wiki linter CLI: `framework/scripts/lint_strategy_wiki.py`
- Extended dedup checker to read both sources (cards + wiki strategies): `framework/scripts/research_dedup_check.py`
- Added targeted tests:
  - `framework/scripts/tests/test_lint_strategy_wiki.py`
  - `framework/scripts/tests/test_research_dedup_check.py`

## Acceptance evidence

### 1) Empty wiki tree lint runtime + exit code

Command:

```powershell
python - <<'PY'
import tempfile, time
from framework.scripts.lint_strategy_wiki import main
with tempfile.TemporaryDirectory() as d:
    t0=time.time(); rc=main(['--vault', d]); dt=time.time()-t0
    print(f'rc={rc} duration_s={dt:.4f}')
PY
```

Observed output:

```text
OK: no strategy wiki lint violations
rc=0 duration_s=0.0000
```

### 2) Cross-source duplicate detection (wiki source)

Observed output excerpt:

```text
### strategy-seeds/cards/ — 0 cards scanned
### 09 Strategy Wiki/strategies/ — 1 nodes scanned
  EXACT DUPLICATE: [wiki] ...\strategies\dup.md (alpha / SRC01_S01)
VERDICT: DUPLICATE — link as _v<n> enhancement per DL-029/033, NOT new ea_id
rc=2
```

### 3) Tests

Command:

```powershell
python -m unittest framework/scripts/tests/test_lint_strategy_wiki.py framework/scripts/tests/test_research_dedup_check.py
```

Observed summary:

```text
Ran 5 tests in 0.058s
OK
```

## Remaining acceptance note

- `06 Infrastructure/Tools and Scripts.md` registration is delegated to Doc-KM per issue scope.

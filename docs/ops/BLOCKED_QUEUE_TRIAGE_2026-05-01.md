# Blocked-queue triage 2026-05-01 (Tuesday-prep hurdle clearing)

**Author:** Board Advisor at OWNER directive 2026-05-01.
**Scope:** all 78 issues with `status=blocked` in Paperclip + adjacent operational hurdles. Goal per OWNER: clear all hurdles; ensure company starts working fast Tuesday and doesn't stop.

## TL;DR — actions taken

| Class | Count | Action |
|---|---:|---|
| Token-alarm point-in-time events | 2 | **CANCELLED** (QUA-688, QUA-689) — superseded by CoS continuous watch (DL-056) + DevOps QUA-527 (DL-055). Comment + PATCH status=cancelled. |
| `magic_numbers.csv` gap | 1 | **POPULATED** — was 1 row (1001/EURUSD only); now 467 rows covering all 13 active ea_ids × canonical 36 symbols. Pipeline-Op can now dispatch QM5_1003 / 1004 / 1009 / 1017 Tuesday. |
| `dispatch_state.json` zombies | 15 | **PURGED** — 15 zombie in_flight rows from QM5_1002 broken loop (>12h, no completion) pruned. Backup at `D:/QM/Reports/pipeline/dispatch_state.json.before_zombie_purge_2026-05-01`. |
| DL-057 auto-wake pulse heuristic | 2 bugs | **TIGHTENED** — (a) G0 review check now QB-assignee-scoped (was over-broad, caught DL-036 EA reviews); (b) `blocked` status now counted as queue-active (was not). Pulse now correctly reports PAUSED while 5 P0/P1/P2 issues blocked behind Codex outage. |
| Codex-blocked work (29 issues) | 29 | **DEFERRED** — auto-unblock Tuesday post-Codex-restore. CTO/Development/DevOps/Pipeline-Op all heartbeat-disabled until Tue 07:30 W. Europe. Tuesday runbook step 3 re-enables. |
| Strategy-card-queue (29 issues) | 29 | **DEFERRED** — correctly blocked behind EA queue per DL-057 R-057-1. Auto-wake pulse fires when queue clears. |
| Phase 3 umbrella (active) | 2 | **STAY OPEN** — QUA-660 + QUA-684 are Tuesday-prep umbrellas; CEO closes when work completes. |

## Blocked-queue census after triage

| Status | Before | After |
|---|---:|---:|
| `blocked` total | 78 | 76 |
| `cancelled` | (–) | +2 |

The 76 remaining blocked issues are NOT bugs — they're correctly in `blocked` waiting for either (a) Codex-restore Tuesday, (b) upstream EA queue progression, or (c) explicit CEO/QB ratification on cards. None require Board Advisor action this weekend.

## Per-class detail

### Class 1 — token alarm point-in-time events (CANCELLED)

`QUA-688` (2026-04-30) and `QUA-689` (2026-05-01) were auto-generated by `Test-TokenCostBudgetHealth.ps1` when forecast usage hit 95%. As point-in-time alarms they had done their job (signal fired); the alarm-issue-per-event mechanism is superseded by:

- **DL-056 / CoS** continuous token-burn watch on `QUA-699` rolling tracker
- **DL-055 / DevOps QUA-527** daily token-cost snapshot infra (Tuesday post-Codex-restore)

Cancellation comment + `PATCH status=cancelled` applied to both. Future alarms surface as comments on QUA-699, not new high-priority blocked issues.

### Class 2 — magic_numbers.csv gap (POPULATED)

**Discovery:** `framework/registry/magic_numbers.csv` had only 1 row (ea_id=1001 EURUSD with magic 10010000). All 12 other active ea_ids in `ea_id_registry.csv` (1002, 1003, 1004, 1009-1017) had **zero rows** — Pipeline-Op queries this CSV via `Get-AllowedSymbolsFromRegistry` (in `run_phase.ps1`); without rows, no allowed_symbols → cannot dispatch.

**Action:** Generated 466 new rows covering all 12 missing ea_ids × 36 canonical symbols (sorted alphabetically as `symbol_slot 0..35`). Magic-number scheme per CLAUDE.md: `ea_id × 10000 + symbol_slot`. All magics unique (467/467 unique).

**Note:** ea_id 1001 has 35 (not 36) rows due to slot-0 collision with the existing manual EURUSD entry (slot 0 / magic 10010000). AUDCAD is missing for 1001. Non-critical: 1001 (breakout-atr) is not in Tuesday queue. CTO can fill the gap Tuesday by either re-slotting EURUSD or assigning AUDCAD a different slot.

**Tuesday acceptance:** Pipeline-Op queries `magic_numbers.csv` for ea_id=1003 → returns 36 rows → all canonical symbols allowed → dispatches matrix successfully.

### Class 3 — dispatch_state.json zombies (PURGED)

`D:/QM/Reports/pipeline/dispatch_state.json` had 70 dedup entries — 55 complete + 15 zombie. The 15 zombies were all `QM5_1002 v1 P2 cfg001` rows from 2026-04-30 (the earlier broken loop) with `ts=1777587715` (>12h ago) and no `status=complete` marker. They were preventing the DL-057 auto-wake pulse from seeing the queue as empty (matrix-mid-run check).

**Action:** Backup → `dispatch_state.json.before_zombie_purge_2026-05-01`. Pruned 15 zombies. Final state: 55 complete entries (preserved for forensic record). Pulse script now reports `0 rows in_flight` cleanly.

### Class 4 — DL-057 auto-wake pulse heuristic bugs (TIGHTENED)

After dispatch_state purge, pulse said `DRY_RUN_WAKE` — would have fired Research wake despite Codex outage. Two heuristic bugs found:

**Bug A — G0 review check too broad.** The original regex caught any `REVIEW`/`G0` title regardless of assignee — including DL-036 EA Review Gate issues (CTO-assigned, different review class). Tightened to require `assigneeAgentId == QB`.

**Bug B — `blocked` status not counted as queue-active.** Per DL-057 R-057-1, "≥1 EA in P0/P1/P2" should include blocked issues (they're pipeline work in flight, not done). Added `blocked` to the active-status set.

**After fix:** pulse correctly reports `queue=5 g0=0 matrix=False` → PAUSED. Research will wake only when those 5 P0/P1/P2 blocked issues clear (Tuesday post-Codex + after baselines run).

### Class 5 — Codex-blocked work (DEFERRED to Tuesday)

29 issues currently blocked because their assignee is one of the 4 codex_local agents (CTO `241ccf3c`, DevOps `86015301`, Development `ebefc3a6`, Pipeline-Operator `46fc11e5`) and Codex token quota is exhausted until Tue 2026-05-05 07:30 W. Europe. Including all SRC04 phase-2 builds (QUA-403..409, QUA-650-657), the strategy build queue, and several governance items.

**No action required this weekend** — Tuesday morning's runbook (`docs/ops/TUESDAY_RESTART_RUNBOOK_2026-05-05.md`) Step 3+5 re-enables these agents.

### Class 6 — Strategy-card queue (DEFERRED per DL-057)

29 strategy-card / build issues correctly blocked behind the EA queue per DL-057 R-057-4 single-run discipline. SRC03_S01..S15 (Williams cards), SRC04_S02..S11 (Lien cards), SRC01_S1..S5 (Davey cards). They auto-progress when the queue empties + Research extracts the next batch.

**No action required** — DL-057 auto-wake pulse handles this Tuesday.

### Class 7 — Phase 3 umbrella (STAY OPEN)

- `QUA-660` Phase 3 ignition (high) — Tuesday-prep umbrella; CEO closes when first V5 EA reaches P7 milestone
- `QUA-684` phantom-PASS recovery (critical) — Tuesday-prep umbrella; CEO closes when DL-054 wired + first clean baseline runs

These represent the active Phase 3 cycle and stay open by design.

## Other operational hurdles cleared today

- ✅ Bar history compiled across T1-T5 (35/35 .DWX, byte-identical, 3.8 GB each)
- ✅ DL-054 anti-theater pass criteria + gate library + CLI runner + integration plan
- ✅ Tester defaults codified (`framework/registry/tester_defaults.json`)
- ✅ DL-057 auto-wake pulse script + Research rolling tracker (QUA-711)
- ✅ DL-056 CoS hire + DL-055 token-burn watch
- ✅ 6 orphan agent dirs moved to `.retired_2026-05-01/`
- ✅ Skills directory populated with 9 laptop skills + VPS path-translation
- ✅ DevOps QUA-671 churn stopped (4 scheduled tasks disabled, agent paused)
- ✅ Doc-KM + QB models downgraded Opus → Sonnet (cost reduction)
- ✅ CoS heartbeat disabled (anti-spam, wake-on-demand only)
- ✅ Codex agents heartbeat-disabled (anti-spam during quota outage)
- ✅ QM5_1003 setfile reconciliation (36 canonical) + matrix manifest
- ✅ QM5_1004 + QM5_SRC04_S03 setfiles + manifests pre-staged
- ✅ Tuesday restart runbook (9 steps, single sheet)

## What's still NOT cleared (deliberately)

- **`verify_import.py` fresh run** — would launch MT5 Python API against live terminals. Held off due to risk of interfering with active processes; not Tuesday-blocking (DL-054 G1 reads filesystem hcc as primary source).
- **Lessons-learned writeup of today's outage class** — Doc-KM territory; Tuesday post-restore can author. Not Tuesday-blocking.
- **Notion mirror of new DLs (054-057)** — Doc-KM Tuesday work. Not blocking.
- **CEO ratification of CoS recommendations** — CoS produced an audit table at `QUA-699` 14:01Z; CEO consumes Tuesday on next heartbeat. Not blocking.

## Cross-references

- DL-046 — anti-theater (binding on every blocked-status decision)
- DL-051 — issue-creation gate
- DL-054 — anti-theater pass criteria (5 gates)
- DL-055 — token-burn watch ownership (DevOps option (b))
- DL-056 — Chief-of-Staff (OS-Controller scope) hire
- DL-057 — Research-resume on baseline-queue-empty
- `docs/ops/TUESDAY_RESTART_RUNBOOK_2026-05-05.md` — 9-step single-sheet
- `framework/scripts/dl054_gates.py` + `dl054_gate_runner.py` — 5-gate enforcement
- `framework/scripts/dl057_research_auto_wake.py` — perpetual-flow mechanism
- `framework/registry/magic_numbers.csv` — populated this commit (467 rows)
- `framework/registry/tester_defaults.json` — DL-054 Gate 2 inputs

— Board Advisor 2026-05-01.

# Calendar scoped-consumer (E1-C option B) - activation checklist

Recorded 19:02Z 2026-09-05 after OWNER-DEC-CALENDAR-SCOPED-CONSUMER-20260905 = YES (receipt f1dc468c). Source: `docs/ops/evidence/OWNER_VORLAGE_2026-09-05_calendar_scoped_consumer.md` section "Exact ingress and reseal ordering if B is approved". Implementation task: Codex Astra `ea22f4b1` (INERT). This checklist is the gate list for the LATER activation card; nothing here is executed by the YES decision.

1. OWNER adoption of the versioned scoped contract and its exact evidence mapping recorded; compatible EA, bundle, run-plan, adjudication and read-only admission consumers delivered INACTIVE; full-scope ingress path preserved.
2. Fixtures pass: tamper / missing / unknown / wildcard / boundary / DST / basket / zero-trade / mixed control-policy; consumer binaries compiled and bound only through the governed queue; proof that no legacy consumer can ingest a scoped bundle.
3. New create-only candidate + scope proof; the original four measured failures stay visible (a scoped validity claim is never encoded as eight full-envelope PASSes); usable requested windows and affected roster/counter projections measured independently BEFORE requesting activation.
4. Separate OWNER activation receipt binding candidate, sidecar, implementation and consumer-cohort hashes; a distinct registered scoped ingress authority (never the E1-A full-scope authority as an exemption).
5. Governed refresh parent: dry-run staged-pair reconciliation + consumer compatibility, new staging child, publish pair + scope identity, mirror and verify identical FILE_COMMON bytes, registered repin chain under the mutation lock; incomplete publication leaves admission closed.
6. New immutable compatible bundles and append-only successor run plans; recheck every required window, source/build vintage and all other holds; defective historical verdicts stay immutable; holds released only per row by the authorized close-out.
7. New pipeline evidence through ordinary workers; E2/E4 and the official counter move only through their existing authorized evidence paths; no live deployment or AutoTrading change.

Boundaries restated: no reseal, no hold release, no active-inventory binary, no purchase, no live effect from the YES; the CEO calendar-taint hold on new news-gate claims stays until an untainted bundle is pinned.

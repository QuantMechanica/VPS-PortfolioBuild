# QUA-407 Blocked — SRC04 Phase-2 Build (Card QUA-347)

Date: 2026-04-28
Issue: QUA-407
Scope: SRC04 phase-2 EA build from card QUA-347 (`SRC04_S08`, `lien-channels`)

## Wake Acknowledgement

Wake assigned `QUA-407 SRC04 phase-2 build: card QUA-347` to Development. Next action is to implement the EA only if hard gates are satisfied.

## Blocking Evidence

1. Strategy card is not approved:
- Path: `strategy-seeds/cards/lien-channels_card.md`
- Header currently declares:
  - `status: DRAFT`
  - `ea_id: TBD`

2. EA ID is not allocated for this strategy:
- Path: `framework/registry/ea_id_registry.csv`
- No row exists with `strategy_id = SRC04_S08`.

3. Required target EA path cannot be deterministically named yet:
- V5 naming rule requires `framework/EAs/QM5_<ea_id>_<slug>/QM5_<ea_id>_<slug>.mq5`.
- Without allocated `ea_id`, implementation would violate framework hard rules.

## Blocked State

`QUA-407` is blocked for Development implementation in this heartbeat.

## Unblock Owner + Action

- Owner: CEO + CTO
- Required action:
1. Approve card `SRC04_S08` (QUA-347) and set card status to APPROVED.
2. Allocate `ea_id` for `slug=lien-channels`, `strategy_id=SRC04_S08` in `framework/registry/ea_id_registry.csv`.
3. Dispatch Development after (1) and (2) are present in this checkout.

## Immediate Next Action After Unblock

Implement `QM5_<ea_id>_lien_channels.mq5` under `framework/EAs/QM5_<ea_id>_lien_channels/` using V5 framework modules (`Strategy_EntrySignal`, `Strategy_ManageOpenPosition`, `Strategy_ExitSignal`) with card section/page citations, then compile clean and hand off to CTO review.

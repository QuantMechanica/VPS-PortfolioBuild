# CODEX BRIEF 2026-08-02 — QM5_10440/NDX Q10 path (diagnose, then recommend honestly)

**Author:** Claude. **Implementer:** Codex (Sol, effort max). **Reviewer:** Claude.
**Authority:** OWNER 2026-08-02 („Q10-Pfad für 10440 als Codex-Ticket aufsetzen").

**Why this matters:** QM5_10440/NDX is the last live DXZ sleeve without a
kill-switch baseline (pulse: `loaded_ok=23/24`, missing `10440|NDX`). Baselines
are generated from a Q10 PASS net-distribution; 10440 has none. It trades with
the daily-loss and portfolio-DD guards, but without its per-EA distribution
guard — the only such sleeve in the book.

**Hard constraints:** factory keeps running; no T_Live contact, no AutoTrading,
no live deploy, no baseline generation in this ticket; read-only against the
farm DB except any enqueue Claude performs after review; explicit-pathspec
commits.

**Explicit anti-goal — read carefully.** Do **not** search for a configuration,
window, or parameter set under which 10440 passes Q10. That is fishing and it
would corrupt the evidence trail. Your job is to establish *why* it fails and
whether a legitimate path exists, where "retire it from the live book" is an
equally acceptable — possibly the correct — answer.

## Tasks

1. **Establish the failure honestly.** Find every 10440/NDX Q10 attempt in the
   farm DB and its evidence; report the exact failing sub-gate(s) and values
   (memory says ~31 % drawdown breached the gate — verify against artifacts,
   do not repeat the number on trust). Cite file paths.
2. **Vintage check (decisive).** Determine whether the Q10 evidence was produced
   by the binary the live book currently runs. Compare the live T_Live EX5 hash
   for 10440 against the EX5 the Q10 attempt bound. If they differ, the failure
   may be stale evidence rather than a current-strategy property — that changes
   the recommendation completely. Apply the same vintage logic used in the
   MNT-043 bill (`docs/ops/evidence/2026-07-31_ks_vintage_recompile_mnt_bill_PROPOSED.json`).
3. **Gate lineage:** verify the full Q02→Q09 chain for 10440/NDX on the current
   binary — a missing or stale upstream gate is itself a finding.
4. **Recommendation, ranked, with reasons.** Exactly one of:
   (a) legitimate re-run on the current binary (state the exact enqueue command
   for Claude; no parameter changes);
   (b) upstream repair needed first (name it);
   (c) **retire from the live book** — if the drawdown property is structural
   and current, say so plainly and outline the removal consequence
   (diversification loss, NDX exposure left with 13128).
   Include what each option costs in machine time.

## Deliverable

`docs/ops/evidence/2026-08-02_10440_q10_path.md` with the evidence table,
vintage verdict, lineage table, and the ranked recommendation. Router task →
REVIEW. No enqueue, no deploy, no baseline write.

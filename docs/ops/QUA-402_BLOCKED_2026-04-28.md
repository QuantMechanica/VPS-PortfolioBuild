# QUA-402 Blocked Update (2026-04-28)

Issue: `QUA-402` — SRC04 phase-2 build for card `QUA-342` (`SRC04_S03`, `lien-fade-double-zeros`).

## Wake Acknowledgement
Wake payload assigns actionable build work for `QUA-402` on `QUA-342`. Immediate triage was run against the active Development checkout before any EA edits.

## Hard-Rule Gate Result
Blocked by governance prerequisites required by V5 implementation rules:

1. **No allocated EA ID for `SRC04_S03` in active registry**  
   File checked: `framework/registry/ea_id_registry.csv`  
   Current rows end at `1008` (`SRC04_S02b`), with no `SRC04_S03` / `lien-fade-double-zeros` allocation.

2. **No compliant target EA path can be created without allocated `ea_id`**  
   Required naming rule: `framework/EAs/QM5_NNNN_<slug>/QM5_NNNN_<slug>.mq5`.

## Evidence Snapshot
- `framework/registry/ea_id_registry.csv` has `1001..1008`; no `SRC04_S03` row.
- Existing non-registry scaffold was located in another worktree (`pipeline-operator`) as `QM5_SRC04_S03_lien_fade_double_zeros`, which is non-compliant with V5 naming/magic rules until mapped to an allocated numeric `ea_id`.

## Blocked Contract
- **Unblock owner:** CTO (with CEO as required by allocation policy)
- **Unblock action:** Append allocated row in `framework/registry/ea_id_registry.csv`:
  - `ea_id=<new>`, `slug=lien-fade-double-zeros`, `strategy_id=SRC04_S03`, `status=active`, `owner=CTO`, `created_at=2026-04-28`

## Immediate Next Action After Unblock
1. Implement `framework/EAs/QM5_<ea_id>_lien_fade_double_zeros/QM5_<ea_id>_lien_fade_double_zeros.mq5` from Strategy Card `QUA-342` with V5 modular structure (`Strategy_EntrySignal`, `Strategy_ManageOpenPosition`, `Strategy_ExitSignal`) and section-cited inline comments.
2. Run local compile/build_check only for the target EA to ensure clean handoff.
3. Submit CTO review packet; do not dispatch Pipeline-Operator.

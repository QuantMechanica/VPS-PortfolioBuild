# QUA-782 Heartbeat (20260508_083008)

## Action taken
- Added one-shot resume helper:
  - scripts/ops/qua782_resume_if_ready.ps1
- Helper behavior:
  - runs unblock probe
  - launches M15 P3 sweep automatically when READY
  - otherwise emits explicit blocker output and exits

## Execution result now
- ction=skipped reason=blocked_missing_setfiles
- status=BLOCKED missing=2

## Next action
- On next wake, run only qua782_resume_if_ready.ps1 to either auto-launch or confirm block.

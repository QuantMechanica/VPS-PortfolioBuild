# P2 G1-Gate Enforcement Audit — QM5_1001 / QM5_1004
**Audit Issue:** QUA-1076
**Triggered by:** QUA-1060 secondary finding (commit `5b5154cc`)
**Auditor:** Quality-Tech
**Date:** 2026-05-09

---

## 1. Scope

Re-evaluate all P2 PASS rows for QM5_1001 and QM5_1004 against the full DL-054 / P2 phase gate:
- G1a: `model4_log_marker_detected == true`
- G1b: `determinism == true`
- G1c: `min_trades >= 20`
- G1d: `PF > 1.30`
- G1e: `DD < 12%`

Verify runner (`framework/scripts/p2_baseline.py` + `run_smoke.ps1`) enforces all five at HEAD.

---

## 2. Report Evidence

### 2.1 QM5_1001

| Evidence path | Status |
|---|---|
| `D:/QM/reports/pipeline/QM5_1001/P2/` | **DOES NOT EXIST** |
| `D:/QM/reports/pipeline/QM5_1001/index.json` | Exists — see below |

**P2 report.csv:** FILE_NOT_FOUND. No P2 run has ever been completed for QM5_1001 under the V5 framework.

**index.json finding (critical):** QM5_1001's `index.json` carries results for phases P3.5, P5, P5b, P5c, P6, P7, and P8 — all timestamped `2026-04-27`. These predate both:
- DL-054 (established 2026-05-01)
- QUA-662 phantom PASS discovery (2026-05-01)

The index.json data is phantom/zombie output from the old QUAA runtime, not from any valid V5 P2 runner invocation. `final_verdict: "READY"` is unfounded — QM5_1001 has no valid V5 baseline run anchoring any subsequent phase.

**PASS rows to re-audit: 0**

### 2.2 QM5_1004

| Evidence path | Status |
|---|---|
| `D:/QM/reports/pipeline/QM5_1004/P2/report.csv` | Exists |
| Total rows | 207 |
| Verdict=PASS rows | **0** |
| Verdict=FAIL rows | ~185 |
| Verdict=INVALID rows | ~22 |

All 207 rows are FAIL or INVALID. Dominant failure reasons: `MIN_TRADES_NOT_MET`, `REPORT_MISSING;INCOMPLETE_RUNS`, `no_summary_json:rc=1`.

**PASS rows to re-audit: 0**

---

## 3. Verdict Table

| EA | Symbol | Timeframe | Claimed PASS? | Source | Re-audit result |
|---|---|---|---|---|---|
| QM5_1001 | — | — | No valid P2 run | phantom index.json | N/A — PHANTOM DATA |
| QM5_1004 | (all) | H1 | No | report.csv 0 PASS | N/A — NEVER PASSED |

**FALSE POSITIVE COUNT: 0**

Neither EA has P2 PASS rows that constitute false positives from the pre-fix runner. The risk stated in QUA-1076 does not materialize for QM5_1001 or QM5_1004.

---

## 4. Runner Gate Enforcement at HEAD

Examined: `framework/scripts/p2_baseline.py` + `framework/scripts/run_smoke.ps1`

| Gate | Enforced in run_smoke.ps1 | Enforced in p2_baseline.py `derive_verdict` | Status |
|---|---|---|---|
| determinism | YES (line 753-756) | YES (via `result != "PASS"`) | PASS |
| min_trades >= 20 | YES (line 739-743) | YES (via `result != "PASS"`) | PASS |
| model4_log_marker | Bypassed — `-AllowMissingRealTicksLogMarker` always passed (p2_baseline.py line 228) | YES (lines 169-170): INVALID if `model4_log_marker_detected == false` | PARTIAL — p2_baseline.py re-gates correctly, but run_smoke.ps1 bypass flag is hardcoded and confusing |
| **PF > 1.30** | **NOT ENFORCED** | **NOT ENFORCED** | **GAP** |
| **DD < 12%** | **NOT ENFORCED** | **NOT ENFORCED** | **GAP** |

`run_smoke.ps1` PASS condition (lines 766-771): `completedRunCount == Runs AND not globalOnInitFailure AND tradeGatePassed AND deterministic AND not globalTimeoutFailure AND realTicksGatePassed`. No PF or DD check.

`p2_baseline.py derive_verdict` (lines 163-177): checks `result == "PASS"`, then `model4_log_marker_detected`, then trade count. No PF or DD check.

**PF and DD are captured** in the summary JSON (`profit_factor`, `drawdown` fields written by run_smoke.ps1 lines 724-726) but **never evaluated against the 1.30 / 12% thresholds by either runner.**

---

## 5. Secondary Finding: QM5_1001 Phantom Pipeline State

QM5_1001 is recorded as `final_verdict: "READY"` in its index.json (2026-04-27) with phases P3.5 through P8 all PASS/COMPLETE — but there is no P2 directory and no P2 report.csv. This is the same phantom-advance pattern that QUA-662 identified for QM5_1003.

QM5_1001 **must be treated as pipeline-VOID until a valid V5 P2 run produces at least one PASS row** in `D:/QM/reports/pipeline/QM5_1001/P2/report.csv`. Its index.json data must not be relied upon for any pipeline decision.

---

## 6. Actions Required

| # | Action | Owner | Priority |
|---|---|---|---|
| A1 | File CTO ticket: add PF>1.30 and DD<12% gate enforcement to `p2_baseline.py` before any next P2 run | Quality-Tech → CTO | BLOCKER for future P2 PASS claims |
| A2 | File CTO ticket: remove hardcoded `-AllowMissingRealTicksLogMarker` from `invoke_run_smoke` (p2_baseline.py line 228); gate should be enforced at run_smoke level, not bypassed | Quality-Tech → CTO | HIGH |
| A3 | Add INVALIDATION notice to `D:/QM/reports/pipeline/QM5_1001/index.json` and reset QM5_1001 PHASE_STATE to pre-P2 | Pipeline-Op | HIGH |
| A4 | QM5_1004: no recovery needed — it genuinely failed P2 | — | DONE (confirmed) |

---

## 7. All-Clear Declaration (Scoped)

**Within the scope of this audit (P2 PASS false positives for QM5_1001 / QM5_1004):** ALL CLEAR.
- Zero false PASS verdicts exist in either EA's P2 report.csv.
- No recovery sub-issues for false PASS revocation are needed.

**NOT all-clear on:** the runner gate gap (A1/A2 above). Any future P2 run on any EA will not enforce PF or DD unless the CTO fix lands first. This is the dominant open risk from QUA-1076.

---

## 8. CTO Confirmation Requirement

Per QUA-1076 acceptance criteria: CTO must confirm that `framework/scripts/p2_baseline.py` enforces the full G1 gate (including PF>1.30 and DD<12%) before the next P2 run. This confirmation blocks any future P2 PASS claim.

**Evidence references:**
- `D:/QM/reports/pipeline/QM5_1001/index.json` — phantom data, do not cite as valid
- `D:/QM/reports/pipeline/QM5_1004/P2/report.csv` — 207 rows, 0 PASS
- `C:/QM/repo/framework/scripts/p2_baseline.py` — lines 163-177 (derive_verdict), line 228 (hardcoded bypass)
- `C:/QM/repo/framework/scripts/run_smoke.ps1` — lines 739-771 (pass criteria)

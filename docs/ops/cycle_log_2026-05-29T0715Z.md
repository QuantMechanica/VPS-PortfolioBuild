# Orchestration Cycle Log — 2026-05-29T0715Z

**Agent:** Claude (claude-orchestration-2)  
**Cycle start:** 2026-05-29T07:15:25Z  
**Status:** No Claude tasks routed or in-progress. Monitoring Q04 post-fix behaviour.

---

## 1. Factory Health

**Overall: FAIL (2 checks)**

| Check | Status | Detail |
|---|---|---|
| `phase_infra_graveyard` | FAIL | Q04: 230/249 INFRA_FAIL in 6h window, 0 PASS |
| `unbuilt_cards_count` | FAIL | 669 approved cards lack .ex5 + auto-build task |
| `mt5_worker_saturation` | OK | 10/10 terminal workers alive (T1–T10) |
| `mt5_dispatch_idle` | OK | 359 pending work_items, 5 active |
| `p_pass_stagnation` | OK | 246 Q03+ PASS in last 6h |
| `disk_free_gb` | OK | D: 52.1 GB free |
| `codex_auth_broken` | OK | No 401 errors |
| `claude_review_starved` | OK | 1 pending (below threshold 3) |

---

## 2. Router Status

**Claude tasks:** None IN_PROGRESS, None routed  
`route-many` returned `no_routable_task`.  
1017 ready approved cards (research frozen at >5 threshold).

**Active Codex task:** `f308fe3f` — Q04 commission fix (ops_issue, IN_PROGRESS, priority 5)

**Gemini:** 2 REVIEW + 2 APPROVED research_strategy tasks

---

## 3. Q04 Walk-Forward — Post-Fix Diagnostic

### What landed
Commit `121da873 feat(run_smoke): apply Q04 commission via per-run tester groups file`:
- Added `[double]$CommissionPerLot = 0` to run_smoke.ps1 param block (line 34)
- Wired into `Set-TesterGroupsCommission` which patches the Darwinex-Live tester groups file per-terminal per-run
- q04_walkforward.py still passes `-CommissionPerLot 7.0` (line 153)

### Current DB state
```
Q04 status       count   latest
done (FAIL)        21    2026-05-29T07:16:25Z
failed (INFRA_FAIL) 3864  2026-05-29T06:32:28Z
pending             3    2026-05-29T07:17:00Z
```

3 pending: QM5_10513 USDJPY, QM5_10491 GBPUSD, QM5_10569 EURJPY (2 terminal64.exe running at cycle time — folds are active).

### 21 done/FAIL items — key signals
- `run_smoke_exit_code: 0` — q04_walkforward.py exits cleanly
- Fold `exit_code: 1`, `trades: 0`, `pf_net: None` for all folds F1/F2/F3
- `verdict_taxonomy: strategy` (not infra)
- Commission in aggregate.json: `commission_per_lot_round_trip: 7.0` ✓

### Root cause — 6th Q04 issue (INFRA, not strategy)

Inspected report directories for all 21 done work items. **No timestamp subdirectories exist** under `{report_root}/QM5_{ea_id}/`. Structure is only:
```
{work_item_id}/QM5_{ea_id}/Q04/{symbol}/aggregate.json
```

run_smoke.ps1 creates timestamp dirs at line 1158 (`New-Item -Path $rawDir`). The absence of these dirs proves run_smoke.ps1 **fails before line 1158** for every fold call.

**Candidate failure point:** `Test-TerminalAlreadyRunning` at line 1121–1123:
```powershell
if (($Terminal -ine "any") -and (-not $AllowRunningTerminal.IsPresent)) {
    if (Test-TerminalAlreadyRunning -TerminalRoot $terminalRoot) {
        throw "Terminal instance is already running for $terminalRoot. Stop it first..."
    }
}
```
Q04 fold calls pass explicit `-Terminal Tx` (not "any") and do not pass `-AllowRunningTerminal`. If the assigned terminal's terminal64.exe is still running (from a preceding Q02/Q03 work item that was just released), this throws immediately — no reportDir created, exit_code=1.

**Diagnostic gap:** q04_walkforward.py captures fold stdout/stderr via `subprocess.run(..., capture_output=True)` but NEVER logs them. The exact error message is lost for every fold failure.

**Evidence for the race hypothesis:**
- 3,864 INFRA_FAILs all occurred when Q04 was piped into an active terminal pool
- The 3 currently pending items ARE running (2 terminal64.exe alive at 07:27Z) — suggests when terminal is free at start, folds DO proceed

### Fix recommended for Codex (task f308fe3f)
Two patches, both needed:

**Patch A (diagnostic):** In `run_fold_via_smoke()` of q04_walkforward.py, after `proc = subprocess.run(...)`, write `proc.stdout` and `proc.stderr` to `{report_root}/QM5_{ea_id}/Q04/{fold_id}/fold_log.txt`. This makes the actual run_smoke error visible.

**Patch B (fix):** In `_spawn_phase_runner_for_work_item` (farmctl.py), call `_stop_terminal_slot_for_release` on the assigned terminal BEFORE spawning q04_walkforward.py. This ensures the terminal is not "already running" when fold F1 starts.

Alternatively in q04_walkforward.py, wait up to 30s for terminal64.exe to exit before starting the first fold if the terminal is detected as running.

---

## 4. QM5_10260 Queue State

```
Phase  Status  Count
Q02    done    25
Q02    failed   1
Q03    done   102
Q04    failed  102  (all verdict=INFRA_FAIL, symbol=NDX.DWX)
```

Last Q04 attempt: 2026-05-28T18:04Z — stale (36h). No new attempts since then.  
**Status:** Waiting for Q04 infrastructure fix to land. Will auto-retry once the Test-TerminalAlreadyRunning race is resolved.

---

## 5. Memory Updates Required

- Q04 state: fold failures now evidence-confirmed as pre-reportDir (6th root cause)
- 3 pending Q04 items actively running at cycle close

---

## 6. Risks & Blockers

| Risk | Severity | Notes |
|---|---|---|
| Q04 fold failures | HIGH | 6th root cause confirmed (pre-reportDir throw); Codex task f308fe3f IN_PROGRESS |
| 669 unbuilt cards | MEDIUM | Health alarm; needs pump cycles; auto-build bridge active |
| QM5_10260 stale at Q04 | LOW | Will auto-clear when Q04 fixed |
| Q04 fold stdout lost | MEDIUM | Diagnostic blindspot; patch A above mitigates |

**No OWNER decision blocking** — Q04 commission confirmed $7/lot (commit b18a8b21). Current task is implementation.

---

## 7. Recommended Next Step

Codex: In task `f308fe3f`, update q04_walkforward.py to:
1. Log fold stdout/stderr to `{report_root}/QM5_{ea_id}/Q04/{fold_id}/fold_log.txt`
2. Before fold F1 starts, poll until terminal64.exe on the assigned terminal is not running (30s timeout)
3. After each fold completes (run_smoke.ps1 exits), confirm terminal64.exe has exited before starting next fold

Then re-run q04_walkforward.py for a sample Q04 work item manually to confirm no-dir-creation error is gone.

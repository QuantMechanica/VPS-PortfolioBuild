# QUA-391 Blocked Snapshot (Development)

Date: 2026-04-28  
Issue: QUA-391 — SRC04 pilot build S02a (EA implementation from card; CTO gate before pipeline)

## Acknowledgement and scope change
Wake payload assigns QUA-391 directly. Next action is implementation for `SRC04`/`S02a` only; no parallel EA starts.

## Why blocked

1. **Card sync landed, but card is not approved yet.**  
   `strategy-seeds/cards/lien-dbb-pick-tops_card.md` now exists in this checkout, but header remains:
- `strategy_id: SRC04_S02a`
- `ea_id: TBD`
- `status: DRAFT`

2. **No EA ID allocation row exists for `SRC04_S02a` in Development registry.**  
   `framework/registry/ea_id_registry.csv` contains `1001..1006` only (`breakout-atr` + `davey-*`), with no `lien-dbb-pick-tops` allocation.

3. **No existing SRC04 EA scaffold is available to patch.**  
   `framework/EAs/` in this checkout currently contains only:
- `QM5_1003_davey_baseline_3bar`
- `QM5_1004_davey_es_breakout`
- `QM5_1006_davey_eu_day`

## Non-blocking observation
Raw source artifacts for `SRC04` exist under Research/Pipeline worktrees (e.g., `strategy-seeds/sources/SRC04/`), but those are not approved Strategy Cards and cannot be coded directly under V5 hard rules.

## Unblock owner and exact action

- **Owner: CEO + CTO**  
  Action: promote `strategy-seeds/cards/lien-dbb-pick-tops_card.md` from `status: DRAFT` to approved and assign concrete `ea_id` in the card header.

- **Owner: CEO + CTO**  
  Action: allocate the same unique `ea_id` in `framework/registry/ea_id_registry.csv` for slug `lien-dbb-pick-tops` / strategy `SRC04_S02a`.

- **Owner: CTO / Harness**  
  Action: confirm this Development worktree remains the writable target for QUA-391 implementation and CTO pre-pipeline review handoff.

## Next action after unblock
Implement `framework/EAs/QM5_<ea_id>_<slug>/QM5_<ea_id>_<slug>.mq5` from the approved `SRC04 S02a` card with:
- `#include <QM/QM_Common.mqh>`
- required input groups (`QuantMechanica V5 Framework`, `Risk`, `News`, `Friday Close`, `Strategy`)
- modular functions `Strategy_EntrySignal`, `Strategy_ManageOpenPosition`, `Strategy_ExitSignal`
- inline comments citing Card section/page refs for each implemented rule
- CTO review handoff package (no pipeline/backtest execution by Development).

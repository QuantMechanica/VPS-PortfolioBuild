# Claude Orchestration Cycle — 2026-05-23 1706Z

## Status: FAIL (2 health checks fail)

## What happened

### No Claude tasks routed
- All 3 TODO tasks are `dropbox-video-extraction` (`video-analysis` + `strategy-extraction` skills)
- Explicitly misrouted from Codex earlier; router locks them to Gemini only
- Gemini at capacity (2/2 in progress); tasks queue until Gemini frees a slot
- `route-many` confirms: `no_available_agent` for task `6672fa16`

### System-class codex review FAILs (health check: codex_review_fail_rate_1h = FAIL)
2 of 14 codex reviews in last hour failed on non-strategy sections:

**QM5_10022 (rw-dual-mom)** — `framework_corset: FAIL`
- `Strategy_IsMonthlyRebalance` uses direct `iTime` timestamp gate (lines 65–66) instead of `QM_IsNewBar`
- `OnTick` calls `Strategy_ExitSignal` before `QM_IsNewBar` at line 260 → monthly rebalance logic runs per tick
- `build_ea` task already `failed`; awaiting Codex rework via pump

**QM5_10034 (rw-pairs-z)** — `framework_corset: FAIL` + `forbidden_grep: FAIL`
- Direct include of `QM_BasketOrder.mqh` at line 6 (must go through `QM_Common.mqh`)
- Opens via `QM_BasketOpenPosition` instead of `QM_TM_OpenPosition`
- Magic resolution uses `QM_MagicChecked` instead of `QM_FrameworkMagic`
- `weights[` pattern at lines 293, 295, 303 (forbidden_grep)
- `build_ea` task already `failed`; awaiting Codex rework via pump

### INFRA_FAIL wave on Q02
Six EAs produced INFRA_FAIL in the last 2h:

| EA | Symbol | Period | Root cause |
|---|---|---|---|
| QM5_10019 | GBPUSD.DWX | M5 | MT5 tester Core 01 "authorization failed (Invalid parameters)" — transient auth failure |
| QM5_10020 | NDX.DWX | D1 | TIMEOUT (1800s), 0 trades, model4_log_marker absent — possible OnInit hang |
| QM5_10021 | — | — | INFRA_FAIL + currently re-running (active) |
| QM5_10718 | EURUSD.DWX | — | INFRA_FAIL (prior wave, known) |
| QM5_10005 | EURUSD.DWX | M15 | ex5_missing — KillSwitch compile defect (g_qm_ks_initialized double-definition, known) |
| QM5_1099 | — | — | Mixed INFRA_FAIL/FAIL on repeated Q02 attempts |

News calendar: status OK on all runs (not the cause).

QM5_10005 is blocked until the KillSwitch naming defect is resolved (Codex task required; not assigned yet).

### QM5_10260 (cieslak-fomc-cycle-idx) — empty queue
- Zero work_items rows; stalled since last TIMEOUT wave
- Perf rework unresolved; no active Codex task for this
- Not a strategy rejection — TIMEOUT is an infrastructure/performance issue

### Unenqueued EAs (health: WARN)
8 EAs passed codex review but have no Q02 work_items:
QM5_10023, QM5_10026, QM5_10027, QM5_10028, QM5_10039, QM5_10041, QM5_10042, QM5_10044
(Pump next cycle should auto-enqueue via `unenqueued_eas_count` hint.)

### p_pass_stagnation (health: FAIL)
- 0 Q03+ PASS verdicts in 12h
- Downstream of INFRA_FAIL wave; no Q02 pass → no Q03 work

## Evidence
- `D:\QM\reports\work_items\fa65c168-...\QM5_10019\20260523_162515\summary.json` — TIMEOUT + Core auth failure
- `D:\QM\reports\work_items\00fbea01-...\QM5_10020\20260523_162516\summary.json` — TIMEOUT, model4_log_marker absent
- `D:\QM\strategy_farm\artifacts\verdicts\codex_review_1c4590a2-....json` — QM5_10022 framework_corset FAIL
- `D:\QM\strategy_farm\artifacts\verdicts\codex_review_a57953a5-....json` — QM5_10034 framework_corset + forbidden_grep FAIL

## Risks / Blockers
1. **QM5_10005 compile block** (KillSwitch defect) — needs Codex rename in `QM_KillSwitchKS.mqh`; no task assigned
2. **QM5_10034 weights[] pattern** — forbidden_grep violation; may indicate improper weight-array logic (not ML library but pattern is reserved); Codex must rewrite
3. **QM5_10020 OnInit hang on NDX D1** — if repeats on re-run, EA needs profiling; 1800s model-4 timeout on a D1 symbol is unusual unless EMA initialization is O(N) per tick
4. **MT5 transient auth failure (QM5_10019)** — single occurrence; will self-resolve on retry

## Recommended next steps (for OWNER)
- **KillSwitch defect (QM5_10005)**: assign Codex task to rename `g_qm_ks_initialized` in `QM_KillSwitchKS.mqh`
- **QM5_10022 / QM5_10034**: pump should create rework Codex tasks; verify pump handles `build_ea failed` → rework loop correctly
- **QM5_10260**: assign Codex task for FOMC cycle EA perf rework (cieslak-fomc-cycle-idx OnTick O(N) fix)
- **QM5_10019 auth failure**: monitor next run; if repeats, check T1 terminal worker health

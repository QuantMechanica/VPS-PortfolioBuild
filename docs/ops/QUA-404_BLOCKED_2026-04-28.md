# QUA-404 Blocked — SRC04 Phase-2 Build (Card QUA-344)

Date: 2026-04-28
Issue: QUA-404
Scope: SRC04 phase-2 EA build from card QUA-344 (`SRC04_S05`, `lien-inside-day-breakout`)

## Wake Acknowledgement

Wake assigned `QUA-404 SRC04 phase-2 build: card QUA-344` to Development. Next action is to implement the EA only if hard gates are satisfied.

## Blocking Evidence

1. Strategy card is not approved:
- Path: `strategy-seeds/cards/lien-inside-day-breakout_card.md`
- Header currently declares:
  - `status: DRAFT`
  - `ea_id: TBD`

2. EA ID is not allocated for this strategy:
- Path: `framework/registry/ea_id_registry.csv`
- No row exists with `strategy_id = SRC04_S05`.

3. Required target EA path cannot be deterministically named yet:
- V5 naming rule requires `framework/EAs/QM5_<ea_id>_<slug>/QM5_<ea_id>_<slug>.mq5`.
- Without allocated `ea_id`, implementation would violate framework hard rules.

## Blocked State

`QUA-404` is blocked for Development implementation in this heartbeat.

## Unblock Owner + Action

- Owner: CEO + CTO
- Required action:
1. Approve card `SRC04_S05` (QUA-344) and set card status to APPROVED.
2. Allocate `ea_id` for `slug=lien-inside-day-breakout`, `strategy_id=SRC04_S05` in `framework/registry/ea_id_registry.csv`.
3. Dispatch Development after (1) and (2) are present in this checkout.

## Immediate Next Action After Unblock

Implement `QM5_<ea_id>_lien_inside_day_breakout.mq5` under `framework/EAs/QM5_<ea_id>_lien_inside_day_breakout/` using V5 framework modules (`Strategy_EntrySignal`, `Strategy_ManageOpenPosition`, `Strategy_ExitSignal`) with card section/page citations, then compile clean and hand off to CTO review.

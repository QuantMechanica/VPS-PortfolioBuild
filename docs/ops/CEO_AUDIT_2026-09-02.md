# CEO Audit 2026-09-02 — QuantMechanica V5 Strategy Factory

**Mandate (OWNER, 2026-09-02):** full operational authority for the Factory CEO (Claude); goal = money via FTMO payouts and Darwinex Zero allocation; build-in-public deferred; processes/tools/DBs may be rebuilt. Remaining hard limits: T_Live AutoTrading toggle, spending money, sealed gate criteria/thresholds (OWNER).

**Method:** 12-dimension audit workflow (`wf_33b75069-ada`, 137 agents, 68 findings, 40 adversarially verified with 3 lenses: 21 survived, 19 refuted), one execution wave (`wf_7cc7053c-d48`: 7 analyses + 3 code fixes), one focused adversarial verification of the selection-bias finding (`wf_6bdf1d21-a3c`), plus direct CEO forensics. All reports live in the session scratchpad (`audit_synthesis.md`, `audit_critique.md`, `wave1/*.md`); the money-relevant ones are mirrored below with their numbers.

---

## 1 · Verdict in five sentences

1. **The counter was structurally stuck at 0/25, not throughput-limited.** The census that `book_build_guard` uses scored the ratified success verdicts `CONFIG_LOCKED` (Q10), `NO_FILTER_CHANGE` (Q12) and `NO_PARAMETER_CHANGE` (Q13) as non-PASS, so no chain could ever be contiguous to Q14. Fixed (`8baa00fde9`): Q11-contiguous 0→11, Q14-complete 0→2 (`QM5_10706/GBPUSD`, `QM5_11422/USDCAD`), `qualified_pairs` 0→2 — honest reading: 2 formal (their Q14 rows are the 25.08 NO_CHALLENGER short-circuits), 0 with a completed real DL-089 census.
2. **The Q12 pattern-filter census (≈90 % of factory hours) could never finish a pair**: the finalizer did not recognise the terminal ledger state `READY_FOR_Q15` (fixed `219217c28c`; first real terminal chain `QM5_11421/EURUSD` → Q14 `ff733cf6` KEEP_INCUMBENT), and 14 Q11 pairs had no `_opt` measurement sibling at all (7 built today, 6 compiled).
3. **The factory lost ~55–60 % of its hours in the last 7 days to control-plane incidents, not to lack of terminals**: four fleet-wide containment trips in 12 h (each serialises 10 terminals to one claim lease), a worker-exit path that strands `active` claims, a release tool that cannot work in serial mode. All three fixed and committed (`54c3e3a3fd`, `48b8e2bbcf`, `bcff7d044d`); Defender exclusions added for the state DB/repo; fleet reloaded staggered.
4. **Money: neither venue has an evidence-backed product yet, and the reason is not what the plan assumed.** The live book's −2,227 USD since 19.07 is ~76 % two untagged `magic=0` trades that belong to no governed sleeve (1.00-lot NDX −1,537 on 27.07, 0.43-lot EURUSD −261 on 24.07); costs are 5.7 %; the governed book is −469 USD / 30 active days with a Sharpe CI of [−6.8, +4.6]. The modeled +2.4 book Sharpe survives a multiple-testing correction at ~0 % (no sleeve reaches DSR ≥ 0.95 at any defensible trial count; E[max SR] under the null ≈ 1.06–1.44), while walk-forward held-out folds lean against zero edge (82 % PF > 1). **Edge is unproven, not disproven; the cheapest decisive test is a 2026-Q1 out-of-sample pass (~3 terminal-hours) that has never been run.**
5. **FTMO: NO-BUY today.** Under live-consistent statistics P(pass P1) ≈ 2–6 % and EV ≈ −$540/attempt; under the modeled statistics EV is positive but those statistics are exactly what is unproven. The EV script also reads a mutable stream dir that shrank from 21 to 5 sleeves (results ~16× off). The park stays, re-anchored from "25 pairs" to "positive out-of-sample edge evidence".

---

## 2 · What was fixed or executed today (evidence)

| # | Item | Evidence |
|---|---|---|
| 1 | Containment trip #4 (07:02Z) released 07:44Z via mutation-lock claim pause + standing DL-086 receipt; orphan claim c261068d released via canonical `release_stale_claims_for_terminal` | `D:/QM/strategy_farm/state/custom_history_containment_mode.json` (reason `ceo_release_after_copy_on_claim_trip4_20260902`), scratchpad `release_with_pause.log` |
| 2 | Q12 finalizer accepts `READY_FOR_Q15`; EUR pilot Q12 `c4bc189b` → NO_FILTER_CHANGE, Q13 `2518ae77`, Q14 `ff733cf6` KEEP_INCUMBENT | commit `219217c28c`; `D:/QM/strategy_farm/artifacts/opt_census/DL089_QM5_11421_EURUSD_DWX_2019_2025/q12_selection_receipt.json`; `D:/QM/reports/optimization_fork/ff733cf6-…/receipt.json` |
| 3 | Census read-model alignment (CONFIG_LOCKED, NO_FILTER_CHANGE, NO_PARAMETER_CHANGE = contiguous evidence) | commit `8baa00fde9`; `book_build_guard.py --status --venue both` → `qualified_pairs: 2` |
| 4 | Seven `_opt` siblings (QM5_41301–41307) built, reviewed (7/7 PASS), magic-allocated, cards published, governed compiles: 41301–41304, 41307 COMPILE_OK and committed; 41305 compiling; 41306 D10 guard fixed, recompile needs a task-bound successor row | commits `66d731abda`, `98eca49aa0`, `2961c3f39a`, `87b0f4b3ce`, `8af8496b0f`; compile rows `377ada81`, `66ca089e`, `a6a473b5`, `fe284ede`, `b4a707da` |
| 5 | Worker fixes: containment scope (INTEGRITY → fleet; CLAIM_LOCAL/TRANSIENT → 15-min terminal quarantine + `custom_history_copy_on_claim_failures.jsonl`), orphan-claim marker + reconcile, standing release in serial mode; 65 new/updated tests green | commits `54c3e3a3fd`, `48b8e2bbcf`, `bcff7d044d` |
| 6 | Defender exclusions for `D:\QM\strategy_farm`, `C:\QM\repo`, `C:\QM\worktrees`, `D:\QM\exports`, python/pythonw/metaeditor64/codex/node (MsMpEng had 479k CPU-s) | `Get-MpPreference` |
| 7 | ~~OWNER-DEC-SAMEPROG-FLEET-20260831 executed~~ **ROLLED BACK 09:42Z**: the same-program canary (a7c69b44) had already been aborted on 01.09 for a lane-preflight decline loop (88→21 cells/h) and the re-activation reproduced it (3–8 cells/10 min, `factory_mutation_lock_busy` storms). Env removed, staggered reload; re-attempt only after the decline loop is fixed under load. CEO error: the execution-task verdict was not read before re-executing. | scratchpad `staggered_worker_reload*.log`, task a7c69b44 verdict |
| 8 | Q10 news-gate unblock: append-only Q07/Q08 reruns for the genuinely blocked census parents (11421 Q07 `2556a768`, 13128 Q08 `d2d76958`, 11288 Q08 `0d5ec1cc`, 13013 Q07 `4668562c`) | `farmctl enqueue-backtest --append-only-rerun-of …` receipts in `work_items` |
| 9 | REQUAL-8 pair 7 (QM5_41221): worker-bound Q01 smoke authenticated PASS; boundary halted fail-closed on a tooling gap (generation successor + hold release were ad-hoc scripts) → canonical `farmctl record-q01-smoke-successor` / `release-hold` in build | `docs/ops/evidence/2026-09-02_1b57e398_q09_requal8_pair7_smoke_authenticated_boundary_tool_gap.md` (commit `353bde1545`) |
| 10 | `QM_StrategyFarm_ClaudeOrchestration_15min` disabled for the interactive session (duplicate-session race class) — re-enable at handoff | `Get-ScheduledTask` |

---

## 3 · Findings that change the plan (verified)

### 3.1 The census/pipeline predicate mismatch (root cause of "0/25")
`rebaseline_census.py` (the authority behind `qualified_pairs`) and the pipeline's own success predicates disagreed in three places. Two are fixed (above). The third is an OWNER call: **Q08 = FAIL_SOFT** is accepted by the news binder (`q09_news_runner.py:1169`) and was explicitly moved into the Q09 baseline lane by OWNER-DEC-DL082-EXT Option D (01.09), but the census scores it ECON_FAIL, so `QM5_21507/XAUUSD` and `QM5_13213/USDJPY` (both with live DL-089 censuses, ~130–150 terminal-h each) can never count. → OWNER ask #2.

### 3.2 Selection bias (verified 2/3; the refutation concerns the inference, not the arithmetic)
- Trials: 13,398 distinct (EA, symbol) pairs backtested at Q02 from 3,001 distinct EAs; E[max annualized Sharpe] under the null = 1.44 (N = 13,398) … 1.06 (N = 300).
- Live book sleeves: annualized per-trade Sharpe median 0.60, max 1.24; Q11 frontier median 0.39, max 0.92. DSR ≥ 0.95: **0/24 and 0/21 for every N ≥ 10.** Book Sharpe +2.13 (reproduced) is diversification of individually insignificant edges.
- Q08 sub-gate 8.2 (DSR) is a trivial deferred pass in 141/166 rows (85 %) — the pipeline's only multiplicity defence never fires. MT5's reported Sharpe is 11–22× the return-based Sharpe on identical trade lists.
- **Refuted part:** "live −3.25 proves the null". Governed book Sharpe −1.10 ± 2.90 over 30 active days; Bayes factor null-vs-modeled 1.9 (no discrimination); WF held-out folds 61/74 PF > 1, geo-mean OOS PF 1.47 (selection-conditioned). Verdict: **undecidable today.** Decisive cheap test: 2026-Q1 OOS diagnostic (data 2026-01-01…04-06 exists on all terminals, never used; ~55 runs, ~3 terminal-hours, non-admission).

### 3.3 Live DXZ book (governance, not edge)
- Two untagged `magic=0` positions = ~76 % of the realized loss since go-live. Not a governed sleeve. Root identity unknown (manual trade? unlabeled EA? magic-strip bug?). → OWNER ask #1.
- 24/24 sleeves are attached (`DarwinexZero_V2_LiveOps`, expertmode=1); 16 traded since 19.07; 3 structurally dark (`12778`, `13117` basket warm-up 0/4 legs; `12969/USDJPY` silent since 28.08); 5 no-signal within design frequency. `loaded_sleeve_count=0` ALARM = pulse lookback artifact (last profile reload 23.08 is one journal outside the 10-file window) — fix in `live_book_pulse.py`, not in the book.
- `10440/NDX` trades live on a Q10 FAIL with no kill-switch baseline (the 23/24 gap); baseline cannot be derived. → OWNER ask #5.
- DXZ rules (web-verified): DarwinIA SILVER rating ≥ 75 (22 % current month / 67 % trailing-5-month return / 11 % 6-month DD), GOLD needs an 8-month track record; **roster changes do not reset the track record**; 15 % profit split; $35–50/month.
- MNT-036 (probation review 06.09) delta written: `wave1/mnt036_delta_2026-09-02.md` — 1556/XAU +150 (CONTINUE), 10706/GBP +395 (CONTINUE), 13128/NDX 0 trades, missed 29.07 FOMC = defect (REQUALIFY/REMOVE).

### 3.4 FTMO
- Rulepack rule facts current (no time limit, 10 %/5 %, 5 %/10 % caps) but every economic field missing (price $540, 100 % fee refund on first payout, 80→90 % split, Swing leverage 1:30/1:15, scaling). Atomic pre-trade daily-loss budgeter still `NOT_IMPLEMENTED` (design written in `wave1/d5-ftmo-path-honestly.md` §3).
- EV table (0.50×): modeled P1 30 %/funded 12 %/break-even fee $9.3k; live-consistent P1 2 %/funded 0 %/break-even ~$0; mixture P1 16 %/break-even $1.9–2.3k. `audit_ev_funded_account.py` reads a drifting stream dir (5 of 21 sleeves) → numbers ~16× off the sealed 19.08 artifact; must be re-pointed.
- Swing swaps: XAU long −66 pts (hostile), USDJPY +0.9, USOIL +4.2 → only a handful of sleeves are Swing-compatible.
- `QM_FTMO_TrialPulse` dies at its task time limit every run → the free-trial gate is unmeasurable.

### 3.5 Factory economics
- Healthy fleet: ~72–80 cells/h; cell median 7 min (tester ~3.5 min + ~3.5 min claim overhead: gate audit ≈ 60 s, lease waits, prestage). CPU-bound at 10 terminals (~96 %). The 27.08 accelerators do not work (warm runner UNSUPPORTED, native optimizer NOT_REPRODUCIBLE); pruning IS live (36 % of resolved cells skipped). T11/T12 expansion is net-negative until the isolation bindings cover them → frozen.
- Compile lane: 23 rows held since 27.08 by a rollout hold that no restart releases + 17 unbound legacy rows (never claimable) — both are dead weight, not on the critical path.
- 8 census programs × 1 lane left ≥ 2 terminals idle at all times → same-program parallelism (OWNER-approved 31.08) activated today.

---

## 4 · CEO decisions (operating doctrine, effective now)

1. **First money is gated by edge validity, not by pair count.** The 25-pair trigger (OWNER-DEC-A1) stays as the sealed book-construction trigger; it is necessary, not sufficient. Standing evidence requirements before any FTMO purchase or DXZ roster expansion: (a) 2026-Q1 out-of-sample pass on the deployed binaries and the frontier; (b) governed live attribution (magic-0 excluded) with confidence intervals; (c) portfolio-level deflated Sharpe reported next to every book candidate.
2. **Stop list:** T11/T12 ignition attempts (until isolation bindings cover them); counting the 27.08 compute accelerators as levers; new card sourcing and build_ea re-attempts (supply is not the constraint: 3,941 cards, 420 built-never-gated EAs); treating the pulse ALARM as a live emergency; the FTMO free-trial probe as an edge test.
3. **Queue doctrine:** census cells only on pairs that are census-contiguous through Q10; Q07/Q08/Q10 unblock reruns for census parents run ahead of new census admissions; Q02/Q04 universe expansion keeps its lowest rank.
4. **Tooling doctrine:** every governed transition that was ever done by an ad-hoc script gets a canonical `farmctl` subcommand before it is done again (first two: `record-q01-smoke-successor`, `release-hold`; next: task-bound compile retry after a source repair).
5. **Reporting doctrine:** `qualified_pairs` is reported as "formal / with completed real census"; MT5's Sharpe field is not used for decisions.

---

## 5 · OWNER asks (short list; everything else is executed under the mandate)

1. **Who placed the two `magic=0` live trades** (1.00-lot NDX 27.07, 0.43-lot EURUSD 24.07)? If manual: they must be excluded from every track-record read and never repeated on the DXZ account; if not manual: a magic-strip defect exists and must be found before the pointer is signed.
2. **Q08 FAIL_SOFT book eligibility** (sealed criterion): either FAIL_SOFT counts as contiguous evidence (consistent with DL082-EXT Option D) or the two affected census programs (21507/XAU, 13213/USDJPY) should be paused. Recommendation: count it; it was your Option D.
3. **06.09 live-book session:** sign the deployment pointer (lifts risk-freeze SP-A1/A2), 3-sleeve probation dispositions per the MNT-036 delta, `10440/NDX` remove-or-re-gate, REDUCE/REMOVE of the drag sleeves (11708, 11132, 10939, 10513, 1567, 13128). Pruning does not reset the DarwinIA track record.
4. **Concentration policy ratification** (`SP-C3`, symbol 40 % / asset class 60 % / family 50 % / session 70 % / stop budget 2.5 %) — without it `build_book_dxz` cannot emit APPLY_RECOMMENDED even with 25 pairs.
5. **Dukascopy backfill to 2026-07** (deferred 30.08): recommend YES (cost 0, factory-parallel) — it is the only way to turn the 2026-Q1 diagnostic into a signed OOS window.
6. FTMO: no purchase; re-anchor the park trigger to positive OOS/live evidence (my recommendation; the money decision remains yours).

---

## 6 · Next 72 h (CEO lane, commissioned as router tasks)

- OOS-2026 single-window diagnostic runner (build, dry-run, 55 runs behind the census) and the edge-read memo.
- Magic-0 forensic; `live_book_pulse` lookback fix; dark-sleeve diagnosis; MNT-036 package to OWNER by 04.09.
- REQUAL-8: finish pair 7 with the canonical tools, hand off pair 8; task-bound compile retry tool for 41306.
- Q08 sub-gate 8.2 (DSR) wired to the real cohort; MT5-Sharpe audit; EV script re-pointed to the sealed streams; rulepack v2 economic fields; `QM_FTMO_TrialPulse` time limit.
- Schtask hygiene (7 failing tasks, ~12 dead one-offs); factory-hours-lost KPI panel.
- Q14 lane hygiene: supersede the orphan Q13/Q14 rows minted off the generic Q12 rows (11421, 10706).

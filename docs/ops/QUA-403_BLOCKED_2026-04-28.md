# QUA-403 Blocked — 2026-04-28

Issue: `QUA-403` — `SRC04 phase-2 build` (`QUA-343` / `SRC04_S04` `lien-waiting-deal`).

## What I validated

- Strategy card exists: `strategy-seeds/cards/lien-waiting-deal_card.md`.
- Card header is not implementation-ready:
  - `strategy_id: SRC04_S04`
  - `ea_id: TBD`
  - `status: DRAFT`
- EA ID registry has no SRC04 allocation yet: `framework/registry/ea_id_registry.csv` currently lists only `SRC01_*` mappings (`1002`-`1006` and framework seed rows).
- No existing EA scaffold/path exists for this card under `framework/EAs/QM5_NNNN_*`.

## Blocker

Development cannot start coding under V5 hard rules until the card is approved and an EA ID is allocated.

## Unblock owner + exact action

- **Owner:** CTO + CEO
- **Action required:**
  1. Update `strategy-seeds/cards/lien-waiting-deal_card.md` from `DRAFT` to approved state per policy.
  2. Allocate EA ID for `SRC04_S04` in `framework/registry/ea_id_registry.csv` (`ea_id`, `slug`, `strategy_id`, `status`, `owner`, `created_at`).
  3. Dispatch Development again on `QUA-403` after (1) and (2) are merged in active checkout.

## Next action after unblock

Create `framework/EAs/QM5_<allocated>_lien_waiting_deal/QM5_<allocated>_lien_waiting_deal.mq5` using V5 framework modules and card-cited inline rule comments, then hand off to CTO review-only gate before Pipeline-Operator.

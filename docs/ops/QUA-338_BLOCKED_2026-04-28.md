# QUA-338 Blocked Snapshot (2026-04-28)

Issue: QUA-338 P1 implementation — `SRC02_S01` cointegration pair MR EA scaffold + compile evidence.

## Blocker

P1 implementation is blocked by missing `ea_id` allocation for `SRC02_S01` in `framework/registry/ea_id_registry.csv`.

Current registry rows stop at:
- `1006,davey-eu-day,SRC01_S02,...`

Hard-rule impact:
- Cannot create compliant EA path/name without allocated `ea_id` (`framework/EAs/QM5_NNNN_<slug>/QM5_NNNN_<slug>.mq5`).
- Cannot wire compliant magic resolution (`QM_Magic(ea_id, slot)`) for strategy-specific module scaffold.

## Unblock Owner + Action

- Owner: CTO (with CEO per allocation policy)
- Action:
1. Allocate `ea_id` for `SRC02_S01` in `framework/registry/ea_id_registry.csv`.
2. Confirm canonical slug for framework path naming (`chan_pairs_stat_arb` vs alternate slug normalization).
3. Re-dispatch Development on QUA-338 after registry commit lands.

## Immediate Next Action After Unblock

Implement `framework/EAs/QM5_<ea_id>_chan_pairs_stat_arb/QM5_<ea_id>_chan_pairs_stat_arb.mq5` scaffold with V5-required input groups and module functions:
- `Strategy_EntrySignal`
- `Strategy_ManageOpenPosition`
- `Strategy_ExitSignal`

Then run strict compile evidence via `framework/scripts/compile_one.ps1 -EAPath <mq5> -Strict` and hand off to CTO review.

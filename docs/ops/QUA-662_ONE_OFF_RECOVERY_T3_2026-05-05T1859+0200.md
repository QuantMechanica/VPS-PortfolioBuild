# QUA-662 one-off recovery action (T3 lane) — 2026-05-05T18:59+02:00

## Trigger

- `T3` matrix runner PID from manifest was missing.
- `T3` lane log remained pinned at first symbol:
  - `GBPAUD.DWX`

## Action taken

- Executed one-off single-symbol recovery on `T3`:
  - `python framework/scripts/p2_baseline.py --ea QM5_1003 --symbols GBPAUD.DWX --terminal T3 --year 2024 --runs 2 --out-prefix D:\QM\reports\pipeline --timeout 1800`

## Outcome

- Explicit failure captured:
  - `run_smoke_fail:REPORT_MISSING;METATESTER_HUNG;INCOMPLETE_RUNS`
- Summary evidence:
  - `D:\QM\reports\pipeline\QM5_1003\P2\QM5_1003\20260505_165821\summary.json`
- `report.csv` auto-appended correctly in this run (no manual reconcile needed).

## Progress delta

- `report.csv` line count:
  - before: `5`
  - after: `6`

## Next action

- Continue serialized lane recovery only:
1. Re-check `T4` and `T5` stall/liveness deltas.
2. Recover one symbol on the most-stalled remaining lane.
3. Keep advancing `report.csv` rows toward full 36-symbol coverage.

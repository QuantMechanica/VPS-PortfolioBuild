# QUA-590 Closeout (2026-05-01)

Issue: QUA-590 (QUA-588/F2)

## Summary

- Removed stray repo-root zero-byte files created by misquoted shell command:
  - `Append-only`
  - `Authoritative`
  - `agents,`
  - `hourly-public`
- Added commit-path guardrails:
  - untracked-file enforcement (optional)
  - repo-root zero-byte sentinel with DL-028 reference
- Wired guard into pre-commit mutex path and documented in `processes/11-disk-and-sync.md`.

## Verification Evidence

- Root zero-byte probe check:
  - created `zz_qua590_probe_zero_byte.tmp` (0-byte)
  - executed `Assert-CommitAllowlist.ps1 -FailOnRepoRootZeroByte`
  - observed `ref=DL-028(worktree_discipline)`
  - observed exit code `4`
  - removed probe file
- Post-cleanup root zero-byte check: `zero_byte_root_files=0`

## Commits

- `10e8a64f` — `infra: remove root zero-byte artifacts and add untracked-file commit guard`
- `4f8081ce` — `infra: enforce repo-root zero-byte guard with DL-028 reference`

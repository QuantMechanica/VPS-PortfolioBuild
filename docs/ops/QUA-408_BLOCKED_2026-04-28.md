# QUA-408 Blocked — SRC04 Phase-2 Build (Card QUA-348)

Date: 2026-04-28
Issue: QUA-408
Scope: SRC04 phase-2 EA build from card QUA-348 (`SRC04_S09`, `lien-perfect-order`)

## Wake Acknowledgement

Wake assigned `QUA-408 SRC04 phase-2 build: card QUA-348` to Development. Next action is to implement the EA only when card approval and EA-ID allocation gates are present in the checkout.

## Blocking Evidence

1. Strategy card is not approved:
- Path: `strategy-seeds/cards/lien-perfect-order_card.md`
- Header currently declares:
  - `status: DRAFT`
  - `ea_id: TBD`

2. EA ID is not allocated for this strategy:
- Path: `framework/registry/ea_id_registry.csv`
- No row exists with `strategy_id = SRC04_S09`.

3. Required target EA path cannot be deterministically named yet:
- V5 naming rule requires `framework/EAs/QM5_<ea_id>_<slug>/QM5_<ea_id>_<slug>.mq5`.
- Without allocated `ea_id`, implementation would violate framework hard rules.

## Blocked State

`QUA-408` is blocked for Development implementation in this heartbeat.

## Unblock Owner + Action

- Owner: CEO + CTO
- Required action:
1. Approve card `SRC04_S09` (QUA-348) and set card status to APPROVED.
2. Allocate `ea_id` for `slug=lien-perfect-order`, `strategy_id=SRC04_S09` in `framework/registry/ea_id_registry.csv`.
3. Re-dispatch Development after (1) and (2) are present in this checkout.

## Immediate Next Action After Unblock

Implement `QM5_<ea_id>_lien_perfect_order.mq5` under `framework/EAs/QM5_<ea_id>_lien_perfect_order/` with V5 module functions (`Strategy_EntrySignal`, `Strategy_ManageOpenPosition`, `Strategy_ExitSignal`), card section/page citation comments, framework-managed risk/news/friday-close wiring, clean compile, then CTO review handoff.

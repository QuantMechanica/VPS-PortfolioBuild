# Claude Orchestration Cycle — 2026-05-23

**Cycle time:** 2026-05-23T14:45Z  
**Worktree:** agents/claude-orchestration-3 (21 commits behind origin/main, 5 ahead)

---

## Status: IDLE — no Claude tasks assigned

The deterministic router had 0 tasks for `claude`. All 5 active/queued
agent_tasks are `research_strategy` / `dropbox-video-extraction` (skill:
`video-analysis`) — Gemini-only capability. Gemini is at max capacity (2/2 IN_PROGRESS).
3 tasks stuck in TODO awaiting Gemini bandwidth.

---

## Health

| Check | Status | Detail |
|---|---|---|
| mt5_worker_saturation | **FAIL** | 0/10 terminal_worker daemons alive |
| p_pass_stagnation | **FAIL** | 0 Q03+ PASS verdicts in last 12h |
| All others | OK | — |

**mt5_worker_saturation** — expected. Factory runs in OWNER's RDP visible session;
workers only start after OWNER clicks Factory ON post-login. No automated workaround;
no action by this cycle.

**p_pass_stagnation** — consequence of no MT5 workers. Zero work_items in queue
(work_items table is completely empty). No EAs actively backtesting.

---

## CRITICAL BLOCKER: Schema fix not on origin/main

**Impact:** 2129 approved strategy cards are ALL blocked — `ready_approved_cards = 0`.
No cards → research replenishment frozen → no new build or backtest work can be
generated by the router.

**Root cause:** Commit `357f93bf` (CR1: STRATEGY_CARD_REQUIRED_BODY_PATTERNS relaxed
from 10 patterns to 5, fixing 1/30 → 30/30 card pass rate) is on branch
`agents/board-advisor` but **NOT merged to origin/main**. The farmctl/router code
on main still enforces the strict 10-pattern validator.

**Branch state:** `agents/board-advisor` is **35 commits ahead of origin/main**.
Contents (newest → oldest):

| Commit | Description |
|---|---|
| c23d2780 | research(G0): strategy cards QM5_11473-11474 (Carter H1) |
| b3353d03 | research(G0): strategy cards QM5_11468-11472 (Naked Forex/Wiley) |
| 8ce387b4 | research(G0): strategy cards QM5_11461-11467 |
| d9702534 | research(G0): 27 strategy cards QM5_11434-11460 |
| 16937827 | ops(registry): EA IDs 11429-11433 |
| e0b5915c | ui(cockpit): pipeline-progress section |
| **357f93bf** | **feat: Qxx phase routing + relaxed card validator + ea_view helper** |
| 133c44f6 | feat(gates): Q-pipeline runners + Davey suite (GW0..GW5 + OR1..OR2) |
| 8e597ca1 | feat(framework): EA framework alignment pass (FW1..FW10) |
| 6b579211 | spec+gov(pipeline): 14-phase rewrite proposal + DL-063 |
| … (25 more) | … |

**Required action (OWNER):** Review and merge `agents/board-advisor` → main.
After merge:
1. Re-run `python tools/strategy_farm/agent_router.py run --min-ready-strategy-cards 5 --max-routes 5`
2. Expect ~931 cards to become ready and build tasks to be generated for Codex.
3. Click Factory ON when in RDP to start MT5 workers and consume the queue.

---

## QM5_10260 Queue State

No work_items records exist for QM5_10260 in the DB. The Q02 TIMEOUT issue
(cieslak-fomc-cycle-idx hangs 1800s across all 37 symbols) has not been re-enqueued
after the prior timeout diagnosis. This is a standing blocker on that EA — no change
from prior cycle.

Reference: `docs/ops/QM5_10260_M15_SETFILES_REQUEUE_2026-05-22.md`

---

## Router Unroutable Tasks

3 TODO `dropbox-video-extraction` tasks (Setup 3/4 from EA-FTMO course) cannot route
because Gemini is at capacity. They will self-resolve when Gemini's current 2 tasks
complete. No action needed.

Task IDs:
- `6672fa16` — Setup 3 (20 MA)
- `9abf0338` — Setup 4 (Fibs Break Out)
- `aac25e1f` — (third EA-FTMO video)

---

## Recommended Next Steps (Priority Order)

1. **[OWNER] Merge `agents/board-advisor` → main** — unblocks 931 cards and
   all factory output.
2. **[OWNER] Click Factory ON** after RDP login to bring MT5 workers live and
   consume the backtest queue once build tasks populate it.
3. **[Standing] QM5_10260 perf rework** — requires code fix before re-enqueue
   (cieslak-fomc-cycle-idx per-tick computation too slow for Q02 real-tick).

# QUA-305 Blocked Snapshot (Development)

Date: 2026-04-28  
Issue: QUA-305 P1 — Development build EA from APPROVED card `davey-es-breakout`

## Why blocked

1. **`ea_id` not allocated (hard-rule gate).**  
   Source: `C:/QM/worktrees/cto/framework/registry/ea_id_registry.csv`  
   Current rows include:
   - `1001,breakout-atr,...`
   - `1002,davey-eu-night,...`
   
   No row exists for slug `davey-es-breakout` / strategy `SRC01_S04`.  
   Per V5 hard rule, no EA code is written before CEO+CTO allocate `ea_id`.

2. **Assigned Development checkout is not synced to active V5 EA tree.**  
   Runtime checkout path:
   - `C:/QM/paperclip/data/instances/default/projects/03d4dcc8-4cea-4133-9f68-90c0d99628fb/71b6d994-70ba-4a28-bd62-732b42a9ea58/_default`
   
   Observed:
   - path is effectively empty for framework work
   - no `framework/EAs` path
   - no git checkout attached to the working directory

## Unblock owner and exact action

- **Owner: CTO + CEO**  
  Action: allocate next `ea_id` for `davey-es-breakout` (`strategy_id: SRC01_S04`) in `framework/registry/ea_id_registry.csv`.

- **Owner: CTO / Harness**  
  Action: sync the Development issue checkout to the active repo/worktree that contains `framework/EAs` and V5 framework files.

## Next action after unblock

Implement `framework/EAs/QM5_<ea_id>_davey-es-breakout/QM5_<ea_id>_davey-es-breakout.mq5` from approved card `SRC01_S04` with:
- V5 framework include `#include <QM/QM_Common.mqh>`
- required input groups
- `Strategy_EntrySignal`, `Strategy_ManageOpenPosition`, `Strategy_ExitSignal` modular functions
- inline card citations (section/page refs)
- compile-check handoff package for CTO review.


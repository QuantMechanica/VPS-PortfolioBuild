# QUA-662 one-off recovery action (2026-05-05T18:55+02:00)

## Trigger

Prior snapshot showed flat progress:
- `report.csv` stuck at 3 lines since `18:42:03`
- T1/T2/T3/T5 runner logs unchanged since `18:40:55`
- Execution contract next step: targeted one-off recovery on most-stalled terminal.

## Action taken (single-terminal only)

1. Stopped stalled T1 runner process from matrix manifest:
- `terminal=T1`, `pid=62984` -> stopped

2. Re-ran one symbol on T1 through canonical P2 runner:
- Command:
  - `python framework/scripts/p2_baseline.py --ea QM5_1003 --symbols AUDCAD.DWX --terminal T1 --year 2024 --runs 2 --out-prefix D:\QM\reports\pipeline --timeout 1800`
- Outcome:
  - `[FAIL] AUDCAD.DWX (T1) ... reason=run_smoke_fail:REPORT_MISSING;METATESTER_HUNG;INCOMPLETE_RUNS`

## Evidence produced

- New row appended to:
  - `D:\QM\reports\pipeline\QM5_1003\P2\report.csv`
- New summary artifact:
  - `D:\QM\reports\pipeline\QM5_1003\P2\QM5_1003\20260505_165504\summary.json`
- Report row count changed:
  - from `3` -> `4` lines total.

## Current runner state

- `T1` runner pid now missing (intentionally stopped for recovery).
- `T2/T3/T4/T5` runner pids still alive per `p2_matrix_runners.json` snapshot.

## Next action

- Continue one-off recovery lane-by-lane only (no parallel restart):
1. Inspect `T2` runner progress delta.
2. If still stalled, recover one symbol on `T2` with the same method.
3. Keep appending evidence rows until matrix reaches 36 data rows or hard root-cause requires CTO/DevOps intervention.

---
source_id: AI-CODEX-XAUXAG-MKLOTZ-SCALE-RV-20260901
record_type: governed_ai_origin_prompt
created: 2026-09-01
created_by: Research+Development
---

# Origin Prompt

Construct exactly one pre-result, structural, low-frequency commodity sleeve
for the OWNER's 2026-09-01 portfolio-diversity mission. Use the registered
`XAUUSD.DWX` and `XAGUSD.DWX` D1 carriers as one opposed-leg logical basket.

The hypothesis must be materially different from existing gold/silver ratio
z-score, channel, OLS, CUSUM, Anderson-Darling, Kuiper, Brown-Forsythe, and
raw-rank builds. Use thirteen synchronized completed broker-month endpoints,
twelve adjacent log gold-minus-silver ratio changes, and fixed old/recent
blocks of six. Remove each block's own arithmetic mean, pool and strictly rank
the twelve centered residuals, and use the fixed `N=12` Klotz squared-normal
rank scores `Phi^-1(rank/13)^2`. Qualify only the inclusive upper half of the
frozen-score label distribution, verified by all `C(12,6)=924` assignments.
Fade the nonzero recent-minus-old raw block-mean shift for one broker month.

Lock one aggregate `RISK_FIXED=1000`, `RISK_PERCENT=0` package budget, equal
target notionals, per-leg frozen `3.5*ATR(20,D1)` hard stops, paired-order
atomicity, no same-month retry, and next-month/forty-day exit. Use no external
runtime data, trained output, prohibited signal indicator, optimization,
grid, martingale, scale-in, live preset, portfolio-gate change, deploy
manifest, `T_Live`, or AutoTrading action. Treat the statistic boundary as an
activity gate, not a significance or performance claim, and require Q02 to
falsify activity and economics without changing the locked rule.
